---
const features = [
  {
    title: 'Cross-device session sync',
    body: 'Continue the same agent thread on another machine with the conversation, not a cold re-prompt.',
  },
  {
    title: 'Multi-agent adapters',
    body: 'Claude Code and Codex first; Gemini CLI, OpenCode, and Grok Build on the roadmap. Not locked to one vendor.',
  },
  {
    title: 'End-to-end encryption',
    body: 'age with passphrase-derived keys. Reinstate uploads encrypted payloads and does not store the passphrase.',
  },
  {
    title: 'Bring-your-own storage',
    body: 'Cloudflare R2, AWS S3, or another compatible S3 service. You own the bucket and credentials.',
  },
  {
    title: 'OS-aware path remapping',
    body: 'Windows ↔ macOS project roots rewritten so resume actually finds sessions. The hard problem is the product.',
  },
  {
    title: 'Sessions first',
    body: 'Phase 1 syncs supported same-vendor sessions. Portable non-secret agent configuration is later roadmap work.',
  },
];
---

<section class="section features" id="features" aria-labelledby="features-title">
  <div class="shell">
    <h2 id="features-title" class="display">Built for the empty quadrant</h2>
    <p class="lede">
      Claude Code and Codex continuity × cross-device × stored sync × encrypted × path-aware.
      Naive file copies can preserve wrong paths or include sensitive artifacts.
    </p>

    <div class="grid">
      {
        features.map((f) => (
          <article>
            <h3>{f.title}</h3>
            <p>{f.body}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .display {
    margin: 0;
    max-width: 14ch;
    font-size: clamp(1.75rem, 3vw, 2.75rem);
  }
  .lede {
    margin: 1.1rem 0 0;
    max-width: 58ch;
    color: var(--ink-2);
    font-size: 1.02rem;
    line-height: 1.6;
    text-wrap: pretty;
  }
  .grid {
    margin-top: 2.5rem;
    display: grid;
    gap: 0;
    border-top: 1px solid var(--rule);
  }
  @media (min-width: 720px) {
    .grid {
      grid-template-columns: 1fr 1fr;
    }
  }
  @media (min-width: 1024px) {
    .grid {
      grid-template-columns: 1fr 1fr 1fr;
    }
  }
  article {
    padding: 1.35rem 1.1rem 1.5rem 0;
    border-bottom: 1px solid var(--rule);
  }
  @media (min-width: 720px) {
    article {
      padding-right: 1.5rem;
    }
    article:nth-child(odd) {
      padding-right: 1.75rem;
      border-right: 1px solid var(--rule);
    }
  }
  @media (min-width: 1024px) {
    article:nth-child(odd) {
      border-right: none;
    }
    article:not(:nth-child(3n)) {
      border-right: 1px solid var(--rule);
      padding-right: 1.75rem;
    }
  }
  h3 {
    margin: 0;
    font-size: 1.05rem;
    font-weight: 400;
    letter-spacing: -0.02em;
  }
  p {
    margin: 0.55rem 0 0;
    color: var(--ink-2);
    font-size: 0.92rem;
    line-height: 1.55;
  }
</style>
