# v0.6.0-rc.8 tagged-artifact acceptance prompts

Use these prompts only after the signed `v0.6.0-rc.8` GitHub prerelease is
published and both live installer routes pin that exact tag. They specialize
the release-neutral
[`v0.6.0` native Windows acceptance contract](v0.6.0-windows-acceptance.md),
which itself composes the
[Phase 5 acceptance contract](phase-5-universal-agent-coverage-acceptance.md),
the [`v0.5.2` CLI experience contract](v0.5.2-cli-experience-acceptance.md),
and hosted ticket #16 into one Windows column, under the
[Windows-first waiver](../../RELEASING.md#v060-windows-first-waiver) in
[ADR 0005](../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md).

`v0.6.0-rc.8` is a corrective candidate. `v0.6.0-rc.7` was published and its
own tagged-artifact acceptance
([`docs/testing/results/2026-09-08-windows-v060rc7.md`](results/2026-09-08-windows-v060rc7.md))
ended device verdict `FAIL` — **214 PASS / 0 PARTIAL / 1 FAIL / 0 NOT
TESTED** of **215** required rows. That candidate's own targeted fix (the
widened Codex CLI range) was fully confirmed on real, completed
conversational turns, and every row carried from `v0.6.0-rc.6`
(`MatrixG:G1`'s Codex half, `codex:E1`–`E3`, `grok:E1`–`E3`, `MatrixH:H7`)
cleared to `PASS`. One new, genuinely blocking regression surfaced, not
present at `v0.6.0-rc.6`:

- **CLI row 13** (`FAIL`, new finding). The interactive switcher's default
  `ScopeAll` scope resolved at least one visible session row's readiness
  glyph to the wrong value (`○ Blocked` standing in for `●`/`◐`) in 11 of
  15 independent launches (73%), using the exact `v0.6.0-rc.4`/
  `rc.6`-documented method, non-self-correcting within a launch's lifetime
  once wrong. `ScopeProject` was unaffected. This candidate's own dispatch
  had expected row 13 to `PASS` again unchanged (nothing in `v0.6.0-rc.7`
  touched the switcher or the readiness prober), so it was scored as a
  genuinely new regression. Source-level pointer:
  `internal/tui/readiness/prober.go`'s own doc comment on
  `maxConcurrentProbes` already named this exact process-creation-stampede
  failure mode, not fully eliminated by that cap alone under a
  multi-executor host's contention.

Publication means ready for physical acceptance; it does not mean acceptance
passed. Current stable remains `v0.5.1`. This candidate does not authorize
stable `v0.6.0`.

One mandatory device run starts this candidate's tagged-artifact acceptance:
native Windows x64 (`windows/amd64`, never WSL). Apple Silicon macOS
acceptance is **deferred** under ADR 0005 (see the "Deferred" section below)
— nothing in this candidate's evidence claims macOS.

The device must report all rows: **178** generated by
`rein doctor --agents --acceptance-matrix --json` on the tagged binary,
built from this tree (section B, unchanged from `v0.6.0-rc.7` — this
candidate widens no range and promotes no tier), **22** interactive CLI rows
(section C), and **16** Hop parity journey rows (section D) — **216** rows
in total, of which `opencode:D4` is `N/A (definitional)` (see
[`v0.6.0-windows-acceptance.md`](v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict)):
**215** required rows. Record each count in the report header. A report whose
Phase 5 count disagrees with the candidate catalog is invalid. `PARTIAL` and
`NOT TESTED` do not pass a required row, except a `NOT TESTED (host
credential)` row for an optional agent under the same disposition rule.

## Fixed release identity and evidence boundary

- `TEST_TAG` is exactly `v0.6.0-rc.8`; `TEST_COMMIT` is its peeled literal
  40-character commit and must be an ancestor of `origin/main`. **`TEST_COMMIT`
  is filled in once the signed tag exists** — it is not yet known while this
  candidate is only a reviewed release commit on `release/v0.6.0-rc.8`.
- Require an annotated tag with a valid signature against
  `.github/allowed_signers` and a published non-draft prerelease tied to it.
- Install from the live bootstrap into a new directory after proving the
  bootstrap pins exactly `v0.6.0-rc.8`. Never replace a user binary.
- Require `rein` and `reinstate` to have identical bytes and to report that
  tag and full commit (`rein version --json`). Report both the tag and the
  full commit in the device report header.
- Rebuild the local session index. Do not replay a stale index without
  exercising the reader's own upgrade path.
- **Host environment.** Every shell that runs `rein`, the Hop lab, or a Go
  test must `unset REINSTATE_BACKEND REINSTATE_MEMORY_BACKEND_DIR` first;
  `rein` honours `REINSTATE_BACKEND` before it reads the profile, so a
  contaminated shell silently uses a local store and every Hop row reports
  nothing to the control plane. The report must state that every shell did
  this. Do **not** unset the host's `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, or
  `XDG_DATA_HOME`: they are its live agent homes, and a Claude Code run
  spawned without `CLAUDE_CONFIG_DIR` falls back to a stale `~/.claude` that
  cannot refresh its sign-in. Isolate a row by pointing the vendor's home
  variable at your own directory. This rule is unchanged from `v0.6.0-rc.7`.
- **Always invoke the tagged binary by its full path**, never bare `rein`
  or `reinstate` — a PATH-resolved binary can silently answer from an
  older installed release. Put the tagged binary's directory first on
  `PATH` only as a convenience for the maintainer's own console commands
  below, and confirm `rein version --json` reports the exact tag before
  relying on it. (CLI row 13's own method below launches the tagged
  binary's own `rein.exe`, by full path, with no subcommand or arguments —
  "bare" there means no CLI arguments, the interactive switcher's entry
  point, not a PATH-resolved lookup.)
- **ConPTY console rule.** Every row that needs a live, running vendor
  process (`E1`–`E3`-shaped rows included) and every CLI-experience row
  launches that process in a real Windows pseudo console via
  [`scripts/testing/conptydriver`](../../scripts/testing/conptydriver) or
  the maintainer's own interactive terminal — never headless, and never a
  bare, unattached process the harness cannot observe completing.
- No real transcripts, prompts, credentials, or private paths in the report.

## What this candidate specifically re-tests

**CLI row 13**, this candidate's own targeted fix, under the exact
`v0.6.0-rc.7` report's own method:

1. Launch the tagged binary's own `rein.exe`, by full path, bare (no
   subcommand or arguments), from a cwd **outside any tracked project and
   outside any Git checkout** — the switcher's documented default
   `ScopeAll`.
2. Wait well past settle before reading the screen.
3. Cross-check **every** rendered row's readiness glyph (`● Ready`,
   `◐ Warn`, `○ Blocked`) against ground truth from
   `rein resume <id> --dry-run --json` / `rein inspect <id> --json` on the
   same fixture sessions the `v0.6.0-rc.7` report used.
4. Repeat this **at least 15 independent launches**. **Every one of the 15
   (or more) must fully match ground truth** — a single wrong row on a
   single launch is `FAIL`, not `PARTIAL`; this is a tighter bar than
   `v0.6.0-rc.7`'s own 15-launch sample (which found 11 of 15 wrong) because
   this candidate's whole purpose is eliminating exactly that
   non-determinism, not merely reducing its rate.
5. Also repeat from inside a tracked project workspace (`ScopeProject`) at
   least once, confirming it is unaffected, matching `v0.6.0-rc.4`/`rc.6`/
   `rc.7`'s own evidence.

**Plus one new check this candidate's second fix (`e7de157e`) specifically
targets: a genuinely out-of-range agent must still settle on `Blocked`,
not loop "still checking" forever.** Launch the tagged binary's own
`rein.exe`, bare, against a `scripts/tuisandbox` home generated with the
`-stale-claude` flag (an out-of-range Claude shim, `2.1.300`, first on
`PATH`, so every Claude row blocks on `agent.version` with exit `5` — see
[`v0.5.2-cli-experience-acceptance.md`](v0.5.2-cli-experience-acceptance.md#matrix)).
Wait well past the background prober's own retry budget (at least
`readiness.ProbeTimeout` × `maxProbeRetries`, generously multiplied) and
confirm every Claude row has settled on `○ Blocked`, not `◌` (still
checking), and that `rein inspect` on one of them names the actual
`agent.version` repair message, not a timeout-shaped one. A row still
showing `◌` at that point is `FAIL` — the deterministic-failure signal
(`e7de157e`'s `TimedOut` flag and `exitcode.Compatibility`) not doing its
job.

Both checks together are what `CLI row 13` is scored `PASS` or `FAIL` on
this run: the 15-launch (or more) all-match requirement, the `ScopeProject`
check, and the `-stale-claude` settle check. A `FAIL` on any of the three
is a new finding, not an expected outcome — this candidate's own dispatch
expects all three to pass, since fixing exactly this is its whole purpose.

**Everything else `v0.6.0-rc.7`'s tagged run left passing is unchanged —
nothing else in this candidate touches it:**

- **`opencode:D4`** — remains `N/A (definitional)`: OpenCode's SQLite-only
  store has no JSONL record boundary the row's mechanism can apply to,
  regardless of any fix. Record the same reason; excluded from the required
  count.

**Every row that cleared at `v0.6.0-rc.7` and is not `CLI row 13` is
expected `PASS` again, unchanged, including the rows `v0.6.0-rc.7` itself
carried forward and confirmed:** `MatrixG:G1` (both halves), `codex:E1`,
`codex:E2`, `codex:E3` (all against the real, installed `0.153.4` binary,
no account usage-limit constraint expected this run — nothing in this
candidate touches Codex), `grok:E1`, `grok:E2`, `grok:E3` (against the real
`1.0.13` binary — nothing in this candidate touches Grok), `MatrixH:H7`
(under the same refined per-file-listing rule below — nothing in this
candidate touches the daemon's own mechanism), `MatrixB:B4`, `MatrixB:B7`,
`MatrixG:G4`, `opencode:E5`, `opencode:E6`, `pi:C3`, every `qwen` row
`C1`–`E6`, `codex:E4`, `codex:E5`, `codex:E6`,
`opencode:C1`–`C6`/`D1`–`D3`/`E1`–`E4`. A `FAIL` on any of those is a new
finding, not a carried disposition.

**Every Hop parity row (section D, including `H7`) and every other CLI
experience row (section C) passed at `v0.6.0-rc.7` and nothing in this
candidate touches Hop, the daemon's own mechanism, or any interactive
surface other than the all-projects readiness path.** Run the full 22 + 16
rows anyway — this is the first time they run against a `v0.6.0-rc.8`
tagged artifact — but a regression in any of them is a new finding, not an
expected one.

**The rest of the 178-row Phase 5 matrix, in full**, exactly as the rc.7
dispatch required, including the widened Claude Code (`2.1.219`–`2.1.263`),
Codex CLI (`0.133.0`–`0.153.4`), Grok Build (`1.0.5`–`1.0.13`), OpenCode
(`1.18.21`–`1.18.29`), and Qwen Code (`0.21.12`–`0.23.0`) ranges, none of
which moves in this candidate; re-check the Claude Code ceiling immediately
before this run — it auto-updates.

**The host contamination rule**, above — confirm `REINSTATE_BACKEND` and
`REINSTATE_MEMORY_BACKEND_DIR` are unset in the shell that runs each row,
not only in the report's preamble.

## Lab isolation for `MatrixH:H7`

Unchanged from `v0.6.0-rc.7`. The go-signal file,
`D:/ReinstateAcceptanceProjects/h7-go.txt`, was already present at the start
of the `v0.6.0-rc.7` recheck (§24 of that report) — check for it **first**,
before starting any other row: if it already exists and its content
confirms the maintainer is present, run `H7` first, ahead of the rest of
the executor's part, rather than deferring it to the end. If it is not yet
present, fall back to the standard order (every other row in the part
first) and poll for it every **60 seconds**, for **up to 150 minutes**,
exactly as before.

- **"Fresh Hop lab account"** means a new **Hop** account created through
  the lab's `hopd` (`hoplab pair init`, or the `rein account init` flow
  against the lab control plane) that has had **zero sessions pushed to
  it** — not a new Windows OS user account. Unchanged from `v0.6.0-rc.7`.
- **(a) Full per-file listing, before and after.** Before the round trip,
  and again after it, list every file (relative path, size, mtime) under
  exactly the **session-bearing subtrees**, not the whole agent-home
  roots: `<CLAUDE_CONFIG_DIR>/projects`, `<CODEX_HOME>/sessions`, and
  `<XDG_DATA_HOME>/opencode`, excluding `*-wal`, `*-shm`, and `*.lock`
  files. Read `CODEX_HOME` as the **persistent, user-level** value the
  scheduled task will actually see (a `daemon status` call's reported
  `roots`, or the registry) — never the executing shell's own transient
  `CODEX_HOME`.
- **(b) `PASS` condition.** The row is `PASS` only if all three hold: the
  daemon's pull restored **zero** snapshots; **no file** under those three
  subtrees was created or modified carrying a lab/synthetic name or a name
  matching the lab account's own session ids; and **every** differing
  entry between the before- and after-listings is attributable, by
  relative-path pattern, to a process other than `rein` (backup rotation,
  another session's own transcript or `file-history` writes, WAL/journal
  churn, and the like). Any entry that cannot be so attributed keeps the
  row at `PARTIAL` or worse — the burden is on the attribution, not on
  digest equality.
- **(c) Reporting.** The report lists every differing entry between the
  before- and after-listings by relative path, with its attribution, even
  when the overall disposition is `PASS` — a clean row still shows its
  work.
- **(d) `#424` itself.** The daemon reading the live roots and pushing
  into the run's disposable locker is **recorded, not failed**, on its
  own: `#424` is a documented design gap, scheduled to be fixed separately
  for `v0.6.1`, not a fresh finding each time `H7` runs until it lands.
- `H7` still needs the maintainer at the keyboard for the one UAC prompt
  the elevation raises — this refinement does not remove that requirement
  and must not attempt to route around it (see "Harness traps" below).
  A shorter poll window than 150 minutes is **not** a basis for `PARTIAL`
  — if the wait exhausts the full window without the file appearing, that
  is the only condition under which `PARTIAL` (operator/harness
  availability) is the correct disposition.

This lab-isolation method is a harness clarification only; it does not
change `rein daemon install`'s own behavior (that is `#424`'s own fix,
scheduled for `v0.6.1`, not attempted by this candidate) and does not
change what `MatrixH:H7`'s row scores on beyond the refined pass condition
itself.

## Dispositions carried from the `v0.6.0-rc.7` tagged run

The `v0.6.0-rc.7` tagged run
([full report](results/2026-09-08-windows-v060rc7.md), Verdict block, §13,
§20, §21, §24) carried one non-`PASS` required row. Record it as `PASS`, or
as the same disposition with the same reason; a new reason is a new
finding.

| Row | `v0.6.0-rc.7` tagged result | This candidate expects |
| --- | ---------------------------- | ----------------------- |
| CLI row `13` | FAIL, new finding — the all-projects switcher rendered a `Ready` or `Warn` session as `Blocked` in 11 of 15 independent launches, non-self-correcting once wrong | Fixed via `eb0a9b39`/`e7de157e` — expect `PASS` on all 15+ independent `ScopeAll` launches matching ground truth, `ScopeProject` unaffected, and the `-stale-claude` settle check above |

**Also carried, cleared at `v0.6.0-rc.7`, expected `PASS` again (not a
disposition, but named here so a report does not mistake a first-time
re-run against this tag for a fresh finding):** `MatrixG:G1` (both halves),
`codex:E1`, `codex:E2`, `codex:E3` (real, completed conversational turns
against `0.153.4`, no account usage-limit constraint expected), `grok:E1`,
`grok:E2`, `grok:E3` (against `1.0.13`, most recently cleared via the
executor-driven path), `MatrixH:H7` (refined per-file-listing rule, above),
`opencode:D4` (`N/A (definitional)`, unchanged since `v0.6.0-rc.1`, excluded
from the required count).

A row not on either list that fails is a new finding, not a carried
disposition.

## Census the candidate must hold

Unchanged from `v0.6.0-rc.7` — this candidate promotes no agent's tier.

| Tier | Keys |
| ---- | ---- |
| T5 | `claude`, `codex`, `opencode` |
| T4 | `grok`, `qwen` |
| T2 | `gemini`, `kimi` |
| T1 | `pi`, `cursor`, `copilot`, `cline` |
| T0 | `aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode` |

Do not promote Gemini to T3 on this candidate. Do not invent a reader for a
T0 agent. If a T1 agent is not installed, that is a scope reduction: drop
that agent to the highest tier this platform verified, in the candidate,
before any later stable. Tiers are never claimed on macOS evidence this
candidate does not have.

## What to run

1. **Section A** automated gates (A1–A8) on the tagged installed binary.
2. **Section B** — the Phase 5 generated matrix, all **178** rows, for every
   installed key per the Phase 5 contract.
3. **Section C** — the 22-row CLI matrix, Windows column, through the
   ConPTY driver (`scripts/testing/conptydriver`) against
   `scripts/tuisandbox`. Rows 2 and 22 compared byte-for-byte against a
   `v0.5.1` release binary. Row 21 (ASCII glyph fallback on legacy conhost)
   is required. **Row 13** gets the expanded, at-least-15-launch method
   above, plus the `-stale-claude` settle check.
4. **Section D** — all 16 Hop parity rows against the local lab, two
   isolated device homes on this one host, real vendor binaries for every
   session a row requires. Check for the `H7` go-signal file first; if
   already present, run `H7` first. Otherwise run every other row first and
   poll for up to 150 minutes.
5. **`codex:E1`–`E3`** and **`grok:E1`–`E3`** — attempt against the real
   installed `0.153.4` and `1.0.13` binaries respectively, through
   `conptydriver`, exactly as the Phase 5 and Matrix G/E contracts specify.

A structured handoff starts a **new** destination session. It is not a
cross-agent resume. Report it that way.

## Reading a refusal correctly

Several refusals in this product are correct behaviour and must not be scored
as defects. A row fails only when the refusal is wrong, not when it fires.

- `working directory is a different repository than the source session` — run
  from the session's own workspace.
- `the recorded Git branch cannot be verified outside a Git repository` — the
  chosen session's workspace is not a repository; choose one that is.
- `source session is active; close it or pass --allow-active` — the operator is
  running that agent. For an embedded store, any write to the database counts.
- `native agent version X is outside the verified range` — the host's vendor CLI
  auto-updated past the ceiling, or (for the `-stale-claude` settle check
  above) the fixture deliberately puts an out-of-range shim first on `PATH`.
  Pin the verified version or record real drift as
  `NOT TESTED (version drift)` per
  [`v0.6.0-windows-acceptance.md`](v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict).
  The `-stale-claude` case itself is not a version-drift disposition — it is
  a deliberate fixture proving the row's own settle behaviour, and the
  refusal message is expected, correct behaviour to observe, not a row to
  excuse.
- `could not reach the Reinstate Hop control plane at <url>: <cause>` (H1c) —
  correct when the control plane genuinely does not resolve; the row passes on
  the message shape and the `control_plane_unreachable` `--json` kind, not on
  the control plane being reachable.

## Harness traps that produce a wrong verdict

Physical acceptance on this program has repeatedly found more harness defects
than product defects, and some have been **false passes**. Check the harness
before concluding the product is wrong.

- Read each agent's `root_env` from `rein doctor --agents --json` rather than
  assuming a variable name. Redirecting with a variable the product does not
  declare tests the operator's real root, not the copy.
- Put the candidate on `PATH` under **both** `rein` and `reinstate`, or an
  installed release answers instead and the comparison runs between two
  builds. Always invoke by full path where the contract requires it, above.
- A `--dry-run` capsule is a preview and carries no `raw_source` boundary.
  Only a harness that truncates a copied source can measure the fidelity rows
  that need it.
- Assert that a refusal **names a range**, not that it names a particular
  version. Hardcoded bounds stop testing the message the moment a ceiling
  moves.
- A Hop row that passes without exercising its mechanism is `FAIL`. Check the
  harness (the lab's env, the isolated home, `hopd.db`) before trusting a
  green result.
- Attribute a write before blaming it on a refresh. Any agent the operator is
  running writes to its own root continuously, and the audit may itself be
  running inside one.
- **CLI row 13 specifically:** a single wrong row on a single launch out of
  15 is a `FAIL`, not a rounding error and not `PARTIAL` — do not average
  across launches or report only the best run. Wait well past settle before
  reading the screen on every launch (10s is not always enough; the
  `v0.6.0-rc.7` report used checkpoints up to 47s) and do not read a launch
  mid-settle and call it wrong when it would have self-corrected — but once
  settled, a wrong glyph is wrong for that launch's whole lifetime, not a
  transient to wait out. For the `-stale-claude` settle check, do not stop
  watching the moment the row first shows anything other than `●`/`◐` —
  confirm it lands on `○ Blocked` and stays there, not `◌` cycling forever;
  a check that gives up early cannot tell the two apart, which is exactly
  what this candidate fixes.
- `MatrixH:H7` specifically: "fresh Hop lab account" means a new **Hop**
  account with zero pushed sessions, not a new Windows OS user account —
  do not abandon the row over a missing Windows account elevation the rule
  does not actually require. Do not attempt an unattended elevation
  workaround (a scheduled task that self-elevates, a stored credential,
  etc.) to route around the missing operator — the row's mechanism is the
  real UAC prompt, and routing around it is a false pass. Score the row
  against the refined per-file listing with attribution, not aggregate
  digest equality. Check for the go-signal file first, before any other
  row, rather than assuming it is absent.

## Required installs

Claude Code, Codex CLI, OpenCode, Grok Build, and Qwen Code at the versions
this candidate verifies (re-check immediately before the run; Claude Code
auto-updates). Optional, never installed solely for a row: Gemini CLI, Kimi
Code CLI, Copilot CLI, Cursor CLI, Cline, Pi.

## Reports

- `docs/testing/results/<DATE>-windows-v060rc8-pretag.md` (pre-tag snapshot
  build; evidence that the candidate is ready for tagged-artifact acceptance,
  authorizes nothing past that)
- `docs/testing/results/<DATE>-windows-v060rc8.md` (tagged artifact; this is
  the run that can authorize stable `v0.6.0`)
- `docs/testing/results/<DATE>-windows-hop-parity-v060.md` (section D; may be
  shared by the pre-tag and tagged runs if re-run against the tagged binary
  and both identities are recorded)

Each report is committed on its own branch. `PARTIAL` and `NOT TESTED` do not
pass a required row; a missing required result is `FAIL`. Cumulative and
append-only: a `FAIL` that later passes keeps both entries and says what
changed.

Stable `v0.6.0` is authorized only when the tagged Windows report is `PASS`
with zero release-blocking findings, section E is recorded exactly as
`DEFERRED` (never as passed), and the stable promotion decision in
`RELEASING.md` names the deferred-macOS issue,
[#403](https://github.com/HarjjotSinghh/reinstate/issues/403).

## Deferred: Apple Silicon macOS

Every row below is **deferred**, not claimed, under
[ADR 0005](../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md) D2. They
are run against this same `v0.6.0-rc.8` tag (or the stable tag it becomes)
once the Apple Silicon macOS host returns from repair. If that later macOS
run fails, the fix ships as `v0.6.1` — the record of this candidate's Windows
acceptance is never edited to hide it.

| Group | Rows | Contract |
| ----- | ---- | -------- |
| Phase 5 generated matrix, macOS column | all rows section B lists | Phase 5 |
| CLI experience, macOS column | rows 1–20 and 22 | `v0.5.2` |
| Hop cross-device journeys | pairing macOS↔Windows; path remap Windows↔macOS both directions; daemon launchd round trip; first push on macOS | hosted #16 |
| Range widening on macOS | Claude Code through `2.1.263`; Codex CLI through `0.153.4`; OpenCode through `1.18.29`; Qwen Code through `0.23.0`; Grok Build through `1.0.13` | ADR 0005 D3 |
| Grok GD8 | tool-approval prompt (already collected on macOS; the Windows half is #368 and stays optional) | Grok T4 |

No document, data file, or release note may state that `v0.6.0` was verified
on macOS until that run exists.
