# v0.4.0-rc.1 tagged-artifact acceptance prompts

Use these prompts only after the signed `v0.4.0-rc.1` GitHub prerelease is
published and both live installer routes pin that exact tag. They specialize
the release-neutral [Phase 4 acceptance contract](phase-4-cross-agent-handoff-acceptance.md)
and use the [Phase 4 report template](results/phase-4-report-template.md).

`v0.4.0-rc.1` is the first structured-handoff candidate. It carries canonical
capsules, five source readers, Claude Code and Codex destinations, the model-free
handoff pipeline, local private lineage, the full CLI surface, synthetic
fixtures, adversarial tests, and documentation. Publication means ready for
physical acceptance; it does not mean acceptance passed. Current stable remains
`v0.3.0`, and stable `v0.4.0` requires a later promotion decision and fresh
validation.

Two mandatory device runs start independently and may run in parallel:

1. Apple Silicon macOS (`darwin/arm64`); and
2. native Windows x64 (`windows/amd64`, never WSL).

Each device must report all 44 required A–D, F, and G rows. The eight E rows are
optional per installed vendor. Gemini CLI, OpenCode, and Grok Build must not be
installed solely for acceptance; their absent rows are `NOT TESTED`. A missing
required result is `FAIL`.

## Fixed release identity and evidence boundary

- `TEST_TAG` is exactly `v0.4.0-rc.1`; `TEST_COMMIT` is its peeled literal
  40-character commit and must be an ancestor of `origin/main`.
- Require an annotated tag with a valid SSH signature against
  `.github/allowed_signers` and a published non-draft prerelease tied to it.
- Before executing downloads, verify the release workflow's exact 25-asset set:
  `checksums.txt` plus five platform archives, five archive SBOMs, five raw
  binaries, eight Linux native packages, and one source archive. Verify every
  checksum, GitHub API digest, and attestation against the repository, tag,
  workflow, and full commit. Inspect safe archive membership without extraction.
- Prove the platform's live bootstrap is byte-identical to the file at
  `TEST_COMMIT`, contains one `v0.4.0-rc.1` pin, and pins the exact canonical
  installer digest. Install into a new directory; never replace a user binary.
- Require `rein` and `reinstate` to have identical bytes and to report version
  `0.4.0-rc.1` plus the literal full `TEST_COMMIT`. Stop before behavior rows on
  any provenance or identity failure. Source builds are supplemental only.

## Fixed isolation and controlled-session rules

1. Use new absolute paths for `REINSTATE_HOME`, the install, private evidence,
   disposable Git repositories, temp files, and every vendor process home.
   Never point a command at the operator's real Claude, Codex, Gemini, OpenCode,
   or Grok tree.
2. Create the required Claude Code and Codex sessions through each vendor's own
   documented CLI in a disposable repository. A human may authenticate the
   throwaway home before the run; credentials and auth contents stay invisible
   to the acceptance agent and report.
3. Fully close the source CLI before every flagship handoff. Handoff execution
   may launch the destination CLI, but Reinstate itself must not create or edit
   a vendor session-store file. Unit tests use fake runners and no network or
   real vendor process.
4. Keep all raw output, timings, hashes, capsule bodies, and terminal recordings
   under the private evidence root. Commit only counts, hashes, timings, exit
   codes, mode labels, and paraphrased destination acknowledgements.
5. Never run `rein init`, configure a backend, sync a capsule, inspect a
   keychain or `.env`, or transfer a real transcript between devices. Use only
   fresh controlled sessions and the tagged synthetic fixtures.

Before and after each relevant row, fingerprint the controlled source artifact
and vendor trees. A changed source hash, unexpected vendor write during a
launch-free row, access to an operator tree, or committed sensitive content is
a release blocker.

## Execution map for the Phase 4 matrices

Read the acceptance contract's pass condition for every ID; the instructions
below define how to collect it. Do not collapse IDs into one result.

### A — flagship quota-switch

- Create one benign task in each required source with a current goal, latest
  request, changed file, known test result, and next action. Include a harmless
  completed marker write so A6 can prove it is not repeated.
- For A1 and A2, close the source process, remove its auth variables from the
  test shell, run the installed `rein handoff` into the other agent from a real
  TTY, and confirm no source process or source API dependency appears. For A3,
  repeat Claude → Codex from a logged-out throwaway source environment.
- For A4, use a fresh controlled session whose vendor CLI is terminated during
  its final response so the vendor naturally leaves an incomplete final record;
  do not hand-edit its store. Confirm the last complete user intent survives.
- For A5–A6, allow only the destination's requested acknowledgement. Paraphrase
  its restatement in the report and prove the harmless source marker did not
  change. Do not authorize additional destination actions.
- For A7, record sanitized `handoff list`/`inspect` evidence: Claude's pinned
  destination ID resolves; Codex is truthfully `resolved`, `unresolved`, or
  `ambiguous`.

### B — fidelity and policy

- Run the tagged `TestHandoffExecutedOutputMatchesDryRunByteForByte` test, then
  compare installed-artifact `--dry-run` and `--no-launch` structured-plan
  stdout and stderr as separate byte sequences for the same frozen source and
  flags. The real-launch route is independently required by A1–A2. Any mismatch
  fails B1; never normalize paths, whitespace, or line endings for comparison.
- Use the tagged 200-turn fixture for B2 and G1. Run checkpoint, balanced, and
  full policies on frozen controlled sources. Record projection size, token
  estimate, truncation count, and sidecar-reference count without recording
  content.
- Inspect sanitized fidelity metadata for all five classes, attachment metadata,
  and unknown opaque records. Each class and every omission/reference needs its
  reason; never paste the capsule or projection into the report.

### C — workspace and capability truth

- Use disposable clean, dirty, moved, missing, and wrong repositories with
  frozen Git identities. Compare reported branch, HEAD, and dirty state to Git;
  create a controlled transcript/Git contradiction and require live Git to win.
- Exercise the tagged macOS and Windows path fixtures plus a real local
  canonical project mapping. Require `${REPO:<id>}` portability and scan private
  artifacts to prove no source-device absolute path survived.
- In test-owned vendor homes only, create then remove bounded dummy MCP, skill,
  and instruction declarations. Require missing-capability reports before
  launch and verify reports contain no values or file contents.
- Exercise one out-of-range source or destination version in a throwaway home.
  Require exit `5` and no artifact or lineage residue.

### D — security

- Run all tagged `internal/handoff` adversarial tests and `make fixture-scan`.
  Exercise the installed CLI against the tagged injection, delimiter,
  source-instruction, secret, and oversized fixtures through disposable roots.
- Privately search every produced artifact for planted synthetic secrets,
  source authority text, and unescaped delimiters. Record only zero/nonzero
  counts. `--show-redactions` may expose categories and counts only.
- Prove launch-free rows do not read credentials and do not change any vendor
  tree. For real destination launches, attribute new vendor files to the vendor
  process and require the source tree to remain byte-identical.
- Verify owner-only storage, capsule sync exclusion, source SHA-256 immutability,
  the Grok warning, forced Grok redaction, and `--no-redact` refusal with exit
  `2`. Grok evidence uses a tagged synthetic fixture when Grok is absent.

### E — optional source-only agents

If a vendor is already installed, create a fresh controlled source in its
throwaway process home and run both allowed destinations. Test rewinds,
OpenCode storage/fallback truth, forced Grok redaction/warning, and destination
refusals. If absent, record the applicable rows `NOT TESTED: vendor absent`.
Grok native continuation is refused for the documented read-only reason.

### F — CLI contract

- Exercise human and JSON output for `handoff`, `list`, `inspect`, `export`,
  `resume --with`, and picker `h NUMBER`. Require the mode label `structured
  handoff` and new-destination-session truth everywhere.
- Check `--json` gating; exact, missing, duplicate, wildcard, and stale warning
  acknowledgement; launch-free exact-command output; stored artifact byte
  reproduction; ambiguous native references; and non-TTY fail-closed behavior.
- Record only exit codes `0`, `1`, `2`, `3`, `5`, `6`, or `7`. Any other public
  code is a release blocker.

### G — fixed performance plan

Use a monotonic process-start-to-exit clock, bounded capture, a 30-second
per-sample timeout, one untimed warmup where specified, and nearest-rank p95.
Validate every exit and result. Keep raw samples private.

| ID | Exact corpus and command | Samples | macOS ceiling | Windows ceiling |
| -- | ------------------------ | ------- | ------------- | --------------- |
| G1 | `go test ./internal/handoff -run '^TestLongHistoryParseCapsuleProjectionUnderCeiling$' -count=1 -v`; tagged 200-turn/400-event fixture | 1 | `<2,000 ms`, projection `≤98,304 bytes` | same |
| G2 | Installed `rein handoff SOURCE --to DEST --policy balanced --dry-run --json` on the largest frozen controlled local session | 1 warmup + 5 timed | maximum `8s` | maximum `12s` |
| G3 | Installed `rein handoff list --limit 100 --json` with exactly 100 valid stored lineage rows | 1 warmup + 20 timed | p95 `2s` | p95 `4s` |

Prepare G3 in a dedicated fresh home by running the same safe, frozen controlled
source through installed `--no-launch --json` exactly 100 times. This creates
100 valid append-only lineage records without spawning a vendor or hand-editing
the store. Require `list --limit 100 --json` to return exactly 100 rows before
timing. Fingerprint the source before and after setup.

G1's 2-second and 98,304-byte limits are the shipped deterministic unit
contract. G2 uses the established cold local-command envelope; G3 uses the
established warm process-start envelope. The looser Windows limits preserve the
existing physical-host allowance without permitting a 20–30 second regression.
The development observation was `9.9 ms` and `46,541 bytes`; record it only as
non-certifying context, never as either device's result. A timeout, invalid
output, source mutation, unbounded growth, or more than 25 percent comparable
same-host p95 regression is blocking even inside an absolute ceiling.

## Prompt 1 — Claude Code on Apple Silicon macOS

```text
Certify the exact signed Reinstate v0.4.0-rc.1 tagged artifacts on native Apple
Silicon macOS. You own the macOS evidence and final reconciliation.

Read AGENTS.md, docs/handoff.md,
docs/testing/phase-4-cross-agent-handoff-acceptance.md,
docs/testing/v0.4.0-rc.1-agent-verification-prompts.md (this dispatch), and
docs/testing/results/phase-4-report-template.md completely before acting.

Set TEST_TAG exactly to v0.4.0-rc.1. Fetch tags without merging; verify the
annotated SSH-signed tag, peel its literal full TEST_COMMIT, require main
ancestry, verify the published prerelease's complete 25-asset provenance, and
install only through the live install.sh after proving its bytes and pin. Stop
before behavior rows on any chain-of-custody failure.

Use fresh isolated REINSTATE_HOME, INSTALL_DIR, evidence root, repositories,
CLAUDE_CONFIG_DIR, CODEX_HOME, and process homes. Never read the operator's real
vendor trees or credentials. Create fresh controlled Claude and Codex sessions
through their own CLIs. Fully close the source before every flagship row. Never
write a vendor-internal session file, execute transcript content, call the
source model on the handoff path, sync a capsule, or commit sensitive evidence.

Run the complete automated gates and all 44 required Phase 4 rows using this
dispatch's execution map. Run optional E rows only for already-installed
vendors. Execute G1–G3 exactly with the macOS ceilings. Run both flagship
directions from a real TTY, collect the destination acknowledgement only, and
leave further destination actions unauthorized. Missing required evidence is
FAIL, never NOT TESTED.

Create test/v0.4.0-rc.1-macos-arm64-report from TEST_COMMIT. Commit only
docs/testing/results/REPORT_DATE-macos-phase4-V040RC1.md using the template.
End it with exactly one PHASE4-DEVICE-REPORT-V1 block. Never amend or force-push
shared evidence; keep the report PR draft and unmerged. Do not edit product
files, merge, tag, release, deploy, or claim stable.

Pause after the immutable macOS report commit until the complete Windows report
commit is supplied. Then independently verify its ancestry, report-only diff,
count arithmetic, privacy, original failures, timings, and supersessions. Add
one PHASE4-RC1-FINAL-RECONCILIATION-V1 block in an additive macOS report commit.
Set stable_v0.4.0_authorized=false and current_stable=v0.3.0 regardless of the
candidate verdict.

Return the device verdict/counts, TEST_COMMIT, installed binary identity,
report branch/commit/draft PR, sanitized failed commands, G1–G3 aggregates,
optional-vendor disposition, and every release-blocking finding.
```

## Prompt 2 — Codex on native Windows x64

```text
Certify the exact signed Reinstate v0.4.0-rc.1 tagged artifacts on native
Windows x64 in a native 64-bit PowerShell process, never WSL. You independently
own the Windows evidence.

Read AGENTS.md, docs/handoff.md,
docs/testing/phase-4-cross-agent-handoff-acceptance.md,
docs/testing/v0.4.0-rc.1-agent-verification-prompts.md (this dispatch),
docs/testing/windows-acceptance-host.md, and
docs/testing/results/phase-4-report-template.md completely before acting.

Provision the host exactly as windows-acceptance-host.md requires before the
product matrix. Set TEST_TAG exactly to v0.4.0-rc.1. Fetch tags without merging;
verify the annotated SSH-signed tag, literal full TEST_COMMIT and main ancestry,
the published prerelease's complete 25-asset provenance, and the live
install.ps1 bytes, digest, and pin. Install into a new directory with
REINSTATE_BOOTSTRAP_PATH_SCOPE=Process. Stop before behavior rows on any
chain-of-custody or native-process failure.

Use fresh isolated REINSTATE_HOME, INSTALL_DIR, evidence root, native-Windows
repositories, CLAUDE_CONFIG_DIR, CODEX_HOME, and process homes. Never read the
operator's real vendor trees or credentials. Create fresh controlled Claude and
Codex sessions through their own CLIs. Fully close the source before every
flagship row. Never write a vendor-internal session file, execute transcript
content, call the source model on the handoff path, sync a capsule, or commit
sensitive evidence.

Run the complete automated gates with Go 1.25.12, including the PowerShell-native
snapshot, staging, artifact, and installer scripts. Run all 44 required Phase 4
rows using this dispatch's execution map and optional E rows only for
already-installed vendors. Execute G1–G3 exactly with the Windows ceilings and
ordinary antivirus protections enabled. Real TTY rows require Windows Terminal
or equivalent human evidence; missing required TTY evidence is FAIL.

Create test/v0.4.0-rc.1-windows-amd64-report from TEST_COMMIT. Commit only
docs/testing/results/REPORT_DATE-windows-phase4-V040RC1.md using the template.
End it with exactly one PHASE4-DEVICE-REPORT-V1 block. Never amend or force-push
shared evidence; keep the report PR draft and unmerged. Do not edit product
files, merge, tag, release, deploy, transfer a real corpus, or claim stable.
Return the immutable report commit to the macOS coordinator.

Return the device verdict/counts, TEST_COMMIT, installed binary identity,
report branch/commit/draft PR, sanitized failed commands, G1–G3 aggregates,
optional-vendor disposition, and every release-blocking finding.
```

## Handoff and reconciliation sequence

1. Publish the signed candidate and pin both live installers before either run.
2. Start both prompts independently at the same `TEST_COMMIT`; each agent owns
   only its report path and private local evidence.
3. Each report branch starts at `TEST_COMMIT` and receives additive,
   report-only commits. No amend, force-push, product edit, or corpus transfer.
4. Give the complete Windows report commit to the existing macOS coordinator.
5. The coordinator fetches only the report ref and independently checks
   ancestry, diff scope, counts, privacy, failures, supersessions, and timings.
6. `phase4_rc1_tagged_artifact_acceptance=PASS` requires both devices to have
   44 required PASS, zero required PARTIAL/FAIL/NOT TESTED, zero release
   blockers, valid report hygiene, and every §14 closeout assertion PASS.
7. Keep both report PRs draft and unmerged pending a maintainer decision. A
   candidate PASS does not authorize stable `v0.4.0`; current stable remains
   `v0.3.0`.
