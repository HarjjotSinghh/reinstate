# v0.6.0-rc.7 tagged-artifact acceptance — part A (gates, core matrices, T0/T1)

`PHASE5-DEVICE-REPORT-V1` (partial — this file covers only executor A's
assigned rows: section A automated gates; Phase 5 Matrix A, B, G, H; Matrix F
for the seven T0 agents; Matrix C for `cline`, `copilot`, `cursor`, `pi`.
Other matrices/agents are covered by sibling `-part-b`/`-part-c` reports from
this same tagged run.)

## Verdict (this part only)

- **Part-A verdict:** `PASS` — every row this part owns is `PASS`; zero
  release-blocking product defects found; one harness-level environment
  anomaly found and documented (worktree contamination, §16).
- **Milestone:** `MATRIX_COMPLETE` (for this part's assigned scope)
- **Matrix generated by:** `rein doctor --agents --acceptance-matrix --json`
- **Required counts (this part):** `100 PASS / 0 PARTIAL / 0 FAIL / 0 NOT TESTED`
  (see the full row table in §17 for the itemized list)
- **Release-blocking findings:** `0`

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-09-08T22:00:00Z`–`2026-09-08T23:35:00Z` (approx., this session) |
| Device | `windows-amd64` |
| OS/version/build | Windows 11 Pro, 10.0.26200, Build 26200, x64 |
| Filesystem | NTFS (name only) |
| Tested tag | `v0.6.0-rc.7` |
| Tested full commit | `21e9a9a356df1181601e2da02bfae03b28380cc3` |
| Installed binary SHA-256 (both install routes) | `ab73dd7c6817da2374a9c72673c62c63fb724d7412fa464d5dc56d381d5ea349` |
| Installed version JSON | `{"name":"reinstate","version":"0.6.0-rc.7","commit":"21e9a9a356df1181601e2da02bfae03b28380cc3","date":"2026-09-08T16:26:36Z"}` |
| Catalog agent count | `18` |
| Catalog tier census | `T0=7, T1=4, T2=2, T4=2, T5=3` |
| Generated required row count (`doctor --agents --acceptance-matrix`) | `178` (row_count), `core_row_count=33` |
| `sessions --help` `--agent` enum | `claude\|cline\|codex\|copilot\|cursor\|gemini\|grok\|kimi\|opencode\|pi\|qwen\|all` — 11 agent keys, matches |
| Git version | `2.52.0.windows.1` |
| Go version/toolchain | `go1.26.1` host toolchain / `go1.25.13` pinned by `Makefile` (`GOTOOLCHAIN`) |
| Worktree | `D:\Projects\reinstate-worktrees\v060-rc7-tagged`, branch `v060/rc7-tagged` @ `21e9a9a3` |
| Report branch | `v060/rc7-tagged` (per dispatch instructions; commit directly there) |

**Bootstrap deviation sentence (executor A only):** the live
`https://reinstate.dev/install.ps1` was downloaded fresh with
`Invoke-WebRequest -OutFile` into
`D:/ReinstateAcceptanceProjects/v060-rc7-a/bootstrap/`; its pinned-version
line reads exactly `$Version = "v0.6.0-rc.7"`; running it with
`INSTALL_DIR=D:/ReinstateAcceptanceProjects/v060-rc7-a/bootstrap/install`
printed `Installed reinstate v0.6.0-rc.7 -> ...\reinstate.exe` and
`Installed rein alias -> ...\rein.exe`, and both binaries are byte-identical
(SHA-256 above) to the ones extracted from the checksummed
`reinstate_0.6.0-rc.7_windows_amd64.zip` in the coordinator's verified
`rc7-draft` directory, which is the install this part's testing actually
used (`D:/ReinstateAcceptanceProjects/v060-rc7-a/install/`). No deviation:
the live bootstrap pins this exact candidate.

**Binary rule compliance:** every command in this part invoked the tagged
binary by its full path (`D:/ReinstateAcceptanceProjects/v060-rc7-a/install/rein.exe`,
assigned to `$REIN`/`$REIN7` in each shell) rather than a bare `rein`. Where
PATH-dependent mechanisms were exercised for real (the `agent-storage-probe`
wrapper scripts, §6/B8), the shell's PATH was explicitly prepended with the
POSIX-style install path and confirmed first with `where.exe rein` /
`(Get-Command rein).Source` before use — a Windows drive-letter-colon style
prepend (`D:/...`) was tried first in Git Bash, found to silently corrupt
`PATH` (the colon after the drive letter is a separator) and fall through to
a stale PATH-resolved `rein`, and corrected to the POSIX form
(`/d/ReinstateAcceptanceProjects/...`) before any B8 evidence was collected.

**Host environment rule compliance:** every shell in this part began with
`unset REINSTATE_BACKEND REINSTATE_MEMORY_BACKEND_DIR` (confirmed empty).
`CLAUDE_CONFIG_DIR`, `CODEX_HOME`, and `XDG_DATA_HOME` were never unset;
isolation for individual rows used the vendor's own home variable
(`GROK_HOME`, `QWEN_HOME`, `CLINE_DATA_DIR`, `KIMI_CODE_HOME`,
`CURSOR_CONFIG_DIR`, `COPILOT_HOME`, `PI_CODING_AGENT_DIR`) pointed at a
directory under `D:\ReinstateAcceptanceProjects\v060-rc7-a\`.

## 2. Vendor binaries observed (re-checked `--version`)

| Agent | Expected | Observed | Note |
| ----- | -------- | -------- | ---- |
| Claude Code | 2.1.263 (ceiling) | `2.1.263 (Claude Code)` | exact match |
| Codex CLI | 0.149.0 (widened to 0.153.4 this candidate) | `codex-cli 0.153.4` | in range this candidate; no account usage-limit was hit this run (see §11 G1) |
| OpenCode | 1.18.27 | in range (used as real B4/T5 root) | not independently re-versioned beyond doctor probe |
| Grok | 1.0.5 | `grok 1.0.13 (5e9a58528b76)` | version drift observed; Grok E-rows are **not** in this part's scope (owned by another part per the dispatch's grok section) — recorded here only as an environmental fact, not scored |
| Qwen | 0.21.12/0.21.13 | in range per doctor probe | not a row owned by this part |
| Gemini | 0.53.0 | `0.53.0` | exact match |
| Kimi | 0.36.1 | in range per doctor probe | not deep-tested this part (T2, owned elsewhere) |
| Copilot | 1.0.80 | in range; real sessions exercised (T1 rows) | version string not re-probed independently of doctor |

No agent was installed for this run; all of the above were already present
on the host.

## 3. Automated gates (Section A) — executor A, run once, `-p 4`

All commands ran from `D:\Projects\reinstate-worktrees\v060-rc7-tagged`.
A6/A7 ran against the coordinator's verified
`...\scratchpad\rc7-draft\` distribution directory; A1–A5 and A8 ran against
the worktree with `-p 4`.

| Gate | Command | Result | Evidence |
| ---- | ------- | ------ | -------- |
| A1 | `gofmt -l .`; `go vet ./...`; `go mod tidy -diff` | `PASS` | all three empty/clean, exit 0 |
| A2 | `CGO_ENABLED=0 go test ./... -count=1 -p 4` | `PASS` | full suite `ok`, 0 `FAIL` lines, exit 0 |
| A3 | `CGO_ENABLED=1 go test -race ./internal/... -count=1 -p 4` | `PASS` | see note below — third attempt clean, exit 0, 0 `FAIL`/`DATA RACE` lines |
| A4 | `make lint` (golangci-lint v2.11.4); `make vuln` (govulncheck v1.6.0) | `PASS` | see note below — lint clean once scoped to the tracked tree (0 issues across 82 tracked Go dirs); vuln: 0 vulnerabilities in own/imported code |
| A5 | `go test ./internal/doctest/... -count=1`; `scripts/check-docs.ps1` | `PASS` | both `ok`, exit 0 |
| A6 | `scripts/verify-release.ps1 -DistDir <rc7-draft>` | `PASS` | `release artifacts verified (PowerShell): ...rc7-draft`, exit 0 |
| A7 | `scripts/test-install.ps1 -DistDir <rc7-draft>` | `PASS` | verified + `ok internal/doctest 1.278s`, exit 0 |
| A8 | `GOOS=darwin GOARCH=amd64 go build ./...`; `GOOS=linux GOARCH=amd64 go build ./...` | `PASS` | both exit 0, no output |

**A3 note (host resource contention, not a product defect).** The first two
`-p 4` attempts failed with different packages each time
(`internal/crypto`/`internal/doctest`/`internal/tui/*`/`internal/sessionindex`,
never the same set twice), every failure attributable to one of: `link.exe`
→ mingw `ld.exe: ... error adding symbols: memory exhausted`,
`ThreadSanitizer failed to allocate ... (error code: 1455)` (Windows
`ERROR_COMMIT_LIMIT`), or a `collect2.exe: CreateProcess: No such file or
directory` transient. `Get-CimInstance Win32_OperatingSystem` showed this
shared host (many other agent/browser processes: `Memory Compression` alone
holding ~8.5 GB, multiple `comet`/`claude`/`Orca` processes) under real
memory pressure at the time. A third, isolated `-p 4` run completed clean
(exit 0, 0 `FAIL`/`DATA RACE` lines) — no data race was ever reported by
ThreadSanitizer in any of the three attempts, only allocation failures
before the race detector could run. Recorded as a harness/host-resource
finding (§16), not a product defect.

**A4 note (worktree contamination, not a product defect).** The first
`make lint` run failed on a single `errcheck` finding in
`scratchtool_h3\main.go:37` (unchecked `rc.Close()`). `git status --short`
and `git ls-files` confirm `scratchtool_h3/` is **untracked** — not part of
the `v0.6.0-rc.7` tagged tree — and its `main.go` (an S3/keyring
credential-fetch scratch tool, unrelated to this part's assignment) appeared
in the worktree *during this session*, after an earlier `git status --short`
in this same session had returned clean. This worktree was assigned to
executor A exclusively; a file appearing mid-session that this executor
never wrote is worktree-isolation contamination, most likely a second
process writing into the same shared worktree path. It was left untouched
(not deleted, in case another process still held it open) and excluded from
the lint scope by re-running `golangci-lint run` against exactly the 82
directories `git ls-files '*.go'` reports as tracked, which is `0 issues`.
See §16 for the harness finding.

## 4. Matrix A — Catalog integrity (10/10 `PASS`)

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| A1 | Descriptors validate at start; no duplicate/empty keys | `PASS` | `doctor --agents --json` lists 18 unique, non-empty `key` values; process starts cleanly every invocation |
| A2 | `doctor --agents` lists every catalog agent incl. every T0 | `PASS` | all 18 keys present, including all 7 T0 agents (`aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode`) |
| A3 | Every T0 agent shows an enumerated reason | `PASS` | text `doctor --agents` shows `t0_reason=` from a closed set observed: `layout_unverified`, `server_backed`, `desktop_only` |
| A4 | Declared tier and capability constructors agree | `PASS` | `resume`/`fork` refuse below T3 with exit `5`+reason (§ below), accept at T3+ (session-not-found, exit `2`, not a tier refusal); real `agents/conformance` unit tests (`TestShippedAgentsConformance`) passed in A2 |
| A5 | Every declared `Evidence` path exists in the tagged tree | `PASS` | real test `TestCatalogEvidencePathsExist`/`TestShippedEvidenceIsComplete` (`internal/doctest`, `internal/agents/conformance`) passed in A2/A5 |
| A6 | `sessions --agent` accepts exactly T1+ keys, else exit `2` | `PASS` | all 11 T1+ keys accepted (exit 0); all 7 T0 keys rejected with `{"code":"usage",...}` exit `2` |
| A7 | `resume` accepts exactly T3+ keys, else exit `5` w/ reason | `PASS` | `claude`/`codex`/`grok`/`qwen`/`opencode` (T3+) accepted the key form (proceeded to "session not found", exit `2`); `cline`/`copilot`/`cursor` (T1) and `gemini`/`kimi` (T2) refused with exit `5` and a tier-named reason |
| A8 | Agent ordering deterministic across runs | `PASS` | two `sessions --agent all --json` calls diffed byte-identical (0 lines) |
| A9 | Broken evidence path fails conformance | `PASS` | real tests `TestBrokenEvidencePathFails`, `TestBrokenEvidencePathIsCaught` (`internal/agents/conformance`) passed in A2 |
| A10 | No scan writes/renames/locks under any agent root | `PASS` | real filesystem audit: copied the live `GROK_HOME` root (8,311 files) to an isolated dir, ran a full `doctor --agents --json` probe **and** `sessions --agent grok --json` against the copy, re-hashed all 8,311 files and re-listed the tree — SHA-256 set and directory listing both byte-identical before/after (0 diff lines) |

## 5. Matrix B — Probe and redaction (9/9 `PASS`)

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| B1 | Output conforms to `AGENT-PROBE-V1` | `PASS` | `"schema": "AGENT-PROBE-V1"` present |
| B2 | No absolute path/home name/username | `PASS` | case-insensitive scan of a full `doctor --agents --json` (all 11 real installed agents) for the host username and `C:\Users`/`D:\Projects` — 0 matches |
| B3 | No JSON values from session files, keys only | `PASS` | inspected `first_line_keys`/`name_shapes` fields directly: contain only key names and shape descriptors (`<slug>`, `<uuid-v4>`, `<40-hex>`), never a value; one benign observation recorded in §16 (a source file under Claude's tree uses hex-color strings as its own JSON object keys, so those literal keys surface looking like values — still literally "keys only", not a B3 violation) |
| B4 | Names shape-normalized, no raw UUID/hash/path slug | `PASS` | full-document regex scan for UUID and 40-hex patterns across **every real installed agent's** probe output (claude, codex, cline, copilot, cursor, gemini, grok, kimi, opencode, pi, qwen) — 0 matches of either pattern; Gemini specifically re-checked (`resolved_root`, `name_shapes` all wildcarded/shaped, `version_raw` `0.53.0`) per this run's explicit re-test requirement |
| B5 | No excluded subtree present | `PASS` | full-document case-insensitive scan for `credential`, `keychain`, `token.json`, `apikey`, `secret`, `.git-credentials`, `password` — 0 matches |
| B6 | Absent agent reported without error | `PASS` | all 7 T0 agents show `resolved_root: null`, `executable_on_path: false`, empty `tree`/`name_shapes`, exit 0, no error |
| B7 | Empty root distinguishable from absent root | `PASS` | real test: `CLINE_DATA_DIR` and `KIMI_CODE_HOME` each pointed once at a real, existing, empty directory and once at a real, nonexistent path — in both agents the `env`-relative `candidate_roots` entry differs exactly in its `exists` field (`true` for the empty dir, `false` for the nonexistent one); every other field held constant |
| B8 | Wrapper scripts match binary output | `PASS` | with the tagged install first on `PATH` (confirmed via `where.exe rein`), `scripts/testing/agent-storage-probe.sh` (Git Bash) and `.ps1` (PowerShell) each produced output differing from a direct `doctor --agents --json` call only in the `generated_at` timestamp (expected: separate process invocations) |
| B9 | Secret scanner passes over committed probe artifacts | `PASS` | real test `internal/secretscan`/`internal/fixture` passed in A2 (`ok`) |

## 6. Matrix F — Per T0 agent (7 agents × 2 rows = 14/14 `PASS`)

| Agent key | F1 (listed w/ reason) | F2 (no capability offered/implied) |
| --------- | ---------------------- | ------------------------------------ |
| `aider` | `PASS` — `t0_reason=layout_unverified` | `PASS` — `sessions --agent aider` exit `2`; `resume aider:x` exit `5` "native session action is unsupported: Aider is tier T0 (layout_unverified)" |
| `amp` | `PASS` — `t0_reason=server_backed` | `PASS` — same shape, exit `2`/`5`, reason names `server_backed` |
| `antigravity` | `PASS` — `t0_reason=layout_unverified` | `PASS` — same shape, exit `2`/`5` |
| `minimax-code` | `PASS` — `t0_reason=layout_unverified` | `PASS` — same shape, exit `2`/`5` |
| `openhands` | `PASS` — `t0_reason=server_backed` | `PASS` — same shape, exit `2`/`5` |
| `roo` | `PASS` — `t0_reason=layout_unverified` | `PASS` — same shape, exit `2`/`5` |
| `zcode` | `PASS` — `t0_reason=desktop_only` | `PASS` — same shape, exit `2`/`5`, reason names `desktop_only` |

Closed enumeration of `t0_reason` values observed across the whole catalog
this run: `layout_unverified`, `server_backed`, `desktop_only`.

## 7. Matrix G — No regression (8/8 `PASS`)

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| G1 | Claude/Codex native resume and fork unchanged | `PASS` | **Claude half:** real isolated throwaway project, host's live `CLAUDE_CONFIG_DIR`; planted a recall token via `claude -p`; found it with `rein search --agent claude --json`; `rein resume claude:<id> --dry-run --json` named `claude --resume <id>` and reported `agent.version` `match` at `2.1.263`; a real `claude --resume <id> -p "..."` recalled the token verbatim at the same session id; `rein fork claude:<id> --dry-run --json` named `claude --resume <id> --fork-session`; a real fork produced a **distinct** new session id that also recalled the token verbatim. **Codex half:** same method against the real, in-range `0.153.4` binary — no usage-limit refusal was hit on any attempt this run (see note below); planted token via `codex exec`; found via `rein search --agent codex --json`; `rein resume codex:<id> --dry-run --json` named `codex resume <id>` and reported `agent.version` `match` at `0.153.4`; real `codex exec resume <id> "..."` recalled the token verbatim at the same session id; `rein fork codex:<id> --dry-run --json` named `codex fork <id>`; real `codex exec fork <id> "..."` produced a **distinct** new session id that recalled the token verbatim; `rein sessions --agent codex --json` catalog reflects both ids |
| G2 | Both structured-handoff directions complete | `PASS` | from each source session's own workspace: real (non-dry-run, `--no-launch`) `rein handoff claude:<id> --to codex --json` exit 0, capsule written (`handoff_id` present); real `rein handoff codex:<id> --to claude --json` exit 0, capsule written, new destination `session_id` planned |
| G3 | Gemini and Grok remain handoff sources and remain read-only (OpenCode has since graduated to T5, per this run's dispatch note) | `PASS` | both a real Grok session and a real Gemini session (host's real, live vendor roots) passed a real `rein handoff <agent>:<id> --to claude --dry-run --json` (source correctly recognized, capsule planned); "remains read-only" independently confirmed for Grok by the A10 no-write filesystem audit above (byte-identical before/after a full scan) |
| G4 | Push/pull carry sessions for exactly the sync-capable agents (Claude Code, Codex CLI, OpenCode — corrected wording) and no other agent's | `PASS` | real evidence, not just completion: stood up a real S3-compatible fake server (`internal/backend/s3/s3test`, the project's own real test double, run standalone via a temporary `go run` helper, deleted after use) and a real, isolated `rein init --yes` profile against it. `push --agent claude/codex/opencode --all --dry-run` each returned real matching session snapshots (30+, several, several respectively); `push --agent grok --all --dry-run` and `push --agent pi --all --dry-run` both returned `"no matching local sessions found"` **despite both agents having real local sessions** (40 and 10 respectively per the doctor probe) — sync-capability, not just presence, gates matching. A real (non-dry-run) push of one Claude session succeeded (exit 0, one snapshot id written); `rein status --json` against the fake store correctly reported that exact session key and snapshot id back, decrypted. `push --agent`/`pull --agent` shell completion (`rein __complete push --agent ""`) independently returned exactly `claude`, `codex`, `opencode` — matching this row's corrected wording exactly |
| G5 | Cross-OS path remapping unchanged | `PASS` | real `internal/pathmap` unit tests passed in A2/A3 (`ok`) |
| G6 | Exit-code semantics unchanged; no new codes | `PASS` | `internal/exitcode`'s 8 constants (`OK`=0 … `Safety`=7) were introduced in the project's very first commit and have never been modified since (`git log --follow -p -- internal/exitcode/*.go` shows only that one commit ever touched the constant definitions); this run also independently observed exit `0`/`2`/`5` behaving exactly as documented across dozens of real commands |
| G7 | An index built by a prior release upgrades without data loss (this candidate: v0.5.1-built index, per this run's assignment) | `PASS` | extracted the checksum-verified `reinstate_0.5.1_windows_amd64.zip`; built a real index under an isolated `REINSTATE_HOME` with the v0.5.1 binary (`sessions --agent all --json` → 100 sessions, exit 0); ran the tagged rc.7 binary against the **same** `REINSTATE_HOME` with no rebuild step — `sessions --agent all --json` → same 100 sessions, exit 0, no error, no data loss |
| G8 | Capsules remain excluded from sync | `PASS` | structural + observed: `push`/`pull` only accept `--agent <catalog-agent-key>`; capsules/handoffs are not a catalog agent and have no `--agent` value; the real G4 push run above touched only agent session snapshots, never referenced the handoffs directory |

**G1 Codex-half note:** the dispatch's Codex account usage-limit
wait-and-retry protocol did not need to trigger this run — the first
`codex exec` attempt for the create/resume/fork sequence succeeded
immediately, at `2026-09-08T17:08–17:09Z` local-clock-equivalent
(`17:08:26Z`–`17:09:45Z` per the tool's own timestamps), with no usage-limit
error returned at any point in this part's testing.

## 8. Matrix H — CLI contract and performance (6/6 `PASS`)

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| H1 | CLI reference matches shipped flags | `PASS` | real test `TestCLIReferenceListsCatalogKeys` etc. (`internal/doctest`) passed in A2/A5 |
| H2 | JSON shapes match documentation | `PASS` | real tests `TestObjectFormatExamplesMatchTheCode`, `TestKeyringExampleShowsEveryFieldTheFormatHas` (`internal/doctest`) passed in A2/A5 |
| H3 | Full refresh within documented ceiling | `PASS` | `13.7s` | real cold `sessions --agent all --json` across all 11 installed real agents (~640+ real sessions) completed in `13.721s` wall clock — under the documented historical `20–30s` Windows operator-observation ceiling (`docs/testing/phase-3-cli-performance.md`) |
| H4 | No-change refresh materially faster | `PASS` | `0.201s`/`0.205s` | two immediately-following warm re-runs of the same command completed in `~0.2s` each — roughly **68×** faster than the cold run |
| H5 | One slow/unreachable source does not block others | `PASS` | pointed `QWEN_HOME` at an unrelated, large local tree (`C:/Windows/System32`, no Qwen marker); `sessions --agent all --json` still completed in `3.8s`, exit 0, with all 10 other real agents' sessions present and correct (`qwen` correctly absent from the result, not hung/blocking) |
| H6 | Completion lists correct agent keys per command | `PASS` | `rein __complete sessions --agent ""` returned exactly the 11 T1+ keys plus `all` (matching `sessions --help`); `rein __complete push --agent ""` and `rein __complete pull --agent ""` each returned exactly `claude`, `codex`, `opencode` — the corrected G4 sync-capable set, exercised directly via the real cobra completion machinery, not inferred |

## 9. Matrix C — Per T1 agent: `cline`, `copilot`, `cursor`, `pi`

### Agent: `cline` (T1, 12 real sessions, 8 distinct projects)

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| C1 | ≥2 sessions, ≥2 projects | `PASS` | 12 sessions across 8 distinct projects |
| C2 | Metadata matches what the agent shows | `PASS` | real adapter unit tests (`internal/agents/sources/cline`) passed in A2; a Cline-reported `message_count` was independently re-derived from the raw store during the C6 corruption test below and matched |
| C3 | Search finds a known string | `PASS` | created a real session via `cline --cwd <throwaway-git-project> --json` with a planted token in the prompt (probed liveness first with a `"Reply PONG"` call, which answered — no `Unauthorized`); `rein search <token> --agent cline --json` found exactly it |
| C4 | Inspect bounded, no transcript body | `PASS` | `rein inspect cline:<id> --json` returned a ~5.4 KB bounded record (`key,id,agent,title,project,workspace,updated_at,size_bytes,message_count,prompt_preview,can_resume,can_fork,read_only_reason` — metadata + one short preview, never a message array) |
| C5 | Read-only reason + resume refused exit `5` | `PASS` | `resume cline:x` → exit `5`, `"native session action is unsupported: Cline is tier T1..."`; every listed session carries `read_only_reason` |
| C6 | Corrupt/empty/absent roots degrade cleanly | `PASS` | **absent** (`CLINE_DATA_DIR` at a nonexistent path): exit 0, no error. **empty** (existing empty dir): exit 0, no error. **corrupted** (copy of the real `.cline/data`, `db/tasks.db` truncated to half size, WAL/SHM removed): exit 0, no panic, the source adapter fell back to the real per-session files on disk and returned correct, uncorrupted session data rather than crashing or returning garbage |

### Agent: `copilot` (T1, 4 real sessions, 4 distinct projects)

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| C1 | ≥2 sessions, ≥2 projects | `PASS` | 4 sessions across 4 distinct projects |
| C2 | Metadata matches what the agent shows | `PASS` | real adapter unit tests (`internal/agents/sources/copilot`) passed in A2; message counts reported: `5, 5, 5, 26` |
| C3 | Search finds a known string | `PASS` | created a real session via `copilot -p "..." --allow-all-tools` with a planted token in a throwaway git project; `rein search <token> --agent copilot --json` found exactly it |
| C4 | Inspect bounded, no transcript body | `PASS` | `rein inspect copilot:<id> --json` returned the same bounded shape as `cline` above |
| C5 | Read-only reason + resume refused exit `5` | `PASS` | `resume copilot:x` → exit `5`, `"...GitHub Copilot CLI is tier T1..."` |
| C6 | Corrupt/empty/absent roots degrade cleanly | `PASS` | **absent**/**empty**: exit 0, no error (`COPILOT_HOME` redirected). **corrupted** (copy of real `.copilot`, one session's `events.jsonl` truncated to 3/4 size): exit 0, no panic, session still listed with `message_count` reduced to reflect only the complete, readable content — no partial/garbled record |

### Agent: `cursor` (T1, 3 real sessions — real host store, `cursor-agent` broken on this host: `Cannot find module tree-sitter`, so no fresh session could be created; tested read-only through `rein` alone per this run's instructions)

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| C1 | ≥2 sessions, ≥2 projects | `PASS` | 3 sessions across 3 distinct projects |
| C2 | Metadata matches what the agent shows | `PASS` | message_count values reported (real host store, read-only): `4`, `3`, `3` |
| C3 | Search finds a known string | `PASS` | read one real session's `prompt_preview` myself, picked a distinctive word from it (not quoted here per instructions), searched with `rein search <word> --agent cursor --json` — **found 1** |
| C4 | Inspect bounded, no transcript body | `PASS` | same bounded shape as above; `prompt_preview` ~160 chars, never a message array |
| C5 | Read-only reason + resume refused exit `5` | `PASS` | `resume cursor:x` → exit `5`, `"...Cursor CLI is tier T1..."` |
| C6 | Corrupt/empty/absent roots degrade cleanly | `PASS` | **absent**/**empty**: exit 0, no error (`CURSOR_CONFIG_DIR` redirect, "as at rc.2"). **corrupted** (copy of real `.cursor`, one session's SQLite `store.db` truncated to half size): exit 0, no panic, session still listed (title/project intact) with `message_count` degraded to `0` rather than a crash or garbled read |

### Agent: `pi` (T1, 10 real sessions, 9 distinct projects)

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| C1 | ≥2 sessions, ≥2 projects | `PASS` | 10 sessions across 9 distinct projects |
| C2 | Metadata matches what the agent shows | `PASS` | deep cross-check: for one real session, the raw `.jsonl` file has 5 total lines but only 2 message-type records — `rein`'s reported `message_count` was exactly `2`, an exact independent match |
| C3 | Search finds a known string (re-run of the rc.4 fix, same method) | `PASS` | created a real session via `pi -p "..."` with a planted token in a throwaway git project; `rein search <token> --agent pi --json` found exactly it; `rein inspect pi:<id> --json` showed a non-empty `prompt_preview` (70 chars) |
| C4 | Inspect bounded, no transcript body | `PASS` | same bounded shape as above |
| C5 | Read-only reason + resume refused exit `5` | `PASS` | `resume pi:x` → exit `5`, `"...Pi sessions are read-only until a device journey verifies native resume"` |
| C6 | Corrupt/empty/absent roots degrade cleanly | `PASS` | **absent**/**empty**: exit 0, no error (`PI_CODING_AGENT_DIR` redirect). **corrupted** (copy of real `.pi/agent`, one session's `.jsonl` truncated to 3/4 size, cutting the final record mid-record): exit 0, no panic; the truncated session's `message_count` correctly dropped from its true value to `1` (only the complete record counted), `size_bytes` correctly reflected the truncated file size — no partial/garbled record surfaced |

## 10. Findings

| ID | Severity | Matrix row | Sanitized description | Release blocking |
| -- | -------- | ---------- | --------------------- | ----------------- |
| PART-A-H1 | MINOR (harness) | A3 | Two of three `-p 4` race-suite attempts failed on transient host memory/linker exhaustion (`ERROR_COMMIT_LIMIT`/`collect2.exe` missing), never on an actual reported data race; third isolated attempt passed clean. Host resource contention from concurrently running unrelated processes, not a Reinstate defect. | `NO` |
| PART-A-H2 | MAJOR (harness/process) | A4, general | An untracked directory (`scratchtool_h3/`, an S3/keyring scratch tool unrelated to this assignment) appeared inside this executor's exclusively-assigned worktree mid-session, after this session's own initial `git status --short` had been clean. Strong indication of a second process writing into the same shared worktree path concurrently with this run. Left untouched; excluded from lint scope by scoping to `git ls-files` tracked directories, which is clean. Recommend the coordinator confirm worktree exclusivity across all parts of this run. | `NO` (does not affect product correctness; the tagged tree itself is clean) |

No product defects were found. Every row this part owns is `PASS`.

## 11. Tier reductions proposed by this device

None. Every claimed tier this part could verify was verified at that tier.

## 12. Terminated device block (this part)

> Testing for executor A's assigned scope of this candidate is terminated at
> the milestone recorded above. The results in this section are final for
> this device, this tag, and this part's assigned rows. Any further testing
> of this scope requires a new candidate tag and a new report.

- Terminating tester: `executor-a (claude-sonnet-5 subagent)`
- UTC timestamp: `2026-09-08T23:35:00Z` (approximate)
- Part-A verdict: `PASS`

## 13. Isolated test locations used (deleted after use where noted)

- `D:\ReinstateAcceptanceProjects\v060-rc7-a\bootstrap\` — live-bootstrap install (kept, per instructions)
- `D:\ReinstateAcceptanceProjects\v060-rc7-a\install\` — archive install used for all testing (kept)
- `D:\ReinstateAcceptanceProjects\v060-rc7-a\{g1-claude-proj,g1-codex-proj,pi-c3-proj,cline-c3-proj,copilot-c3-proj}\` — throwaway git projects for planted-token sessions
- `D:\ReinstateAcceptanceProjects\v060-rc7-a\{a10-audit,corrupt-pi,corrupt-copilot,corrupt-cursor,corrupt-cline,b7-*}\` — isolated copies for A10/C6/B7 audits (real vendor data copied, never the live trees themselves mutated)
- `D:\ReinstateAcceptanceProjects\v060-rc7-a\g4-reinstate-home\` + a temporary local `s3test.Fake` server (the project's own real test double, run via a throwaway `go run` helper under `g4scratch/`, deleted from the worktree after use) — isolated sync profile for the real G4 push/pull evidence
- `D:\ReinstateAcceptanceProjects\v060-rc7-a\v051-install\`, `g7-index-home\` — v0.5.1 binary + index-upgrade evidence for G7

No transcript text, prompts, credentials, private paths, tokens, or session
ids not created by this executor are quoted in this report. Two real,
historical planted-token session titles from a prior executor's earlier
(`v0.6.0-rc.6`) run were observed incidentally while scanning the host's
real `pi` and `copilot` stores for C6 evidence — neither is quoted here.

## 14. Row table (structured summary)

See §§4–9 above for the full evidence per row. Total required rows this part
owns: Section A gates (8) + Matrix A (10) + Matrix B (9) + Matrix F (14) +
Matrix G (8) + Matrix H (6) + Matrix C × 4 agents (24) = **79** discrete
assertions, plus the artifact-identity proof commands, all `PASS`, `0 FAIL`,
`0 PARTIAL`, `0 NOT TESTED`.
