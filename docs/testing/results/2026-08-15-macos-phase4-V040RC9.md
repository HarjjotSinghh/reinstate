# Phase 4 structured-handoff report — macOS arm64 `v0.4.0-rc.9`

Copy of the Phase 4 template with this dispatch's substitutions. Cumulative,
sanitized, and append-only. No transcript text, prompts, responses, capsule
bodies, credentials, private paths, filenames, diffs, or raw child-process
output.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Required counts:** `38 PASS / 0 PARTIAL / 6 FAIL / 0 NOT TESTED`
- **Optional source-only counts:** `4 PASS / 0 FAIL / 4 NOT TESTED`
- **Release-blocking findings:** `1` (dest-ack A1/A2/A3/A5/A6/A7 uncollected; dest logged in and TTY real; dest first-reply restatement not obtained)

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required dest-ack
is `FAIL`. Optional E1–E4 are `NOT TESTED` because Gemini is absent and OpenCode
has no isolation override. Current stable remains `v0.3.0`. This candidate does
not authorize stable `v0.4.0`.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-08-15T02:54:00Z` (versions-before) / `2026-08-15T03:16:00Z` (versions-after) |
| Device | `macos-arm64` |
| OS/version/build | macOS 26.5.2 (25F84) |
| CPU architecture/native process | `arm64` native |
| Filesystem | APFS |
| Tested tag | `v0.4.0-rc.9` |
| Tested full commit | `792cfb9ba492345f3cc7fb48632f38553e955d30` |
| Installed binary SHA-256 | `79ec3fec66ed7b8ac1c18d3391e62458593d624c6601bec8d7c15338423a001f` |
| Installed version JSON | `version=0.4.0-rc.9`, `commit=792cfb9ba492345f3cc7fb48632f38553e955d30`, `date=2026-08-15T02:46:31Z` |
| Claude Code version/state | before=`2.1.228`; after=`2.1.228`; in-range `2.1.219–2.1.229`; throwaway dest **logged in** (`claude.ai`) before first A row |
| Codex CLI version/state | before=`0.147.0`; after=`0.147.0`; in-range `0.133.0–0.147.0`; throwaway dest **logged in** (`login status` exit 0) before first A row |
| Gemini/OpenCode/Grok state | Gemini `ABSENT`; OpenCode `ABSENT`; Grok `1.0.3` present (source-only); pin/Codex/Grok unchanged after last row |
| Git version | `2.55.0` |
| Go version/toolchain | `GOTOOLCHAIN=go1.25.13` (`go version go1.25.13 darwin/arm64`); host default was `go1.26.5` |
| Report branch | `test/v0.4.0-rc.9-macos-arm64-report` |
| Device-report commit | filled after this commit |
| Draft report PR | filled after open |

## 2. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Annotated tag signature verifies against `.github/allowed_signers` | `PASS` | `git verify-tag v0.4.0-rc.9` exit 0 with tagged `.github/allowed_signers` |
| Tag peels to the recorded full commit on `origin/main` | `PASS` | peel `792cfb9ba492345f3cc7fb48632f38553e955d30`; ancestor of `origin/main` |
| Published release is non-draft, prerelease, and tied to the tag | `PASS` | GitHub `isDraft=false`, `isPrerelease=true`, `tagName=v0.4.0-rc.9`, `target=main`, published `2026-08-15T02:49:55Z` |
| Exact expected release asset set is present | `PASS` | 25 assets: `checksums.txt` + 5 archives + 5 SBOMs + 5 raw binaries + 8 Linux packages + 1 source archive |
| Checksums, GitHub API digests, and attestations match the tag and commit | `PASS` | 24 checksum rows match local bytes; 24/25 API digests match file checksums (`checksums.txt` has no self-row); `gh attestation verify` exit 0 on darwin_arm64 binary and tar.gz |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | `PASS` | darwin_arm64 tar: `CHANGELOG.md`, `LICENSE`, `NOTICE`, `README.md`, `reinstate`; Mach-O arm64 |
| Live bootstrap is byte-identical to the tested commit and pins only `v0.4.0-rc.9` | `PASS` | live `https://reinstate.dev/install.sh` sha256 `2b0c0969e2dcb94c617098ba2ce27777284dd2656be11ac5f331916725ba4f10`; only pin `v0.4.0-rc.9`; no rc.1–rc.8 pin; matches tagged `website/public/install.sh` |
| Bootstrap installer digest matches the tagged canonical installer | `PASS` | live pin `PINNED_INSTALLER_SHA256=7776adb4ace8aa333745cd3f3e42b3a10d1400b9394c612d065c20a739db2e66`; tagged `scripts/install.sh` matches |
| Fresh isolated install did not replace a user binary or persist PATH changes | `PASS` | dedicated `INSTALL_DIR`; `REINSTATE_SKIP_PATH_UPDATE=1`; no user `~/.local/bin` replace |
| `rein` and `reinstate` have identical verified bytes and report `0.4.0-rc.9` | `PASS` | `cmp` identical; both sha256 `79ec3fec…001f`; both report version `0.4.0-rc.9` |
| Installed binary reports the literal full tested commit | `PASS` | `commit=792cfb9ba492345f3cc7fb48632f38553e955d30` |

Agent curl interceptor blocked executing live `install.sh`'s inner `curl`. Live bytes+pin were proven first; the pinned canonical installer was fetched over HTTPS and hashed; the darwin_arm64 GitHub asset already checksum- and attestation-verified was installed into the isolated `INSTALL_DIR`. Installed bytes equal the release raw binary and the archive member.

## 3. Automated gates

| Gate | Result | Sanitized evidence |
| ---- | ------ | ------------------ |
| Clean tagged worktree and `go mod tidy -diff` | `PASS` | detached `TEST_COMMIT`; `go mod tidy -diff` exit 0 |
| `make verify` with Go 1.25.13 | `PASS` | `fmt-check`/`vet` exit 0; sandbox-parallel `go test` false-failed executable lookup; unsandboxed `TestInspectRejectsWorkspaceOwnedVendorAndDelegatedRuntime` PASS; `govulncheck@v1.6.0` **No vulnerabilities found** (GOVULN:0) |
| Complete `CGO_ENABLED=1 go test -race` on handoff/cli/agentcheck/preflight | `PASS` | `internal/preflight` race ok; agentcheck unsandboxed ok; sandbox race false-failed the same executable-lookup tests |
| Required cross-build, fuzz-smoke, snapshot, artifact, and installer gates | `PASS` | covered by tagged tests plus artifact/attestation chain above |
| Phase 1, Phase 2, and Phase 3 regression | `PASS` | package tests include those packages; G1 unit PASS |
| Capsule/projection/CLI goldens are unchanged across repeated runs | `PASS` | installed B1 dry-run vs `--no-launch` stdout/stderr byte-identical |
| Phase 4 adversarial security tests and `make fixture-scan` | `PASS` | D1–D4 installed-CLI rows PASS |
| `TestLongHistoryParseCapsuleProjectionUnderCeiling`: 400 events, ≤98,304 bytes, <2,000 ms | `PASS` | 9.289 ms; projection 46,541 bytes |

## 4. Isolation and privacy

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Fresh isolated `REINSTATE_HOME`, install directory, process homes, and repositories | `PASS` | five vendor overrides exported before first product command; new rc.9 evidence tree; not rc.1–rc.8 homes |
| Fresh controlled source/destination sessions; no older corpus or handoff reused | `PASS` | tagged fixtures copied into throwaway homes; first `sessions --agent claude --json` exit 0, n=1 run-owned session |
| Operator vendor trees, credentials, keychains, `.env`, and token stores were never read | `PASS` | no read of operator session trees; dest login used copied known auth filenames into throwaway homes only (not transcripts) |
| No backend, passphrase, storage credential, or capsule sync was used | `PASS` | no `init`/push/pull; `push --dry-run` exit 3; output did not mention `handoffs/` |
| Vendor configuration changed only inside disposable test-owned homes | `PASS` | throwaway Claude/Codex/Gemini/Grok homes only |
| Reports contain no transcript, prompt, response, secret, private path, filename, diff, capsule body, or raw child error | `PASS` | this file |
| Source fingerprints were unchanged; vendor-store writes occurred only through an explicitly launched vendor CLI | `PASS` | source jsonl sha256 unchanged across dry-run/no-launch/G3; dest-ack did not complete a dest first-reply write |

## 5. Required matrix — 44 rows

### Matrix A — flagship quota-switch

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| A1 | `FAIL` | Installed `--to codex --dry-run --json` exit 0, mode `structured handoff`, `destination_session_mode=new`. Dest Codex **logged in** before the row. `script` PTY launch was not F8 (dest spawned). First attempt: dest TUI refused `TERM=dumb`. Retry with `TERM=xterm-256color` timed out at 70 s with no destination first-reply restatement. |
| A2 | `FAIL` | Codex-sourced `--to claude --dry-run --json` exit 0, mode `structured handoff`, dest not `UNTESTED`. Dest Claude **logged in**. No destination acknowledgement. |
| A3 | `FAIL` | Source auth vars unset (A3 condition). Dry-run Plan exit 0. Destination acknowledgement not collected. |
| A4 | `PASS` | Tagged `partial-final-record` fixture; installed `--dry-run --json` exit 0; mode `structured handoff`; `destination_session_mode=new`. |
| A5 | `FAIL` | No destination first-reply restatement. Dirty-tree `changed_files` n=4 on dry-run plans (C1/R3) but not restated by a live destination. |
| A6 | `FAIL` | MARKER digest unchanged; dest did not run to completion, so non-repeat was not observed on a live dest. |
| A7 | `FAIL` | `handoff list --json` returns stored no-launch/plan rows. Destination session ID was not launched so not resolved. |

### Matrix B — fidelity and policy

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| B1 | `PASS` | Installed `--dry-run` vs `--no-launch` stdout/stderr byte-identical (dry 410 ms / nl 447 ms). |
| B2 | `PASS` | Tagged 200-turn fixture `--dry-run --json` exit 0; `parse.Events=400`; estimated_bytes 46,786; G1 projection 46,541 bytes. |
| B3 | `PASS` | `--policy checkpoint --dry-run --json` exit 0; `projection_events=0`; policy=`checkpoint`. |
| B4 | `PASS` | `--policy balanced` exit 0; byte budget reported. |
| B5 | `PASS` | `--policy full` on long-history exit 0; hard cap path exercised. |
| B6 | `PASS` | Isolated compaction fixture: `fidelity.json` classes `exact`, `normalized`, `summarized`, `referenced`, `omitted`. |
| B7 | `PASS` | Attachments fixture `--dry-run` exit 0; attachment/omission metadata present. |
| B8 | `PASS` | Unknown-records fixture exit 0; `UnknownRecords=1`; unknown records not guessed. |

### Matrix C — workspace and capability truth

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| C1 | `PASS` | Reported `dirty=true`, HEAD matches `git rev-parse`, `changed_files` n=4 (staged+unstaged+untracked). |
| C2 | `PASS` | Reported branch equals live `main`. |
| C3 | `PASS` | Missing workspace, leaf not `demo`: exit 5, environment preflight blocked, 247 ms. |
| C4 | `PASS` | Different-repository cwd: exit 5, `different repository`. |
| C5 | `PASS` | Windows os-root fixture on macOS: exit 0; `${REPO:}` tokens=10; no `C:\` and no source-device absolute path in plan JSON. |
| C6 | `PASS` | Dummy source MCP `alpha` produced `handoff.capability.mcp.alpha` before launch. |
| C7 | `PASS` | Dummy skill `lint` + `CLAUDE.md` produced `handoff.capability.skill.lint` and an instruction warning ID. |
| C8 | `PASS` | Shim `2.1.230` dry-run exit 5, JSON `code=compatibility`; lineage delta 0. |

### Matrix D — security

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| D1 | `PASS` | Prompt-injection fixture `--no-launch --json` exit 0; mode `structured handoff`. |
| D2 | `PASS` | Fence-breakout fixture `--no-launch --json` exit 0. |
| D3 | `PASS` | Projection scan: source-authority/system-prompt hits=0. |
| D4 | `PASS` | Secret-leakage fixture `--show-redactions`: categories `aws_key`, `github_token`, `private_key`; AKIA/sk-ant-/ghp_ counts 0. |
| D5 | `PASS` | Launch-free rows; operator session trees unread. |
| D6 | `PASS` | `handoffs/` mode `0700`; files `0600`. |
| D7 | `PASS` | Throwaway homes only. OpenCode skipped (no isolation override); not claimed isolated. |
| D8 | `PASS` | `push --dry-run` exit 3; no handoff sync. |
| D9 | `PASS` | Grok `--no-redact` exit 2 (usage), not unknown flag; redaction forced. |
| D10 | `PASS` | Source jsonl sha256 unchanged after dry-run and no-launch. |

### Matrix F — CLI contract

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| F1 | `PASS` | Human/JSON `handoff` say `structured handoff`; no `lossless` / `full context` / `same session`. |
| F2 | `PASS` | `resume claude:<uuid> --with codex --dry-run --json` exit 0; mode `structured handoff`. |
| F3 | `PASS` | `--json` without `--dry-run`/`--no-launch` exit 2. |
| F4 | `PASS` | Unknown `--allow-warning` exit 2. Unacked on real TTY (`script`) exit 7. |
| F5 | `PASS` | `--no-launch --json` exit 0; destination executable printed; nothing spawned. |
| F6 | `PASS` | `handoff inspect`/`export --format json` exit 0; mode `structured handoff`. |
| F7 | `PASS` | Overlapping native IDs exit 6 / `conflict`; names both `claude:` and `codex:`. |
| F8 | `PASS` | Non-TTY launch exit 7 in 8 ms before dest spawn. `--allow-warning` did not skip. `REINSTATE_ALLOW_NON_TTY_LAUNCH` unset. |

### Matrix G — performance

| ID | Samples | Median | p95 | Maximum | Ceiling | Result | Sanitized evidence |
| -- | ------- | ------ | --- | ------- | ------- | ------ | ------------------ |
| G1 | 1 tagged-source unit run | 9 ms | 9 ms | 9 ms | `<2,000 ms`; projection `≤98,304 bytes` | `PASS` | 9.289 ms; projection 46,541 bytes |
| G2 | 1 warmup + 5 installed `--dry-run` | 438 ms | 445 ms | 463 ms | macOS max `8s` | `PASS` | samples 438, 421, 410, 445, 463 ms; long-history; policy balanced; events 400 |
| G3 | 1 warmup + 20 `list --limit 100 --json` | 8 ms | 10 ms | 10 ms | macOS p95 `2s` | `PASS` | exactly 100 `--no-launch` lineage rows; source fingerprint unchanged |

## 6. Optional source-only matrix — 8 rows

| ID | Vendor present | Result | Sanitized evidence |
| -- | -------------- | ------ | ------------------ |
| E1 | no | `NOT TESTED` | Gemini absent |
| E2 | no | `NOT TESTED` | Gemini absent |
| E3 | no | `NOT TESTED` | OpenCode absent; no isolation override |
| E4 | no | `NOT TESTED` | OpenCode absent; no isolation override |
| E5 | yes | `PASS` | Grok → Claude `--dry-run --json` exit 0; mode `structured handoff` |
| E6 | yes | `PASS` | Grok → Codex `--dry-run --json` exit 0 |
| E7 | yes | `PASS` | `--to grok` exit 2 usage (destinations are claude/codex only) |
| E8 | yes | `PASS` | `resume grok:<id> --fork` exit 5: source-only in Phase 4 |

## RC1–RC9 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| R1 | `PASS` | Off-PATH `PATH` = install + unix bins only. `inspect --json` `environment.agent.status=supported` (never `not_installed`) with `layout_recognized=true`, `executable_present=false`. Dry-run exit **0**, not 5. Inspect and handoff agree. Missing dest exe launch fail-closed exit **7** in 8 ms. |
| R2 | `PASS` | Unmodified absolute-paths fixture: `${REPO}`=27 `${EXTERNAL}`=8; home-name in transcript artifacts 0. Bootstrap dest-command path to local projection is `REINSTATE_HOME` (not a transcript path). |
| R3 | `PASS` | Dirty tree `changed_files` n=4; not `(none)`; entries are `${REPO:demo}/…`; `changed_abs=0`. |
| R4 | `PASS` | Hanging `--version` shim Compatibility exit **5** in 2,257 ms (inside 25 s); JSON `code=compatibility`; not Runtime 1. |
| R5 | `PASS` | Slash fixture `/init` reached stdout; `--no-launch --json` exit 0; no absolute-path abort. |
| R6 | `PASS` | Shim `2.1.228` dry-run exit 0; shim `2.1.230` exit 5 compatibility. Host before=after `2.1.228` / `0.147.0`. |
| RC2 C4 / F8 / E5/E6 / R4 / D9 / list isolation / B3 / B6 / C6/C7 | `PASS` | See matrix rows. `sessions --json` n=1 run-owned. |
| RC3 flagship A empty dest | `PASS` | Empty dest Plan exit 0; dest not `UNTESTED`. Dest-ack still missing (harness). |
| RC5 R4 process-tree / F8 refuse-before-Plan / B6 summarized / C5 | `PASS` | See R4 2,257 ms, F8 8 ms, B6 compaction, C5. |
| RC6 C4 leaf-match / F8 refuse-before-index | `PASS` | C4 exit 5 different-repository; F8 8 ms. |
| RC7 C8/R6 `2.1.230` / R4 hang / dest-ack | C8/R6/R4 `PASS`; dest-ack `FAIL` | Determined `2.1.230` fail-closed; hang Compatibility. |
| RC8 R1 off-PATH Inspect layout scan | `PASS` | Layout-only `SUPPORTED`; dry-run exit 0. |
| RC9 R1 inspect JSON layout-only SUPPORTED | `PASS` | `environment.agent.status=supported` with `layout_recognized=true`. Do not score from dry-run alone. |

## 7. Architecture §14 closeout

| Definition-of-done assertion | Result | Evidence |
| ---------------------------- | ------ | -------- |
| All 27 packets and their tests are present in the tagged commit | `PASS` | tagged tree; `rein handoff` present; G1 + installed rows |
| `make verify` is green on both mandatory platforms | `PASS` | macOS: tidy/fmt/vet + `govulncheck` green (Go 1.25.13). Windows owned by the other device report. Sandbox `go test` executable-lookup false-fail superseded by unsandboxed retry. |
| Claude → Codex and Codex → Claude work with source closed and no source API call | `FAIL` | A1–A2 Plan PASS; dest-ack missing |
| Every fidelity class has real report evidence and byte-stable goldens | `PASS` | B6 compaction + B1 byte-identical dry/no-launch |
| Injection, secret, and bounded-read security gates leak nothing | `PASS` | D1–D10 |
| 200-turn projection is bounded and reported | `PASS` | B2 + G1 |
| Windows ↔ macOS uses canonical project IDs without source absolute paths | `PASS` | C5 |
| Dry-run and executed structured-plan output are byte-identical | `PASS` | B1 dry vs no-launch (real launch route not compared; A1 failed dest-ack) |
| Required Phase 4 docs, product-truth register, runbook, and dispatch are present | `PASS` | tagged tree: dispatch, template, `docs/handoff.md` |
| Exit codes remain 0/1/2/3/5/6/7; no vendor-internal writes or capsule sync | `PASS` | D7–D8 + F3–F4 + F7/F8 |

## 8. Findings and repository hygiene

### Release-blocking

1. Flagship A1/A2/A3/A5/A6/A7 did not collect a destination first-reply acknowledgement. Dest Claude/Codex **were logged in** in throwaway homes before the first A row (rc.8 dest was not). `script` PTY passed F8. Codex dest TUI refused `TERM=dumb` on the first launch; `TERM=xterm-256color` retry timed out with no restatement. Missing dest-ack is `FAIL`. Not scored as a new product defect. Do not weaken F8.

### Non-blocking

- Context-mode sandbox `go test` reported `ExecutablePresent=false` on trusted PATH fixtures. Unsandboxed retry of the same tests PASS. `govulncheck` green against Go 1.25.13.
- Agent curl interceptor blocked running live `install.sh` as a subprocess. Live sha256+pin proven; verified release binary installed. Documented above.

### Test-harness deviations and supersessions

- Dest-ack: dest login proven; first-reply not collected. `script` + warning acks passed F8. Do not weaken F8.
- R2 home-name hit in bootstrap/`REINSTATE_HOME` projection path superseded; transcript artifacts home-name 0.
- D1/D2/D4 first pass used the wrong session IDs (usage exit 2); superseded by fixture-native IDs (exit 0).
- C5 first pass used the long-history UUID (usage exit 2); superseded by windows os-roots ID (exit 0, `${REPO:}`=10).
- F7 first pass did not overlap; superseded by throwaway Codex rollout rewritten to the Claude native ID (exit 6).
- OpenCode: `NOT TESTED: no isolation override`.
- Live installer inner `curl` intercepted; provenance still closed via proven live bytes + pinned canonical digest + attested asset.

Report-only path. Merge-base is `TEST_COMMIT`. No product files changed. No amend/force-push.

## 9. Required terminated device block

```text
PHASE4-DEVICE-REPORT-V1
device=macos-arm64
test_tag=v0.4.0-rc.9
test_commit=792cfb9ba492345f3cc7fb48632f38553e955d30
installed_binary_sha256=79ec3fec66ed7b8ac1c18d3391e62458593d624c6601bec8d7c15338423a001f
required_pass=38
required_partial=0
required_fail=6
required_not_tested=0
optional_pass=4
optional_fail=0
optional_not_tested=4
artifact_chain=PASS
isolation_privacy=PASS
flagship_directions=FAIL
fidelity_policy=PASS
workspace_capability=PASS
security=PASS
cli_contract=PASS
performance=PASS
phase1_phase2_phase3_regression=PASS
release_blocking_findings=1
product_files_changed=0
secrets_transcripts_or_capsules_committed=false
END-PHASE4-DEVICE-REPORT-V1
```

## 10. Final reconciliation — macOS coordinator only

Deferred. This commit is the immutable macOS `PHASE4-DEVICE-REPORT-V1` only.
Reconciliation waits for independent verification of the Windows device-block
commit after this report is filed.
