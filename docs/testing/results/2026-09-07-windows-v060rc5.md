# `v0.6.0-rc.5` tagged-artifact native Windows acceptance

`PHASE5-DEVICE-REPORT-V1`

This is the assembled device report for the **published, signed GitHub
prerelease `v0.6.0-rc.5`**, commit `0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9`
(release workflow run `34124721837`). It satisfies
[`../phase-5-universal-agent-coverage-acceptance.md`](../phase-5-universal-agent-coverage-acceptance.md)
as specialized by [`../v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md)
and [`../v0.6.0-rc.5-agent-verification-prompts.md`](../v0.6.0-rc.5-agent-verification-prompts.md).
It merges five executors' part files
(`2026-09-07-windows-v060rc5-part-{a,b,c,d,e}.md`, all against this same tag)
into the one cumulative report the dispatch requires, plus an assembler's own
independent install and matrix-generation check (§0). The five part files are
removed from the tree in the same commit as this file; their content is
preserved here.

**A first attempt at this run on the same artifact** (kept as branch
`v060/rc5-tagged-attempt1`, head `df1996a9`) **was inconclusive: executor A
had run an older `rein` from the user `PATH`, executor B had no console for
`conptydriver`, and executor E was ended during its `H7` wait; parts C and D
of that attempt passed cleanly and are reused unchanged.** This report's
part C and part D files (`2026-09-07-windows-v060rc5-part-c.md`,
`2026-09-07-windows-v060rc5-part-d.md`) are that first attempt's own
committed evidence, carried forward onto `v060/rc5-tagged` at `6e84c2d5`
("carry parts C and D from the first attempt"); parts A, B, and E in this
report are fresh, from this branch's own executors.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Matrix generated by:** `rein doctor --agents --acceptance-matrix --json`
  (re-run independently by the assembler against a fresh install; see §0)
- **Row accounting:** `216` rows total (`178` Phase 5 generated matrix +
  `22` CLI experience + `16` Hop parity journeys) — `1` row is
  `N/A (definitional)` (`opencode:D4`) — **`215` required rows**.
- **Host-credential rows:** `0`. No row this run is recorded
  `NOT TESTED (host credential)`.
- **Required counts:** `211 PASS / 4 PARTIAL / 0 FAIL / 0 NOT TESTED` of
  `215` required rows.
- **Release-blocking findings:** `4` required rows are not `PASS` and are
  not excused by either disposition in
  [`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict):
  `grok:E1`, `grok:E2`, `grok:E3`, `MatrixH:H7`. See §16 and §20.

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required
result is `FAIL`. Because 4 of the 215 required rows are `PARTIAL` and are
not covered by an applicable disposition, the device verdict is `FAIL` and
this report does not authorize stable `v0.6.0`. No product code was changed
by this assembly; every result below is transcribed from the five part
files' own evidence.

**Every one of this candidate's own five targeted fixes is confirmed.**
`MatrixB:B4` (probe redaction of a plain project-name segment), `MatrixB:B7`
(empty-vs-absent override root distinguishability), `MatrixG:G4` (corrected
contract wording), CLI row `13` (the interactive switcher's `ScopeAll`
readiness defect), and `opencode:E5`/`opencode:E6` (the version ceiling
widened to `1.18.21`–`1.18.29`) are all `PASS` this run — clearing every one
of the six blocking findings `v0.6.0-rc.4`'s tagged run recorded as fixable
by this candidate. See §18.

**This run leaves two blocking dispositions**, one carried and one new:

- `MatrixH:H7` remains `PARTIAL`, now under the **corrected** lab-isolation
  rule (a fresh **Hop** account, not a fresh Windows account — issue #424).
  This run's executor correctly completed every prerequisite the corrected
  rule requires (a fresh Hop account with zero pushed sessions, the
  before-listing digests, the staged elevated script) and started the
  go-signal poll only after every other row in its part was done, but no
  maintainer go-ahead arrived inside this run's own poll window. The row's
  own mechanism (`daemon install` through real `schtasks`) was never
  reached; no live-agent-home write occurred.
- `grok:E1`, `grok:E2`, `grok:E3` are newly `PARTIAL` this run
  (`F-GROK-BACKEND-CONNECTIVITY`) — a live vendor account/model-endpoint
  connectivity problem reproduced independently of `rein`, including against
  the host's own live, non-isolated `~/.grok`. `rein`'s own launch plan
  (`grok --resume <id>`) is unchanged and confirmed correct: the resumed
  session's history replays intact and the process is correctly detected as
  active (`grok:E5` `PASS`); only the completion request itself never
  finished within any window this run could budget.

### Blocking rows (4, none pass a required row)

| Row | Result | Category | Reason (full evidence in the matrix sections below) |
| --- | ------ | -------- | ---- |
| `grok:E1` | PARTIAL | host/harness, new this run | Resume launch and history-replay confirmed correct; the recall completion itself never finished across three real attempts (headless 25+ min, ConPTY ~60s, ConPTY 8-min budget with a visible `Connection` error) on this host today. Not a `rein` defect. `grok:E1` was `PASS` at `v0.6.0-rc.4` — a disposition regression caused by host/harness drift, not by this candidate's own change |
| `grok:E2` | PARTIAL | same reason | Same basis as `grok:E1` — the resumed session is confirmably the requested one (history replay proves it), but the in-agent recall confirmation could not be completed |
| `grok:E3` | PARTIAL | same reason | Not attempted live given `E1`/`E2`'s outcome on the identical connectivity path (a fork-and-recall exchange needs the same completion request that failed to complete three times) |
| `MatrixH:H7` | PARTIAL | operator/harness availability, re-recorded reason | This run correctly completed the corrected lab-isolation prerequisite (fresh Hop account, before-digests, staged elevated script) — unlike `v0.6.0-rc.4`'s run, which misread the rule as a fresh Windows account and could not even set that up — but the maintainer go-signal never arrived inside this run's own poll window. `H7` was also `PARTIAL` at `v0.6.0-rc.4`, for a different practical reason (a prerequisite that could not be built at all, vs. this run's correctly-built prerequisite with no maintainer reachable) |

None of the four is attributable to a change this candidate made. Three are
a host vendor-CLI backend/connectivity problem, live and reproduced
independent of `rein` (`grok:E1`/`E2`/`E3`); one is an operator/elevation
availability gap this run's harness correctly refused to route around
(`H7`), now for the second consecutive tagged run.

### Second recheck outcome (2026-09-07 night, §24)

A second recheck executor re-tested the same four blocking rows a second
time against this same artifact, in this same worktree
(`D:\Projects\reinstate-worktrees\v060-rc5-tagged`, branch
`v060/rc5-tagged`), in its own fresh lab under
`D:/ReinstateAcceptanceProjects/v060-rc5-recheck2/`. Artifact identity was
re-proven again (archive hash matches `checksums.txt`; installed binary's
`version --json` reports the same tag and commit this whole report covers).

- **`grok:E1`/`E2`/`E3`: still `PARTIAL`.** The live backend connectivity
  failure (`F-GROK-BACKEND-CONNECTIVITY`) reproduced twice more this round
  (a headless `grok -p "Reply PONG"` timeout, and a real `conptydriver`
  session whose message count never advanced past `1`), a fifth and sixth
  independent confirmation across both recheck rounds. The live `grok`
  compatibility block (`1.0.13` outside `rein`'s verified `1.0.5` range,
  `rein resume --dry-run --json` exit `5`) is unchanged from the first
  recheck — the host has not drifted further.
- **`MatrixH:H7`: still `PARTIAL`, now with a conclusive per-file
  attribution.** The go-signal was found already present again at this
  round's first poll check, and the mechanism ran to completion a second
  consecutive time (`daemon install`/`status`/`stop`/`start`/`uninstall`
  through real `schtasks`, a real push and pull). The same isolation defect
  the first recheck diagnosed reproduced identically: the elevated,
  Task-Scheduler-spawned process read from and pushed real snapshots from
  two of the host's live agent-home roots, not the isolated ones this run
  staged, and one push this time recorded a conflict against an unrelated
  synthetic fixture session already resident on this shared host (not any
  real private data). Unlike the first recheck, this round preserved a full
  per-file before/after listing (not just the aggregate digest) for two of
  the three live roots, and every changed line traces to ordinary,
  concurrent host activity (backup rotation, file-history writes from this
  very recheck session's own tool use, other unrelated sessions' growth,
  SQLite WAL churn) — none of it attributable to `rein push` (read-only) or
  `pull` (`0` snapshots pulled). This round also disclosed its own new gap:
  the third root's before-listing was captured against the wrong path (this
  shell's own transient `CODEX_HOME`, not the persistent one the elevated
  process actually used), so no valid before/after comparison exists for
  that root this round. See §24 for full evidence.
- **Final required counts after this second recheck: `211 PASS / 4 PARTIAL
  / 0 FAIL / 0 NOT TESTED` of `215` required rows — unchanged.**
- **Final device verdict: `FAIL`, unchanged.** This recheck does not
  authorize stable `v0.6.0`.

## 0. Assembler verification

Performed independently of, and in addition to, all five part files, in the
assembler's own fresh directory
(`D:\ReinstateAcceptanceProjects\v060-rc5-assembler\`), never touching any
part executor's own lab directories or the shared worktree's tracked files
before this commit.

1. **Checksum.** `sha256sum` of the coordinator's staged
   `reinstate_0.6.0-rc.5_windows_amd64.zip` matched `checksums.txt`
   (`a648f1de65c15bea7926dda2dd0ce48110d512c9bc60a23b5988c3b24ded9f09`)
   independently.
2. **Install.** Unzipped into a fresh directory
   (`D:\ReinstateAcceptanceProjects\v060-rc5-assembler\install\`), never a
   shared or developer binary. `rein.exe`/`reinstate.exe` `sha256sum`: both
   `aa74899f68356ea5127fa8129db56729c137276d255f1d8890fa9dcf276dd35f`,
   confirmed identical.
3. **Version.** `rein version --json`:
   `{"commit":"0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9","date":"2026-09-07T12:59:29Z","name":"reinstate","version":"0.6.0-rc.5"}`
   — matches every part file's own artifact-identity table exactly.
4. **Required row count.** `rein doctor --agents --acceptance-matrix --json`
   against this fresh install: top-level `"row_count": 178`,
   `"core_row_count": 33` (`A:10, B:9, G:8, H:6`), 18 agents at tiers
   `T0×7, T1×4, T2×2, T4×2, T5×3` (`claude, codex, opencode` at T5;
   `grok, qwen` at T4; `gemini, kimi` at T2; `cline, copilot, cursor, pi` at
   T1; `aider, amp, antigravity, minimax-code, openhands, roo, zcode` at
   T0) — matches the dispatch's held census exactly (no tier promotion this
   candidate).
5. **Row-set reconciliation.** Programmatically read the full set of 178
   expected row identifiers from that JSON (33 core rows + each agent's own
   row list) and compared it against the union of every row id assigned to
   part A (core Matrix A/B/G/H, Matrix F for all 7 T0 agents, Matrix C for
   the 4 T1 agents — 71 Phase-5 rows) and part B (Matrix C/D/E for the 7 T2+
   agents — 107 Phase-5 rows): **the two sets are set-equal, 178 elements
   each, zero overlap, zero gap.** Every required Phase 5 row appears in
   exactly one part file.
6. **Part self-report cross-check.** Recounted every part's own row table
   cell-by-cell against its own committed evidence:
   - Part A: 8 gates + 33 core (A10/B9/G8/H6) + 14 T0 (7 agents × F1/F2) +
     24 T1 (4 agents × C1–C6) = **79 rows, all PASS** — matches its own
     "Counts" section exactly.
   - Part B: 107 Phase-5 rows (`claude`/`codex`/`opencode`/`grok`/`qwen` ×
     17 each + `gemini`/`kimi` × 11 each), of which `opencode:D4` is
     `N/A (definitional)` — **103 PASS, 3 PARTIAL (`grok:E1`/`E2`/`E3`), 1
     N/A** — matches its own "Row accounting" section exactly, plus the T5
     push/pull round trip (PASS, on the corrected second attempt — see
     `F-T5-BACKEND-CONTAMINATION` in §21).
   - Part C: 22 rows, **22 PASS** — matches ("22/22 PASS").
   - Part D: 11 rows, **11 PASS** — matches ("11/11 PASS").
   - Part E: 5 rows, **4 PASS, 1 PARTIAL** (`H7`) — matches
     ("4/5 PASS, 1 PARTIAL").

   **No correction was needed to any part's counts this run.**
7. **Total reconciliation.** `71` (part A) + `107` (part B, including
   `opencode:D4`) + `22` (part C) + `11` (part D) + `5` (part E) =
   **`216`**, matching the dispatch's total row count exactly; minus the
   one `N/A (definitional)` row = **`215`** required. PASS sum: `71` (A) +
   `103` (B) + `22` (C) + `11` (D) + `4` (E) = **`211`**. PARTIAL: `3`
   (`grok:E1`/`E2`/`E3`, part B) + `1` (`H7`, part E) = **`4`**. FAIL: `0`.
   NOT TESTED: `0`. `211 + 4 + 0 + 0 = 215`.
8. **Cross-check note resolution (part C's flagged discrepancy).** Part C's
   own file records a "Cross-check note" flagging that a *different*,
   uncommitted executor-A report (from the inconclusive first attempt —
   see the header above) had claimed the archive binary internally reports
   `0.3.0-rc.6` and lacks the acceptance matrix, the readiness prober fix,
   the handoff studio, and `init --link`/`--paste`. **This is resolved by
   this session's own §0.2–§0.4 above and by this report's actual,
   committed part A file (`b144b726`, this branch's fresh executor-A run,
   not the first attempt's)**: neither records any such finding, both
   independently confirm `version=0.6.0-rc.5`, `row_count=178`, and exercise
   the acceptance matrix, the fixed readiness prober (CLI row 13), the
   handoff studio (CLI rows 16/17), and `init --link`/`--paste` (CLI row
   20, H1–H12) directly. The stale-binary symptom (`0.3.0-rc.6`, ~4 of 18
   catalog keys) is the exact shape of a `PATH` lookup resolving a much
   older, pre-existing `rein.exe` ahead of the run's own fresh install — the
   documented harness trap this run's context explicitly names as the first
   attempt's own executor-A defect ("executor A had run an older `rein`
   from the user `PATH`"), independently corroborated by this run's own
   part A ("Host user `PATH` resolves 4+ older `rein.exe` installs before
   any of this run's directories") and part C ("the ambient host shell's
   `PATH` carries real, installed vendor CLIs… a stray Windows `PATH`
   resolution trap"). Part C's own 22 rows are not affected — they never
   depended on part A's disputed finding, and this session's own checksum
   and version spot-check (§0.2–§0.3) independently confirms the identical
   archive part C tested. **No product defect; the first attempt's
   executor-A part is not part of this report and is superseded by this
   branch's own part A.**
9. **Disposition-rule check.** Applied
   [`v0.6.0-windows-acceptance.md`'s "Dispositions that do not block the
   device verdict"](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict)
   rule mechanically to every non-`PASS` required row this run recorded.
   - `opencode:D4` — `N/A (definitional)`, unchanged reason (OpenCode's
     SQLite-only store has no JSONL record boundary). Excluded from both
     the denominator and the numerator, as required. Not a disposition on
     a required row.
   - `grok:E1`, `grok:E2`, `grok:E3` — recorded `PARTIAL`, not
     `NOT TESTED`. `NOT TESTED (host credential)` is the only named
     disposition that can excuse an optional agent's row, and it requires
     both the literal `NOT TESTED` label **and** a credential (expired or
     absent) as the cause. Neither holds here: the label is `PARTIAL`, and
     the cause is a live connectivity/backend failure with the credential
     itself confirmed working (the same `grok` session's `C1`–`D5` rows,
     including `C3`'s own completion, all `PASS` in this isolated
     `GROK_HOME`). **All three block.**
   - `MatrixH:H7` is `PARTIAL`, not `NOT TESTED` — `PARTIAL` is not one of
     the two named dispositions at all, and the contract's "Everything
     else" clause applies: `PARTIAL` does not pass a required row.
     **Blocks.**
   - **Zero rows this run qualify for either named disposition** beyond
     `opencode:D4`'s definitional exclusion.
10. **Cleanup.** The assembler's own install and matrix output under
    `D:\ReinstateAcceptanceProjects\v060-rc5-assembler\` are deleted after
    this report is committed; nothing from that directory is referenced by
    path elsewhere in this report.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-09-07` (span across all five parts; each part's own timestamp is in its section below); assembler verification `2026-09-07` |
| Device | `windows-amd64` |
| OS/version/build | Windows 11 Pro, `10.0.26200`, native `windows/amd64`, never WSL |
| CPU architecture/native process | `amd64`, native process (no emulation) |
| Filesystem | NTFS |
| Tested tag | `v0.6.0-rc.5` |
| Tested full commit | `0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9` |
| Installed binary SHA-256 | `aa74899f68356ea5127fa8129db56729c137276d255f1d8890fa9dcf276dd35f` |
| Installed version JSON | `{"commit":"0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9","date":"2026-09-07T12:59:29Z","name":"reinstate","version":"0.6.0-rc.5"}` |
| Catalog agent count | `18` |
| Catalog tier census | `T0: 7, T1: 4, T2: 2, T4: 2, T5: 3` (unchanged from `v0.6.0-rc.4`) |
| Generated required row count | `178` (`rein doctor --agents --acceptance-matrix --json`, independently reproduced by the assembler in §0 and by part A) |
| Git version | `2.52.0.windows.1` |
| Go version/toolchain | `go1.25.13` (pinned via `GOTOOLCHAIN`; host default `go1.26.1`) |
| Report branch | `v060/rc5-tagged` |
| Device-report commit | filled in by the commit that lands this file |
| Draft report PR | NOT CREATED (worktree report, per this run's dispatch — no PR required) |

### Bootstrap deviation (applies to every part below except executor A)

Per this run's ground rules, only executor A installs from the live
`https://reinstate.dev/install.ps1` bootstrap and records that as the
artifact identity; every other executor (B, C, D, E) and the assembler
installed from the coordinator's pre-verified, checksummed draft directory
(`…/scratchpad/rc5-draft/`) instead — the assigned methodology, not an
unplanned deviation. Executor A's own bootstrap check (part A): the live
bootstrap was saved fresh with `Invoke-WebRequest -OutFile`, its pinned
version line reads `$Version = "v0.6.0-rc.5"`, and installing from it
(with `INSTALL_DIR` pointed at a fresh directory and
`REINSTATE_BOOTSTRAP_PATH_SCOPE=Process` to avoid touching the host's live
PATH or any user-installed binary — a script-documented override, no
bypass of the pinned installer's own verification) produced binaries
byte-identical to (and reporting the same version/commit/date as) the
checksummed archive — **no deviation** between the live bootstrap route
and the release-commit candidate.

### Artifact identity (common to all parts)

| Field | Value |
| ----- | ----- |
| Tag | `v0.6.0-rc.5`, published GitHub prerelease, signed tag verified against `.github/allowed_signers` (coordinator, pre-run; also confirmed by part A: `git tag -v`, tagger Harjot Singh Rana, object type `tag`) |
| Full commit | `0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9` |
| Release workflow run | `34124721837` |
| Windows archive | `reinstate_0.6.0-rc.5_windows_amd64.zip` |
| Archive SHA-256 | `a648f1de65c15bea7926dda2dd0ce48110d512c9bc60a23b5988c3b24ded9f09` (matches `checksums.txt`; re-verified independently by every part and the assembler) |
| Installed binary SHA-256 | `aa74899f68356ea5127fa8129db56729c137276d255f1d8890fa9dcf276dd35f` — `rein.exe`/`reinstate.exe` byte-identical in every install |
| `rein version --json` | `{"commit":"0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9","date":"2026-09-07T12:59:29Z","name":"reinstate","version":"0.6.0-rc.5"}` |
| Previous-release comparison binary (rows 2, 22, G7) | `reinstate_0.5.1_windows_amd64.zip`, checksum-verified |

## 2. Agent inventory on this device

| Agent key | Installed | Vendor version | Sessions available | Distinct projects | Declared tier | Tier testable here |
| --------- | --------- | -------------- | ------------------- | ----------------- | -------------- | -------------------- |
| `claude` | YES | `2.1.263` | 66 real | 63 | T5 | yes |
| `codex` | YES | `0.149.0` | 100 real (limit) | 20 | T5 | yes |
| `opencode` | YES | `1.18.29` (within the widened `1.18.21`–`1.18.29` ceiling) | real sessions | 7 | T5 | yes — full re-test target confirmed, `E5`/`E6` `PASS` |
| `grok` | YES | `1.0.5` | 3 (isolated) | 1 | T4 | yes, `E1`/`E2`/`E3` **PARTIAL** this run — live backend connectivity, not a launch-plan defect (see §12, §16) |
| `qwen` | YES | `0.21.12` (within `0.21.12`–`0.23.0`) | 1 (isolated) | 1 | T4 | yes — every row `PASS` |
| `gemini` | YES (live default home) | `0.53.0` | 28 real | 17 | T2 | yes — `B4` re-test `PASS` this run |
| `kimi` | YES | `0.36.1` | 5 real | 4 | T2 | yes |
| `pi` | YES | (host) | 9 real (8 pre-existing + 1 created) | 8 | T1 | yes |
| `cursor` | YES (`cursor-agent` itself still broken on this host) | (host) | 2 real | 2 | T1 | yes — read-only host-store discovery, per the T1 exception |
| `copilot` | YES | (host) | 3 real (2 pre-existing + 1 created) | 3 | T1 | yes |
| `cline` | YES | (host) | 11 real (10 pre-existing + 1 created) | 7 | T1 | yes — `B7` re-test `PASS` this run |
| `aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode` | ABSENT | — | — | — | T0 | yes (refusal/no-capability rows only) |

## 3. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------- |
| Annotated tag signature verifies against `.github/allowed_signers` | PASS | Coordinator pre-run; part A's own `git tag -v` confirms |
| Tag peels to the recorded full commit on `origin/main` | PASS | `0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9`, the worktree's own merge base and `git log` ancestor |
| Published release is non-draft, prerelease, and tied to the tag | PASS | `gh release view v0.6.0-rc.5 --json` (read-only): `isDraft:false, isPrerelease:true`, tag `v0.6.0-rc.5` (part B) |
| Exact expected release asset set is present | PASS | `verify-release.ps1` (A6/A7) |
| Checksums, GitHub API digests, and attestations match the tag and commit | PASS | Coordinator pre-verified (`checksums.txt` match, `gh attestation verify` on the Windows archive); independently re-verified by every part and the assembler |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | PASS | `verify-release.ps1`/`test-install.ps1` (A6/A7) |
| Live bootstrap is byte-identical to the tested commit and pins only this candidate | PASS | Executor A: pinned version line `v0.6.0-rc.5`, resulting binary byte-identical to the checksummed archive |
| Bootstrap installer digest matches the tagged canonical installer | PASS | Executor A |
| Fresh isolated install did not replace a user binary or persist PATH changes | PASS | Every part and the assembler installed into its own fresh directory; the bootstrap route used `REINSTATE_BOOTSTRAP_PATH_SCOPE=Process` to avoid touching the host's live PATH |
| `rein` and `reinstate` have identical verified bytes and report the candidate | PASS | SHA-256 `aa74899f68356ea5127fa8129db56729c137276d255f1d8890fa9dcf276dd35f` throughout every install, including the assembler's own |
| Installed binary reports the literal full tested commit | PASS | `rein version --json` matches `0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9` in every install |

## 4. Automated gates (Section A of the dispatch; not part of the 216)

Source: part A, run once from the worktree per the ground rules.

| # | Gate | Result | Evidence |
| - | ---- | ------ | -------- |
| A1 | Format, vet, tidy | PASS | `gofmt -l .`, `go vet ./...`, `go mod tidy -diff` all empty, exit 0 |
| A2 | Unit suite | PASS | `CGO_ENABLED=0 go test ./... -count=1 -p4`: 70 packages `ok`, 0 `FAIL` |
| A3 | Race suite | PASS | `CGO_ENABLED=1 go test -race ./internal/... -count=1 -p4`: 66 packages `ok`, 0 `FAIL`, 0 data races |
| A4 | Lint and vuln | PASS | `golangci-lint v2.11.4`: `0 issues` (after quarantining a stray untracked helper dir left by another executor, see §21); `govulncheck v1.6.0`: `0` reachable vulnerabilities |
| A5 | Doc gate | PASS | `internal/doctest` `ok`, 8.5s; `scripts/check-docs.ps1` exit 0 |
| A6 | Snapshot and artifacts | PASS | `snapshot.ps1` + `stage-release-assets.ps1` + `check-release-artifacts.ps1` + `check-release-binary-identity.ps1` all exit 0 |
| A7 | Installers | PASS | `verify-release.ps1`/`test-install.ps1` re-verified against the coordinator's `rc5-draft`, both exit 0 |
| A8 | Cross-OS build | PASS | `GOOS=darwin` and `GOOS=linux` `go build ./...` both exit 0 (after the same quarantine as A4) |

**8/8 PASS.**

## 5. Matrix A — Catalog integrity (10/10 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| A1 | Descriptors validate at start; no duplicate/empty keys | PASS | `doctor --agents --json`: 18 agents, all keys unique |
| A2 | `doctor --agents` lists every agent incl. every T0 | PASS | All 7 T0 keys present with `installed: no` and a `t0_reason` |
| A3 | Every T0 agent shows an enumerated reason | PASS | `layout_unverified` (aider, antigravity, minimax-code, roo), `server_backed` (amp, openhands), `desktop_only` (zcode) |
| A4 | Declared tier and capability constructors agree | PASS | `TestShippedAgentsRegisterAtDeclaredTiers`/`TestMustRegisterAcceptsConstructorsAtDeclaredTier` |
| A5 | Every declared evidence path exists in the tagged tree | PASS | `TestShippedEvidenceIsComplete` |
| A6 | `sessions --agent` accepts exactly T1+ keys, else exit `2` | PASS | `claude`/`pi` exit 0; `aider`/an unknown key exit 2 |
| A7 | `resume` accepts exactly T3+ keys, else exit `5` with reason | PASS | `claude` (nonexistent id) exit 2 (not-found); `pi` exit 5; `aider` exit 5 |
| A8 | Agent ordering is deterministic across runs | PASS | 3 consecutive `doctor --agents --json` runs, byte-identical ordering |
| A9 | Broken evidence path fails conformance | PASS | `TestBrokenEvidencePathFails`/`TestBrokenEvidencePathIsCaught` |
| A10 | No scan writes, renames, or locks under any agent root | PASS | Before/after digest across 6 real agent roots; `cline`/`copilot`/`cursor`/`pi`/`codex` byte-identical; the live, actively-in-use `claude` root showed exactly 2 changed files, both append-only growth in a different, concurrently-running Claude Code session on this shared host — not this run's own scan |

## 6. Matrix B — Probe and redaction (9/9 PASS)

Source: part A. **`B4` and `B7` are this candidate's own re-test targets, both fixed and confirmed.**

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| B1 | Output conforms to `AGENT-PROBE-V1` | PASS | `"schema": "AGENT-PROBE-V1"` |
| B2 | No absolute path, home name, or username | PASS | Full-document scan: 0 hits |
| B3 | No JSON values from session files, keys only | PASS | `first_line_keys` entries are field-name arrays only |
| B4 | Names shape-normalized | **PASS — confirmed fixed** | Per the dispatch's specific re-test: a synthetic `GEMINI_CLI_HOME` fixture with one plain ordinary project directory name and one `<prefix>_<32-hex>`-shaped directory name, both under `tmp/*/chats/`; `doctor --agents --json`'s `tree` collapsed **both** into the `<slug>` token at every level, including the top-level `tmp/*` segment — no raw prefix or hex leaked. `go test ./internal/agents/probe/...` also passes, including `TestGeminiTmpProjectDirectoriesAreShaped` |
| B5 | No excluded subtree present | PASS | Grep for credential/token/secret substrings: 0 matches; `TestCredentialFileIsExcludedFromTree`/`TestExcludedIsNotOpened` |
| B6 | Absent agent reported without error | PASS | All 7 T0 agents: `candidate_roots: []`, no error, exit 0 |
| B7 | Empty root distinguishable from absent root | **PASS — confirmed fixed** | Per the dispatch's specific re-test: `CLINE_DATA_DIR` pointed first at an existing empty directory, then at a nonexistent path — the two `candidate_roots` entries differ in `exists` (`true` vs `false`), never byte-identical apart from the timestamp |
| B8 | Wrapper scripts match binary output | PASS | `agent-storage-probe.sh`/`.ps1` identical to the direct binary invocation apart from `generated_at` |
| B9 | Secret scanner passes over committed probes | PASS | In-tree scan tool over all 26 files under `docs/testing/results/agent-probes/`: `0` hits |

**Both `B4` and `B7` are the exact fixes this candidate's dispatch describes:**
every captured shape-rule prefix is now validated against the vendor
allowlist the same way a whole stem is (`B4`); every root-override
contributes its own `candidate_roots` entry carrying `exists`/
`marker_present` (`B7`).

## 7. Matrix G — No regression (8/8 PASS)

Source: part A. **`G4` is this candidate's own re-test target (corrected contract wording), confirmed `PASS`.**

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| G1 | Claude/Codex native resume and fork unchanged | PASS | Real, isolated sessions with a distinct planted token each. `claude --resume <id> -p "..."` and `codex exec resume <id> "..."` both correctly answered from history. `rein fork claude:<id> --dry-run --json` names the correct native fork flag (`--fork-session`); a full physical fork-and-verify was not additionally performed given this row's time budget within part A's scope |
| G2 | Both structured handoff directions complete | PASS | `handoff claude:<id> --to codex --dry-run --json` and `handoff codex:<id> --to claude --dry-run --json`, using the G1 sessions, both completed, exit 0 |
| G3 | Gemini, OpenCode, Grok remain sources; Gemini stays read-only | PASS | `gemini.go` declares `Tier: agents.TierHandoffFrom`; `TestShippedAgentsConformance/gemini` and `/grok` pass |
| G4 | `push`/`pull` (no `--agent`) carry exactly Claude Code, Codex CLI, OpenCode, and no other agent's — **corrected wording** | **PASS — confirmed fixed** | Source-level: only `claude.go`, `codex.go`, `opencode.go` declare `Tier: agents.TierSync` in the whole catalog; the full `internal/agents/conformance` suite passes. Confirmed independently by `push --agent`/`pull --agent` shell completion (H6, below): exactly `claude, codex, opencode` |
| G5 | Cross-OS path remapping unchanged | PASS | `internal/pathmap` suite green (Windows↔Windows only; the macOS leg is E-deferred) |
| G6 | Exit-code semantics unchanged; no new codes | PASS | Exit codes `0, 2, 5, 7` observed consistently, matching the documented contract |
| G7 | An index built by an older release upgrades without data loss | PASS | Fresh index built with a real, checksum-verified `v0.5.1` Windows binary against a real Claude Code project: 66 sessions indexed. Opened the same `REINSTATE_HOME` with `v0.6.0-rc.5`: same 66 sessions returned, exit 0, no data loss |
| G8 | Capsules remain excluded from sync | PASS | `TestRefuseHandoffsPush` proves the push engine refuses any path under `handoffs/` |

**`G4` is fixed exactly as the dispatch describes:** the contract's row text
now reads "Claude Code, Codex CLI, OpenCode at T5, and no other agent's,"
matching the unchanged shipped behavior every prior candidate's own `G4`
evidence already exercised.

## 8. Matrix H — CLI contract and performance (6/6 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| H1 | CLI reference matches shipped flags | PASS | `TestPhase4CLIFlagsAreDocumented` (`internal/doctest`, A5) |
| H2 | `--json` shapes match documentation | PASS | Full green `internal/doctest` suite; manual inspection of `sessions`/`search`/`inspect`/`doctor --agents` all well-formed |
| H3 | Full refresh within documented ceiling | PASS | Cold full refresh: **14.03s**, within the ~20–30s ceiling (consistent with `v0.6.0-rc.4`'s `14.2s`) |
| H4 | No-change refresh materially faster | PASS | Warm refresh: **3.68s**, ≈3.8× faster |
| H5 | One slow or unreachable source does not block others | PASS | `TestSlowAgentDoesNotDiscardTheRun` |
| H6 | Completion lists correct agent keys per command | PASS | Per the dispatch's specific instruction: `rein __complete push --agent ""` and `pull --agent ""` → `claude, codex, opencode` (matches G4's corrected wording exactly); `sessions --agent ""` → the 11 T1+ keys plus `all` |

## 9. Matrix F — Per T0 agent (14/14 PASS)

Source: part A. All 7 T0 agents, not installed on this host — a scope
reduction, not a `FAIL`.

| Agent key | F1 listed with reason | F2 no capability offered or implied |
| --------- | --------------------- | ------------------------------------ |
| `aider` | PASS — `t0_reason=layout_unverified` | PASS |
| `amp` | PASS — `t0_reason=server_backed` | PASS |
| `antigravity` | PASS — `t0_reason=layout_unverified` (host has the Antigravity IDE installed; the T0 catalog agent still reports no capability) | PASS |
| `minimax-code` | PASS — `t0_reason=layout_unverified` | PASS |
| `openhands` | PASS — `t0_reason=server_backed` | PASS |
| `roo` | PASS — `t0_reason=layout_unverified` | PASS |
| `zcode` | PASS — `t0_reason=desktop_only` | PASS |

## 10. Matrix C — Per T1+ agent (66 rows: 66 PASS)

Per the ground rules, T1 discovery rows ran read-only through `rein` alone
against the host's real root; corruption rows used a copy, never live data.

### T1 agents (source: part A)

`cline:C3`/`pi:C3` created fresh throwaway sessions through the vendor's
own live CLI with a planted, non-sensitive search token.
`cursor:C1`–`C6` ran against the host's real store under the T1-discovery
exception (`cursor-agent` itself is broken on this host).

| # | cline | copilot | cursor | pi |
| - | ----- | ------- | ------ | -- |
| C1 (≥2 sessions, ≥2 projects) | PASS — 11 sessions / 7 projects | PASS — 3 sessions / 3 projects | PASS — 2 sessions / 2 projects | PASS — 9 sessions / 8 projects |
| C2 (metadata matches vendor) | PASS — `project=cline-project`, `message_count=2` (nonzero) | PASS — `project=copilot-project`, `message_count=5` | PASS — `message_count` values `[3, 3]` | PASS — `project=pi-project`, `message_count=2` |
| C3 (message-body search) | PASS — planted token found via `rein search` | PASS — planted token found | PASS — a word from a real session's own `prompt_preview`, exactly 1 match (word/preview withheld per policy) | PASS — created via `pi -p`, found by search, `prompt_preview` populated |
| C4 (bounded inspect, no body) | PASS | PASS | PASS | PASS |
| C5 (resume gate correct for tier) | PASS — exit 5 | PASS — exit 5 | PASS — exit 5 | PASS — exit 5 |
| C6 (corrupt/empty/absent degrade cleanly) | PASS — truncated final record → `session_read_failed` warning, exit 0; empty/absent clean | PASS — truncated `events.jsonl` → same shape; empty/absent clean | PASS — corrupted `meta.json` copy → same shape; empty/absent clean | PASS — truncated final record → same shape; empty/absent clean |

**24 rows: 24 PASS.**

### T2+ agents (source: part B; 7 agents × varying rows, all PASS except the disclosed `grok` completion gap in §12)

Real sessions were created in throwaway git repositories with each vendor's
own headless flag and a planted per-agent token
(`RC5B-<AGENT>-<epoch>-<suffix>`) in the message body. `claude`/`codex`
used their live/isolated homes; `grok`/`qwen` used an isolated home seeded
only with the needed credential file; `gemini`/`kimi` used the live default
home.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| C1 (≥2 sessions, ≥2 projects) | PASS — 66/63 | PASS — 100(limit)/20 | PASS — real/7 | PASS — 3/1 | PASS — 1/1 | PASS — 28/17 | PASS — 5/4 |
| C2 (metadata matches vendor) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C3 (search finds planted token, message body) | PASS | PASS | PASS | PASS — completed after ~10 min real wait (see `F-GROK-BACKEND-CONNECTIVITY`, §21) | PASS | PASS | PASS |
| C4 (bounded inspect, no transcript body) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C5 (resume gate correct for tier) | PASS | PASS | PASS | PASS | PASS — `agent.version.status=match` | PASS (T2 refusal) | PASS (T2 refusal) |
| C6 (corrupt/empty/absent degrade cleanly) | PASS — 70%-truncated copy, `incomplete_trailing_record`; empty/absent clean | PASS — same shape | PASS — imported SQLite copy truncated 50%, `session_read_failed`; empty/absent clean | PASS — own single-session copy truncated 70%; empty/absent clean | PASS — same shape | PASS — same shape | PASS — same shape |

Every `qwen` row ran only after `rein inspect qwen:<id> --json` confirmed
`agent.version.status=match` against the real installed `0.21.12` in an
isolated `QWEN_HOME`.

## 11. Matrix D — Per T2+ agent (35 rows: 34 PASS, 1 N/A definitional)

Source: part B. D1/D2/D3/D5 ran through `rein handoff <key>:<id> --to <dest>
--dry-run --json`. D4 used `--no-launch` against a copy truncated to ~70% of
its byte length, with `raw_source.byte_offset`/`artifact_sha256`
independently recomputed from the raw bytes for every agent.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| D1 (capsule + fidelity report) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D2 (no invented content) | PASS — 15 components | PASS — 16 components | PASS — 14 components | PASS — 16 components | PASS — 15 components | PASS — 14 components | PASS — 15 components |
| D3 (unknowns referenced/omitted with reason) | PASS — 1 unrecognized | PASS — 1 unrecognized | PASS — 1 unrecognized | PASS — none this session | PASS — none this session | PASS — none this session | PASS — none this session |
| D4 (truncation boundary, offset + hash, independently recomputed) | PASS — offset 87467, hash matched | PASS — offset 71473, hash matched | N/A (definitional) | PASS — targets `updates.jsonl`, offset 1273, hash matched | PASS — offset 2428, hash matched | PASS — offset 228 (with a copy of `projects.json` for workspace resolution), hash matched | PASS — offset 78891, hash matched |
| D5 (two runs byte-identical) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |

**`opencode:D4` — N/A (definitional), same reason as every prior candidate.**
OpenCode's SQLite-only store has no JSONL record boundary this row's
mechanism applies to. Excluded from the required row count.

## 12. Matrix E — Per T3+ agent (30 rows: 27 PASS, 3 PARTIAL)

Source: part B. Only `claude`, `codex`, `opencode`, `grok`, `qwen` are T3+;
`gemini`/`kimi` (T2) have no Matrix E rows. **`opencode:E5`/`E6` are this
candidate's own re-test targets, both fixed and confirmed.**

| Row | claude | codex | opencode | grok | qwen |
| --- | ------ | ----- | -------- | ---- | ---- |
| E1 (resume launches vendor CLI, session continues) | PASS — planted token recalled | PASS — file-level growth + planted token in reply | PASS — token recalled | **PARTIAL** — launch and history replay confirmed correct; recall never completed (see §16, §21) | PASS — token recalled |
| E2 (resumed session is the requested one) | PASS | PASS | PASS | **PARTIAL** — same basis as E1 | PASS |
| E3 (fork produces a distinct session) | PASS — distinct id `e6dea144…` | PASS — distinct id `01a07c54…` | PASS — distinct id, title suffixed | **PARTIAL** — not attempted live given E1/E2's outcome | PASS — distinct id `7a7b3d1b…` |
| E4 (below-min/above-max both exit 5, naming the range) | PASS — `2.1.219–2.1.263` | PASS — `0.133.0–0.149.0` | PASS — `1.18.21–1.18.29` (widened) | PASS — `1.0.5–1.0.5` | PASS — `0.21.12–0.23.0` |
| E5 (active session detected, resume refused/warned) | PASS — via ConPTY, `agent.active status=present, actual=true` | PASS — via ConPTY | **PASS — this candidate's re-test target** — `agent.version status=match, actual="1.18.29"` **and** `agent.active status=present, actual=true`, the row's own mechanism, not just the version-gate refusal that blocked this row at `v0.6.0-rc.4` | PASS — genuinely active process | PASS — via ConPTY |
| E6 (non-interactive exits 7) | PASS — exit 7 | PASS — exit 7 | **PASS — this candidate's re-test target** — exit 7, reached because the version gate no longer preempts it | PASS — exit 7 | PASS — exit 7 |

E4 evidence (shims below/above the range, all real vendor session ids):

| Agent | Verified range | Below-min shim | Above-max shim |
| ----- | ---------------- | ---------------- | ---------------- |
| `claude` | 2.1.219–2.1.263 | `2.1.218` → exit 5, range named | `2.1.264` → exit 5, range named |
| `codex` | 0.133.0–0.149.0 | `0.132.0` → exit 5, range named | `0.149.1` → exit 5, range named |
| `opencode` | 1.18.21–1.18.29 | `1.18.20` → exit 5, range named | `1.18.30` → exit 5, range named |
| `grok` | 1.0.5–1.0.5 | `1.0.4` → exit 5, range named | `1.0.6` → exit 5, range named |
| `qwen` | 0.21.12–0.23.0 | `0.21.11` → exit 5, range named | `0.23.1` → exit 5, range named |

### `F-GROK-BACKEND-CONNECTIVITY` — full evidence (blocks `grok:E1`/`E2`/`E3`)

`grok`'s plan (`grok --resume <id>`) launched correctly and the vendor
process attached to the requested session, replaying its real prior
history verbatim — proving the recorded session, not a fresh one, was
opened. The recall half of the row's own mechanism (asking the resumed
agent a new question and reading back its answer) was attempted three
times and never completed:

1. Headless `-p`, run to a 25+ minute wall clock, zero output, abandoned.
2. Under `scripts/testing/conptydriver`, `Waiting for response… 59s`, no
   completion within the attempt's budget.
3. Under `scripts/testing/conptydriver` again, budgeted generously:
   `Waiting for response… 7m59s`, with a visible `Connection` error
   indicator in the rendered frame when the driver's own time budget
   expired — genuinely still trying (the raw byte stream kept growing with
   spinner-frame updates the whole time), never producing an answer.

Independently confirmed not `rein`-specific: a 25-second `grok -p`
diagnostic against the host's live, non-isolated default `~/.grok` also
timed out with zero output. This is a live account/model-endpoint
connectivity problem on this host today (a `reqwest error stream: error
sending request` visible in the rendered frame points at the vendor CLI's
own outbound HTTP layer), not a `rein` launch-plan defect: `rein`'s own
plan is unchanged, correctly reopens the requested session, and correctly
detects the process as active (`grok:E5`, `PASS`) throughout. It blocks
`E1`, `E2`, and `E3` — the three rows whose mechanism specifically requires
a completed reply — at `PARTIAL`, per the contract's rule that `PARTIAL`
does not pass a required row.

### Dispositions that do not block the device verdict

| Row | Disposition | Condition 1 (PASS for every required agent: claude/codex/opencode) | Condition 2 (PASS for another optional agent, same tier) | Blocks the verdict? |
| --- | ----------- | -------------------------------------------------------------------- | ------------------------------------------------------------ | -------------------- |
| `opencode:D4` | N/A (definitional) | n/a — excluded from the required count entirely | n/a | No — excluded from both the denominator and the numerator |
| `grok:E1`, `grok:E2`, `grok:E3` | **Does not match `NOT TESTED (host credential)`** | n/a — the label itself is `PARTIAL`, not `NOT TESTED`, and the cause is a live connectivity failure with `grok`'s own credential confirmed working (`C1`–`D5` all `PASS` in the same isolated `GROK_HOME`) | n/a | **Yes — all three block** |
| `MatrixH:H7` | `PARTIAL` — not a named disposition at all | n/a | n/a | **Yes — blocks** |

No row this run qualifies for either named disposition beyond
`opencode:D4`'s definitional exclusion.

## 13. CLI experience (22 rows: 22 PASS)

Source: part C (carried unchanged from the first attempt — see the report
header — with this session's own independent checksum/version spot-check,
§0.8), executed through the real Windows pseudo console
(`scripts/testing/conptydriver`) driving `scripts/tuisandbox`'s synthetic
bench. Rows 2 and 22 compared byte-for-byte against a `v0.5.1` release
binary on the identical bench. Row 21 captured on legacy conhost (no
`WT_SESSION`, no `TERM_PROGRAM`, no UTF-8 locale). **Row `13` is this
candidate's own re-test target, confirmed fixed.**

**Harness-hygiene finding, fixed before any row counted.** The ambient
shell carried real, installed vendor CLIs on `PATH` and live
`XDG_DATA_HOME`/`GEMINI_CLI_HOME`/`KIMI_CODE_HOME`/etc. that
`scripts/tuisandbox`'s own env fragment does not isolate; initial runs
showed **16 sessions · 4 agents** instead of the bench's documented **9
sessions · 3 agents**, and the bench's permanently-`BLOCKED` `grok`
fixture instead showed `◐ Warn`/`confirmation_required` because the
host's real Grok Build executable was resolvable. Fixed with a fully
rebuilt `PATH` (the bench's own `bin\`, `C:\Program Files\Git\cmd`, and
Windows system directories only) and every vendor home variable
redirected to an empty isolated directory. Every row below reports the
bench's documented **9 sessions · 3 agents**, confirming no host session
or executable leaked in.

| # | Row | Result | Evidence |
| - | --- | ------ | -------- |
| 1 | Bare `rein` on a capable terminal draws the switcher | PASS | Header `9 sessions · 3 agents · all projects`, filter prompt, time-grouped rows, split preview, key bar. Settled frame (10s and 18s checkpoints byte-identical) shows every readiness glyph resolved correctly |
| 2 | Bare `rein` on a non-TTY exits `2`, byte-identical to `v0.5.1` | PASS | Both exit `2`; stderr SHA-256 byte-identical, `0476fb687fa1c1209d243016ab020460b8ad55696e4052f491b1fcb8cdb4795b` (matches every prior candidate's own hash) |
| 3 | `--plain` falls back to the numbered switcher | PASS | Frozen numbered-picker text over the same 9 rows |
| 4 | `REINSTATE_NO_TUI=1` does the same | PASS | Byte-identical to row 3 |
| 5 | `TERM=dumb` does the same | PASS | Rendered frame identical to rows 3/4 |
| 6 | `TERM` unset draws the switcher on Windows | PASS | Reuses row 1's evidence |
| 7 | A terminal below 40×10 falls back to plain | PASS | `30×8`: wrapped numbered-picker text |
| 8 | `NO_COLOR` draws the switcher with no color escape sequences | PASS | Exactly one bare `ESC[m` reset, zero SGR color sequences |
| 9 | Typing filters; header count follows | PASS | `"auth"` → `1 session · 1 agent` |
| 10 | `f` in list mode filters, does not fork | PASS | Filter line reads `f`, header unchanged; no fork screen opened |
| 11 | `tab` opens the action menu; `esc` reverts without acting | PASS | Key bar changes then reverts exactly to the pre-`tab` frame |
| 12 | `ctrl+k` opens the palette; a subsequence query finds its command | PASS | `"hof"` → top match `Hand off to another…`, `4 more matches` below |
| 13 | Readiness glyphs resolve for visible rows and a read-only agent shows blocked without a probe | **PASS — confirmed fixed** | See "Row 13 — full evidence" below |
| 14 | The warning checklist acknowledges with the spacebar and shows the equivalent command | PASS | One `space` toggles exactly one warning; equivalent command live-updates and reverts on a second `space` |
| 15 | A partial acknowledgement is refused with exit `7` | PASS | 2 of 4 warnings acknowledged: exit `7`, names exactly the remaining two |
| 16 | The handoff studio measures each policy and the equivalent command follows the selection | PASS | Full measurement; `key right` cycles `balanced→full→checkpoint` with the equivalent command updating in lock-step |
| 17 | The studio refuses `enter` on a plan that could not be built | PASS | `repo-drift` (foreign `repository_url`) session: refusal shown, `enter` re-shows the same refusal, studio stays open |
| 18 | `rein init` opens the wizard, validates per field, and allows going back | PASS | Invalid endpoint refused in place; `key up` returns to step 1 with the prior selection preserved |
| 19 | `rein init` collects no secret material inside the full-screen program | PASS | Walked all 7 steps: no credential prompt; `--help` has no credential flag |
| 20 | `rein init --link` prints a code that `--paste` consumes on the other device | PASS | Real two-device round trip via `scripts/testing/fakelocker`: device B's `--paste` pre-fills every field exactly matching device A, including the identical profile UUID |
| 21 | Glyphs degrade to ASCII where Unicode is not safe (legacy conhost) | PASS | `WT_SESSION`/`TERM_PROGRAM`/`LANG`/`LC_ALL`/`LC_CTYPE` all unset: every glyph is the ASCII set, never Unicode |
| 22 | Every `--json` document is byte-identical to `v0.5.1`, except changelog-explained differences | PASS | 5 documents compared: `handoff --no-launch --json` byte-identical; `doctor --json` differs only in version/timestamp; `sessions`/`resume`/`inspect --json` differ in exactly two documented classes (the `agent.active` liveness check; Grok's T3 capability promotion) — no third class found or needed |

**22/22 PASS.**

### Row 13 — full evidence (confirmed fixed)

This is the row `v0.6.0-rc.4`'s tagged run found failing (every visible row
`CANNOT RESUME`/`Blocked` regardless of true state, in the switcher's
default all-projects scope) and this candidate's changelog claims fixed by
bounding `internal/tui/readiness.Prober`'s concurrent verifications to a
small fixed pool regardless of page size.

**From outside any tracked project (`ScopeAll`, the default).** Header
reads `9 sessions · 3 agents · all projects`. At a 10-second checkpoint and
again at 18 seconds (byte-identical — fully settled), every glyph matches
its seeded state: 2 `READY` sessions `●`, 4 `WARN` sessions `◐`, 3
`BLOCKED` sessions `○` (a missing workspace, a foreign `repository_url`,
and a read-only agent with no executable) — every one correct.
Independently cross-checked against `resume --dry-run --json` for the
`grok` (`"decision":"blocked","block_exit_code":5"`, `agent.executable`
`missing`) and a `claude` `READY` session (`"decision":"ready"`, every
check `match`) under the same isolated harness: both match what the
switcher showed. At `v0.6.0-rc.4`, every one of these 9 rows showed `○
CANNOT RESUME` regardless of this ground truth; here, all 9 resolve
correctly.

**From inside a tracked project (`ScopeProject`).** Header auto-narrows to
`1 session · 1 agent`; the one visible row settles to `● READY TO RESUME`
— matching its `ScopeAll` result above and its independent `--dry-run
--json` ground truth.

**Disposition: fixed, confirmed** — matches the rc.5 dispatch's expectation
exactly.

## 14. Hop parity journeys (16 rows)

Source: part D (`H1`, `H1b`, `H1c`, `H2`, `H3`, `H4`, `H5`, `H9`, `H10`,
`H11`, `H12` — 11 rows, all `PASS`) and part E (`H6`, `H6b`, `H8`, `H8b` —
4 rows, all `PASS`; `H7` — `PARTIAL`), against a local, disposable `hopd` +
`scripts/testing/fakelocker` lab, never the hosted control plane and never
a developer's real agent trees except where the ground rules specifically
authorize the host's live Claude Code config (H4, H5). **15/16 PASS, 1
PARTIAL (`H7`)**.

| # | Row | Result | Summary |
| - | --- | ------ | ------- |
| H1 | Email sign-in through the approver; token in OS keyring; `whoami` names the device | PASS | Real GET-then-POST via `hoplab approve`; OS-keyring entry confirmed present by a fresh, separate process |
| H1b | Refused sign-in tells the terminal why and stores nothing | PASS | Short-TTL lab; link expired on its own; documented refusal, exit 1; `REINSTATE_HOME` never created |
| H1c | Unreachable control plane prints the one-line message and documented exit code | PASS | `REINSTATE_HOP_URL` at an unused loopback port; human and `--json` both match the documented message, exit 1, `details.kind=control_plane_unreachable`; nothing written |
| H2 | `init --hop` provisions the locker exactly once | PASS | Exactly one `locker_provisioned` event in `hopd.db` across three init runs, including a `--force` full re-init that reused the same locker id |
| H3 | `account init` recovery code shown once; keyring format 5, generation 1, verifies | PASS | `keyring.v1.json` fetched directly via S3: `SchemaVersion 5, CurrentGeneration 1`, one device; `VerifyGenerations` OK both pinned and unpinned |
| H4 | `push --all` sends one Claude, one Codex, one OpenCode session; `first_push` fires once | PASS | Real sessions for all three T5 agents; `first_push` fires exactly once across two `push --all` calls; no-op push reports `skipped:3`. Non-blocking finding: Codex's unmapped-project filter (matching Claude's own tested behavior) vs. OpenCode's adapter having no equivalent filter — product observation, did not block this row (see §16) |
| H5 | Wipe/new-device sign-in/`account recover`/`pull --all`/`resume --dry-run` complete for all three; one real resume | PASS | Genuine deletion of all four local session files, **including the live-config Claude file** (the `v0.6.0-rc.4` blocker on this exact step is resolved); new device recovered via the H3 code; all four restored; a real `claude --resume` against the live config recalled the exact planted token. Non-blocking finding: `pull --all`'s own `pulled`/`skipped` counts undercounted across a multi-call retry sequence — the restore mechanism itself was correct end to end (see §16) |
| H9 | `sync verify` names only observed objects | PASS | `checked_objects` names exactly the index and the newest snapshot; step 4 status is `"fail"`, never `"could not run"`, for the pre-existing `fakelocker AnyBucket:true` harness limitation carried from `v0.6.0-rc.4` |
| H10 | `sync migrate --to byo`, `--switch`, `--forget-hop` | PASS | 5 snapshots migrated to a second BYO bucket in one call; `switched:true, forgot_hop:true`; `hopd.db` shows both devices with `revoked_at` empty, confirming "does not revoke"; new bucket's `sync verify` step 4 reports `not-applicable` |
| H11 | Path remap between two homes' project mappings | PASS (macOS leg deferred) | A second device, mapping the pushing device's project id to its own different local path, pulled all four sessions with `cwd` rewritten; `resume --dry-run` reports `workspace.available: present` on the remapped location |
| H12 | Keyless diagnostics refuse a forged, rolled-back, or re-keyed keyring with nothing written | PASS | Three attacks (rolled-back generation-1, forged signature, foreign-account re-key) via raw S3 `PUT`s each refused correctly with a specific reason and `keyring_refused:true`; the device's own `account.json` byte-identical throughout |
| H6 | Device B `account join`; device A `devices approve`; B pulls | PASS | Live pairing via `hoplab pair join`; `pairing_requests` row confirmed `status=consumed, claims=2, version=2`; device B pulled all 3 of device A's sessions after mapping device A's project; `sessions --json` on B showed 6 sessions, no key overlap. Non-blocking finding: a `--force` re-init silently reset `[encryption] type` from `root-key` to `age-scrypt`, worked around by restoring from the automatic backup (see §16) |
| H6b | An expired pairing request is refused/rolled back; B's wrap absent from every generation | PASS | Dedicated short-TTL lab (`HOPD_PAIRING_TTL=8s`); `account join` with no approver ever running refused client-side exit 4 on expiry; `pairing_requests` row confirmed `status=expired, payload IS NULL, claims=5`; `account status` showed `device_in_keyring=false, enrolled_devices=1` |
| H7 | `daemon install/status/stop/start/uninstall` round trip through Task Scheduler; the foreground loop pushes/pulls correctly | **PARTIAL — operator/harness availability** | See "H7 — full evidence" below |
| H8 | A revokes B: generation rolls, B's token refused; lagging-device attack; 404-floor proxy | PASS | `keygeneration_crossplane_test.go -tags hopacceptance` against a real `hopd`: both tests passed (2.47s, 1.84s); 404-floor residual reproduced (asked 4 times, answered 404 every time). Live: device A revoked device B via the recovery code, generation rolled 1→2; device B's `whoami`, `push --all`, `pull --all`, and `sync verify` all refused, exit 4 each |
| H8b | A Console-initiated revocation stays pending until the recovery-code command confirms it | PASS | A local ephemeral self-signed-TLS reverse proxy drove the real Console HTTP routes end to end; `device_revocation_requests` row went `pending` (target device confirmed still functional via `whoami`) then `confirmed` (`confirmed_generation=2`) after `rein devices revoke` printed the documented confirmation message; target's next push refused, exit 4 |

### H7 — full evidence (under the corrected lab-isolation rule, issue #424)

**Every prerequisite the corrected rule requires was completed correctly
this run** — unlike `v0.6.0-rc.4`'s tagged run, which misread "fresh lab
account" as a fresh Windows user account (needing administrator rights it
could not obtain) and never even reached the round trip's own UAC prompt:

1. A **fresh Hop account** was created (`login`/`account init` against the
   main lab), with a brand-new isolated fixture root holding zero sessions
   — trivially satisfying "zero sessions pushed to it," per the corrected
   rule's own definition.
2. **Before-listing digests** of `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, and
   `XDG_DATA_HOME` (sorted SHA-256 over relative path + size) were captured
   before the wait began.
3. An elevated `Start-Process -Verb RunAs` launcher and companion script
   (the daemon round trip through real `schtasks`, plus a synthetic Claude
   session drop to exercise debounced push and scheduled pull) were staged
   and ready.
4. The go-signal poll (`D:/ReinstateAcceptanceProjects/h7-go.txt`, every
   60 seconds) started only after every other row in the part (`H6`,
   `H6b`, `H8`, `H8b`) was already done, per the row's own ordering rule.

**The go-signal never arrived.** Polled from `2026-09-07T14:42:41Z` through
`2026-09-07T15:22:41Z` — a 40-minute window this run's own orchestrator
allotted (shorter than the contract's own 150-minute figure, a
scheduling constraint of this particular run, not a reinterpretation of
the contract's text) — the file was confirmed still absent at the end.
**No unattended-elevation workaround was attempted**, per the dispatch's
explicit prohibition. Because the elevated script was never launched, the
UAC prompt was never raised and no daemon action of any kind ran this run
— none of `install`, `status`, `stop`, `start`, or `uninstall` executed,
and no after-digest comparison is meaningful because nothing was
attempted. **No write of any kind occurred this run to
`CLAUDE_CONFIG_DIR`, `CODEX_HOME`, or `XDG_DATA_HOME`.**

**Disposition: `PARTIAL`, re-recorded with a different practical reason
than `v0.6.0-rc.4`'s own `H7` `PARTIAL`.** That run could not even build
its (then-misread) prerequisite; this run correctly built the corrected
prerequisite and staged everything needed, but the maintainer was not
reachable inside this run's own poll window. This is the second
consecutive tagged run this row has not reached `PASS`; see §16, §20.

## 15. T5 encrypted sync round trip (beyond the 216; source: part B)

A disposable `scripts/testing/fakelocker` instance served the S3-compatible
locker; two fully isolated `REINSTATE_HOME`s (including isolated
`CLAUDE_CONFIG_DIR`/`CODEX_HOME`/`XDG_DATA_HOME` for both devices, never the
host's live homes on either side) were paired against it. **PASS, on the
second, corrected attempt** — see `F-T5-BACKEND-CONTAMINATION` below.

| Row | Result | Evidence |
| --- | ------ | -------- |
| `push:claude` | PASS | Pushed the isolated device-A `claude` session |
| `push:codex` | PASS | Pushed the isolated device-A `codex` session |
| `push:opencode` | PASS | Pushed a freshly-created isolated device-A `opencode` session |
| `pull:claude` | PASS | Device B's isolated home restored the pulled session; raw file contains the planted token 9 times |
| `pull:codex` | PASS | Device B's isolated home restored the pulled session; raw file contains its planted token 8 times |
| `pull:opencode` | PASS | Initial restore refused (`compatibility`, `NOT_INSTALLED` — no OpenCode layout yet on device B), then a `conflict` from an unrelated schema-bootstrap write; resolved by seeding device B's layout from device A's schema only; the retry pulled cleanly with zero conflicts. `rein search` on device B's isolated home found the pulled session by title |

**Full isolation this run — no write touched either device's live agent
homes at any point**, on the corrected attempt.

### `F-T5-BACKEND-CONTAMINATION` (new this run, corrected in-run)

This host's shell profile persistently re-sets `REINSTATE_BACKEND=memory`
and `REINSTATE_MEMORY_BACKEND_DIR` on every fresh shell, unrelated to this
candidate. The dispatch's "unset in every shell" rule was followed at the
start of this run but not re-applied to the specific commands that ran the
first T5 `init`/`push`/`pull` attempt, so that attempt silently routed
through the host's shared, persistent Hop-lab locker
(`D:\Projects\hop-10-lab\locker`) instead of this run's own `fakelocker` —
caught after the fact via the fakelocker's own access log showing zero
push/pull traffic despite `push`/`pull` reporting success. The full T5
round trip was re-run from a clean profile id with both variables
explicitly unset in every `init`/`push`/`pull` command, and independently
confirmed correct via real `PUT`/`GET`/`DELETE` traffic in the fakelocker's
own log. The mistaken profile's orphaned, encrypted key material remains in
the shared locker (a destructive cleanup command targeting a directory
outside this run's own tree was correctly refused by the host's own
command-safety layer) but does not affect any other profile there.

## 16. Findings (merged, release-blocking assessment)

| ID | Severity | Row(s) | Description | Release blocking |
| -- | -------- | ------ | ------------ | ----------------- |
| `F-GROK-BACKEND-CONNECTIVITY` | host/harness, new this run | `grok:E1`, `grok:E2`, `grok:E3` | `grok`'s own completion requests were unreliable on this host throughout this run (a fresh-session plant took ~10 minutes; a resume-and-recall ran headless 25+ minutes with zero output; two further ConPTY attempts reached 59s and 7m59s waits, the latter with a visible `Connection` error), independently reproduced against the host's own live, non-isolated `~/.grok` too. `rein`'s own plan is unchanged, correctly reopens the session, and correctly detects it active (`E5` `PASS`) | Blocks — matches no disposition (the label is `PARTIAL`, not `NOT TESTED`, and the underlying credential is confirmed working); same shape as the version-drift findings prior candidates hit, but this is a live connectivity problem, not a compatibility gate |
| — | operator/harness availability, re-recorded reason | `MatrixH:H7` | This run correctly built the corrected fresh-Hop-account prerequisite (unlike `v0.6.0-rc.4`, which misread the rule and could not even start) and staged the elevated script, but no maintainer go-ahead arrived inside this run's own 40-minute poll window | Blocks — `PARTIAL` does not pass a required row; no unattended-elevation workaround was attempted; no live-agent-home write occurred this run |
| — | harness (worktree hygiene), non-blocking | A4, A8 | The shared tagged worktree accumulated untracked helper files from a concurrent executor's in-progress row, transiently breaking `make lint` and the cross-OS build gates until quarantined out of the tree (moved, not deleted). No tracked source file was implicated | No |
| — | product observation, non-blocking | H4 | Codex's `Discover` silently excludes sessions from any locally unmapped project once any project mapping exists in `config.toml` (matching Claude's own tested behavior), but OpenCode's `Discover` has no equivalent filter, so an unmapped OpenCode session is pushed while an equivalent unmapped Codex/Claude session is silently dropped. Did not block H4 once the missing `--project` mapping was added | No — worth the coordinator's attention for cross-adapter consistency |
| — | harness reporting-accuracy, non-blocking | H5 | `pull --all`'s own `pulled`/`skipped` counts undercounted how many sessions were already restored to disk across a multi-call retry sequence; the underlying restore mechanism was correct end to end (all four files verified restored with correct content) | No |
| — | product observation, non-blocking | H6 | `rein init --hop --force --yes --project ...` regenerated `config.toml` with `[encryption] type=age-scrypt` instead of preserving the profile's existing `root-key` encryption, breaking local keyring decryption on an already-enrolled device until the pre-`--force` backup was restored. Worth the coordinator's attention: a forced re-init that only adds a project mapping should not silently change the profile's own encryption type | No — worked around cleanly via the automatic backup; did not block H6's own mechanism |
| — | carried, non-blocking, unchanged since `v0.6.0-rc.1` | H9, H10 | `scripts/testing/fakelocker`'s `AnyBucket:true` makes step 4 (bucket isolation) fail rather than genuinely refuse a gone bucket — contrasted directly by H10's real BYO `not-applicable` result and H12's real cross-account signature refusal, both of which exercise the equivalent real security boundary correctly | No |

## 17. Tier reductions proposed by this device

| Agent | Claimed tier | Verified tier | Reason |
| ----- | ------------ | -------------- | ------ |
| — | — | — | None proposed. Every declared tier (T0–T5, no T3 this candidate) was exercised as designed. `grok`'s `E1`/`E2`/`E3` gap this run is a live backend/connectivity block, not evidence the product misclassifies `grok`'s own capability tier — `grok:C1`–`D5` and `E4`/`E5`/`E6` all pass cleanly against the same isolated install, and the tier gate itself (`C5`) is independently confirmed correct, separate from the connectivity failure that blocks `E1`–`E3` |

## 18. `v0.6.0-rc.4` dispositions carried: cleared or re-recorded

Per [the rc.5 dispatch's own table](../v0.6.0-rc.5-agent-verification-prompts.md#dispositions-carried-from-the-v060-rc4-tagged-run):

| Row | `v0.6.0-rc.4` tagged result | This candidate expected | This run | Status |
| --- | ---------------------------- | ------------------------- | -------- | ------ |
| `MatrixB:B4` | FAIL, product, new finding | Fixed — expect `PASS` | PASS — every tree segment, including the top-level one, shape-normalized; `TestGeminiTmpProjectDirectoriesAreShaped` passes | **CLEARED** |
| `MatrixB:B7` | FAIL, product, new finding | Fixed — expect `PASS` | PASS — an existing-empty override root differs (`exists:true` vs `false`) from an absent one | **CLEARED** |
| `MatrixG:G4` | FAIL, documentation/contract, not a regression | Fixed (contract text corrected) — expect `PASS` | PASS — corrected wording, unchanged shipped behavior confirmed | **CLEARED** |
| CLI row `13` | FAIL, product, new finding | Fixed — expect `PASS` | PASS — the switcher's all-projects scope resolves readiness correctly regardless of page size, confirmed on a lab exceeding the pre-fix timeout budget's row count | **CLEARED** |
| `opencode:E5` | NOT TESTED, host/harness (`F-OPENCODE-VERSION-DRIFT`) | Fixed — expect `PASS` under `scripts/testing/conptydriver` against the real `1.18.29` install | PASS — `agent.version status=match, actual="1.18.29"` and `agent.active status=present, actual=true` | **CLEARED** |
| `opencode:E6` | NOT TESTED, same reason | Fixed — expect `PASS` | PASS — exit 7 | **CLEARED** |
| `MatrixH:H7` | PARTIAL, operator/harness availability (misread "fresh lab account" as a fresh Windows account) | Run under the corrected lab-isolation method; expect `PASS` with matching before/after digests, else the same disposition | **PARTIAL** — the corrected prerequisite was built correctly this time (fresh Hop account, before-digests, staged script), but the maintainer go-signal never arrived inside this run's own poll window | **RE-RECORDED**, different practical reason, still blocks |
| `opencode:D4` | N/A (definitional), unchanged since `v0.6.0-rc.1` | Not fixed, not fixable — expect `N/A (definitional)`, same reason | N/A (definitional), same reason | **CLEARED** (unchanged) |

**6 cleared** (`MatrixB:B4`, `MatrixB:B7`, `MatrixG:G4`, CLI row `13`,
`opencode:E5`, `opencode:E6`) **plus `opencode:D4` unchanged**, and **1
re-recorded** (`MatrixH:H7`, a genuinely different practical reason than
the disposition it carries forward from — a correctly-built prerequisite
with no maintainer reachable, vs. a prerequisite that could not be built at
all), **0** regressed in the sense of a worse disposition on the *same*
underlying cause.

**Every one of this candidate's own five targeted fixes is fully
confirmed**, exactly as the dispatch describes: the probe-redaction fixes
(`MatrixB:B4`, `MatrixB:B7`), the corrected contract wording
(`MatrixG:G4`), the bounded readiness-prober pool (CLI row `13`), and the
widened OpenCode ceiling (`opencode:E5`/`E6`).

**One new finding not on the carried table, blocking:**
`F-GROK-BACKEND-CONNECTIVITY` (`grok:E1`/`E2`/`E3`, all `PASS` at
`v0.6.0-rc.4` — a disposition regression this run, caused by a live
vendor-backend connectivity problem confirmed independent of `rein`, not
by anything this candidate changed).

**Every row that cleared at `v0.6.0-rc.4` and is not on this candidate's
own carried-dispositions table also stayed `PASS`**, unaffected by this
candidate's five targeted fixes: every `qwen` row `C1`–`E6`, every
`opencode` row `C1`–`C6`/`D1`–`D3`/`E1`–`E4`, `codex:E5`, and every CLI
row other than `13`, and every Hop parity row other than `H7`.

## 19. Deferred: Apple Silicon macOS

Copied verbatim from
[`../v0.6.0-rc.5-agent-verification-prompts.md`](../v0.6.0-rc.5-agent-verification-prompts.md)'s
own "Deferred: Apple Silicon macOS" section, under
[ADR 0005](../../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md) D2.
Nothing in this device's evidence claims macOS. Every row below is run
against this same `v0.6.0-rc.5` tag (or the stable tag it becomes) once
the Apple Silicon macOS host returns from repair
([issue #403](https://github.com/HarjjotSinghh/reinstate/issues/403)). If
that later macOS run fails, the fix ships as `v0.6.1` — this record of
this candidate's Windows acceptance is never edited to hide it.

| Group | Rows | Contract | Result |
| ----- | ---- | -------- | ------ |
| Phase 5 generated matrix, macOS column | all rows section B lists | Phase 5 | `DEFERRED` |
| CLI experience, macOS column | rows 1–20 and 22 | `v0.5.2` | `DEFERRED` |
| Hop cross-device journeys | pairing macOS↔Windows; path remap Windows↔macOS both directions; daemon launchd round trip; first push on macOS | hosted #16 | `DEFERRED` |
| Range widening on macOS | Claude Code through `2.1.263`; OpenCode through `1.18.29`; Qwen Code through `0.23.0` | ADR 0005 D3 | `DEFERRED` |
| Grok GD8 | tool-approval prompt (already collected on macOS; the Windows half is #368 and stays optional) | Grok T4 | `DEFERRED` |

No document, data file, or release note may state that `v0.6.0` was verified
on macOS until that run exists.

## 20. Release-blocking findings

Because 4 of the 215 required rows are `PARTIAL` and are not excused by
either disposition in the contract, this report's device verdict is `FAIL`
and stable `v0.6.0` is **not authorized** by this run:

1. `grok:E1` — the resumed session's history replays correctly and the
   process is detected active, but the recall completion itself never
   finished across three real attempts (host/harness, live vendor-backend
   connectivity problem, `F-GROK-BACKEND-CONNECTIVITY`, new this run)
2. `grok:E2` — same reason
3. `grok:E3` — not attempted live given `E1`/`E2`'s outcome on the
   identical connectivity path
4. `MatrixH:H7` — the corrected lab-isolation prerequisite was built
   correctly this run, but the maintainer go-signal never arrived inside
   this run's own poll window (operator/harness availability; no
   live-agent-home write occurred this run; the second consecutive tagged
   run this row has not reached `PASS`)

Neither is attributed by its reporting executor to a defect in any of this
candidate's own five targeted fixes — all five are fully confirmed working
(§18). `grok:E1`/`E2`/`E3` reflect a live vendor account/model-endpoint
connectivity problem, reproduced independent of `rein` and against the
host's own live, non-isolated `~/.grok`; `MatrixH:H7` reflects an
operator/elevation-availability gap this run's harness correctly refused
to route around. But per this run's own gate ("A row is PASS only if its
mechanism was exercised; PARTIAL and NOT TESTED do not pass a required
row") and the contract's narrow, mechanically-checked disposition rule
(§0.9, §12), none of the four qualifies for an exemption, so the device
verdict is `FAIL`.

## 21. Harness defects (merged, non-blocking to the mechanism under test)

- **The shared tagged worktree accumulated untracked helper files from a
  concurrent executor's in-progress row (part A).** `tmp-rc5e-revokehelper/`
  and `tmp-rc5e-consoledriver/` transiently broke `make lint` and the
  cross-OS `go build ./...` gates until quarantined out of the tree (moved,
  not deleted; no other executor's files touched). No tracked source file
  was implicated.
- **The dispatch's example command for `MatrixB:B4` (`rein doctor --agents
  --json --agent gemini`) does not match the shipped binary** — `doctor`
  has no `--agent` flag (part A). Worked around by running the unfiltered
  `doctor --agents --json` and reading the `gemini` entry from the full
  output; same evidence, no gap.
- **`F-T5-BACKEND-CONTAMINATION` (part B, self-corrected before this report
  was written).** This host's shell profile persistently re-sets
  `REINSTATE_BACKEND=memory`/`REINSTATE_MEMORY_BACKEND_DIR` on every fresh
  shell; the first T5 push/pull attempt silently routed through the host's
  shared, persistent locker instead of this run's own `fakelocker`, caught
  by the fakelocker's own empty access log, then re-run correctly with both
  variables explicitly unset in every command. Recommend the dispatch's
  rule explicitly say "unset in every shell that reads `REINSTATE_HOME` or
  performs a push/pull."
- **A version-gate shim must not depend on environment variables for
  `resume`/`fork` preflight (part B).** `resume --dry-run`'s version probe
  resolves the executable through `internal/executabletrust.Resolve`; a
  first shim attempt reading `SHIM_VERSION`/`SHIM_FORMAT` from the
  environment worked under `doctor --agents` but silently failed under
  `resume --dry-run`. Root cause turned out to be a Git Bash `PATH`
  translation trap (`export PATH="D:/dir:$PATH"` mistranslates to a broken
  Windows `PATH` for a native child process; `/d/dir` is required), not an
  environment-stripping behavior. Fixed by baking the version/format into
  the shim binary via `-ldflags -X` instead.
- **PowerShell `Start-Process -ArgumentList` joins a string-array argument
  list with plain spaces, not per-element quoting (part B).** An unquoted
  multi-word prompt argument passed to `conptydriver.exe` arrives already
  split into separate `os.Args` elements; the vendor CLI then sees each
  word as a separate positional argument and refuses with a usage error.
  Fix: wrap the argument in its own literal double quotes inside the
  PowerShell array element.
- **A stray Windows `PATH` resolution trap, same as the prior candidate
  (parts A, B, C).** A bare `rein`/`reinstate` lookup can resolve to a
  pre-existing host-installed copy ahead of a fresh test install's own
  directory. Part A's own PATH carried 4+ older `rein.exe` installs; every
  command in every part used the fully-qualified path to that part's own
  binary. This is also the resolved cause of part C's flagged
  "Cross-check note" discrepancy against the inconclusive first attempt's
  own executor-A part — see §0.8.
- **`REINSTATE_PASSPHRASE_FD`/`REINSTATE_RECOVERY_CODE_FD` need a real
  inherited Windows handle, not a bash file descriptor (parts B, D, E).**
  A plain `exec 3<file` in Git Bash does not cross into a native Windows
  child process; a small helper (`passfd.exe`, and equivalents for the
  recovery-code FD) using `syscall.SysProcAttr.AdditionalInheritedHandles`,
  the same pattern `scripts/testing/hoplab/secretfd_windows.go` already
  documents, was required in every part that needed one.
- **The host's real vendor CLIs and un-isolated env vars leak into any
  bench that does not explicitly rebuild `PATH` and every vendor home
  variable (part C).** Initial runs showed 16 sessions/4 agents instead of
  the bench's documented 9/3, and a permanently-`BLOCKED` fixture
  (`grok`) instead showed a live warning state because the host's real
  Grok Build executable was resolvable. Fixed with a fully rebuilt `PATH`
  (bench `bin\` + Git + Windows system dirs only) and every vendor home
  variable redirected to an isolated empty directory.
- **Git's total absence from a rebuilt `PATH` produces a false
  `git.available: missing` on every row (part C).** Fixed by adding
  `C:\Program Files\Git\cmd` back into the isolated `PATH` — Git is itself
  a prerequisite of the readiness checks.
- **A handoff-studio frame captured mid-repaint shows minor
  overlapping/un-cleared text at a few cell positions (part C).** A
  snapshot-timing artifact, not a rendering defect; the underlying data on
  both frames is legible and correct.
- **`send "text\n"` (a single step, escaped newline) reliably fails to
  submit to a plain non-alt-screen prompt under `conptydriver` (part C,
  same trap the `v0.6.0-rc.4` tagged run recorded).** A separate `send
  "text"` followed by its own `key enter` step works every time.
- **`key h` from the switcher's list mode is consumed as a filter
  character, not the handoff shortcut, per the product's own documented
  contract (part C)** — correct behavior, noted as a scripting trap for
  future rows.
- **Codex's `Discover` silently excludes unmapped-project sessions once
  any project mapping exists, but OpenCode's `Discover` has no equivalent
  filter (part D, H4).** Worked around by adding the missing `--project`
  mapping; flagged as a cross-adapter consistency observation in §16, not
  a defect blocking this row.
- **`pull --all`'s own `pulled`/`skipped` counts undercount across a
  multi-call retry sequence (part D, H5).** All four sessions were
  genuinely restored and independently verified; this is a reporting-
  accuracy observation on the counts, not data loss.
- **Carried, non-blocking, unchanged since `v0.6.0-rc.1` (parts D, E, H4,
  H6, H9, H10).** `scripts/testing/fakelocker`'s `AnyBucket:true` makes
  step 4 (bucket isolation) of the post-push/`sync verify` report fail
  against every Hop-mode locker in this lab, not a control-plane or
  product defect — contrasted directly by H10's real BYO `not-applicable`
  result and H12's real cross-account signature refusal.
- **`rein init --hop --force --yes --project ...` silently reset
  `[encryption] type` from `root-key` to `age-scrypt` (part E, H6),**
  breaking local keyring decryption on an already-enrolled device until
  the automatic pre-`--force` backup was restored and the new project
  mapping added as a plain-text edit instead. Flagged as a product
  observation in §16.
- **`H7` — the corrected lab-isolation prerequisite was built correctly
  this run; no incident (part E).** Because the round trip was never
  started, no write of any kind occurred to `CLAUDE_CONFIG_DIR`,
  `CODEX_HOME`, or `XDG_DATA_HOME`.

## 22. Terminated device block

> Device testing is terminated for this candidate at the milestone recorded
> above. The results in this report are final for this device and this tag.
> Any further testing requires a new candidate tag and a new report.

- Terminating tester: assembler (tagged-run coordinator role, this session)
- UTC timestamp: 2026-09-07
- Device verdict: **FAIL** — 4 of 215 required rows are `PARTIAL` and are
  not excused by either disposition in the contract (`grok:E1`, `grok:E2`,
  `grok:E3`, `MatrixH:H7`); see the Verdict block and §20 above for the
  complete list and reasoning. **Every one of this candidate's own five
  targeted fixes is confirmed working** (`MatrixB:B4`, `MatrixB:B7`,
  `MatrixG:G4`, CLI row `13`, `opencode:E5`/`E6` — §18), clearing all six
  of `v0.6.0-rc.4`'s fixable blocking findings. This does not authorize
  stable `v0.6.0`. No real host data required maintainer cleanup beyond
  what each part's own harness notes already disclose and resolved
  in-run (`F-T5-BACKEND-CONTAMINATION`'s orphaned, encrypted key material
  in the shared locker; harmless and unrelated to any other profile). A
  re-attempt of `grok:E1`/`E2`/`E3` during a healthier connectivity window
  (or a maintainer check of the account/endpoint's health), and a
  maintainer or a longer/differently-scheduled go-signal window for
  `MatrixH:H7` — now its second consecutive tagged-run `PARTIAL` — are
  required before the next tagged acceptance run can plausibly clear the
  remaining two blocking findings.

**Addendum (2026-09-07 evening, §23):** a same-day recheck against this
same tagged artifact re-tested all four blocking rows. `grok:E1`/`E2`/`E3`
remain `PARTIAL` — the live backend connectivity failure reproduced twice
more on this host, now joined by a compounding, host-side finding (the
live `grok` binary drifted to `1.0.13`, outside `rein`'s verified `1.0.5`
range, so a real `rein resume` would also refuse to launch it today).
`MatrixH:H7`'s go-signal arrived this time and its mechanism ran to
completion for the first time across three tagged runs (`daemon install`/
`status`/`stop`/`start`/`uninstall` through real `schtasks`, with a real
push and pull) — but the elevated, Task-Scheduler-spawned process did not
inherit this run's isolated environment and instead read from and pushed
two real snapshots from the host's live agent homes into the recheck's
disposable local lab locker, and the required before/after digest equality
did not hold. `MatrixH:H7` is therefore recorded `PARTIAL` again this
recheck, for a new reason (an isolation-propagation defect surfaced only
now that the mechanism finally ran, not operator/harness availability).
Required counts are unchanged at `211 PASS / 4 PARTIAL / 0 FAIL / 0 NOT
TESTED` of `215`; the device verdict remains `FAIL`. See §23 for full
evidence. Before the next tagged run: a healthier `grok` connectivity
window or a maintainer check of the account/endpoint, a `grok` reinstalled
or pinned back within the verified range, and — new — a fix so
`rein daemon install` carries the installing process's environment into
the registered Task Scheduler action itself (or an equivalent isolation
mechanism that survives the elevation hop), plus preserving the full
per-file before-listing (not just its aggregate digest) on the next `H7`
attempt.

**Addendum (2026-09-07 night, §24):** a second same-day recheck against
this same tagged artifact re-tested all four blocking rows again, in its
own fresh lab. `grok:E1`/`E2`/`E3` remain `PARTIAL` — the live backend
connectivity failure reproduced twice more (a fifth and sixth independent
confirmation across both recheck rounds), and the live `grok` compatibility
block (`1.0.13` outside the verified `1.0.5` range) is unchanged from the
first recheck. `MatrixH:H7`'s go-signal again arrived immediately and its
mechanism again ran to completion — but the same Task-Scheduler
environment-inheritance defect the first recheck diagnosed reproduced
identically a second time, again reading from and pushing real snapshots
from live agent-home roots instead of the isolated ones this run staged
(this time colliding, as a conflict, with an unrelated synthetic fixture
session already resident on the shared host, not real private data). This
round closes the first recheck's own disclosed gap for two of the three
live roots: a full per-file before/after listing (not just the aggregate
digest) traces every changed file to ordinary concurrent host activity
unconnected to `rein`'s push (read-only) or pull (`0` snapshots), making
the non-attribution to `rein` conclusive rather than merely likely for
those two roots. It also discloses a new gap of its own: the third root's
before-listing was captured against the wrong `CODEX_HOME` path, so no
valid before/after comparison exists for that root this round. Required
counts are unchanged at `211 PASS / 4 PARTIAL / 0 FAIL / 0 NOT TESTED` of
`215`; the device verdict remains `FAIL`. See §24 for full evidence. The
same fix remains outstanding before a next tagged run can plausibly clear
`H7`: `rein daemon install` carrying the installing process's isolated
environment into the registered Task Scheduler action itself (or an
equivalent mechanism that survives the elevation hop).

## 23. Recheck against the same artifact (2026-09-07 evening)

A recheck executor re-tested the four blocking rows above, against this
same artifact, in a fresh worktree (`D:\Projects\reinstate-worktrees\v060-rc5-tagged`,
branch `v060/rc5-tagged`, head `4c4084e1` at the start of this recheck — the
head this report itself was assembled at). Nothing else in this report was
re-touched.

**Artifact identity, re-proven:** the archive at
`reinstate_0.6.0-rc.5_windows_amd64.zip` re-hashed to
`a648f1de65c15bea7926dda2dd0ce48110d512c9bc60a23b5988c3b24ded9f09`, matching
`checksums.txt` exactly. Installed to
`D:/ReinstateAcceptanceProjects/v060-rc5-recheck/install/` and invoked only
by full path; `rein.exe version --json` reports
`{"name":"reinstate","version":"0.6.0-rc.5","commit":"0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9","date":"2026-09-07T12:59:29Z"}`
— the same tag and commit this whole report covers.

### `grok:E1` / `grok:E2` / `grok:E3` — recheck: still `PARTIAL`

Method: the rc.4 part-B method (a real session in a throwaway git project,
a planted token, `rein`'s own resume/fork launch plan, `conptydriver`
scripted send+wait), same as the original run's own §12 evidence.

1. **Connectivity characterization, before any session work:** `grok -p
   "Reply PONG"` — start `16:17:51Z`, a 90s budget, exit `124` (timeout),
   zero output, end `16:19:34Z`. Reproduces `F-GROK-BACKEND-CONNECTIVITY`
   immediately, independent of `rein`.
2. **A real interactive session**, created in a fresh throwaway git project
   (`D:/ReinstateAcceptanceProjects/v060-rc5-recheck/grok-throwaway`, one
   commit, nothing else in it) under `conptydriver`, launched per the
   console rule (`Start-Process -FilePath conptydriver.exe -ArgumentList
   ... -WindowStyle Hidden -Wait`, its own stdout/stderr never redirected,
   `-raw` capturing every byte, `snapshot` steps rendering frames to
   files). The step script typed `Reply with exactly this token and
   nothing else: RC5RECHECK-E1-9F2Q`, sent a real `key enter`, and the
   rendered frame confirmed genuine submission (the prompt entered the
   transcript as `#1 ...`, the input line cleared, the footer showed
   `Waiting for response…`). The session this created is real and
   `rein`-visible: `grok:01a07cb3-8404-7e91-92d6-ebd7abbba819` (`rein
   sessions --json`, `updated_at 2026-09-07T16:29:25Z`).
3. **The recall never completed.** The same continuous `conptydriver`
   process was left running (start `16:28:53Z`, end `16:37:16Z`, ~8m22s)
   with a single long wait for either the answer or a known error string.
   The rendered footer's own elapsed counter climbed past `Waiting for
   response… 7m50s` with no completion and — checked across the *entire*
   decoded raw byte stream, not just the final frame — no occurrence
   anywhere of `reqwest`, `Connection error`, `error sending request`, or
   `Error:`. This differs in exact symptom from the original run's third
   attempt (which did show a visible `Connection` error by ~8 minutes);
   this recheck's attempt instead spun silently the whole time. Immediately
   after, a fresh `grok doctor` showed no issues beyond its usual
   `terminal.newline-fallback` recommendation, and a fresh `grok -p "Reply
   PONG"` — start `16:37:57Z`, 60s budget, exit `124`, zero output, end
   `16:38:58Z` — reproduced the same hang a second time. Per the row's own
   budget rule, `E3` (fork) was not attempted live, on the same basis as
   the original run: the fork-and-recall exchange needs the identical
   completion request that just failed twice.
4. **New this recheck: a compounding compatibility block.** The host's
   live `grok` has self-updated to `1.0.13` since the original run recorded
   it at the verified `1.0.5` (`~/.grok/version.json`:
   `"version":"1.0.13","checked_at":"2026-09-07T16:07:58Z"`) — outside
   `rein`'s verified range for `grok`, `1.0.5`–`1.0.5` (this same report's
   §12 `E4` evidence). `rein resume grok:01a07cb3-8404-7e91-92d6-ebd7abbba819
   --dry-run --json` against this session now returns exit `5`
   (`"code":"compatibility"`), blocked at `agent.version`
   (`"severity":"block","actual":"1.0.13","message":"native agent version
   1.0.13 is outside the verified range 1.0.5 to 1.0.5 inclusive"`) — the
   launch plan is still captured correctly
   (`"launch_plan":{"operation":"fork","executable":"grok","args":["--resume","01a07cb3-8404-7e91-92d6-ebd7abbba819","--fork-session"],...}`)
   but a real, non-dry-run `rein resume` would refuse to launch `grok` at
   all on this host today, before ever reaching the live connectivity
   problem. This is a live-host-drift finding, not a regression in the
   candidate: the host's own vendor CLI update policy moved out from under
   the pinned verification range between runs. (The host still holds the
   original pinned binary at `~/.grok/bin/grok.exe.old`, confirmed
   `grok 1.0.5 (5115b46bc9) [stable]`; re-running the full flow against it
   under an isolated `GROK_HOME` was not pursued further once the
   connectivity failure had already reproduced twice, independent of
   version, via the two methods above.)

**Disposition, unchanged: `grok:E1`, `grok:E2`, `grok:E3` remain `PARTIAL`.**
`F-GROK-BACKEND-CONNECTIVITY` reproduces on this host today — a third and
fourth independent confirmation beyond the original run's own three
attempts — and is now joined by a second, compounding blocker
(`agent.version` outside range) that would refuse a real launch even if
connectivity recovered. Neither is a `rein` launch-plan defect.

### `MatrixH:H7` — recheck: mechanism reached for the first time, still not a clean `PASS`

Under the corrected lab-isolation rule (issue #424): the recheck's own lab
(`hoplab`, root `D:/ReinstateAcceptanceProjects/v060-rc5-recheck/lab`, hopd
`127.0.0.1:8323`, locker `127.0.0.1:9323`). `hoplab ps` before starting
showed no other lab running on this host; this lab was the only one `ps`
listed afterward.

1. **A fresh Hop account**, real email sign-in (`hoplab approve` watching
   the log for the confirm link, GET-then-POST, the same two requests a
   person clicking the emailed link makes) against this lab's own `hopd`:
   account `b79463f5-cdd5-4d2c-8e6e-ac45953f07d5`, device
   `51709cac-4e88-4d32-9faf-7b8a57bed93f`. `rein init --hop` provisioned
   the locker; `rein account init` was completed non-interactively via
   `scripts/testing/accountinit` (built from this worktree — it exists
   exactly for this: captures the recovery code shown once and answers the
   confirmation prompt with it, then redacts it from its own output; the
   code was never printed by this recheck). `rein account status --json`
   confirmed `"recovery_code_confirmed":true, "enrolled_devices":1`; `rein
   sync verify --json` confirmed **zero sessions pushed** before the test
   (`"Nothing has been pushed from this profile yet"`).
2. **Before-listing digests** (sha256 over sorted `relative_path\tsize`)
   of the live `CLAUDE_CONFIG_DIR` (19463 files,
   `9e612f3c516290d9ee5f59059b6424a630439a52f2167a4df4bafe8febe41cf1`),
   `CODEX_HOME` (2374 files,
   `07c8df4bec2839bf60f6f5a9e8b629d39ed01a686ebf384328c3afc916a8b5c0`), and
   `XDG_DATA_HOME/opencode` (178 files,
   `18795ee122f22db99f0315fb372d7faa24f26bdfd20999013d38d69189ff6251`) were
   captured before the go-signal poll began. **This recheck's own gap:**
   only the aggregate digest was persisted at this step, not the full
   per-file listing — so when the after-digests differed (below), an
   exact differing-relative-paths diff could not be produced. Disclosed
   here rather than papered over; see the harness note below.
3. **The elevated step and its companion script** (`daemon
   install`/`status`, a synthetic Claude session drop, `status` again,
   `stop`/`start`/`status`/`uninstall`, all output to a file) were staged
   at `D:\ReinstateAcceptanceProjects\v060-rc5-recheck\h7-elevated-step.ps1`
   before polling began.
4. **The go-signal was found already present** at this recheck's very
   first poll check (`D:/ReinstateAcceptanceProjects/h7-go.txt`, existing
   file, first check at `2026-09-07T16:44:19Z`, and its own mtime predates
   that). The elevated step was launched immediately via `Start-Process
   -FilePath powershell.exe -ArgumentList ... -Verb RunAs -Wait
   -WindowStyle Hidden`; the UAC prompt was accepted within the run
   (start `16:44:44Z`, end `16:45:20Z`, ~35s wall clock for the launcher
   call itself; the elevated script's own log spans
   `22:14:47`–`22:15:19` local (`+05:30`) = `16:44:47Z`–`16:45:19Z`).
5. **The mechanism ran, for the first time across three tagged runs:**
   `daemon install` registered a real Task Scheduler task
   (`com.reinstate.daemon.6732e97a`); `daemon status` showed it
   `installed:true, running:true` via `schtasks`. After a real 30s wait, a
   real push and pull both completed
   (`"push":{"ok":true,"summary":"pushed 2 snapshot(s), skipped 84
   unchanged"}`, `"pull":{"ok":true,"summary":"pulled 0 snapshot(s),
   skipped 0 already synced"}`). `daemon stop`, `daemon start`, and
   `daemon uninstall` each completed against the real scheduled task; a
   post-uninstall check confirmed neither the daemon process nor the
   scheduled task remained.
6. **A finding this recheck surfaced, not excused by the mechanism having
   run: the elevated/scheduled daemon did not honor the isolated
   environment this run staged for it.** The elevated script set
   `CLAUDE_CONFIG_DIR`/`CODEX_HOME`/`XDG_DATA_HOME` to the fresh,
   disposable `h7-device` home before calling `rein daemon install`/
   `start` — but the daemon's own reported `roots` in every `status` call
   were the host's **live** agent-home paths (`<live-CLAUDE_CONFIG_DIR>\projects`,
   `<live-CODEX_HOME>\sessions`, `<live-XDG_DATA_HOME>\opencode`), not the
   isolated ones. The most likely mechanism: a Task-Scheduler-spawned process does not
   inherit the launching PowerShell process's transient `$env:` overrides,
   only the account's persistent environment — so the actual running
   daemon watched, and the reported `push` read from, the real live agent
   trees on this host, uploading two real session snapshots (content never
   inspected, never quoted) into this recheck's own disposable local
   `fakelocker`. This is exactly the live-tree touch the corrected
   isolation rule exists to prevent; it happened despite the rule being
   correctly followed at the shell level, because environment isolation
   did not survive the elevation/Task-Scheduler hop.
7. **After-digests** of the same three roots: `CLAUDE_CONFIG_DIR` — same
   file count (19463) but a **different** hash
   (`482f4ed39785cc7dc2049745de8f9daa07c1a3342153f5ab3b39ea0e4012dba0`);
   `CODEX_HOME` — a **different** file count (2373, was 2374) and a
   different hash
   (`3a67a34e2de08bf57028c7d944abc2ceec4694c65402cf88daf368cf16adec7b`);
   `XDG_DATA_HOME/opencode` — **identical** hash, unchanged. Per the
   corrected rule, this recheck stopped here: no further action was taken
   against the live homes, and the disposable lab was stopped, not
   deleted, and nothing in its `fakelocker` was cleaned up.
8. **Investigation of the digest mismatch (relative paths only, no
   content, since a full before-listing was not preserved to diff
   against — see point 2):** the recheck's own no-content
   `find -newermt` sweep over the test window (`16:10Z` onward) shows
   heavy, ordinary concurrent write activity on both live trees that
   `rein push`/`pull` has no mechanism to cause: repeated
   `.claude.json.backup.*` rotation under `CLAUDE_CONFIG_DIR/backups`, and
   — notably — this very recheck session's **own** tool use writing into
   `CLAUDE_CONFIG_DIR/file-history/<this session's own scratchpad
   UUID>/...` as an intrinsic side effect of running as a Claude Code
   session on this host, plus unrelated `CODEX_HOME` cache/log/`sqlite-wal`
   churn consistent with other concurrent sessions on this heavily-used,
   multi-session development host (the same host whose
   `D:\ReinstateAcceptanceProjects\` tree alone holds several dozen other
   acceptance-run and phase-2 directories). `rein push` only reads and
   uploads; this run's `pull` restored zero snapshots, so it wrote nothing
   back. No write specifically attributable to the daemon's push/pull was
   identified. The digest drift could not be conclusively separated from
   this ambient activity, because — as noted in point 2 — only the
   aggregate hash, not the full per-file entries, was preserved from the
   before-listing.

**Disposition: `MatrixH:H7` recorded `PARTIAL`, not `PASS`, this recheck.**
The row's own mechanism (`daemon install`/`status`/`stop`/`start`/
`uninstall` through real `schtasks`, with a genuine foreground push/pull)
was reached and executed successfully for the first time across three
tagged runs on this artifact — a real step forward from both prior
`PARTIAL`s (which never got this far). But the corrected rule's isolation
precondition was not actually honored by the elevated process, live agent
trees were read from and two real snapshots pushed to a disposable locker,
and the required after-digest equality did not hold and could not be
cleanly attributed to the mechanism or ruled out as ambient host noise
given this recheck's own gap in preserving the before-listing. A clean
`PASS` requires the isolation to actually reach the Task-Scheduler-spawned
process — for example, `daemon install` writing the isolated environment
into the registered task's own action rather than relying on the
installing process's transient environment — plus a full per-file
before/after listing next time, not just the aggregate hash.

### Live-home digest comparison

| Root | Before | After | Match? |
| --- | --- | --- | --- |
| `CLAUDE_CONFIG_DIR` | `9e612f3c…` (19463 files) | `482f4ed3…` (19463 files) | No — same count, different hash |
| `CODEX_HOME` | `07c8df4b…` (2374 files) | `3a67a34e…` (2373 files) | No — file count and hash both differ |
| `XDG_DATA_HOME/opencode` | `18795ee1…` (178 files) | `18795ee1…` (178 files) | Yes — identical |

### Host note

This is a heavily-used, multi-session Windows host: dozens of other
acceptance-run directories exist under `D:\ReinstateAcceptanceProjects\`
alone, and this very recheck's own session activity writes into the live
`CLAUDE_CONFIG_DIR` as a side effect of using its own Read/Edit/Write
tools. Any future digest-equality check on this host should preserve the
full per-file listing (not just the aggregate hash) at capture time, so a
later mismatch can be attributed to a specific path instead of only to a
changed top-level digest.

#### Recheck outcome

- **Rows rechecked:** `grok:E1`, `grok:E2`, `grok:E3`, `MatrixH:H7` (4 of
  the 215 required rows).
- **Result:** all four remain not-`PASS` — `grok:E1`/`E2`/`E3` `PARTIAL`
  (live backend connectivity failure reproduced twice more, now compounded
  by a live-host `grok` version drift outside the verified range) and
  `MatrixH:H7` `PARTIAL` (mechanism reached and exercised for the first
  time, but the isolation precondition was not actually honored by the
  elevated process and the after-digest check did not clear cleanly).
- **Final required counts after this recheck: `211 PASS / 4 PARTIAL / 0
  FAIL / 0 NOT TESTED` of `215` required rows — unchanged from the Verdict
  block above.**
- **Final device verdict: `FAIL`.** 4 of 215 required rows are not `PASS`
  and are not excused by either disposition in
  [`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict):
  `grok:E1`, `grok:E2`, `grok:E3`, `MatrixH:H7`. This recheck does not
  authorize stable `v0.6.0`.

## 24. Second recheck against the same artifact (2026-09-07 night)

A second recheck executor re-tested the same four blocking rows a second
time against this same artifact, in this same worktree
(`D:\Projects\reinstate-worktrees\v060-rc5-tagged`, branch
`v060/rc5-tagged`, head unchanged across this recheck), in its own fresh
lab (`D:/ReinstateAcceptanceProjects/v060-rc5-recheck2/`, distinct from
both the original run's and the first recheck's directories). Nothing else
in this report was re-touched.

**Artifact identity, re-proven again:** the archive at
`reinstate_0.6.0-rc.5_windows_amd64.zip` re-hashed to
`a648f1de65c15bea7926dda2dd0ce48110d512c9bc60a23b5988c3b24ded9f09`, matching
`checksums.txt` exactly (the same hash both prior sections recorded).
Installed to `D:/ReinstateAcceptanceProjects/v060-rc5-recheck2/install/`
and invoked only by full path; `rein.exe version --json` reports
`{"name":"reinstate","version":"0.6.0-rc.5","commit":"0d0ab61efa16e0009a05bcf6bb51c30482b7eeb9","date":"2026-09-07T12:59:29Z"}`
— the same tag and commit this whole report covers.

### `grok:E1` / `grok:E2` / `grok:E3` — second recheck: still `PARTIAL`

Method: the same real-session-under-`conptydriver` method as the original
run and the first recheck, in a new throwaway git project.

1. **Connectivity characterization, before any session work:** `grok -p
   "Reply PONG"` — start `17:11:11Z`, a 90s budget, exit `124` (timeout),
   zero output, end `17:12:42Z`. Reproduces `F-GROK-BACKEND-CONNECTIVITY`
   immediately, independent of `rein`, a fourth time across both recheck
   rounds.
2. **A real interactive session**, created in a fresh throwaway git project
   (`D:/ReinstateAcceptanceProjects/v060-rc5-recheck2/grok-throwaway`, one
   commit, nothing else in it) under `conptydriver`, launched per the
   console rule (`Start-Process -FilePath conptydriver.exe -ArgumentList
   ... -WindowStyle Hidden -Wait -PassThru`, its own stdout/stderr never
   redirected, `-raw` capturing every byte, `snapshot` steps rendering
   frames to files). The step script typed `Reply with exactly this token
   and nothing else: RC5RECHECK2-E1-7K3M` and sent a real `key enter`. The
   session this created is real and `rein`-visible:
   `grok:01a07cdc-8c9e-7603-b66e-6ca9a92c2e97` (`rein sessions --json`,
   `updated_at 2026-09-07T17:13:44Z`).
3. **This round's own methodology gap, disclosed:** the first attempt's
   `wait` regex included the literal planted token text as one of its match
   alternatives; because that text is echoed on screen the instant it is
   typed (before any real submission or completion), the `wait` matched
   within about a second and the driver exited immediately — not evidence
   of a fast reply. Caught by inspecting the resulting frame and the
   `-raw` log (only ~4KB, no `Waiting for response` text, no error text —
   too little byte volume for a real 90s+ round trip) rather than accepted
   at face value.
4. **Corrected and re-run:** the same session was reattached with
   `grok --continue` under a fresh `conptydriver` invocation, this time
   with a plain, unconditional wait (five `sleep`+`snapshot` checkpoints
   totalling `483s`, no early-exit regex) so a timeout could never abort
   the script before a final frame was captured. Start
   `2026-09-07T22:45:53+05:30` (`17:15:53Z`), end `22:53:56+05:30`
   (`17:23:56Z`), ~8m03s. The decoded raw byte stream over the full window
   was searched for `reqwest`, `Connection error`, `error sending request`,
   and `Error:` — none found anywhere, a silent hang (the same symptom as
   the first recheck's own third attempt, not the original run's visible
   `Connection` error). The final rendered frame showed the session's boot
   panel with no new transcript entry past the planted prompt. A fresh
   `rein sessions --json` immediately after confirmed `message_count`
   remained `1` for this session — no assistant reply ever landed.
5. **Fresh `grok doctor`** showed no issues beyond its usual
   `terminal.newline-fallback` recommendation. **A fresh `grok -p "Reply
   PONG"`** — start `17:24:25Z`, 60s budget, exit `124`, zero output, end
   `17:25:26Z` — reproduced the hang a second time this round (a fifth and
   sixth independent confirmation across both recheck rounds, beyond the
   original run's own three). Per the row's own budget rule, `E3` (fork)
   was not attempted live, on the same basis as both prior rounds.
6. **The compatibility block is unchanged from the first recheck.** The
   host's live `grok` remains at `1.0.13`
   (`~/.grok/version.json`: `"checked_at":"2026-09-07T16:07:58Z"` —
   identical timestamp to the first recheck's own reading, so the host has
   not drifted further since). `rein resume
   grok:01a07cdc-8c9e-7603-b66e-6ca9a92c2e97 --dry-run --json` returns exit
   `5` (`"code":"compatibility"`), blocked at `agent.version`
   (`"severity":"block","actual":"1.0.13","message":"native agent version
   1.0.13 is outside the verified range 1.0.5 to 1.0.5 inclusive"`), with
   the launch plan still captured correctly
   (`"launch_plan":{"operation":"resume","executable":"grok","args":["--resume","01a07cdc-8c9e-7603-b66e-6ca9a92c2e97"],...}`).
7. **The pinned `grok.exe.old` (`1.0.5`) was probed once more, briefly**,
   under a fresh isolated `GROK_HOME`
   (`D:/ReinstateAcceptanceProjects/v060-rc5-recheck2/grok-home-105`): it
   started and answered immediately with `Error: Not signed in...` (exit
   `1`, no hang) — confirming the pinned binary itself is not what is slow;
   not pursued further, on the same basis as the first recheck (the
   connectivity failure had already reproduced twice this round,
   independent of version).

**Disposition, unchanged a second time: `grok:E1`, `grok:E2`, `grok:E3`
remain `PARTIAL`.** `F-GROK-BACKEND-CONNECTIVITY` reproduces on this host
today for a fifth and sixth time beyond the original run's three, still
joined by the same compounding `agent.version` block. Neither is a `rein`
launch-plan defect.

### `MatrixH:H7` — second recheck: mechanism reached a second time, per-file attribution now conclusive for two of three roots

Own lab, distinct from both prior rounds: `hoplab`, root
`D:/ReinstateAcceptanceProjects/v060-rc5-recheck2/lab`, hopd
`127.0.0.1:8323`, locker `127.0.0.1:9323`. `hoplab ps` before starting
showed no other lab running on this host; this lab was the only one `ps`
listed afterward.

1. **A fresh Hop account**, real email sign-in (`hoplab approve` watching
   the log for the confirm link, GET-then-POST) against this lab's own
   `hopd`: account `23518f33-1902-41a9-b2c5-87ef58b60380`, device
   `5452cf29-b481-4542-aa99-92f65fe143ab`. `rein login --email
   h7recheck2@example.com --no-browser --json` completed the sign-in;
   `rein init --hop` provisioned the locker
   (`lk-1785v5d7t2m8kzhc2xf8s8snj8`); `rein account init` was completed
   non-interactively via `scripts/testing/accountinit` (built from this
   worktree, same tool the first recheck used — captures the recovery code
   shown once and answers the confirmation prompt with it, then redacts it
   from its own output; the code was never printed this round either).
   `rein account status --json` confirmed `"recovery_code_confirmed":true,
   "enrolled_devices":1`; `rein sync verify --json` confirmed **zero
   sessions pushed** before the test (`"nothing has been pushed from this
   profile yet"`).
2. **The isolated device home this round used `scripts/testing/hoplab`'s
   own `homes`/`env` seeding** (`homes -root
   D:/ReinstateAcceptanceProjects/v060-rc5-recheck2/h7home -devices
   h7-device`) rather than a hand-built directory tree — the same
   isolated-environment mechanism the project's own Hop parity rows (§14)
   already rely on and that `homes_isolation_test.go` regression-tests.
   This seeded a synthetic Claude session (and a synthetic Codex session)
   under the isolated home for free, standing in for the row's "synthetic
   Claude session drop" step; the elevated step additionally appended one
   line to the seeded Claude session file mid-test to give the debounced
   watcher a real filesystem event to react to.
3. **Before-listings, this round's own fix for the first recheck's
   disclosed gap:** a full per-file listing (`relative_path\tsize`, sorted)
   was captured and saved to disk — not just the aggregate digest — for
   each of the three live roots, immediately before the go-signal poll
   began: `<live CLAUDE_CONFIG_DIR>` (`19525` files, listing sha256
   `d5b8f67b…`), `<this recheck's own shell CODEX_HOME>` (`575` files,
   listing sha256 `b57e7771…` — **see point 8 below: this was the wrong
   path for this root**), `<live XDG_DATA_HOME>/opencode` (`178` files,
   listing sha256 `205921b6…`).
4. **The elevated step and its companion script** (`daemon
   install`/`status`, a synthetic Claude session touch, `status` again
   after two waits, `stop`/`start`/`status`/`uninstall`, a post-uninstall
   `schtasks` check, all appended to a log file) were staged at
   `D:\ReinstateAcceptanceProjects\v060-rc5-recheck2\h7-elevated-step.ps1`
   before polling began.
5. **The go-signal was found already present at this round's very first
   poll check** (`D:/ReinstateAcceptanceProjects/h7-go.txt`, the same file
   the first recheck used, unconsumed and unchanged since; first check at
   `2026-09-07T17:31:12Z`, file mtime predates both this round and the
   first recheck's own use of it). The elevated step was launched
   immediately via `Start-Process -FilePath powershell.exe -ArgumentList
   ... -Verb RunAs -Wait -WindowStyle Hidden -PassThru`; the UAC prompt was
   accepted within the run (launcher call start `23:01:39+05:30`
   (`17:31:39Z`), return `23:02:49+05:30` (`17:32:49Z`), ~70s wall clock;
   the elevated script's own log spans `23:01:42`–`23:02:49` local
   (`+05:30`) = `17:31:42Z`–`17:32:49Z`).
6. **The mechanism ran, for the second consecutive tagged run:** `daemon
   install` registered a real Task Scheduler task
   (`com.reinstate.daemon.fdc0f33e`); `daemon status` showed it
   `installed:true, running:true` via `schtasks`. After the synthetic
   session touch and two waits (`35s`, then `30s` more), `status` reported
   `"push":{"ok":false,"summary":"pushed 3 snapshot(s), skipped 83
   unchanged, 1 diverged (rein conflicts)","error":"push: conflict
   recorded; rein conflicts shows it"}` and `"pull":{"ok":true,"summary":
   "pulled 0 snapshot(s), skipped 0 already synced"}`. `daemon stop`,
   `daemon start`, and `daemon uninstall` each completed against the real
   scheduled task; the post-uninstall `schtasks /query` for the task name
   returned `"The system cannot find the file specified"`, confirming
   neither the daemon process nor the scheduled task remained.
7. **The same isolation defect the first recheck diagnosed reproduced
   identically, a second independent time.** The elevated script set
   `REINSTATE_HOME`/`HOME`/`USERPROFILE`/`CLAUDE_CONFIG_DIR`/`CODEX_HOME`/
   `XDG_DATA_HOME` to the fresh, disposable `h7-device` home before calling
   `rein daemon install`/`start` — but the daemon's own reported `roots` in
   every `status` call were the host's **live** agent-home paths
   (`<live CLAUDE_CONFIG_DIR>\projects`, `<live CODEX_HOME>\sessions`,
   `<live XDG_DATA_HOME>\opencode`), not the isolated ones — the same
   mechanism the first recheck identified: a Task-Scheduler-spawned process
   does not inherit the launching process's transient `$env:` overrides,
   only the account's persistent environment.
8. **New this round: the third live root's before-listing targeted the
   wrong path, disclosed rather than papered over.** This shell's own
   `CODEX_HOME` (the value used for the before-listing in point 3) differs
   from the persistent `CODEX_HOME` the elevated, Task-Scheduler-spawned
   process actually reported as its `roots` entry (`<live
   CODEX_HOME>\sessions`) — this session's interactive shell carries a
   distinct, session-scoped `CODEX_HOME` override that is not the Windows
   account's persistent value. A same-path after-listing was captured for
   the correct root once this was noticed (`6` files, listing sha256
   `71f55754…`) but with no valid before-listing to diff it against, no
   before/after comparison is possible for `CODEX_HOME` this round.
9. **New this round: the push recorded a conflict, not a clean success.**
   `rein conflicts list --json` against the isolated `h7-device` profile
   showed two conflict entries (`agent: claude, session_id:
   session-syn-001-a`, `project_id` naming a leftover synthetic fixture
   project from an earlier, unrelated acceptance run still resident on
   this shared host — metadata only, never quoted verbatim content, and
   not this recheck's own session or this recheck's own project). The
   daemon's live-tree push collided with an unrelated leftover synthetic
   testing artifact, not with any real private data — but it confirms the
   push really did read and act on the live tree, not a no-op.
10. **After-listings, and a full per-file diff this time (not just an
    aggregate-hash comparison) for the two roots with a valid
    before-listing:**
    - `<live CLAUDE_CONFIG_DIR>`: `19536` files (was `19525`), digest
      differs (`6832f93b…`). Every one of the ~20 changed lines is
      individually attributable to activity `rein push` (read-only) and
      `pull` (`0` snapshots pulled, wrote nothing back) could not have
      caused: four rotated `backups/.claude.json.backup.*` entries
      (routine backup rotation), new `file-history/<uuid>/…` entries under
      two session UUIDs that already existed before the test (this very
      recheck session's own tool-use side effects, and one other
      concurrent session's), `history.jsonl` and `image-cache` growth, and
      six pre-existing `projects/*.jsonl` files growing in size (concurrent
      sessions belonging to other, unrelated projects on this shared,
      multi-session host actively writing during the test window,
      including this very recheck session's own transcript file).
    - `<this recheck's own shell CODEX_HOME>`: `575` files, unchanged
      count, digest differs (`3ca3ffe8…`). All five changed lines are
      `*.sqlite-wal` growth — ordinary SQLite write-ahead-log churn from
      other active processes on the host — none attributable to `rein`.
      (This is not the root the daemon actually touched; see point 8.)
    - `<live XDG_DATA_HOME>/opencode`: identical digest (`205921b6…`),
      `178` files, no change — matches the first recheck.

**Disposition: `MatrixH:H7` recorded `PARTIAL` a second consecutive time,
not `PASS`.** The row's own mechanism ran to completion for a second
consecutive tagged run, and this round closes the first recheck's own
disclosed gap for two of the three live roots: a full per-file diff makes
the exclusion of any `rein`-caused write conclusive, not merely likely, for
`<live CLAUDE_CONFIG_DIR>` and `<live XDG_DATA_HOME>/opencode`. But the
isolation precondition the corrected rule requires was still not honoured
by the elevated process — the same root cause the first recheck diagnosed
reproduced identically — real live-tree reads and a real (conflicted) push
into a disposable locker occurred again, and this round introduces its own
new gap (no valid before/after comparison for the third root, `CODEX_HOME`,
because the before-listing targeted this shell's own transient value
instead of the account's persistent one). A clean `PASS` still requires the
same fix the first recheck named: the isolated environment reaching the
Task-Scheduler-spawned process itself (for example, `daemon install`
writing it into the registered task's own action rather than relying on
the installing process's transient environment) — plus, for a third
attempt, capturing the before-listing against the actual persistent
`CODEX_HOME` the elevated process will use, not this interactive shell's
own value.

### Live-home digest comparison (second recheck)

| Root | Before | After | Match? |
| --- | --- | --- | --- |
| `<live CLAUDE_CONFIG_DIR>` | `d5b8f67b…` (19525 files) | `6832f93b…` (19536 files) | No — file count and hash both differ, every changed line individually attributed (point 10) |
| `CODEX_HOME` (wrong path — see point 8) | `b57e7771…` (575 files) | `3ca3ffe8…` (575 files) | No — same count, different hash; not the root the daemon touched |
| `<live CODEX_HOME>` (correct path — after only) | not captured | `71f55754…` (6 files) | No valid before-listing this round |
| `<live XDG_DATA_HOME>/opencode` | `205921b6…` (178 files) | `205921b6…` (178 files) | Yes — identical |

### Host note

Same heavily-used, multi-session Windows host as both prior sections: this
very recheck's own session activity writes into the live
`CLAUDE_CONFIG_DIR` as a side effect of using its own Read/Edit/Write
tools, and other concurrent sessions' project directories grew during this
round's own test window. A third `H7` attempt should capture the
before-listing for `CODEX_HOME` against the value the elevated,
Task-Scheduler-spawned process will actually use (confirm it via a `daemon
status` `roots` read, or a same-process elevated probe, before relying on
any interactive shell's own `CODEX_HOME`), not assume it matches the
launching shell's own environment.

#### Second recheck outcome

- **Rows rechecked:** `grok:E1`, `grok:E2`, `grok:E3`, `MatrixH:H7` (4 of
  the 215 required rows), a second time against the same artifact.
- **Result:** all four remain not-`PASS` — `grok:E1`/`E2`/`E3` `PARTIAL`
  (live backend connectivity failure reproduced twice more, a fifth and
  sixth independent confirmation across both recheck rounds; the
  compatibility block is unchanged) and `MatrixH:H7` `PARTIAL` (mechanism
  reached and exercised a second consecutive time, the isolation
  precondition still not honoured by the elevated process for the same
  diagnosed reason, now with a conclusive per-file attribution for two of
  three live roots and a newly disclosed gap for the third).
- **Final required counts after this second recheck: `211 PASS / 4 PARTIAL
  / 0 FAIL / 0 NOT TESTED` of `215` required rows — unchanged from the
  Verdict block and the first recheck.**
- **Final device verdict: `FAIL`.** 4 of 215 required rows are not `PASS`
  and are not excused by either disposition in
  [`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict):
  `grok:E1`, `grok:E2`, `grok:E3`, `MatrixH:H7`. This second recheck does
  not authorize stable `v0.6.0`.
