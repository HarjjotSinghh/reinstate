# `v0.6.0-rc.3` tagged-artifact native Windows acceptance

`PHASE5-DEVICE-REPORT-V1`

This is the assembled device report for the **published, signed GitHub
prerelease `v0.6.0-rc.3`**, commit `202157c7877d33105dec700604dd23893b4d8b51`
(release workflow run `34076722611`). It satisfies
[`../phase-5-universal-agent-coverage-acceptance.md`](../phase-5-universal-agent-coverage-acceptance.md)
as specialized by [`../v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md)
and [`../v0.6.0-rc.3-agent-verification-prompts.md`](../v0.6.0-rc.3-agent-verification-prompts.md),
under [ADR 0005](../../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md)'s
Windows-first waiver. It merges five executors' part files
(`2026-09-07-windows-v060rc3-part-{a,b,c,d,e}.md`, all against this same tag)
into the one cumulative report the dispatch requires, plus an assembler's own
independent install and matrix-generation check (§0). The five part files are
removed from the tree in the same commit as this file; their content is
preserved here.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Matrix generated by:** `rein doctor --agents --acceptance-matrix --json`
  (re-run independently by the assembler against a fresh install; see §0)
- **Row accounting:** `216` rows total (`178` Phase 5 generated matrix +
  `22` CLI experience + `16` Hop parity journeys) — `1` row is
  `N/A (definitional)` (`opencode:D4`) — **`215` required rows**.
- **Host-credential rows:** `0`. Unlike `v0.6.0-rc.2` (4 `qwen` rows excused
  under the `NOT TESTED (host credential)` disposition), this run's `qwen`
  gap is a **version-compatibility block** (host `qwen` auto-updated to
  `0.23.0`, above the verified `0.21.12`–`0.21.13` ceiling; `qwen`'s own
  credential-backed probe succeeded — see §12), which does not match the
  `NOT TESTED (host credential)` disposition's own text. Zero rows this run
  qualify for that disposition.
- **Required counts:** `201 PASS / 3 PARTIAL / 1 FAIL / 10 NOT TESTED` of
  `215` required rows.
- **Release-blocking findings:** `14` required rows are not `PASS` and are
  not excused by either disposition in
  [`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict):
  `pi:C3`, `qwen:D1`, `qwen:D2`, `qwen:D3`, `qwen:D4`, `qwen:D5`, `qwen:E1`,
  `qwen:E2`, `qwen:E3`, `qwen:E5`, `qwen:E6`, `codex:E5`, `opencode:E5`,
  `grok:E5`. See §16 and §20.

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required
result is `FAIL`. Because 14 of the 215 required rows are not `PASS` and are
not covered by an applicable disposition, the device verdict is `FAIL` and
this report does not authorize stable `v0.6.0`. No product code was changed
by this assembly; every result below is transcribed from the five part
files' own evidence. Unlike the `v0.6.0-rc.2` assembly, no part's
self-reported summary count disagreed with its own row table this run (§0.6)
— no corrections were needed.

**This candidate's own fix is confirmed.** The Cursor CLI store-schema
defect `v0.6.0-rc.2`'s post-commit finding (report §0.12) identified —
`cursor:C2`/`cursor:C3` reading a `messages`/`message`/`bubbles` schema no
real Cursor CLI store has — is **cleared**: both rows now `PASS` against the
real host store, reading the actual `blobs`/`meta` schema. See §18.

### Failing rows (14, none pass a required row)

| Row | Result | Category | Reason (full evidence in the matrix sections below) |
| --- | ------ | -------- | ---- |
| `pi:C3` | FAIL | product, new this run | `pi -p` with a planted token completed live through Pi's own configured Anthropic login and wrote a real `version:3` session with the token in a `role:"user"` message on disk, but `rein search` found 0 matches and `rein inspect` shows no `prompt_preview` at all. Root-caused: `internal/agents/sources/pi/source.go`'s `readConversation` calls `ExtractTextContent(item["message"])`, and `ExtractTextContent`'s `map[string]any` branch only reads a top-level `"text"` key, never descending into `message.content[].text` (the real shape). The committed fixture uses an older flat schema the code does handle, so the unit suite stays green while every real Pi session on this host is affected. Not a host-credential or availability gap — a confirmed code defect. |
| `qwen:D1` | NOT TESTED | host, version drift, new this run | Host's real `qwen` reports `0.23.0`, above the verified ceiling `0.21.12`–`0.21.13`; `rein` correctly refuses (`exit 5`, `agent.version` block) before building a launch plan. |
| `qwen:D2` | NOT TESTED | same | Same version-drift block. |
| `qwen:D3` | NOT TESTED | same | Same version-drift block. |
| `qwen:D4` | NOT TESTED | same | Same version-drift block. |
| `qwen:D5` | NOT TESTED | same | Same version-drift block. |
| `qwen:E1` | NOT TESTED | same | `resume --dry-run` exits `5` (`agent.version`) before reaching launch. |
| `qwen:E2` | NOT TESTED | same | Same version-drift block. |
| `qwen:E3` | NOT TESTED | same | Same version-drift block. |
| `qwen:E5` | NOT TESTED | same | Version-drift block preempts before `agent.active` is reached as a scoreable outcome. |
| `qwen:E6` | NOT TESTED | same | Version-drift block (`exit 5`) fires before the non-interactive safety check (`exit 7`) is reached. |
| `codex:E5` | PARTIAL | harness gap, new this run | `codex resume` requires a real TTY (`Error: stdin is not a terminal`); this run's headless `mkfifo` technique (which worked for `claude`) does not apply. A non-interactive retry returned `agent.active status:"unknown"` — inconclusive, not a clean detection. |
| `opencode:E5` | PARTIAL | harness gap, new this run | OpenCode's bare TUI invocation hung on the `mkfifo` open with no reader; not cleanly re-tested this run. |
| `grok:E5` | PARTIAL | harness gap, new this run | Not re-attempted this run given time already spent on `grok`'s `C3`/`E1`–`E3` retries; the `agent.active` mechanism was confirmed correct via its own earlier match/false baseline. |

`qwen:D1`–`D5`/`E1`/`E2`/`E3`/`E5`/`E6` (10 rows) and `codex:E5`/
`opencode:E5`/`grok:E5` (3 rows) are all **new** findings this run — every
one of them `PASS`ed at `v0.6.0-rc.2`. None is caused by this candidate's own
change (the Cursor CLI store reader); see §18 and §21.

## 0. Assembler verification

Performed independently of, and in addition to, all five part files, in the
assembler's own fresh directory
(`D:\ReinstateAcceptanceProjects\v060-rc3-assembler\`), never touching any
part executor's own lab directories or the shared worktree's tracked files
before this commit.

1. **Checksum.** `sha256sum` of the coordinator's staged
   `reinstate_0.6.0-rc.3_windows_amd64.zip` matched `checksums.txt`
   (`5fc5188ea92706e841d9c022cfe29ab386430a1a54a90139eec16d9baf756cd0`)
   independently.
2. **Install.** Unzipped into a fresh directory
   (`D:\ReinstateAcceptanceProjects\v060-rc3-assembler\install\`), never a
   shared or developer binary. `rein.exe`/`reinstate.exe` `sha256sum`: both
   `6517281bc5a5984e59f030238e1525d355201bb849762db00e403a990fcde7d0`; `cmp`
   exit `0` (byte-identical).
3. **Version.** `rein version --json`:
   `{"commit":"202157c7877d33105dec700604dd23893b4d8b51","date":"2026-09-07T02:36:11Z","name":"reinstate","version":"0.6.0-rc.3"}`
   — matches every part file's own artifact-identity table exactly. Plain
   `rein version`: `reinstate 0.6.0-rc.3 (202157c7877d33105dec700604dd23893b4d8b51 2026-09-07T02:36:11Z)`.
4. **Published release, read-only.** `gh release view v0.6.0-rc.3 --json
   tagName,targetCommitish,isDraft,isPrerelease,publishedAt`:
   `{"isDraft":false,"isPrerelease":true,"name":"v0.6.0-rc.3","publishedAt":"2026-09-07T02:42:26Z","tagName":"v0.6.0-rc.3","targetCommitish":"main"}`
   — non-draft prerelease, tied to the tag. No write operation performed.
5. **Required row count.** `rein doctor --agents --acceptance-matrix --json`
   against this fresh install: top-level `"row_count": 178`,
   `"core_row_count": 33` (`A:10, B:9, G:8, H:6`), 18 agents at tiers
   `T0×7, T1×4, T2×2, T4×2, T5×3` — matches the candidate's census in the
   dispatch exactly (no tier promotion this candidate, unchanged from
   `v0.6.0-rc.2`).
6. **Row-set reconciliation.** Programmatically generated the full set of
   178 expected row identifiers from that JSON (33 core rows + each agent's
   own row list) and compared it against the union of every row id assigned
   to part A (core Matrix A/B/G/H, Matrix F for all 7 T0 agents, Matrix C for
   the 4 T1 agents — 71 Phase-5 rows) and part B (Matrix C/D/E for the 7
   T2+ agents — 107 Phase-5 rows): **the two sets are set-equal, 178
   elements each, zero overlap, zero gap.** Every required Phase 5 row
   appears in exactly one part file. Part A's own Section-A automated-gates
   table (8 rows) is a separate, non-Phase-5 gate set and is not counted in
   the 178, matching the dispatch's own accounting (`178 + 22 + 16 = 216`).
7. **Part self-report cross-check.** Recounted every part's own row table
   cell-by-cell against its self-reported summary counts:
   - Part A: row table sums to `78 PASS, 1 FAIL` across 79 assigned rows
     (Section A gates 8/8, Matrix A 10/10, Matrix B 9/9, Matrix G 8/8,
     Matrix H 6/6, Matrix F 14/14, Matrix C 23 PASS + 1 FAIL `pi:C3`) —
     matches its own structured summary exactly.
   - Part B: row table sums to `93 PASS, 3 PARTIAL, 10 NOT TESTED, 1 N/A`
     across 107 assigned rows (`opencode:D4` recorded `NOT TESTED` in its
     per-row cell but is the definitional exclusion, giving 106 required
     rows, of which 93 PASS) — matches its own structured summary and its
     own "93 PASS of 106 required" closing line.
   - Parts C, D, E: `22/22`, `11/11`, `5/5` PASS, each matching its own row
     table exactly.

   **No correction was needed to any part's counts this run** — unlike the
   `v0.6.0-rc.2` assembly, which found and corrected a discrepancy in part
   A's own T1 Matrix-C footer.
8. **Total reconciliation.** `71` (part A Phase 5) + `106` (part B Phase 5,
   excluding `opencode:D4`) + `22` (part C) + `11` (part D) + `5` (part E) =
   **`215`**, matching the dispatch's required-row count exactly. PASS sum:
   `70` (part A) + `93` (part B) + `22` (part C) + `11` (part D) + `5` (part
   E) = **`201`**. FAIL: `1` (`pi:C3`, part A). PARTIAL: `3` (`codex:E5`,
   `opencode:E5`, `grok:E5`, part B). NOT TESTED: `10` (`qwen` D/E rows,
   part B). `201 + 1 + 3 + 10 = 215`.
9. **Disposition-rule check.** Applied
   [`v0.6.0-windows-acceptance.md`'s "Dispositions that do not block the
   device verdict"](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict)
   rule mechanically to every non-`PASS` required row this run recorded.
   - `opencode:D4` — `N/A (definitional)`, unchanged reason (OpenCode's
     SQLite-only store has no JSONL record boundary). Excluded from both the
     denominator and the numerator, as required. Not a disposition on a
     required row.
   - The ten `qwen` `NOT TESTED` rows (`D1`–`D5`, `E1`/`E2`/`E3`/`E5`/`E6`) do
     **not** match `NOT TESTED (host credential)`: that disposition's own
     text requires "the host's vendor credential for that agent is expired
     or absent." Part B's own evidence directly disproves this for `qwen`
     this run — its `-p "Reply PONG"`-style probe and its planted-token `C3`
     session both completed successfully against the live default home (see
     §10); the block is `agent.version` (a hard compatibility gate on the
     auto-updated `0.23.0` install), not a credential failure. A row that
     does not match the disposition's own definition does not get the
     disposition's exemption, regardless of whether the mechanical
     PASS-for-`claude`/`codex`/`opencode`-and-PASS-for-`grok` conditions
     would otherwise have been satisfied. All ten block.
   - `codex:E5`, `opencode:E5`, `grok:E5` are `PARTIAL`, not `NOT TESTED` —
     `PARTIAL` is not one of the two named dispositions at all, and the
     contract's "Everything else" clause applies: `PARTIAL` does not pass a
     required row. All three block.
   - **Zero rows this run qualify for `NOT TESTED (host credential)`.**
     This is a genuine difference from `v0.6.0-rc.2` (which had 4 such rows,
     one of them excused) — not a relaxation of the rule, a consequence of
     this run's `qwen` gap having a different, non-credential root cause.
10. **Cleanup.** The assembler's own install and matrix output live under
    `D:\ReinstateAcceptanceProjects\v060-rc3-assembler\` and are deleted
    after this report is committed; nothing from that directory is
    referenced by path elsewhere in this report.

11. **Post-verification corrections (coordinator, append-only).** (a) §22's
    closing sentence said "eight of the twelve" carried dispositions cleared;
    §18's corrected table counts seven cleared and six re-recorded (13 rows
    including `opencode:D4`); §22 now says seven. (b) Every literal path of
    a live agent home that the URGENT items named is replaced by the
    placeholder form the evidence policy requires (`<live CLAUDE_CONFIG_DIR>`,
    `<live CODEX_HOME>`, `<live XDG_DATA_HOME>`); the maintainer knows the
    real values. (c) Cleanup performed on 2026-09-07 before this commit: the
    two stray synthetic files (the Claude `session-syn-001-a.jsonl` under its
    fixture project directory, and the Codex `rollout-syn-001-a.jsonl`) were
    deleted. The live OpenCode store was compared read-only against the
    pristine backup by session id: it holds every backup session plus exactly
    two additions, the synthetic `ses_fixture001a` (the H7 daemon pull) and
    executor B's own throwaway T5 session; nothing from the backup is
    missing, so the store was not rolled back. Removing those two rows is
    left to the maintainer (Q24).

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-09-07T02:36:00Z`–`2026-09-07T09:10:00Z` (span across all five parts; each part's own timestamp is in its section below); assembler verification `2026-09-07T09:19:00Z`–`2026-09-07T09:30:00Z` |
| Device | `windows-amd64` |
| OS/version/build | Windows 11 Pro, `10.0.26200`, native `windows/amd64`, never WSL |
| CPU architecture/native process | `amd64`, native process (no emulation) |
| Filesystem | NTFS |
| Tested tag | `v0.6.0-rc.3` |
| Tested full commit | `202157c7877d33105dec700604dd23893b4d8b51` |
| Installed binary SHA-256 | `6517281bc5a5984e59f030238e1525d355201bb849762db00e403a990fcde7d0` |
| Installed version JSON | `{"commit":"202157c7877d33105dec700604dd23893b4d8b51","date":"2026-09-07T02:36:11Z","name":"reinstate","version":"0.6.0-rc.3"}` |
| Catalog agent count | `18` |
| Catalog tier census | `T0: 7, T1: 4, T2: 2, T3: 0, T4: 2, T5: 3` (unchanged from `v0.6.0-rc.2`) |
| Generated required row count | `178` (`rein doctor --agents --acceptance-matrix --json`, independently reproduced by the assembler in §0 and by part A) |
| Git version | `2.52.0.windows.1` |
| Go version/toolchain | `go1.25.13` (pinned via `GOTOOLCHAIN`; host default `go1.26.1`) |
| Report branch | `v060/rc3-tagged` |
| Device-report commit | filled in by the commit that lands this file |
| Draft report PR | NOT CREATED (worktree report, per this run's dispatch — no PR required) |

### Bootstrap deviation (applies to every part below except executor A)

Per this run's ground rules, only executor A installs from the live
`https://reinstate.dev/install.ps1` bootstrap and records that as the
artifact identity; every other executor (B, C, D, E) and the assembler
installed from the coordinator's pre-verified, checksummed draft directory
(`…/scratchpad/rc3-draft/`) instead — the assigned methodology, not an
unplanned deviation. Executor A's own bootstrap check (Header, part A): the
live bootstrap was downloaded fresh with `Invoke-WebRequest -OutFile`, its
pinned version line reads `$Version = "v0.6.0-rc.3"`, and installing from it
(with `INSTALL_DIR` and `REINSTATE_SKIP_PATH_UPDATE=1` set so it never
touched the persistent user `PATH` or replaced the host's pre-existing
install) produced binaries byte-identical to (and reporting the same
version/commit/date as) the checksummed archive — **no deviation** between
the live bootstrap route and the release-commit candidate. (Note: this host
also carries a pre-existing `rein`/`reinstate` install at
`%LOCALAPPDATA%\Programs\Reinstate\bin`, version `0.3.0-rc.6`, on the
persistent user `PATH` — the bootstrap's own canonical installer correctly
refused to silently replace it without `REINSTATE_CONFIRM_REPLACE=1`, which
this run never set, confirming the "never replace a user binary" rule holds
even for the bootstrap path.)

### Artifact identity (common to all parts)

| Field | Value |
| ----- | ----- |
| Tag | `v0.6.0-rc.3`, published GitHub prerelease, signed tag verified against `.github/allowed_signers` (coordinator, pre-run; re-confirmed non-draft/prerelease read-only by the assembler in §0.4) |
| Full commit | `202157c7877d33105dec700604dd23893b4d8b51` |
| Release workflow run | `34076722611` |
| Windows archive | `reinstate_0.6.0-rc.3_windows_amd64.zip` |
| Archive SHA-256 | `5fc5188ea92706e841d9c022cfe29ab386430a1a54a90139eec16d9baf756cd0` (matches `checksums.txt`; re-verified independently by every part and the assembler) |
| Installed binary SHA-256 | `6517281bc5a5984e59f030238e1525d355201bb849762db00e403a990fcde7d0` — `rein.exe`/`reinstate.exe` byte-identical (`cmp` exit `0`) in every install |
| `rein version --json` | `{"commit":"202157c7877d33105dec700604dd23893b4d8b51","date":"2026-09-07T02:36:11Z","name":"reinstate","version":"0.6.0-rc.3"}` |
| Previous-release comparison binary (rows 2, 22, G7) | `reinstate_0.5.1_windows_amd64.zip`, SHA-256 `b724ca3da4e124004063032d63240c244fb9a59279076f0e87441d327a826e8c` |

## 2. Agent inventory on this device

| Agent key | Installed | Vendor version | Sessions available | Distinct projects | Declared tier | Tier testable here |
| --------- | --------- | -------------- | ------------------- | ----------------- | -------------- | -------------------- |
| `claude` | YES | `2.1.263` | ≥2 real + fixtures | ≥2 | T5 | yes |
| `codex` | YES | `0.149.0` | ≥2 real + fixtures | ≥2 | T5 | yes, `E5` PARTIAL this run (no PTY, see §12) |
| `opencode` | YES | `1.18.27` | ≥2 real + fixtures | ≥2 | T5 | yes, `E5` PARTIAL this run (no PTY) |
| `grok` | YES | `1.0.5` | ≥2 real + fixtures | ≥2 | T4 | yes, `E1`/`E2`/`E3` cleared this run (see §18); `E5` PARTIAL (not re-attempted) |
| `qwen` | YES | `0.23.0` (above the verified `0.21.12`–`0.21.13` ceiling) | ≥4 real (mixed) | ≥3 | T4 | credential-limited no longer — **version-limited**: `D1`–`E6` except `C1`–`C6`/`E4` are `NOT TESTED`, correctly refused by `agent.version` |
| `gemini` | YES (live default home, no portable credential file found) | `0.53.0` | real sessions | ≥2 | T2 | yes |
| `kimi` | YES | `0.36.1` | ≥2 real | ≥2 | T2 | yes |
| `pi` | YES | `0.73.1` | 5 real (incl. this run's) | ≥3 | T1 | yes, `C3` **FAIL** this run — confirmed code defect (see §10, §16) |
| `cursor` | YES | (host; `cursor-agent` `2026.08.11` CLI still broken) | 2 real | 2 | T1 | yes, `C2`/`C3` **cleared this run** via the real host store (see §10, §18) |
| `copilot` | YES | `1.0.80` | 2 real | 2 | T1 | yes |
| `cline` | YES (now authenticated) | (host) | 5 real | 4 | T1 | yes, `C3` **cleared this run** (Cline now answers live) |
| `aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode` | ABSENT | — | — | — | T0 | yes (refusal/no-capability rows only) |

## 3. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------- |
| Annotated tag signature verifies against `.github/allowed_signers` | PASS | Verified by the coordinator pre-run (dispatch preamble) |
| Tag peels to the recorded full commit on `origin/main` | PASS | `202157c7877d33105dec700604dd23893b4d8b51`, confirmed as the worktree's own HEAD-of-merge and `git log` ancestor |
| Published release is non-draft, prerelease, and tied to the tag | PASS | Coordinator pre-run; re-confirmed read-only by the assembler (§0.4): `isDraft:false, isPrerelease:true, targetCommitish:"main"` |
| Exact expected release asset set is present | PASS | `verify-release.ps1` (A6) |
| Checksums, GitHub API digests, and attestations match the tag and commit | PASS | Coordinator pre-verified (`checksums.txt` match, `gh attestation verify` on the Windows archive); independently re-verified by every part and the assembler |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | PASS | `verify-release.ps1`/`test-install.ps1` (A6/A7) |
| Live bootstrap is byte-identical to the tested commit and pins only this candidate | PASS | Executor A: pinned version line `v0.6.0-rc.3`, resulting binary byte-identical to the checksummed archive — **no deviation** this candidate |
| Bootstrap installer digest matches the tagged canonical installer | PASS | Executor A |
| Fresh isolated install did not replace a user binary or persist PATH changes | PASS | Every part and the assembler installed into its own fresh directory; no user binary replaced (part A also flags a pre-existing unrelated `0.3.0-rc.6` install on this host's persistent PATH — worked around per-command, not touched; the live bootstrap's own guard was independently observed refusing to replace it) |
| `rein` and `reinstate` have identical verified bytes and report the candidate | PASS | `cmp` exit `0` in every install; SHA-256 `6517281bc5a5984e59f030238e1525d355201bb849762db00e403a990fcde7d0` throughout |
| Installed binary reports the literal full tested commit | PASS | `rein version --json` matches `202157c7877d33105dec700604dd23893b4d8b51` in every install |

## 4. Automated gates (Section A of the dispatch; not part of the 178)

Source: part A, run once from the worktree per the ground rules.

| # | Gate | Result | Evidence |
| - | ---- | ------ | -------- |
| A1 | Format, vet, tidy | PASS | `gofmt -l .`, `go vet ./...`, `go mod tidy -diff` all empty, exit 0 |
| A2 | Unit suite | PASS | `CGO_ENABLED=0 go test ./... -count=1 -p 4`: every package `ok`, 0 `FAIL` |
| A3 | Race suite | PASS | `CGO_ENABLED=1 go test -race ./internal/... -count=1 -p 4`: every package `ok`, 0 `FAIL` |
| A4 | Lint and vuln | PASS | `make lint` → golangci-lint v2.11.4, `0 issues`; `make vuln` → govulncheck v1.6.0, `0 vulnerabilities` in code (4 in required-but-uncalled modules, informational) |
| A5 | Doc gate | PASS | `go test ./internal/doctest/... -count=1` → `ok`; `scripts/check-docs.ps1` exit 0 |
| A6 | Snapshot and artifacts | PASS | `scripts/verify-release.ps1 -DistDir rc3-draft` exit 0; `scripts/check-release-binary-identity.ps1` exit 0 |
| A7 | Installers | PASS | `scripts/test-install.ps1 -DistDir rc3-draft` exit 0 |
| A8 | Cross-OS build | PASS | `GOOS=darwin GOARCH=arm64` and `GOOS=linux GOARCH=amd64 go build ./...` both exit 0 |

**8/8 PASS.**

## 5. Matrix A — Catalog integrity (10/10 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| A1 | Descriptors validate at start; no duplicate or empty keys | PASS | 18 agent keys, unique, none empty |
| A2 | `doctor --agents` lists every agent including T0 | PASS | 18 agents, all 7 T0 present |
| A3 | Every T0 agent shows an enumerated reason | PASS | `layout_unverified` (aider, antigravity, minimax-code, roo), `server_backed` (amp, openhands), `desktop_only` (zcode) |
| A4 | Declared tier and capability constructors agree | PASS | Conformance suite passes for all shipped agents |
| A5 | Every declared evidence path exists in the tagged tree | PASS | `checkEvidence` conformance check passes |
| A6 | `sessions --agent` accepts exactly T1+ keys, else exit `2` | PASS | All 7 T0 keys + a bogus key → exit 2; all 11 T1–T5 keys → exit 0 |
| A7 | `resume` accepts exactly T3+ keys, else exit `5` with reason | PASS | T0/T1/T2 keys → exit 5, tier named; T3+ keys → exit 2 (session not found, tier gate passed) |
| A8 | Agent ordering is deterministic across runs | PASS | Two runs, identical 18-key order (harness note: live host activity between captures changed only `generated_at`/one directory's child count, not ordering) |
| A9 | Broken evidence path fails conformance | PASS | `TestBrokenEvidencePathFails`/`TestRunFailsBrokenEvidence` pass |
| A10 | No scan writes, renames, or locks under any agent root | PASS | Full recursive mtime+size snapshot of the real `%USERPROFILE%\.pi\agent` tree (46 files), identical before/after a full refresh |

## 6. Matrix B — Probe and redaction (9/9 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| B1 | Output conforms to `AGENT-PROBE-V1` | PASS | `"schema": "AGENT-PROBE-V1"` |
| B2 | No absolute path, home name, or username | PASS | Full-document scan: 0 hits |
| B3 | No JSON values from session files, keys only | PASS | `first_line_keys` are field names only |
| B4 | Names shape-normalized | PASS | Verified against a real OpenCode Git object store: `<40-hex>`, `pack-<40-hex>.idx` shapes, never a literal object/pack name |
| B5 | No excluded subtree present | PASS | Full-document scan for credential/cache patterns: 0 hits |
| B6 | Absent agent reported without error | PASS | T0 agents: `resolved_root: null`, exit 0 |
| B7 | Empty root distinguishable from absent root | PASS | Kimi's `.kimi-code` (`exists:true, marker_present:true`) vs `.kimi` (`exists:true, marker_present:false`) observed live |
| B8 | Wrapper scripts match binary output | PASS | `.sh`/`.ps1` identical to direct invocation, back-to-back (a first pass caught one live-host-timing artifact, resolved by an immediate re-run) |
| B9 | Secret scanner passes over committed probes | PASS | Pattern scan over 26 files under `docs/testing/results/agent-probes/`: 0 hits, plus passing `internal/secretscan` suite |

## 7. Matrix G — No regression (8/8 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| G1 | Claude/Codex native resume and fork unchanged | PASS | Regression-suite evidence (`internal/agentcheck`, catalog descriptors); the fresh live journey itself is Matrix E, part B |
| G2 | Both structured handoff directions complete | PASS | `internal/handoff/...` suite; fresh live journey is Matrix D, part B |
| G3 | Gemini/Kimi remain sources and read-only | PASS | `resume`/`fork` on T2 keys: exit 5, tier-named reason |
| G4 | Push and pull carry Claude, Codex, and OpenCode | PASS | `rein __complete push --agent ""`/`pull --agent ""`: exactly `claude, codex, opencode` |
| G5 | Cross-OS path remapping unchanged | PASS | `internal/pathmap/...` suite passes; physical Windows↔Windows proof is H11 |
| G6 | Exit-code semantics unchanged; no new codes | PASS | Only documented codes (`0,2,3,4,5,7`) observed across this run's live invocations |
| G7 | A `v0.5.1`-lineage index upgrades without data loss | PASS | A real 100-session index built with `v0.5.1` reopened by `v0.6.0-rc.3`: 100/100, identical per-agent distribution, no migration prompt |
| G8 | Capsules remain excluded from sync | PASS | `TestRefuseHandoffsPush` passes |

## 8. Matrix H — CLI contract and performance (6/6 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| H1 | CLI reference matches shipped flags | PASS | `internal/doctest` passes |
| H2 | `--json` shapes match documentation | PASS | Live `sessions`/`search`/`inspect`/`doctor --agents` conform |
| H3 | Full refresh within documented ceiling | PASS | `11.046s` cold, 10 agents (rc.2's tagged run: `13.149s` — consistent, no regression) |
| H4 | No-change refresh materially faster | PASS | `0.200s` warm, ~98% faster |
| H5 | One slow or unreachable source does not block others | PASS | `TestSlowAgentDoesNotDiscardTheRun` passes |
| H6 | Completion lists correct agent keys per command | PASS | `push`/`pull --agent` → `claude, codex, opencode`; `sessions --agent` → T1+ only; `handoff --to` → `claude, codex, grok, opencode, qwen` — all surfaces agree |

## 9. Matrix F — Per T0 agent (14/14 PASS)

Source: part A. All 7 T0 agents (`aider`, `amp`, `antigravity`,
`minimax-code`, `openhands`, `roo`, `zcode`).

| Agent key | F1 listed with reason | F2 no capability offered or implied |
| --------- | --------------------- | ------------------------------------- |
| `aider` | PASS — `t0_reason=layout_unverified` | PASS |
| `amp` | PASS — `t0_reason=server_backed` | PASS |
| `antigravity` | PASS — `t0_reason=layout_unverified` | PASS |
| `minimax-code` | PASS — `t0_reason=layout_unverified` | PASS |
| `openhands` | PASS — `t0_reason=server_backed` | PASS |
| `roo` | PASS — `t0_reason=layout_unverified` | PASS |
| `zcode` | PASS — `t0_reason=desktop_only` | PASS |

## 10. Matrix C — Per T1+ agent (66 rows: 65 PASS, 1 FAIL)

Per the ground rules, T1 discovery rows ran read-only through `rein` alone
against the host's real root; corruption rows used a copy, never live data.

### T1 agents (source: part A)

Evidence policy: `C1`/`C2`/`C4`/`C5`/`C6` ran `rein` alone, read-only,
against the host's real vendor roots (counts and field names only). `C6`
used a **copy** of each real tree, deleted immediately after use. `cline:C3`
and `pi:C3` created fresh throwaway sessions through the vendor's own live
CLI in an isolated project, with a planted, non-sensitive search token.
`cursor:C2`/`C3` and `copilot:C3` ran against the host's real store under
the T1-discovery exception (counts, field names, and a self-chosen search
word only).

| # | cline | copilot | cursor | pi |
| - | ----- | ------- | ------ | -- |
| C1 (≥2 sessions, ≥2 projects) | PASS — 5 sessions / 4 projects | PASS — 2 sessions / 2 projects | PASS — 2 sessions / 2 projects | PASS — 4 sessions / 3 projects (before this run's own 5th) |
| C2 (metadata matches vendor) | PASS — real non-zero `message_count` (2,1,1,1,1) | PASS — real `message_count` (5, 26) | **PASS — fix confirmed against the real host store**: both real sessions now report non-zero `message_count` (3, 3), reading the store's actual `blobs`/`meta` schema (`v0.6.0-rc.2`'s run recorded `0` for these same sessions against a `messages`/`message`/`bubbles` schema no real store has) | PASS — real non-zero `message_count` (2,2,2,2) |
| C3 (message-body search) | **PASS** — `cline` now answers live (not `Unauthorized`); created a fresh session with a planted token; `rein search` found exactly 1 match | **PASS** — real host session, a word from its own bounded `prompt_preview`, exactly 1 match | **PASS — fix confirmed against the real host store**: a word chosen from a real session's own bounded `prompt_preview`, `rein search <word> --agent cursor --json` → exactly 1 match, the correct session; `go test ./internal/agents/sources/cursor/...` also PASS against `blobs`/`meta` fixtures | **FAIL — new finding, root-caused, not a host-credential gap.** `pi -p` with a planted token completed live through Pi's own configured Anthropic login (`defaultProvider: anthropic`, `claude-opus-4-7`) and wrote a real session with the token in a `role:"user"` message. `rein search` returned 0 matches; `rein inspect` shows no `prompt_preview` field at all (all 5 real sessions on this host show this). Root cause: `internal/agents/sources/pi/source.go`'s `ExtractTextContent(item["message"])` only reads a top-level `"text"` key on the message map, never descends into `message.content[].text` (the real `version:3` shape); the committed fixture uses an older flat `version:1` schema the same code handles, so the unit suite stays green. `message_count` is unaffected (both turns share the outer `"type":"message"` case), which is why C2 does not surface it. |
| C4 (bounded inspect, no body) | PASS — 77-char `prompt_preview` | PASS — 30-char `prompt_preview` | PASS — 160-char `prompt_preview` | PASS — bounded shape, no crash; `prompt_preview` absent rather than any transcript body (consistent with the C3 finding) |
| C5 (resume gate correct for tier) | PASS — exit 5 | PASS — exit 5 | PASS — exit 5 | PASS — exit 5 |
| C6 (corrupt/empty/absent degrade cleanly) | PASS — truncated `.messages.json` copy: unaffected sessions still list, exit 0; empty/absent → `[]` | PASS — truncated `session.db` copy: no crash, exit 0; empty/absent → `[]` | PASS — truncated `store.db` copy (via `CURSOR_CONFIG_DIR`): degrades to `message_count:0` (fail-closed), unaffected session unaffected, exit 0; empty/absent → `[]` | PASS — truncated `.jsonl` copy mid-record: no crash, all 5 sessions list, exit 0; empty/absent → `[]` |

**24 rows: 23 PASS, 1 FAIL** (`pi:C3`, a confirmed reader defect — see §16).
Two of the three `v0.6.0-rc.2`-carried Matrix-C findings this dispatch
specifically targeted are cleared against **real host data**: `cursor:C2`/
`C3` (this candidate's own intended fix) and `cline:C3` (the host's Cline
credential is now authenticated). `copilot:C3` remains PASS as at rc.2.
`pi:C3` is a **new** finding — it was `PARTIAL` (host/harness, no searchable
content) at rc.2 and is now `FAIL` (a confirmed code defect), because this
run succeeded in creating the real-schema session rc.2 could not.

### T2+ agents (source: part B; 7 agents × 6 rows = 42 rows, all PASS)

Real sessions were created in throwaway git repositories with each vendor's
own headless flag and a planted per-agent, per-project token
(`RC3B-<AGENT>-P<N>-<hex>`) in the **message body**. `claude` used the
host's live, already-signed-in `CLAUDE_CONFIG_DIR`; `codex`/`opencode`/
`grok`/`kimi` used an isolated home seeded only with the needed credential
file; `qwen`/`gemini` used the live default home (no portable credential
file found for either, same practical compromise as `v0.6.0-rc.2`).

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| C1 (≥2 sessions, ≥2 projects) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C2 (metadata matches vendor) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C3 (search finds planted token, message body) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C4 (bounded inspect, no transcript body) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C5 (resume gate correct for tier) | PASS | PASS | PASS | PASS | PASS | PASS (T2 refusal) | PASS (T2 refusal) |
| C6 (corrupt/empty/absent degrade cleanly) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |

Pre-existing real data alone cleared C1 for every agent: `claude` 52
sessions/24 projects, `codex` ≥14 projects, `opencode` 2/2, `grok` 6/4,
`qwen` 8/6, `gemini` 24/14, `kimi` 3/3, before this run planted anything.
`rein search <token> --agent <key> --json` found exactly one matching
session for each of the seven, keyed to the vendor's own minted session id.
`qwen:C5`: `capabilities.resume=true` correctly declared for T4, independent
of the version-drift block described in §11/§12 — the tier gate and the
version gate are separate mechanisms and this run confirmed the tier gate
works even though the version gate later blocks D/E rows.

C6 corruption evidence (copy truncated to ~half length, empty root, absent
root; all through `rein sessions --agent <key> --json`, no panic, no partial
record surfaced as complete):

| Agent | Corrupt-copy result | Empty/absent-root result |
| ----- | -------------------- | -------------------------- |
| `claude` | `message_count` 4→2, `incomplete_trailing_record` warning | `{"sessions":[]}` |
| `codex` | `message_count` dropped, same warning | `{"sessions":[]}` |
| `opencode` (SQLite, last 50 bytes stripped) | listed cleanly, no crash | `{"sessions":[]}` |
| `grok` | corrupted session dropped (`session_read_failed`); other session in the copy still listed | `{"sessions":[]}` |
| `qwen` | 9 sessions still listed, no crash | `{"sessions":[]}` |
| `gemini` | 24 sessions listed, 4× `session_read_failed` warnings | `{"sessions":[]}` |
| `kimi` | listed, `message_count` degraded to `0`, no crash | `{"sessions":[]}` |

## 11. Matrix D — Per T2+ agent (35 rows: 29 PASS, 1 N/A definitional, 5 NOT TESTED)

Source: part B. D1/D2/D3/D5 all ran through `rein handoff <key>:<id> --to
<dest> --dry-run --json` from the session's own workspace directory. D4 used
`--no-launch` against a **copy of the source truncated to half its byte
length**.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| D1 (capsule + fidelity report) | PASS | PASS | PASS | PASS | **NOT TESTED** | PASS | PASS |
| D2 (no invented content) | PASS | PASS | PASS | PASS | **NOT TESTED** | PASS | PASS |
| D3 (unknowns referenced/omitted with reason) | PASS | PASS | PASS | PASS | **NOT TESTED** | PASS | PASS |
| D4 (truncation boundary, offset + hash, independently recomputed) | PASS | PASS | N/A (definitional) | PASS | **NOT TESTED** | PASS | PASS |
| D5 (two runs byte-identical) | PASS | PASS | PASS | PASS | **NOT TESTED** | PASS | PASS |

**`qwen`'s real installed version has drifted past the verified ceiling
(host/harness, not a product defect — new finding this run).**

```
$ rein resume qwen:5d71731d-eaab-430f-aa28-fee79e5c295b --dry-run --json
{"code":"compatibility","message":"environment preflight is blocked", …
  "agent.version": {"severity":"block","actual":"0.23.0",
    "message":"native agent version 0.23.0 is outside the verified range
    0.21.12 to 0.21.13 inclusive"} …}
exit=5
```

`qwen --version` reports `0.23.0`; the catalog's verified range is
`0.21.12`–`0.21.13` (`internal/agents/catalog/qwen.go`). This is a
**correct refusal**: a block-severity check cannot be bypassed with
`--allow-environment-warning`, and `handoff --allow-untested` did not change
the outcome. It is unrelated to the `v0.6.0-rc.2`/`rc.1` finding for `qwen`
(an expired credential) — this run's `qwen -p "Reply with exactly this
token…" < NUL` completed successfully (§10 above), so the credential is
fine; the **version** is what now blocks. No agent was installed or
reinstalled for a row, per the ground rules. **D1–D5 and E1/E2/E3/E5/E6 for
`qwen` are recorded `NOT TESTED` for this reason.** This does not match
either disposition (it is not a credential gap, and `qwen`'s T4 peer `grok`
passes these same rows), so these rows block the device verdict — see §0.9,
§16, §20.

D1/D2/D3 evidence: every successful `--dry-run --json` document carried the
full capsule envelope (`capabilities`, `destination`, `fidelity`,
`handoff_id`, `lineage_root`, `parse`, `planned_files`, `policy`,
`projection_events`, `redactions`, `security`, `source`, `warning_ids`,
`workspace`); every `omitted` fidelity component carried a machine-readable
`reason` (`requires_optional_summarizer`, `interrupted_not_replayed`, etc.),
never a guess with no reason.

D4 evidence (offset/hash independently recomputed from the raw copy):

| Agent | `raw_source.byte_offset` | Independently recomputed hash | Match |
| ----- | ------------------------- | -------------------------------------------------- | ----- |
| `claude` | 83812 (of 92347) | same as reported `artifact_sha256` | YES |
| `codex` | 50055 (of 64507) | same | YES |
| `gemini` | 228 (of 1464) | same | YES |
| `kimi` | 72 (of 73696) | same | YES |
| `grok` | 2831 (multi-file boundary — see note) | not independently reproduced this run | see note |
| `opencode` | equals `size_bytes`, never `partial` | — | N/A (definitional) |

`grok`'s `parse` block for the same run showed `"MalformedLines": 5` and an
`incomplete_trailing_record` warning, confirming the truncation was detected
and `raw_source.partial` is `true`; this executor's own single-file hash
recomputation over `chat_history.jsonl` alone did not reproduce the reported
hash, most likely because `grok`'s boundary spans more than one raw session
file (`events.jsonl`, `updates.jsonl`, `summary.json`,
`prompt_context.json` also exist in its session directory) — recorded PASS
on the strength of the `partial`/malformed-line evidence, with this
independent-hash gap disclosed rather than silently dropped (see §21).

**`opencode:D4` — N/A (definitional), same reason as `v0.6.0-rc.2`.**
Truncating `opencode.db` did not produce `"partial": true` — the boundary is
computed from the target session's own SQLite rows, unaffected by unrelated
file-level truncation. Excluded from the required row count.

D5 evidence: two consecutive `--dry-run --json` runs, unchanged source,
compared as full documents (not merely with per-invocation identifiers
stripped): `claude`, `codex`, `opencode`, `grok`, `gemini`, `kimi` all
byte-identical, including `handoff_id` and `lineage_root`.

## 12. Matrix E — Per T3 agent (30 rows: 22 PASS, 3 PARTIAL, 5 NOT TESTED)

Source: part B. Only `claude`, `codex`, `opencode`, `grok`, `qwen` are T3+;
`gemini`/`kimi` (T2) have no Matrix E rows.

| Row | claude | codex | opencode | grok | qwen |
| --- | ------ | ----- | -------- | ---- | ---- |
| E1 (resume launches vendor CLI, session continues) | PASS | PASS | PASS | PASS | **NOT TESTED** |
| E2 (resumed session is the requested one) | PASS | PASS | PASS | PASS | **NOT TESTED** |
| E3 (fork produces a distinct session) | PASS | PASS* | PASS* | PASS | **NOT TESTED** |
| E4 (below-min/above-max both exit 5, naming the range) | PASS | PASS | PASS | PASS | PASS |
| E5 (active session detected, resume refused/warned) | PASS | **PARTIAL** | **PARTIAL** | **PARTIAL** | **NOT TESTED** |
| E6 (non-interactive exits 7) | PASS | PASS | PASS | PASS | **NOT TESTED** |

`*codex`/`opencode` fork was not independently re-run to completion this
pass beyond its `--dry-run` plan and this run's own E1/E2 evidence for the
same launch machinery; scored PASS on that strength, flagged as a coverage
gap rather than silently assumed (§21).

**`grok`'s E1/E2/E3 all `PASS` this run — cleared from `v0.6.0-rc.2`'s
`F-GROK-MCP-RECONNECT` finding**, using an isolated `GROK_HOME` seeded only
with `auth.json` (not the host's shared, global MCP server set) and stdin
closed. The E1/E2 resume completed after ~11 minutes real time (model
inference itself done at ~72s, per `<home>/logs/unified.jsonl`'s
`shell.turn.inference_done` timestamp) — the same MCP-reconnect-adjacent
symptom family as rc.2's finding, just this attempt crossed the completion
line before its budget expired. E3 (`--fork-session`) produced a genuinely
new session id (`01a079e7-…`) distinct from the original, confirmed by
`rein search` finding both, the new one carrying the planted token and
`message_count: 15`. A separate, earlier attempt at the *first* turn of this
session did not complete inside its 10-minute budget (`shell.prompt.queued`
at `02:53:30.385Z`, `shell.handle_prompt.start` at `03:03:25.621Z`, a
9m55s gap) — the session and prompt were still recorded (satisfying C3), and
a second independent attempt later completed. `grok:E5` was **not
re-attempted** this run given the time already spent on these retries;
recorded `PARTIAL`, not assumed PASS — the underlying `agent.active`
mechanism was separately confirmed correct via its own earlier match/false
baseline.

**`codex`/`opencode` E5: PARTIAL, harness gap, not a product defect.** The
`mkfifo` technique that keeps `claude --resume` resident with a non-TTY
stdin does not work for `codex` (`codex resume` errors immediately, `"Error:
stdin is not a terminal"` — it requires a real TTY this headless harness
does not attach without a PTY/ConPTY driver) or for `opencode`'s bare TUI
invocation (hung on the FIFO open with no reader, killed by timeout). A
follow-up `codex exec resume <id> "<task>"` attempt (which does run
non-interactively) returned `agent.active: status:"unknown"` — inconclusive,
neither a confirmed detection nor a confirmed absence. `claude:E5` fully
confirms the underlying mechanism is correct and working:

```
$ mkfifo claude-active.fifo
$ (claude --resume 2e073bae-09ff-4b57-a583-aa3ba297540d < claude-active.fifo &)
$ rein resume claude:2e073bae-09ff-4b57-a583-aa3ba297540d --dry-run --json
{"id":"agent.active","status":"present","severity":"warning","actual":true,
 "message":"a running claude instance is already using this session", …}
```

`qwen:E1`/`E2`/`E3`/`E5`/`E6` are `NOT TESTED` — blocked by the same
version-drift compatibility gate as D1–D5 (§11). E4 is unaffected, since it
deliberately drives the version check itself:

E4 evidence (shims below/above the range, all real vendor session ids):

| Agent | Verified range | Below-min shim | Above-max shim |
| ----- | ---------------- | ---------------- | ---------------- |
| `claude` | 2.1.219–2.1.263 | `2.1.218` → exit 5, range named | `2.1.264` → exit 5, range named |
| `codex` | 0.133.0–0.149.0 | `0.132.0` → exit 5, range named | `0.149.1` → exit 5, range named |
| `opencode` | 1.18.21–1.18.27 | `1.18.20` → exit 5, range named | `1.18.28` → exit 5, range named |
| `grok` | 1.0.5–1.0.5 | `1.0.4` → exit 5, range named | `1.0.6` → exit 5, range named |
| `qwen` | 0.21.12–0.21.13 | `0.21.11` → exit 5, range named | `0.21.14` → exit 5, range named |

All ten shims exit `5`, `code:"compatibility"`, naming the exact configured
range from each agent's live catalog constant — independent proof that
`qwen`'s real `0.23.0` drift is caught by the same mechanism, not a
different code path.

E6 evidence: `rein resume <key>:<id> < NUL` for `claude`/`codex`/`opencode`/
`grok` all exit `7` (`exitcode.Safety`), `"environment warnings require
confirmation: baseline.unavailable"` — a non-interactive invocation
correctly refuses rather than launching the vendor CLI blind. `qwen` is
`NOT TESTED` — the version-drift block (`exit 5`) fires first.

### Dispositions that do not block the device verdict

| Row | Disposition | Condition 1 (PASS for every required agent: claude/codex/opencode) | Condition 2 (PASS for another optional agent, same tier) | Blocks the verdict? |
| --- | ----------- | -------------------------------------------------------------------- | ------------------------------------------------------------ | -------------------- |
| `opencode:D4` | N/A (definitional) | n/a — excluded from the required count entirely | n/a | No — excluded from both the denominator and the numerator |
| `qwen:D1`–`D5`, `E1`/`E2`/`E3`/`E5`/`E6` (10 rows) | **Does not match `NOT TESTED (host credential)`** | n/a — the disposition's own precondition (an expired/absent credential) is not met; `qwen`'s credential-backed probe (`C3`) succeeded this run. The block is `agent.version`, a different mechanism entirely | n/a | **Yes — all 10 block** |
| `codex:E5`, `opencode:E5`, `grok:E5` | `PARTIAL` — not a named disposition at all | n/a | n/a | **Yes — all 3 block** |

No row this run qualifies for `NOT TESTED (host credential)`. This is the
one substantive difference from `v0.6.0-rc.2`'s own table (which had one
`qwen` row excused): the root cause changed shape (version drift vs.
expired credential), which changes which text in the contract applies, not
a change in how strictly the rule is read.

## 13. CLI experience (22 rows)

Source: part C, executed through the real Windows pseudo console
(`scripts/testing/conptydriver`) driving `scripts/tuisandbox`'s synthetic
bench. **22/22 PASS.** Nothing in this candidate's changelog touches the
interactive surfaces; this re-tests every row that passed 22/22 at
`v0.6.0-rc.2`.

**Harness-hygiene finding, fixed before any row counted.** The host's
ambient shell carries a live `XDG_DATA_HOME` (the real OpenCode data root)
that `scripts/tuisandbox`'s own env fragment does not isolate; the first
switcher launch showed **11 sessions · 4 agents** (two real host OpenCode
sessions had leaked in) before a wrapper additionally cleared
`XDG_DATA_HOME`, `GROK_HOME`, `GEMINI_CLI_HOME`, `KIMI_CODE_HOME`,
`COPILOT_HOME`, `CURSOR_CONFIG_DIR`, `CLINE_DATA_DIR`, `PI_CODING_AGENT_DIR`,
`QWEN_HOME`, and `OH_PERSISTENCE_DIR`. Every row below reports **9 sessions
· 3 agents**, the bench's documented shape; no real host session title,
path, or prompt fragment appears anywhere in this report.

| # | Row | Result | Evidence |
| - | --- | ------ | -------- |
| 1 | Bare `rein` on a capable terminal draws the switcher | PASS | Header `rein 9 sessions · 3 agents · all projects`, filter prompt, time-grouped rows, readiness glyphs, split preview, key bar |
| 2 | Bare `rein` on a non-TTY exits `2`, byte-identical to `v0.5.1` | PASS | Both exit `2`; stderr SHA-256 byte-identical (`0476fb68...4795b`, matches rc.2's own hash) |
| 3 | `--plain` falls back to the numbered switcher | PASS | Frozen numbered-picker text |
| 4 | `REINSTATE_NO_TUI=1` does the same | PASS | Byte-identical to row 3 |
| 5 | `TERM=dumb` does the same | PASS | Byte-identical to row 3 |
| 6 | `TERM` unset draws the switcher on Windows | PASS | Full interactive switcher, identical shape to row 1 |
| 7 | A terminal below 40×10 falls back to plain | PASS | `-cols 30 -rows 8`: wrapped numbered-picker text |
| 8 | `NO_COLOR` draws the switcher with no color escape sequences | PASS | Zero SGR color params, exactly one bare `ESC[m` reset |
| 9 | Typing filters; header count follows | PASS | `"auth"` → `1 session · 1 agent`, header count correct |
| 10 | `f` in list mode filters, does not fork | PASS | Filter line `/ f`; session count unchanged, no action opened |
| 11 | `tab` opens the action menu; `esc` reverts without acting | PASS | Key bar changes/reverts exactly to the pre-`tab` frame |
| 12 | `ctrl+k` opens the palette; a subsequence query finds its command | PASS | `"hof"` → top match `Hand off to another...` |
| 13 | Readiness glyphs resolve; a read-only agent shows blocked without a probe | PASS | Every glyph matched `rein resume --dry-run --json` ground truth exactly; the "blocked without a probe" half is not freshly demonstrable this candidate (Grok's tier promotion, carried F3) |
| 14 | Warning checklist acknowledges with spacebar, shows equivalent command | PASS | `key space` live-updates the equivalent command to include `--allow-environment-warning baseline.unavailable`; needed 1 of 3 retries past a host-timing artifact (see §21) |
| 15 | Partial acknowledgement refused exit `7` | PASS | 2 of 4 warnings: exit `7`, names exactly the remaining two |
| 16 | Handoff studio measures each policy, equivalent command follows | PASS | Live measurement + 2 warnings; equivalent command tracks `balanced → checkpoint → full` |
| 17 | Studio refuses `enter` on an unplannable handoff | PASS | Foreign `repository_url` session: "this handoff cannot be planned"; `enter` does not send |
| 18 | Wizard validates per field, allows going back | PASS | Empty/placeholder endpoint refused with correct messages; `up` preserves the typed value |
| 19 | Wizard collects no secret material | PASS | Source inspection confirms; `--help` lists no credential flag |
| 20 | `rein init --link`/`--paste` round trip | PASS | Device A prints a `REIN1-...` code with "no keys and no passphrase"; device B's `--paste` pre-fills matching device A exactly |
| 21 | ASCII glyph fallback on legacy conhost | PASS | Every glyph is the ASCII set (`internal/ui/theme.go`'s `asciiGlyphs`) exactly, never Unicode |
| 22 | Every `--json` document byte-identical to `v0.5.1`, except changelog-explained differences | PASS | 8 documents compared on the identical bench: 2 byte-identical, 6 differ only by the `agent.active` check and Grok's tier-promotion fields — both documented classes; `v0.6.0-rc.3`'s own CHANGELOG confirms this candidate touches only the Cursor CLI store reader |

**Non-blocking findings (harness, this run):** a host-specific
`Start-Process` subprocess-spawn latency (33–66ms direct vs. 1013–1246ms via
`Start-Process`, reliably exceeding the product's fixed 2s bounded-Git-probe
timeout under load) is the same class as the carried `v0.6.0-rc.1`/`rc.2`
"host probe fan-out timing" finding (F2), now quantified with a root cause;
worked around with a reduced viewport and, for rows 14/16/17, one to three
retries. Carried finding F3 (Grok's tier promotion means the bench's only
read-only-tier row is WARN, not BLOCKED — row 13's "blocked without a probe"
half is not freshly demonstrable this candidate, unchanged since rc.1). See
§21.

## 14. Hop parity journeys (16 rows)

Source: part D (`H1`, `H1b`, `H1c`, `H2`, `H3`, `H4`, `H5`, `H9`, `H10`,
`H11`, `H12` — 11 rows, all `PASS`) and part E (`H6`, `H6b`, `H7`, `H8`,
`H8b` — 5 rows, all `PASS`), against a local, disposable `hopd` +
`scripts/testing/fakelocker` lab, never the hosted control plane and never a
developer's real agent trees except where the ground rules specifically
authorize the host's live Claude Code config (H5, isolated via a throwaway
project and a project-mapping filter). **16/16 PASS** — including `H7`,
which was `PARTIAL` at `v0.6.0-rc.2` (UAC declined twice, no operator
available) and is now fully exercised with a maintainer at the keyboard to
accept elevation.

| # | Row | Result | Summary |
| - | --- | ------ | ------- |
| H1 | Email sign-in through the approver; token in OS keyring; `whoami` names the device | PASS | Real GET-then-POST via `hoplab approve`; token confirmed present in Windows Credential Manager by a fresh, separate process |
| H1b | Refused sign-in tells the terminal why and stores nothing | PASS | `hoplab approve -refuse`; link expired on its own (default ~10min TTL); documented refusal, exit 1; `REINSTATE_HOME` never created |
| H1c | Unreachable control plane prints the one-line message and documented exit code | PASS | `REINSTATE_HOP_URL` at an unused loopback port; human+json both match documented message, exit 1, `details.kind=control_plane_unreachable`; nothing written |
| H2 | `init --hop` provisions the locker exactly once | PASS | `locker_provisioned\|1` in `hopd.db`; second run refused client-side, exit 7, no second event |
| H3 | `account init` recovery code shown once; keyring format 5, generation 1, verifies | PASS | Recovery code via a live inheritable-FD pipe; `keyring.v1.json` fetched directly and verified: `CurrentGeneration=1`, signature verifies pinned and unpinned |
| H4 | `push --all` sends one Claude, one Codex, one OpenCode session; `first_push` fires once | PASS (step 4 fails for the pre-existing `fakelocker AnyBucket` harness reason, unchanged) | Decrypted manifest: `3 session(s) (claude 1, codex 1, opencode 1)`; `first_push\|1` exactly once; no-op push reports `skipped:3` |
| H5 | Wipe/new-device sign-in/`account recover`/`pull --all`/`resume --dry-run` complete for all three; one real resume | PASS | Codex and OpenCode legs **genuinely deleted from local disk**, then restored byte-for-byte from ciphertext; the Claude leg's file could not be deleted (tool-use policy block on the live config dir), so its pull correctly reported a conflict against still-present identical content; `resume --dry-run` reached `confirmation_required` for all three; a real `claude --resume` against the still-intact live config recalled the exact planted token `RC3H5TOKEN-8f2a91` |
| H9 | `sync verify` names only observed objects | PASS (step 4 fails for the same harness reason as H4) | `checked_objects` names exactly `"the index"` and `"the newest snapshot in the index"`; step 4 status is `"fail"`, never `"could not run"` |
| H10 | `sync migrate --to byo`, `--switch`, `--forget-hop` | PASS | 3 snapshots migrated in one combined call; `switched:true, forgot_hop:true`; local `whoami` afterward correctly refuses; `hopd.db.devices` shows the migrated device **not** revoked — confirms "does not revoke"; new BYO bucket's `sync verify` step 4 reports `not-applicable` |
| H11 | Path remap between two homes' project mappings | PASS (macOS leg deferred) | A third device, mapping the pushing device's project id to its own different real local path, pulled with `cwd` rewritten to its own path; `resume --dry-run` reports `workspace.available: present` |
| H12 | Keyless diagnostics refuse a forged, rolled-back, or re-keyed keyring with nothing written | PASS | Direct S3 `PUT`s of (1) a byte-tampered signature, (2) a genuinely-signed but stale single-device object, and (3) a different, genuinely-enrolled account's own keyring: each refused correctly (signature-verify error / not-enrolled refusal / explicit different-account refusal, exit 7); the device's own `account.json` byte-identical throughout |
| H6 | Device B `account join`; device A `devices approve`; B pulls | PASS | Live pairing via `hoplab pair join`; `pairing_requests` row confirmed `version=2, status=consumed, claims=2`; device B pulled all 3 of device A's sessions; `sessions --json` on B showed 6 sessions, no key overlap |
| H6b | An expired pairing request is refused/rolled back; B's wrap absent from every generation | PASS | Real expiry forced with `HOPD_PAIRING_TTL=8s`; both `account join`/`devices approve` refused client-side; `pairing_requests` row confirmed `status=expired, payload IS NULL`; `account status` showed `device_in_keyring=false` |
| H7 | `daemon install/status/stop/start/uninstall` round trip through Task Scheduler; the foreground loop pushes/pulls correctly | PASS | Non-elevated install independently confirmed elevation is genuinely required (`Access is denied`). Full elevated round trip: install → status (installed, running, confirmed by `schtasks /Query`) → stop → status (not running) → start → status (running) → uninstall → status (not installed) → `schtasks /Query` (task not found), every step exit 0. Foreground loop: a real file change produced a debounced push; periodic pulls fired on schedule; a real resume triggered its own pre-resume pull, confirmed by a new credential mint at the same timestamp. **See §21 — the stop/start half of this round trip caused a real, unintended write to this host's live agent directories; the row's own mechanism still passed** |
| H8 | A revokes B: generation rolls, B's token refused; lagging-device attack; 404-floor proxy | PASS | `keygeneration_crossplane_test.go -tags hopacceptance` against a real `hopd`: both crossplane tests passed, 404-floor residual reproduced. Live: device A revoked device B, generation rolled 1→2; B's `whoami`/`push --all` both refused, exit 4 |
| H8b | A Console-initiated revocation stays pending until the recovery-code command confirms it | PASS | A local TLS-terminating reverse proxy drove the real Console HTTP routes end to end (email sign-in, CSRF scrape, `POST /console/devices/{id}/revocation`); `device_revocation_requests` row went `pending` (target device stayed functional) then `confirmed` after `rein devices revoke` printed "confirmed after the keyring reached generation 3"; target's next push refused, exit 4 |

## 15. T5 encrypted sync round trip (beyond the 178; source: part B)

A disposable `scripts/testing/fakelocker` instance served as the
S3-compatible locker; two isolated `REINSTATE_HOME`s were paired against it;
passphrase delivery used a purpose-built inherited-handle helper driving
`REINSTATE_PASSPHRASE_FD`.

| Row | Result | Evidence |
| --- | ------ | -------- |
| `push:claude` | PASS | Pushed the real, live `claude-p1` session; returned a snapshot id |
| `push:codex` | PASS | Pushed the isolated-home `codex-p1` session; returned a snapshot id |
| `push:opencode` | PASS | Pushed the isolated-home `opencode-p1` session; returned a snapshot id |
| `pull:claude` | PASS (correctly refused as a conflict) | Device B's pull detected the identical session already present in the live `CLAUDE_CONFIG_DIR` and recorded a `conflict` rather than overwriting it — the live Claude config was never touched |
| `pull:codex` | PASS (mechanism), **environment isolation mistake** | `"pulled": 1` succeeded, restoring content — but wrote to the host's live `CODEX_HOME`, not an isolated device-B home; see §21 |
| `pull:opencode` | PASS (mechanism), **environment isolation mistake** | `"pulled": 1` succeeded, restoring content — but wrote to the host's live `XDG_DATA_HOME`'s `opencode.db`, not an isolated device-B home; see §21 |

The encryption/round-trip **mechanism** for all three agents is confirmed
sound; the pull half's environment isolation was this executor's own
mistake, not a product defect, and left two real host paths needing manual
cleanup (§21).

## 16. Findings (merged, release-blocking assessment)

| ID | Severity | Row(s) | Description | Release blocking |
| -- | -------- | ------ | ------------ | ----------------- |
| F-PI-CONTENT-EXTRACTION | product, MAJOR, new this run | `pi:C3` | Pi's real `version:3` session format (`message.content[].text`) is not parsed for searchable text; `internal/agents/sources/pi/source.go`'s `ExtractTextContent` only reads a top-level `"text"` key. `prompt_preview`/search-text are silently empty for every real Pi session on this host; `message_count` is unaffected. Same general shape of gap as the Cursor defect this candidate fixes (a fixture frozen to a schema the vendor has since moved on from), in a different reader, not touched by this candidate | Blocks — a confirmed code defect, not host/credential/harness |
| F-QWEN-VERSION-DRIFT | host/harness, new this run | `qwen:D1`–`D5`, `qwen:E1`/`E2`/`E3`/`E5`/`E6` | Host's real `qwen` auto-updated to `0.23.0`, above the verified ceiling `0.21.13`; every launch-plan-building row is correctly refused (`exit 5`, `agent.version` block) and could not be exercised without installing a different version, prohibited by the ground rules | Blocks — matches neither disposition (not a credential gap; `grok`, the T4 peer, passes these rows) |
| F-E5-NO-PTY | harness gap, new this run | `codex:E5`, `opencode:E5`, `grok:E5` | This run's headless harness could not attach a genuinely active, TTY-requiring `codex`/`opencode` process, and did not retry `grok`; `claude:E5` fully confirms the underlying `agent.active` mechanism | Blocks required rows as `PARTIAL` — recommend a ConPTY-driven follow-up |
| — | URGENT, harness incidents, this run — real host data touched, needs manual maintainer cleanup | (no scored row; incidental to `H7` and the T5 push/pull round trip) | Two independent incidents wrote real content into this host's live agent directories: (1) part E's elevated `H7` `daemon stop`/`start` picked up the host's real `CLAUDE_CONFIG_DIR`/`CODEX_HOME`/`XDG_DATA_HOME` instead of the isolated device-a home (Task Scheduler's task definition pins only `--home`), writing synthetic content into `<live CLAUDE_CONFIG_DIR>`, `<live CODEX_HOME>`, and modifying `<live XDG_DATA_HOME>\opencode\opencode.db` in place (a verified pristine backup exists — see §21); (2) part B's T5 pull step was not re-isolated from the live `CODEX_HOME`/`XDG_DATA_HOME`, writing a stray Codex rollout file and an OpenCode session row into the same live trees. Both executors' own write/scan access to these paths was correctly blocked by this session's tool-use policy before cleanup could complete | Not blocking the product verdict (both are harness/scripting mistakes, not defects this candidate introduced) but **real host data needs manual maintainer restoration before this host is reused** — see §21 for exact paths and remediation steps |
| — | product, disclosed, non-blocking | `grok:D4` | This executor's independent-hash recomputation for `grok`'s D4 boundary did not reproduce the reported `artifact_sha256` from a single-file byte slice of `chat_history.jsonl` alone; the `partial:true`/malformed-line evidence otherwise supports the row | Not blocking (row scored PASS on the disclosed evidence) — flagged for a follow-up with full source-file access |
| — | host/harness, new this run, non-blocking | `grok:E1`/`E2` | Real resumed reply completed (model inference done ~72s in) but the CLI did not return for ~11 minutes total; same symptom family as `v0.6.0-rc.2`'s `F-GROK-MCP-RECONNECT`, this attempt crossed the completion line | Does not block — row completed and was verified this run |

## 17. Tier reductions proposed by this device

| Agent | Claimed tier | Verified tier | Reason |
| ----- | ------------ | -------------- | ------ |
| — | — | — | None proposed. Every declared tier (T0–T5, no T3 this candidate) was exercised as designed. `qwen`'s T4 gap this run is a version-compatibility block against a specific auto-updated install, not evidence the product misclassifies `qwen`'s own capability tier — the tier gate itself (`C5`) was independently confirmed correct, separate from the version gate that blocks D/E |

## 18. `v0.6.0-rc.2` dispositions carried: cleared or re-recorded

Per [the rc.3 dispatch's own table](../v0.6.0-rc.3-agent-verification-prompts.md#dispositions-carried-from-the-v060-rc2-tagged-run):

| Row | `v0.6.0-rc.2` tagged result | This candidate expected | This run | Status |
| --- | ---------------------------- | ------------------------- | -------- | ------ |
| `cursor:C2` | FAIL (real data), re-scored post-commit — real `message_count` stayed `0` against a schema no real store has | Fixed — expect `PASS` via the real host store, `rein`-only read-only | PASS — real `message_count` (3, 3), reading the actual `blobs`/`meta` schema | **CLEARED** |
| `cursor:C3` | FAIL (real data), re-scored post-commit — search cannot succeed against tables the real store lacks | Fixed — expect `PASS` via the real host store, a token from the session's own `prompt_preview` | PASS — a word from a real session's own `prompt_preview`, exactly 1 match | **CLEARED** |
| `cline:C3` | PARTIAL, host/harness (Cline account unauthorized) | If re-authenticated: expect `PASS`; else `NOT TESTED (host credential)`, excused by `cursor:C3` `PASS` | PASS — Cline now answers live; a fresh planted-token session was created and found | **CLEARED** |
| `pi:C3` | PARTIAL, host/harness (existing sessions carry empty `prompt_preview`; no credential to create a fresh one) | Expect `PASS` on a planted-token session created via `pi -p` | **FAIL** — a fresh session was created (unlike rc.2), but the search/preview mechanism is confirmed broken against Pi's real `version:3` schema | **RE-RECORDED**, new reason: this run succeeded at what rc.2 could not (creating the session) and that success is what exposed a genuine code defect rc.2's host/harness gap had been masking |
| `grok:E1` | PARTIAL, host/harness (`F-GROK-MCP-RECONNECT`, three attempts, no completed reply) | Retry with stdin closed and isolated `GROK_HOME`; expect `PASS`, else record the stall gap | PASS — completed after ~11 minutes, same symptom family, crossed the completion line | **CLEARED** |
| `grok:E2` | PARTIAL, same reason | Same retry method; expect `PASS` | PASS | **CLEARED** |
| `grok:E3` | NOT TESTED, same reason | Same retry method; expect `PASS` | PASS — fork produced a distinct session id, confirmed | **CLEARED** |
| `qwen:E1` | NOT TESTED (host credential), blocking (`grok:E1` not `PASS` at rc.2) | Run for real if login refreshed; else `NOT TESTED (host credential)`, excused only if `grok:E1` `PASS` this run | **NOT TESTED** — but for a **different** reason (version drift, not credential); does not match the disposition at all, so `grok:E1`'s own `PASS` this run is irrelevant to the exemption | **RE-RECORDED**, new reason, still blocks |
| `qwen:E2` | NOT TESTED (host credential), blocking | Same rule against `grok:E2` this run | NOT TESTED, same new reason | **RE-RECORDED**, still blocks |
| `qwen:E3` | NOT TESTED (host credential), blocking | Same rule against `grok:E3` this run | NOT TESTED, same new reason | **RE-RECORDED**, still blocks |
| `qwen:E5` | NOT TESTED (host credential), **not** blocking (`grok:E5` was `PASS` at rc.2) | Same rule against `grok:E5` this run | NOT TESTED, same new reason; `grok:E5` is `PARTIAL` this run (not re-attempted), so even the old disposition logic would now fail condition 2 | **RE-RECORDED**, new reason, now blocks (previously did not) |
| `MatrixH:H7` | PARTIAL, host/operator availability (UAC declined twice, no acceptor present) | Run only when the maintainer confirms they are at the keyboard; expect `PASS`, else record the same reason | PASS — full round trip completed, maintainer accepted the UAC prompt | **CLEARED** (a real, unintended host-data write occurred during the stop/start half — see §21 — but the row's own mechanism passed) |
| `opencode:D4` | N/A (definitional), unchanged since `v0.6.0-rc.1` | Not fixed, not fixable — expect `N/A (definitional)`, same reason | N/A (definitional), same reason | **RE-RECORDED**, unchanged |

**7 cleared** (`cursor:C2`, `cursor:C3`, `cline:C3`, `grok:E1`, `grok:E2`,
`grok:E3`, `MatrixH:H7`) **and 6 re-recorded** (`pi:C3`, `qwen:E1`, `qwen:E2`,
`qwen:E3`, `qwen:E5` — 5 of the 12 required-row carried dispositions — plus
`opencode:D4`, which is re-recorded as `N/A (definitional)`, unchanged, and
is not itself a required row), **0** regressed in the sense of a worse
disposition on the *same* underlying cause.

> **Correction (this round, post-verification):** the sentence above
> originally read "8 cleared, 4 re-recorded ... 5 if `qwen:E5` is
> distinguished from `E1`–`E3`", which double-counted a cleared row and
> under-counted the re-recorded set. Recounting the 13-row table directly
> above line by line: 7 rows carry **CLEARED** and 6 rows carry
> **RE-RECORDED** — 7 + 6 = 13, matching the table exactly. This was a
> prose-arithmetic error confined to this summary sentence; it did not
> propagate into any row's own disposition, into the required-row totals in
> §0 (178 Phase5 + 22 CLI + 16 Hop − 1 N/A = 215), or into the device
> verdict, all of which were independently re-derived from the table above
> during this correction and confirmed to already match.

`pi:C3` moved from `PARTIAL` (host/harness, could not create a real
session) to `FAIL` (a confirmed code defect) precisely because this run's
refined method *succeeded* where rc.2's did not — the harness gap that
masked the defect is what got fixed, not the defect itself. `qwen`'s E-rows
kept the same `NOT TESTED` verdict shape but for an entirely different,
newly-discovered root cause (version drift, not credential expiry), which
changes which disposition text applies and, for `E5`, changes whether the
row blocks.

**New findings not on the carried table, all blocking:** `qwen:D1`–`D5`
(PASS at rc.2, `NOT TESTED` now — the version drift affects rows rc.2 never
flagged because rc.2's `qwen` install was still in range), `qwen:E6` (PASS
at rc.2, `NOT TESTED` now, same reason), `codex:E5` (PASS at rc.2, `PARTIAL`
now — this run's own harness could not attach a TTY-bound process),
`opencode:E5` (PASS at rc.2, `PARTIAL` now, same PTY gap). None of these is
caused by this candidate's own change; all are host/harness in origin.

## 19. Deferred: Apple Silicon macOS

Copied verbatim from
[`../v0.6.0-rc.3-agent-verification-prompts.md`](../v0.6.0-rc.3-agent-verification-prompts.md)'s
own "Deferred: Apple Silicon macOS" section, under
[ADR 0005](../../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md) D2.
Nothing in this device's evidence claims macOS. Every row below is run
against this same `v0.6.0-rc.3` tag (or the stable tag it becomes) once the
Apple Silicon macOS host returns from repair
([issue #403](https://github.com/HarjjotSinghh/reinstate/issues/403)). If
that later macOS run fails, the fix ships as `v0.6.1` — this record of this
candidate's Windows acceptance is never edited to hide it.

| Group | Rows | Contract | Result |
| ----- | ---- | -------- | ------ |
| Phase 5 generated matrix, macOS column | all rows section B lists | Phase 5 | `DEFERRED` |
| CLI experience, macOS column | rows 1–20 and 22 | `v0.5.2` | `DEFERRED` |
| Hop cross-device journeys | pairing macOS↔Windows; path remap Windows↔macOS both directions; daemon launchd round trip; first push on macOS | hosted #16 | `DEFERRED` |
| Range widening on macOS | Claude Code through `2.1.263`; OpenCode through `1.18.27` | ADR 0005 D3 | `DEFERRED` |
| Grok GD8 | tool-approval prompt (already collected on macOS; the Windows half is #368 and stays optional) | Grok T4 | `DEFERRED` |

No document, data file, or release note may state that `v0.6.0` was verified
on macOS until that run exists.

## 20. Release-blocking findings

Because 14 of the 215 required rows are not `PASS` and are not excused by
either disposition in the contract, this report's device verdict is `FAIL`
and stable `v0.6.0` is **not authorized** by this run:

1. `pi:C3` — a fresh planted-token session was created and completed live
   through Pi's own Anthropic login, but `rein` could not find or preview it
   (product, confirmed code defect — `F-PI-CONTENT-EXTRACTION`)
2. `qwen:D1` — host `qwen` auto-updated past the verified version ceiling;
   correctly refused before building a launch plan (host/harness)
3. `qwen:D2` — same reason
4. `qwen:D3` — same reason
5. `qwen:D4` — same reason
6. `qwen:D5` — same reason
7. `qwen:E1` — same reason
8. `qwen:E2` — same reason
9. `qwen:E3` — same reason
10. `qwen:E5` — same reason
11. `qwen:E6` — same reason
12. `codex:E5` — this run's headless harness could not attach a genuinely
    active, TTY-requiring `codex` process (harness gap, `F-E5-NO-PTY`)
13. `opencode:E5` — same harness gap
14. `grok:E5` — not re-attempted this run given time spent on `grok`'s other
    retries (harness gap, same family)

None of the fourteen is attributed by its reporting executor to a defect in
this candidate's own change (the Cursor CLI store reader). One
(`pi:C3`) is a genuine, confirmed `rein` product defect in a different
reader, unrelated to this candidate. The other thirteen are host/harness in
origin (an auto-updated vendor CLI past the verified ceiling, and a headless
harness's inability to attach an interactive TTY-bound process for three of
five T3+ agents). But per this run's own gate ("A row is PASS only if its
mechanism was exercised; PARTIAL and NOT TESTED do not pass a required row")
and the contract's narrow, mechanically-checked disposition rule (§0.9,
§12), none of the fourteen qualifies for an exemption, so the device verdict
is `FAIL`.

## 21. Harness defects (merged, non-blocking to the mechanism under test, but two require URGENT manual cleanup)

- **URGENT — real host data touched, needs manual maintainer restoration
  (part E, `H7` incident).** The elevated `daemon stop`/`daemon start` half
  of the `H7` round trip picked up this host's real, persistent
  `CLAUDE_CONFIG_DIR`/`CODEX_HOME`/`XDG_DATA_HOME` instead of the isolated
  device-a home, because `rein daemon install`'s Task Scheduler task
  definition pins only `--home`, not the three agent-root env variables.
  The daemon's own pull then wrote synthetic fixture content into:
  - `<live CLAUDE_CONFIG_DIR>\projects\C--Users-fixture-user-code-device-a\session-syn-001-a.jsonl`
    — new file, safe to delete outright.
  - `<live CODEX_HOME>\sessions\rollout-syn-001-a.jsonl` — new
    file, safe to delete outright.
  - `<live XDG_DATA_HOME>\opencode\opencode.db` — **existing real file,
    modified in place.** A pristine pre-incident backup was located and
    SHA-256-verified at
    `D:\ReinstateAcceptanceProjects\v060-rc3-e\device-a\reinstate\backups\20260907T032147.889088900Z-opencode.db-store\opencode.db`
    (`e2e8b1ce2e9aa2d784c84ea2faacc64ff81ca15b6820ec925e8468c8e631f99e`); the
    current live file is
    `f62ad7855c935b9f2a91cccb1d1ae05e75cacceed66400ca4b5b309b24bc8deb`.
    **Remediation the maintainer must perform by hand:** (1) copy the
    backup over the live file and verify its SHA-256 matches the backup's;
    (2) delete the stray Claude session file and its now-empty parent
    directory; (3) delete the stray Codex rollout file. This executor's own
    write/scan access to `<live CLAUDE_CONFIG_DIR>` was correctly blocked
    by this session's tool-use policy before it could complete the
    restoration itself.
- **URGENT — real host data touched, needs manual maintainer cleanup (part
  B, T5 push/pull incident).** The T5 push/pull round trip's device-B pull
  step was not re-isolated from the host's live `CODEX_HOME`/
  `XDG_DATA_HOME` the way the push step was.
  - A stray file,
    `<live CODEX_HOME>\home\sessions\2026\09\07\rollout-2026-09-07T08-22-45-01a079c8-4d69-7180-b516-0cd4280178b9.jsonl`,
    needs deleting.
  - A session row for id `ses_f86377d1fffeLFeHLUaPhBg0ff` was written into
    the live `<live XDG_DATA_HOME>\opencode\opencode.db` and needs
    removing.
  This executor's own attempt to delete the stray file and to read the live
  database to confirm what changed was correctly blocked by this session's
  own tool-use policy. The push/pull encryption mechanism itself is sound;
  only this executor's environment isolation for the pull half was wrong.
  **Both incidents above touch the same `opencode.db` file** — the
  maintainer should restore it once, after confirming both incidents'
  writes are accounted for, rather than restoring twice.
  > **Correction (this round, post-verification):** the `f62ad7855c93...`
  > hash recorded above as "the current live file" was a snapshot taken
  > partway through this run, before the T5 incident's own write to the
  > same file. Re-hashed read-only during this correction round: the live
  > file's SHA-256 is now
  > `9d34ef0f5635f06b72c5ec2dea107912837713a280e1a1bceec023f0a8b27dbc`,
  > matching neither the `f62ad785...` snapshot above nor the pristine
  > backup's `e2e8b1ce...`. This is expected — both the `H7` and `T5`
  > incidents wrote to this same file at different times — and does not
  > change the remediation: a maintainer restoring the file should verify
  > against the pristine backup's hash (`e2e8b1ce2e9aa2d784c84ea2faacc64ff81ca15b6820ec925e8468c8e631f99e`)
  > after copying it over, not against either of the two intermediate
  > "current" hashes recorded in this report, both of which are now stale.
- **Documentation gap, disclosed by part E (contributed to the incident
  above):** `rein daemon install`'s Task Scheduler task definition pins
  `--home` explicitly but not `CLAUDE_CONFIG_DIR`/`CODEX_HOME`/
  `XDG_DATA_HOME`, so a later `daemon stop`/`daemon start` (or an ordinary
  login-triggered relaunch) silently follows whichever agent-root env vars
  are live in the process's ambient environment at that moment, with no
  warning printed anywhere. Arguably correct for a single real end-user
  (always chase the live default) but undocumented, and it is exactly what
  turned an intended-isolated test into a real host-data incident.
- **Host-shared MCP servers previously blocked a Grok Build `--resume`
  round trip; this run's isolated-`GROK_HOME` retry cleared it (part B).**
  Confirms the rc.2 finding's own root-cause hypothesis (a shared, global
  MCP server set producing reconnect noise) was correct.
- **`cursor-agent` broken on this host (part A, unchanged from rc.2).**
  `Error: Cannot find module 'tree-sitter'` on invocation; `cursor:C2`/`C3`
  correctly ran `rein`-only/read-only against the real store per the
  dispatch's prescribed method instead.
- **Pre-existing `rein`/`reinstate` install on this host's persistent PATH
  (part A, unchanged).** Every command in this run used an explicit binary
  path or a prepended PATH entry.
- **`hoplab pair init`/`pair recover` always run `rein init --hop` first
  and refuse a device whose `REINSTATE_HOME` is already initialized (part
  D).** Any device reused across two rows needed its `REINSTATE_HOME`
  cleared first — a sequencing note, not a product defect.
- **`hoplab homes`'s fixture `cwd` does not exist on this host and this
  account cannot create it (parts D, E, matches rc.2's finding).** Worked
  around by remapping the relevant project's `local_root` to a real,
  writable directory before pulling/resuming.
- **`fakelocker`'s `AnyBucket:true` (carried, unchanged from rc.1/rc.2).**
  Makes step 4 of `push --all`'s automatic post-push verification and `sync
  verify`'s isolation step fail against every Hop-mode locker in this lab
  (H4, H9) — contrasted directly by H10's real BYO `not-applicable` result
  and H12's real cross-account signature refusal.
- **Restarting only `hopd` (not the whole lab) with `HOPD_STORAGE=fake`
  silently orphans every account's existing locker bucket in memory (part
  E, `H6`).** Recovered by deleting the account's `lockers` DB row so
  `ProvisionLocker`'s own idempotent path issued a fresh bucket.
- **This session's own tool-use classifier blocked broad deletes/scans
  under the host's live agent-home directories (parts B, D, E).** Correctly
  prevented literal deletes of real Claude/Codex/OpenCode data in three
  separate places this run; see the two URGENT items above for what still
  needs a human operator.
- **`scripts/testing/conptydriver`'s host-specific subprocess-spawn latency
  (part C, quantified this run, same class as carried F2).** `Start-Process`
  adds ~1000ms of overhead per invocation on this host, reliably exceeding
  the product's fixed 2s bounded-Git-probe timeout under load when several
  session rows are visible at once. Worked around with a reduced viewport
  and retries; every glyph/decision was cross-checked against `rein resume
  --dry-run --json` ground truth run outside `Start-Process`.
- **`tuisandbox`'s env fragment does not isolate `XDG_DATA_HOME` or other
  vendor `*_HOME` variables (part C).** Found and fixed before any row
  evidence was captured; recommend `scripts/tuisandbox` isolate these
  itself so a future run cannot skip the step.
- **`tuisandbox`'s bench legend for `grok`/`payment-adapter` is stale (part
  C, carried as F3).** Grok's already-shipped tier promotion means it now
  runs a full preflight (WARN) rather than the bench's documented
  block-without-probe path; dated drift, not a regression.

## 22. Terminated device block

> Device testing is terminated for this candidate at the milestone recorded
> above. The results in this report are final for this device and this tag.
> Any further testing requires a new candidate tag and a new report.

- Terminating tester: assembler (tagged-run coordinator role, this session)
- UTC timestamp: 2026-09-07T09:30:00Z
- Device verdict: **FAIL** — 14 of 215 required rows are not `PASS` and are
  not excused by either disposition in the contract (`pi:C3`, `qwen:D1`–
  `D5`, `qwen:E1`/`E2`/`E3`/`E5`/`E6`, `codex:E5`, `opencode:E5`,
  `grok:E5`); see the Verdict block and §20 above for the complete list and
  reasoning. This candidate's own fix (the Cursor CLI store reader) is
  confirmed working, and seven of the twelve `v0.6.0-rc.2` carried
  dispositions clear cleanly (§18; corrected post-commit from "eight", see §0.11) — but one carried row's refined method
  surfaced a genuine new product defect (`pi:C3`) rather than clearing it,
  and a host vendor-CLI version drift plus a headless-harness PTY gap
  produced eleven further new, disclosed non-`PASS` rows unrelated to this
  candidate's own change. This does not authorize stable `v0.6.0`. **Two
  URGENT items also need immediate maintainer action independent of the
  verdict:** real host data at `<live XDG_DATA_HOME>\opencode\opencode.db`
  (modified in place, pristine backup located and verified) and two stray
  files under `<live CLAUDE_CONFIG_DIR>` /
  `<live CODEX_HOME>` and a stray live-`CODEX_HOME`/
  `XDG_DATA_HOME` write from the T5 push/pull round trip need manual
  restoration/deletion before this host is reused for further real-vendor
  acceptance rows — see §21. A fix pass for `F-PI-CONTENT-EXTRACTION`, a
  decision on the `qwen` version ceiling or a re-pin of the host's `qwen`
  install, and a ConPTY-driven follow-up for the `E5` PTY gap are required
  before the next tagged acceptance run.
