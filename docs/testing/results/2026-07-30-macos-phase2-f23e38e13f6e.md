# Phase 2 acceptance — macOS Device A report

**Verdict:** `FAIL`
**Milestone:** `FINAL_RECONCILIATION`
**Required counts:** `29 PASS / 0 PARTIAL / 1 FAIL / 0 NOT TESTED`
**Optional physical counts:** `0 PASS / 2 NOT TESTED`

This report covers only the exact disposable sessions and paths created for
this run. No real transcript content or secret was used as evidence.

One release-blocking defect was found: `rein fork codex:<id>` produces a
vendor fork that Reinstate mis-attributes to the source session, leaving the
fork unaddressable and contaminating the source record. See finding **F-1**.

## 1. Test record

| Field | Value |
| ----- | ----- |
| UTC date/time | 2026-07-30T08:41Z start, 2026-07-30T10:40Z end |
| Device | `macOS Device A` |
| Tested Git commit | `f23e38e13f6eaf73e2c52d4f65730d7c8f94864b` |
| Signed tag, if any | none — development acceptance (no tag points at HEAD) |
| Reinstate version JSON | `version=v0.1.0-33-gf23e38e`, `commit=f23e38e`, `name=reinstate` |
| OS/version/build | macOS 26.5.2, build 25F84 |
| Architecture | `arm64` |
| Native shell | `zsh 5.9 (arm64-apple-darwin25.0)` |
| Claude Code version/state | `2.1.220` — installed, used for physical evidence |
| Codex CLI version/state | `codex-cli 0.145.0` — installed, used for physical evidence |
| Gemini CLI version/state | `NOT_INSTALLED` (`gemini --version` exits 127) |
| OpenCode version/state | `NOT_INSTALLED` (`opencode --version` exits 127) |
| Git version | `2.55.0` |
| Go version | `go1.26.5 darwin/arm64`; build toolchain pinned to `go1.25.12` by the Makefile |
| Report branch | `test/phase2-f23e38e13f6e-macos-report` |
| Draft PR | see section 13 |

`HEAD` is **not** reachable from `origin/main`; it is the tip of
`origin/feat/phase2-local-index`. This is development acceptance of an exact
commit built from source, not a published release.

## 2. Provenance and repository hygiene

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Tested commit matches the requested commit | PASS | `git rev-parse HEAD` = `f23e38e13f6e…4864b`, equals `origin/feat/phase2-local-index` |
| Binary reports the tested commit | PASS | `rein version --json` → `commit=f23e38e`; identical for the `reinstate` name |
| Product tree was clean before testing | PASS | `git status --porcelain` empty before and after all product tests |
| Report branch starts at the tested commit | PASS | report worktree `git rev-parse HEAD` = tested commit |
| Report is the only committed change | PASS | staged diff contains exactly one added file under `docs/testing/results/` |
| No secret/transcript/private path was committed | PASS | report contains no username, absolute home path, prompt body, or assistant text |

## 3. Isolation and local-only proof

| Assertion | Result | Evidence |
| --------- | ------ | -------- |
| Fresh isolated `REINSTATE_HOME` | PASS | `~/.reinstate-phase2-acceptance-<RUN_ID>` did not exist before the run; verified absent immediately before the first command |
| No `rein init` run | PASS | `init`, `push`, `pull`, `status`, `diff`, and conflict commands were never invoked |
| No `config.toml` or sync state created | PASS | after the first command: `config.toml`, `state.json`, `backups/`, `keys`, `credentials`, `profile.json` all absent |
| No credential/passphrase/keyring request | PASS | no Phase 2 command prompted for or referenced a credential, passphrase, or keyring entry |
| No backend/network dependency | PASS | no storage env values set; all Phase 2 commands succeeded offline |
| Only derived index state created | PASS | the entire home tree is `cache/session-index-v1.sqlite` and nothing else |
| Index and parent permissions are owner-only | PASS | home `drwx------`, `cache` `drwx------`, database `-rw-------`; still `-rw-------` after forced rebuild |

First `rein sessions` from the fresh home: exit `0`, elapsed `3s`, 51 records.

## 4. Controlled corpus

| Agent | Composite reference | Disposable project label | Marker found | Capability |
| ----- | ------------------- | ------------------------ | ------------ | ---------- |
| Claude | `claude:f1325cb1-…-7153c5cfbd51` | `…-acceptance-<RUN_ID>-alpha` | yes, exact challenge response | full |
| Claude | `claude:ddcd1645-…-d1f68c6dcb24` | `…-acceptance-<RUN_ID>-alpha` | yes, plus one structured file reference | full |
| Claude | `claude:e000c8ee-…-8ff752b818ea` | `…-acceptance-<RUN_ID>-alpha` | prompt only (vendor API 529 on that attempt) | full |
| Claude fork | `claude:5ed52272-…-096ec5318aff` | `…-acceptance-<RUN_ID>-alpha` | yes, exact challenge response | full |
| Codex | `codex:019fb245-…-e9b5ba197bd2` | `…phase2 <RUN_ID> unicode-β` | yes, exact challenge response | full |
| Codex fork | vendor id `019fb28c-…-ce2a69e99ca1` | `…phase2 <RUN_ID> unicode-β` | marker written by the vendor, **not indexed as its own identity** | see F-1 |
| Gemini | — | — | — | `NOT_INSTALLED` |
| OpenCode | — | — | — | `NOT_INSTALLED` |

Two disposable Git repositories were created with distinct branches
(`phase2/<RUN_ID>-alpha`, `phase2/<RUN_ID>-beta`) and one controlled relative
file each. No vendor session file was edited by hand at any point.

## 5. Automated verification

| Gate | Result | Sanitized evidence |
| ---- | ------ | ------------------ |
| `make fmt-check` | PASS | exit `0`, no output |
| Focused session-index/CLI/adapter tests | PASS | exit `0`; `sessionindex`, `cli`, `adapter`, `adapter/claude`, `adapter/codex` all `ok` |
| Full Go test suite | PASS | `go test ./...` exit `0`, 21 packages `ok`, zero non-`ok` lines |
| Race suite | PASS | `go test -race ./...` exit `0`, 21 packages `ok` |
| Vet / `make verify` | PASS | `make vet` exit `0`; `make verify` (fmt-check, vet, lint, test, test-race, govulncheck) exit `0`, ends `verify ok` |
| Required cross-builds | PASS | `CGO_ENABLED=0` builds for `windows/amd64`, `darwin/arm64`, `darwin/amd64`, `linux/amd64` all exit `0` |
| Phase 1 regression | PASS | `adapter/...`, `crypto/...`, `pathmap/...`, `sync/...` all `ok` |

Cited deterministic gates, re-run individually and all `--- PASS`:
`TestPhase2MacOSWindowsAndWSLFixtures`,
`TestStoreRebuildsCorruptAndIncompatibleDerivedState`,
`TestStoreReplaceSearchResolveDeleteAndPermissions`,
`TestStoreSerializesConcurrentRefreshAndSearch`,
`TestScanJSONLinesBoundsAndToleratesConcurrentTail`,
`TestSafeTextRemovesTerminalControlsAndCollapsesWhitespace`,
`TestCoalesceRecordsMergesNativeSessionSegmentsDeterministically`,
`TestMissingAndAmbiguousReferencesNeverLaunch`,
`TestAmbiguousReferenceErrorSupportsErrorsIs`,
`TestPlanLaunchRejectsReadOnlyAndMissingWorkspace`,
`TestNativeLaunchRefusalsUseStableCodes`,
`TestGeminiSourceAppliesMetadataAndRewindReadOnly`,
`TestOpenCodeSourceUsesDocumentedJSONCommand`.

No automated gate failed. No gate was skipped or interrupted.

## 6. Configless index and refresh

- `rein sessions` succeeded from the fresh home with no config and discovered
  every controlled Claude and Codex reference.
- `rein sessions --json` and `reinstate sessions --json` were **byte-identical**.
- Ordering is newest `updated_at` first, then agent, then native ID; verified
  programmatically across all 51–53 records with zero violations.
- Three consecutive runs returned identical record counts, identical key order,
  and zero duplicate keys. Two consecutive no-change refreshes produced
  byte-identical record sets.
- The derived index lives only at `cache/session-index-v1.sqlite`.
- Overwriting the database with 45 bytes of non-SQLite text caused a clean
  rebuild on the next command: exit `0`, all controlled references present,
  permissions still `-rw-------`, and both controlled vendor source files
  unchanged by SHA-256.
- `rein list` retains Phase 1 semantics: tab-separated `agent / id / project-key`
  output, distinct from `rein sessions`, and is not redefined as the Phase 2
  canonical listing. See non-blocking finding **N-3** about the runbook wording.
- Structured warnings observed on the real corpus carry codes
  `oversized_record` and `coalesced_session_segments`; the bounded-record guard
  ignored individual JSONL records larger than 4 MiB without dropping the
  containing session.

## 7. Search and inspect

All searches ran against the controlled corpus. Result counts below are
matching-session counts.

| Dimension | Result | Evidence |
| --------- | ------ | -------- |
| Prompt-fragment literal search | PASS | exact Claude marker → 2 controlled sessions; exact Codex marker → 1 |
| Case-insensitive | PASS | fully lowercased marker returned the same 2 sessions |
| AND of multiple terms | PASS | marker + branch fragment → 2; marker + absent term → 0 |
| `--agent` | PASS | `claude` → 2 Claude sessions, `codex` → 1 Codex session |
| `--project` | PASS | alpha fragment → Claude only; `unicode-β` → Codex only |
| `--branch` | PASS | `-alpha` → Claude only; `-beta` → Codex only |
| `--file` | PASS | structured reference fragment and `src/` → exactly the one session holding a file reference; bogus fragment → 0 |
| `--limit 1` | PASS | 3-match query returned exactly 1 |
| Zero match | PASS | deliberate token → empty result, exit `0`, no fallback to all sessions |
| Unicode literal | PASS | `unicode-β` matched only the Codex project |
| Shell metacharacters as literal data | PASS | `$(id); rm -rf / && echo \`whoami\` \| tee …` → 0 matches, exit `0`, no shell execution |
| No network/semantic service | PASS | all searches offline, no storage or embedding call |

Preview and privacy policy:

- `rein sessions --json` records expose only
  `agent, branch, capabilities, files, id, key, message_count, project,
  size_bytes, title, updated_at, workspace` — there is no preview or transcript
  field at all.
- Human `rein search` output prints composite reference, project, branch, and
  title only; it never prints the matching passage.
- `rein inspect` for all three controlled references returned accurate identity,
  agent, timestamps, workspace, project, branch, message count, capability
  flags, and known file references. Every `prompt_preview` was exactly 160
  Unicode code points, contained no control characters, had whitespace
  collapsed, and began with the user-authored prompt.
- No assistant message, reasoning, tool output, environment dump, or auth
  content appeared in any Phase 2 output.
- Strongest privacy evidence: a controlled token that the vendor produced
  **only in an assistant reply** occurs 3 times in the raw Codex session file
  but returns **0** search hits, while the user-authored phrase from the same
  turn is searchable. Phase 2 indexes user prompt text only.
- Inspecting a missing reference fails with `session not found` and exit `2`;
  healthy sessions continued to list and inspect normally.

Refresh:

- After a real vendor append (section 8), the appended user text became
  searchable and resolved to the **same** composite identity, message count
  `2 → 4`, with zero duplicate keys across the whole index.
- A new controlled session created after the earlier ones became the newest
  record within the controlled corpus. See deviation **D-5**.

## 8. Last, resume, and fork

Dry-run plans, all with an argv **array** (never a shell string) and the
recorded workspace as `cwd`, all producing no launch and no source mutation
(SHA-256 of both controlled source files unchanged):

| Command | `executable` | `args` | `cwd` |
| ------- | ------------ | ------ | ----- |
| `resume claude:<id> --dry-run --json` | `claude` | `["--resume", "<id>"]` | alpha project |
| `resume codex:<id> --dry-run --json` | `codex` | `["resume", "<id>"]` | unicode-β project |
| `fork claude:<id> --dry-run --json` | `claude` | `["--resume", "<id>", "--fork-session"]` | alpha project |
| `fork codex:<id> --dry-run --json` | `codex` | `["fork", "<id>"]` | unicode-β project |
| `last --project <alpha> --dry-run --json` | `claude` | `["--resume", "<id>"]` | alpha project |
| `last --agent codex --project unicode-β --dry-run --json` | `codex` | `["resume", "<id>"]` | unicode-β project |

Plan JSON keys: `agent, args, cwd, executable, operation, session_ref`.

`rein last`:

- unfiltered `last --dry-run --json` selected the newest resumable record and
  matching agent, verified programmatically against `sessions --json`;
- `--agent claude` and `--agent codex` each produced the matching executable;
- `--project` with no match → `session not found`, exit `2`, no launch.

Real same-vendor resume:

- **Claude** — `rein resume claude:<id>` launched from `/` (not the workspace),
  exit `0`, and the child returned the **exact** controlled challenge token. The
  vendor appended to its own session file (25848 → 29281 bytes). Reinstate did
  not modify the file before launch: its SHA-256 was unchanged across indexing,
  search, inspect, and all dry-runs.
- **Codex** — `rein resume codex:<id>` launched the real process
  `codex resume 019fb245-…` (confirmed by `ps` argv) and the resumed TUI
  displayed the original session's own history in the recorded unicode-β
  workspace. The controlled turn completed, the vendor appended to the **same**
  rollout file (112951 → 254089 bytes with the fork excluded; 112951 at the
  point of the resume append), the user-authored phrase became searchable under
  the **same** composite identity, message count `5 → 10`, and no duplicate
  identity was created. Driving this required a real terminal; see **D-4**.

Vendor-native fork:

- **Claude — PASS.** `rein fork claude:<id>` exit `0`, exact challenge token
  returned, source SHA-256 unchanged, and the vendor created one distinct
  session file. Refresh discovered **both** source and fork as separate
  identities with zero duplicates. The fork inspected as its own record. A
  resume of the fork alone changed the fork and left the source byte-identical,
  and the fork-only marker searched to the fork identity and **not** the source.
- **Codex — FAIL.** See finding **F-1**.

Safety and failure paths:

| Gate | Result | Exit | Sanitized behaviour |
| ---- | ------ | ---- | ------------------- |
| Missing reference, `resume` | PASS | `2` | `session not found: claude:0000…`, no launch, empty stdout |
| Missing reference, `fork` | PASS | `2` | same |
| Bare unique native ID resolves | PASS | `0` | resolved to the correct composite plan for both agents |
| Ambiguous bare ID | PASS (injected gate) | — | `TestMissingAndAmbiguousReferencesNeverLaunch`, `TestAmbiguousReferenceErrorSupportsErrorsIs`; not manufactured physically because that would require editing a real vendor file |
| Missing workspace | PASS | `5` | disposable project temporarily moved; `recorded session workspace is unavailable`, no launch; project restored |
| Missing executable | PASS | `5` | `PATH=/usr/bin:/bin`; `native agent executable is unavailable`, no launch |
| `--json` without `--dry-run` | PASS | `2` | refused for `resume`, `fork`, and `last`: `--json requires --dry-run for native agent launches` |
| Child failure propagation | PASS | `1` | a controlled shim named `claude` exited `42`; Reinstate reported `claude native resume failed: exit status 42` and returned non-zero. Exit `1` is the documented "unexpected runtime failure" code; see **N-4** |
| Sources unchanged through all failure gates | PASS | — | controlled source SHA-256 unchanged |

The shim also independently confirmed the argv contract at the real process
boundary: `argv=--resume <id>` with `cwd=<recorded alpha workspace>`.

## 9. Interactive switcher

Terminal type: real PTY via a `pty.fork` driver for the line-oriented picker,
and GNU `screen` attached from a native `Terminal.app` window for the Codex TUI
(required by the vendor; see **D-4**). Both binary names were exercised.

| Gate | Result | Evidence |
| ---- | ------ | -------- |
| Bare `rein` on a TTY shows the numbered picker | PASS | header `Local sessions`, rows of `NUMBER  agent  project  native-id` |
| Bare `reinstate` on a TTY shows the picker | PASS | same, controlled Codex filter returned 1 row |
| `/text` filter | PASS | Claude marker filter narrowed to exactly 3 rows, all controlled; a fork-only marker narrowed to exactly 1 row |
| `i NUMBER` inspect | PASS | rendered the same bounded metadata block including the 160-code-point preview |
| `NUMBER` resume | PASS | shim recorded `argv=--resume 5ed52272-…` and `cwd=<recorded alpha workspace>` — the exact displayed composite reference |
| `f NUMBER` fork | PASS | shim recorded `argv=--resume 5ed52272-… --fork-session` and the same recorded `cwd` |
| Invalid input recoverable | PASS | `zzz-not-a-command` and out-of-range `999` both produced `Enter a number, /text, i NUMBER, f NUMBER, or q.` and re-rendered; no default session was chosen and nothing launched |
| `q` cancel | PASS | child exit `0`, no launch, no mutation |
| EOF | PASS | `Ctrl-D` → child exit `0`, no launch |
| Interrupt | PASS | `Ctrl-C` → terminated by `SIGINT`, no launch |
| No unrelated transcript bodies | PASS | picker rows carry only number, agent, project label, and native ID; no prompt or assistant text |
| Non-TTY invocation | PASS | piped empty stdin and `/dev/null` stdin both exit `2` in `0s` with `interactive session picker requires a terminal; use \`rein sessions --json\``, for both binary names; it never hung and never selected the newest session |

Picker output is not reproduced here because the unfiltered list contains
unrelated local sessions. All assertions above were made programmatically.

## 10. Read-only adapters

- Gemini CLI: `NOT_INSTALLED` — `gemini --version` exits `127`.
- OpenCode: `NOT_INSTALLED` — `opencode --version` exits `127`.
- Neither vendor was installed for this run. Rows 28 and 29 are honestly
  `NOT TESTED`.
- `rein sessions --agent gemini` and `--agent opencode` both exit `0` with 0
  records and no error; the same for `rein search --agent`.
- Row 30 is satisfied by the deterministic injected-record gate
  `TestNativeLaunchRefusalsUseStableCodes`, which asserts exit code `5`
  (`ExitCompatibility`) and a `read only by design` message for native actions
  on a read-only adapter, with no launch. Fixture-backed read paths
  (`TestGeminiSourceAppliesMetadataAndRewindReadOnly`,
  `TestGeminiSourceSupportsLegacyConversationJSON`,
  `TestOpenCodeSourceUsesDocumentedJSONCommand`,
  `TestOpenCodeSourceRejectsOversizedCommandOutput`,
  `TestOpenCodeSourceCommandFailuresAreNonDestructive`,
  `TestOpenCodeSourceMissingExecutableIsOptional`) all pass.
- No mutation, resume, fork, or sync capability was offered for either vendor.

## 11. Mandatory matrix

| # | Gate | macOS | Windows |
| - | ---- | ----- | ------- |
| 1 | Exact tested commit/binary provenance | PASS (§1, §2) | PASS |
| 2 | Full local verification and required cross-builds | PASS (§5) | NOT TESTED — toolchain |
| 3 | Fresh configless home; no `init`, credentials, passphrase, or backend | PASS (§3) | PARTIAL — harness |
| 4 | `rein sessions` discovers exact Claude sessions | PASS (§4, §6) | PASS |
| 5 | `rein sessions` discovers exact Codex sessions | PASS (§4, §6) | PASS |
| 6 | `rein` / `reinstate` JSON parity and deterministic ordering | PASS (§6) | PASS |
| 7 | Derived index path, rebuild, idempotency, and private permissions | PASS (§3, §6) | PASS |
| 8 | Prompt-fragment literal search | PASS (§7) | PASS |
| 9 | Agent filter | PASS (§7) | PASS |
| 10 | Project filter | PASS (§7) | PASS |
| 11 | Branch filter | PASS (§7) | PASS |
| 12 | File filter | PASS (§7) | NOT TESTED — corpus |
| 13 | AND terms, limit, case, Unicode, and zero-match behavior | PASS (§7) | PASS |
| 14 | `sessions` and `search` do not dump transcript passages | PASS (§7) | PASS |
| 15 | `inspect` metadata/160-code-point user preview policy | PASS (§7) | PASS |
| 16 | Append/new-session refresh and no-change idempotency | PASS (§6, §7, D-5) | PASS (see R-16) |
| 17 | `last` selects the correct resumable session and filters | PASS (§8) | PASS |
| 18 | Claude dry-run plan has exact argv/cwd and no mutation | PASS (§8) | PASS |
| 19 | Codex dry-run plan has exact argv/cwd and no mutation | PASS (§8) | PASS |
| 20 | Claude native resume | PASS (§8) | NOT TESTED — logged out |
| 21 | Codex native resume | PASS (§8) | PARTIAL |
| 22 | Claude vendor-native fork, source preserved | PASS (§8) | NOT TESTED — logged out |
| 23 | Codex vendor-native fork, source preserved | **FAIL (F-1)** | **FAIL (F-1)** |
| 24 | Missing/ambiguous reference and missing executor fail safely | PASS (§8) | PASS |
| 25 | JSON/native-child separation and child failure propagation | PASS (§8) | PASS |
| 26 | TTY picker filter, inspect, resume, fork, and cancel | PASS (§9) | FAIL |
| 27 | Non-TTY prompt failure is immediate and actionable | PASS (§9) | PASS |
| 28 | Gemini read-only physical path, when installed | NOT TESTED — `NOT_INSTALLED` (§10) | FAIL — unproven |
| 29 | OpenCode read-only physical path, when installed | NOT TESTED — `NOT_INSTALLED` (§10) | FAIL — vendor |
| 30 | Read-only resume/fork refusal with exit `5` (physical or injected-record gate) | PASS (§10) | PASS |
| 31 | Malformed/concurrent/oversized fixture and privacy gates | PASS (§5, §6, §7) | PASS |
| 32 | Phase 1 automated regression remains green | PASS (§5, §6) | PASS (see R-32) |

The Windows column is intentionally empty; this device did not observe Device B.

## 12. Findings

### Release-blocking

**F-1 — `rein fork codex:<id>` mis-attributes the vendor fork to the source
session, making the fork unaddressable and contaminating the source record.**

Observed, in one physical run:

1. `rein fork codex:019fb245-…` launched the correct process
   (`ps` argv `codex fork 019fb245-…`) in the recorded workspace.
2. The vendor created a new rollout file whose
   `session_meta.payload.session_id` is its **own** new id
   `019fb28c-…-ce2a69e99ca1`, with `forked_from_id = 019fb245-…`. The same file
   also replays source history records that carry the **source** id.
3. Reinstate indexed that file under the **source** identity and emitted
   `coalesced_session_segments: coalesced 2 local records for one native session`.
4. Consequences:
   - `rein resume codex:019fb28c-…` → `session not found`, exit `2`;
   - `rein inspect codex:019fb28c-…` → `session not found`, exit `2`;
   - the fork never appears in `rein sessions` as its own identity;
   - the **source** record absorbed the fork: `message_count 10 → 24`,
     `size_bytes 112951 → 254089`, and the fork's user prompt (including the
     fork-only controlled marker) is searchable under the source composite
     reference, even though the source's own rollout file is byte-for-byte
     unchanged and does not contain that marker.

This breaks four mandatory results of runbook section 11 for Codex: distinct
native identity, refresh discovering both source and fork, each resuming
through its vendor's native path, and changing the fork not changing the
source.

Root cause, `internal/sessionindex/codex.go` lines 121–128: the identity loop
iterates `[event, payload]` and, for `index == 0`, accepts `id`/`sessionId`/
`session_id` from **any** top-level record regardless of type — the
`session_meta` guard only constrains the payload map. Last write wins, so the
replayed source-id records overwrite the fork's own `session_meta` identity.
`forked_from_id` is not consulted.

Suggested fix direction (not applied — no product code was modified): take the
native identity from the first `session_meta` record only, and treat a record
carrying `forked_from_id` as establishing a **new** identity whose parent is the
source, so coalescing never merges a fork segment into its source.

Coverage gap: no automated test exercises a Codex fork rollout shape
(`session_meta.payload.session_id` = fork id, `forked_from_id` = source id,
plus replayed source-id records). `TestCoalesceRecordsMergesNativeSessionSegments
Deterministically` covers legitimate continuation segments only, which is why
this passed every merge gate. Retest scope for the fix: that new fixture, the
full `sessionindex` and `cli` suites, `make verify`, and a repeat of physical
rows 21, 22, 23, and 16 on both devices.

### Non-blocking

**N-1 — uncalled vulnerability in a required module.** `make verify`'s
`govulncheck` pass reports `GO-2026-5932` in `golang.org/x/crypto@v0.54.0`,
`Fixed in: N/A`, not reachable from Reinstate's call graph. `verify` exits `0`.
Worth tracking until an upstream fix exists.

**N-2 — warning volume on a real corpus.** The first refresh over the real
vendor trees emitted 16 warning lines, 13 of them one-per-record
`oversized_record` notices for individual Codex JSONL records above 4 MiB. The
bounded-record guard behaves correctly and the containing sessions were still
indexed, but a per-session summarised count would be friendlier than one line
per record on human output.

**N-3 — runbook wording conflicts with actual `rein list` behaviour.** Runbook
section 6 item 5 states that `rein list` "is not expected to work from this
fresh configless home". In practice `rein list` exits `0` from the fresh
configless home and prints Phase 1 adapter output (tab-separated
`agent / id / project-key`) that is clearly distinct from `rein sessions`. The
product behaviour is correct — `list` is not redefined as the Phase 2 canonical
listing — so the runbook sentence should be corrected rather than the code.

**N-4 — child exit codes are not mirrored.** A native child exiting `42`
surfaces as Reinstate exit `1`. This matches the documented stable table in
`docs/cli-reference.md` (`1 = unexpected runtime failure`) and preserves the
Phase 1 contract, so it is recorded as expected behaviour, not a defect.

### Test-harness deviations

**D-1** `timeout(1)` is not present on macOS; a small `perl` alarm wrapper was
used for bounded vendor invocations.

**D-2** An isolated `CLAUDE_CONFIG_DIR` was attempted first so the real vendor
tree would not be touched; the vendor rejected it with `Not logged in`. Auth
files were **not** copied. The run therefore used the real vendor root with all
filesystem inspection confined to the disposable project's own subdirectory.
The first controlled Claude attempt also returned vendor `API Error: 529
Overloaded` and was retried; that attempt's session
(`claude:e000c8ee-…`) is listed in section 4 because it is part of the
controlled corpus.

**D-3** `codex exec` consumed the inherited pipe as stdin and blocked; it was
re-run with stdin from `/dev/null`.

**D-4** The Codex TUI cannot be driven from a bare PTY harness: it blocks
waiting for terminal capability replies (`OSC 10/11`, `DSR`, `DA1`, kitty
keyboard) and, on this machine, was additionally blocked on an interactive
vendor **update prompt** (`Update available! 0.145.0 -> 0.146.0`). The
identical behaviour reproduced with `codex resume <id>` invoked **directly**,
with Reinstate entirely out of the loop, so it is a vendor/harness interaction
and not a Reinstate defect. Rows 21 and 23 were driven through GNU `screen`
attached from a real `Terminal.app` window, which the vendor accepts.

**D-5** Row 16's "the new session becomes the newest eligible record" was
asserted against the controlled corpus rather than globally, because the
operating agent's own live Claude Code session is inherently the newest record
on this device. Global deterministic ordering was separately verified across all
records with zero violations.

No deviation was converted into a `PASS`.

## 13. Repository hygiene

- report-only branch: `test/phase2-f23e38e13f6e-macos-report`
- tested base commit: `f23e38e13f6eaf73e2c52d4f65730d7c8f94864b`
- changed files: `docs/testing/results/2026-07-30-macos-phase2-f23e38e13f6e.md` (added)
- private/local artifacts excluded: isolated `REINSTATE_HOME`, derived SQLite
  index, disposable projects, vendor session files, controlled markers' raw
  logs, and every generated binary remain outside the repository
- product code unchanged: `true`
- secrets/transcripts committed: `false`

## 14. Device milestone block

```text
PHASE2-DEVICE-REPORT-V1
device=macos
test_commit=f23e38e13f6eaf73e2c52d4f65730d7c8f94864b
reinstate_version=v0.1.0-33-gf23e38e
report_path=docs/testing/results/2026-07-30-macos-phase2-f23e38e13f6e.md
report_branch=test/phase2-f23e38e13f6e-macos-report
claude_ref=claude:f1325cb1-27da-4699-ade9-7153c5cfbd51
codex_ref=codex:019fb245-747c-7271-b421-e9b5ba197bd2
gemini_state=NOT_INSTALLED
opencode_state=NOT_INSTALLED
required_pass=29
required_partial=0
required_fail=1
required_not_tested=0
optional_physical_pass=0
optional_physical_not_tested=2
configless_local_only=PASS
preview_privacy=PASS
claude_resume_fork=PASS
codex_resume_fork=FAIL
picker=PASS
phase1_regression=PASS
release_blocking_findings=1
product_files_changed=0
secrets_or_transcripts_committed=false
END-PHASE2-DEVICE-REPORT-V1
```

## 15. Final reconciliation

Added by the macOS coordinator after validating the complete native-Windows
report, both report branch tips, and both report-only diffs. Peer `PASS` labels
were not trusted; every row below cites which device produced physical evidence
and which automated gate covers synthetic-only cases.

### 15.1 Windows report validation

| Assertion | Result | Evidence |
| --------- | ------ | -------- |
| Report branch tip matches the claimed commit | PASS | `origin/test/phase2-f23e38e13f6e-windows-report` = `75952cfd6b43…cc0f3` |
| Branch is based on the same tested commit | PASS | tested commit is an ancestor; two commits on top, both report-only |
| Diff versus the tested commit is report-only | PASS | `A docs/testing/results/2026-07-30-windows-phase2-f23e38e13f6e.md`, nothing else |
| Complete terminated `PHASE2-DEVICE-REPORT-V1` block | PASS | `device=windows`, `test_commit=f23e38e…4864b`, `END-` terminator present |
| Both reports name the same tested commit | PASS | identical full SHA in both blocks |
| No secret, transcript, or absolute private path committed | PASS | scan of the committed file found no Windows or POSIX absolute home path and no secret pattern |
| Counts internally consistent | PASS | `20 + 1 + 9 + 0 = 30` required rows |

Two provenance deviations on Device B are accepted as recovered and honest: the
supplied checkout initially pointed at an older report commit, and the report
was first written into that stale checkout before being moved into the
dedicated worktree. Both were disclosed and the final artefacts are correct.
Device B's binary was **not** produced by `make build` (`make` is absent on that
host), which is why its version string is `v0.1.0-rc.8-72-gf23e38e` with the
full SHA in `commit`, against `v0.1.0-33-gf23e38e` with the short SHA here.
Both binaries embed the same tested commit, so provenance holds.

### 15.2 Independent verification of the tested commit's CI

Required checks on `f23e38e13f6eaf73e2c52d4f65730d7c8f94864b`, read directly
from the forge rather than from either report:

| Check | Conclusion |
| ----- | ---------- |
| `Test (macos-latest)` | success |
| `Test (ubuntu-latest)` | success |
| `Test (windows-latest)` | success |
| `Lint` | success |
| `Security` | success |
| `Website` | success |

All six required checks passed, including the Windows job. This is decisive for
rows 2 and 32 and is used below.

### 15.3 Corrections to Device B's findings

Device B's `PASS` labels were not accepted uncritically, and neither were its
`FAIL` labels. Three of its nine release-blocking findings do not survive
independent verification:

**Corrected — "installed Codex version is outside the compatibility
allowlist".** It is not. `internal/adapter/codex/codex.go` pins the verified
range to `0.133.0 … 0.145.0` inclusive, and a direct probe of
`adapter.StableVersionInRange("0.145.0", "0.133.0", "0.145.0")` returns `true`.
The same Codex `0.145.0` on this Mac yields `agent.codex status=ok` and
`setup check --json` exit `3`, exactly what the test expects. Device B's
`UNTESTED` result therefore indicates a **Windows-specific agent-detection
failure** — most plausibly `codex --version` not being resolvable or parseable
through Go's `exec.LookPath` when the vendor ships a shim — not an
out-of-range version. This still needs a Windows-side root-cause fix, but it is
a different defect from the one reported.

**Corrected — "required verification is red/incomplete".** `make` and GCC are
absent on that host, so `make fmt-check`, `make vet`, `make verify`, and the
race suite could not **start** (exit `127` / missing gcc). Two of its three
test failures (`TestVerifyAvoidsRedundantDoctestRuns`,
`TestQuickGateStaysFocusedAndNonRelease`) are doctests that shell out to
`make`. Non-execution is `NOT TESTED`, not `FAIL`. `Test (windows-latest)` in
CI ran the suite on Windows against this exact commit and passed.

**Corrected — row 16.** Device B's own section 6 records that the Codex append
refresh found the new literal, that the unchanged refresh was byte-idempotent,
and that Reinstate changed neither controlled source. Its row 16 `FAIL` is
attributable to the fork not being discovered, which is finding **F-1** at row
23. Counting it twice overstates the defect surface.

**Upheld and reinforced — F-1.** Device B reached the same root cause
independently: the fork rollout contains a fork `session_meta` ID followed by a
source `session_meta` ID, the Codex parser lets the later one win, and the fork
collapses into the source record. Its evidence adds
`sessions --agent codex --limit 1000` returning zero rows for the fork and
`inspect` exit `2`. Two independent devices, same diagnosis: **CONFIRMED**.

### 15.4 Reconciled 32-row matrix

`PASS` in the "Both" column means both devices produced genuine passing
evidence.

| # | macOS | Windows | Both | Reconciliation |
| - | ----- | ------- | ---- | -------------- |
| 1 | PASS | PASS | **PASS** | Both built and ran the same commit; both binaries embed it |
| 2 | PASS | NOT TESTED | no | Device B lacked `make`/GCC so the gates never started; CI `Test (windows-latest)` green on this commit |
| 3 | PASS | PARTIAL | no | Device B's main isolated home was correct; one invalid `$home` retry wrote an index under the user profile. Operator harness error, not a product defect |
| 4 | PASS | PASS | **PASS** | Physical on both |
| 5 | PASS | PASS | **PASS** | Physical on both |
| 6 | PASS | PASS | **PASS** | Byte-identical alias JSON and contract ordering on both |
| 7 | PASS | PASS | **PASS** | Both forced a corrupt-index rebuild; both confirmed owner-only permissions under their OS model |
| 8–11 | PASS | PASS | **PASS** | Prompt, agent, project, branch filters physical on both |
| 12 | PASS | NOT TESTED | no | Device B's corpus contained no structured file reference (Claude logged out, Codex emitted none), so the dimension was never exercised. macOS proved it with a real tool-use session |
| 13 | PASS | PASS | **PASS** | AND, limit, case, Unicode, zero-match on both |
| 14 | PASS | PASS | **PASS** | Neither device saw a transcript passage in `sessions` or `search` |
| 15 | PASS | PASS | **PASS** | 160 code points here, 159/160 there; collapsed and control-free on both |
| 16 | PASS | PASS | **PASS** | Append refresh, stable identity, and no-change idempotency held on both; Device B's `FAIL` label is F-1 at row 23 (see 15.3) |
| 17 | PASS | PASS | **PASS** | `last` and its filters physical on both |
| 18 | PASS | PASS | **PASS** | Exact `claude --resume ID` argv array and recorded cwd on both |
| 19 | PASS | PASS | **PASS** | Exact `codex resume ID` argv array and recorded cwd on both |
| 20 | PASS | NOT TESTED | no | Claude Code is logged out on Device B; Reinstate launched the exact argv but no turn could complete. macOS has full physical evidence including the exact challenge response |
| 21 | PASS | PARTIAL | no | Both devices independently hit the same Codex TUI automation wall; macOS cleared it through a real terminal and completed the turn, Device B fell back to vendor-native resume of the same ID |
| 22 | PASS | NOT TESTED | no | Same logged-out cause as row 20 |
| 23 | **FAIL** | **FAIL** | **FAIL** | **F-1, confirmed independently on both devices** |
| 24 | PASS | PASS | **PASS** | Missing reference physical on both; ambiguity by injected gate on both |
| 25 | PASS | PASS | **PASS** | JSON/dry-run separation physical on both; child-failure propagation physical here, injected gate there |
| 26 | PASS | FAIL | no | macOS passed every interaction including `Ctrl-C`. Device B's resume/fork failures trace to the Codex TUI wall, but its **interrupt** failure is Windows-console-specific and unexplained — a genuine open risk |
| 27 | PASS | PASS | **PASS** | Immediate exit `2` with the `rein sessions --json` hint on both |
| 28 | NOT TESTED | FAIL | no | Not installed here. Installed but unauthenticated there, so the read path was never exercised; per the runbook it cannot be `NOT TESTED` when the vendor is installed |
| 29 | NOT TESTED | FAIL | no | Not installed here. There, `opencode session list --format json` fails on OpenCode 1.18.2 with `no such column: name`. Reinstate degrades correctly to a `source_scan_failed` warning and keeps indexing other sources |
| 30 | PASS | PASS | **PASS** | Injected-record gate `TestNativeLaunchRefusalsUseStableCodes` (exit `5`, no launch) on both |
| 31 | PASS | PASS | **PASS** | Malformed, oversized, concurrent-tail, and privacy fixtures on both |
| 32 | PASS | PASS | **PASS** | No Phase 1 regression. Device B's `FAIL` is `TestSetupCheckJSONPreservesFailureExit`, a host-environment-dependent test (see R-32); CI Windows and this Mac both pass it |

**Required dual-device rows passed: 21 of 30.** One row (23) genuinely fails on
both devices. Eight rows lack Windows evidence for environmental or harness
reasons and must be re-run there.

### 15.5 Findings after reconciliation

**Release-blocking (1):**

- **F-1 — Codex fork identity collapse.** Confirmed on macOS and native
  Windows with independently derived root causes that agree.
  `internal/sessionindex/codex.go` lets any later `session_meta` (or top-level
  `id`) override the fork's own identity, so `rein fork codex:<id>` produces a
  fork Reinstate cannot address and the source record absorbs it.

**Must fix before re-certification, not product defects (4):**

- **R-2 — Windows Codex detection.** `0.145.0` is inside the verified range yet
  Device B's `setup check` reported `UNTESTED`. Root-cause the Windows
  `codex --version` probe.
- **R-32 — host-dependent merge gate.** `TestSetupCheckJSONPreservesFailureExit`
  shells out to the real installed agents and asserts an exact exit code, so it
  passes or fails according to what is installed on the machine. It passed here
  and on CI Windows and failed on a Windows box with Codex installed. A merge
  gate must not depend on host vendor state.
- **R-26 — Windows picker interrupt.** `Ctrl-C` produced no result on Device B.
  Not reproducible here, not explained by the Codex TUI issue, and Windows
  console signal handling genuinely differs. Needs a targeted Windows retest.
- **R-29 — OpenCode read surface.** The documented
  `opencode session list --format json` command errors on OpenCode 1.18.2.
  Reinstate's handling is correct, but the Phase 2 OpenCode read path cannot be
  validated against the current vendor release.

**Non-blocking (4):**

- **N-1** `GO-2026-5932` in `golang.org/x/crypto@v0.54.0`, uncalled, no fix
  available.
- **N-2** One `oversized_record` warning line per record on a real corpus;
  a per-session summary would read better.
- **N-3** Runbook section 6 item 5 wrongly states `rein list` will not work
  from a configless home; it exits `0` with Phase 1 output distinct from
  `sessions`. Fix the runbook, not the code.
- **N-5** The Codex verified range tops out at `0.145.0` while `0.146.0` is
  already shipping — both devices saw the vendor's own update prompt. The
  allowlist will go stale imminently.

### 15.6 Verdict and retest scope

Phase 2 does **not** pass physical acceptance. Row 23 fails on both devices,
and eight further required rows have no valid Windows evidence.

Fixing F-1 requires a new fixture for the Codex fork rollout shape
(`session_meta` fork ID, `forked_from_id`, then replayed source-ID records) —
absent today, which is why this passed every merge gate — plus the
`sessionindex` and `cli` suites, `make verify`, and a physical re-run of rows
16, 21, 22, and 23 on both devices. The Windows re-run additionally needs
`make` and a C toolchain installed, Claude Code logged in, a tool-using session
in the corpus for row 12, and a corrected isolation harness.

```text
PHASE2-FINAL-RECONCILIATION-V1
test_commit=f23e38e13f6eaf73e2c52d4f65730d7c8f94864b
mac_report_commit=9be6780ed987e983a12a512fdf9e9708cd100006
windows_report_commit=75952cfd6b43d1d22ac1d8e6fd95daff901cc0f3
mandatory_rows=32
required_dual_device_rows_passed=21
optional_rows_28_29_macos=NOT_TESTED
optional_rows_28_29_windows=FAIL
automated_gates=PASS
physical_macos=FAIL
physical_windows=FAIL
release_blocking_findings=1
phase2_status=FAIL
END-PHASE2-FINAL-RECONCILIATION-V1
```
