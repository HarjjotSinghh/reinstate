# Phase 4 structured-handoff report — macOS arm64 / v0.4.0-rc.4

Copy of the Phase 4 template with this dispatch's substitution table applied.
No rc.1, rc.2, or rc.3 corpus, home, capsule, lineage, or report row was reused.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Required counts:** `37 PASS / 0 PARTIAL / 7 FAIL / 0 NOT TESTED`
- **Optional source-only counts:** `2 PASS / 2 FAIL / 4 NOT TESTED`
- **Release-blocking findings:** `3`

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required result
is `FAIL`. Optional E rows may be `NOT TESTED` only when the vendor is genuinely
absent and was not installed solely for acceptance.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-08-13T08:59:22Z` start; matrix complete `2026-08-13T09:21:03Z` |
| Device | `macos-arm64` |
| OS/version/build | `macOS 26.5.2 (25F84)` |
| CPU architecture/native process | `arm64` native |
| Filesystem | `APFS` |
| Tested tag | `v0.4.0-rc.4` |
| Tested full commit | `08d8b0ee469472e7382f12feb85914c75a9bdee0` |
| Installed binary SHA-256 | `e53ef41edbd603b31815bf3e5ad8e9f7fee24c2624a9b0369ad97316dd356e01` |
| Installed version JSON | `version=0.4.0-rc.4 commit=08d8b0ee469472e7382f12feb85914c75a9bdee0 date=2026-08-13T08:44:44Z` |
| Claude Code version/state | before `2.1.228` (in range 2.1.219–2.1.229); after `2.1.228`; throwaway home not logged in; `DISABLE_AUTOUPDATER=1`; `claude config set autoUpdates false` wrote only into the throwaway home |
| Codex CLI version/state | before `0.147.0` (in range 0.133.0–0.147.0); after `0.147.0` |
| Gemini/OpenCode/Grok state | Gemini `ABSENT`; OpenCode `ABSENT` (no isolation override; skipped; never claimed isolated); Grok `1.0.3` present |
| Git version | `git 2.55.0` |
| Go version/toolchain | host `go1.26.5`; gates `GOTOOLCHAIN=go1.25.12` |
| Report branch | `test/v0.4.0-rc.4-macos-arm64-report` |
| Device-report commit | `(filled after this commit)` |
| Draft report PR | `(filled after open)` |

## 2. Signed artifact and installer chain

No downloaded executable ran until the live bootstrap pin and canonical installer digest passed.

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Annotated tag signature verifies against `.github/allowed_signers` | `PASS` | `git verify-tag v0.4.0-rc.4` good git SSH signature, ED25519 key SHA256:uXE5rh19MaNxQO/0pgR8n2K73xR+ck0O7fwf351W030 |
| Tag peels to the recorded full commit on `origin/main` | `PASS` | peeled `08d8b0ee469472e7382f12feb85914c75a9bdee0`; `merge-base --is-ancestor` vs `origin/main` |
| Published release is non-draft, prerelease, and tied to the tag | `PASS` | `gh release view`: draft=false prerelease=true tag=`v0.4.0-rc.4` target=main |
| Exact expected release asset set is present | `PASS` | 25 assets: checksums.txt, five platform archives, five archive SBOMs, five raw binaries, eight Linux native packages, one source archive |
| Checksums, GitHub API digests, and attestations match the tag and commit | `PASS` | darwin_arm64 archive sha256 `385df870846aec30a6186ad70d349ae441e884ff9a3e1d758f02401961e01b92` matches checksums.txt and GitHub API digest; raw binary sha256 `e53ef41edbd603b31815bf3e5ad8e9f7fee24c2624a9b0369ad97316dd356e01`; `gh attestation verify` exit 0 |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | `PASS` | tar members `CHANGELOG.md LICENSE NOTICE README.md reinstate`; Mach-O arm64; SBOM present |
| Live bootstrap is byte-identical to the tested commit and pins only `v0.4.0-rc.4` | `PASS` | live `https://reinstate.dev/install.sh` sha256 `37b0218816255c6f76c674495823b93db22dc8df1a103478040a5009a696b4c4` matches tagged `website/public/install.sh`; pin `VERSION=v0.4.0-rc.4`; no `v0.4.0-rc.1`, `v0.4.0-rc.2`, or `v0.4.0-rc.3` pin. Independently re-proved `install.ps1` sha256 `ec029d63267a0ec82021691cc5973f4f66268e1bd0876a633160733a889d82e5` |
| Bootstrap installer digest matches the tagged canonical installer | `PASS` | `PINNED_INSTALLER_SHA256=7776adb4ace8aa333745cd3f3e42b3a10d1400b9394c612d065c20a739db2e66` equals tagged `scripts/install.sh`; installer reported checksum ok |
| Fresh isolated install did not replace a user binary or persist PATH changes | `PASS` | new `INSTALL_DIR` then only `reinstate` + `rein` symlink; `REINSTATE_SKIP_PATH_UPDATE=1`; user binary different inode and hash |
| `rein` and `reinstate` have identical verified bytes and report `v0.4.0-rc.4` | `PASS` | `rein` is symlink to `reinstate`; sha256 `e53ef41edbd603b31815bf3e5ad8e9f7fee24c2624a9b0369ad97316dd356e01`; `version --json` identical |
| Installed binary reports the literal full tested commit | `PASS` | `reinstate 0.4.0-rc.4 (08d8b0ee469472e7382f12feb85914c75a9bdee0 2026-08-13T08:44:44Z)` |

Any failure here stops product-behavior testing. A source build is supplemental
and never substitutes for the installed tagged artifact.

## 3. Automated gates

| Gate | Result | Sanitized evidence |
| ---- | ------ | ------------------ |
| Clean tagged worktree and `go mod tidy -diff` | `PASS` | report worktree porcelain empty; `go mod tidy -diff` 0 bytes |
| `make verify` with Go 1.25.12 | `PASS` | `GOTOOLCHAIN=go1.25.12` `env -i` run ended `verify ok`; `govulncheck` 0 code vulnerabilities. An earlier run that inherited isolation env / host load failed two CLI tests with dest `UNTESTED`; those tests PASS in a clean env (`TestHandoffFromRealisticClaudeInstall`, `TestHandoffFromClaudeInstallWithoutExecutable`, `TestHandoffExecutedOutputMatchesDryRunByteForByte`) |
| Complete `CGO_ENABLED=1 go test -race ./... -count=1` | `PASS` | included in the clean `make verify` (`internal/cli` race 13.652s ok) |
| Required cross-build, fuzz-smoke, snapshot, artifact, and installer gates | `PASS` | four `CGO_ENABLED=0` builds darwin/amd64, darwin/arm64, linux/amd64, windows/amd64 exit 0; `make fixture-scan` exit 0; `goreleaser build --snapshot --single-target` exit 0; worktree remained clean |
| Phase 1, Phase 2, and Phase 3 regression | `PASS` | `go test ./...` inside `make verify` exit 0 |
| Capsule/projection/CLI goldens are unchanged across repeated runs | `PASS` | unit goldens in `make verify`; installed `--dry-run` vs `--no-launch` stdout+stderr byte-identical after exact `--allow-warning` IDs |
| Phase 4 adversarial security tests and `make fixture-scan` | `PASS` | `make verify` + explicit `make fixture-scan` PASS; installed adversarial fixtures D1/D2/D4 exit 0 |
| `TestLongHistoryParseCapsuleProjectionUnderCeiling`: 400 events, ≤98,304 bytes, <2,000 ms | `PASS` | 15.757542 ms; projection 46,541 bytes; Events=400 |

## 4. Isolation and privacy

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Fresh isolated `REINSTATE_HOME`, install directory, process homes, and repositories | `PASS` | five vendor overrides exported before product commands; throwaway INSTALL_DIR/home/repos; not rc.1/rc.2/rc.3 homes |
| Fresh controlled source/destination sessions; no older corpus or handoff reused | `PASS` | tagged fixtures copied into throwaway config dirs; Claude print-mode attempted only in the throwaway Claude home |
| Operator vendor trees, credentials, keychains, `.env`, and token stores were never read | `PASS` | all five overrides set before first product command; list-isolation decoy HOME session not shown; operator-tree path counts in case artifacts 0 |
| No backend, passphrase, storage credential, or capsule sync was used | `PASS` | no `rein init`; `rein push` exit 3 |
| Vendor configuration changed only inside disposable test-owned homes | `PASS` | throwaway Claude gained `.claude.json` + two jsonl sessions from `claude -p` / config attempt |
| Reports contain no transcript, prompt, response, secret, private path, filename, diff, capsule body, or raw child error | `PASS` | this file; private bytes stayed off-repo |
| Source fingerprints were unchanged; vendor-store writes occurred only through an explicitly launched vendor CLI | `PASS` | long-history jsonl sha256 unchanged across dry-run/no-launch/G3; Claude jsonl created only by Claude CLI in the throwaway home; Codex dest tree changed only during the explicit A1 PTY launch |

After first index refresh, `rein sessions --json` showed only throwaway-run Claude sessions (count 1–2). No operator-home paths. OpenCode skipped / `NOT TESTED`.

## 5. Required matrix — 44 rows

### Matrix A — flagship quota-switch

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| A1 | `FAIL` | Logged-out throwaway Claude print-mode session dry-run `--to codex --json` exit 0, mode `structured handoff`, `destination_session_mode=new`. Real PTY launch (`TERM=xterm-256color`) produced 87 bytes of destination TUI control sequences and no destination acknowledgement within 12 s. Source jsonl hashes unchanged. Dest Codex tree gained vendor files attributed to the launched vendor process. |
| A2 | `FAIL` | No Codex-authored throwaway task session and no Claude destination acknowledgement. |
| A3 | `FAIL` | Throwaway Claude already logged out (A3 source condition). A1 dry-run from that logged-out source exit 0, but destination acknowledgement was not collected. |
| A4 | `PASS` | Tagged `partial-final-record` fixture; installed `--dry-run --json` exit 0; mode `structured handoff`; `destination_session_mode=new`. |
| A5 | `FAIL` | No destination first-reply restatement collected. Dirty-tree `changed_files` were present on dry-run plans (C1/R3) but were not restated by a live destination. |
| A6 | `FAIL` | No destination acknowledgement; MARKER non-repeat not observed. |
| A7 | `FAIL` | Stored no-launch lineage lists/inspects (mode `structured handoff`, inspect/export exit 0) but Claude destination ID was not launched so not resolved; Codex destination unresolved by absence of launch. |

### Matrix B — fidelity and policy

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| B1 | `PASS` | Unit `TestHandoffExecutedOutputMatchesDryRunByteForByte` in `make verify`; installed `--dry-run` vs `--no-launch` stdout **and** stderr byte-identical after three exact `--allow-warning` IDs; both exit 0. |
| B2 | `PASS` | Tagged 200-turn/400-event fixture; parse Events=400; G1 projection 46,541 bytes under 98,304. |
| B3 | `PASS` | `--policy checkpoint --dry-run --json` exit 0; `projection_events=0`. |
| B4 | `PASS` | `--policy balanced` exit 0; no-launch wrote sidecar artifacts under owner-only `handoffs/`. |
| B5 | `PASS` | `--policy full --dry-run` exit 0; parse Events=400; no silent drop observed. |
| B6 | `PASS` | Compaction fixture `--no-launch --json` exit 0; `fidelity.components` portabilities `exact`, `normalized`, `summarized`, `referenced`, `omitted`; `fidelity.json` contains `summarized`. Long-history fixture lacked `summarized` (no vendor summary event); compaction supplies the class. |
| B7 | `PASS` | Tagged attachments fixture `--dry-run --json` exit 0. |
| B8 | `PASS` | Tagged unknown-records fixture `--dry-run --json` exit 0. |

### Matrix C — workspace and capability truth

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| C1 | `PASS` | Disposable dirty repo; plan `changed_files` count=3; staged+unstaged+untracked. |
| C2 | `PASS` | Throwaway copy with a transcript claim that the tree was clean; live Git still reported dirty with 3 changed files and `git.working_tree` warning. |
| C3 | `PASS` | Session cwd rewritten to a missing path; `--dry-run --json` exit **5**. |
| C4 | `PASS` | Same source from a different git repo cwd; `--dry-run --json` exit **5** with `different repository`. Same-repo subdirectory exit 0. Non-git cwd did not refuse for that reason. |
| C5 | `FAIL` | Windows os-roots fixture `--dry-run --json` exit **5** `environment preflight is blocked`; no live remap onto the macOS disposable checkout. |
| C6 | `PASS` | Source MCP `browser` absent at Codex dest; warning `handoff.capability.mcp.browser`; launch without that ack exit **7**. |
| C7 | `PASS` | Source skill `review` absent at dest; warning `handoff.capability.skill.review`; same unacked launch exit **7**. |
| C8 | `PASS` | Fake `claude --version` = `2.1.230`; `--dry-run --json` exit **5**; handoffs residue files=0. |

### Matrix D — security

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| D1 | `PASS` | Prompt-injection fixture `--no-launch --json --show-redactions` exit 0; injected phrase appears as source Goal/Latest/Imported-history text; control flow unchanged (handoff completes). |
| D2 | `PASS` | Fence-breakout fixture exit 0; operator-tree counts 0. |
| D3 | `PASS` | Projection scan: 0 hits for system/developer instruction markers. |
| D4 | `PASS` | Secret-leakage fixture exit 0; AKIA/sk-ant-/ghp_ counts 0. |
| D5 | `PASS` | Launch-free rows used throwaway homes only. |
| D6 | `PASS` | `handoffs/` mode `0700`; files `0600`. |
| D7 | `PASS` | Isolated Claude/Codex/Gemini/Grok roots only. OpenCode skipped (no override). Vendor dest writes only during explicit A1 PTY launch. |
| D8 | `PASS` | No init; `rein push` exit 3; output did not mention `handoffs/`. |
| D9 | `PASS` | `--no-redact` is a known flag. Claude source `--no-redact --dry-run --json` exit 0. Grok `--no-redact` refused exit **2** (not unknown flag) with forced-redaction reason. |
| D10 | `PASS` | Source jsonl sha256 identical before/after dry-run, no-launch, G3×100, and A1 PTY. |

### Matrix F — CLI contract

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| F1 | `PASS` | Human and JSON surfaces used `structured handoff` and `destination_session_mode=new`. Cross-agent copy said “a new codex session, not native resume”. No `same session` / `lossless` / `full context`. |
| F2 | `PASS` | `rein resume … --with codex --dry-run --json` exit 0; structured-handoff notice; mode `structured handoff`. (`--allow-warning` is not a `resume` flag; row collected without it.) |
| F3 | `PASS` | `--json` without `--dry-run`/`--no-launch` exit **2**. |
| F4 | `PASS` | unknown / duplicate / wildcard / stale `--allow-warning` each exit **2**. Unacked **launch** exit **7** with IDs `baseline.unavailable`, `git.working_tree`, `handoff.capability.attachment.support`. Unacked `--no-launch` exit 0 and records lineage (RC3 G3: unacked warnings must not zero the list). |
| F5 | `PASS` | `--no-launch --json` with exact warning acks exit 0; no vendor spawn; artifacts written. |
| F6 | `PASS` | `handoff inspect --json` and `handoff export --format json` exit 0 on a stored no-launch id. |
| F7 | `PASS` | Bare native id matching Claude and Codex fixtures exit **6**. |
| F8 | `PASS` | Non-TTY launch after warning acks exit **7** with interactive-terminal text; wall clock 1972–2097 ms; dest spawn not observed on this path. `--allow-warning` did not skip the refusal. `REINSTATE_ALLOW_NON_TTY_LAUNCH` unset. |

### Matrix G — performance

| ID | Samples | Median | p95 | Maximum | Ceiling | Result | Sanitized evidence |
| -- | ------- | ------ | --- | ------- | ------- | ------ | ------------------ |
| G1 | 1 tagged-source unit run | 15.758 ms | 15.758 ms | 15.758 ms | `<2,000 ms`; projection `≤98,304 bytes` | `PASS` | 46,541 bytes; 400 events |
| G2 | 1 warmup + 5 installed `--dry-run --json` | 904 ms | 914 ms | 914 ms | macOS max `8s` | `PASS` | all exit 0; frozen 200-turn source; policy balanced; estimated bytes 46,761 |
| G3 | 1 warmup + 20 installed `handoff list --limit 100 --json` | 9 ms | 10 ms | 10 ms | macOS p95 `2s` | `PASS` | dedicated home; 100 `--no-launch` setup created=100; list n=100 every sample; source fingerprint unchanged |

G2 source: 400 events. Ceilings treated as absolute. No >25% regression vs this-run G3 p95.

## 6. Optional source-only matrix — 8 rows

| ID | Vendor present | Result | Sanitized evidence |
| -- | -------------- | ------ | ------------------ |
| E1 | `no` | `NOT TESTED` | Gemini absent |
| E2 | `no` | `NOT TESTED` | Gemini absent |
| E3 | `no` | `NOT TESTED` | OpenCode absent; no isolation override |
| E4 | `no` | `NOT TESTED` | OpenCode absent; no isolation override |
| E5 | `yes` | `FAIL` | Grok 1.0.3 + tagged basic fixture; `--to claude --dry-run --json` exit **5** `environment preflight is blocked`. Did **not** exit 1 `unsupported agent "grok"` (RC2 busy-check). Did not complete with forced redaction / destination warning. |
| E6 | `yes` | `FAIL` | Same preflight block toward Codex; no `unsupported agent "grok"` busy-check. |
| E7 | `yes` | `PASS` | `--to grok` exit **2** usage: destination not in `{claude,codex}` |
| E8 | `yes` | `PASS` | `resume` grok id exit **5**: source-only in Phase 4 |

## 7. Architecture §14 closeout

| Definition-of-done assertion | Result | Evidence |
| ---------------------------- | ------ | -------- |
| All 27 packets and their tests are present in the tagged commit | `PASS` | WP-01–WP-27 named in tagged plans; `rein handoff` present |
| `make verify` is green on both mandatory platforms | `FAIL` | macOS `make verify` PASS; Windows not in this report |
| Claude → Codex and Codex → Claude work with source closed and no source API call | `FAIL` | A1–A2 |
| Every fidelity class has real report evidence and byte-stable goldens | `PASS` | B6 compaction fixture; goldens in `make verify` |
| Injection, secret, and bounded-read security gates leak nothing | `PASS` | D1–D4 counts; `make verify` adversarial tests |
| 200-turn projection is bounded and reported | `PASS` | B2 + G1 |
| Windows ↔ macOS uses canonical project IDs without source absolute paths | `FAIL` | C5 |
| Dry-run and executed structured-plan output are byte-identical | `PASS` | B1 dry vs no-launch after acks (real launch route not compared; A1 failed dest-ack) |
| Required Phase 4 docs, product-truth register, runbook, and dispatch are present | `PASS` | tagged tree: dispatch, template, `docs/handoff.md`, `docs/seo/product-truth-register.md` |
| Exit codes remain 0/1/2/3/5/6/7; no vendor-internal writes or capsule sync | `PASS` | observed 0/2/3/5/6/7 only; D7–D8 |

## 8. Findings and repository hygiene

### Release-blocking

1. Flagship A1/A2/A3/A5/A6/A7 did not complete a new destination session acknowledgement on this host (throwaway Claude not logged in; Codex PTY produced 87 bytes and no acknowledgement). Empty-dest **Plan** succeeds (RC3 flagship A); missing dest-ack after Plan is harness for that RC3 row, but A5 still requires a live restatement.
2. Installed Grok-sourced dry-run (E5/E6) exits 5 `environment preflight is blocked`. Busy-check no longer returns exit 1 `unsupported agent "grok"`. Optional installed-vendor failure is a candidate blocker.
3. C5 Windows os-roots fixture does not remap a recorded Windows workspace onto the live macOS checkout; preflight blocks.

### Non-blocking

- Host Claude is `2.1.228`, not `2.1.229`. Range includes 2.1.229 via C8 fail-closed on `2.1.230`.
- `get.reinstate.dev` did not resolve from this host; `reinstate.dev/install.sh` was the live bootstrap that matched the tagged bytes.
- OpenCode warning `agent_not_installed` on `sessions --json` is expected.

### Test-harness deviations and supersessions

- Isolated Claude print-mode session failed closed (not logged in); no operator `.claude` tree was read. Two jsonl files exist only under the throwaway `CLAUDE_CONFIG_DIR`.
- A1 used Python PTY with `TERM=xterm-256color`; destination TUI emitted 87 bytes; dest tree writes attributed to the vendor process; source hashes unchanged.
- `make verify` must be run without leaked `CODEX_HOME`/`CLAUDE_CONFIG_DIR` and without parallel load; a contaminated/load run classified in-range dest Codex as `UNTESTED`. Clean `env -i` `make verify` is the recorded gate.
- `goreleaser build --snapshot --single-target` left the worktree clean.
- No rc.1, rc.2, or rc.3 corpus reused.
- F4 unacked `--no-launch` now exit 0 (lineage recorded); unacked **launch** still exit 7. This matches RC3 G3 (“unacknowledged warnings must not zero the list”).

Record the report-only diff, privacy scan result, merge-base with `TEST_COMMIT`,
branch tip, and every superseded row. Never amend or force-push evidence already
shared with the coordinator.

## RC1 regression re-verification (R1–R6)

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| R1 | `PASS` | Throwaway Claude dir has no `version` file. In-range dry-run exit 0, not UNTESTED. Off-PATH source dry-run exit 0 (layout-only SUPPORTED). |
| R2 | `PASS` | Absolute-paths fixture `--no-launch` exit 0; `${REPO}` tokens=19 and `${EXTERNAL:}` tokens=8 in artifacts; operator-home path count 0; no absolute-path abort. |
| R3 | `PASS` | Dirty tree `changed_files` nonempty (count=3); not `(none)`. |
| R4 | `PASS` | Hanging `--version` shim (grandchild `sleep` without exec) classified `UNTESTED`, exit **5**, Compatibility, wall 10247 ms (inside 25 s). Not Runtime/absent. |
| R5 | `PASS` | Slash fixture first user `/init`; `--no-launch --json` exit 0; `/init` reached stdout; no absolute-path abort. |
| R6 | `PASS` | Installed Claude `2.1.228` handoff exit 0 (SUPPORTED). Fake `2.1.230` exit 5 fail-closed. Host did not auto-update (before=after `2.1.228` / `0.147.0`). |

## RC2 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| C4 | `PASS` | Wrong-repo cwd exit 5 `different repository`. |
| F8 | `PASS` | Non-TTY fail-closed exit 7 before dest spawn. |
| E5/E6 busy-check | `PASS` | No exit 1 `unsupported agent "grok"`. Rows themselves `FAIL` on preflight block. |
| R4 classification | `PASS` | Timed-out/hanging probe is Compatibility `UNTESTED` exit 5, not Runtime. |
| D9 | `PASS` | `--no-redact` known; Grok refused exit 2. |
| List isolation | `PASS` | Isolated `rein list` hid decoy HOME session. |
| B3 | `PASS` | Checkpoint `projection_events=0`. |
| B6 | `PASS` | All five fidelity classes on compaction fixture. |
| C6/C7 | `PASS` | Missing MCP/skill warning IDs; unacked launch exit 7. |

## RC3 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| Flagship A empty dest | `PASS` | Empty dest `CODEX_HOME` (no `sessions/` or `projects/`); dest Plan `--dry-run --json` exit 0; dest not `UNTESTED`. Dest-ack still missing (harness; see A1). |
| R4 hang | `PASS` | See R4; 10247 ms; exit 5 `UNTESTED`. |
| Windows `make verify` | `N/A` | Owned by the Windows device report. macOS `make verify` PASS. |
| G3 `--no-launch` list | `PASS` | created=100; `handoff list --limit 100 --json` n=100; unacked launch still exit 7. |
| B6 summarized | `PASS` | Compaction `fidelity.json` includes `summarized`; not rewritten to `referenced`. |

## 9. Required terminated device block

Required counts must sum to 44 and optional counts to 8. This block occurs
exactly once and remains the last report content until reconciliation.

```text
PHASE4-DEVICE-REPORT-V1
device=macos-arm64
test_tag=v0.4.0-rc.4
test_commit=08d8b0ee469472e7382f12feb85914c75a9bdee0
installed_binary_sha256=e53ef41edbd603b31815bf3e5ad8e9f7fee24c2624a9b0369ad97316dd356e01
required_pass=37
required_partial=0
required_fail=7
required_not_tested=0
optional_pass=2
optional_fail=2
optional_not_tested=4
artifact_chain=PASS
isolation_privacy=PASS
flagship_directions=FAIL
fidelity_policy=PASS
workspace_capability=FAIL
security=PASS
cli_contract=PASS
performance=PASS
phase1_phase2_phase3_regression=PASS
release_blocking_findings=3
product_files_changed=0
secrets_transcripts_or_capsules_committed=false
END-PHASE4-DEVICE-REPORT-V1
```
