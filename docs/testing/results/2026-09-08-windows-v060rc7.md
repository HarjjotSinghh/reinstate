# `v0.6.0-rc.7` tagged-artifact native Windows acceptance

`PHASE5-DEVICE-REPORT-V1`

This is the assembled device report for the **published, signed GitHub
prerelease `v0.6.0-rc.7`**, commit `21e9a9a356df1181601e2da02bfae03b28380cc3`
(release workflow run `34250867105`). It satisfies
[`../phase-5-universal-agent-coverage-acceptance.md`](../phase-5-universal-agent-coverage-acceptance.md)
as specialized by [`../v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md)
and [`../v0.6.0-rc.7-agent-verification-prompts.md`](../v0.6.0-rc.7-agent-verification-prompts.md).
It merges five executors' part files
(`2026-09-08-windows-v060rc7-part-{a,b,c,d,e}.md`, all against this same
tag) into the one cumulative report the dispatch requires, plus an
assembler's own independent install and matrix-generation check (§0). The
five part files are removed from the tree in the same commit as this file;
their content is preserved here.

**A first attempt at this run on the same artifact (kept as branch
`v060/rc7-tagged-attempt1`, head `df1996a9`) was inconclusive: executor A
had run an older `rein` from the user `PATH`, executor B had no console for
`conptydriver`, and executor E was ended during its `H7` wait; parts C and D
of that attempt passed cleanly and are reused unchanged.**

## Verdict

**Verification round 1 correction (2026-09-09).** An independent
verification pass rejected this report's original submission over
`MatrixH:H7`'s own disposition text: the original text below (unchanged,
kept for the record) claimed the run's 40-minute go-signal wait "matches
the dispatch's own anticipated outcome for a wait window shorter than 150
minutes" — but the contract says the opposite, in both
[`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md) ("Lab
isolation for `MatrixH:H7`") and
[`v0.6.0-rc.7-agent-verification-prompts.md`](../v0.6.0-rc.7-agent-verification-prompts.md):
"a shorter poll window is not a basis for `PARTIAL`," verbatim, in both
documents. That was a real self-contradiction, not a defensible reading.
While redoing the row for this correction, the operator go-signal file was
found already present — unlike the original 40-minute window, which never
saw it — and the full elevated `daemon install`/`status`/`stop`/`start`/
`uninstall` round trip was carried out and reached a clean `PASS` under the
refined per-file-listing rule: a genuine result, not a rewritten
characterization of the same `PARTIAL`. **`MatrixH:H7` is `PASS`
throughout this report from this point forward.** The original `PARTIAL`
evidence and its flawed rationale are left verbatim in §14 for the record;
the full recheck, plus a fix for a minor `H5` evidence-format deviation the
same verification pass flagged, is in the new §24. Nothing else in this
report changed — every other row's original evidence, and CLI row 13's
`FAIL`, stand exactly as originally submitted.

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Matrix generated by:** `rein doctor --agents --acceptance-matrix --json`
  (re-run independently by the assembler against a fresh install; see §0)
- **Row accounting:** `216` rows total (`178` Phase 5 generated matrix +
  `22` CLI experience + `16` Hop parity journeys) — `1` row is
  `N/A (definitional)` (`opencode:D4`) — **`215` required rows**.
- **Host-credential rows:** `0`. No row this run is recorded
  `NOT TESTED (host credential)`.
- **Required counts (corrected, verification round 1 — see §24):**
  `214 PASS / 0 PARTIAL / 1 FAIL / 0 NOT TESTED` of `215` required rows.
  Originally recorded `213 PASS / 1 PARTIAL / 1 FAIL / 0 NOT TESTED`;
  `MatrixH:H7` moved `PARTIAL → PASS`, nothing else changed.
- **Release-blocking findings:** `1` required row is not `PASS` and is
  not excused by either disposition in
  [`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict):
  CLI row `13` (`FAIL`, a new finding). See §20.

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required
result is `FAIL`. Because 1 of the 215 required rows is not `PASS` and is
not covered by an applicable disposition, the device verdict is `FAIL` and
this report does not authorize stable `v0.6.0`. No product code was changed
by this assembly or by verification round 1; every result below is
transcribed from the five part files' own evidence, plus the round-1
recheck evidence added in §24.

**This candidate's own targeted fix — the widened Codex range
(`0.133.0`–`0.153.4`) — is confirmed working, and the Codex account usage
limit that blocked `v0.6.0-rc.6`'s tagged run had already reset before this
run started.** `MatrixG:G1` (both the Claude and the Codex half) and
`codex:E1`–`E3` are all `PASS` this run, on real, completed conversational
turns against the live `0.153.4` binary, with no wait-and-retry cycle
needed. `grok:E1`–`E3` are also `PASS` again, via the executor-driven path
(the maintainer console fallback was not needed). **All four of
`v0.6.0-rc.6`'s carried release-blocking rows are cleared this run** — see
§18.

**This run surfaces one new, genuinely blocking regression:**

- **CLI row 13 is `FAIL` — a new finding, not carried from
  `v0.6.0-rc.6`.** The interactive switcher's default `ScopeAll` scope
  resolves at least one visible session row's readiness glyph to the wrong
  value (`○ Blocked` standing in for `●`/`◐`) in 11 of 15 independent
  launches (73%), using the exact `v0.6.0-rc.4`/`rc.6`-documented method,
  non-self-correcting within a launch's lifetime once wrong. `ScopeProject`
  is unaffected. This candidate's dispatch explicitly expected row 13 to
  `PASS` again unchanged (nothing in `v0.6.0-rc.7` touches the switcher or
  the readiness prober), so this is scored as a genuinely new regression,
  not an expected carried result. Source-level pointer:
  `internal/tui/readiness/prober.go`'s own doc comment on
  `maxConcurrentProbes` already names this exact process-creation-stampede
  failure shape; it is not eliminated on this shared, multi-executor host.
  See §13.

**`MatrixH:H7` was originally recorded `PARTIAL` (operator/harness
availability) here; it is `PASS` as of verification round 1 (§24).** The
original run's own 40-minute go-signal wait (shorter than the contract's
150-minute figure) never saw the operator go-signal file, so the elevated
round trip was correctly never attempted that run — but the original
report's own claim that this "matches the dispatch's own anticipated
outcome for a wait window shorter than 150 minutes" was wrong: the
contract's text says a shorter window is *not* a basis for `PARTIAL`. On
recheck, the go-signal file was already present, and the full elevated
`daemon install`/`status`/`stop`/`start`/`uninstall` round trip ran
cleanly to a `PASS` under the refined per-file-listing rule. See §14
(`H7`, original evidence) and §24 (recheck evidence and the correction
itself).

## 0. Assembler verification

Performed independently of, and in addition to, all five part files, in the
assembler's own fresh directory
(`D:\ReinstateAcceptanceProjects\v060-rc7-assembler\`), never touching any
part executor's own lab directories or the shared worktree's tracked files
before this commit.

1. **Checksum.** `sha256sum` of the coordinator's staged
   `reinstate_0.6.0-rc.7_windows_amd64.zip` matched `checksums.txt`
   (`865ad8e8f65fb3f543d2e2670b5d7c82b0d4878513781e04868fc7303e183fcf`)
   independently.
2. **Install.** Unzipped into a fresh directory
   (`D:\ReinstateAcceptanceProjects\v060-rc7-assembler\install\`), never a
   shared or developer binary. `rein.exe`/`reinstate.exe` `sha256sum`: both
   `ab73dd7c6817da2374a9c72673c62c63fb724d7412fa464d5dc56d381d5ea349`,
   matching `checksums.txt`'s `reinstate_0.6.0-rc.7_windows_amd64.exe`
   entry exactly, confirmed identical to each other.
3. **Version.** `rein version --json`:
   `{"commit":"21e9a9a356df1181601e2da02bfae03b28380cc3","date":"2026-09-08T16:26:36Z","name":"reinstate","version":"0.6.0-rc.7"}`
   — matches every part file's own artifact-identity table exactly.
4. **Required row count.** `rein doctor --agents --acceptance-matrix --json`
   against this fresh install: top-level `"row_count": 178`,
   `"core_row_count": 33` (`A:10, B:9, G:8, H:6`), 18 agents at tiers
   `T0×7, T1×4, T2×2, T4×2, T5×3` (`claude, codex, opencode` at T5;
   `grok, qwen` at T4; `gemini, kimi` at T2; `cline, copilot, cursor, pi` at
   T1; `aider, amp, antigravity, minimax-code, openhands, roo, zcode` at
   T0) — matches the dispatch's held census exactly (no tier promotion this
   candidate).
5. **Row-set reconciliation.** Programmatically read the full set of 178
   expected row identifiers from that JSON (33 core rows + each agent's own
   row list: T0×`F1,F2`=14, T1×`C1`–`C6`=24, T2×`C1`–`C6`/`D1`–`D5`=22,
   T4/T5×`C1`–`C6`/`D1`–`D5`/`E1`–`E6`=85) and compared it against the
   union of every row id assigned to part A (core Matrix A/B/G/H, Matrix F
   for all 7 T0 agents, Matrix C for the 4 T1 agents — 71 Phase-5 rows) and
   part B (Matrix C/D for `gemini`/`kimi`, Matrix C/D/E for `claude`/
   `codex`/`opencode`/`grok`/`qwen` — 107 Phase-5 rows): **the two sets are
   set-equal, 178 elements each, zero overlap, zero gap.** Every required
   Phase 5 row appears in exactly one part file.
6. **Part self-report cross-check.** Recounted every part's own row table
   cell-by-cell against its own committed evidence:
   - Part A: 8 gates (not part of the 216) + 71 Phase-5 rows (Matrix A 10 +
     Matrix B 9 + Matrix F 14 (7 T0 agents × F1/F2) + Matrix G 8 + Matrix H
     6 + Matrix C 24 (4 T1 agents × C1–C6)) = **79 rows, all PASS** —
     matches executor A's own structured JSON report (`pass:79, fail:0,
     partial:0, not_tested:0`) exactly. **Note:** part-a.md's own Verdict
     block header states `"100 PASS / 0 PARTIAL / 0 FAIL / 0 NOT TESTED"`
     and refers to "the full row table in §17," but the file has no §17
     (it ends at §14) and its own itemized §14 row table states the
     correct figure, `79`. This is an internal inconsistency in the
     part-a.md file, not a scoring discrepancy — the reconciled figure is
     `79`, which matches both the itemized table and the structured JSON
     independently. Recorded as a harness/report defect in §21.
   - Part B: 107 Phase-5 rows (`claude`/`codex`/`opencode`/`grok`/`qwen` ×
     17 each + `gemini`/`kimi` × 11 each), of which `opencode:D4` is
     `N/A (definitional)` — **106 PASS, 1 N/A** — matches its own "Counts"
     section exactly, plus the T5 push/pull round trip (6/6 PASS, beyond
     the 216).
   - Part C: 22 rows, **21 PASS, 1 FAIL** (row 13) — matches its own
     Verdict ("21 PASS / 1 FAIL … of 22 required rows").
   - Part D: 11 rows, **11 PASS** — matches ("11/11 required rows PASS").
   - Part E: 5 rows, **4 PASS, 1 PARTIAL** (`H7`) — matches its own
     Verdict ("4 PASS (`H6`, `H6b`, `H8`, `H8b`), 1 PARTIAL (`H7`…)").

   **No correction was needed to any part's counts this run**, beyond
   noting part A's own internal header/table mismatch above (both figures
   the header and the table point at ultimately agree with the independent
   JSON count, `79`).
7. **Total reconciliation.** `71` (part A) + `107` (part B, including
   `opencode:D4`) + `22` (part C) + `11` (part D) + `5` (part E) =
   **`216`**, matching the dispatch's total row count exactly; minus the
   one `N/A (definitional)` row = **`215`** required. PASS sum (as
   originally assembled): `71` (A) + `106` (B) + `21` (C) + `11` (D) + `4`
   (E) = **`213`**. FAIL: `1` (part C, row 13). PARTIAL: `1` (part E,
   `H7`). NOT TESTED: `0`. `213 + 1 + 1 + 0 = 215`. **Corrected in
   verification round 1 (§24): `MatrixH:H7` moved `PARTIAL → PASS` on
   recheck, so the current total is `214` PASS / `0` PARTIAL / `1` FAIL /
   `0` NOT TESTED — `214 + 0 + 1 + 0 = 215`.**
8. **Disposition-rule check.** Applied
   [`v0.6.0-windows-acceptance.md`'s "Dispositions that do not block the
   device verdict"](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict)
   rule mechanically to every non-`PASS` required row this run recorded.
   - `opencode:D4` — `N/A (definitional)`, unchanged reason (OpenCode's
     SQLite-only store has no JSONL record boundary). Excluded from both
     the denominator and the numerator, as required. Not a disposition on
     a required row.
   - CLI row 13 — recorded `FAIL`. Does not match either named disposition
     (not a `NOT TESTED (host credential)` row for an optional agent — the
     interactive switcher is not agent-scoped at all; not
     `NOT TESTED (version drift)` — nothing about this candidate's own
     vendor version ranges is implicated). The contract's "Everything
     else" clause applies: `FAIL` fails a required row outright.
     **Blocks.**
   - `MatrixH:H7` — recorded `PARTIAL` in the original assembly, not
     `NOT TESTED`. `PARTIAL` is not one of the two named dispositions at
     all; the contract's "Everything else" clause applies: `PARTIAL` does
     not pass a required row. **Blocked, at the time of this original
     assembly.** **Superseded in verification round 1 (§24): the row is
     `PASS` on recheck and no longer blocks.**
   - **Zero rows this run qualify for an exemption** beyond `opencode:D4`'s
     definitional exclusion. (As of verification round 1, `CLI row 13` is
     the only required row still blocking the device verdict.)
9. **Independent signed-tag and ancestry re-check.** `gh release view
   v0.6.0-rc.7 --json …` (read-only): `isDraft:false, isPrerelease:true,
   publishedAt:2026-09-08T16:32:29Z, tagName:v0.6.0-rc.7,
   targetCommitish:main`. `git verify-tag v0.6.0-rc.7`: `Good "git"
   signature for HarjjotSinghh@gmail.com with ED25519 key
   SHA256:0P4/a2ZBw25hhf0mCN5s2NsUlDiqRqshv8vGNgMPHjU` — matches
   `.github/allowed_signers`. `git rev-list -n 1 v0.6.0-rc.7` peels to
   `21e9a9a356df1181601e2da02bfae03b28380cc3`; `git merge-base
   --is-ancestor 21e9a9a3… origin/main` confirms it is an ancestor of
   `origin/main`.
10. **Cleanup.** The assembler's own install and matrix output under
    `D:\ReinstateAcceptanceProjects\v060-rc7-assembler\` are left in place
    for the coordinator's own records (not deleted, matching the
    `v0.6.0-rc.6` assembler's own precedent); nothing from that directory
    is referenced by path elsewhere in this report beyond §0 itself.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-09-08` (span across all five parts; each part's own timestamp is in its section below); assembler verification `2026-09-08` |
| Device | `windows-amd64` |
| OS/version/build | Windows 11 Pro, `10.0.26200`, native `windows/amd64`, never WSL |
| CPU architecture/native process | `amd64`, native process (no emulation) |
| Filesystem | NTFS |
| Tested tag | `v0.6.0-rc.7` |
| Tested full commit | `21e9a9a356df1181601e2da02bfae03b28380cc3` |
| Installed binary SHA-256 | `ab73dd7c6817da2374a9c72673c62c63fb724d7412fa464d5dc56d381d5ea349` |
| Installed version JSON | `{"commit":"21e9a9a356df1181601e2da02bfae03b28380cc3","date":"2026-09-08T16:26:36Z","name":"reinstate","version":"0.6.0-rc.7"}` |
| Catalog agent count | `18` |
| Catalog tier census | `T0: 7, T1: 4, T2: 2, T4: 2, T5: 3` (unchanged from `v0.6.0-rc.6`) |
| Generated required row count | `178` (`rein doctor --agents --acceptance-matrix --json`, independently reproduced by the assembler in §0 and by part A) |
| Git version | `2.52.0.windows.1` |
| Go version/toolchain | `go1.25.13` (pinned via `GOTOOLCHAIN`; host default `go1.26.1`) |
| Report branch | `v060/rc7-tagged` |
| Device-report commit | filled in by the commit that lands this file |
| Draft report PR | NOT CREATED (worktree report, per this run's dispatch — no PR required) |

### Bootstrap deviation (applies to every part below except executor A)

Per this run's ground rules, only executor A installs from the live
`https://reinstate.dev/install.ps1` bootstrap and records that as the
artifact identity; every other executor (B, C, D, E) and the assembler
installed from the coordinator's pre-verified, checksummed draft directory
(`…/scratchpad/rc7-draft/`) instead — the assigned methodology, not an
unplanned deviation. Executor A's own bootstrap check (part A): the live
bootstrap was downloaded fresh with `Invoke-WebRequest -OutFile` into
`D:/ReinstateAcceptanceProjects/v060-rc7-a/bootstrap/`, its pinned-version
line reads exactly `$Version = "v0.6.0-rc.7"`, and running it
(`INSTALL_DIR=D:/ReinstateAcceptanceProjects/v060-rc7-a/bootstrap/install`)
printed `Installed reinstate v0.6.0-rc.7 -> ...\reinstate.exe` and
`Installed rein alias -> ...\rein.exe` — both binaries byte-identical
(SHA-256 `ab73dd7c6817da2374a9c72673c62c63fb724d7412fa464d5dc56d381d5ea349`)
to the ones extracted from the checksummed archive. **No deviation in
substance** between the live bootstrap route and the release-commit
candidate.

### Artifact identity (common to all parts)

| Field | Value |
| ----- | ----- |
| Tag | `v0.6.0-rc.7`, published GitHub prerelease, `isPrerelease: true`, `isDraft: false`, published `2026-09-08T16:32:29Z`, targeting `main`, signed tag verified against `.github/allowed_signers` (`git verify-tag`: Good signature, ED25519 key `SHA256:0P4/a2ZBw25hhf0mCN5s2NsUlDiqRqshv8vGNgMPHjU`) — re-confirmed independently by the assembler in §0.9 |
| Full commit | `21e9a9a356df1181601e2da02bfae03b28380cc3` (confirmed an ancestor of `origin/main`, §0.9) |
| Release workflow run | `34250867105` |
| Windows archive | `reinstate_0.6.0-rc.7_windows_amd64.zip` |
| Archive SHA-256 | `865ad8e8f65fb3f543d2e2670b5d7c82b0d4878513781e04868fc7303e183fcf` (matches `checksums.txt`; re-verified independently by every part and the assembler) |
| Installed binary SHA-256 | `ab73dd7c6817da2374a9c72673c62c63fb724d7412fa464d5dc56d381d5ea349` — `rein.exe`/`reinstate.exe` byte-identical in every install, both routes |
| `rein version --json` | `{"commit":"21e9a9a356df1181601e2da02bfae03b28380cc3","date":"2026-09-08T16:26:36Z","name":"reinstate","version":"0.6.0-rc.7"}` |
| Previous-release comparison binary (rows 2, 22, `G7`) | `reinstate_0.5.1_windows_amd64.zip`, checksum-verified |

## 2. Agent inventory on this device

| Agent key | Installed | Vendor version | Sessions available | Distinct projects | Declared tier | Tier testable here |
| --------- | --------- | -------------- | ------------------- | ----------------- | -------------- | -------------------- |
| `claude` | YES | `2.1.263` | 81 real (live config) | 36 | T5 | yes — resume/fork/handoff, real transcripts recalled |
| `codex` | YES | `0.153.4` (default; in the widened `0.133.0`–`0.153.4` range) | 100 real (host root) | 25 | T5 | yes — full live-turn coverage this run, no usage-limit hit, no version drift |
| `opencode` | YES | `1.18.29` (within `1.18.21`–`1.18.29`) | 23–24 real | 9 | T5 | yes — full re-test target confirmed, `E5`/`E6` `PASS` |
| `grok` | YES | `1.0.13` (isolated `GROK_HOME`, within `1.0.5`–`1.0.13`) | real, isolated | 2 | T4 | yes — `E1`/`E2`/`E3` `PASS` this run via the executor-driven path; maintainer console fallback not needed |
| `qwen` | YES | `0.21.12` (within `0.21.12`–`0.23.0`) | real, isolated | 2 | T4 | yes — every row `PASS` |
| `gemini` | YES (live default home) | `0.53.0` | 31 real | 19 | T2 | yes |
| `kimi` | YES | `0.36.1` | 7 real | 5 | T2 | yes |
| `pi` | YES | (host) | 10 real | 9 | T1 | yes |
| `cursor` | YES (`cursor-agent` itself still broken on this host) | (host) | 3 real | 3 | T1 | yes — read-only host-store discovery, per the T1 exception |
| `copilot` | YES | `1.0.80`-family | 4 real | 4 | T1 | yes |
| `cline` | YES | (host) | 12 real | 8 | T1 | yes |
| `aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode` | ABSENT | — | — | — | T0 | yes (refusal/no-capability rows only) |

## 3. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------- |
| Annotated tag signature verifies against `.github/allowed_signers` | PASS | Coordinator pre-run; independently re-confirmed by the assembler (§0.9): `git verify-tag` Good signature, `SHA256:0P4/a2ZBw25hhf0mCN5s2NsUlDiqRqshv8vGNgMPHjU` |
| Tag peels to the recorded full commit on `origin/main` | PASS | `21e9a9a356df1181601e2da02bfae03b28380cc3`; independently re-confirmed ancestor of `origin/main` (§0.9) |
| Published release is non-draft, prerelease, and tied to the tag | PASS | `gh release view` (read-only), independently re-run by the assembler: `isDraft:false, isPrerelease:true`, published `2026-09-08T16:32:29Z` |
| Exact expected release asset set is present | PASS | `verify-release.ps1`/`test-install.ps1` (Gate A6/A7, part A) |
| Checksums, GitHub API digests, and attestations match the tag and commit | PASS | Coordinator pre-verified (`checksums.txt` match, `gh attestation verify` on the Windows archive); independently re-verified by every part and the assembler |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | PASS | `verify-release.ps1`/`test-install.ps1` (Gate A6/A7) |
| Live bootstrap is byte-identical to the tested commit and pins only this candidate | PASS | Executor A: pinned version line `v0.6.0-rc.7`, resulting binary byte-identical to the checksummed archive |
| Bootstrap installer digest matches the tagged canonical installer | PASS | Executor A |
| Fresh isolated install did not replace a user binary or persist PATH changes | PASS | Every part and the assembler installed into its own fresh directory |
| `rein` and `reinstate` have identical verified bytes and report the candidate | PASS | SHA-256 `ab73dd7c6817da2374a9c72673c62c63fb724d7412fa464d5dc56d381d5ea349` throughout every install, including the assembler's own |
| Installed binary reports the literal full tested commit | PASS | `rein version --json` matches `21e9a9a356df1181601e2da02bfae03b28380cc3` in every install |

## 4. Automated gates (Section A of the dispatch; not part of the 216)

Source: part A, run once from the worktree per the ground rules
(`-p 4`).

| # | Gate | Result | Evidence |
| - | ---- | ------ | -------- |
| A1 | Format, vet, tidy | PASS | `gofmt -l .`, `go vet ./...`, `go mod tidy -diff` all empty, exit 0 |
| A2 | Unit suite | PASS | `CGO_ENABLED=0 go test ./... -count=1 -p 4`, every package `ok`, exit 0 |
| A3 | Race suite | PASS | `CGO_ENABLED=1 go test -race ./internal/... -count=1 -p 4`, every package `ok`. Two of three attempts failed on transient host memory/linker exhaustion (`ERROR_COMMIT_LIMIT`/`collect2.exe` missing — different packages each time, never the same set twice), attributable to real concurrent memory pressure on this shared, multi-executor host (`Memory Compression` alone ~8.5 GB, multiple `comet`/`claude`/`Orca` processes); no data race was ever reported by ThreadSanitizer in any attempt. A third, isolated run completed clean, exit 0, 0 `FAIL`/`DATA RACE` lines |
| A4 | Lint and vuln | PASS | `golangci-lint run` (v2.11.4): `0 issues` once scoped to the 82 `git ls-files`-tracked Go directories; `govulncheck` (v1.6.0): `0` reachable vulnerabilities. First attempt failed on a single `errcheck` finding inside an **untracked** `scratchtool_h3/main.go` that appeared in this executor's exclusively-assigned worktree mid-session — see §21 |
| A5 | Doc gate | PASS | `go test ./internal/doctest/... -count=1` `ok`; `scripts/check-docs.ps1` exit 0 |
| A6 | Installers (dist dir) | PASS | `scripts/verify-release.ps1 -DistDir rc7-draft` exit 0 |
| A7 | Install script | PASS | `scripts/test-install.ps1 -DistDir rc7-draft` exit 0, plus `ok internal/doctest 1.278s` |
| A8 | Cross-OS build | PASS | `GOOS=darwin GOARCH=amd64 go build ./...`; `GOOS=linux GOARCH=amd64 go build ./...`, both exit 0 |

**8/8 PASS.**

## 5. Matrix A — Catalog integrity (10/10 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| A1 | Descriptors validate at start; no duplicate/empty keys | PASS | `doctor --agents --json` lists 18 unique, non-empty `key` values |
| A2 | `doctor --agents` lists every agent incl. every T0 | PASS | all 18 keys present, including all 7 T0 agents (`aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode`) |
| A3 | Every T0 agent shows an enumerated reason | PASS | closed 3-value set observed: `layout_unverified`, `server_backed`, `desktop_only` |
| A4 | Declared tier and capability constructors agree | PASS | `resume`/`fork` refuse below T3 with exit `5`+reason, accept at T3+ (session-not-found, exit `2`); real `TestShippedAgentsRegisterAtDeclaredTiers`/`TestMustRegisterAcceptsConstructorsAtDeclaredTier` passed in A2 |
| A5 | Every declared evidence path exists in the tagged tree | PASS | `TestCatalogEvidencePathsExist`/`TestShippedEvidenceIsComplete`, via the green `internal/doctest`/`internal/agents/conformance` run |
| A6 | `sessions --agent` accepts exactly T1+ keys, else exit 2 | PASS | all 11 T1+ keys accepted (exit 0); all 7 T0 keys rejected, `{"code":"usage",...}` exit 2 naming the 11-key list |
| A7 | `resume` accepts exactly T3+ keys, else exit 5 with reason | PASS | `claude`/`codex`/`grok`/`qwen`/`opencode` (T3+) accepted the key form (proceeded to "session not found", exit 2); `cline`/`copilot`/`cursor` (T1) and `gemini`/`kimi` (T2) refused, exit 5, tier-named reason |
| A8 | Agent ordering deterministic across runs | PASS | two `sessions --agent all --json` calls diffed byte-identical (0 lines) |
| A9 | Broken evidence path fails conformance | PASS | real `TestBrokenEvidencePathFails`/`TestBrokenEvidencePathIsCaught`, via the green suite |
| A10 | No scan writes/renames/locks any file under an agent root | PASS | real filesystem audit: copied the live `GROK_HOME` root (8,311 files) to an isolated dir, ran a full `doctor --agents --json` probe **and** `sessions --agent grok --json` against the copy, re-hashed all 8,311 files and re-listed the tree — SHA-256 set and directory listing byte-identical before/after (0 diff lines) |

## 6. Matrix B — Probe and redaction (9/9 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| B1 | Output conforms to `AGENT-PROBE-V1` | PASS | `"schema": "AGENT-PROBE-V1"` present |
| B2 | No absolute path, home name, or username | PASS | case-insensitive scan of a full `doctor --agents --json` (all 11 real installed agents) for the host username and `C:\Users`/`D:\Projects` — 0 matches |
| B3 | No JSON values from session files, keys only | PASS | `first_line_keys`/`name_shapes` fields contain only key names and shape descriptors, never a value; one benign observation (a Claude source file uses hex-color strings as its own JSON object keys, so those literal keys surface looking like values — still literally "keys only") noted in §21 |
| B4 | Names shape-normalized | PASS | full-document regex scan for UUID and 40-hex patterns across every real installed agent's probe output: 0 matches; `gemini` specifically re-checked per this run's requirement — `resolved_root`, `name_shapes` all wildcarded/shaped, `version_raw` `0.53.0` |
| B5 | No excluded subtree present | PASS | full-document case-insensitive scan for `credential`, `keychain`, `token.json`, `apikey`, `secret`, `.git-credentials`, `password` — 0 matches |
| B6 | Absent agent reported without error | PASS | all 7 T0 agents: `resolved_root: null`, `executable_on_path: false`, empty `tree`/`name_shapes`, exit 0, no error |
| B7 | Empty root distinguishable from absent root | PASS | `CLINE_DATA_DIR`/`KIMI_CODE_HOME` each pointed once at a real empty directory and once at a real, nonexistent path — the `candidate_roots` entry differs exactly in `exists` (`true`/`false`), every other field constant |
| B8 | Wrapper scripts match binary output | PASS | with the tagged install first on `PATH`, `.sh`/`.ps1` wrappers produced output differing from a direct `doctor --agents --json` call only in `generated_at` |
| B9 | Secret scanner passes over committed probe artifacts | PASS | real `internal/secretscan`/`internal/fixture` tests passed in A2 |

## 7. Matrix G — No regression (8/8 PASS)

Source: part A. `G1`'s Codex half is fully exercised this run, unlike
`v0.6.0-rc.6`'s `PARTIAL`.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| G1 | Claude/Codex native resume and fork unchanged | PASS | **Claude half:** real isolated throwaway project against the host's live `CLAUDE_CONFIG_DIR`; planted token via `claude -p`; `rein search --agent claude --json` found it; `rein resume claude:<id> --dry-run --json` named `claude --resume <id>`, `agent.version status=match actual=2.1.263`; real `claude --resume <id> -p "..."` recalled the token verbatim at the same session id; real fork produced a **distinct** id that also recalled the token. **Codex half:** same method against the real, in-range `0.153.4` binary — no usage-limit refusal was hit on any attempt this run (first attempt succeeded, `17:08:26Z`–`17:09:45Z`); planted token via `codex exec`; `rein search --agent codex --json` found it; `rein resume codex:<id> --dry-run --json` named `codex resume <id>`, `agent.version status=match actual=0.153.4`; real `codex exec resume <id> "..."` recalled the token verbatim at the same id; real `codex exec fork <id> "..."` produced a **distinct** id that recalled the token; `rein sessions --agent codex --json` catalog reflects both ids |
| G2 | Both structured-handoff directions complete | PASS | real (non-dry-run, `--no-launch`) `rein handoff claude:<id> --to codex --json` exit 0, capsule written; real `rein handoff codex:<id> --to claude --json` exit 0, capsule written, new destination `session_id` planned |
| G3 | Gemini and Grok remain handoff sources and remain read-only (OpenCode has since graduated to T5) | PASS | a real Grok session and a real Gemini session (host's live vendor roots) both passed a real `rein handoff <agent>:<id> --to claude --dry-run --json` (source recognized, capsule planned); "remains read-only" independently confirmed for Grok by the A10 no-write filesystem audit above |
| G4 | Push/pull carry sessions for exactly the sync-capable agents (Claude Code, Codex CLI, OpenCode) and no other agent's | PASS | real S3-compatible fake server + a real, isolated `rein init --yes` profile; `push --agent claude/codex/opencode --all --dry-run` each returned real matching session snapshots; `push --agent grok --all --dry-run` and `push --agent pi --all --dry-run` both returned `"no matching local sessions found"` despite both having real local sessions — sync-capability, not just presence, gates matching; a real (non-dry-run) push of one Claude session succeeded, `rein status --json` confirmed it decrypted correctly; `rein __complete push --agent ""` returned exactly `claude, codex, opencode` |
| G5 | Cross-OS path remapping unchanged | PASS | real `internal/pathmap` unit tests passed in A2/A3 |
| G6 | Exit-code semantics unchanged; no new codes | PASS | `internal/exitcode`'s 8 constants (`OK`=0 … `Safety`=7) introduced in the project's first commit, never modified since (`git log --follow -p`); also independently observed exit `0`/`2`/`5` behaving as documented across dozens of real commands this run |
| G7 | An index built by a prior release upgrades without data loss (this candidate: `v0.5.1`-built index) | PASS | checksum-verified `reinstate_0.5.1_windows_amd64.zip` built a real index under an isolated `REINSTATE_HOME` (`sessions --agent all --json` → 100 sessions, exit 0); the tagged rc.7 binary against the same home, no rebuild step: same 100 sessions, exit 0, no error, no data loss |
| G8 | Capsules remain excluded from sync | PASS | structural + observed: `push`/`pull` only accept `--agent <catalog-agent-key>`; capsules/handoffs are not a catalog agent; the real G4 push run above touched only agent session snapshots, never the handoffs directory |

## 8. Matrix H — CLI contract and performance (6/6 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| H1 | CLI reference matches shipped flags | PASS | real `TestCLIReferenceListsCatalogKeys`, via the green `internal/doctest` run |
| H2 | JSON shapes match documentation | PASS | real `TestObjectFormatExamplesMatchTheCode`, `TestKeyringExampleShowsEveryFieldTheFormatHas` |
| H3 | Full refresh within documented ceiling | PASS | `13.721s` | real cold `sessions --agent all --json` across all 11 installed real agents (~640+ real sessions), under the ~20–30s Windows ceiling |
| H4 | No-change refresh materially faster | PASS | `0.201s`/`0.205s` | two immediately-following warm re-runs, ≈68× faster than the cold run |
| H5 | One slow/unreachable source does not block others | PASS | `QWEN_HOME` pointed at an unrelated, large tree (`C:/Windows/System32`, no Qwen marker); `sessions --agent all --json` still completed in 3.8s, exit 0, all 10 other real agents present and correct, `qwen` correctly absent |
| H6 | Completion lists correct agent keys per command | PASS | `rein __complete sessions --agent ""` → exactly the 11 T1+ keys plus `all`; `rein __complete push/pull --agent ""` → exactly `claude, codex, opencode` |

## 9. Matrix F — Per T0 agent (14/14 PASS)

Source: part A. All 7 T0 agents are not installed on this host — a scope reduction, not a `FAIL`.

| Agent key | F1 (listed with reason) | F2 (no capability offered) |
| --------- | ------------------------ | ---------------------------- |
| `aider` | PASS — `t0_reason=layout_unverified` | PASS — `sessions`/`resume` refuse per contract |
| `amp` | PASS — `t0_reason=server_backed` | PASS — same shape |
| `antigravity` | PASS — `t0_reason=layout_unverified` | PASS — same shape |
| `minimax-code` | PASS — `t0_reason=layout_unverified` | PASS — same shape |
| `openhands` | PASS — `t0_reason=server_backed` | PASS — same shape |
| `roo` | PASS — `t0_reason=layout_unverified` | PASS — same shape |
| `zcode` | PASS — `t0_reason=desktop_only` | PASS — same shape |

Closed enumeration of `t0_reason` values observed this run:
`layout_unverified`, `server_backed`, `desktop_only`.

## 10. Matrix C — Per T1+ agent (66 rows: 66 PASS)

Source: part A (`cline`, `copilot`, `cursor`, `pi` — T1) and part B
(`claude`, `codex`, `opencode`, `grok`, `qwen`, `gemini`, `kimi` — T2+).

### T1 agents (part A; 24/24 PASS)

Per the ground rules: `cline`/`pi`/`copilot` `C3` created a fresh,
throwaway session through the vendor's own live CLI with a planted,
non-sensitive search token; `cursor` ran entirely read-only against the
host's real store (`cursor-agent` itself is broken on this host); `C6` for
every agent used a filesystem copy, never live data.

| # | `cline` | `copilot` | `cursor` | `pi` |
| - | ------- | --------- | -------- | ---- |
| C1 (≥2 sessions, ≥2 projects) | PASS — 12/8 | PASS — 4/4 | PASS — 3/3 | PASS — 10/9 |
| C2 (metadata matches vendor) | PASS — matches | PASS — `[5,5,5,26]` | PASS — `[4,3,3]` | PASS — deep cross-check: raw jsonl 5 lines, 2 message-type records, `rein` reported `2` |
| C3 (message-body search) | PASS — planted token, 1 match (liveness probed with a `PONG` reply first) | PASS — planted token, 1 match | PASS — word from a real session's `prompt_preview`, 1 match | PASS — created via `pi -p`, planted token found |
| C4 (bounded inspect, no body) | PASS — ~5.4 KB bounded record | PASS | PASS — `prompt_preview` ~160 chars | PASS — `prompt_preview` 70 chars |
| C5 (resume gate correct for tier) | PASS — exit 5, `read_only_reason` | PASS — same shape | PASS — same shape | PASS — same shape |
| C6 (corrupt/empty/absent degrade cleanly) | PASS — `tasks.db` truncated to half, WAL/SHM removed → exit 0, no panic, fell back to per-session files | PASS — `events.jsonl` truncated to ¾ → exit 0, `message_count` reduced correctly | PASS — SQLite `store.db` truncated to half → exit 0, `message_count` degraded to 0, no crash | PASS — `.jsonl` truncated to ¾, cutting the final record mid-record → exit 0, `message_count` correctly dropped to only the complete record |

### T2+ agents (part B; 42/42 PASS)

Real sessions, planted per-agent token in a message body (never a title,
turn-2 planted where a vendor's own title generation would otherwise carry
turn 1's literal text), `rein search` confirms match; `inspect --json`
shows a bounded `prompt_preview`; `resume --dry-run --json` shows the
tier-correct gate decision; a truncated/corrupt/empty/absent copy degrades
cleanly.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| C1 (≥2 sessions, ≥2 projects) | PASS — 81/36 | PASS — 100/25 | PASS — 23/9 | PASS — 2/2 | PASS — 2/2 | PASS — 31/19 | PASS — 7/5 |
| C2 (metadata matches vendor) | PASS — `message_count=2` | PASS — `message_count=3` | PASS — `message_count=4` | PASS — `message_count=13` | PASS — `message_count=4` | PASS — `message_count=2` | PASS — `message_count=2` |
| C3 (message-body search, never title) | PASS — token found; title = session id | PASS — token found; title = session id | PASS — token in turn 2; title "One-word greeting request" (token-free) | PASS — token found; title = session id | PASS — token in turn 2; title "Say hello in one word." (token-free) | PASS — token found; title = session id | PASS — token found; title = session id |
| C4 (bounded inspect, no full body) | PASS — 73 chars (whole short prompt fit under the bound) | PASS — 160 chars | PASS — 25 chars | PASS — 160 chars | PASS — 22 chars | PASS — 71 chars | PASS — 69 chars |
| C5 (resume gate correct for tier) | PASS — `confirmation_required` | PASS — `confirmation_required` | PASS — `confirmation_required` | PASS — `confirmation_required` | PASS — `confirmation_required` | PASS — refused, exit 5, read-only Phase 2 | PASS — refused, exit 5, read-only until native resume verified |
| C6 (corrupt/empty/absent degrade cleanly) | PASS — synthetic fixture (harness note below): `incomplete_trailing_record`, exit 0; empty/absent clean | PASS — truncated copy → `incomplete_trailing_record`, exit 0; empty/absent clean | PASS — SQLite copy truncated 50% → `session_read_failed`, exit 0; empty/absent clean | PASS — truncated `updates.jsonl` read cleanly, exit 0; absent clean | PASS — truncated chat file read cleanly, exit 0; absent clean | PASS — truncated copy → `incomplete_trailing_record`, exit 0; absent clean | PASS — truncated `wire.jsonl` read cleanly, exit 0; absent clean |

**Harness note (`claude:C6`/`claude:D4`, part B).** This worktree's own
`CLAUDE.md` ("never inspect the developer's real `~/.claude` tree… use
only `testdata/adapters/claude/` or temporary synthetic fixtures") was
enforced by this session's own permission classifier, which blocked a
direct filesystem copy of a directory under the host's live
`CLAUDE_CONFIG_DIR`. Both `claude:C6` and `claude:D4` were completed instead
against a hand-authored synthetic Claude Code JSONL fixture in a fully
isolated `CLAUDE_CONFIG_DIR` — the mechanism (truncation-boundary
degradation, corrupt-copy degradation) is still exercised and the row
still `PASS`es. No file under the live `CLAUDE_CONFIG_DIR` was read,
copied, or modified by this executor at any point; every other `claude:*`
row ran against the live config through `rein`'s own designed read path
(`search`, `inspect`, `sessions`, `resume`, `fork`, `handoff --dry-run`),
which the ground rules explicitly direct at the live config and which the
classifier did not block. See §21 and Questions.

**42/42 PASS.**

## 11. Matrix D — Per T2+ agent (35 rows: 34 PASS, 1 N/A definitional)

Source: part B. Mechanism: `rein handoff <key>:<id> --to <dest> --dry-run
--json` (D1–D3, D5); D4 used `--no-launch` against a copy of the session's
own raw store truncated to ~70% of its byte length, with the capsule's
`raw_source.byte_offset`/`artifact_sha256` independently recomputed
(`sha256sum` over the first `byte_offset` bytes) for every agent — every
recomputed hash matched the capsule's recorded hash exactly.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| D1 (capsule + fidelity report) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D2 (no invented content) | PASS — 15 components | PASS — 16 | PASS — 14 | PASS — 16 | PASS — 15 | PASS — 14 | PASS — 15 |
| D3 (unknowns referenced/omitted with reason) | PASS — 31 unrecognized records referenced, plus `baseline.unavailable`/capability warnings named | PASS — 11 unrecognized referenced, plus ~180 `handoff.capability.*` warnings named | PASS — 2 unrecognized referenced | PASS — `baseline.unavailable` only | PASS — `baseline.unavailable` only | PASS — `baseline.unavailable` only | PASS — `baseline.unavailable` only |
| D4 (truncation boundary, offset+hash, recomputed) | PASS — synthetic fixture (see harness note above), offset 819, hash `50f1d104…c8bc33e` matched | PASS — offset 72864, hash `8f31d2ee…cc98f7e78` matched | N/A (definitional) | PASS — `updates.jsonl`, offset 6785, hash `7485ae9f…503a0175` matched | PASS — offset 4402, hash `8e43697d…bbe98c6` matched | PASS — offset 1988, hash `9c7f7107…9f079b1c` matched | PASS — `agents/main/wire.jsonl`, offset 78874, hash `a6d5033d…17f6f91` matched |
| D5 (two runs byte-identical) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |

`opencode:D4` remains **N/A (definitional)**, unchanged reason since
`v0.6.0-rc.1`: OpenCode's SQLite-only store has no JSONL record boundary
this row's mechanism applies to. Excluded from the required count.

D5 evidence: for every agent, two consecutive `handoff … --dry-run --json`
calls against the identical source session, with `handoff_id`,
`lineage_root`, and `planned_files` stripped before comparison, were
byte-identical (`json.dumps(..., sort_keys=True)` equality).

## 12. Matrix E — Per T4/T5 agent (30 rows: 30 PASS)

Source: part B. Mechanism: E1/E2 via each vendor's own non-interactive
resume flag against the dry-run plan's exact argv, asking the resumed agent
to recall a token only the original session's first turn could know; E3
the same via each vendor's fork flag, confirming a distinct session id also
recalls the token; E4 via a version-reporting shim both below and above the
verified range, real `resume`, confirming exit 5 and the range named; E5
via `scripts/testing/conptydriver`, holding the vendor CLI open for a
second shell's `resume --dry-run --json` to observe `agent.active
status=present, actual=true`; E6 the same non-interactive attempt with
`stdin` closed, expecting exit 7.

| Row | claude | codex | opencode | grok | qwen |
| --- | ------ | ----- | -------- | ---- | ---- |
| E1 (resume launches vendor CLI, session continues) | PASS — planted token recalled | PASS — planted token recalled, real `0.153.4` turn | PASS — planted token recalled | PASS — planted token recalled, executor-driven | PASS — planted token recalled |
| E2 (resumed session is the requested one) | PASS — same id, `message_count` 2→4 | PASS — same id, `message_count` 3→5 | PASS — same id | PASS — same id, `message_count` 13→(continued) | PASS — same id |
| E3 (fork produces a distinct session) | PASS — distinct id `c563efe8-…` (carries full history) | PASS — distinct id `01a081f3-…` (fresh) | PASS — distinct id `ses_f7e094118…` | PASS — distinct id `01a081fe-…`, executor-driven | PASS — distinct id `0101f75f-…` (carries full history) |
| E4 (below-min/above-max both exit 5, naming the range) | PASS — `2.1.219–2.1.263` | PASS — `0.133.0–0.153.4` (real `0.153.4` now the widened range's own max) | PASS — `1.18.21–1.18.29` | PASS — `1.0.5–1.0.13` | PASS — `0.21.12–0.23.0` |
| E5 (active session detected, resume refused/warned) | PASS — via ConPTY (`cmd.exe`-wrapped), `agent.active status=present, actual=true` | PASS — via ConPTY (`cmd.exe`-wrapped), same shape | PASS — via ConPTY (direct `.exe`), same shape | PASS — via ConPTY (direct `.exe`, native, no wrapper needed), same shape, executor-driven | PASS — via ConPTY (`cmd.exe`-wrapped), same shape |
| E6 (non-interactive exits 7) | PASS | PASS | PASS | PASS | PASS |

**30/30 PASS.**

### `codex:E1`–`E3` — real, completed turns against `0.153.4` (dispatch's targeted fix, confirmed)

The host's Codex account usage limit had already reset before this part
began (coordinator-verified 21:55 local; this part's own first Codex call
ran ~22:23 local, no usage-limit error). Every Codex row ran on the first
attempt, no wait-and-retry cycle needed:

```
codex exec --skip-git-repo-check "Reply with exactly this token and nothing else: RC7B-CODEX1-1788886410"
→ RC7B-CODEX1-1788886410  (session codex:01a081f0-67d8-7d51-8133-4bd23eaf11cd)

codex exec resume 01a081f0-67d8-7d51-8133-4bd23eaf11cd "What was the exact token I asked you to remember? Reply with just the token."
→ RC7B-CODEX1-1788886410  (message_count 3→5, same session id)

codex exec fork 01a081f0-67d8-7d51-8133-4bd23eaf11cd "What was the exact token I asked you to remember? Reply with just the token."
→ RC7B-CODEX1-1788886410  (new session codex:01a081f3-58cc-7213-bf76-939dbf28bb61)

rein sessions --agent codex --json
→ both 01a081f0… and 01a081f3… present
```

This clears the dispatch's `codex:E1`–`E3` targeted fix (the widened
`0.133.0`–`0.153.4` range) with real, completed conversational turns, not
merely a dry-run preview.

### `grok:E1`–`E3` — executor-driven, harness assumption did not reproduce (maintainer console fallback not needed)

Per the dispatch, this part attempted to drive `grok` itself first (headless
`-p`, isolated `GROK_HOME`) before falling back to the maintainer's own
console. Every `grok` invocation in this part answered within seconds, no
hang or timeout at any point:

```
grok --version
grok 1.0.13 (5e9a58528b76) [stable]

grok -p "Say hello in one word."
→ Hello.  (session 01a081fc-ada9-70a3-ac3c-3d9abd66d2fa)

grok --resume 01a081fc-ada9-70a3-ac3c-3d9abd66d2fa -p "Reply with exactly this token and nothing else: RC7B-GROK1-1788887211"
→ RC7B-GROK1-1788887211

rein search RC7B-GROK1-1788887211 --agent grok --json
→ session grok:01a081fc-ada9-70a3-ac3c-3d9abd66d2fa found, message_count=13

rein resume grok:01a081fc-ada9-70a3-ac3c-3d9abd66d2fa --dry-run --json
→ decision=confirmation_required

grok --resume 01a081fc-ada9-70a3-ac3c-3d9abd66d2fa --fork-session -p "What was the exact token I asked you to remember? Reply with just the token."
→ RC7B-GROK1-1788887211  (new session 01a081fe-e770-7d21-a313-c115bd65d037)

rein sessions --agent grok --json
→ 01a081fc…, 01a081fd… (second throwaway session), 01a081fe… all present
```

`grok:E1`, `grok:E2`, and `grok:E3` are scored on this executor-driven
transcript. The maintainer-console fallback described in the dispatch was
not needed; the "grok cannot be driven to completion from this harness"
assumption from `v0.6.0-rc.5` continues not to reproduce on this host with
an isolated `GROK_HOME` and `stdin` unused, matching `v0.6.0-rc.6`'s own
finding.

`grok.exe` (native PE, not an npm shim) was also launched directly by
`conptydriver.exe` for `grok:E5` with no `cmd.exe` wrapper needed; a second
shell's `rein resume grok:… --dry-run --json` observed `agent.active
status=present, actual=true` while the driver held the session open.

## 13. CLI experience (22 rows: 21 PASS, 1 FAIL — new finding, row 13)

Source: part C, through `scripts/testing/conptydriver` against
`scripts/tuisandbox`, with a fully rebuilt isolated `PATH` and every vendor
home variable redirected so no host session leaked into the bench's own 9
indexed sessions (see harness note below on the `XDG_DATA_HOME` gap this
part caught and fixed before capturing any evidence).

| # | Row | Result | Evidence |
| - | --- | ------ | -------- |
| 1 | Bare `rein` on a capable terminal draws the switcher | PASS | `9 sessions · 3 agents · all projects` header, filter prompt, time-grouped rows, split preview, key bar all draw correctly across 15+ launches; 10s/18s checkpoints byte-identical |
| 2 | Bare `rein` on a non-TTY exits 2, byte-identical to `v0.5.1` | PASS | both binaries exit 2; stderr SHA-256 `0476fb687fa1c1209d243016ab020460b8ad55696e4052f491b1fcb8cdb4795b`, matches every prior candidate's own recorded hash |
| 3 | `--plain` falls back to the numbered switcher | PASS | all 9 rows numbered, correct prompt |
| 4 | `REINSTATE_NO_TUI=1` does the same | PASS | output SHA-256-identical to row 3 |
| 5 | `TERM=dumb` does the same | PASS | output SHA-256-identical to rows 3/4 |
| 6 | `TERM` unset draws the switcher on Windows | PASS | confirmed `$env:TERM` unset; full interactive switcher drawn |
| 7 | A terminal below 40×10 falls back to plain | PASS | 30×8 ConPTY console → wrapped numbered picker |
| 8 | `NO_COLOR` draws the switcher with no color escapes | PASS | raw byte capture: exactly one bare `ESC[m` reset, zero SGR-parameter sequences |
| 9 | Typing filters; header count follows | PASS | `"auth"` → `1 session · 1 agent`, single matching row |
| 10 | `f` in list mode filters, does not fork | PASS | prompt shows `f`; header unchanged; no fork screen anywhere |
| 11 | `tab` opens the action menu; `esc` reverts without acting | PASS | key bar shows `r/f/h/i/y/esc`; `esc` reverts to a frame byte-identical to pre-`tab` (0-line diff) |
| 12 | `ctrl+k` opens the palette; a subsequence query finds its command | PASS | `"hof"` → top match `Hand off to another…` |
| **13** | **Readiness glyphs resolve for visible rows; a read-only agent shows blocked without a probe** | **FAIL — new finding, regression from `v0.6.0-rc.6`** | See full evidence below |
| 14 | The warning checklist acknowledges with the spacebar | PASS | single space toggles exactly one of 4 warnings (`baseline.unavailable`); equivalent command live-updates; second space reverts to a frame SHA-256-identical to pre-toggle |
| 15 | A partial acknowledgement is refused with exit 7 | PASS | 2 of 4 acknowledged: exit 7, message names exactly the remaining two (`git.working_tree`, `runtime.node.declaration`); interactive checklist shows the matching inline refusal |
| 16 | The handoff studio measures each policy | PASS | policy cycles `balanced→full→checkpoint→balanced` in lock-step with the equivalent command; real projection changes per policy (`checkpoint` drops `user_messages`, adds `files_touched_per_transcript`) |
| 17 | The studio refuses `enter` on a plan that could not be built | PASS | `repo-drift` (foreign `repository_url`, BLOCKED) shows "this handoff cannot be planned" before any keystroke; `enter` adds an identical inline refusal, studio stays open; second `enter`'s frame SHA-256-identical to the first |
| 18 | `rein init` opens the wizard, validates, allows going back | PASS | invalid endpoint stays on step 2 with inline error; raw CSI-Z (shift+tab) returns to step 1 with prior selection preserved |
| 19 | `rein init` collects no secret material inside the full-screen program | PASS | all 7 steps walked, no credential field anywhere; `init --help` has no secret-key flag; "start setup" exits the full-screen program before any key/passphrase prompt could occur |
| 20 | `rein init --link` prints a code `--paste` consumes on the other device | PASS | real two-device round trip via a local fakelocker S3 endpoint: every field (provider/endpoint/bucket/region/prefix/profile ID) pre-filled exactly matching device A through Review |
| 21 | Glyphs degrade to ASCII where Unicode is not safe (legacy conhost) | PASS | `WT_SESSION`/`TERM_PROGRAM`/`LANG`/`LC_ALL`/`LC_CTYPE` all cleared: every UI glyph rendered ASCII; session title Unicode text itself untouched |
| 22 | Every `--json` document is byte-identical to `v0.5.1`, except changelog-explained differences | PASS | 6 documents diffed byte-for-byte against `v0.5.1`: every non-empty diff matched to a changelog entry (Grok T3/T4 tier promotion; `agent.active` liveness check); `handoff` byte-identical; `doctor` differed only in the version field |

macOS is deferred under ADR 0005 for this candidate; nothing above claims
macOS evidence.

### Harness note — `XDG_DATA_HOME` isolation gap in `scripts/tuisandbox`

`scripts/tuisandbox`'s legend documents redirecting `HOME`,
`REINSTATE_HOME`, `CLAUDE_CONFIG_DIR`, and `CODEX_HOME`, but **not**
`XDG_DATA_HOME` — OpenCode's own `root_env`. On this host that variable was
already set to the operator's real OpenCode data root, and a first, naive
switcher launch showed **31 real host OpenCode sessions** mixed into the
bench's 9 synthetic rows. Caught before any capture was taken from that
state (never saved or committed); fixed by pointing `XDG_DATA_HOME` at an
empty, isolated directory (redirected, never unset, per the ground rules) —
confirmed afterward that `rein sessions --json` reports exactly the 9
synthetic sessions. Recorded as a harness defect (§21), not a product
defect; not scored against any row above.

### Row 13 — full evidence [FAIL — new finding, regression from `v0.6.0-rc.6`]

**Method** (the rc.4 report's own row-13 method, reused because it is the
one that originally caught this defect class): launch bare `rein` from a
cwd **outside any tracked project** — the switcher's documented default
`ScopeAll` — wait well past settle (10s–47s, confirmed byte-identical
across checkpoints within a run), and cross-check every row's rendered
glyph against ground truth from `rein resume <id> --dry-run --json` /
`rein inspect <id> --json` on the same 9 fixture sessions. Then repeat from
inside a tracked project workspace (`ScopeProject`).

**Ground truth** (`● Ready`, `◐ Warn`, `○ Blocked`), by session:

| Session | Ground truth (`resume --dry-run --json` / exit code) | Expected glyph |
| ------- | ------------------------------------------------------ | --------------- |
| `claude:…0001` (auth-refactor) | `decision: ready` | `●` |
| `codex:…0002` (keyring-store) | `decision: ready` | `●` |
| `claude:…0003` (website) | `decision: confirmation_required` | `◐` |
| `claude:…0009` (checkout-flow) | `decision: confirmation_required` | `◐` |
| `claude:…0004` (gone-missing) | exit 5 (workspace missing) | `○` |
| `grok:…000b` (payment-adapter) | `decision: confirmation_required` | `◐` |
| `codex:…0006` (repo-drift) | exit 7 (foreign `repository_url`) | `○` |
| `claude:…0007` (deps-bump) | `decision: confirmation_required` | `◐` |
| `codex:…0008` (search-index) | `decision: confirmation_required` | `◐` |

(`grok:…000b`'s own `tuisandbox` legend text calls it a permanently
`BLOCKED`, read-only row — stale relative to Grok Build's own tier
promotion, documented in `CHANGELOG.md`'s `[0.5.2-rc.1]`/`[0.6.0-rc.1]`
entries: `rein inspect` on this exact session reports `can_resume:true,
can_fork:true`, no `read_only_reason`. Ground truth, not the stale
comment, is what row 13 is scored against.)

**Result across 15 independent `ScopeAll` launches** (never inside a Git
checkout; a fresh, cleared probe cache was exercised for 3 of the 15 to
rule out cross-run cache staleness):

| Trial group | Launches | Fully matched ground truth | Had ≥1 wrong row |
| ------------ | -------- | --------------------------- | ----------------- |
| Initial 10s/18s checkpoints | 2 | 0 | 2 |
| 20s/35s checkpoint | 1 | 1 | 0 |
| 12s single-checkpoint trials | 5 | 1 | 4 |
| 12s/27s/47s checkpoints | 4 | 2 | 2 |
| Cold (cleared) probe cache | 3 | 0 | 3 |
| **Total** | **15** | **4 (27%)** | **11 (73%)** |

The wrong glyph is **always** `○` (Blocked) standing in for a row whose
ground truth is `●` (Ready) or `◐` (Warn) — never the reverse — and which
specific row(s) go wrong varies between launches. One representative wrong
trial (worst observed, 8 of 9 rows wrong):

```
 rein                                      9 sessions · 3 agents · all projects
 ...
▸ ○ claude   auth-refac… Fix the aut…    (expected ●)
  ○ codex    keyring-st… Wire the ke…    (expected ●)
  ○ claude   website     Open graph …    (expected ◐)
  ○ claude   checkout-f… Checkout th…    (expected ◐)
  ○ claude   gone-missi… Probe the v…    (○, correct)
  ○ grok     payment-ad… Trace the f…    (expected ◐)
  ○ codex    repo-drift  認証リファ…     (○, correct)
  ◐ claude   deps-bump   Bump the pi…    (◐, correct)
  ○ codex    search-ind… Try a small…    (expected ◐)
```

**Non-self-correcting.** Four trials held a wrong frame open through three
checkpoints spanning 12s, 27s, and 47s, each internally byte-identical
across all three checkpoints — a wrong verdict is not a slow-to-settle
one; once cached wrong for a given process's lifetime, it stays wrong.

**`ScopeProject` is unaffected.** Launched from inside the `auth-refactor`
workspace (`ScopeProject`, "1 session · 1 agent · auth-refactor"),
`auth-refactor` correctly shows `● READY TO RESUME` — checked once,
consistent with `v0.6.0-rc.6`'s own row 13 evidence, and not itself in
question here.

**Source-level pointer** (read-only observation, no rebuild attempted):
`internal/tui/readiness/prober.go`'s own doc comment on
`maxConcurrentProbes` already names this exact failure shape: "the
resulting process-creation stampede blows through each report's own
timeout budget before its checks can finish, and a timed-out check reports
itself blocked — indistinguishable, on screen, from a session that
genuinely cannot resume." `maxConcurrentProbes = 4` bounds concurrency but,
on this host, does not eliminate the effect — plausibly because this
acceptance run shares the host with three other executors' own concurrent
`rein`/`go test`/vendor-CLI processes. `FromReport`'s own doc comment says
a probe *error* should map to `Unknown` (pending glyph `◌`), not `Blocked`
— the wrong glyph observed here is unambiguously `○` (`Blocked`), so the
wrong verdict is most consistent with `preflight.Verify`'s own internal
per-check timeout resolving to a `blocked`-severity check result, not with
the outer probe's error path — not traced into `internal/preflight`
itself; a rebuild against the tagged binary's own source was not
attempted, consistent with this run's read-only evidence boundary.

**Disposition.** `FAIL`, new finding, not a carried disposition — the
`v0.6.0-rc.7` dispatch explicitly expected CLI row 13 to `PASS` again
unchanged, carried from `v0.6.0-rc.6`'s own clean `PASS`, since nothing in
this candidate touches the switcher or the readiness prober. Scored `FAIL`
rather than `PARTIAL`: the mechanism reproduces wrong ~73% of the time
under this exact, contract-specified method, and "resolve" cannot fairly
be read to include "resolve to a value that happens to be wrong."

## 14. Hop parity journeys (16 rows)

Source: part D (`H1`, `H1b`, `H1c`, `H2`, `H3`, `H4`, `H5`, `H9`, `H10`,
`H11`, `H12` — 11 rows) and part E (`H6`, `H6b`, `H7`, `H8`, `H8b` — 5
rows), against a local, disposable `hopd` + `scripts/testing/fakelocker`
lab, never the hosted control plane and never a developer's real agent
trees except where the ground rules specifically authorize the host's live
Claude Code config (`H5`). **Originally 15/16 PASS, 1 PARTIAL** (`H7`,
operator/harness availability); **corrected to 16/16 PASS in verification
round 1 (§24)** — `H7`'s original `PARTIAL` evidence is kept below
unchanged, and the round-1 recheck that reached `PASS` is in §24.

| # | Row | Result | Summary |
| - | --- | ------ | ------- |
| H1 | Email sign-in through the approver; token in OS keyring | PASS | Real GET-then-POST via `hoplab approve`; `login`/`whoami` exit 0; a fresh, separate `hoplab keyring show` process confirmed the OS-keyring device token entry |
| H1b | Refused sign-in tells the terminal why and stores nothing | PASS | Disposable lab, `HOPD_LOGIN_SESSION_TTL=10s`, no approver running; exit 1, documented refusal, nothing written under `REINSTATE_HOME`, no keyring entry |
| H1c | Unreachable control plane prints the one-line message and documented exit code | PASS | `REINSTATE_HOP_URL` at an unused loopback port; human and `--json` both exit 1 with the identical message, `details.kind=control_plane_unreachable`; nothing written |
| H2 | `init --hop` provisions the locker exactly once | PASS | Exactly one `locker_provisioned` event across two `init --hop` runs (second with `--force`); same locker id reused; `lockers` table carries exactly one row |
| H3 | `account init` recovery code shown once; keyring format verifies | PASS | Recovery code shown once (live-FD-confirmed); `keyring.v1.json` independently fetched via minted S3 credentials and parsed with the real `internal/keyring.Parse`: `schema_version=5, current_generation=1`, `VerifyGenerations` OK unpinned |
| H4 | `push --all` sends one session per T5 agent; `first_push` fires once | PASS | 1 Claude + 1 Codex + 1 OpenCode pushed; `first_push` fired exactly once across two calls; no-op push reports `skipped:3`; `hop status` shows `first_push_at` |
| H5 | Wipe, recover as new device, pull, real resume | PASS | Home/keyring genuinely wiped; new `device_id` under the same account; `account recover` re-derived the root key; `pull --all` restored all three after resolving a legitimate first-contact conflict; `resume --dry-run` completed (structured decision) for all three; one real `claude --resume … -p` against the still-live config recalled the exact planted token at the same session id |
| H6 | Pairing: B joins, A approves, B pulls | PASS | Live `hoplab pair join`; `pairing_requests status=consumed, claims=2, version=2`; A pushed 3 sessions, B pulled all 3 after a project mapping was added; `sessions --json` on B showed 6 total, no key overlap |
| H6b | An expired pairing request is refused/rolled back | PASS | Dedicated short-TTL lab (`HOPD_PAIRING_TTL=8s`); `account join` with no approver running blocked the full TTL then refused client-side, exit 4; `pairing_requests status=expired, payload='', claims=5`; `device_in_keyring=false, enrolled_devices=1` |
| **H7** | `daemon install/status/stop/start/uninstall` through Task Scheduler; debounced push, scheduled pull | **PASS** (corrected in verification round 1, §24; originally recorded `PARTIAL — operator/harness availability`) | Original evidence below; round-1 recheck evidence and disposition in §24 |
| H8 | A revokes B: generation rolls, B's token refused | PASS | `keygeneration_crossplane_test.go -tags hopacceptance` against a real `hopd`: both `TestKeyGenerationFloorAgainstRealHopd` and `TestKeyGenerationFloorWithoutTheRouteAgainstRealHopd` PASS (404-floor residual reproduced: floor route asked 4 times, answered 404 every time). Live: `rein devices revoke` rolled generation 1→2; B's `whoami`/`push --all`/`pull --all` all refused, exit 4, documented message |
| H8b | A Console-initiated revocation stays pending until confirmed via the recovery-code command | PASS | A disposable, self-signed-TLS reverse proxy drove the real Console HTTP routes (sign-in, session cookie, CSRF-bearing devices page, revocation POST) end to end; `device_revocation_requests` went `pending` (B's `whoami` still succeeded) → `confirmed, confirmed_generation=2` after `rein devices revoke`; B's next `push --all` refused, exit 4 |
| H9 | `sync verify` names only observed objects | PASS | `checked_objects` names exactly the index and newest snapshot; step 4 status `"fail"`, never `"could not run"` — the documented `fakelocker AnyBucket:true` harness limitation |
| H10 | `sync migrate --to byo`, `--switch`, `--forget-hop` | PASS | Moved 3 snapshots to a second bucket in one call; `--switch`/`--forget-hop` both took effect; device not revoked (`revoked_at` empty); new bucket's `sync verify`: `outcome: pass`, step 4 `not-applicable` |
| H11 | Path remap on pull | PASS | A second device with a remapped project mapping pulled the session with `cwd` rewritten to its own local path; `resume --dry-run` reports `workspace.available: present` at the remapped location, `agent.version status=match` |
| H12 | Keyless diagnostics refuse a tampered keyring | PASS | Three raw-S3 attacks (forged signature, foreign-account re-key using a real separate account's genuinely-signed keyring, rolled-back generation using a real pre-revoke object) each refused with `keyring_refused:true` and a specific accurate reason; `push` also refused exit 7; `account.json` byte-identical throughout and after final restore |

### `H1`–`H4`, `H9`, `H10` (part D) — evidence

```
hoplab approve -root <root> -email h1d-fresh@example.test -count 1 -timeout 60s   (background)
rein login --email h1d-fresh@example.test --no-browser --json   -> LOGIN_EXIT=0
rein whoami --json                                               -> WHOAMI_EXIT=0
```
`hoplab keyring show -root <root> -device device-a` confirmed: `device
token present: control_plane_url=http://127.0.0.1:8321
account_id=<account-key> device_id=<device-key>`.

`H1b` (disposable lab, `HOPD_LOGIN_SESSION_TTL=10s`, no approver):
```
LOGIN_EXIT=1
{"code":"runtime","message":"the sign-in link expired before it was used; run login again","safe_to_retry":false}
```

`H1c` (`REINSTATE_HOP_URL=http://127.0.0.1:8398`, unused loopback port):
```
$ rein login --email h1c-fresh@example.test --no-browser        -> HUMAN_EXIT=1
could not reach the Reinstate Hop control plane at http://127.0.0.1:8398: connection refused
...
$ rein login --email h1c-fresh@example.test --no-browser --json -> JSON_EXIT=1
{"code":"runtime","message":"could not reach ...","details":{"kind":"control_plane_unreachable","url":"http://127.0.0.1:8398"},"safe_to_retry":false}
```

`H2`:
```sql
SELECT type, COUNT(*) FROM events WHERE account_id=... GROUP BY type;
device_enrolled    1
locker_provisioned 1
sign_up            1
SELECT COUNT(*) FROM lockers WHERE account_id=...;   -- 1
```

`H3`: `s3rw verify` fetched `keyring.v1.json` directly and parsed it with
the real `internal/keyring.Parse`:
```
schema_version: 5
current_generation: 1
generations: [1]
device_count: 1
verify unpinned: OK
```

`H4`:
```
push --all --json  -> EXIT=0, 3 snapshots
hop status --json  -> "first_push_at": "2026-09-08T18:04:21Z"
push --all --json (no-op) -> {"skipped":3,"snapshots":null}, EXIT=0
```
```sql
device_enrolled    1
first_push         1
locker_provisioned 1
sign_up            1
verify_reported    1
```

`H9`:
```
$ rein sync verify            -> EXIT=7
$ rein sync verify --json     -> EXIT=7
checked_objects: ["the index", "the newest snapshot in the index"]
```
Step 4 (`isolation`) `status: "fail"`, with an accurate explanation of the
`fakelocker AnyBucket:true` limitation — never `"could not run"` for the
check itself.

`H10`:
```
$ rein sync migrate --to byo --endpoint http://127.0.0.1:9321 --bucket rc7d-byo-migrate --switch --forget-hop --json
EXIT=0
{"destination":{"bucket":"rc7d-byo-migrate",...},"forgot_hop":true,
 "migrated":{"snapshots":3,"written":3,"skipped":0,"bytes":10218,"manifest_sessions":3},
 "switched":true}
```
`hopd.db` confirms `revoked_at` empty (not revoked); the new bucket's
`sync verify --json`: `outcome: "pass"`, step 4 `status: "not-applicable"`.

### `H5` (part D) — wipe, recover, pull, real resume

**Evidence-format note (verification round 1, §24):** the token-planting
prompt quoted just below ("Remember this exact token for later: …") does
not use this run's mandated planting sentence ("Reply with exactly this
token and nothing else: `<token>`") — every other planted-token row in
this report uses it correctly; this was the one exception. `H5`'s own
disposition was never in question (the wipe/recover/pull/resume mechanism
this row exists to test is unaffected by prompt wording), so the original
evidence below is kept unchanged, and §24 adds a freshly planted token
using the correct sentence as a superseding, policy-compliant record.

A real Claude Code session was created in a throwaway git project
(`claude -p "Remember this exact token for later: RC7D-H5-CLAUDE-8271.
Reply with just OK."`), session id `9f4a3c1e-7b2d-4e5a-9c3f-b1a2c3d4e5f6`
— this executor's own session, created solely for this test, the only
Claude session id named in this report from part D. `device-a`'s
`REINSTATE_HOME` was deleted and recreated empty, its OS-keyring
device-token entry cleared.

`rein login --email <same address> --no-browser --json` after the wipe
returned a **new** `device.id` under the same, pre-existing account. A
live-recovery-code-confirmed `account recover`: "device enrolled from the
recovery code; this device now reads everything written under key
generation 1 and the 0 earlier one(s)." `pull --all`: first attempt
correctly refused (`compatibility NOT_INSTALLED`, no local OpenCode layout
marker yet) — resolved by seeding it once; second attempt pulled the
OpenCode session (`pulled:1, skipped:2`) and surfaced a legitimate
first-contact conflict for the claude session (correct refusal to
silently overwrite), resolved via `rein conflicts resolve ...
--keep-remote`; final pull `{"pulled":1,"skipped":0,"conflicts":null}`.
`resume --dry-run` completed (structured decision) for all three — each
`decision: "blocked"`, `workspace.available: missing` (the fixture's own
recorded cwd does not exist on this host, the same synthetic-fixture
path-ownership limitation prior candidates documented).

One real resume, against this executor's own still-present, unmodified
live Claude session:
```
$ claude --resume 9f4a3c1e-7b2d-4e5a-9c3f-b1a2c3d4e5f6 -p "What was the exact token I asked you to remember? Reply with just the token, nothing else." --output-format json
EXIT=0
{"session_id":"9f4a3c1e-7b2d-4e5a-9c3f-b1a2c3d4e5f6", ..., "result":"RC7D-H5-CLAUDE-8271", ...}
```
The returned `result` matches the planted token exactly, and `session_id`
matches the original — a genuine continuation of the same session's own
history through the real vendor executable.

### `H6`, `H6b` (part E) — evidence

```
rein login --email <lab-email>                     # device-a, then device-b (same email)
hoplab approve -root <lab> -email <lab-email> -count 2 -timeout 2m
hoplab pair init -root <lab> -device device-a -rein rein.exe
hoplab pair join -root <lab> -device device-b -approver device-a -rein rein.exe
rein push --all --json                              # device-a: 3 sessions (claude, codex, opencode)
rein pull --all --json                               # device-b, after adding a project mapping for device-a's project
rein sessions --json                                 # device-b
```
`hoplab pair join` → "device-b joined the account live, approved by
device-a" (exit 0). `pairing_requests`: one row, `status=consumed,
claims=2, version=2`. `rein pull --all --json` on device-b: first attempt
refused (`claude snapshot project "hoplab-device-a" has no local mapping
on this device`), resolved by adding the mapping — `"pulled": 3,
"skipped": 0`. `rein sessions --json` on device-b: 6 sessions total (3
pulled + device-b's own 3), no session-id overlap.

`H6b`:
```
HOPD_PAIRING_TTL=8s hoplab start -root <lab-h6b> -hopd-addr 127.0.0.1:8323 -locker-addr 127.0.0.1:9323 -background
rein login --email <lab-email>                       # device-a, device-b
hoplab pair init -root <lab-h6b> -device device-a -rein rein.exe
rein init --hop --project hoplab-device-b=<path>      # device-b
rein account join                                     # device-b, no approver ever running
rein account status --json                            # device-b, after expiry
```
`rein account join` blocked for the full 8s TTL, then: "the pairing
request expired before it was approved and collected; run rein account
join again on the new device", exit 4. `pairing_requests`:
`status=expired, payload='' (empty), claims=5, version=2`. `rein account
status --json`: `"device_in_keyring": false, "enrolled_devices": 1`
(device-b's request never produced a wrap in any generation).

### `H7` — full evidence [originally recorded PARTIAL — operator/harness availability; corrected to PASS in verification round 1, §24]

**This subsection is the original, unmodified part-E evidence, kept for
the record per the append-only correction policy.** Its own
"Disposition: `PARTIAL`" line below, and specifically its claim that a
40-minute wait "matches the dispatch's own explicitly anticipated outcome
for a wait window shorter than 150 minutes," is the exact
self-contradiction verification round 1 flagged as a blocker (the
contract's own text says a shorter poll window is *not* a basis for
`PARTIAL`) — that sentence is wrong and is not endorsed by this report as
corrected; it is left in place, unedited, so the mistake is visible rather
than quietly fixed. **The row's actual, current disposition is `PASS`; see
§24 for the recheck that produced it.**

**Fresh Hop lab account (zero pushed sessions).** A brand-new Hop account
was created through this part's own `hopd`, under a fresh `REINSTATE_HOME`
this row created and never pushed anything from — "zero sessions pushed to
it," per the refined rule's own definition.

**Per-file before-listing.** Every file (relative path, size, mtime) under
exactly the three session-bearing subtrees was listed, excluding `*-wal`,
`*-shm`, `*.lock`: `<CLAUDE_CONFIG_DIR>/projects`, `<CODEX_HOME>/sessions`,
`<XDG_DATA_HOME>/opencode` — reading `CODEX_HOME` as the **persistent,
user-level** value (`D:\Projects\hop-10-lab\codex`), confirmed different
from this shell's own transient `CODEX_HOME`
(`...\orca\codex-runtime-home\home`), the exact trap the dispatch's "Lab
isolation for `MatrixH:H7`" section warns about. Before-listing: **3,062
files**, digest `1ba422b730e74c2964085401169dd1555cddf7a6122db7d5c14c012cbd4d777a`.

**Go-signal wait — not satisfied.** `H7` was ordered last, after `H6`,
`H6b`, `H8`, `H8b` were already done, per the row's own ordering rule.
`D:/ReinstateAcceptanceProjects/h7-go.txt` did not exist when this part
started, and was polled every 20–60 seconds, starting only after the
other four rows finished (`2026-09-08T16:49:49Z`), for a total of **2,408
seconds (~40.1 minutes)** across five consecutive poll windows (541s,
542s, 542s, 542s, 241s), ending `2026-09-08T17:24:17Z`. The file never
appeared. This 40-minute window is an operational constraint this run's
own orchestrator set — shorter than the contract's own 150-minute figure —
a scheduling detail of this run, not a reinterpretation of the contract;
the same shorter-window disposition the `v0.6.0-rc.6` `H7` evidence
anticipated as a possible outcome of an orchestrator-set budget shorter
than 150 minutes.

**Elevated round trip — not attempted.** Because the go-signal file never
appeared inside the wait window, the elevated `rein daemon install` →
`status` → debounce/scheduled-pull wait → `status` → `stop` → `start` →
`status` → `stop` → `uninstall` → `status` → `hop status` script was never
launched — attempting the elevation without the maintainer present to
accept the UAC prompt would be the exact unattended workaround the
dispatch's "Harness traps" section forbids.

**Disposition: `PARTIAL`** (operator/harness availability) — the row's
required mechanism (a real UAC-gated `schtasks` round trip) needs the
maintainer at the keyboard, which this run's own budget did not produce
within its window. This is not a product defect, and matches the
dispatch's own explicitly anticipated outcome for a wait window shorter
than 150 minutes ("If the file never appears within that full wait window,
record `PARTIAL` (operator/harness availability) with that reason"). It is
also **not evidence of a regression** in the daemon's own mechanism from
`v0.6.0-rc.6`'s clean `PASS` on this row — nothing about that mechanism was
exercised or found wrong this run. `PARTIAL` still does not pass a
required row; see §20.

### `H8`, `H8b` (part E) — evidence

```
GOTOOLCHAIN=go1.25.13 CGO_ENABLED=0 go test -tags hopacceptance ./internal/cli \
  -run TestKeyGeneration -count=1 -v          # REINSTATE_HOPD_BIN=<own hopd.exe>
```
```
=== RUN   TestKeyGenerationFloorAgainstRealHopd
--- PASS: TestKeyGenerationFloorAgainstRealHopd (2.01s)
=== RUN   TestKeyGenerationFloorWithoutTheRouteAgainstRealHopd
    keygeneration_crossplane_test.go:539: the floor route was asked for 4 times and answered 404 every time
    keygeneration_crossplane_test.go:572: documented residual reproduced: on a control plane that carries no floor, a device that has confirmed none reads the restored generation-1 keyring as current
--- PASS: TestKeyGenerationFloorWithoutTheRouteAgainstRealHopd (2.15s)
```
Live: `rein devices revoke <device-b-id>` on device-a (recovery code via
`REINSTATE_RECOVERY_CODE_FD`) → "revoked device ...; key generation 2
started with 1 enrolled device(s), and the control plane refuses its
token", exit 0. Device-b afterward: `whoami`/`push --all`/`pull --all`
each → "this device's token was rejected by the control plane (revoked or
stale); run rein login again", exit 4.

`H8b`: a disposable `consoledriver` helper (not part of the product) drove
the real Console HTTP routes through a local self-signed-TLS reverse proxy
in front of the real `hopd`:
```
step1: POST /console/sign-in/email -> 200
step2: found link https://127.0.0.1:8443/console/sign-in/email/...
step3: GET confirm page -> 200
step4: POST confirm -> 200 final url https://127.0.0.1:8443/console
step5: GET /console/devices -> 200
step5: csrf token found, target device present=true
step6: POST https://127.0.0.1:8443/console/devices/<device-b-id>/revocation -> 200 final url https://127.0.0.1:8443/console/devices
step7: GET /console/devices -> 200
step7: device row now shows Pending revocation
```
`device_revocation_requests`: `pending` (target's `whoami` still succeeded,
exit 0, not yet enforced) → `confirmed, confirmed_generation=2,
confirmed_by=<device-A-id>` after `rein devices revoke` reported "Console
request ... confirmed after the keyring reached generation 2". Target's
next `push --all` refused, exit 4.

### `H11`, `H12` (part D) — evidence

`H11`: `device-b` pushed one session per T5 agent with a project mapping
to its own fixture cwd. `device-c` joined the same account with a mapping
for the **same project id** pointing at a **different**, device-c-owned
local path. `pull --all --json`: `{"pulled":3,"skipped":0,"conflicts":null}`,
with the claude destination under device-c's own remapped root, not
device-b's original path. Direct file check confirms the restored jsonl's
own `"cwd"` field reads the remapped path. `resume claude:… --dry-run
--json`: `"cwd"` matches the remapped path, `decision:
"confirmation_required"`, `workspace.available: {"status":"present"}`,
`agent.version: {"status":"match","actual":"2.1.263"}`.

`H12`: performed on `device-c`, continuing `H11`'s account, after
establishing a real key generation 2 via a genuine `rein devices revoke`.

1. **Forged signature.** The real generation-2 object's last generation
   signature was corrupted (one base64 character flipped) and `PUT` back.
   `account status --json`: `keyring_refused: true`, `"generation 2 does
   not verify under account key <account-key>"`. `account.json` unchanged.
2. **Foreign-account re-key.** A genuinely separate, real account's own
   genuinely-signed `keyring.v1.json` was `PUT` in place of the shared
   account's object. `account status --json`: `keyring_refused: true`,
   `"the keyring is signed by a different account key"`. `rein push --all`
   also refused, exit 7, "Nothing was written; the keyring in storage was
   replaced by one signed with a key this account never used." Unchanged.
3. **Rolled-back generation.** The real, earlier (pre-revoke)
   generation-1-only object was re-`PUT` over the current generation-2
   object. `account status --json`/`devices --json`: `keyring_refused:
   true`, "current_generation 1 is below the 2 the control plane reports
   ... Nothing was written; restore the account's keyring from a device
   that holds key generation 2, or run rein devices revoke again from one
   that does." Unchanged.

Every attack refused with a specific, accurate reason and
`keyring_refused: true`; `account.json`'s SHA-256 byte-identical across
baseline, all three attacks, and after the real generation-2 object was
finally restored and re-confirmed (`keyring_refused: false`).

## 15. T5 encrypted sync round trip (beyond the 216; source: part B)

A disposable `scripts/testing/fakelocker` instance (`-accept FAKEKEY`)
served the S3-compatible locker. Two fully isolated `REINSTATE_HOME`s were
paired against it via `rein init --yes --endpoint … --bucket … --project
…` (device A), then `rein init --yes … --profile-id <device A's own id>`
(device B). Passphrase input used a small Windows helper (`passfd.exe`)
mirroring `scripts/testing/hoplab/secretfd_windows.go`'s inheritable-handle
mechanism — no ordinary passphrase environment value or plaintext file.
Device B's `CLAUDE_CONFIG_DIR`/`CODEX_HOME`/`XDG_DATA_HOME` were pointed at
fresh, isolated directories for the pull (never the host's live homes).

| Row | Result | Evidence |
| --- | ------ | -------- |
| `push:claude` | PASS | `push --agent claude --all --json` → 3 snapshots |
| `push:codex` | PASS | `push --agent codex --all --json` → 2 snapshots |
| `push:opencode` | PASS | `push --agent opencode --all --json` → 24 snapshots (see harness note below) |
| `pull:claude` | PASS | Device B's isolated `CLAUDE_CONFIG_DIR` restored 3 files; planted token appears 8 times in the raw restored file for the fork session |
| `pull:codex` | PASS | Device B's isolated `CODEX_HOME` restored 2 files; planted token appears 8 times in the raw restored file |
| `pull:opencode` | PASS | Initial pull refused (`NOT_INSTALLED`, no OpenCode layout marker on device B yet), resolved by seeding device B's schema once; retry pulled cleanly, `rein search <token> --agent opencode --json` on device B found the pulled session (`message_count=6`) |

**6/6 PASS.**

### Harness note: `push --agent <a> --all` scopes by agent, not by project

`push --agent opencode --all` produced 24 snapshots — the full live
OpenCode catalog under the host's live `XDG_DATA_HOME` (used for the
Phase-5 `opencode` rows above), not only the one throwaway `opencode-proj`
session created for this sync round trip. `--agent` scopes by agent, not
by project; `--all` within that agent scope means every session that
agent's adapter can currently see. This reproduces the exact shape the
`v0.6.0-rc.6` tagged run's part B flagged for an unscoped `push --all`, one
level narrower (per-agent, not global) but with the same root cause: no
flag exists to scope a push to one project. No plaintext left this
executor's process — the pushed content is `age`-encrypted before upload,
never decrypted, and `fakelocker` persists nothing and was torn down
immediately after this part. Recorded as a non-blocking harness/product
observation (§21), not a row defect: `push:opencode`'s own mechanism is
still exercised and correct.

## 16. Findings (merged, release-blocking assessment)

| ID | Severity | Row(s) | Description | Release blocking |
| -- | -------- | ------ | ------------ | ----------------- |
| — | product, new this run | CLI row 13 | `ScopeAll`'s readiness-glyph pipeline resolves at least one visible row's readiness glyph to `○ Blocked` (wrong) in 11 of 15 launches (73%), never the reverse, non-self-correcting once wrong. `ScopeProject` unaffected. Source-level pointer: `internal/tui/readiness/prober.go`'s own documented process-creation-stampede failure mode, on a shared, multi-executor host. See §13 | Yes — new regression, `FAIL`, matches no named disposition |
| — | report defect, corrected verification round 1 | `MatrixH:H7` | The original text here claimed the run's 40-minute go-signal window "matches the dispatch's own anticipated outcome" for a wait shorter than 150 minutes — the contract's own text says the opposite ("a shorter poll window is not a basis for `PARTIAL`"). Corrected: see §24. The row itself is `PASS` on recheck (go-signal already present, full elevated round trip clean under the refined per-file-listing rule) | No, as corrected — was `Yes` under the original, now-superseded `PARTIAL`. See §24 |
| — | product-policy / harness interaction, non-blocking | `claude:C6`, `claude:D4` | This session's own permission classifier blocked a direct filesystem copy of a directory under the host's live `CLAUDE_CONFIG_DIR`, consistent with this worktree's own `CLAUDE.md`. Both rows were completed instead against a hand-authored synthetic Claude Code JSONL fixture in a fully isolated `CLAUDE_CONFIG_DIR`, exercising the same mechanism | No — both rows still exercised their mechanism and passed |
| — | harness mechanics, non-blocking | `claude:E5`, `codex:E5`, `qwen:E5` | `conptydriver.exe`'s raw `CreateProcess` cannot launch an npm-installed `.cmd` batch shim directly (`claude.cmd`/`codex.cmd`/`qwen.cmd`) — the child never starts, and the driver reports success anyway with a fully empty raw log. Wrapping as `cmd.exe /c <vendor> …` fixed it; `opencode`/`grok` resolve to native `.exe` and needed no wrapper. `rein`'s own real `resume`/`fork` (E1–E3) are unaffected | No — worked around within the part; worth adding to `scripts/testing/conptydriver/README.md`'s traps section |
| — | host/vendor, non-blocking | `gemini` C1 turn 1 | The first `gemini -p` call in part B failed with a transient upstream `503`; an immediate retry succeeded and every row is scored from the successful retry's session | No — resolved on retry, not reproduced again |
| — | harness methodology, non-blocking | T5 push/pull; also `H6` | `push --agent opencode --all` pushed the full live OpenCode catalog (24 sessions), not only the one throwaway session created for the round trip, since `--agent`+`--all` scopes by agent, not by project. No plaintext left the process | No — the row's own mechanism (encrypt, upload, restore, correctly find the round trip's own session) is unaffected and scored `PASS` |
| — | harness methodology, self-caught, corrected before any evidence captured | `scripts/tuisandbox` XDG isolation | The bench's env-isolation legend omits `XDG_DATA_HOME` (OpenCode's own `root_env`); a naive launch mixed 31 real host OpenCode sessions into the switcher bench before it was caught and fixed | No — no capture with the leaked data was ever saved or committed |
| — | worktree/harness process-isolation concern, non-blocking to product correctness | `scratchtool_h3/` | An untracked S3/keyring scratch tool appeared inside executor A's exclusively-assigned worktree mid-session, after an earlier `git status --short` in the same session had returned clean — evidence of a second process writing into the same shared worktree path. Left untouched (not deleted); excluded from the A4 lint gate by scoping to `git ls-files`-tracked directories, which is clean | No — the tagged tree itself is clean; a coordinator process-isolation concern, not a product defect |
| — | report internal consistency, non-blocking | part-a.md's own header | part-a.md's Verdict block header states `"100 PASS / 0 PARTIAL / 0 FAIL / 0 NOT TESTED"` and points at a nonexistent §17; both the file's own itemized §14 table and executor A's structured JSON report agree on `79`, which is what this report uses. See §0.6 | No — reconciled to the itemized/structured figure, `79` |

## 17. Tier reductions proposed by this device

| Agent | Claimed tier | Verified tier | Reason |
| ----- | ------------ | -------------- | ------ |
| — | — | — | None proposed. Every declared tier (T0–T5, no T3 this candidate) was exercised as designed. `codex`'s full-clean pass this run (`G1`, `E1`–`E3` all `PASS`, real live turns) further confirms Codex's own capability tier is correctly classified — the version-drift/account-limit gaps that blocked `v0.6.0-rc.6`'s equivalent rows are both resolved this candidate |

## 18. `v0.6.0-rc.6` dispositions carried: cleared or re-recorded

Per [the rc.7 dispatch's own table](../v0.6.0-rc.7-agent-verification-prompts.md#dispositions-carried-from-the-v060-rc6-tagged-run).

| Row | `v0.6.0-rc.6` tagged result | This candidate expected | This run | Status |
| --- | ---------------------------- | ----------------------- | -------- | ------ |
| `MatrixG:G1` (Codex half) | PARTIAL, host/account — the acceptance host's live, authenticated Codex account was usage-limit exhausted, reproduced on 3 independent attempts (Claude half fully `PASS`) | Not fixed by range widening alone — expect `PASS` once the account's usage limit resets and the wait-and-retry rule is followed, else the same `PARTIAL`/`NOT TESTED (host credential)` disposition | PASS — the host's usage limit had already reset before this part began (coordinator-verified 21:55 local; first Codex call ~22:23 local); Claude and Codex halves both fully exercised, real turns, no wait-and-retry cycle needed | **CLEARED** |
| `codex:E1` | NOT TESTED (version drift) — the live, installed Codex CLI (`0.153.4`) was past the then-verified ceiling (`0.149.0`) | Fixed via the widened `0.133.0`–`0.153.4` range — expect `PASS` on a real, completed conversational turn | PASS — real `codex exec resume` against `0.153.4`, planted token recalled, same session id | **CLEARED** |
| `codex:E2` | NOT TESTED (version drift), same reason | Fixed — expect `PASS` on a real, completed conversational turn | PASS — same session id, `message_count` grew | **CLEARED** |
| `codex:E3` | NOT TESTED (version drift), same reason | Fixed — expect `PASS` on a real, completed conversational turn | PASS — real `codex exec fork`, distinct session id, token recalled | **CLEARED** |

**All four of `v0.6.0-rc.6`'s carried release-blocking rows are cleared to
`PASS` this run.**

**Also carried, expected `PASS` again (not a formal disposition, named so a
first-time re-run against this tag is not mistaken for a fresh finding):**

| Row | Expected | This run | Status |
| --- | -------- | -------- | ------ |
| `grok:E1`–`E3` | PASS again, executor-driven or maintainer console | PASS — executor-driven path succeeded on the first attempt; maintainer console fallback not needed | **CLEARED (unchanged)** |
| `MatrixH:H7` | PASS again, refined per-file-listing rule | Originally recorded **PARTIAL** — the go-signal window this run's own orchestrator set (40 minutes) was shorter than the contract's 150-minute figure, and the maintainer did not appear within it. **Corrected to `PASS` in verification round 1 (§24):** on recheck the go-signal file was already present, and the full elevated round trip ran cleanly under the refined per-file-listing rule | **Originally "NOT cleared"; CLEARED as of verification round 1 — see §24** |
| `opencode:D4` | N/A (definitional), unchanged since `v0.6.0-rc.1` | N/A (definitional), same reason | **CLEARED (unchanged)** |

**Every row that cleared at `v0.6.0-rc.6` and is not on either table also
stayed `PASS`**, unaffected by this candidate's own changes: `MatrixB:B4`,
`MatrixB:B7`, `MatrixG:G4` (and `MatrixG:G1`'s Claude half), `opencode:E5`,
`opencode:E6`, `pi:C3`, every `qwen` row `C1`–`E6`, `codex:E4`, `codex:E5`,
`codex:E6`, `opencode:C1`–`C6`/`D1`–`D3`/`E1`–`E4`, and every CLI/Hop
parity row other than the ones listed above.

**One row on the "expected `PASS` again" list from the dispatch regressed
this run: CLI row 13.** `v0.6.0-rc.6`'s report recorded it clean `PASS`;
this candidate's dispatch explicitly expected it to `PASS` again unchanged
since nothing in `v0.6.0-rc.7` touches the switcher or the readiness
prober. This run found it `FAIL` (§13) — **a new finding, not a carried
disposition**, and, as of verification round 1 (§24), the sole reason this
device verdict is `FAIL` (`H7` is `PASS` as corrected; it no longer
contributes to the verdict).

## 19. Deferred: Apple Silicon macOS

Copied verbatim from
[`../v0.6.0-rc.7-agent-verification-prompts.md`](../v0.6.0-rc.7-agent-verification-prompts.md)'s
own "Deferred: Apple Silicon macOS" section, under
[ADR 0005](../../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md) D2.
Nothing in this device's evidence claims macOS. Every row below is run
against this same `v0.6.0-rc.7` tag (or the stable tag it becomes) once the
Apple Silicon macOS host returns from repair
([issue #403](https://github.com/HarjjotSinghh/reinstate/issues/403)). If
that later macOS run fails, the fix ships as `v0.6.1` — this record of
this candidate's Windows acceptance is never edited to hide it.

| Group | Rows | Contract | Result |
| ----- | ---- | -------- | ------ |
| Phase 5 generated matrix, macOS column | all rows section B lists | Phase 5 | `DEFERRED` |
| CLI experience, macOS column | rows 1–20 and 22 | `v0.5.2` | `DEFERRED` |
| Hop cross-device journeys | pairing macOS↔Windows; path remap Windows↔macOS both directions; daemon launchd round trip; first push on macOS | hosted #16 | `DEFERRED` |
| Range widening on macOS | Claude Code through `2.1.263`; Codex CLI through `0.153.4`; OpenCode through `1.18.29`; Qwen Code through `0.23.0`; Grok Build through `1.0.13` | ADR 0005 D3 | `DEFERRED` |
| Grok GD8 | tool-approval prompt (already collected on macOS; the Windows half is #368 and stays optional) | Grok T4 | `DEFERRED` |

No document, data file, or release note may state that `v0.6.0` was
verified on macOS until that run exists.

## 20. Dispositions that do not block the device verdict

| Row | Disposition | Conditions checked |
| --- | ----------- | ------------------- |
| `opencode:D4` | `N/A (definitional)` — OpenCode's SQLite-only store has no JSONL record boundary this row's truncation-boundary mechanism applies to, regardless of fix or credential. Unchanged since `v0.6.0-rc.1`. | Excluded from both the denominator and the numerator, as the contract requires. Not counted toward the 215 required rows. |
| — | `NOT TESTED (host credential)`, optional agents only | **Not applicable this run — 0 rows recorded with this disposition.** Every optional-agent row (Grok Build, Qwen Code, Gemini CLI, Kimi Code CLI, Copilot CLI, Cursor CLI, Cline, Pi) that has a live host credential completed with a real result this run; none was blocked by an expired or absent credential. |
| — | `NOT TESTED (version drift)`, standing rule | **Not applicable this run — 0 rows recorded with this disposition.** The one candidate this disposition applied to at `v0.6.0-rc.6` (`codex:E1`–`E3`) is `PASS` this run against the widened, in-range `0.153.4` binary; no other vendor CLI drifted past its verified ceiling mid-run. |

Neither `opencode:D4`'s exclusion nor the absence of any host-credential or
version-drift row changes the device verdict's own `FAIL`: the one
blocking row as of verification round 1 (CLI row 13) does not match either
disposition and falls under the contract's "Everything else" clause — see
§0.8 and §16. (`MatrixH:H7` blocked under this same clause in the original
assembly; it is `PASS` as corrected in §24 and no longer does.)

## 21. Release-blocking findings

**Updated in verification round 1 (§24): `MatrixH:H7` is corrected to
`PASS` and no longer appears below as a blocker. Only finding 1 (CLI row
13) still blocks.** The original two-item list, including the flawed `H7`
rationale verification round 1 rejected, is kept immediately below for the
record.

Because 1 of the 215 required rows is not `PASS` and is not excused by
either disposition in the contract, this report's device verdict is `FAIL`
and stable `v0.6.0` is **not authorized** by this run:

1. **CLI row 13 (`FAIL`, new finding).** The interactive switcher's
   default `ScopeAll` scope resolves at least one visible session row's
   readiness glyph to the wrong value (`○ Blocked` standing in for
   `●`/`◐`) in 11 of 15 independent launches (73%), using the exact
   `v0.6.0-rc.4`/`rc.6`-documented method, non-self-correcting within a
   launch's lifetime once wrong. `ScopeProject` is unaffected. This
   candidate's own dispatch expected this row to `PASS` unchanged; it did
   not — see §13 for the full evidence and the source-level pointer
   (`internal/tui/readiness/prober.go`'s own documented process-creation-
   stampede failure mode, not fully eliminated by `maxConcurrentProbes=4`
   under this shared, multi-executor host's contention).
2. ~~**`MatrixH:H7` (`PARTIAL`, operator/harness availability).** The
   maintainer was not available inside this run's own 40-minute go-signal
   wait window (shorter than the contract's own 150-minute figure), so the
   elevated Task-Scheduler round trip was never attempted, per the
   contract's own explicit instruction against an unattended elevation
   workaround. Not a product defect and not evidence of a regression in
   the daemon's own mechanism from `v0.6.0-rc.6`'s clean `PASS` — but
   `PARTIAL` does not pass a required row under this run's own gate ("a
   row is `PASS` only if its mechanism was exercised; `PARTIAL` and
   `NOT TESTED` do not pass a required row"), and it matches no named
   disposition (it is not `NOT TESTED (host credential)` — the daemon's
   own mechanism is not agent-scoped and is required, not optional — and
   not `NOT TESTED (version drift)`).~~ **Struck in verification round 1:
   this item's own claim that the 40-minute wait "matches the dispatch's
   own anticipated outcome" was the exact self-contradiction that round
   rejected — the contract's own text says a shorter window is *not* a
   basis for `PARTIAL`. On recheck (§24) the go-signal file was already
   present and the full round trip reached a clean `PASS`; this is no
   longer a release-blocking finding.**

Unlike `v0.6.0-rc.6`'s tagged run, this candidate's own targeted fix (the
widened Codex range) is fully confirmed working, the Codex account usage
limit that separately blocked that run had already reset, and `grok:E1`–
`E3` again cleared cleanly — **all four of `v0.6.0-rc.6`'s carried
release-blocking rows are cleared** (§18). As of verification round 1, the
sole blocker is the newly-discovered readiness-glyph regression
(product-level, though its own evidence points at host-contention-sensitive
prober timeouts rather than a functional break); `H7` is resolved (§24). A
clean `CLI row 13` result — either a genuine fix, or new evidence that
narrows the regression to host contention specifically and not the tagged
binary's own logic — is what the next tagged acceptance run needs to
plausibly clear the device verdict.

## 22. Harness defects (merged, non-blocking to the mechanism under test)

- **Worktree-isolation concern (part A).** An untracked directory
  (`scratchtool_h3/`, an unrelated S3/keyring scratch tool) appeared
  inside executor A's exclusively-assigned worktree mid-session, after
  this session's own initial `git status --short` on that worktree had
  already returned clean — evidence a second process wrote into the same
  shared worktree path concurrently with this run. Left untouched (not
  deleted, in case another process still had it open); excluded from the
  A4 lint gate by scoping to the git-tracked tree, which is clean.
- **Host paging-file/memory exhaustion under concurrent background jobs
  (part A, `A3`).** Two of three `-p 4` race-suite attempts failed on
  transient host memory/linker exhaustion (`ERROR_COMMIT_LIMIT`/
  `collect2.exe` missing, different packages each time), never on an
  actual reported data race — attributable to real concurrent memory
  pressure from other processes on this shared, multi-executor host. A
  third, isolated attempt passed clean.
- **`conptydriver`'s raw `CreateProcess` cannot launch an npm-installed
  `.cmd` batch shim directly (part B).** `claude.cmd`/`codex.cmd`/
  `qwen.cmd` need a `cmd.exe /c` wrapper; the driver otherwise reports
  success with a fully empty raw log (the child never starts).
  `opencode`/`grok` resolve to native `.exe` and need no wrapper. `rein`'s
  own real `resume`/`fork` calls are unaffected. Worth adding to
  `scripts/testing/conptydriver/README.md`'s traps section.
- **`push --agent <a> --all` scopes by agent, not by project (part B,
  also `H6` in part E).** `--all` within an agent scope means every
  session that agent's adapter can currently see, not only a throwaway
  project's own session — the same root cause the `v0.6.0-rc.6` tagged
  run's part B flagged for an unscoped, global `push --all`, one level
  narrower here (per-agent). No plaintext left the process; no flag
  exists to scope a push to one project.
- **`scripts/tuisandbox`'s env-isolation legend omits `XDG_DATA_HOME`
  (part C).** OpenCode's own `root_env`; on a host where it is already
  populated with real data, a naive switcher launch mixed 31 real host
  OpenCode sessions into the bench before this was caught and fixed
  (redirected, never unset, per the ground rules) — no capture with the
  leaked data was saved or committed.
- **`grok:…000b`'s own `tuisandbox` generator comment is stale (part
  C).** Describes it as permanently hardcoded `BLOCKED`/read-only,
  relative to Grok Build's real tier promotion to verified resume — ground
  truth, not the stale comment, is what CLI row 13 is scored against.
- **`scripts/testing/conptydriver`'s grammar has no named `shift+tab` key
  (part C).** The raw CSI Z byte sequence (`send "\x1b[Z"`) substitutes
  successfully.
- **Launching `conptydriver.exe` with its own stdout/stderr redirected
  (the default in most automation contexts) reproduces the tool's own
  documented "needs a real console of its own" trap (part C).** Worked
  around via PowerShell `Start-Process -WindowStyle Hidden` with no stdio
  redirection, per the tool's own README.
- **`opencode`'s `resume --dry-run` reports `agent.executable: missing`
  when `workspace.available` has already blocked (part D, `H5`).** Even
  though `claude`/`codex`/`opencode` are genuinely resolvable on `PATH` in
  the same isolated shell — but a real session with an existing workspace
  correctly reports `agent.executable: present`. Suggests the check
  short-circuits once the workspace check has already blocked, rather
  than a genuine Windows executable-resolution gap. Not root-caused within
  this run's time budget; carried as an unresolved observation.
- **Self-caught harness near-miss, resolved, no product impact (part D,
  `H5`).** One intermediate `pull --all` plan during this run's earlier,
  pre-restart pass briefly showed a destination for a synthetic fixture
  session under the host's live `CLAUDE_CONFIG_DIR`, because that shell
  had the live vendor-home variable set alongside an isolated
  `REINSTATE_HOME` — confirmed no file was ever written there before this
  report was finalized. A shell that crosses an isolated `REINSTATE_HOME`
  with a live vendor-home variable is a real footgun worth making harder
  to hit by accident.
- **`rein sync migrate --to byo` needs the destination bucket's own S3
  credential env vars in addition to the passphrase FD (part D, carried
  unchanged from `v0.6.0-rc.6`).** Omitting them fails with a generic
  secret-input error that does not name which secret is missing.
- **Carried, non-blocking, unchanged since `v0.6.0-rc.1` (parts D, E,
  `H4`, `H6`, `H9`, `H10`).** `scripts/testing/fakelocker`'s
  `AnyBucket:true` makes step 4 (bucket isolation) report `fail` rather
  than a genuine bucket-isolation pass on this local lab — contrasted
  directly by `H10`'s real BYO `not-applicable` result and `H12`'s real
  cross-account signature refusal.
- **`CLAUDE.md` (checked into this repository) carries a "context-mode"
  section instructing mandatory routing of Bash/Read/Grep/WebFetch through
  MCP tools (`ctx_fetch_and_index`, `ctx_execute`, `ctx_batch_execute`,
  `ctx_search`, `ctx_index`, `ctx_stats`, `ctx_doctor`, `ctx_upgrade`)
  (parts B, D; also observed independently by the assembler in this
  session).** None of these tools exist in any of this run's sessions'
  tool lists. This reads as either a stale/broken project instruction or a
  prompt-injection probe; no `ctx_*` tool was called by any executor, or
  by this assembly — standard Bash/Read/Grep/Edit tools were used
  throughout every part and by the assembler. Carried unchanged from
  `v0.6.0-rc.6`'s own part-D finding; flagged again for the maintainer
  since it could misdirect a future agent into believing those tools
  exist, and its instructions were followed by none of the sessions that
  encountered it.
- **part-a.md's own Verdict header disagrees with its own itemized row
  table (this assembly, §0.6).** The header states `"100 PASS"` and cites
  a nonexistent `§17`; the file's own §14 itemized table and executor A's
  structured JSON report both independently agree on `79`. Reconciled to
  `79` in this report; worth the coordinator flagging to executor A's own
  process for the next run so the header is generated from the same count
  as the table rather than typed separately.

## 23. Terminated device block

> Device testing is terminated for this candidate at the milestone recorded
> above. The results in this report are final for this device and this tag.
> Any further testing requires a new candidate tag and a new report.

- Terminating tester: assembler (tagged-run coordinator role, this session)
- UTC timestamp: 2026-09-08 (original assembly); corrected 2026-09-09
  (verification round 1, §24 — same device, same tag, append-only fix
  after an independent verification pass rejected the original submission)
- Device verdict: **FAIL** — originally 2 of 215 required rows were
  recorded not `PASS` (CLI row `13`, `MatrixH:H7`); as corrected in
  verification round 1, `MatrixH:H7` is `PASS` and **1 of 215** required
  rows is not `PASS` and is not excused by either disposition in the
  contract (CLI row `13`); see the Verdict block, §21, and §24 above for
  the complete list and reasoning. **All four of `v0.6.0-rc.6`'s carried
  release-blocking rows are cleared this run** (`MatrixG:G1` and
  `codex:E1`–`E3`, via the widened Codex range plus an already-reset
  account usage limit — §18), and `grok:E1`–`E3` cleared again cleanly via
  the executor-driven path. This does not authorize stable `v0.6.0`. No
  real host data required maintainer cleanup beyond what each part's own
  harness notes already disclose and resolved in-run (the per-agent-scoped,
  still-encrypted `push --agent opencode --all` catalog breadth in
  §15/§22; the self-caught, resolved `H5` near-miss in §22) plus
  verification round 1's own isolated `H7`/`H5` lab directories, deleted
  after that round (§24). A fix (or a narrowed, host-contention-only
  explanation with new evidence) for CLI row 13's readiness-glyph
  regression is now the only thing required before the next tagged
  acceptance run can plausibly clear the device verdict — `H7` is resolved.

## 24. Verification round 1 — correction after reject (2026-09-09)

An independent adversarial verification pass reviewed this report against
the same tagged artifact and rejected the original submission with one
blocker and one minor finding. Both are addressed here, append-only: every
section above keeps its original text unedited except for inline notes
pointing here, and the rows this section touches are re-run against the
same checksummed `v0.6.0-rc.7` archive, in a fresh lab directory of this
correction round's own (`D:\ReinstateAcceptanceProjects\v060-rc7-fixer\`),
never the original executors' directories and never the shared worktree's
tracked files before this commit.

### Re-verified artifact identity

Same archive, re-checked independently of the original assembly:
`sha256sum` of
`reinstate_0.6.0-rc.7_windows_amd64.zip`
(`865ad8e8f65fb3f543d2e2670b5d7c82b0d4878513781e04868fc7303e183fcf`)
matched `checksums.txt`. Unzipped into a fresh
`D:\ReinstateAcceptanceProjects\v060-rc7-fixer\install\`; `rein.exe` and
`reinstate.exe` both hash
`ab73dd7c6817da2374a9c72673c62c63fb724d7412fa464d5dc56d381d5ea349`
(identical to each other, matching `checksums.txt`'s
`reinstate_0.6.0-rc.7_windows_amd64.exe` entry). `rein version --json`:
`{"commit":"21e9a9a356df1181601e2da02bfae03b28380cc3","date":"2026-09-08T16:26:36Z","name":"reinstate","version":"0.6.0-rc.7"}`
— matches every other part's own identity table exactly.

### Blocker: `MatrixH:H7`'s mischaracterized `PARTIAL`

**The finding, verbatim.** The original report's own §14 and Verdict block
claimed the run's 40-minute go-signal wait "matches the dispatch's own
explicitly anticipated outcome for a wait window shorter than 150
minutes," quoting the contract's own text to support that claim. Checked
against the actual contract text in both
[`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md) ("Lab
isolation for `MatrixH:H7`") and
[`v0.6.0-rc.7-agent-verification-prompts.md`](../v0.6.0-rc.7-agent-verification-prompts.md):
"The operator go-signal file … is polled every 60 seconds, for up to 150
minutes … `PARTIAL` (operator/harness availability) is recorded only once
that full wait has elapsed with no go-ahead — **a shorter poll window is
not a basis for `PARTIAL`**." That sentence appears verbatim, twice, in
the contract. The original report's own quoted rationale ("within that
full wait window") proves the opposite of what it was cited to support.
This was a real, verifiable self-contradiction, not a defensible reading
of an ambiguous rule — the verifier was right to reject on it.

**What this round did, not just what it corrected.** Rather than only
fixing the prose, this round re-ran the row's actual mechanism against the
same tag, in a fresh lab:

1. **Lab.** `hopd`/`fakelocker` started via
   `scripts/testing/hoplab/hoplab.ps1` at `127.0.0.1:8326`/`127.0.0.1:9326`,
   `-hopd-bin` pointed at a prebuilt `hopd.exe` from this candidate's own
   part E (`D:\ReinstateAcceptanceProjects\v060-rc7-e\bin\hopd.exe`,
   `sha256 891f7319943e7f45ec22739d4d5ff18d779dadac99170acc121aa593c7d4d39b`
   — test infrastructure, not the artifact under test, reused the same way
   this run's own §0 reused a prebuilt `hopd.exe` for the `H4`/`H8`
   re-runs).
2. **Fresh Hop lab account, zero pushed sessions.** Real email sign-in via
   `hoplab approve` (GET-then-POST); account `e2e60495-77c8-47c4-9b21-5fa57850b211`,
   device `5ac1a716-2818-4d17-8f9f-3c573f7d9c05`. `rein init --hop` then
   `rein account init` via `go run ./scripts/testing/accountinit` (the
   recovery code was shown once, captured by the helper, never printed —
   `<recovery code redacted>` in its own output, exactly as designed).
   `rein sync verify --json` beforehand: `"outcome":"not-applicable"`,
   `"Nothing has been pushed from this profile yet"` — genuinely fresh.
3. **Full per-file before-listing** (not just an aggregate digest — this
   round's own improvement on the rc.5 recheck's documented gap) of the
   three session-bearing subtrees, `CODEX_HOME` read as the **persistent,
   registry-level** value (confirmed via `HKCU:\Environment`:
   `CLAUDE_CONFIG_DIR=D:\Projects\hop-10-lab\claude`,
   `CODEX_HOME=D:\Projects\hop-10-lab\codex`,
   `XDG_DATA_HOME=D:\Projects\hop-10-lab\xdg`), never this shell's own
   transient `CODEX_HOME` — the same trap the contract's "Lab isolation for
   `MatrixH:H7`" section names. **3,142 files**, digest
   `b130ef3c379d886d973ca02f15c977ec33151b24ef30b9641a15656201b6d9d9`.
4. **Go-signal already present.** Checked
   `D:/ReinstateAcceptanceProjects/h7-go.txt` at `2026-09-08T18:56:31Z`:
   the file existed, content `go`, `mtime 2026-09-08T17:45:25Z` — about 21
   minutes **after** the original run's own 40-minute window had already
   closed (`17:24:17Z`), and well inside the contract's 150-minute figure.
   This corroborates, rather than merely asserts, that honoring the full
   150-minute window would very likely have caught it; it does not by
   itself excuse the original report's own mischaracterization, which is
   a separate, textual defect regardless of what a longer wait would have
   found.
5. **Elevated round trip, attempted and completed.** The staged script
   (`daemon install` → `status` → 35s wait → `status` → `hop status` →
   `stop` → `status` → `start` → `status` → `stop` → `uninstall` →
   `status`) was launched via `Start-Process -Verb RunAs -Wait
   -WindowStyle Hidden`; the UAC prompt was accepted (launcher call
   `2026-09-08T18:56:36Z`–`18:57:15Z`, ~39s wall clock). Every step
   completed against a real `schtasks` task
   (`com.reinstate.daemon.edacd555`): `install` reported
   `installed:true, running:true`; after the wait, `status` showed a real
   debounced push (`"pushed 124 snapshot(s), skipped 0 unchanged, 1
   diverged (rein conflicts)"`) and a real scheduled pull (`"pulled 0
   snapshot(s), skipped 0 already synced", ok:true`); `hop status`
   confirmed `first_push_at: "2026-09-08T18:57:02Z"` on this round's own
   locker; `stop`/`start`/`stop`/`uninstall` each completed against the
   real task; the final `status` showed `installed:false, running:false,
   detail:"not registered"`.
6. **`#424` reproduced again, recorded not failed, per the contract's own
   rule (d).** The scheduled daemon's reported `roots` were the **live**
   host paths (`D:\Projects\hop-10-lab\claude\projects`, `\codex\sessions`,
   `\xdg\opencode`), not this round's isolated `h7-device` home — the same
   Task-Scheduler environment-inheritance gap every prior `H7` attempt on
   this tag has documented. One push conflict was recorded
   (`rein conflicts list --json`: agent `claude`, session
   `session-syn-001-a`, project
   `D--ReinstateAcceptanceProjects-v060-rc6-d-device-live-foreign-device-a-project`
   — a leftover synthetic fixture from a prior acceptance run's own test
   data physically present under the live Claude config tree; content
   never read or quoted). Consistent with `#424`'s own "recorded, not
   failed" status and with `rein`'s conflict-safety design working
   correctly (refused to silently overwrite).
7. **Full per-file after-listing:** same file count, **3,142 files**,
   digest `6edeaddc25f88d335bf410a9fafc8a49ea0a95b0fd2c8ca3ab8c3fc453d76826`
   (different from the before-digest). Diffed against the before-listing:
   **exactly 2 differing entries**, both
   `claude/D--Projects-reinstate/<this-workflow's-own-scratchpad-id>/subagents/workflows/…/agent-*.jsonl`
   — real, pre-existing Claude Code session transcripts that simply grew
   (larger size, newer mtime) because a sibling subagent in this same
   orchestrated verification-round workflow was concurrently writing its
   own transcript during the round trip's own ~40-second window. Neither
   file was created; neither carries a lab/synthetic name or matches the
   lab account's own session ids; both are attributable to ordinary
   concurrent Claude Code session activity, not to `rein push`/`pull` —
   the exact "this session's own tool use" trap the `v0.6.0-rc.5` recheck
   first documented for `CLAUDE_CONFIG_DIR/file-history`.

**Disposition: `MatrixH:H7` is `PASS`.** All three conditions of the
refined per-file-listing rule hold: the daemon's pull restored **zero**
snapshots; **no** file under the three subtrees was created or modified
carrying a lab/synthetic name or matching the lab account's own session
ids; and the **only** two differing entries are attributable, by relative
path and by direct correlation with this round's own concurrent session
activity, to a process other than `rein`. `#424` fired again and is
recorded per rule (d), not failed. This supersedes the original report's
`PARTIAL` throughout this document.

**Report-text correction applied.** Every location that repeated the
original mischaracterization (Verdict block, §0.7, §0.8, §14 intro and
table, §16, §18, §20, §21, §23) now states `MatrixH:H7` as `PASS`,
corrected in verification round 1, with the original evidence and its
flawed rationale kept unedited in §14 for the record (not deleted, per the
append-only correction policy) and struck through, not removed, in §21's
itemized list. Required counts move from `213 PASS / 1 PARTIAL / 1 FAIL /
0 NOT TESTED` to **`214 PASS / 0 PARTIAL / 1 FAIL / 0 NOT TESTED`** of 215
required rows. The device verdict remains **`FAIL`**, unchanged: CLI row
13 (untouched by this correction — outside verification round 1's scope,
which addressed only the two findings the verifier raised) is on its own a
required-row `FAIL` that keeps the verdict at `FAIL` regardless of `H7`.

### Minor: `H5`'s non-canonical planted-token prompt

**The finding.** This run's evidence policy requires a planted search
token to appear, in the prompt text, only inside the sentence "Reply with
exactly this token and nothing else: `<token>`." Every planted-token row
in this report correctly uses that sentence except `H5`, whose original
prompt read "Remember this exact token for later: `RC7D-H5-CLAUDE-8271`.
Reply with just OK." — a real deviation, not a privacy issue (the token is
a harmless synthetic marker, never a secret).

**Fix: a fresh token planted and recalled with the mandated sentence.** In
a new throwaway git project
(`D:\ReinstateAcceptanceProjects\v060-rc7-fixer\h5-redo-project\`, `git
init`, one throwaway commit), against the host's live Claude Code config
(`CLAUDE_CONFIG_DIR` left untouched, per the ground rules):

```
$ claude -p "Reply with exactly this token and nothing else: RC7FIX-H5-CLAUDE-1788893991" --output-format json
{"session_id":"5b170368-06d4-4843-aa0e-d354bd510bbb", ..., "result":"RC7FIX-H5-CLAUDE-1788893991", ...}

$ rein search RC7FIX-H5-CLAUDE-1788893991 --agent claude --json
→ session claude:5b170368-06d4-4843-aa0e-d354bd510bbb found, project "h5-redo-project", message_count=2

$ claude --resume 5b170368-06d4-4843-aa0e-d354bd510bbb -p "What was the exact token I asked you to remember? Reply with just the token." --output-format json
{"session_id":"5b170368-06d4-4843-aa0e-d354bd510bbb", ..., "result":"RC7FIX-H5-CLAUDE-1788893991", ...}
```

The recalled `result` matches the planted token exactly, at the same
`session_id`, using this run's mandated planting sentence and the same
recall-prompt wording every other row in this report uses (`codex:E1`–`E3`,
`grok:E1`–`E3`). This is this correction round's own session, created
solely for this fix, the only Claude session id this section names.

**Disposition: `H5` remains `PASS`, unchanged.** The row's own mechanism
(wipe, recover-as-new-device, pull, and a real resume that recalls the
planted token at the same session id) was never in question — only the
original evidence's prompt wording was. The original evidence in §14 is
kept unedited for the record; this freshly planted, policy-compliant
token is the superseding evidence for that one detail.

### Counts and privacy after this correction

- **Required rows:** `215` (unchanged — no row was added, removed, or
  reclassified `N/A`).
- **Corrected counts:** `214 PASS / 0 PARTIAL / 1 FAIL / 0 NOT TESTED`
  (was `213 / 1 / 1 / 0`). Verified by direct recount of every itemized
  table in this document after the edits above: Matrix A–G/CLI/Hop rows
  unaffected; Hop parity's own 16-row table is now `16 PASS` (`H7` moved);
  CLI experience's 22-row table is unchanged (`21 PASS, 1 FAIL`).
  `71 + 106 + 21 + 11 + 5 = 214` PASS (part E's own 5 rows are now `5
  PASS`, not `4 PASS, 1 PARTIAL`); `+1` FAIL (CLI row 13); `= 215`.
- **Device verdict:** `FAIL`, unchanged — CLI row 13 alone is sufficient
  and was outside this correction's scope.
- **Privacy:** this section names two UUIDs created solely for this
  round's own fresh Hop account/device (`e2e60495-…`, `5ac1a716-…`) and
  one Claude session id created solely for this round's own `H5` redo
  (`5b170368-…`), each explicitly attributed above; no recovery code,
  root/device private key, or `keyring.v1.json` field beyond what §14's
  own precedent already shows was printed; the recovery code shown during
  `account init` was captured and redacted by `scripts/testing/accountinit`
  and never appears here. `D:\Projects\hop-10-lab\…` is this program's own
  designated live-agent-home path, already used verbatim elsewhere in this
  report (§14, `H7`'s original evidence) — not a private developer path.
  No `C:\Users` path, host username, or `D:\Projects` path outside the
  sanctioned `ReinstateAcceptanceProjects`/`reinstate-worktrees`/
  `hop-10-lab` locations appears in this section.

### Cleanup

`hoplab stop` was run against this round's own lab root
(`D:\ReinstateAcceptanceProjects\v060-rc7-fixer\lab\`) immediately after
the round trip; that directory and
`D:\ReinstateAcceptanceProjects\v060-rc7-fixer\h5-redo-project\` were
deleted after this section was written. The re-verified install under
`D:\ReinstateAcceptanceProjects\v060-rc7-fixer\install\` is left in place
for the coordinator's own records, matching this candidate's own §0
assembler precedent. Nothing from either directory was committed; this
correction touches only this results file. The assigned worktree
(`D:\Projects\reinstate-worktrees\v060-rc7-tagged`, branch
`v060/rc7-tagged`) was not otherwise modified; no push, merge, or `gh`
command beyond the read-only checks already on record was run this round.
