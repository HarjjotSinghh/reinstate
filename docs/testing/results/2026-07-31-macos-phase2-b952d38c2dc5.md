# Phase 2 acceptance — macOS Device A report

**Verdict:** `PASS` (device-local; Phase 2 status pending cross-device reconciliation)
**Milestone:** `DEVICE_COMPLETE`
**Required counts:** `30 PASS / 0 PARTIAL / 0 FAIL / 0 NOT TESTED`
**Optional physical counts:** `0 PASS / 2 NOT TESTED`

This report covers only the exact disposable sessions and paths created for
this run. No real transcript content or secret was used as evidence.

## 1. Test record

| Field | Value |
| ----- | ----- |
| UTC date/time | 2026-07-31T14:35Z – 2026-07-31T15:25Z |
| Device | `macOS Device A` |
| Tested Git commit | `b952d38c2dc57b0a96bc696860318ea7c2975800` |
| Signed tag, if any | none (development acceptance, not a published release) |
| Reinstate version JSON | `{"commit":"b952d38","date":"2026-07-31T14:37:20Z","name":"reinstate","version":"v0.1.0-39-gb952d38"}` |
| OS/version/build | macOS 26.5.2, build 25F84 |
| Architecture | arm64 |
| Native shell | zsh 5.9 (arm64-apple-darwin25.0) |
| Claude Code version/state | 2.1.220, installed |
| Codex CLI version/state | codex-cli 0.146.0, installed |
| Gemini CLI version/state | `NOT_INSTALLED` (no `gemini` executable on PATH) |
| OpenCode version/state | `NOT_INSTALLED` (adapter warning `agent_not_installed`) |
| Git version | git 2.55.0 |
| Go version | go1.26.5 darwin/arm64 |
| Report branch | `test/phase2-b952d38c2dc5-macos-report` |
| Draft PR | see section 13 |

## 2. Provenance and repository hygiene

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Tested commit matches the requested commit | `PASS` | worktree `HEAD` = `b952d38c2dc57b0a96bc696860318ea7c2975800` = `EXPECTED_TEST_COMMIT` |
| Binary reports the tested commit | `PASS` | `rein version --json` → `commit=b952d38`, `version=v0.1.0-39-gb952d38`; `git rev-parse b952d38` resolves to the full tested SHA |
| Product tree was clean before testing | `PASS` | `git status --porcelain` empty in the fresh worktree |
| Report branch starts at the tested commit | `PASS` | branch created with `git worktree add -b … b952d38c2dc5…` |
| Report is the only committed change | `PASS` | staged diff = 1 added file under `docs/testing/results/` |
| No secret/transcript/private path was committed | `PASS` | report contains only versions, composite references, counts, booleans, exit codes, bounded markers, and `~`-redacted relative paths |

Commit reachability: `TEST_COMMIT` is reachable from
`origin/fix/opencode-top-level-timestamps` (`git merge-base --is-ancestor` → true).
It is **not** reachable from `origin/main` (expected for this development run;
main reachability is not required).

Tag state: no annotated tag points at `TEST_COMMIT`, so this is **development
acceptance built from source**. The public `v0.1.0` installer was not used as
Phase 2 evidence.

## 3. Isolation and local-only proof

Record booleans and redacted relative paths only.

| Assertion | Result | Evidence |
| --------- | ------ | -------- |
| Fresh isolated `REINSTATE_HOME` | `PASS` | `~/.reinstate-phase2-acceptance-b952d38c2dc5` proven absent before the first command |
| No `rein init` run | `PASS` | no `init`/`push`/`pull`/`status`/`diff`/conflict command was executed at any point |
| No `config.toml` or sync state created | `PASS` | post-run home contains exactly `cache/` and `cache/session-index-v1.sqlite`; `config.toml`, `state.json`, `backups/` all absent |
| No credential/passphrase/keyring request | `PASS` | no command prompted for setup, storage, credential, passphrase, or keyring access |
| No backend/network dependency | `PASS` | every Phase 2 command succeeded configless and offline-capable; no storage or remote object involved |
| Only derived index state created | `PASS` | full `find` of the home lists only the two derived-cache entries above |
| Index and parent permissions are owner-only | `PASS` | home `drwx------`, `cache/` `drwx------`, `session-index-v1.sqlite` `-rw-------` |

## 4. Controlled corpus

| Agent | Composite reference | Disposable project label | Marker found | Capability |
| ----- | ------------------- | ------------------------ | ------------ | ---------- |
| Claude | `claude:75e29698-ad6a-494f-b4b2-113db3f5583f` | `alpha` | `…-MACOS-CLAUDE-A1`, `…-A1B`, `…-R2` | full |
| Claude (new session) | `claude:e005a7a8-4bf4-421a-8ffa-1f61e8896fce` | `alpha` | `…-MACOS-CLAUDE-A2` | full |
| Claude (fork of A1) | `claude:a05fe06f-82d8-4703-80da-b672db1e09b6` | `alpha` | `…-MACOS-CLAUDE-F1` | full |
| Codex | `codex:019fb89d-33bb-7482-8b1d-79bdc011bef7` | `phase2 … unicode-β` | `…-MACOS-CODEX-A1`, `…-R1` | full |
| Codex (fork) | `codex:019fb8b9-cf7f-7671-b8c7-8eb49f4547b5` | `phase2 … unicode-β` | `…-MACOS-CODEX-F1` | full |
| Gemini | n/a | n/a | n/a | not installed |
| OpenCode | n/a | n/a | n/a | not installed |

Disposable Git projects: branch `phase2/b952d38c2dc5-alpha` with
`src/phase2_b952d38c2dc5_alpha.txt`, and branch `phase2/b952d38c2dc5-beta-β`
with `src/phase2_b952d38c2dc5_beta_β.txt`. Native IDs were identified from
before/after vendor metadata diffs and exact controlled marker presence only.
No vendor session file was altered by hand at any point.

## 5. Automated verification

All gates were run from the tested commit in the clean worktree.

| Gate | Result | Sanitized evidence |
| ---- | ------ | ------------------ |
| Focused OpenCode regression | `PASS` | `go test ./internal/sessionindex -run OpenCode -count=1` → exit 0; 6 tests ran, including `TestOpenCodeRecordSupportsTopLevelTimestamps` and `TestOpenCodeTopLevelTimestampRemainsVisibleUnderDefaultLimit` |
| Focused session-index/CLI/adapter tests | `PASS` | `go test ./internal/sessionindex ./internal/cli ./internal/adapter/...` → exit 0 |
| Full Go test suite | `PASS` | `go test ./...` → exit 0 |
| Race suite | `PASS` | `go test -race ./...` → exit 0 |
| Vet / `make verify` | `PASS` | `make fmt-check` exit 0; `make vet` exit 0; `make verify` (complete merge gate: fmt-check, vet, lint, test, test-race, vuln) exit 0 |
| Required cross-builds | `PASS` | `CGO_ENABLED=0 go build ./cmd/reinstate` exit 0 for `windows/amd64`, `darwin/arm64`, `darwin/amd64`, `linux/amd64` |
| Phase 1 regression | `PASS` | Phase 1 unit, fixture, CLI, path-map, encryption, conflict, and restore-safety tests are inside `go test ./...`, `-race`, and `make verify`, all exit 0 |

No gate was skipped or interrupted. There were no failing commands.

Targeted deterministic gates re-run and confirmed individually:
`TestMissingAndAmbiguousReferencesNeverLaunch`,
`TestNativeLaunchRefusalsUseStableCodes`,
`TestNativeChildFailurePropagates`,
`TestPlanLaunchRejectsReadOnlyAndMissingWorkspace`,
`TestRunLaunchUsesInjectedStructuredPlanAndPropagatesFailure`,
`TestStoreRebuildsCorruptAndIncompatibleDerivedState`,
`TestPhase2MacOSWindowsAndWSLFixtures`,
`TestNativeClaudeLaunchThroughWindowsCommandShim`,
`TestGeminiSourceAppliesMetadataAndRewindReadOnly` — all `PASS`.

## 6. Configless index and refresh

- `rein sessions` succeeded from the fresh configless home (exit 0) and
  discovered both exact controlled references on the first run.
- **Alias parity:** `rein sessions --json` and `reinstate sessions --json`
  captured at the same instant are field-for-field identical across all
  records. A first naive comparison showed a byte difference; repeated
  interleaved runs isolated it to exactly one record — a live, non-controlled
  Claude session whose `message_count`/`updated_at`/`size_bytes` advance
  between invocations because its vendor is actively writing. `rein` vs `rein`
  shows the same drift, so this is vendor liveness, not binary-name divergence.
  Record ordering was identical across all four captures.
- **Ordering:** verified programmatically as `updated_at` descending, then
  agent, then native ID.
- **Stable identity / no duplicates:** composite keys are unique across the
  full 102-record listing; the controlled Claude session kept one stable
  composite identity across append and resume.
- **Idempotency:** two consecutive no-change refreshes produced identical
  record counts, identical key order, and byte-identical controlled records.
- **Corrupt index rebuild:** the derived SQLite file was overwritten with
  38 bytes of non-SQLite data; the next `rein sessions --json` returned exit 0,
  rebuilt the index, restored all records including both controlled
  references, emitted no error output, and preserved `-rw-------`.
- **`rein list` compatibility:** `rein list` is **not** redefined as an alias of
  `rein sessions`. It keeps distinct Phase 1 help text (`List local agent
  sessions` vs `List indexed local coding-agent sessions`), a distinct flag set
  (`--agent claude|codex|all` vs `--agent claude|codex|gemini|opencode|all`,
  no `--limit`), and distinct tab-separated output. Outputs are not identical.
- **Append refresh:** after a vendor-native append to the controlled Claude
  session, the new marker became searchable and `message_count` advanced 2 → 4.
- **New-session refresh:** the newly created controlled session became the
  newest record among the controlled corpus and was discovered without any
  extra flag.
- **No vendor mutation:** size, mtime, and SHA-256 of both controlled vendor
  source files were byte-identical before and after running `sessions`,
  `search`, and `inspect`.

## 7. Search and inspect

All searches ran configless; matching is literal, case-insensitive, and
performed locally with no network or embedding service.

| Dimension | Result | Evidence |
| --------- | ------ | -------- |
| Prompt fragment (literal) | `PASS` | controlled Claude marker → exactly 1 match, the correct composite reference |
| Case-insensitive | `PASS` | lowercased marker → same single correct match |
| AND terms | `PASS` | marker + filename term → 1 match; marker + non-matching term → 0 matches |
| Agent filter | `PASS` | `--agent claude` → 1; `--agent codex` on the Claude marker → 0; `--agent codex` on the Codex marker → 1 |
| Project filter | `PASS` | matches on project basename (`alpha`), on a workspace fragment, and case-insensitively; a wrong fragment → 0 |
| Branch filter | `PASS` | `phase2/b952d38c2dc5-alpha` → Claude record; `phase2/b952d38c2dc5-beta-β` → Codex record; crossed filters → 0 |
| File filter | `PASS` | exact and partial fragments of the recorded relative file → the correct session; a non-existent file → 0 |
| Limit | `PASS` | `--limit 1` returned 1 of 2 otherwise-matching controlled records |
| Unicode | `PASS` | a query containing `β` matched the Unicode-project Codex record |
| Zero match | `PASS` | deliberate zero-match query → exit 0 with an empty result set, not an error and not a fallback to all sessions |

Spaces, Unicode, and the disposable Unicode workspace name were handled as
literal query and path data; no shell command string is constructed inside
Reinstate (proved separately by the argv-array launch evidence in section 8).

**Preview policy.** `sessions` and `search` emit only bounded metadata rows
(agent, project, native ID) and never a transcript passage. `rein inspect`
(human and `--json`, for both controlled references) returned identity, agent,
title, project, workspace, branch, timestamp, message count, file references,
and capability metadata. `prompt_preview` is exactly 160 Unicode code points,
truncated, sourced only from the user-authored prompt, with whitespace
collapsed and no terminal control characters. Every string field in both JSON
inspect payloads was ≤160 code points and control-character-free. No assistant
reasoning, assistant message, tool output, environment dump, or auth content
appeared in any output.

**Note on file references.** The first two controlled sessions used
deliberately inert "do not act" prompts, so the vendors recorded no file
references and the `files` field was absent — an artifact of the controlled
prompt, not a product defect. A third controlled Claude session that actually
read the disposable fixture file recorded
`…/alpha/src/phase2_b952d38c2dc5_alpha.txt`, and the `--file` filter matched it
on both exact and partial fragments.

**Oversized-record handling on real data.** Indexing emitted bounded, sanitized
`oversized_record` warnings (agent, session id, code, ≤51-character message)
for pre-existing large vendor records and continued indexing healthy sessions
normally — a physical confirmation of the bounded-field/oversized-record gate.

## 8. Last, resume, and fork

**Dry-run plans** (captured before every real launch; all exit 0, argv is a
JSON array rather than a shell string, and `cwd` is the recorded workspace):

| Command | executable | args | cwd |
| ------- | ---------- | ---- | --- |
| `resume claude:75e29698…` | `claude` | `["--resume","75e29698-ad6a-494f-b4b2-113db3f5583f"]` | `~/…-b952d38c2dc5/alpha` |
| `resume codex:019fb89d…` | `codex` | `["resume","019fb89d-33bb-7482-8b1d-79bdc011bef7"]` | `~/…-b952d38c2dc5/phase2 b952d38c2dc5 unicode-β` |
| `fork claude:75e29698…` | `claude` | `["--resume","75e29698-ad6a-494f-b4b2-113db3f5583f","--fork-session"]` | `~/…-b952d38c2dc5/alpha` |
| `fork codex:019fb89d…` | `codex` | `["fork","019fb89d-33bb-7482-8b1d-79bdc011bef7"]` | `~/…-b952d38c2dc5/phase2 b952d38c2dc5 unicode-β` |

These match the runbook's expected plan tables exactly. Vendor source
fingerprints were unchanged across all dry runs.

**`last`.** `rein last --dry-run --json` selected the most recently updated
resumable session and emitted a correct Claude plan. `--agent claude` selected
the newest Claude session. `--project` scoped correctly: the Unicode project
fragment selected the Codex record, and `alpha` selected the newest Claude
record in that project.

**Exact launch delivery and non-mutation.** Recording executor stubs on `PATH`
proved that `rein resume`/`rein fork` physically deliver the exact executable
name, exact argv array, and exact recorded workspace (including the Unicode
workspace with spaces) for all four operations, and that both controlled vendor
source files were byte-identical (SHA-256) before and after — so Reinstate
performs no mutation of its own before launch.

**Claude native resume:** `rein resume claude:75e29698…` through a real PTY
started Claude Code in the recorded workspace, returned the exact controlled
challenge response `…-MACOS-CLAUDE-R2`, and resumed **in place** — no new
session file was created, and the vendor appended to the same source
(14 → 28 records) after launch. The appended marker was searchable on the next
refresh, with `message_count` advancing to 9.

**Codex native resume:** `rein resume codex:019fb89d…` through a real PTY
started Codex in the recorded Unicode workspace and persisted the exact
controlled challenge response `…-MACOS-CODEX-R1` as an `assistant`-role
message in the same rollout file (verified by bounded role-field inspection,
not by reading transcript prose). The vendor's own exit banner confirmed the
same native ID.

**Claude vendor-native fork:** `rein fork claude:75e29698…` produced a distinct
new native identity `claude:a05fe06f-82d8-4703-80da-b672db1e09b6`. The fork
inherited the source history (original `…-A1` marker present) and contains the
new `…-CLAUDE-F1` marker; the source contains **zero** occurrences of `…-F1`
and its SHA-256 was unchanged. Refresh discovered both source and fork, and the
fork produced its own valid independent resume plan.

**Codex vendor-native fork:** `rein fork codex:019fb89d…` produced a distinct
new native identity `codex:019fb8b9-cf7f-7671-b8c7-8eb49f4547b5` with the same
properties: source history inherited, `…-CODEX-F1` present only in the fork,
source SHA-256 unchanged, both discovered by refresh, fork independently
resumable.

**Safety and error gates** (all fail before any launch):

| Gate | Exit | Behaviour |
| ---- | ---- | --------- |
| `resume --json` without `--dry-run` | `2` | `--json requires --dry-run for native agent launches` |
| `fork --json` without `--dry-run` | `2` | same refusal |
| `last --json` without `--dry-run` | `2` | same refusal |
| Missing reference | `2` | `session not found: claude:00000000-…`, no launch |
| Unknown agent prefix | `2` | `session not found: …`, no launch |
| Unique bare native ID | `0` | resolved to the correct composite reference |
| Missing executable (`PATH` without `claude`) | `5` | `native agent executable is unavailable: …`, no launch |
| Missing workspace (disposable project temporarily renamed) | `5` | `recorded session workspace is unavailable: …`, no launch; workspace restored afterwards |
| Child failure propagation (stub exits 7) | `1` | failure propagated as `ExitRuntime` |
| Ambiguous bare ID | n/a | covered by the deterministic injected gate `TestMissingAndAmbiguousReferencesNeverLaunch`; manufacturing it physically would require altering a real vendor file, which the runbook forbids |

Child-failure note: the child's raw exit status is intentionally **not** passed
through. Reinstate maps it onto its stable code scheme (`Runtime = 1`), because
raw pass-through would collide with Reinstate-defined codes such as
`Safety = 7`. This matches the product contract asserted by
`TestNativeChildFailurePropagates`. Recorded as non-blocking.

## 9. Interactive switcher

Driven through a real macOS PTY (`pty.fork`), for **both** binary names
`rein` and `reinstate`, which behaved identically.

| Action | Result | Evidence |
| ------ | ------ | -------- |
| Bare invocation on a TTY | `PASS` | refreshes and shows the numbered local switcher with the prompt `Choose NUMBER, /text, i NUMBER, f NUMBER, or q:` |
| `/text` filter | `PASS` | filtering on the run ID narrowed the listing to only matching controlled sessions |
| `i NUMBER` inspect | `PASS` | same bounded metadata policy as `rein inspect`, 160-code-point user-prompt preview, then returns to the filtered list |
| `NUMBER` resume | `PASS` | launched the exact displayed composite reference with correct vendor, argv, and recorded cwd |
| `f NUMBER` fork | `PASS` | launched the exact displayed composite reference with the vendor's fork argv |
| Invalid input | `PASS` | `zzz-invalid-input` → `Enter a number, /text, i NUMBER, f NUMBER, or q.`, list redisplayed, no default session chosen, recoverable |
| Out-of-range number | `PASS` | `999` → same recoverable rejection, no default session chosen |
| `q` cancel | `PASS` | exits 0 without launching or mutating |
| EOF (Ctrl-D) | `PASS` | exits 0, no launch, vendor sources unchanged |
| Interrupt (Ctrl-C) | `PASS` | terminates on SIGINT, no launch, vendor sources unchanged |
| Unrelated-transcript privacy | `PASS` | picker rows carry only agent, project label, and native ID; no transcript body was displayed |

Picker resume/fork targeting was verified with recording executor stubs so the
exact delivered reference could be asserted without starting extra vendor
processes; vendor source SHA-256 was unchanged across those runs.

**Non-TTY invocation:** piping empty stdin to `rein` and to `reinstate` exits
`2` immediately (measured 0.03 s — no hang, no silent selection of the newest
session) and prints the required hint:
`interactive session picker requires a terminal; use `rein sessions --json``.

## 10. Read-only adapters

Neither optional vendor is installed on this device, so no vendor was installed
solely for acceptance.

- **Gemini CLI:** `NOT_INSTALLED` — no `gemini` executable on `PATH`.
  `rein sessions --agent gemini --json` returns 0 records. Row 28 is
  `NOT TESTED` for macOS.
- **OpenCode:** `NOT_INSTALLED` — the adapter emits the honest sanitized
  warning `{"agent":"opencode","code":"agent_not_installed","message":"OpenCode
  executable was not found; OpenCode sessions were not indexed"}` and returns 0
  records for `--agent opencode`. Row 29 is `NOT TESTED` for macOS. **Native
  Windows is the physical regression target for the OpenCode top-level
  timestamp fix under test.**

Fixture-backed automated evidence remains green on this device: the six
OpenCode tests (including both top-level-timestamp regression tests),
`TestGeminiSourceAppliesMetadataAndRewindReadOnly`, and
`TestGeminiSourceSupportsLegacyConversationJSON` all pass.

**Read-only refusal (row 30).** With neither vendor installed, the required
exit-`5` contract is proved by the deterministic injected-record CLI gate
`TestNativeLaunchRefusalsUseStableCodes`, which asserts that
`fork gemini:<id> --dry-run` on an injected read-only record returns
`ExitCompatibility` with the read-only reason and launches no process.
`internal/exitcode` pins `Compatibility = 5` (and `Usage = 2`), and
`TestPlanLaunchRejectsReadOnlyAndMissingWorkspace` covers the planner side.
Read-only adapters were never given a fabricated mutation implementation, and
resume/fork/push/pull are not offered as Phase 2 capabilities for them.

## 11. Mandatory matrix

| # | Gate | macOS | Windows |
| - | ---- | ----- | ------- |
| 1 | Exact tested commit/binary provenance | `PASS` (§2) | |
| 2 | Full local verification and required cross-builds | `PASS` (§5) | |
| 3 | Fresh configless home; no `init`, credentials, passphrase, or backend | `PASS` (§3) | |
| 4 | `rein sessions` discovers exact Claude sessions | `PASS` (§6) | |
| 5 | `rein sessions` discovers exact Codex sessions | `PASS` (§6) | |
| 6 | `rein` / `reinstate` JSON parity and deterministic ordering | `PASS` (§6) | |
| 7 | Derived index path, rebuild, idempotency, and private permissions | `PASS` (§3, §6) | |
| 8 | Prompt-fragment literal search | `PASS` (§7) | |
| 9 | Agent filter | `PASS` (§7) | |
| 10 | Project filter | `PASS` (§7) | |
| 11 | Branch filter | `PASS` (§7) | |
| 12 | File filter | `PASS` (§7) | |
| 13 | AND terms, limit, case, Unicode, and zero-match behavior | `PASS` (§7) | |
| 14 | `sessions` and `search` do not dump transcript passages | `PASS` (§7) | |
| 15 | `inspect` metadata/160-code-point user preview policy | `PASS` (§7) | |
| 16 | Append/new-session refresh and no-change idempotency | `PASS` (§6) | |
| 17 | `last` selects the correct resumable session and filters | `PASS` (§8) | |
| 18 | Claude dry-run plan has exact argv/cwd and no mutation | `PASS` (§8) | |
| 19 | Codex dry-run plan has exact argv/cwd and no mutation | `PASS` (§8) | |
| 20 | Claude native resume | `PASS` (§8) | |
| 21 | Codex native resume | `PASS` (§8) | |
| 22 | Claude vendor-native fork, source preserved | `PASS` (§8) | |
| 23 | Codex vendor-native fork, source preserved | `PASS` (§8) | |
| 24 | Missing/ambiguous reference and missing executor fail safely | `PASS` (§8) | |
| 25 | JSON/native-child separation and child failure propagation | `PASS` (§8) | |
| 26 | TTY picker filter, inspect, resume, fork, and cancel | `PASS` (§9) | |
| 27 | Non-TTY prompt failure is immediate and actionable | `PASS` (§9) | |
| 28 | Gemini read-only physical path, when installed | `NOT TESTED` — not installed (§10) | |
| 29 | OpenCode read-only physical path, when installed | `NOT TESTED` — not installed (§10) | |
| 30 | Read-only resume/fork refusal with exit `5` (physical or injected-record gate) | `PASS` — injected-record gate (§10) | |
| 31 | Malformed/concurrent/oversized fixture and privacy gates | `PASS` (§5, §7) | |
| 32 | Phase 1 automated regression remains green | `PASS` (§5) | |

The Windows column is intentionally empty; it is filled only by Device B's own
report and reconciled later.

## 12. Findings

### Release-blocking

None.

### Non-blocking

1. **Child exit code is normalized, not passed through.** A native child that
   exits `7` surfaces as `rein` exit `1` (`ExitRuntime`). This is the
   documented contract — raw pass-through would collide with Reinstate's stable
   codes (`Safety = 7`) — but callers cannot recover the vendor's exact status.
   Worth stating explicitly in user documentation.
2. **`files` is omitted rather than empty in `sessions --json`.** Records with
   no known file references omit the key entirely instead of emitting an empty
   array, which makes naive JSON consumers key-error. Cosmetic; the `--file`
   filter itself is correct.

### Test-harness deviations

1. **Nested-agent environment markers had to be cleared.** Because this run was
   driven from inside a Claude Code session, the launched vendor child
   inherited `CLAUDE_CODE_CHILD_SESSION` (and sibling `CLAUDE_CODE_*` markers)
   and disabled its own transcript saving, printing *"Transcript saving is off
   — inherited CLAUDE_CODE_CHILD_SESSION marker"*. Under that condition the
   Claude fork produced no new session file. Re-running the same `rein` command
   with only those inherited markers unset — no Reinstate flag changed, no
   sandbox or approval control disabled, no vendor file touched — produced the
   correct vendor-native behavior. This is a harness artifact of running
   acceptance inside an agent session, not Reinstate behavior. Both the
   affected Claude resume and fork gates were re-run to completion and are
   reported on the clean-environment runs only.
2. **First Claude PTY launch consumed one scripted input.** Claude Code's
   one-time "do you trust this folder" prompt absorbed the first scripted
   keystroke on the very first launch in the disposable project. That attempt
   was discarded and the gate re-run after the folder was trusted; only the
   completed run is reported.
3. **Codex TUI echoes input character-by-character**, so the challenge marker
   does not appear as a contiguous string in the raw PTY capture. Verification
   used the persisted vendor record (assistant-role field) instead, which is
   stronger evidence.
4. **Ambiguous-bare-ID and read-only exit-`5` gates were satisfied by the
   deterministic injected-record tests**, as the runbook permits, because
   manufacturing them physically would require altering a real vendor session
   file or installing an absent vendor.

No deviation was converted into a `PASS` without re-running the gate to
completion.

## 13. Repository hygiene

- report-only branch: `test/phase2-b952d38c2dc5-macos-report`
- tested base commit: `b952d38c2dc57b0a96bc696860318ea7c2975800`
- changed files: `docs/testing/results/2026-07-31-macos-phase2-b952d38c2dc5.md` (added)
- private/local artifacts excluded: derived index, isolated Reinstate home,
  disposable projects, vendor session files, PTY captures, and build outputs
  all live outside the repository and none were committed
- product code unchanged: `true` (0 product files changed)
- secrets/transcripts committed: `false`

## 14. Device milestone block

```text
PHASE2-DEVICE-REPORT-V1
device=macos
test_commit=b952d38c2dc57b0a96bc696860318ea7c2975800
reinstate_version=v0.1.0-39-gb952d38
report_path=docs/testing/results/2026-07-31-macos-phase2-b952d38c2dc5.md
report_branch=test/phase2-b952d38c2dc5-macos-report
claude_ref=claude:75e29698-ad6a-494f-b4b2-113db3f5583f
codex_ref=codex:019fb89d-33bb-7482-8b1d-79bdc011bef7
gemini_state=NOT_INSTALLED
opencode_state=NOT_INSTALLED
required_pass=30
required_partial=0
required_fail=0
required_not_tested=0
optional_physical_pass=0
optional_physical_not_tested=2
configless_local_only=PASS
preview_privacy=PASS
claude_resume_fork=PASS
codex_resume_fork=PASS
picker=PASS
phase1_regression=PASS
release_blocking_findings=0
product_files_changed=0
secrets_or_transcripts_committed=false
END-PHASE2-DEVICE-REPORT-V1
```

## 15. Final reconciliation block

Not yet applicable. This device has completed its local matrix and is paused
awaiting the complete sanitized native-Windows report. Phase 2 status is
**pending cross-device reconciliation** until the Mac coordinator validates
both reports and adds `PHASE2-FINAL-RECONCILIATION-V1`.
