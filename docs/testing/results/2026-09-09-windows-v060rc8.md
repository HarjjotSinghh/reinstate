# `v0.6.0-rc.8` tagged-artifact native Windows acceptance

`PHASE5-DEVICE-REPORT-V1`

This is the assembled device report for the **published, signed GitHub
prerelease `v0.6.0-rc.8`**, commit `3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f`
(release workflow run `34286767506`). It satisfies
[`../phase-5-universal-agent-coverage-acceptance.md`](../phase-5-universal-agent-coverage-acceptance.md)
as specialized by [`../v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md)
and [`../v0.6.0-rc.8-agent-verification-prompts.md`](../v0.6.0-rc.8-agent-verification-prompts.md).
It merges five executors' part files
(`2026-09-09-windows-v060rc8-part-{a,b,c,d,e}.md`, all against this same
tag) into the one cumulative report the dispatch requires, plus an
assembler's own independent install and matrix-generation check (§0). The
five part files are removed from the tree in the same commit as this file;
their content is preserved here.

**A first attempt at this run on the same artifact (kept as branch
`v060/rc8-tagged-attempt1`, head `df1996a9`) was inconclusive: executor A
had run an older `rein` from the user `PATH`, executor B had no console for
`conptydriver`, and executor E was ended during its `H7` wait; parts C and D
of that attempt passed cleanly and are reused unchanged.**

## Verdict

- **Device verdict:** `PASS`
- **Milestone:** `MATRIX_COMPLETE`
- **Matrix generated by:** `rein doctor --agents --acceptance-matrix --json`
  (re-run independently by the assembler against a fresh install; see §0)
- **Row accounting:** `216` rows total (`178` Phase 5 generated matrix +
  `22` CLI experience + `16` Hop parity journeys) — `1` row is
  `N/A (definitional)` (`opencode:D4`) — **`215` required rows**.
- **`N/A (definitional)` rows:** `1` — `opencode:D4` (OpenCode's
  SQLite-only store has no JSONL record boundary the row's mechanism
  applies to; unchanged since `v0.6.0-rc.1`). Excluded from both the
  denominator and the numerator below.
- **`NOT TESTED (host credential)` rows:** `0`. No row this run required
  this disposition — every optional-agent row with a live host credential
  completed with a real result.
- **Required counts:** `215 PASS / 0 PARTIAL / 0 FAIL / 0 NOT TESTED` of
  `215` required rows.
- **Release-blocking findings:** `0`.

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required
result is `FAIL`. Every one of the 215 required rows is `PASS`, and the one
non-required row (`opencode:D4`) carries its unchanged, contract-recognized
`N/A (definitional)` disposition — so the device verdict is `PASS`. No
product code was changed by this assembly; every result below is
transcribed from the five part files' own evidence plus the assembler's own
independent verification in §0.

**This candidate's own targeted fix — `v0.6.0-rc.7`'s sole carried
release-blocking regression, CLI row 13 — is confirmed fixed.** Under the
dispatch's tightened, expanded method (at least 15 independent `ScopeAll`
launches, every one required to fully match ground truth; `ScopeProject`
confirmed unaffected; a `-stale-claude` settle check confirming deterministic
`○ Blocked` rather than `◌` looping forever), part C found **0 of 15 launches
wrong** — directly reversing `v0.6.0-rc.7`'s tagged `FAIL` (11 of 15 launches
wrong, 73%). See §13 and §18.

**Every other row that cleared at `v0.6.0-rc.7`'s tagged run cleared again
here, unchanged — no regression found anywhere in this run.** This includes
`MatrixG:G1` (both the Claude and Codex halves), `codex:E1`–`E3` (real,
completed conversational turns against `0.153.4`, no account usage-limit
constraint hit), `grok:E1`–`E3` (executor-driven, no maintainer console
needed), and `MatrixH:H7` (the refined per-file-listing rule; `#424` fired
again and is recorded, not failed, per the contract's own rule (d)).

**Positive, non-blocking observation carried through this run.** The host's
live Claude Code has auto-updated to `2.1.265`. The tagged tree at
`3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f` had already widened its own
verified ceiling from `2.1.263` (the dispatch text's anticipated figure) to
`2.1.265` earlier the same day, before this candidate's tag was cut
(`docs/testing/results/2026-09-09-windows-range-widening-claude-v060.md`) —
so every Claude-version row this run is a real, unmodified `PASS`, not an
excused version-drift disposition and not the dispatch's anticipated
fail-closed scenario. See §2, §7, §16.

This report's `PASS` device verdict, zero release-blocking findings, and
§19's `DEFERRED` (never claimed) recording of Apple Silicon macOS satisfy
the Windows-column condition this candidate needs; the stable `v0.6.0`
promotion decision itself is recorded separately in `RELEASING.md`, which
must independently name the deferred-macOS issue
[#403](https://github.com/HarjjotSinghh/reinstate/issues/403) before that
decision is made — this report does not itself make that decision.

## 0. Assembler verification

Performed independently of, and in addition to, all five part files, in the
assembler's own fresh directory
(`D:\ReinstateAcceptanceProjects\v060-rc8-assembler\`), never touching any
part executor's own lab directories or the shared worktree's tracked files
before this commit.

1. **Checksum.** `sha256sum` of the coordinator's staged
   `reinstate_0.6.0-rc.8_windows_amd64.zip` matched `checksums.txt`
   (`658dc27e607fdec14d14156dfe3edaf9d685bfa9add1e467d604ab0fa298e28f`)
   independently.
2. **Install.** Unzipped into a fresh directory
   (`D:\ReinstateAcceptanceProjects\v060-rc8-assembler\install\`), never a
   shared or developer binary. `rein.exe`/`reinstate.exe` `sha256sum`: both
   `482b5de12db1312c1a767bcf40c7999de9f8cef7f47bff5bce802fab960c3b71`,
   matching `checksums.txt`'s `reinstate_0.6.0-rc.8_windows_amd64.exe`
   entry exactly, `cmp` confirms identical to each other.
3. **Version.** `rein version --json`:
   `{"commit":"3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f","date":"2026-09-08T22:38:21Z","name":"reinstate","version":"0.6.0-rc.8"}`
   — matches every part file's own artifact-identity table exactly.
4. **Required row count.** `rein doctor --agents --acceptance-matrix --json`
   against this fresh install: top-level `"row_count": 178`,
   `"core_row_count": 33` (`A:10, B:9, G:8, H:6`), 18 agents at tiers
   `T0×7, T1×4, T2×2, T4×2, T5×3` (`claude, codex, opencode` at T5;
   `grok, qwen` at T4; `gemini, kimi` at T2; `cline, copilot, cursor, pi` at
   T1; `aider, amp, antigravity, minimax-code, openhands, roo, zcode` at
   T0) — matches the dispatch's held census exactly (no tier promotion this
   candidate).
5. **Row-set reconciliation.** Programmatically read the full set of 178
   expected row identifiers from that JSON (33 core rows + each agent's own
   row list: T0×`F1,F2`=14, T1×`C1`–`C6`=24, T2×`C1`–`C6`/`D1`–`D5`=22,
   T4/T5×`C1`–`C6`/`D1`–`D5`/`E1`–`E6`=85) and compared it against the
   union of every row id assigned to part A (core Matrix A/B/G/H, Matrix F
   for all 7 T0 agents, Matrix C for the 4 T1 agents — 71 Phase-5 rows) and
   part B (Matrix C/D for `gemini`/`kimi`, Matrix C/D/E for `claude`/
   `codex`/`opencode`/`grok`/`qwen` — 107 Phase-5 rows): **the two sets are
   set-equal, 178 elements each, zero overlap, zero gap.** Every required
   Phase 5 row appears in exactly one part file.
6. **Part self-report cross-check.** Recounted every part's own row table
   cell-by-cell against its own committed evidence:
   - Part A: 8 gates (not part of the 216) + 71 Phase-5 rows (Matrix A 10 +
     Matrix B 9 + Matrix F 14 (7 T0 agents × F1/F2) + Matrix G 8 + Matrix H
     6 + Matrix C 24 (4 T1 agents × C1–C6)) = **79 rows, all PASS** —
     matches executor A's own structured JSON report (`pass:79, fail:0,
     partial:0, not_tested:0`) exactly, and part-a.md's own Verdict block
     agrees (`79/79 PASS`), with no internal inconsistency this run.
   - Part B: 107 Phase-5 rows (`claude`/`codex`/`opencode`/`grok`/`qwen` ×
     17 each + `gemini`/`kimi` × 11 each), of which `opencode:D4` is
     `N/A (definitional)` — **106 PASS, 1 N/A** — matches its own itemized
     Matrix D table ("34 PASS (required), 1 N/A") exactly, plus the T5
     push/pull round trip (6/6 PASS, beyond the 216). **Note:** part-b.md's
     own top-of-file Verdict block states "107 PASS / 0 PARTIAL / 0 FAIL /
     0 NOT TESTED on the required 107," which loosely folds the `N/A`
     row into the PASS count; the file's own itemized §5 Matrix D table
     correctly separates it as "34 PASS (required), 1 N/A." This report
     uses the itemized figure (`106 PASS, 1 N/A` of 107), which is also
     what the contract's own disposition rule requires. Recorded as a
     report-wording inconsistency in §22, not a scoring discrepancy.
   - Part C: 22 rows, **22 PASS** — matches its own Verdict ("22 PASS / 0
     PARTIAL / 0 FAIL / 0 NOT TESTED").
   - Part D: 11 rows, **11 PASS** — matches ("11/11 required rows PASS").
   - Part E: 5 rows, **5 PASS** — matches ("5 PASS / 0 PARTIAL / 0 FAIL /
     0 NOT TESTED").

   **No correction was needed to any part's counts this run**, beyond
   noting part B's own loose verdict-header wording above (the figure it
   points at — `107` — is right; only whether the `N/A` row is folded into
   "PASS" or kept separate differs, and this report keeps it separate per
   the contract).
7. **Total reconciliation.** `71` (part A) + `107` (part B, including
   `opencode:D4`) + `22` (part C) + `16` (parts D + E: `11` + `5`) =
   **`216`**, matching the dispatch's total row count exactly; minus the
   one `N/A (definitional)` row = **`215`** required. PASS sum: `71` (A) +
   `106` (B) + `22` (C) + `11` (D) + `5` (E) = **`215`**. FAIL: `0`.
   PARTIAL: `0`. NOT TESTED: `0`. `215 + 0 + 0 + 0 = 215`, exactly the
   required count.
8. **Disposition-rule check.** Applied
   [`v0.6.0-windows-acceptance.md`'s "Dispositions that do not block the
   device verdict"](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict)
   rule mechanically to every non-`PASS` row this run recorded.
   - `opencode:D4` — `N/A (definitional)`, unchanged reason (OpenCode's
     SQLite-only store has no JSONL record boundary). Excluded from both
     the denominator and the numerator, as required. Not a disposition on
     a required row.
   - **No other row this run is anything other than `PASS`.** Zero rows
     needed `NOT TESTED (host credential)` or `NOT TESTED (version drift)`
     — every optional-agent row with a live credential completed with a
     real result, and the one apparent version-drift condition (Claude
     Code auto-updated to `2.1.265`) resolved to a real, in-range `PASS`
     because the tagged tree's own verified ceiling had already widened to
     cover it before the tag was cut (§0.9 below re-confirms the tag's own
     commit; §2/§7/§16 carry the detail).
9. **Independent signed-tag and ancestry re-check.** `git verify-tag
   v0.6.0-rc.8`: `Good "git" signature for HarjjotSinghh@gmail.com with
   ED25519 key SHA256:0P4/a2ZBw25hhf0mCN5s2NsUlDiqRqshv8vGNgMPHjU` —
   matches `.github/allowed_signers`. `git rev-list -n 1 v0.6.0-rc.8` peels
   to `3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f`; `git merge-base
   --is-ancestor 3f6aa7b9… origin/main` confirms it is an ancestor of
   `origin/main`. `gh release view v0.6.0-rc.8 --json …` (read-only):
   `isDraft:false, isPrerelease:true, publishedAt:2026-09-08T22:43:53Z,
   tagName:v0.6.0-rc.8, targetCommitish:main`.
10. **Cleanup.** The assembler's own install and matrix output under
    `D:\ReinstateAcceptanceProjects\v060-rc8-assembler\` are left in place
    for the coordinator's own records (matching the `v0.6.0-rc.7`
    assembler's own precedent); nothing from that directory is referenced
    by path elsewhere in this report beyond §0 itself.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-09-09` (span across all five parts; each part's own timestamp is in its section below); assembler verification `2026-09-09` |
| Device | `windows-amd64` |
| OS/version/build | Windows 11 Pro, `10.0.26200`, native `windows/amd64`, never WSL |
| CPU architecture/native process | `amd64`, native process (no emulation) |
| Filesystem | NTFS |
| Tested tag | `v0.6.0-rc.8` |
| Tested full commit | `3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f` |
| Installed binary SHA-256 | `482b5de12db1312c1a767bcf40c7999de9f8cef7f47bff5bce802fab960c3b71` |
| Installed version JSON | `{"commit":"3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f","date":"2026-09-08T22:38:21Z","name":"reinstate","version":"0.6.0-rc.8"}` |
| Catalog agent count | `18` |
| Catalog tier census | `T0: 7, T1: 4, T2: 2, T4: 2, T5: 3` (unchanged from `v0.6.0-rc.7`) |
| Generated required row count | `178` (`rein doctor --agents --acceptance-matrix --json`, independently reproduced by the assembler in §0 and by part A) |
| Git version | `2.52.0.windows.1` |
| Go version/toolchain | `go1.25.13` (pinned via `GOTOOLCHAIN`; host default `go1.26.1`) |
| Report branch | `v060/rc8-tagged` |
| Device-report commit | filled in by the commit that lands this file |
| Draft report PR | NOT CREATED (worktree report, per this run's dispatch — no PR required) |

### Bootstrap deviation (applies to every part below except executor A)

Per this run's ground rules, only executor A installs from the live
`https://reinstate.dev/install.ps1` bootstrap and records that as the
artifact identity; every other executor (B, C, D, E) and the assembler
installed from the coordinator's pre-verified, checksummed draft directory
(`…/scratchpad/rc8-draft/`) instead — the assigned methodology, not an
unplanned deviation. Executor A's own bootstrap check (part A): the live
bootstrap was downloaded fresh with `Invoke-WebRequest -OutFile` into
`D:/ReinstateAcceptanceProjects/v060-rc8-a/bootstrap/`, its pinned-version
line `$Version = "v0.6.0-rc.8"` confirmed at line 5 before execution, and
running it (`INSTALL_DIR` and `REINSTATE_BOOTSTRAP_PATH_SCOPE=Process`
pointed at `D:/ReinstateAcceptanceProjects/v060-rc8-a/bootstrap/bin`)
installed cleanly and reported the exact tag/commit above from both the
bootstrap install and the checksummed-archive install. **No deviation in
substance** between the live bootstrap route and the release-commit
candidate.

### Artifact identity (common to all parts)

| Field | Value |
| ----- | ----- |
| Tag | `v0.6.0-rc.8`, published GitHub prerelease, `isPrerelease: true`, `isDraft: false`, published `2026-09-08T22:43:53Z`, targeting `main`, signed tag verified against `.github/allowed_signers` (`git verify-tag`: Good signature, ED25519 key `SHA256:0P4/a2ZBw25hhf0mCN5s2NsUlDiqRqshv8vGNgMPHjU`) — re-confirmed independently by the assembler in §0.9 |
| Full commit | `3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f` (confirmed an ancestor of `origin/main`, §0.9) |
| Release workflow run | `34286767506` |
| Windows archive | `reinstate_0.6.0-rc.8_windows_amd64.zip` |
| Archive SHA-256 | `658dc27e607fdec14d14156dfe3edaf9d685bfa9add1e467d604ab0fa298e28f` (matches `checksums.txt`; re-verified independently by every part and the assembler) |
| Installed binary SHA-256 | `482b5de12db1312c1a767bcf40c7999de9f8cef7f47bff5bce802fab960c3b71` — `rein.exe`/`reinstate.exe` byte-identical in every install, both routes |
| `rein version --json` | `{"commit":"3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f","date":"2026-09-08T22:38:21Z","name":"reinstate","version":"0.6.0-rc.8"}` |
| Previous-release comparison binary (rows 2, 22, `G7`) | `reinstate_0.5.1_windows_amd64.zip`, checksum-verified |

## 2. Agent inventory on this device

| Agent key | Installed | Vendor version | Sessions available | Distinct projects | Declared tier | Tier testable here |
| --------- | --------- | -------------- | ------------------- | ----------------- | -------------- | -------------------- |
| `claude` | YES | `2.1.265` (in the tagged, same-day-widened `2.1.219`–`2.1.265` range; dispatch text anticipated `2.1.263`, see §0.8/§16) | live host config (part B/D/E); ~669 real sessions across all 11 real installed agents combined (part A `MatrixH:H3`); 2 throwaway isolated sessions in part B's own C1 sample; 14 real (part A `A10` filesystem audit's own live root) | live host + isolated throwaways | T5 | yes — resume/fork/handoff, real transcripts recalled |
| `codex` | YES | `0.153.4` (default; in the widened `0.133.0`–`0.153.4` range) | isolated throwaway home (part B); 2 sessions/2 projects (`C1`) | 2 (isolated) | T5 | yes — full live-turn coverage this run, no usage-limit hit, no version drift |
| `opencode` | YES | `1.18.29` (within `1.18.21`–`1.18.29`) | isolated throwaway home (part B); 2 sessions/2 projects (`C1`) | 2 (isolated) | T5 | yes — full re-test target confirmed, `E5`/`E6` `PASS` |
| `grok` | YES | `1.0.13` (isolated `GROK_HOME`, within `1.0.5`–`1.0.13`) | isolated throwaway home (part B); 2 sessions/2 projects (`C1`) | 2 (isolated) | T4 | yes — `E1`/`E2`/`E3` `PASS` this run via the executor-driven path; maintainer console fallback not needed |
| `qwen` | YES | `0.21.12` (within `0.21.12`–`0.23.0`) | isolated throwaway home (part B); 2 sessions/2 projects (`C1`) | 2 (isolated) | T4 | yes — every row `PASS` |
| `gemini` | YES (live default home, worked around a `GEMINI_CLI_HOME` suffix mismatch — see §16) | `0.53.0` | isolated throwaway home (part B); 2 sessions/2 projects (`C1`) | 2 (isolated) | T2 | yes |
| `kimi` | YES | `0.36.1` | isolated throwaway home (part B); 2 sessions/2 projects (`C1`) | 2 (isolated) | T2 | yes |
| `pi` | YES | (host) | 11 real (part A, live host root) | 10 | T1 | yes |
| `cursor` | YES (`cursor-agent` itself broken on this host, `Cannot find module tree-sitter`) | (host) | 3 real (part A) | 3 | T1 | yes — read-only host-store discovery, per the T1 exception |
| `copilot` | YES | `1.0.80`-family | 5 real (part A) | 5 | T1 | yes |
| `cline` | YES | (host) | 14 real (part A) | 8 | T1 | yes |
| `aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode` | ABSENT | — | — | — | T0 | yes (refusal/no-capability rows only) |

`claude`/`codex`/`opencode`/`grok`/`qwen`/`gemini`/`kimi` C1–E6/D5 evidence
(part B) was gathered against fully isolated throwaway homes seeded with
exactly two sessions/two projects each (except `claude`, which additionally
used the host's live `CLAUDE_CONFIG_DIR` per the ground rules, with no
isolated-throwaway session count separately reported for that agent in this
run). Part A's `MatrixH:H3` cold-refresh gate independently confirms ~669
real sessions exist across all 11 real installed agents combined on this
host, corroborating that every T1+ agent above has substantially more real
data available than the minimum Matrix C requires.

## 3. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------- |
| Annotated tag signature verifies against `.github/allowed_signers` | PASS | Coordinator pre-run; independently re-confirmed by the assembler (§0.9): `git verify-tag` Good signature, `SHA256:0P4/a2ZBw25hhf0mCN5s2NsUlDiqRqshv8vGNgMPHjU` |
| Tag peels to the recorded full commit on `origin/main` | PASS | `3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f`; independently re-confirmed ancestor of `origin/main` (§0.9) |
| Published release is non-draft, prerelease, and tied to the tag | PASS | `gh release view` (read-only), independently re-run by the assembler: `isDraft:false, isPrerelease:true`, published `2026-09-08T22:43:53Z` |
| Exact expected release asset set is present | PASS | `verify-release.ps1`/`test-install.ps1` (Gate A6/A7, part A) |
| Checksums, GitHub API digests, and attestations match the tag and commit | PASS | Coordinator pre-verified (`checksums.txt` match, `gh attestation verify` on the Windows archive); independently re-verified by every part and the assembler |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | PASS | `verify-release.ps1`/`test-install.ps1` (Gate A6/A7) |
| Live bootstrap is byte-identical to the tested commit and pins only this candidate | PASS | Executor A: pinned version line `v0.6.0-rc.8`, resulting binary byte-identical to the checksummed archive |
| Bootstrap installer digest matches the tagged canonical installer | PASS | Executor A |
| Fresh isolated install did not replace a user binary or persist PATH changes | PASS | Every part and the assembler installed into its own fresh directory |
| `rein` and `reinstate` have identical verified bytes and report the candidate | PASS | SHA-256 `482b5de12db1312c1a767bcf40c7999de9f8cef7f47bff5bce802fab960c3b71` throughout every install, including the assembler's own |
| Installed binary reports the literal full tested commit | PASS | `rein version --json` matches `3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f` in every install |

## 4. Automated gates (Section A of the dispatch; not part of the 216)

Source: part A, run once from the worktree per the ground rules (`-p 4`).

| # | Gate | Result | Evidence |
| - | ---- | ------ | -------- |
| A1 | Format, vet, tidy | PASS | `gofmt -l .` → 0 lines; `go vet ./...` → exit 0; `go mod tidy -diff` → exit 0 |
| A2 | Unit suite | PASS | `CGO_ENABLED=0 go test <82 tracked dirs> -count=1 -p 4` → 70 `ok`, 0 `FAIL`, exit 0 |
| A3 | Race suite | PASS | `CGO_ENABLED=1 go test <80 tracked dirs> -race -count=1 -p 4` → 68 `ok`, 0 `FAIL`, 0 `DATA RACE`, exit 0 |
| A4 | Lint and vuln | PASS | `golangci-lint run` (v2.11.4, scoped to tracked dirs): `0 issues`; `govulncheck` (v1.6.0, pinned toolchain): `0` reachable vulnerabilities (an ambient-toolchain-only false-positive of 17 stdlib CVEs self-corrected once pinned to `go1.25.13` — see §22) |
| A5 | Doc gate | PASS | `go test ./internal/doctest/... -count=1` → `ok`; `scripts/check-docs.ps1` → exit 0 |
| A6 | Installers (dist dir) | PASS | `scripts/verify-release.ps1 -DistDir rc8-draft` → exit 0 |
| A7 | Install script | PASS | `scripts/test-install.ps1 -DistDir rc8-draft` → exit 0, `ok internal/doctest 1.232s` |
| A8 | Cross-OS build | PASS | `GOOS=darwin GOARCH=amd64 go build <tracked dirs>` → exit 0; `GOOS=linux GOARCH=amd64 go build <same>` → exit 0 |

**8/8 PASS.**

## 5. Matrix A — Catalog integrity (10/10 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| A1 | Descriptors validate at start; no duplicate/empty keys | PASS | `doctor --agents --json` → 18 unique, non-empty `key` values; `internal/agents/conformance` suite → 195 `--- PASS`, 0 `--- FAIL`, `ok`, exit 0 |
| A2 | `doctor --agents` lists every agent incl. every T0 | PASS | all 18 keys present, including all 7 T0 agents, each `installed=no` |
| A3 | Every T0 agent shows an enumerated reason | PASS | closed 3-value set observed: `layout_unverified`, `server_backed`, `desktop_only` |
| A4 | Declared tier and capability constructors agree | PASS | `TestShippedAgentsConformance` plus empirical cross-check: T5 (`claude`) accepts `sessions`+`resume` (exit 0); T1 (`cline`) accepts `sessions`, refuses `resume` (exit 5); T0 (`aider`) refuses both |
| A5 | Every declared evidence path exists in the tagged tree | PASS | `TestShippedEvidenceIsComplete` over `agents.All()`, `ok` |
| A6 | `sessions --agent` accepts exactly T1+ keys, else exit 2 | PASS | `sessions --agent aider --json` → `code:"usage"`, exit 2, naming the 11-key list; T1+ keys accepted, exit 0 |
| A7 | `resume` accepts exactly T3+ keys, else exit 5 with reason | PASS | `resume aider:x` (T0) → exit 5, `t0_reason`-named message; `resume gemini:<id>` (T2) → exit 5, `"Gemini CLI sessions are read-only in Phase 2"`; `resume claude:<id>` (T5) → launch plan, exit 0 |
| A8 | Agent ordering deterministic across runs | PASS | 5× `doctor --agents --json` identical order; 3× `sessions --agent all --json --limit 200` identical order |
| A9 | Broken evidence path fails conformance | PASS | `TestBrokenEvidencePathFails`/`TestBrokenEvidencePathIsCaught`/`TestRunFailsBrokenEvidence`, all `--- PASS` |
| A10 | No scan writes, renames, or locks under any agent root | PASS | Full before/after filesystem snapshot of 11 real agent roots across a full scan: 0 diffs in 10 roots; 5 diffs in the `claude` root, all attributable to this live acceptance session's own concurrent transcript writes, not `rein`'s read-only scan |

## 6. Matrix B — Probe and redaction (9/9 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| B1 | Output conforms to `AGENT-PROBE-V1` | PASS | `"schema": "AGENT-PROBE-V1"` present |
| B2 | No absolute path, home name, or username | PASS | case-insensitive scan for `admin`, `C:\Users`, `D:\Projects` — 0 matches |
| B3 | No JSON values from session files, keys only | PASS | full key inventory contains only schema/shape descriptors, no session titles/prompts; scan for a known planted-token substring elsewhere on the host → 0 matches |
| B4 | Names shape-normalized | PASS | re-tested against the real `gemini` tree and every other real root; 0 raw-UUID-v4 regex matches; only template shapes present |
| B5 | No excluded subtree present | PASS | full-document scan for `credential`, `keyring`, `token`, `apikey`, `.cache`, `secret` — 0 matches |
| B6 | Absent agent reported without error | PASS | `CLINE_DATA_DIR`/`KIMI_CODE_HOME` pointed at nonexistent paths → exit 0 both, `exists:false, resolved_root:null` |
| B7 | Empty root distinguishable from absent root | PASS | same two agents pointed at an existing empty directory vs. nonexistent: differs exactly in `exists`, both exit 0 |
| B8 | Wrapper scripts match binary output | PASS | `agent-storage-probe.ps1` byte-identical to `doctor --agents --json` after normalizing `generated_at`; `.sh` out of scope (WSL-only, this is native-Windows-never-WSL) |
| B9 | Secret scanner passes over committed probes | PASS | `gitleaks v8.30.1` over `docs/testing/results/agent-probes`: 366.45 KB scanned, no leaks found, exit 0 |

## 7. Matrix G — No regression (8/8 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| G1 | Claude Code and Codex CLI native resume and fork unchanged | PASS | **Claude half** (real `2.1.265`, in the tagged, same-day-widened `2.1.219`–`2.1.265` range): real isolated throwaway project; planted token via `claude -p`; `rein search --agent claude --json` found it; `resume --dry-run --json` → `agent.version status=match actual=2.1.265`; real `claude --resume <id> -p "…"` recalled the token verbatim; real fork produced a **distinct** id, also recalling the token. **Codex half** (real `0.153.4`, no usage-limit refusal on first attempt): planted token via `codex exec`; `search`/`resume --dry-run` both correct; real `codex exec resume`/`codex exec fork` both recalled the token, fork producing a distinct id |
| G2 | Both structured handoff directions complete | PASS | `handoff claude:<id> --to codex --dry-run --json` → exit 0, full capsule+fidelity, `destination_session_mode:"new"`; `handoff codex:<id> --to claude --dry-run --json` → exit 0, freshly-minted destination Claude session id, distinct from any existing session |
| G3 | Gemini (T2) remains a read-only handoff source; OpenCode (T5) and Grok (T4) build real launch plans, per their current tier promotions | PASS, evaluated against current tier assignments | Gemini (T2) refuses `resume`, exit 5, `"Gemini CLI sessions are read-only in Phase 2"`. OpenCode/Grok both build real native launch plans, not read-only refusals — consistent with their tier promotions past T3 already reflected in the census this candidate holds. The contract's literal doc text ("Gemini, OpenCode, and Grok remain read-only") is stale for Grok's own promotion, the same way it is already noted stale for OpenCode's — scored against current, correct tier assignments, a documentation-staleness note, not a product defect; see §16 |
| G4 | `push`/`pull` carry sessions for exactly the sync-capable agents (Claude Code, Codex CLI, OpenCode) and no other agent's | PASS | Own isolated Hop lab; fixture Claude+Codex+OpenCode+Gemini sessions all locally visible. `push --all` → `rein status --json` afterward lists **exactly** `claude:…, codex:…, opencode:…` as remote — Gemini never appears (not sync-registered, no `NewSyncAdapter`); `pull --all --dry-run --json` confirms the same 3-agent scope |
| G5 | Cross-OS path remapping unchanged | PASS (Windows-side; macOS leg deferred per ADR 0005) | `internal/pathmap` unit suite (macOS↔Windows fixtures) passing |
| G6 | Exit-code semantics unchanged; no new codes | PASS | `internal/exitcode`: exactly 8 values (`OK=0` … `Safety=7`), matching every code observed empirically this run |
| G7 | An index built by a prior release upgrades without data loss (this candidate: `v0.5.1`-built index) | PASS | Checksum-verified `reinstate_0.5.1_windows_amd64.zip` built a real index under an isolated `REINSTATE_HOME` (`sessions --agent all --json` → 678 sessions); the tagged rc.8 binary against the same home, no rebuild step → same 678 sessions, no loss |
| G8 | Capsules remain excluded from sync | PASS | `internal/sync/push.go`'s `containsHardExcludedPath` hard-excludes any `handoffs` path segment (covered by the passing A2 suite); behaviorally confirmed by G4's own push, which never touched `~/.reinstate/handoffs` |

## 8. Matrix H — CLI contract and performance (6/6 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| H1 | CLI reference matches shipped flags | PASS | `TestPhase4CLIFlagsAreDocumented`/`TestCLIReferenceListsCatalogKeys`, via the green doctest run |
| H2 | JSON shapes match documentation | PASS | `internal/doctest` schema/format contracts passing; dozens of real invocations this run produced stable, self-consistent shapes |
| H3 | Full refresh within documented ceiling | PASS | `14.633s` cold `sessions --agent all --json` across all 11 real installed agents (~669 real sessions), within the ~20–30s Windows ceiling |
| H4 | No-change refresh materially faster | PASS | `0.375s` warm re-run, ≈39× faster than cold |
| H5 | One slow/unreachable source does not block others | PASS | `QWEN_HOME=C:/Windows/System32`: `4.711s`, exit 0, all 10 other real agents present, `qwen` correctly absent |
| H6 | Completion lists correct agent keys per command | PASS | `push`/`pull --agent ""` → exactly `claude, codex, opencode`; `sessions --agent ""` → the 11 T1+ keys plus `all` |

## 9. Matrix F — T0 agents (14/14 PASS)

Source: part A. All 7 T0 agents are not installed on this host — a scope reduction, not a `FAIL`.

| Agent key | F1 (listed with reason) | F2 (no capability offered) |
| --------- | ------------------------ | ---------------------------- |
| `aider` | PASS — `t0_reason=layout_unverified` | PASS — `sessions`/`resume` refuse per contract |
| `amp` | PASS — `t0_reason=server_backed` | PASS — same shape |
| `antigravity` | PASS — `t0_reason=layout_unverified` | PASS — same shape |
| `minimax-code` | PASS — `t0_reason=layout_unverified` | PASS — same shape |
| `openhands` | PASS — `t0_reason=server_backed` | PASS — same shape |
| `roo` | PASS — `t0_reason=layout_unverified` | PASS — same shape |
| `zcode` | PASS — `t0_reason=desktop_only` | PASS — same shape |

Closed enumeration of `t0_reason` values observed this run:
`layout_unverified`, `server_backed`, `desktop_only`.

## 10. Matrix C — Per T1+ agent (66 rows: 66 PASS)

Source: part A (`cline`, `copilot`, `cursor`, `pi` — T1) and part B
(`claude`, `codex`, `opencode`, `grok`, `qwen`, `gemini`, `kimi` — T2+).

### T1 agents (part A; 24/24 PASS)

Per the ground rules: `cline`/`copilot`/`pi` `C3` used the vendor's own live
CLI with a planted, non-sensitive search token; `cursor` ran entirely
read-only against the host's real store (`cursor-agent` itself is broken on
this host); `C6` for every agent used a filesystem copy, never live data.

| # | `cline` | `copilot` | `cursor` | `pi` |
| - | ------- | --------- | -------- | ---- |
| C1 (≥2 sessions, ≥2 projects) | PASS — 14/8 | PASS — 5/5 | PASS — 3/3 | PASS — 11/10 |
| C2 (metadata matches vendor) | PASS — `message_count=2`, matches vendor's own `.messages.json` array length exactly | PASS — project/branch/title/timestamp confirmed exact; `message_count=5` plausible, not fully independently re-derived (see §16) | PASS — `message_count` values `[4,3,3]` | PASS — `message_count=2`, matches vendor's own `type:"message"` entry count exactly |
| C3 (message-body search) | PASS — real `cline --cwd … --json` planted-token session, 1 match | PASS — planted token, 1 match on a real, existing session | PASS — word from a real session's own `prompt_preview`, 1 match | PASS — real `pi -p` planted-token session, 1 match, `prompt_preview` present |
| C4 (bounded inspect, no body) | PASS — bounded fields only, no transcript body | PASS — same shape | PASS — same shape | PASS — same shape |
| C5 (resume gate correct for tier) | PASS — `read_only_reason`, exit 5 | PASS — same shape | PASS — same shape | PASS — same shape |
| C6 (corrupt/empty/absent degrade cleanly) | PASS — truncated copy of `.messages.json` → exit 0, no panic, corrupted session silently excluded; empty/absent → `{"sessions":[]}`, exit 0 | PASS — empty/absent `COPILOT_HOME` → `{"sessions":[]}`, exit 0 (representative corrupted-record architecture confirmed via `cline`) | PASS — `CURSOR_CONFIG_DIR` redirect honored (`F-CURSOR-ROOTENV` stays fixed); isolated empty dir → `{"sessions":[]}`, not the real 3 | PASS — empty/absent `PI_CODING_AGENT_DIR` → `{"sessions":[]}`, exit 0 |

**24/24 PASS.**

### T2+ agents (part B; 42/42 PASS)

Real vendor sessions in throwaway projects (isolated homes for every agent
except `claude`, per the ground rules), planted per-agent token in a
message body (never a title), `rein search` confirms match; `inspect --json`
shows a bounded `prompt_preview`; `resume --dry-run --json` shows the
tier-correct gate decision; a truncated/corrupt/empty/absent copy degrades
cleanly.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| C1 (≥2 sessions, ≥2 projects) | PASS — 2/2 | PASS — 2/2 | PASS — 2/2 | PASS — 2/2 | PASS — 2/2 | PASS — 2/2 | PASS — 2/2 |
| C2 (metadata matches vendor) | PASS — `message_count=4` | PASS — `message_count=5` | PASS — `message_count=4` | PASS — `message_count=10` | PASS — `message_count=4` | PASS — `message_count=4` | PASS — `message_count=4` |
| C3 (message-body search, never title) | PASS — token found; title = session id | PASS — token found; title = session id | PASS — token in turn 2; title token-free | PASS — token found; title = session id | PASS — token found; title token-free | PASS — token found; title = session id | PASS — token found; title = session id |
| C4 (bounded inspect, no full body) | PASS — 22-char preview | PASS — bounded system-preamble preview | PASS — 25-char preview | PASS — bounded `<user_info>` preamble preview | PASS — 22-char preview | PASS — 22-char preview | PASS — 22-char preview |
| C5 (resume gate correct for tier) | PASS — `confirmation_required` | PASS — `confirmation_required` | PASS — `confirmation_required` | PASS — `confirmation_required` | PASS — `confirmation_required` | PASS — refused, exit 5, `"Gemini CLI sessions are read-only in Phase 2"` | PASS — refused, exit 5, `"Kimi Code CLI sessions are read-only until a device journey verifies native resume"` |
| C6 (corrupt/empty/absent degrade cleanly) | PASS — synthetic fixture (harness note below), `incomplete_trailing_record`, exit 0; empty/absent clean | PASS — truncated copy, `message_count` reduced correctly, exit 0; empty/absent clean | PASS — SQLite copy truncated 50%, `session_read_failed`, exit 0; empty/absent clean | PASS — truncated `updates.jsonl` copy read cleanly, exit 0; empty/absent clean | PASS — truncated copy, `message_count`/`size_bytes` reduced correctly; empty/absent clean | PASS — truncated copy, `message_count` reduced 4→2; empty/absent clean | PASS — truncated `wire.jsonl` copy, read cleanly, unaffected; empty/absent clean |

**Harness note (`claude:C6`/`claude:D4`).** Per this worktree's own
`CLAUDE.md` ("never inspect the developer's real `~/.claude` tree… use only
`testdata/adapters/claude/` or temporary synthetic fixtures"), both rows
used a hand-authored synthetic Claude Code JSONL fixture (modeled on the
sanitized shape `doctor --agents --json` itself reports, never live file
content) in a fully isolated `CLAUDE_CONFIG_DIR`. Every other `claude:*`
row ran against the live config through `rein`'s own designed read path
(`search`, `inspect`, `resume`, `fork`, `handoff --dry-run`), which the
ground rules direct at the live config. Both rows still exercise the
mechanism and still `PASS`. See §16.

**42/42 PASS.**

## 11. Matrix D — Per T2+ agent (35 rows: 34 PASS, 1 N/A definitional)

Source: part B. Mechanism: `rein handoff <key>:<id> --to <dest> --dry-run
--json` (D1–D3, D5); D4 used `--no-launch` against a copy of the session's
own raw store truncated to ~70% of its byte length, with the capsule's
`raw_source.byte_offset`/`artifact_sha256` independently recomputed
(`sha256sum` over the first `byte_offset` bytes) for every agent — every
recomputed hash matched the capsule's recorded hash exactly.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| D1 (capsule + fidelity report) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D2 (no invented content) | PASS — 15 fidelity components | PASS — 16 | PASS — 14 | PASS — 16 | PASS — 15 | PASS — 14 | PASS — 16 |
| D3 (unknowns referenced/omitted with reason) | PASS — unrecognized records referenced, `baseline.unavailable` named | PASS — 27 unrecognized referenced, plus ~160 `handoff.capability.skill.*` warnings named | PASS — 3 unrecognized referenced, `baseline.unavailable` only | PASS — `baseline.unavailable` only (28 non-fatal `MalformedLines` from auxiliary session files, non-blocking) | PASS — `baseline.unavailable` only | PASS — `baseline.unavailable` only | PASS — 1 unrecognized referenced, `baseline.unavailable` only |
| D4 (truncation boundary, offset+hash, recomputed) | PASS — synthetic fixture, offset `772`, hash `4584e041…f996c5dbf` matched | PASS — offset `54989`, hash `64e8f51d…b6e9c6` matched | N/A (definitional) | PASS — `updates.jsonl`, offset `10766`, hash `64e8f51d…cb6e9c6` matched | PASS — offset `7635`, hash `d5d0dccb…4b76778` matched | PASS — offset `4980`, hash `89b75902…c4ee7dda` matched | PASS — `agents/main/wire.jsonl`, offset `148986`, hash `5142c112…c7321732` matched |
| D5 (two runs byte-identical) | PASS — `handoff_id` and destination `session_id` also identical across both runs | PASS — same | PASS — same, once the source was confirmed not `agent.active` (see §16) | PASS — same | PASS — same | PASS — same | PASS — same |

`opencode:D4` remains **N/A (definitional)**, unchanged reason since
`v0.6.0-rc.1`: OpenCode's SQLite-only store has no JSONL record boundary
this row's mechanism applies to. Excluded from the required count.

**34/34 PASS (required), 1 N/A.**

## 12. Matrix E — Per T4/T5 agent (30 rows: 30 PASS)

Source: part B. Mechanism: E1/E2 via each vendor's own non-interactive
resume flag against the dry-run plan's exact argv, asking the resumed agent
to recall a token only the original session's first turn could know; E3
the same via each vendor's fork flag, confirming a distinct session id also
recalls the token; E4 via a version-reporting shim both below and above the
verified range, real `resume`, confirming exit 5 and the range named; E5
via `scripts/testing/conptydriver`, holding the vendor CLI open for a
second shell's `resume --dry-run --json` to observe `agent.active
status=present, actual=true`; E6 the same non-interactive attempt with
`stdin` closed, expecting exit 7.

| Row | claude | codex | opencode | grok | qwen |
| --- | ------ | ----- | -------- | ---- | ---- |
| E1 (resume launches vendor CLI, session continues) | PASS — planted token recalled | PASS — planted token recalled, real `0.153.4` turn, no account-limit error | PASS — planted token recalled | PASS — planted token recalled, executor-driven, no hang | PASS — planted token recalled |
| E2 (resumed session is the requested one) | PASS — same id, `message_count` 4→6 | PASS — same id, `message_count` 5→7 | PASS — same id | PASS — same id | PASS — same id |
| E3 (fork produces a distinct session) | PASS — distinct id (carries full history, `message_count=8`) | PASS — distinct id (fresh) | PASS — distinct id | PASS — distinct id, executor-driven | PASS — distinct id (carries full history) |
| E4 (below-min/above-max both exit 5, naming the range) | PASS — `2.1.219 to 2.1.265 inclusive` | PASS — `0.133.0 to 0.153.4 inclusive` | PASS — `1.18.21 to 1.18.29 inclusive` | PASS — `1.0.5 to 1.0.13 inclusive` | PASS — `0.21.12 to 0.23.0 inclusive` |
| E5 (active session detected, resume refused/warned) | PASS — via ConPTY (`claude --resume <id>`), `agent.active status=present, actual=true` | PASS — via ConPTY (`cmd.exe /c codex resume <id>`), same shape | PASS — via ConPTY (direct `.exe`, no wrapper needed), same shape | PASS — via ConPTY (direct `.exe`, no wrapper needed), same shape, executor-driven | PASS — via ConPTY (`cmd.exe /c qwen --resume <id>`), same shape |
| E6 (non-interactive exits 7) | PASS | PASS | PASS | PASS | PASS |

**30/30 PASS.**

### `codex:E1`–`E3` — real, completed turns against `0.153.4`

Every Codex call in part B (`codex exec --skip-git-repo-check`, `codex exec
resume`, `codex exec fork`) completed on the first attempt with a real
`0.153.4` conversational turn; no usage-limit error was returned at any
point.

### `grok:E1`–`E3` — executor-driven, no maintainer console needed

Per the dispatch, part B attempted to drive `grok` itself first: headless
`grok -p` with an isolated `GROK_HOME` seeded only with `auth.json`, `stdin`
closed. Every invocation answered within seconds, no hang or timeout.

```
grok --version                     -> grok 1.0.13 (5e9a58528b76)
grok -p "Say hello in one word."   -> Hello.
grok --resume <session-1> -p "Reply with exactly this token and nothing else: <token>"
                                    -> <token>
grok --resume <session-1> -p "What was the exact token I asked you to remember earlier? Reply with just the token."
                                    -> <token>  (same session id)
grok --resume <session-1> --fork-session -p "What was the exact token I asked you to remember earlier? Reply with just the token."
                                    -> <token>  (new, distinct session id)
```

## 13. CLI experience (22 rows: 22 PASS)

Source: part C, through `scripts/testing/conptydriver` against
`scripts/tuisandbox`, with a fully rebuilt isolated `PATH` and every vendor
home variable redirected so no host session leaked into the bench's own 9
indexed sessions.

| # | Row | Result | Evidence |
| - | --- | ------ | -------- |
| 1 | Bare `rein` on a capable terminal draws the switcher | PASS | `9 sessions · 3 agents · all projects` header, filter prompt, time-grouped rows, split preview, key bar all draw correctly; all 9 readiness glyphs match ground truth |
| 2 | Bare `rein` on a non-TTY exits 2, byte-identical to `v0.5.1` | PASS | both binaries exit 2; stderr SHA-256 `0476fb687fa1c1209d243016ab020460b8ad55696e4052f491b1fcb8cdb4795b`, matches every prior candidate's own recorded hash |
| 3 | `--plain` falls back to the numbered switcher | PASS | all 9 rows numbered, correct prompt |
| 4 | `REINSTATE_NO_TUI=1` does the same | PASS | output SHA-256-identical to row 3 |
| 5 | `TERM=dumb` does the same | PASS | output SHA-256-identical to rows 3/4 |
| 6 | `TERM` unset draws the switcher on Windows | PASS | confirmed `$env:TERM` unset; full interactive switcher drawn |
| 7 | A terminal below 40×10 falls back to plain | PASS | 30×8 ConPTY console → wrapped numbered picker |
| 8 | `NO_COLOR` draws the switcher with no color escapes | PASS | raw byte capture: exactly one bare `ESC[m` reset, zero SGR-parameter sequences |
| 9 | Typing filters; header count follows | PASS | `"auth"` → `1 session · 1 agent`, single matching row; required fixing a test-harness gap (`vtscreen.go` ECH support) first, see §22 |
| 10 | `f` in list mode filters, does not fork | PASS | prompt shows `f`; header unchanged (all 9 fixture titles/branches genuinely contain `f`); no fork screen anywhere |
| 11 | `tab` opens the action menu; `esc` reverts without acting | PASS | key bar shows `r/f/h/i/y/esc`; `esc` reverts to a frame byte-identical to pre-`tab` |
| 12 | `ctrl+k` opens the palette; a subsequence query finds its command | PASS | `"hof"` → top match `Hand off to another…` |
| **13** | **Readiness glyphs resolve for visible rows; a read-only agent shows blocked without a probe** | **PASS — this candidate's own targeted fix, confirmed** | See full evidence below |
| 14 | The warning checklist acknowledges with the spacebar | PASS | single space toggles exactly one of 4 warnings (`baseline.unavailable`); equivalent command live-updates; second space reverts to a frame byte-identical to pre-toggle |
| 15 | A partial acknowledgement is refused with exit 7 | PASS | 2 of 4 acknowledged: exit 7 naming exactly the remaining two (`git.working_tree`, `runtime.node.declaration`); interactive checklist shows matching inline refusal |
| 16 | The handoff studio measures each policy | PASS | policy cycles `balanced→full→checkpoint→balanced` in lock-step with the equivalent command; settled projection genuinely differs (`user_messages` carried under `balanced`, left-behind under `checkpoint`) |
| 17 | The studio refuses `enter` on a plan that could not be built | PASS | `repo-drift` (foreign `repository_url`, BLOCKED) shows "this handoff cannot be planned" before any keystroke; `enter` adds an identical inline refusal, studio stays open; second `enter`'s frame byte-identical to the first; no handoff record created |
| 18 | `rein init` opens the wizard, validates, allows going back | PASS | invalid endpoint stays on step 2 with inline validation error; raw CSI-Z (shift+tab) returns to step 1 with prior provider selection preserved |
| 19 | `rein init` collects no secret material inside the full-screen program | PASS | all 7 steps walked, no credential field anywhere; `init --help` has no secret-key flag; start-setup releases the alt-screen program before the plain-mode access-key prompt appears |
| 20 | `rein init --link` prints a code `--paste` consumes on the other device | PASS | real two-device round trip via a local fakelocker S3 endpoint; every field (endpoint/bucket/region/prefix/profile ID) pre-filled exactly matching device A's own `config.toml` |
| 21 | Glyphs degrade to ASCII where Unicode is not safe (legacy conhost) | PASS | `WT_SESSION`/`TERM_PROGRAM`/`LANG`/`LC_ALL`/`LC_CTYPE` all unset: every UI glyph renders ASCII, correctly matching each row's readiness; session title Unicode text renders untouched |
| 22 | Every `--json` document is byte-identical to `v0.5.1`, except changelog-explained differences | PASS | 9 documents diffed byte-for-byte vs `v0.5.1` on an identical bench; every non-empty diff traces to exactly 3 documented classes: `agent.active` liveness check, Grok tier promotion, and Claude compatibility range-ceiling text widening (all `CHANGELOG.md`-explained); `doctor` differs only in the version field; `handoff` byte-identical |

macOS is deferred under ADR 0005 for this candidate; nothing above claims
macOS evidence.

### Row 13 — full evidence [PASS — this candidate's targeted fix confirmed]

**Method** (the `v0.6.0-rc.7` tagged report's own row-13 method, reused
exactly, under the dispatch's expanded, tighter bar): launch the tagged
binary's own `rein.exe`, by full path, bare, from a cwd outside any tracked
project and outside any Git checkout — the switcher's documented default
`ScopeAll`. Checkpoints at ~5s, ~12s (cumulative), and ~30s (cumulative),
well past settle. Cross-check every row's rendered glyph against ground
truth from `rein resume <id> --dry-run --json`/`rein inspect <id> --json`
on the same 9 fixture sessions. Repeat for 15 independent launches; every
one must fully match on every checkpoint — one wrong row on one launch is a
`FAIL` for the whole row.

**15/15 `ScopeAll` launches — 45/45 checkpoints, 0 mismatches.** Each launch
used a distinct `WT_SESSION` value (the readiness cache is in-memory
per-process, never persisted, so every launch is inherently a cold,
independent trial). Verified programmatically, not by eye: for every trial
and every checkpoint, the rendered glyph for each of the 9 fixture rows was
extracted and compared against ground truth — **0 mismatches across 15
trials × 3 checkpoints × 9 rows = 405 individual row checks.** Every
trial's three checkpoint captures are SHA-256-identical to each other —
every launch settled to a **correct** answer and held it, byte-for-byte,
through the full 30-second window. Representative comparison against
`v0.6.0-rc.7`'s own recorded worst trial (8 of 9 rows wrong, for context,
not re-run): every one of this candidate's 15 trials had 0 of 9 rows wrong,
equally stable.

**`ScopeProject` unaffected.** Launched from inside the bench's own
`auth-refactor` workspace, header reads `1 session · 1 agent ·
auth-refactor`; the single row shows `● READY TO RESUME` — correct,
consistent with every prior candidate's own evidence.

**`-stale-claude` settle check (this candidate's second fix, `e7de157e`).**
Bench regenerated with `-stale-claude` (out-of-range `2.1.300` Claude shim
first on `PATH`; every Claude row necessarily blocks on `agent.version`).
Ground truth confirms all 5 Claude fixture sessions report exit 5, block
reason `agent.version`, message `"native agent version 2.1.300 is outside
the verified range 2.1.219 to 2.1.265 inclusive"` (one, with a missing
workspace, reports the layout-still-readable variant instead — still a
deterministic, non-timeout block). Interactive checkpoints at 15s, 40s,
70s, and 100s — `ProbeTimeout` is 12s, `maxProbeRetries` is 3, so 100s is
well past `12s × 3 = 36s`, generously multiplied. All 5 Claude rows show
`○` at the 15s checkpoint already (no `◌` observed at any checkpoint) and
hold it, unchanged, through 100s — all four checkpoint captures are
SHA-256-identical. The non-Claude rows are unaffected by the stale shim,
matching their own ground truth throughout.

**Disposition: `PASS`.** All three parts of the expanded method — 15+
`ScopeAll` launches all fully matching, `ScopeProject` unaffected, and the
`-stale-claude` settle check landing on and staying on `○ Blocked` rather
than looping on `◌` — hold. This directly reverses `v0.6.0-rc.7`'s tagged
`FAIL` on this exact row (11 of 15 launches wrong, 73% failure rate); this
run found 0 of 15 wrong.

### Row 22 — full evidence [PASS]

Nine documents diffed byte-for-byte between the tagged `rein.exe` and
`v0.5.1`'s `rein.exe`, both run against the identical bench:
`sessions --json`; `resume <key> --dry-run --json` for a ready session, a
warn session, both blocked sessions (missing-workspace and
foreign-repository), and the `grok` session; `inspect --json`;
`doctor --json`; `handoff <key> --to codex --dry-run --json`; and (against
the `-stale-claude` bench) `resume` on an out-of-range Claude session.
Every non-empty diff traces to exactly three classes, all explained by
`CHANGELOG.md`: the new `agent.active` liveness check, Grok's later T4
tier promotion (`resume`/`fork` now `true` with a full environment report,
where `v0.5.1` reported `read_only_reason`), and the Claude compatibility
range-ceiling text (`2.1.238` in `v0.5.1` vs. `2.1.265` in this tag, widened
across every `v0.6.0-rcN`). `doctor --json` differs only in the `version`
field; `handoff --dry-run --json` is byte-identical.

## 14. Hop parity journeys (16 rows: 16 PASS)

Source: part D (`H1`, `H1b`, `H1c`, `H2`, `H3`, `H4`, `H5`, `H9`, `H10`,
`H11`, `H12` — 11 rows) and part E (`H6`, `H6b`, `H7`, `H8`, `H8b` — 5
rows), against a local, disposable `hopd` + `scripts/testing/fakelocker`
lab, never the hosted control plane and never a developer's real agent
trees except where the ground rules specifically authorize the host's live
Claude Code config (`H5`).

| # | Row | Result | Summary |
| - | --- | ------ | ------- |
| H1 | Email sign-in through the approver; token in OS keyring | PASS | Real GET-then-POST via `hoplab approve`; `login`/`whoami` exit 0; a fresh, separate `hoplab keyring show` process confirmed the OS-keyring device token entry |
| H1b | Refused sign-in tells the terminal why and stores nothing | PASS | Dedicated lab, `HOPD_LOGIN_SESSION_TTL=10s`, no approver running; human and `--json` both exit 1, documented refusal, nothing written under `REINSTATE_HOME`, no keyring entry |
| H1c | Unreachable control plane prints the one-line message and documented exit code | PASS | `REINSTATE_HOP_URL` at an unused loopback port; human and `--json` both exit 1 with the identical message, `details.kind=control_plane_unreachable`; nothing written |
| H2 | `init --hop` provisions the locker exactly once | PASS | Exactly one `locker_provisioned` event across two `init --hop` runs (second with `--force`); same locker id reused; `lockers` table carries exactly one row |
| H3 | `account init` recovery code shown once; keyring format verifies | PASS | Recovery code shown once (live-FD-confirmed); `keyring.v1.json` independently fetched via minted S3 credentials and parsed with the real `internal/keyring.Parse`: `schema_version=5, current_generation=1`, `VerifyGenerations` returned `nil` (verifies, unpinned) |
| H4 | `push --all` sends one session per T5 agent; `first_push` fires once | PASS | 1 Claude + 1 Codex + 1 OpenCode pushed; `first_push` fired exactly once across two calls; no-op push reports `skipped:3`; `hop status` shows `first_push_at` |
| H5 | Wipe, recover as new device, pull, real resume | PASS | Home/keyring genuinely wiped; new `device_id` under the same account; `account recover` re-derived the root key; `pull --all` restored all three after resolving three legitimate first-contact conflicts; `resume --dry-run` completed for all three; one real `claude --resume … -p` against the still-live host config (auto-updated to `2.1.265`, within this candidate's own verified range) recalled the exact planted token at the same session id |
| H6 | Pairing: B joins, A approves, B pulls | PASS | Live `hoplab pair join`; `pairing_requests status=consumed, claims=2, version=2`; A pushed 3 fixture sessions, B pulled all 3 after a project mapping was added; `sessions --json` on B showed 6 total, no key overlap |
| H6b | An expired pairing request is refused/rolled back | PASS | Dedicated short-TTL lab (`HOPD_PAIRING_TTL=8s`); `account join` blocked the full TTL then refused client-side, exit 4; `pairing_requests status=expired, payload='', claims=5`; `device_in_keyring=false, enrolled_devices=1` |
| H7 | `daemon install/status/stop/start/uninstall` through Task Scheduler; debounced push, scheduled pull | PASS | Fresh Hop lab account with zero pushed sessions (`sync verify --json outcome=not-applicable` beforehand). Full per-file before-listing of the live `CLAUDE_CONFIG_DIR/projects`, persistent-registry `CODEX_HOME/sessions`, `XDG_DATA_HOME/opencode`: 3,191 files. Elevated round trip via `Start-Process -Verb RunAs` (maintainer accepted UAC promptly) ran the full daemon install/status/wait/status/hop-status/stop/status/start/status/stop/uninstall/status sequence against real `schtasks`; push observed (25 snapshots from the live host roots), pull observed (0 restored). `#424` reproduced again (the scheduled task's reported roots were the live host paths, not the isolated H7 home) — recorded per rule (d), not failed. After-listing: 3,193 files, exactly 8 differing entries, all attributable to concurrent Claude Code activity (this executor's own growing session transcript, 5 sibling-subagent transcripts under the same workflow id, and 2 new files from a different concurrent tagged-run executor's own throwaway project) — none lab/synthetic-named, none matching this row's own (zero) pushed session ids, and pull itself restored zero snapshots. All three conditions of the refined rule hold |
| H8 | A revokes B: generation rolls, B's token refused | PASS | `keygeneration_crossplane_test.go -tags hopacceptance` against a real `hopd`: both `TestKeyGenerationFloorAgainstRealHopd` (lagging-device attack refused on push/pull/sync-verify, each naming control plane) and `TestKeyGenerationFloorWithoutTheRouteAgainstRealHopd` (404-floor proxy: floor route asked 4 times, answered 404 every time, documented residual reproduced) PASS. Live: `rein devices revoke` on H6's device-a targeting H6's device-b rolled generation 1→2; device-b's `whoami`/`push`/`pull` all refused after, exit 4 each, naming the control plane |
| H8b | A Console-initiated revocation stays pending until confirmed via the recovery-code command | PASS | A disposable self-signed-TLS reverse-proxy Go helper drove the real Console HTTP routes end to end (sign-in, session cookie, CSRF-bearing devices page, revocation POST); `device_revocation_requests` went `pending` (target device's `whoami` still succeeded) → `confirmed, confirmed_generation=2` after `rein devices revoke` from the recovery-code holder; target device's next `push --all` refused, exit 4 |
| H9 | `sync verify` names only observed objects | PASS | `checked_objects` names exactly the index and newest snapshot; step 4 status is `"fail"`, never `"could not run"` — the documented `fakelocker AnyBucket:true` harness limitation |
| H10 | `sync migrate --to byo`, `--switch`, `--forget-hop` | PASS | Moved 3 snapshots to a second bucket in one call; `--switch`/`--forget-hop` both took effect (`whoami` refuses locally, device not revoked in `hopd.db`); new bucket's `sync verify`: `outcome: pass`, step 4 `not-applicable` |
| H11 | Path remap on pull | PASS | A second device with a different real local path mapped to the same project id pulled all 3 sessions with `cwd` rewritten to the remapped path (confirmed in the pull plan and directly in the restored jsonl's own `cwd` field); `resume --dry-run` reports the remapped `cwd`, `workspace.available` present, `agent.version` match |
| H12 | Keyless diagnostics refuse a tampered keyring | PASS | Three attacks (forged signature, foreign-account re-key using a real separate account's genuinely-signed keyring, rolled-back generation using the real pre-revoke generation-1 slice) each refused with `keyring_refused:true` and a specific accurate reason; `push` also refused exit 7; `account.json` byte-identical throughout and after final restore |

**16/16 PASS.**

### `H1`–`H5`, `H9`–`H12` (part D) — condensed evidence

```
H1:  hoplab approve ... -> rein login/whoami --json EXIT=0; hoplab keyring show confirms device token
H1b: HOPD_LOGIN_SESSION_TTL=10s, no approver -> LOGIN_EXIT=1, "the sign-in link expired..."; nothing written
H1c: REINSTATE_HOP_URL=<unused loopback> -> HUMAN_EXIT=1 / JSON_EXIT=1, details.kind=control_plane_unreachable
H2:  init --hop (then --force) -> locker_provisioned fires once; lockers table has exactly 1 row
H3:  labverify fetched keyring.v1.json via minted S3 creds; internal/keyring.Parse: schema_version=5,
     current_generation=1, VerifyGenerations("") -> nil
H4:  push --all --json -> EXIT=0, 3 snapshots; first_push fires once across two calls; no-op -> skipped:3
H5:  wipe REINSTATE_HOME + keyring -> new device_id, same account; account recover; pull --all restores
     3 after resolving conflicts (--keep-remote); resume --dry-run completes for all 3; real
     claude --resume <id> -p "..." recalls RC8D-H5-CLAUDE-4471 exactly, same session id
H9:  sync verify --json -> EXIT=7; checked_objects: ["the index", "the newest snapshot in the index"];
     step 4 status="fail" (fakelocker AnyBucket:true limitation, documented, not "could not run")
H10: sync migrate --to byo --switch --forget-hop --json -> EXIT=0, migrated 3 snapshots; whoami refuses
     locally afterward; hopd.db revoked_at empty (not revoked); new bucket verify: outcome=pass
H11: device-c remapped hoplab-device-b's project id to its own local path; pull --all --json ->
     pulled:3; restored .jsonl's own "cwd" field matches the remapped path exactly; resume --dry-run
     confirms remapped cwd, workspace.available=present, agent.version=match
H12: forged signature / foreign-account re-key / rolled-back generation -> each keyring_refused:true
     with a specific accurate reason; push also refused EXIT=7 during re-key; account.json
     byte-identical (same SHA-256) across baseline, all three attacks, and after final restore
```

### `H6`, `H6b`, `H7`, `H8`, `H8b` (part E) — condensed evidence

```
H6:  hoplab pair join (device-b publishes, device-a approves, no code typed by hand) -> exit 0;
     pairing_requests status=consumed version=2 claims=2; device-a push --all -> 3 snapshots;
     device-b pull refused (no local project mapping) -> mapping added via config.toml [[projects]]
     (the refusal's own offered alternative); second pull -> pulled:3, skipped:0; sessions --json on
     device-b shows 6 total, no key overlap
H6b: HOPD_PAIRING_TTL=8s, no approver -> account join blocks full 8s then refuses client-side, EXIT=4;
     pairing_requests status=expired, payload='', claims=5, version=2; device_in_keyring=false,
     enrolled_devices=1 (no wrap ever produced)
H7:  go-signal file already present at start -> ran first, ahead of H6/H6b/H8/H8b, as required. Fresh
     Hop lab account, zero pushed sessions (sync verify --json outcome=not-applicable beforehand).
     Full per-file before-listing: 3,191 files. Elevated round trip via Start-Process -Verb RunAs
     (~44s incl. the script's own 40s wait) ran install/status/wait/status/hop-status/stop/status/
     start/status/stop/uninstall/status against real schtasks (com.reinstate.daemon.a76d4cff); push
     observed (25 snapshots from the live host roots), pull observed (0 restored). #424 reproduced
     again (scheduled task's reported roots were the live host paths, not the isolated H7 home) --
     recorded per rule (d), not failed. After-listing: 3,193 files, exactly 8 differing entries, all
     attributable to concurrent Claude Code activity (this executor's own transcript, 5 sibling-
     subagent transcripts under the same workflow id, 2 new files from a different concurrent
     tagged-run executor's own throwaway project) -- none lab/synthetic-named, none matching this
     row's own (zero) pushed session ids, pull restored zero. All three conditions of the refined
     rule hold
H8:  keygeneration_crossplane_test.go -tags hopacceptance, REINSTATE_HOPD_BIN pointed at this
     executor's own hopd: TestKeyGenerationFloorAgainstRealHopd PASS (lagging-device attack refused
     on push/pull/sync-verify, each naming control plane) and
     TestKeyGenerationFloorWithoutTheRouteAgainstRealHopd PASS (404-floor proxy: floor route asked
     4 times, answered 404 every time, documented residual reproduced). Live: rein devices revoke
     on H6's device-a targeting H6's device-b rolled generation 1->2; device-b's whoami/push/pull
     all refused after, exit 4 each, naming the control plane
H8b: Fresh device-c/device-d pair. A disposable self-signed-TLS reverse-proxy Go helper drove the
     real Console HTTP routes against the real hopd end to end: POST sign-in email (200) -> found
     confirm link in hopd.log -> GET confirm page (200) -> POST confirm (200, session cookie set) ->
     GET /console/devices (200, CSRF extracted, target present) -> POST revocation with CSRF (200) ->
     GET /console/devices shows "Pending revocation". device_revocation_requests row: status=pending.
     Target device-d's whoami still succeeded (not yet enforced). rein devices revoke from device-c
     (recovery code) confirmed the request after the keyring reached generation 2; DB row moved to
     status=confirmed, confirmed_generation=2. Device-d's next push refused, exit 4
```

## 15. T5 encrypted sync round trip (beyond the 216; source: part B)

A disposable `scripts/testing/fakelocker` instance served the S3-compatible
locker in-memory. Two fully isolated `REINSTATE_HOME`s were paired against
it via `rein init --yes` with per-exact-project mappings; passphrase input
used a small Windows helper (`passfd.exe`) mirroring `hoplab`'s own
inheritable-handle mechanism — no ordinary passphrase environment value or
plaintext file ever used.

| Row | Result | Evidence |
| --- | ------ | -------- |
| `push:claude` | PASS | `push --agent claude --all --json` → 2 snapshots |
| `push:codex` | PASS | `push --agent codex --all --json` → 2 snapshots |
| `push:opencode` | PASS | `push --agent opencode --all --json` → 3 snapshots |
| `pull:claude` | PASS | Device B's isolated `CLAUDE_CONFIG_DIR` restored 2 files; planted token appears 5 times in the raw restored file |
| `pull:codex` | PASS | Device B's isolated `CODEX_HOME` restored 2 files; planted token appears 8 times in the raw restored file |
| `pull:opencode` | PASS | Initial pull refused (no OpenCode layout marker on device B yet), resolved by seeding device B's schema once; retry pulled cleanly, `rein search` on device B found the pulled sessions |

**6/6 PASS.** No plaintext left the process at any point.

## 16. Findings (merged, release-blocking assessment)

| ID | Severity | Row(s) | Description | Release blocking |
| -- | -------- | ------ | ------------ | ----------------- |
| — | product-adapter, non-blocking | `gemini` (all rows) | `rein doctor --agents --json` names `GEMINI_CLI_HOME` as Gemini's `root_env` and resolves the `env` candidate root with no `.gemini` suffix appended, but the real installed Gemini CLI (`0.53.0`) treats the variable as the **parent** of its own `.gemini` directory and appends the suffix itself — confirmed empirically both directions. A user who sets `GEMINI_CLI_HOME` the way the vendor CLI actually documents would find `rein` silently discovering zero Gemini sessions. Every Gemini row still exercised its real mechanism and passed with a manual workaround (the `.gemini`-suffixed path for `rein`, the parent path for the real CLI) | No — does not block this candidate (Gemini stays T2, default unset-env resolution works fine); worth a product fix or a docs note |
| — | documentation clarity, non-blocking | sync `push`/`pull` | Sync's project-mapping requires a session's workspace to match a declared portable project's `local_root` **exactly**, not merely fall under it as a descendant, unlike ordinary discovery (`sessions`/`search`/`inspect`), which has no such restriction — confirmed by reading `internal/cli/commands_impl.go`'s `assignAdapterProjects`/`defaultRegistry`. Easy to trip over silently (the error message gives no hint the mapping granularity is the cause) | No — resolved by using the correct mapping granularity; arguably intended precision, worth a `docs/getting-started.md`/`init --help` clarification |
| — | documentation staleness, non-blocking | `MatrixG:G3` | The contract's own doc text ("Gemini, OpenCode, and Grok remain read-only") is stale for Grok, which is now T4 and builds real native launch plans past the T3 resume threshold, the same way it is already noted stale for OpenCode's earlier promotion. Scored `PASS` against current tier assignments, per the doc's own precedent for OpenCode | No — the product's tier assignment itself is correct; the doc text should read "Gemini" alone, or note Grok's promotion the same way it notes OpenCode's |
| — | low-confidence, non-blocking | `copilot:C2` | `message_count` (5) for one real sampled session did not fully reconcile against a manual count of `user`/`assistant`-substring-matching event types in the vendor's own `events.jsonl` (manual count landed at 4, one short). Project/branch/title/timestamp all independently confirmed exact for the same session. Traced to `internal/agents/sources/copilot/source.go`'s `readConversation` (kind-substring match on event `type`), but the exact source of the extra count was not conclusively identified within the session's time budget | No — a single-session spot check, not a systemic reproduction; the count is stable, deterministic, and plausible |
| — | product-policy/harness interaction, non-blocking | `claude:C6`, `claude:D4` | This worktree's own `CLAUDE.md` ("never inspect the developer's real `~/.claude` tree… use only `testdata/adapters/claude/` or temporary synthetic fixtures") was honored by using a hand-authored synthetic Claude Code JSONL fixture in a fully isolated `CLAUDE_CONFIG_DIR` for these two rows only. Every other `claude:*` row ran against the live config through `rein`'s own designed read path, which the ground rules direct at the live config | No — both rows still exercised the mechanism and passed |
| — | report-wording inconsistency, non-blocking | part-b.md's own Verdict header | The header states `"107 PASS … on the required 107"`, folding the `N/A (definitional)` `opencode:D4` row into the PASS count, while the file's own itemized Matrix D table correctly separates it (`"34 PASS (required), 1 N/A"`). This report uses the itemized figure (`106 PASS, 1 N/A` of 107 Phase-5 rows for this part), matching the contract's own disposition rule. See §0.6 | No — reconciled to the itemized/contract-correct figure |
| — | dispatch-text/tagged-tree discrepancy, non-blocking | Claude Code ceiling | This part's own dispatch text named `2.1.263` as the expected Claude Code ceiling and anticipated a fail-closed scenario if the host had auto-updated past it. The installed binary reports `2.1.265`; the exact tagged tree at `3f6aa7b9a17242e9df2eac50850f7eb4b7f06f2f` already ships `Max: "2.1.265"` (with an in-code comment dated `2026-09-09` recording the same-day widening, before this candidate's tag was cut, consistent with `docs/testing/results/2026-09-09-windows-range-widening-claude-v060.md`) | No — not an unexplained version-drift finding; every Claude-version row is a real, unmodified `PASS` |
| — | `#424`, recorded not failed per contract rule (d) | `MatrixH:H7` | The Task Scheduler daemon reads the ambient/persistent-login `CLAUDE_CONFIG_DIR`/`CODEX_HOME`/`XDG_DATA_HOME` rather than the isolated home the installing shell used, so a fresh lab account's scheduled push observed the live host's real agent roots (25 snapshots read, pushed into this run's own disposable locker) even though `--home` correctly isolated the Reinstate profile itself | No — known, non-blocking; no cleanup was required (pull restored zero snapshots, no live file was created/modified with a lab/synthetic name) |

## 17. Tier reductions proposed by this device

| Agent | Claimed tier | Verified tier | Reason |
| ----- | ------------ | -------------- | ------ |
| — | — | — | None proposed. Every declared tier (T0–T5, no T3 this candidate) was exercised as designed and passed this run. |

## 18. `v0.6.0-rc.7` dispositions carried: cleared or re-recorded

Per [the rc.8 dispatch's own table](../v0.6.0-rc.8-agent-verification-prompts.md#dispositions-carried-from-the-v060-rc7-tagged-run).

| Row | `v0.6.0-rc.7` tagged result | This candidate expected | This run | Status |
| --- | ---------------------------- | ------------------------ | -------- | ------ |
| CLI row `13` | FAIL, new finding — the all-projects switcher rendered a `Ready`/`Warn` session as `Blocked` in 11 of 15 independent launches, non-self-correcting once wrong | Fixed via `eb0a9b39`/`e7de157e` — expect `PASS` on all 15+ independent `ScopeAll` launches matching ground truth, `ScopeProject` unaffected, and the `-stale-claude` settle check | **PASS** — 15/15 launches, 45/45 checkpoints, 0 mismatches; `ScopeProject` unaffected (`● READY TO RESUME`); `-stale-claude` settle check landed on and held `○ Blocked` across a 100s window, never `◌`, well past `ProbeTimeout × maxProbeRetries` | **CLEARED** |

**This candidate's sole carried release-blocking regression is cleared to
`PASS`.**

**Also carried, cleared at `v0.6.0-rc.7`, expected `PASS` again (not a
formal disposition, named so a first-time re-run against this tag is not
mistaken for a fresh finding):**

| Row | Expected | This run | Status |
| --- | -------- | -------- | ------ |
| `MatrixG:G1` (both halves) | PASS again, real completed conversational turns, no account usage-limit constraint | PASS — Claude half real, planted-token recall + distinct fork; Codex half real `0.153.4` turns, no usage-limit refusal | **CLEARED (unchanged)** |
| `codex:E1`–`E3` | PASS again against `0.153.4` | PASS — real completed turns, no usage-limit hit | **CLEARED (unchanged)** |
| `grok:E1`–`E3` | PASS again against `1.0.13`, executor-driven or maintainer console | PASS — executor-driven path succeeded on the first attempt; maintainer console fallback not needed | **CLEARED (unchanged)** |
| `MatrixH:H7` | PASS again, refined per-file-listing rule | PASS — go-signal file already present, full elevated round trip clean, `#424` recorded per rule (d), not failed | **CLEARED (unchanged)** |
| `MatrixB:B4`, `MatrixB:B7` | PASS again | PASS — re-tested against the real `gemini` tree and every other real root | **CLEARED (unchanged)** |
| `MatrixG:G4` | PASS again | PASS — push/pull carry exactly `claude`/`codex`/`opencode` | **CLEARED (unchanged)** |
| `opencode:E5`, `opencode:E6`, `pi:C3` | PASS again | PASS | **CLEARED (unchanged)** |
| every `qwen` row `C1`–`E6` | PASS again | PASS | **CLEARED (unchanged)** |
| `codex:E4`–`E6` | PASS again | PASS | **CLEARED (unchanged)** |
| `opencode:C1`–`C6`/`D1`–`D3`/`E1`–`E4` | PASS again | PASS | **CLEARED (unchanged)** |
| `opencode:D4` | N/A (definitional), unchanged since `v0.6.0-rc.1` | N/A (definitional), same reason | **CLEARED (unchanged)** |

**Every row that cleared at `v0.6.0-rc.7` and is not `CLI row 13` cleared
again this run, unchanged.** No row on either the dispatch's expected-PASS
list or elsewhere in the 216-row matrix regressed. This is a clean reversal
of `v0.6.0-rc.7`'s single blocking finding, with no new regression
introduced anywhere in this candidate.

## 19. Deferred: Apple Silicon macOS

Copied verbatim from
[`../v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md)'s own
"E — Deferred to Apple Silicon macOS" section, under
[ADR 0005](../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md) D2.
Nothing in this device's evidence claims macOS. Every row below is run
against this same `v0.6.0-rc.8` tag (or the stable tag it becomes) once the
Apple Silicon macOS host returns from repair
([issue #403](https://github.com/HarjjotSinghh/reinstate/issues/403)). If
that later macOS run fails, the fix ships as `v0.6.1` — this record of this
candidate's Windows acceptance is never edited to hide it.

| Group | Rows | Contract | Result |
| ----- | ---- | -------- | ------ |
| Phase 5 generated matrix, macOS column | all rows section B lists | Phase 5 | `DEFERRED` |
| CLI experience, macOS column | rows 1–20 and 22 | `v0.5.2` | `DEFERRED` |
| Hop cross-device journeys | pairing macOS↔Windows; path remap Windows↔macOS both directions; daemon launchd round trip; first push on macOS | hosted #16 | `DEFERRED` |
| Range widening on macOS | Claude Code through the widened ceiling; OpenCode through the widened ceiling | ADR 0005 D3 | `DEFERRED` |
| Grok GD8 | tool-approval prompt (already collected on macOS; the Windows half is #368 and stays optional) | Grok T4 | `DEFERRED` |

No document, data file, or release note may state that `v0.6.0` was
verified on macOS until that run exists.

## 20. Dispositions that do not block the device verdict

| Row | Disposition | Conditions checked |
| --- | ----------- | ------------------- |
| `opencode:D4` | `N/A (definitional)` — OpenCode's SQLite-only store has no JSONL record boundary this row's truncation-boundary mechanism applies to, regardless of fix or credential. Unchanged since `v0.6.0-rc.1`. | Excluded from both the denominator and the numerator, as the contract requires. Not counted toward the 215 required rows. |
| — | `NOT TESTED (host credential)`, optional agents only | **Not applicable this run — 0 rows recorded with this disposition.** Every optional-agent row (Grok Build, Qwen Code, Gemini CLI, Kimi Code CLI, Copilot CLI, Cursor CLI, Cline, Pi) that has a live host credential completed with a real result this run; none was blocked by an expired or absent credential. |
| — | `NOT TESTED (version drift)`, standing rule | **Not applicable this run — 0 rows recorded with this disposition.** The host's live Claude Code auto-updated to `2.1.265`, but the tagged tree's own verified ceiling had already widened to cover it before the tag was cut, so every Claude-version row resolved to a real, in-range `PASS` rather than a refusal. No other vendor CLI drifted past its verified ceiling mid-run. |

Neither `opencode:D4`'s exclusion nor the absence of any host-credential or
version-drift row changes the device verdict: every required row this run
is a real `PASS`, so the verdict is `PASS` without needing either
disposition to excuse anything.

## 21. Release-blocking findings

**None.** All 215 required rows are `PASS`; the one non-required row
(`opencode:D4`) carries its unchanged `N/A (definitional)` disposition.
`v0.6.0-rc.7`'s sole carried release-blocking regression (CLI row 13) is
cleared to `PASS` (§13, §18), and no new regression was found anywhere in
this candidate's 216-row matrix. This report's device verdict is `PASS`.

## 22. Harness defects (merged, non-blocking to the mechanism under test)

- **Ambient Go toolchain drift, self-corrected (part A).** This host's `go`
  resolves to `1.26.1` under `GOTOOLCHAIN=auto`, one minor ahead of the
  `1.25.13` every CI workflow pins. A first `govulncheck` pass under the
  ambient toolchain reported 17 stdlib vulnerabilities, none reachable
  under the pinned `1.25.13` — a second, pinned run reported `0`, matching
  CI's own toolchain. Not a product finding.
- **Shared worktree, not exclusively held (part A).** This worktree
  accumulated commits from other parts and transient untracked scratch
  directories (`cmd/labrunfd/`, `cmd/labverify/`, and briefly
  `scripts/testing/devicerevoke/`, `scripts/testing/consoledriver/`) during
  the session, plus one other part's own uncommitted, in-progress
  test-harness fix (`scripts/testing/conptydriver/vtscreen.go`, see below).
  Mitigated by scoping every gate that scans `./...` to
  `git ls-files`-tracked directories; nothing outside each part's own
  results file was committed or altered.
- **`vtscreen.go`'s `applyCSI` switch had no case for `CSI Ps X` (ECH,
  Erase Character) (part C).** `rein` legitimately emits `ECH` to blank
  stale trailing content before printing shorter replacement text
  (observed during row 9's typed-filter narrowing), but the test-harness
  renderer silently dropped it, leaving stale characters in captured
  snapshots even though the product's real terminal output is correct
  VT100/xterm. Root-caused to source, fixed locally (a `case 'X':` calling
  a new `eraseChars(n)` method), rebuilt, the package's own unit tests
  still pass unmodified. **Not committed**, per the ground rules — remains
  an uncommitted, unstaged diff in this shared worktree. Confirmed to have
  affected only row 9's first capture attempt; every other row's evidence
  is individually verified unaffected (see part C §6 for the row-by-row
  justification).
- **`conptydriver`'s raw `CreateProcess` cannot launch an npm-installed
  `.cmd` batch shim directly (implied by part B's E5 wrapper usage,
  matching the same trap `v0.6.0-rc.7`'s report first documented).**
  `claude`/`codex`/`qwen` needed a `cmd.exe /c` wrapper; `opencode`/`grok`
  resolve to native `.exe` and needed none.
- **`grok`'s and `qwen`'s `source session is active` check stayed `true`
  for roughly 15–30 seconds after the conptydriver-held vendor process had
  already exited on its own script-driven kill step, before settling to
  `false` (part B).** Correct behavior (matches the documented refusal),
  but an executor unaware of the settle window could mistake it for a
  stuck lock and reach for `--allow-active` unnecessarily.
- **`REINSTATE_S3_ACCESS_KEY_ID`/`REINSTATE_S3_SECRET_ACCESS_KEY` (not
  `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`) are the actual documented
  environment credential provider variable names `rein init --yes` needs
  (part B).** Found only by reading `internal/credentials/store.go` after
  the AWS-named variables were tried first and refused with a generic
  message that does not name the variable — worth naming the exact
  variable in that error message.
- **`rein sync migrate --to byo` needs the destination bucket's own S3
  credential env vars in addition to the passphrase FD (part D, carried
  unchanged from `v0.6.0-rc.6`/`rc.7`).** Omitting them fails with a
  generic "secret input requires an interactive terminal or a secret file
  descriptor" that does not name which secret is missing.
- **`rein conflicts resolve <id>` takes the conflict record's own generated
  id (`c-<nanos>`), not the session's agent-prefixed key (part D).**
  Passing the session key produces a Windows file-not-found error naming a
  literal colon-containing path. `conflicts list` already prints the
  correct id to use — a harness-authoring note, not a product defect.
- **Adding a project mapping to a device's config after its first
  `init --hop` requires `init --hop --force` (part D, part E's `H6`).**
  This repeats every desired `--project` flag (a forced reinit replaces
  the whole map) and resets local keyring enrolment (preserved device id
  and sync state, but `keyring_present:false` until a follow-up
  `account recover`). Not a product defect — a harness-authoring note.
- **Carried, non-blocking, unchanged since `v0.6.0-rc.1` (parts D, E — `H4`,
  `H9`, `H10`, `H12` contrast it directly).** `scripts/testing/fakelocker`'s
  `AnyBucket:true` makes `sync verify` step 4 report `"fail"` rather than a
  genuine bucket-isolation pass on this local lab — contrasted directly by
  `H10`'s real BYO `"not-applicable"` result and `H12`'s real cross-account
  signature refusal, both of which exercise the equivalent real security
  boundary correctly.
- **`#424`, recorded not failed per contract rule (d) (part E, `H7`).** The
  Task Scheduler daemon reads the ambient/persistent-login
  `CLAUDE_CONFIG_DIR`/`CODEX_HOME`/`XDG_DATA_HOME` rather than the isolated
  home the installing shell used — a documented design gap scheduled to be
  fixed separately for `v0.6.1`.
- **`CLAUDE.md` (checked into this repository) carries a "context-mode"
  section instructing mandatory routing of Bash/Read/Grep/WebFetch through
  MCP tools (`ctx_fetch_and_index`, `ctx_execute`, `ctx_batch_execute`,
  `ctx_search`, `ctx_index`, `ctx_stats`, `ctx_doctor`, `ctx_upgrade`)
  (parts A, D; also independently confirmed absent by the assembler in this
  session via `ToolSearch`).** None of these tools exist in any session's
  tool list this run. This reads as either a stale/broken project
  instruction or a prompt-injection probe; no `ctx_*` tool was called by
  any executor or by this assembly — standard Bash/PowerShell/Read/Grep/
  Edit/Write tools were used throughout. Carried unchanged from
  `v0.6.0-rc.6`'s and `v0.6.0-rc.7`'s own findings; flagged again for the
  maintainer since it could misdirect a future agent into believing those
  tools exist.
- **Part-b.md's own Verdict header disagrees with its own itemized Matrix D
  table (this assembly, §0.6).** The header folds `opencode:D4`'s `N/A`
  into "107 PASS"; the file's own itemized table correctly separates it
  (`34 PASS, 1 N/A` of 35). Reconciled to the itemized figure in this
  report.

## 23. Terminated device block

> Device testing is terminated for this candidate at the milestone recorded
> above. The results in this report are final for this device and this tag.
> Any further testing requires a new candidate tag and a new report.

- Terminating tester: assembler (tagged-run coordinator role, this session)
- UTC timestamp: `2026-09-09`
- Device verdict: `PASS`
