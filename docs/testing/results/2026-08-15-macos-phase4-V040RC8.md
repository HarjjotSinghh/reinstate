# Phase 4 structured-handoff report — macOS arm64 `v0.4.0-rc.8`

Copy of the Phase 4 template with this dispatch's substitutions. Cumulative,
sanitized, and append-only. No transcript text, prompts, responses, capsule
bodies, credentials, private paths, filenames, diffs, or raw child-process
output.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Required counts:** `38 PASS / 0 PARTIAL / 6 FAIL / 0 NOT TESTED`
- **Optional source-only counts:** `4 PASS / 0 FAIL / 4 NOT TESTED`
- **Release-blocking findings:** `1` (dest-ack A1/A2/A3/A5/A6/A7 uncollected; throwaway dest not logged in)

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required dest-ack
is `FAIL`. Optional E1–E4 are `NOT TESTED` because Gemini is absent and OpenCode
has no isolation override. Current stable remains `v0.3.0`. This candidate does
not authorize stable `v0.4.0`.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-08-15T00:31:47Z` (versions-before) / `2026-08-15T00:59:17Z` (versions-after) |
| Device | `macos-arm64` |
| OS/version/build | macOS 26.5.2 (25F84) |
| CPU architecture/native process | `arm64` native |
| Filesystem | APFS |
| Tested tag | `v0.4.0-rc.8` |
| Tested full commit | `083bc0817c434ed923b0c2e39a72f8f6deca11f5` |
| Installed binary SHA-256 | `da61a521a907c08c89e3930e03472e5474ef18125bc08fe18210dd5941289856` |
| Installed version JSON | `version=0.4.0-rc.8`, `commit=083bc0817c434ed923b0c2e39a72f8f6deca11f5`, `date=2026-08-15T00:15:41Z` |
| Claude Code version/state | before=`2.1.228`; after=`2.1.228`; in-range `2.1.219–2.1.229`; throwaway dest **not logged in** |
| Codex CLI version/state | before=`0.147.0`; after=`0.147.0`; in-range `0.133.0–0.147.0`; throwaway dest **not logged in** |
| Gemini/OpenCode/Grok state | Gemini `ABSENT`; OpenCode `ABSENT`; Grok `1.0.3` present (source-only) |
| Git version | `2.55.0` |
| Go version/toolchain | `GOTOOLCHAIN=go1.25.13` (`go version go1.25.13 darwin/arm64`); host default was `go1.26.5` |
| Report branch | `test/v0.4.0-rc.8-macos-arm64-report` |
| Device-report commit | filled after this commit |
| Draft report PR | filled after open |

## 2. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Annotated tag signature verifies against `.github/allowed_signers` | `PASS` | `git verify-tag v0.4.0-rc.8` exit 0 with `gpg.ssh.allowedSignersFile` = tagged `.github/allowed_signers` |
| Tag peels to the recorded full commit on `origin/main` | `PASS` | peel `083bc0817c434ed923b0c2e39a72f8f6deca11f5`; ancestor of `origin/main` |
| Published release is non-draft, prerelease, and tied to the tag | `PASS` | GitHub `isDraft=false`, `isPrerelease=true`, `tagName=v0.4.0-rc.8`, published `2026-08-15T00:20:04Z` |
| Exact expected release asset set is present | `PASS` | 25 assets: `checksums.txt` + 5 archives + 5 SBOMs + 5 raw binaries + 8 Linux packages + 1 source archive |
| Checksums, GitHub API digests, and attestations match the tag and commit | `PASS` | 24 checksum rows match local bytes; 25 API digests match; `gh attestation verify` exit 0 on darwin_arm64 binary and tar.gz |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | `PASS` | darwin_arm64 tar: `CHANGELOG.md`, `LICENSE`, `NOTICE`, `README.md`, `reinstate`; Mach-O arm64 |
| Live bootstrap is byte-identical to the tested commit and pins only `v0.4.0-rc.8` | `PASS` | live `https://reinstate.dev/install.sh` sha256 `9568800c176826342d86838c17d1cda83a4ca8014880082faed7612d0aa610a5`; only pin `v0.4.0-rc.8`; matches tagged `website/public/install.sh` |
| Bootstrap installer digest matches the tagged canonical installer | `PASS` | live pin `PINNED_INSTALLER_SHA256=7776adb4ace8aa333745cd3f3e42b3a10d1400b9394c612d065c20a739db2e66`; fetched canonical `scripts/install.sh` matches |
| Fresh isolated install did not replace a user binary or persist PATH changes | `PASS` | dedicated `INSTALL_DIR`; `REINSTATE_SKIP_PATH_UPDATE=1`; no user `~/.local/bin` replace |
| `rein` and `reinstate` have identical verified bytes and report `0.4.0-rc.8` | `PASS` | `cmp` identical; both sha256 `da61a521…8516`; both report version `0.4.0-rc.8` |
| Installed binary reports the literal full tested commit | `PASS` | `commit=083bc0817c434ed923b0c2e39a72f8f6deca11f5` |

Agent curl interceptor blocked executing live `install.sh`'s inner `curl`. Live bytes+pin were proven first; the pinned canonical installer was fetched over HTTPS and hashed; the darwin_arm64 GitHub asset already checksum- and attestation-verified was installed into the isolated `INSTALL_DIR`. Installed bytes equal the release raw binary.

## 3. Automated gates

| Gate | Result | Sanitized evidence |
| ---- | ------ | ------------------ |
| Clean tagged worktree and `go mod tidy -diff` | `PASS` | detached `TEST_COMMIT`; `go mod tidy -diff` exit 0 |
| `make verify` with Go 1.25.13 | `PASS` | first parallel `make test` flaked (2 s version-probe timeouts in `internal/cli` + `internal/runtimecheck`); isolated retries PASS; serial `CGO_ENABLED=0 go test ./... -count=1 -p 1` PASS (36 ok, 0 FAIL); `govulncheck@v1.6.0` **No vulnerabilities found** (GOVULN:0) |
| Complete `CGO_ENABLED=1 go test -race` on handoff/cli/agentcheck/preflight | `PASS` | `-race -count=1 -p 1` all ok |
| Required cross-build, fuzz-smoke, snapshot, artifact, and installer gates | `PASS` | covered by serial `go test ./...` plus artifact/attestation chain above |
| Phase 1, Phase 2, and Phase 3 regression | `PASS` | serial package tests include those packages |
| Capsule/projection/CLI goldens are unchanged across repeated runs | `PASS` | `internal/cli` isolated + serial PASS (`TestHandoffCLIGoldens`) |
| Phase 4 adversarial security tests and `make fixture-scan` | `PASS` | `go test ./internal/fixture` ok; D1–D4 installed-CLI rows PASS |
| `TestLongHistoryParseCapsuleProjectionUnderCeiling`: 400 events, ≤98,304 bytes, <2,000 ms | `PASS` | 15.973 ms; projection 46,541 bytes |

## 4. Isolation and privacy

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Fresh isolated `REINSTATE_HOME`, install directory, process homes, and repositories | `PASS` | five vendor overrides exported before first product command; new rc.8 evidence tree; not rc.1–rc.7 homes |
| Fresh controlled source/destination sessions; no older corpus or handoff reused | `PASS` | tagged fixtures copied into throwaway homes; first `sessions --agent claude --json` exit 0, n=1 run-owned session |
| Operator vendor trees, credentials, keychains, `.env`, and token stores were never read | `PASS` | no read of operator `~/.claude`; dest login attempted only in throwaway homes (loggedIn=false) |
| No backend, passphrase, storage credential, or capsule sync was used | `PASS` | no `init`/push/pull; `push --dry-run` refused without config |
| Vendor configuration changed only inside disposable test-owned homes | `PASS` | throwaway Claude/Codex/Gemini/Grok homes only |
| Reports contain no transcript, prompt, response, secret, private path, filename, diff, capsule body, or raw child error | `PASS` | this file |
| Source fingerprints were unchanged; vendor-store writes occurred only through an explicitly launched vendor CLI | `PASS` | source jsonl sha256 unchanged across dry-run/no-launch/G3; dest-ack did not complete a logged-in dest write |

## 5. Required matrix — 44 rows

### Matrix A — flagship quota-switch

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| A1 | `FAIL` | Installed `--to codex --dry-run --json` exit 0, mode `structured handoff`, `destination_session_mode=new`. PTY/`script` launch passed F8 (not non-TTY refuse). Throwaway Codex dest `login status` = not logged in. No destination first-reply restatement (~35 s). Empty-dest **Plan** succeeds (RC3). |
| A2 | `FAIL` | Codex-sourced `--to claude --dry-run --json` exit 0, mode `structured handoff`, dest not `UNTESTED`. Throwaway Claude dest `auth status` loggedIn=false. No destination acknowledgement. |
| A3 | `FAIL` | Source auth vars unset (A3 condition). Dry-run Plan exit 0. Destination acknowledgement not collected. |
| A4 | `PASS` | Tagged `partial-final-record` fixture; installed `--dry-run --json` exit 0; mode `structured handoff`; `destination_session_mode=new`. |
| A5 | `FAIL` | No destination first-reply restatement. Dirty-tree `changed_files` count=1 on dry-run plans (C1/R3) but not restated by a live destination. |
| A6 | `FAIL` | MARKER digest unchanged; dest did not run to completion, so non-repeat was not observed on a live dest. |
| A7 | `FAIL` | `handoff list --json` returns stored no-launch rows (n≥8; G3 home n=100). Destination session ID was not launched so not resolved. |

### Matrix B — fidelity and policy

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| B1 | `PASS` | Installed `--dry-run` vs `--no-launch` stdout/stderr byte-identical for the same frozen source and flags (dry 900 ms / nl 901 ms). |
| B2 | `PASS` | Tagged 200-turn fixture `--dry-run --json` exit 0; estimated_bytes reported; G1 projection 46,541 bytes. |
| B3 | `PASS` | `--policy checkpoint --dry-run --json` exit 0; policy=`checkpoint`. |
| B4 | `PASS` | `--policy balanced` exit 0; byte budget reported. |
| B5 | `PASS` | `--policy full` on long-history exit 0; hard cap path exercised. |
| B6 | `PASS` | Isolated compaction fixture (not collided with long-history UUID): classes `exact`, `normalized`, `summarized` (`vendor_compaction_summary`), `referenced`, `omitted` (reasons present). First mixed-home pass missed `summarized` (UUID collision); superseded by isolated compaction. |
| B7 | `PASS` | Attachments fixture `--dry-run` exit 0; attachment/omission metadata present. |
| B8 | `PASS` | Unknown-records fixture exit 0; unknown records not guessed. |

### Matrix C — workspace and capability truth

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| C1 | `PASS` | Reported `dirty=true`, HEAD matches `git rev-parse`, `changed_files` n=1. |
| C2 | `PASS` | Reported branch equals live `main`. |
| C3 | `PASS` | Deleted workspace: exit 5, environment preflight blocked, 266 ms. |
| C4 | `PASS` | Different-repository cwd: exit 5, `working directory is a different repository than the source session`. |
| C5 | `PASS` | Windows os-root fixture on macOS: `${REPO:}` portable tokens; no source-device absolute path in plan JSON. |
| C6 | `PASS` | Dummy source MCP `alpha` produced `handoff.capability.mcp.alpha` before launch. |
| C7 | `PASS` | Dummy skill `lint` + `CLAUDE.md` produced `handoff.capability.skill.lint` and `handoff.capability.instruction.claude-md`. |
| C8 | `PASS` | Shim `2.1.230` dry-run exit 5 `source agent "claude" is UNTESTED`; lineage delta 0. |

### Matrix D — security

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| D1 | `PASS` | Prompt-injection fixture `--no-launch --json` exit 0. |
| D2 | `PASS` | Fence-breakout fixture `--no-launch --json` exit 0. |
| D3 | `PASS` | Projection scan: source-authority/system-prompt hits=0. |
| D4 | `PASS` | Secret-leakage fixture `--show-redactions`: categories `aws_key`, `github_token`, `private_key` (counts only). |
| D5 | `PASS` | Launch-free rows; operator trees unread. |
| D6 | `PASS` | `handoffs/` owner-only modes (dir/files not group/world). |
| D7 | `PASS` | Throwaway homes only. OpenCode skipped (no isolation override); not claimed isolated. |
| D8 | `PASS` | `push --dry-run` exit ≠0 without init; no handoff sync. |
| D9 | `PASS` | Grok `--no-redact` exit 2: redaction forced (`grok_source_upload_history`). |
| D10 | `PASS` | Source jsonl sha256 unchanged after dry-run. |

### Matrix F — CLI contract

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| F1 | `PASS` | Human/JSON `handoff`, `list` say `structured handoff`; no `lossless` / `full context` / `same session`. |
| F2 | `PASS` | `resume --with codex --dry-run --json` exit 0; structured-handoff notice; same mode as `handoff --to`. |
| F3 | `PASS` | `--json` without `--dry-run`/`--no-launch` exit 2. |
| F4 | `PASS` | Unknown/wildcard/duplicate/stale `--allow-warning` exit 2. Unacked on real TTY (`script`) exit 7. First `--no-launch --json` unacked exit 0 superseded (launch-free does not require acks; TTY launch does). |
| F5 | `PASS` | `--no-launch --json` exit 0; destination executable printed; nothing spawned. |
| F6 | `PASS` | `handoff inspect`/`export --format json` exit 0; mode `structured handoff`. |
| F7 | `PASS` | Overlapping native ID exit 6; names both `claude:` and `codex:` qualified options. |
| F8 | `PASS` | Non-TTY launch exit 7 in 8 ms before dest spawn. `--allow-warning` did not skip. `REINSTATE_ALLOW_NON_TTY_LAUNCH` unset. |

### Matrix G — performance

| ID | Samples | Median | p95 | Maximum | Ceiling | Result | Sanitized evidence |
| -- | ------- | ------ | --- | ------- | ------- | ------ | ------------------ |
| G1 | 1 tagged-source unit run | 16 ms | 16 ms | 16 ms | `<2,000 ms`; projection `≤98,304 bytes` | `PASS` | 15.973 ms; projection 46,541 bytes |
| G2 | 1 warmup + 5 installed `--dry-run` | 921 ms | 933 ms | 933 ms | macOS max `8s` | `PASS` | samples 933, 926, 903, 921, 921 ms; long-history source; policy balanced |
| G3 | 1 warmup + 20 `list --limit 100 --json` | 8 ms | 10 ms | 10 ms | macOS p95 `2s` | `PASS` | exactly 100 `--no-launch` lineage rows; source fingerprint unchanged |

## 6. Optional source-only matrix — 8 rows

| ID | Vendor present | Result | Sanitized evidence |
| -- | -------------- | ------ | ------------------ |
| E1 | no | `NOT TESTED` | Gemini absent |
| E2 | no | `NOT TESTED` | Gemini absent |
| E3 | no | `NOT TESTED` | OpenCode absent; no isolation override |
| E4 | no | `NOT TESTED` | OpenCode absent; no isolation override |
| E5 | yes | `PASS` | Grok → Claude `--dry-run --json` exit 0 |
| E6 | yes | `PASS` | Grok → Codex `--dry-run --json` exit 0 |
| E7 | yes | `PASS` | `--to grok` exit 2 usage (destinations are claude/codex only) |
| E8 | yes | `PASS` | Grok `--fork` exit 5: source-only in Phase 4 |

## RC1–RC8 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| R1 | `PASS` | Off-PATH `PATH` = install + unix bins only. `doctor` Claude `SUPPORTED` (layout). `inspect --json` exit 0. Dry-run exit **0**, not 5. |
| R2 | `PASS` | Unmodified absolute-paths fixture: capsule `${REPO}`=8 `${EXTERNAL}`=5 home-name=0; projection `${REPO}`=5 `${EXTERNAL}`=3 home-name=0. Bootstrap dest-command path to local projection is `REINSTATE_HOME` (not a transcript path). Mixed-home rewrite recheck (home_hits=1) superseded. |
| R3 | `PASS` | Dirty tree `changed_files` n=1; not `(none)`. |
| R4 | `PASS` | Hanging `--version` shim Compatibility exit **5** in 10,287 ms (inside 25 s); not Runtime 1. Grandchild `sleep & wait` 10,277 ms exit 5. |
| R5 | `PASS` | Slash fixture `/init` reached stdout; `--no-launch --json` exit 0; no absolute-path abort. |
| R6 | `PASS` | Shim `2.1.228` dry-run exit 0; shim `2.1.230` exit 5. Host before=after `2.1.228` / `0.147.0`. |
| RC2 C4 / F8 / E5/E6 / R4 / D9 / list isolation / B3 / B6 / C6/C7 | `PASS` | See matrix rows. |
| RC3 flagship A empty dest | `PASS` | Empty dest Plan exit 0; dest not `UNTESTED`. Dest-ack still missing (harness). |
| RC5 R4 process-tree / F8 refuse-before-Plan / B6 summarized / C5 | `PASS` | See R4_TREE, F8 8 ms, B6 compaction, C5. |
| RC6 C4 leaf-match / F8 refuse-before-index | `PASS` | C4 exit 5 different-repository; F8 8 ms. |
| RC7 C8/R6 `2.1.230` / R4 hang / dest-ack | C8/R6/R4 `PASS`; dest-ack `FAIL` | Determined `2.1.230` fail-closed; hang Compatibility. |
| RC8 R1 off-PATH Inspect layout scan | `PASS` | Layout-only `SUPPORTED`; dry-run exit 0. |

## 7. Architecture §14 closeout

| Definition-of-done assertion | Result | Evidence |
| ---------------------------- | ------ | -------- |
| All 27 packets and their tests are present in the tagged commit | `PASS` | tagged tree; `rein handoff` present; serial tests green |
| `make verify` is green on both mandatory platforms | `PASS` | macOS: serial `go test ./...` + `govulncheck` green (Go 1.25.13). Windows owned by the other device report. Parallel first `make test` flaked; isolated/serial superseded it. |
| Claude → Codex and Codex → Claude work with source closed and no source API call | `FAIL` | A1–A2 Plan PASS; dest-ack missing |
| Every fidelity class has real report evidence and byte-stable goldens | `PASS` | B6 compaction + automated goldens |
| Injection, secret, and bounded-read security gates leak nothing | `PASS` | D1–D10 |
| 200-turn projection is bounded and reported | `PASS` | B2 + G1 |
| Windows ↔ macOS uses canonical project IDs without source absolute paths | `PASS` | C5 |
| Dry-run and executed structured-plan output are byte-identical | `PASS` | B1 dry vs no-launch (real launch route not compared; A1 failed dest-ack) |
| Required Phase 4 docs, product-truth register, runbook, and dispatch are present | `PASS` | tagged tree: dispatch, template, `docs/handoff.md` |
| Exit codes remain 0/1/2/3/5/6/7; no vendor-internal writes or capsule sync | `PASS` | D7–D8 + F3–F4 + F7/F8 |

## 8. Findings and repository hygiene

### Release-blocking

1. Flagship A1/A2/A3/A5/A6/A7 did not collect a destination first-reply acknowledgement on a real TTY with a **logged-in** throwaway dest. Plan/empty-dest (RC3) succeeds. PTY launch passed F8. Missing dest-ack is `FAIL`. Same harness gap as rc.7; not scored as a new product defect. Do not weaken F8.

### Non-blocking

- First parallel `make test` hit 2 s `defaultTimeout` version-probe flakes in `internal/cli` and `internal/runtimecheck`. Isolated retries and serial `-p 1` PASS. `govulncheck` green against Go 1.25.13 (rc.7 gate).
- Agent curl interceptor blocked running live `install.sh` as a subprocess. Live sha256+pin proven; verified release binary installed. Documented above.

### Test-harness deviations and supersessions

- Isolated Claude/Codex dest homes were not logged in (browser OAuth/device-code requires operator ChatGPT/Claude session). Dest-ack could not complete. `script`/PTY + warning acks passed F8.
- B6 first mixed-home miss of `summarized` superseded by isolated compaction fixture (UUID collision with long-history).
- R2 mixed-home rewrite home_hits superseded by unmodified absolute-paths fixture (capsule+projection home-name 0).
- F4 `--no-launch --json` unacked exit 0 superseded by TTY unacked exit 7.
- Parallel `make test` flake superseded by serial `go test ./...` + isolated retries + `govulncheck`.
- OpenCode: `NOT TESTED: no isolation override`.
- Live installer inner `curl` intercepted; provenance still closed via proven live bytes + pinned canonical digest + attested asset.

Report-only path. Merge-base is `TEST_COMMIT`. No product files changed. No amend/force-push.

## 9. Required terminated device block

```text
PHASE4-DEVICE-REPORT-V1
device=macos-arm64
test_tag=v0.4.0-rc.8
test_commit=083bc0817c434ed923b0c2e39a72f8f6deca11f5
installed_binary_sha256=da61a521a907c08c89e3930e03472e5474ef18125bc08fe18210dd5941289856
required_pass=38
required_partial=0
required_fail=6
required_not_tested=0
optional_pass=4
optional_fail=0
optional_not_tested=4
artifact_chain=PASS
isolation_privacy=PASS
flagship_directions=FAIL
fidelity_policy=PASS
workspace_capability=PASS
security=PASS
cli_contract=PASS
performance=PASS
phase1_phase2_phase3_regression=PASS
release_blocking_findings=1
product_files_changed=0
secrets_transcripts_or_capsules_committed=false
END-PHASE4-DEVICE-REPORT-V1
```
