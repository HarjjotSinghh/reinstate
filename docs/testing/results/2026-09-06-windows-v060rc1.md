# v0.6.0-rc.1 tagged-artifact native Windows acceptance

`PHASE5-DEVICE-REPORT-V1`

This is the assembled device report for the **published, signed GitHub
prerelease `v0.6.0-rc.1`**, commit `63ac5a5b02bacc5ad52ea0826b4d3d77fdc597e0`.
It satisfies [`../phase-5-universal-agent-coverage-acceptance.md`](../phase-5-universal-agent-coverage-acceptance.md)
as specialized by [`../v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md)
and [`../v0.6.0-rc.1-agent-verification-prompts.md`](../v0.6.0-rc.1-agent-verification-prompts.md),
under [ADR 0005](../../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md)'s
Windows-first waiver. It merges five executors' part files
(`2026-09-06-windows-v060rc1-part-{a,b,c,d,e}.md`, all against this same tag)
into the one cumulative report the dispatch requires, plus an assembler's own
independent install and matrix-generation check. The five part files are
removed from the tree in the same commit as this file, per the assembly
instructions; their content is preserved here.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Matrix generated by:** `rein doctor --agents --acceptance-matrix` (re-run
  independently by the assembler against a fresh install; see §0)
- **Required counts:** `201 PASS / 3 PARTIAL / 7 FAIL / 5 NOT TESTED` of
  `216` required rows (`178` Phase 5 generated matrix + `22` CLI experience +
  `16` Hop parity journeys)
- **Release-blocking findings:** `7` (the 7 required-row `FAIL`s below; see
  §16)

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required
result is `FAIL`. Because 15 of the 216 required rows are not `PASS`, the
device verdict is `FAIL` and this report does not authorize stable `v0.6.0`.
No product code was changed by this assembly; every result below is
transcribed from the five part files' own evidence, corrected only where the
assembler's own row-by-row recount found a part's self-reported summary
count did not match its own row table (§0).

### Failing rows (15, none pass a required row)

| Row | Result | Category | Reason (see full evidence in the matrix section below) |
| --- | ------ | -------- | ------ |
| `MatrixH:H6` | FAIL | product (MINOR) | `push --agent`/`pull --agent` shell completion offers no candidates at all |
| `cline:C2` | FAIL | product (MINOR) | `message_count` always `0`; vendor's own sidecar has real content |
| `cline:C3` | FAIL | product (MINOR) | `SearchText` excludes message body by construction |
| `cursor:C2` | FAIL | product (MINOR) | `message_count` always `0`; vendor's own `store.db` has real content |
| `cursor:C3` | FAIL | product (MINOR) | `SearchText` excludes message body by construction |
| `cursor:C6` | FAIL | product (MAJOR, F-CURSOR-ROOTENV) | `CURSOR_CONFIG_DIR` isolation does not reach session discovery; mechanism not exercised |
| `opencode:D5` | FAIL | product (PD-B1) | two dry-runs over an unchanged source produced different `handoff_id`/destination `session_id` |
| `claude:D4` | PARTIAL | host/fixture (re-recorded, same reason as pre-tag) | boundary confirmed; byte-exact offset+hash cross-check blocked by the fixture's non-Windows recorded workspace (correct refusal) |
| `codex:D4` | PARTIAL | host/fixture (same reason) | same as `claude:D4` |
| `qwen:E1` | PARTIAL | host (re-recorded, same reason as pre-tag) | real launch, correct session id; expired host credential (401) blocks a completed turn |
| `opencode:D4` | NOT TESTED | definitional (re-recorded, same reason as pre-tag) | SQLite store has no JSONL truncation boundary |
| `grok:D4` | NOT TESTED | fixture gap (not carried; new this run, see §17) | no committed `partial-final-record` fixture; ad-hoc truncation inconclusive |
| `qwen:E2` | NOT TESTED | host (re-recorded, same reason as pre-tag) | same expired credential as `qwen:E1` |
| `qwen:E3` | NOT TESTED | host (re-recorded from pre-tag's `PARTIAL`, same reason) | same expired credential |
| `qwen:E5` | NOT TESTED | host (re-recorded, reason changed: was WMI, now credential-driven process lifetime) | process exits (401) too fast for this run's polling method |

## 0. Assembler verification

Performed independently of, and in addition to, all five part files, in the
assembler's own fresh directory (`D:\ReinstateAcceptanceProjects\v060-w7b-assembler\`),
never touching any part executor's own lab directories or the shared
worktree's tracked files before this commit.

1. **Checksum.** `sha256sum` of the coordinator's staged
   `reinstate_0.6.0-rc.1_windows_amd64.zip` matched `checksums.txt`
   (`3d1ebf24…26542`) independently.
2. **Install.** Unzipped into a fresh directory
   (`D:\ReinstateAcceptanceProjects\v060-w7b-assembler\install\`), never a
   shared or developer binary. `rein.exe`/`reinstate.exe` `sha256sum`:
   both `d13d683e…24e2c`; `cmp` exit `0` (byte-identical).
3. **Version.** `rein version --json`:
   `{"commit":"63ac5a5b02bacc5ad52ea0826b4d3d77fdc597e0","date":"2026-09-06T11:17:45Z","name":"reinstate","version":"0.6.0-rc.1"}`
   — matches every part file's own artifact-identity table exactly.
4. **Required row count.** `rein doctor --agents --acceptance-matrix --json`
   against this fresh install: top-level `"row_count": 178`,
   `"core_row_count": 33` (`A:10, B:9, G:8, H:6`), 18 agents at tiers
   `T0×7, T1×4, T2×2, T4×2, T5×3` — matches the candidate's census in the
   dispatch exactly (grok/qwen at T4, gemini/kimi at T2, not promoted; no T3
   agent this candidate).
5. **Row-set reconciliation.** Programmatically generated the full set of
   178 expected row identifiers from that JSON (33 core rows + each agent's
   own row list) and compared it against the union of every row id recorded
   in part A (core Matrix A/B/G/H, Matrix F for all 7 T0 agents, Matrix C for
   the 4 T1 agents) and part B (Matrix C/D/E for the 7 T2+ agents): **the two
   sets are set-equal, 178 elements each, zero overlap, zero gap.** Every
   required Phase 5 row appears in exactly one part file. Section A's own
   8-row automated-gates table (part A) is a separate, non-Phase-5 gate set
   and is not counted in the 178, matching the dispatch's own accounting
   (`178 + 22 + 16 = 216`).
6. **Part-B count correction.** Recounting part B's own 110-row table
   (107 Phase 5 rows + 3 sync rows) row-by-row against its self-reported
   summary (`pass:102, fail:1, partial:3, not_tested:4`) found the summary's
   `pass`/`not_tested` figures did not match its own row table: the row table
   itself lists **five** `NOT TESTED` rows (`opencode:D4`, `grok:D4`,
   `qwen:E2`, `qwen:E3`, `qwen:E5`), not four, and therefore **101** `PASS`
   rows, not 102. This report's verdict, counts, and failing-row list use the
   row-by-row recount (`pass:101, fail:1, partial:3, not_tested:5` for part
   B's 110 rows), not part B's own summary line, per this assembly's "never
   invent a result" rule — the correction is a re-tally of the executor's own
   recorded rows, not a new judgment. No other part's self-reported summary
   disagreed with its own row table (parts A, C, D, E all reconciled exactly).
7. **Cleanup.** The assembler's own install and matrix output live under
   `D:\ReinstateAcceptanceProjects\v060-w7b-assembler\` and are deleted after
   this report is committed; nothing from that directory is referenced by
   path elsewhere in this report.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-09-06T11:31:00Z`–`2026-09-06T17:55:00Z` (span across all five parts; each part's own timestamp is in its section below) |
| Device | `windows-amd64` |
| OS/version/build | Windows 11 Pro, `10.0.26200`, native `windows/amd64`, never WSL |
| CPU architecture/native process | `amd64`, native process (no emulation) |
| Filesystem | NTFS |
| Tested tag | `v0.6.0-rc.1` |
| Tested full commit | `63ac5a5b02bacc5ad52ea0826b4d3d77fdc597e0` |
| Installed binary SHA-256 | `d13d683eda6e1d59082be08c0326be196ffaa856cb8b83a35ef181d3ec624e2c` |
| Installed version JSON | `{"commit":"63ac5a5b02bacc5ad52ea0826b4d3d77fdc597e0","date":"2026-09-06T11:17:45Z","name":"reinstate","version":"0.6.0-rc.1"}` |
| Catalog agent count | `18` |
| Catalog tier census | `T0: 7, T1: 4, T2: 2, T3: 0, T4: 2, T5: 3` |
| Generated required row count | `178` (`rein doctor --agents --acceptance-matrix --json`, independently reproduced by the assembler in §0 and by part A) |
| Git version | `2.52.0.windows.1` |
| Go version/toolchain | `go1.25.13` (pinned; host default `go1.26.1`) |
| Report branch | `v060/w7b-tagged` |
| Device-report commit | filled in by the commit that lands this file |
| Draft report PR | NOT CREATED (worktree report, per this run's dispatch — no PR required) |

### Bootstrap deviation (applies to every part below)

The dispatch asks for an install from the live bootstrap (reinstate.dev)
after proving it pins `v0.6.0-rc.1`. At run time the live bootstrap still
pins `v0.5.2-rc.1` because the guarded website deploy's test gate fails on
three Windows-only test files, so every part's install — and the assembler's
own independent install in §0 — came from the published GitHub prerelease
assets (`reinstate_0.6.0-rc.1_windows_amd64.zip`) instead, whose sha256 was
independently re-verified against `checksums.txt` by the coordinator and
separately by every executor, and whose GitHub attestation the coordinator
had already verified.

### Artifact identity (common to all parts)

| Field | Value |
| ----- | ----- |
| Tag | `v0.6.0-rc.1`, published GitHub prerelease, non-draft, `isPrerelease: true`, `targetCommitish: main` (`gh release view`) |
| Full commit | `63ac5a5b02bacc5ad52ea0826b4d3d77fdc597e0` |
| Release workflow run | `34029733057` |
| Windows archive | `reinstate_0.6.0-rc.1_windows_amd64.zip` |
| Archive SHA-256 | `3d1ebf243c6d6f234ffe95955b9504502c340bfda16f42f776db97893bc26542` (matches `checksums.txt`; re-verified independently by every part and the assembler) |
| Installed binary SHA-256 | `d13d683eda6e1d59082be08c0326be196ffaa856cb8b83a35ef181d3ec624e2c` — `rein.exe`/`reinstate.exe` byte-identical (`cmp` exit `0`) in every install |
| `rein version --json` | `{"commit":"63ac5a5b02bacc5ad52ea0826b4d3d77fdc597e0","date":"2026-09-06T11:17:45Z","name":"reinstate","version":"0.6.0-rc.1"}` |
| Previous-release comparison binary (rows 2, 22, G7) | `reinstate_0.5.1_windows_amd64.zip`, SHA-256 `b724ca3da4e124004063032d63240c244fb9a59279076f0e87441d327a826e8c`, installed `rein version --json` commit `e8d1ec28edee73005a51ca8802a04ced369f4bcb`, version `0.5.1` |

## 2. Agent inventory on this device

One row per catalog agent, tiers as generated by the assembler's own fresh
`rein doctor --agents --acceptance-matrix --json` (§0) and cross-checked
against every part's own vendor-version re-check.

| Agent key | Installed | Vendor version | Sessions available | Distinct projects | Declared tier | Tier testable here |
| --------- | --------- | -------------- | ------------------- | ----------------- | -------------- | -------------------- |
| `claude` | YES | `2.1.263` | ≥3 real + fixtures | ≥2 | T5 | yes |
| `codex` | YES | `0.149.0` | ≥3 real + fixtures | ≥2 | T5 | yes |
| `opencode` | YES | `1.18.27` | ≥2 real + fixtures | ≥2 | T5 | yes |
| `grok` | YES | `1.0.5` | ≥2 real + fixtures | ≥2 | T4 | yes |
| `qwen` | YES | `0.21.12` | ≥2 real + fixtures | ≥2 | T4 | yes, credential-limited for E rows (see §16) |
| `gemini` | YES (isolated auth partially worked; see part B §5/INV-B1) | `0.53.0` | fixture (real session written but not resolved by `rein`) | ≥2 (fixture) | T2 | yes, via committed fixture |
| `kimi` | YES | `0.36.1` | ≥2 real | ≥2 | T2 | yes |
| `pi` | YES | `0.73.1` | 3 real | 2 | T1 | yes |
| `cursor` | YES | `3.19.7` | 2 real | 2 | T1 | yes, isolation defective (F-CURSOR-ROOTENV) |
| `copilot` | YES | `1.0.80` | 2 real | 2 | T1 | yes |
| `cline` | YES | `3.0.61` | 3 real | 2 | T1 | yes |
| `aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode` | ABSENT | — | — | — | T0 | yes (refusal/no-capability rows only) |

## 3. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------- |
| Annotated tag signature verifies against `.github/allowed_signers` | PASS | Confirmed by the coordinator pre-run; `gh release view` shows the tag non-draft, prerelease, `targetCommitish: main` |
| Tag peels to the recorded full commit on `origin/main` | PASS | `63ac5a5b02bacc5ad52ea0826b4d3d77fdc597e0` |
| Published release is non-draft, prerelease, and tied to the tag | PASS | `gh release view` |
| Exact expected release asset set is present | PASS | `verify-release.ps1` (A6) |
| Checksums, GitHub API digests, and attestations match the tag and commit | PASS | Coordinator pre-verified (`checksums.txt` match, `gh attestation verify`); independently re-verified by every part and the assembler |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | PASS | `verify-release.ps1`/`test-install.ps1` (A6/A7) |
| Live bootstrap is byte-identical to the tested commit and pins only this candidate | **FAIL** (documented deviation) | Live bootstrap pins `v0.5.2-rc.1`, not this candidate — see the bootstrap deviation note above; installs used the published release assets instead |
| Bootstrap installer digest matches the tagged canonical installer | NOT APPLICABLE this run | Bootstrap not used, per the deviation |
| Fresh isolated install did not replace a user binary or persist PATH changes | PASS | Every part and the assembler installed into its own fresh directory; no user binary replaced |
| `rein` and `reinstate` have identical verified bytes and report the candidate | PASS | `cmp` exit `0` in every install; SHA-256 `d13d683e…24e2c` throughout |
| Installed binary reports the literal full tested commit | PASS | `rein version --json` matches `63ac5a5b02bacc5ad52ea0826b4d3d77fdc597e0` in every install |

The one `FAIL` above is the disclosed, expected bootstrap deviation, not a
surprise finding — the dispatch itself anticipates and pre-authorizes testing
from the verified release assets when this happens.

## 4. Automated gates (Section A of the dispatch; not part of the 178)

Source: part A, run once with `-p 4` from the worktree per the ground rules.

| # | Gate | Result | Evidence |
| - | ---- | ------ | -------- |
| A1 | Format, vet, tidy | PASS | `gofmt -l .` empty; `go vet -p 4 ./...` exit 0; `go mod tidy -diff` exit 0 |
| A2 | Unit suite | PASS | `CGO_ENABLED=0 go test ./... -count=1 -p 4`: every package `ok` or `[no test files]` |
| A3 | Race suite | PASS | `CGO_ENABLED=1 go test -race ./internal/... -count=1 -p 4`: every package `ok` |
| A4 | Lint and vuln | PASS (harness note) | `make lint`/cross-build failures were isolated entirely to two untracked, uncommitted, self-documented throwaway helper files (`cmd/h3probe/main.go`, `cmd/hopfdrunner/main.go`) left on the shared worktree by the concurrent Section-D executor's own in-flight work — not touched by executor A to avoid destroying in-progress state, confirmed gone from `git status` by the time this report was committed. Scoped to `./cmd/reinstate/... ./internal/... ./scripts/...`: `make lint` 0 issues, `make vuln` 0 vulnerabilities in code |
| A5 | Doc gate | PASS | `internal/doctest`: 18/18 PASS; `scripts/check-docs.ps1` exit 0 |
| A6 | Snapshot and artifacts | PASS | `scripts/verify-release.ps1 -DistDir rc1-draft` exit 0 |
| A7 | Installers | PASS | `scripts/test-install.ps1 -DistDir rc1-draft` exit 0 (includes `TestInstaller`) |
| A8 | Cross-OS build | PASS (harness note) | darwin/arm64 and linux/amd64 both fail on the literal `./...` tree, both isolated to the same untracked `cmd/hopfdrunner` helper (imports `golang.org/x/sys/windows`); scoped to the release/test tree, both build clean |

**Harness defect (not product):** two untracked Go files left by a concurrent
executor's Hop-journey work caused `make lint`/A8 to fail when run unscoped;
neither was part of the product, both were confirmed gone by commit time, and
neither affected any product test result (see part D/E's own cleanup notes,
§15).

## 5. Matrix A — Catalog integrity (10/10 PASS)

Source: part A. `rein`/`reinstate` both confirmed on `PATH` ahead of any other
installed release before every command.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| A1 | Descriptors validate at start; no duplicate or empty keys | PASS | 18 agent keys, 18 unique, none empty |
| A2 | `doctor --agents` lists every agent including T0 | PASS | Human table lists all 18, including all 7 T0 agents with `installed: no, root: no` |
| A3 | Every T0 agent shows an enumerated reason | PASS | `t0_reason` values: `layout_unverified` (aider, antigravity, minimax-code, roo), `server_backed` (amp, openhands), `desktop_only` (zcode) — closed 3-value enumeration |
| A4 | Declared tier and capability constructors agree | PASS | `TestShippedAgentsConformance`: claude/codex/gemini/opencode/grok/kimi all PASS |
| A5 | Every declared evidence path exists in the tagged tree | PASS | `TestShippedEvidenceIsComplete`: PASS |
| A6 | `sessions --agent` accepts exactly T1+ keys, else exit `2` | PASS | T0 key: exit `2`, listing exactly the 11 T1+ keys; T1 key: exit `0` with real data; unknown key: exit `2`, same message |
| A7 | `resume` accepts exactly T3+ keys, else exit `5` with reason | PASS | T0 key: exit `5`, tier-named reason; T1/T2 keys: exit `5`, tier-named reason; a valid-tier but nonexistent session: exit `2` (lookup failure past the tier gate) |
| A8 | Agent ordering deterministic across runs | PASS | Two independent invocations, byte-for-byte identical ordering |
| A9 | Broken evidence path fails conformance | PASS | `TestBrokenEvidencePathFails`/`TestBrokenEvidencePathIsCaught`: both PASS |
| A10 | No scan writes, renames, or locks under any agent root | PASS | Recursive file count/size/`LastWriteTimeUtc` for 4 real T1 roots identical before/after a full refresh (12.8s); `TestIsolationFSRejectsWritesAndOutsideRoot`/`TestIsolationFSOpenFileWriteRejected`: both PASS |

## 6. Matrix B — Probe and redaction (9/9 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| B1 | Output conforms to `AGENT-PROBE-V1` | PASS | Top-level `"schema": "AGENT-PROBE-V1"`, documented shape |
| B2 | No absolute path, home name, or username | PASS | Full-document scan for host username and `C:\Users\...` patterns: 0 hits |
| B3 | No JSON values from session files, keys only | PASS | `first_line_keys` are field names only; every ambiguous-looking substring resolved to a field name or filename, never a session value |
| B4 | Names shape-normalized | PASS | Only normalized tokens (`<slug>`, `<32-hex>`, `<uuid-v4>`, `<n>`) appear |
| B5 | No excluded subtree appears anywhere in output | PASS | Cross-referenced every source's declared `Excluded` patterns against every path in the probe document: no excluded leaf appears as an exact match anywhere |
| B6 | Absent agent reported without error | PASS | All 7 T0 agents reported cleanly inside one successful run, exit 0 |
| B7 | Empty root distinguishable from absent root | PASS | `candidate_roots[]` carries independent `exists`/`marker_present` booleans; both states observed live in one run |
| B8 | Wrapper scripts match binary output | PASS | `agent-storage-probe.sh`/`.ps1` output identical to a direct call, excluding the per-invocation `generated_at` |
| B9 | Secret scanner passes over committed probes | PASS | `internal/secretscan.Scan` over 26 files under `docs/testing/results/agent-probes/`: 0 matches |

## 7. Matrix C — Per T1+ agent (66 rows: 61 PASS, 5 FAIL — corrected by the
2026-09-06 verification-fix pass, §23; the section's own tables always
listed 5 `FAIL` rows — `cline:C2`, `cline:C3`, `cursor:C2`, `cursor:C3`,
`cursor:C6` — matching the top-of-document failing-rows list and §16/§21;
only this header's parenthetical undercounted them as 4. 3 of the 61 `PASS`
rows carry a disclosed caveat — `opencode:C3`, `grok:C6`, `qwen:C6` — and
were always counted inside the 61, not as a separate category)

Per the ground rules, T1 discovery rows ran read-only through `rein` alone
against the host's real root; corruption rows used a copy or synthetic
fixture, never live data.

### T1 agents (source: part A)

#### `cline` (4 PASS, 2 FAIL)

| # | Result | Evidence |
| - | ------ | -------- |
| C1 | PASS | 3 sessions across 2 distinct projects |
| C2 | **FAIL** | `updated_at`/`title` matched the vendor exactly, but `message_count` was `0` for all 3 sessions while the vendor's own `<id>.messages.json` sidecar (opened directly, real data) has 1 real message each. `internal/agents/sources/cline/source.go` never assigns `MessageCount` at all (Go zero value), unlike `pi`/`copilot` |
| C3 | **FAIL** | A title-match search returned the session, but `SearchText` is built from `id, title, project, workspace` only (`source.go:163`) — message-body content, confirmed present, is never indexed |
| C4 | PASS | Bounded fields, capped preview, no transcript body |
| C5 | PASS | `read_only_reason` present; resume exit `5` |
| C6 | PASS | Empty/absent `CLINE_DATA_DIR`: `{"sessions":[]}`, exit 0. Copy of a real session's `db/sessions.db` + sidecars with the `.messages.json` truncated mid-record: still lists cleanly, no panic (consistent with C3 — the sidecar is never opened) |

#### `copilot` (6/6 PASS)

| # | Result | Evidence |
| - | ------ | -------- |
| C1 | PASS | 2 sessions across 2 distinct projects |
| C2 | PASS | `message_count: 5` derived from real `events.jsonl` content, non-zero, non-hardcoded |
| C3 | PASS | `SearchText` includes `parsed.prompts.String()` (`source.go:168`); live search by known token returns the session |
| C4 | PASS | Bounded fields, capped preview |
| C5 | PASS | `read_only_reason` present; resume exit `5` |
| C6 | PASS | Empty/absent `COPILOT_HOME`: both `{"sessions":[]}`, exit 0 — genuine isolation confirmed (`RootEnv` correctly wired, `source.go:103`) |

#### `cursor` (3 PASS, 3 FAIL)

| # | Result | Evidence |
| - | ------ | -------- |
| C1 | PASS | 2 sessions across 2 distinct projects |
| C2 | **FAIL** | `message_count: 0` for both sessions, while each session's own `store.db` (opened directly, real data) has 11 rows in `blobs`. `size_bytes` also reflects only the tiny `meta.json` sidecar, never `store.db` — confirming `store.db` is never opened for metadata |
| C3 | **FAIL** | Same root cause as `cline:C3`: `SearchText` built from `id, title, project, workspace` only (`source.go:173`) |
| C4 | PASS | Bounded fields, capped preview |
| C5 | PASS | `read_only_reason` present; resume exit `5` |
| C6 | **FAIL** | `CURSOR_CONFIG_DIR` pointed at an empty directory, and separately a nonexistent one: **no effect** on `rein sessions --agent cursor --json` — it kept returning the same 2 real sessions from the true host root both times. This candidate's own isolation mechanism does not work for `cursor`, so C6's required "against a copy, never the tester's real data" could not be exercised — see finding **F-CURSOR-ROOTENV** below |

#### `pi` (6/6 PASS)

| # | Result | Evidence |
| - | ------ | -------- |
| C1 | PASS | 3 sessions across 2 distinct projects |
| C2 | PASS | Raw `.jsonl` has exactly 2 `message`-type events; `rein` reports `message_count: 2` exactly; `updated_at` matches the last raw event's timestamp to the second |
| C3 | PASS | `SearchText` includes `parsed.prompts.String()` (`source.go:146`); confirmed by source and a live search hit |
| C4 | PASS | Bounded fields, capped preview, `read_only_reason` present |
| C5 | PASS | `read_only_reason` present; resume exit `5` |
| C6 | PASS | Empty/absent `PI_CODING_AGENT_DIR`: both `{"sessions":[]}`, exit 0 — genuine isolation confirmed (`RootEnv` correctly wired, `source.go:95`) |

### T2+ agents (source: part B)

Real sessions were created in throwaway git repositories with each vendor's
own headless flag and a planted per-agent, per-project token
(`RWB6-<AGENT>-P<N>-TOKEN`); `claude` used the host's live, already-signed-in
`CLAUDE_CONFIG_DIR` per the ground rules, the rest used isolated homes seeded
with a copied real credential file. `gemini` used the committed
`testdata/sessionindex/gemini/windows` fixture (see part B §5/INV-B1 for why
a real, credentialed gemini session could not be resolved by `rein` this
run — not chased further, not scored as a row result).

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| C1 (≥2 sessions, ≥2 projects) | PASS | PASS | PASS | PASS | PASS | PASS (fixture) | PASS |
| C2 (metadata matches vendor) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C3 (search by known string) | PASS | PASS | PASS by title only (documented, pre-existing) | PASS | PASS | PASS | PASS |
| C4 (bounded inspect) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C5 (resume gate correct for tier) | PASS | PASS | PASS | PASS | PASS | PASS (T2 refusal) | PASS |
| C6 (corrupt/empty/absent degrade cleanly) | PASS | PASS | PASS | PASS (no boundary warning emitted, but no crash — `summary.json` is the real count source) | PASS (same note as grok) | PASS | PASS |

`C2:*` evidence: every listing carries `project`/`updated_at`/`message_count`
consistent with the real invocation (e.g. `codex` shows `message_count:3` for
one real turn incl. system/user/assistant records; `claude` shows
`branch:"prod"` matching the throwaway repo). `opencode:C3` reproduces the
pre-existing, disclosed title-only search gap with fresh real evidence
(message-body search: 0 hits; a second project's LLM-generated title happened
to contain the planted token: 1 hit by title).

## 8. Matrix D — Per T2+ agent (35 rows: 30 PASS, 2 PARTIAL, 2 NOT TESTED, 1 FAIL)

Source: part B. `D2`/`D3` (no invented content; unknowns referenced/omitted
with reason) are PASS for all 7 agents — every capsule's `capabilities`/
`fidelity` blocks trace only to real source content or an explicit
`omitted`/`referenced` placeholder.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| D1 (capsule + fidelity report) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D2 (no invented content) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D3 (unknowns referenced/omitted) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D4 (truncation boundary, offset+hash) | **PARTIAL** | **PARTIAL** | **NOT TESTED** | **NOT TESTED** | PASS | PASS | PASS |
| D5 (two runs byte-identical) | PASS | PASS | **FAIL** | PASS | PASS | PASS | PASS |

- **`claude:D4`/`codex:D4` — PARTIAL** (re-recorded from pre-tag, same
  reason): the boundary assertion itself is confirmed (`message_count:2`,
  `incomplete_trailing_record`/`malformed_record` warning on the committed
  `testdata/handoff/*/partial-final-record` fixture), but the byte-exact
  offset+hash cross-check via `handoff --no-launch --json` is not reached —
  the fixture's recorded workspace (a macOS-style path) does not resolve on
  this Windows host, so the handoff route correctly refuses
  (`working directory is a different repository than the source session`,
  a correct refusal per the contract, not a defect).
- **`opencode:D4` — NOT TESTED** (re-recorded, same reason as pre-tag):
  OpenCode's embedded-SQLite store has no JSONL-style truncation boundary to
  exercise.
- **`grok:D4` — NOT TESTED** (new this run; no committed
  `testdata/handoff/grok/partial-final-record` fixture exists; an ad-hoc
  truncation of a copied `chat_history.jsonl` did not move `message_count`
  because `summary.json` is grok's authoritative count source — inconclusive
  for this specific assertion, not chased further this run).
- **`opencode:D5` — FAIL (PD-B1):** two `rein handoff opencode:<id> --to
  claude --dry-run --json` runs over the same unchanged source produced
  **different** `handoff_id` and destination `session_id` values each time.
  `claude`, `codex`, `grok`, `qwen`, and `gemini` all reproduced
  byte-identical capsules under the identical test shape in the same run, so
  the non-determinism is specific to an `opencode` source, not to
  `--to claude` generally.

## 9. Matrix E — Per T3+ agent (30 rows: 26 PASS, 1 PARTIAL, 3 NOT TESTED)

Source: part B. Only `claude`, `codex`, `opencode`, `grok`, `qwen` are T3+;
`gemini`/`kimi` (T2) have no Matrix E rows.

| Row | claude | codex | opencode | grok | qwen |
| --- | ------ | ----- | -------- | ---- | ---- |
| E1 (resume launches vendor CLI, session continues) | PASS | PASS | PASS | PASS | **PARTIAL** |
| E2 (resumed session is the requested one) | PASS | PASS | PASS | PASS | **NOT TESTED** |
| E3 (fork produces a distinct session) | PASS | PASS | PASS | PASS | **NOT TESTED** |
| E4 (below-min/above-max both exit 5, naming the range) | PASS | PASS | PASS | PASS | PASS |
| E5 (active session detected, resume refused/warned) | PASS | PASS | PASS | PASS | **NOT TESTED** |
| E6 (non-interactive exits 7) | PASS | PASS | PASS | PASS | PASS |

`E1`–`E3`/`E5` for `claude`/`codex`/`opencode`/`grok` all used real vendor
processes and planted tokens (e.g. `claude --resume <id> -p "..."` recalled
the exact token planted in that session's first turn; `rein fork` produced a
distinct new session id with the source session unaffected; a real
background vendor process was detected via `agent.active` for all four —
clearing the pre-tag report's `claude:E5`/`codex:E5`/`grok:E5`/`opencode:E5`
`NOT TESTED` dispositions to full `PASS` now that process enumeration works
on this host). `E4` used a `.cmd` shim intercepting only `--version`,
forwarding every other argument to the real vendor binary unmodified.

**`qwen`'s E1/E2/E3/E5 — credential-blocked, not mechanism-blocked:**
`qwen --resume <id> -p "..."` launched the real, correct binary with the
correct `--resume <id>` argv and the correct `session_id` in its own output,
but every turn fails with `[API Error: 401 invalid access token or token
expired]` — this host's `~/.qwen/settings.json`
`BAILIAN_CODING_PLAN_API_KEY` is expired/invalid right now, reproduced on
every real invocation this run (`E1` scored `PARTIAL` on this evidence: the
launch mechanism and correct session id are proven, a completed round trip is
not; `E2`/`E3` `NOT TESTED` since no completed turn exists to verify session
identity or fork content beyond argv/id; `E5` `NOT TESTED` because the 401
failure returns in ~4s, faster than this run's manual `tasklist`-polling
method could reliably catch the process — a harness/timing limitation of this
run's method, not a claim about the underlying mechanism).

## 10. Matrix F — Per T0 agent (14/14 PASS)

Source: part A. All 7 T0 agents (`aider`, `amp`, `antigravity`,
`minimax-code`, `openhands`, `roo`, `zcode`).

| Agent key | F1 listed with reason | F2 no capability offered or implied |
| --------- | --------------------- | ------------------------------------- |
| `aider` | PASS | PASS |
| `amp` | PASS | PASS |
| `antigravity` | PASS | PASS |
| `minimax-code` | PASS | PASS |
| `openhands` | PASS | PASS |
| `roo` | PASS | PASS |
| `zcode` | PASS | PASS |

**F1 evidence (all 7):** each appears in `rein doctor --agents`'s human table
with `installed: no`, `root: no`, and a `t0_reason` from the closed
enumeration.

**F2 evidence (all 7):** for every key, `sessions --agent <key> --json` exit
`2` (key not in the accepted list), `resume <key>:x --dry-run --json` exit
`5` (`"...<DisplayName> is tier T0 (<reason>); native resume is
unsupported"`), and `handoff <src> --to <key> --dry-run --json` exit `2` (not
an accepted handoff destination) — 21 commands total, none implying an
upcoming capability.

## 11. Matrix G — No regression (8/8 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| G1 | Claude Code and Codex CLI native resume and fork unchanged | PASS | Isolated homes seeded only with committed fixtures: `resume`/`fork --dry-run --json` argv matches `docs/cli-reference.md`'s documented table exactly for both agents; `agent.version` matched the live `2.1.263` |
| G2 | Both structured handoff directions complete | PASS | `claude→codex` and `codex→claude --dry-run --json` both exit 0, full capsule+fidelity, `destination_session_mode: "new"` |
| G3 | Gemini remains a source and read-only (Grok since promoted past this gate) | PASS | `rein resume gemini:x --dry-run --json`: exit `5`, tier-named reason. Grok is `T4` in this candidate's catalog; the row's live subject this candidate is Gemini, per the dispatch's own instruction to read G3 against the current census |
| G4 | Push and pull carry only the T5 agents | PASS | `push --agent`'s registry is built dynamically from `agents.Capable(agents.CapabilitySync)`, not a hardcoded list — currently `claude`/`codex`/`opencode`; exercised by `internal/cli/opencode_sync_test.go` and the Hop first-push tests, both passing under A2/A3 |
| G5 | Cross-OS path remapping unchanged | PASS | `internal/pathmap` full unit suite PASS; physical Windows↔macOS proof is H11's mechanism (§15) |
| G6 | Exit-code semantics unchanged; no new codes | PASS | `internal/exitcode` defines exactly 8 codes (`0-7`), matching `docs/cli-reference.md`'s table exactly; no undocumented code observed across ~150 live invocations in part A alone |
| G7 | A prior-stable index upgrades without data loss | PASS | A 100-session index built with `v0.5.1` reopened by `v0.6.0-rc.1`: 100/100, no loss; reopened again with `v0.5.1`: still 100/100 — forward- and backward-compatible |
| G8 | Capsules remain excluded from sync | PASS | A real structured handoff appears in `handoff list --json` but is absent from `sessions --json` (0 matches for its id); `internal/sync/*.go` contains zero capsule references |

## 12. Matrix H — CLI contract and performance (5 PASS, 1 FAIL)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| H1 | CLI reference matches shipped flags | PASS | `internal/doctest` gates PASS; live `claude`/`codex` argv matched documentation exactly |
| H2 | `--json` shapes match documentation | PASS | Live `sessions`/`search`/`inspect`/`doctor --agents` produce documented field sets; `inspect` correctly shows only a capped `prompt_preview` |
| H3 | Full refresh within a reasonable ceiling | PASS | Cold full refresh, 11 agents, real sessions: `13.978s` |
| H4 | No-change refresh materially faster | PASS | Re-run twice: `4.158s`, then `3.740s` — ~70% faster than cold |
| H5 | One slow/unreachable source does not block others | PASS | `CODEX_HOME` pointed at a file (hard-broken source): exit `0`, every other agent's real sessions still returned |
| H6 | Completion lists correct agent keys per command | **FAIL** | `sessions --agent`/`search --agent` correctly list exactly the 11 T1+ keys; `handoff --to` correctly lists exactly the 5 T4+ keys. But `push --agent`/`pull --agent` completion returns `ShellCompDirectiveDefault` with **no candidates at all** — neither command has `RegisterFlagCompletionFunc` wired for `--agent` (finding **F-COMPLETION-PUSHPULL**) |

## 13. T5 encrypted sync round trip (beyond the 178; source: part B)

A disposable `scripts/testing/fakelocker` instance served as the S3-compatible
locker; two isolated `REINSTATE_HOME`s were paired against it; passphrase
delivery used a real inherited-handle helper (`passrunner`) driving
`REINSTATE_PASSPHRASE_FD`, exercising the product's real non-interactive
passphrase path.

| Row | Result | Evidence |
| --- | ------ | -------- |
| `sync:claude` | PASS | Pushed one real Claude session from device A's live config; pulled into a fresh, isolated device B; content restored, never touching the live tree for the write |
| `sync:codex` | PASS | Pushed all 3 real codex sessions; pulled `pulled:3` into device B; ids, `message_count`s, and workspaces all correct |
| `sync:opencode` | PASS | First pull into a never-run destination correctly refused `NOT_INSTALLED` (a correct refusal for the embedded-SQLite store); after running opencode once on that device, the pull succeeded |

## 14. CLI experience (22 rows)

Source: part C, executed through the real Windows pseudo console
(`scripts/testing/conptydriver`) driving `scripts/tuisandbox`'s synthetic
bench. **22/22 PASS.**

| # | Row | Result | Evidence |
| - | --- | ------ | -------- |
| 1 | Bare `rein` on a capable terminal draws the switcher | PASS | Header `rein 9 sessions · 3 agents · all projects`, filter prompt, time-grouped rows, readiness glyphs, split preview, key bar |
| 2 | Bare `rein` on a non-TTY exits `2`, byte-identical stderr to `v0.5.1` | PASS | Both exit `2`; raw stderr SHA-256 identical (`0476fb68...4795b`) via true raw-byte `Start-Process` capture; stdout differs only by legitimately added top-level commands/flags since `v0.5.1`, out of the frozen-output guarantee's scope |
| 3 | `--plain` falls back to the numbered switcher | PASS | Exact frozen numbered-picker text |
| 4 | `REINSTATE_NO_TUI=1` does the same | PASS | Byte-identical to row 3 |
| 5 | `TERM=dumb` does the same | PASS | Identical frozen text — wins over a capable-terminal identity |
| 6 | `TERM` unset draws the switcher on Windows | PASS | Full interactive switcher, same shape as row 1 |
| 7 | A terminal below 40x10 falls back to plain | PASS | `-cols 30 -rows 8`: wrapped frozen numbered-picker text |
| 8 | `NO_COLOR` draws the switcher with no color escape sequences | PASS | Raw capture: exactly one bare `ESC[m` reset, zero SGR color params, switcher still renders fully |
| 9 | Typing filters; header count follows | PASS | `9 sessions · 3 agents` → `1 session · 1 agent` on `"auth"`, matched to ground truth (`rein search auth --json`); visual list-narrow obscured by a known harness VT gap (F1) |
| 10 | `f` in list mode filters, does not fork | PASS | `key f` → filter prompt `❯ f`, no vendor process launched, no fork confirmation |
| 11 | `tab` opens the action menu; `esc` returns without acting | PASS | Key bar changes and reverts correctly, no action taken |
| 12 | `ctrl+k` opens the palette; a subsequence query finds its command | PASS | `"hof"` narrows to exactly `▸ Hand off to another…` |
| 13 | Readiness glyphs resolve; a blocked-without-probe row | PASS | Ground truth matched via `rein resume --dry-run --json`; required a reduced viewport on this host (F2); `B1`/grok no longer demonstrates the no-probe path, dated drift (F3) |
| 14 | Warning checklist acknowledges with spacebar, shows equivalent command | PASS | Real ConPTY `key space` toggles `[ ]`→`[x]`, live-updating equivalent command; confirms fix commit `d3036646` present and working |
| 15 | Partial acknowledgement refused exit `7` | PASS | 2 of 4 required warnings: exit `7`, correct message naming the remaining two |
| 16 | Handoff studio measures each policy, equivalent command follows | PASS | `balanced`→`full`→`checkpoint`, equivalent command tracked every change |
| 17 | Studio refuses `enter` on an unplannable handoff | PASS | Foreign `repository_url`: studio shows the blocked reason, `enter` does not send |
| 18 | `rein init` wizard validates per field, allows going back | PASS | Empty/malformed endpoint refused with correct messages; shift+tab (raw `\x1b[Z`, driver has no named key — F4) preserves the typed value |
| 19 | `rein init` collects no secret material in the full-screen program | PASS | Source inspection: wizard package doc and `secret` field comment confirm; `--help` lists no credential flag; credentials read only after exit |
| 20 | `rein init --link`/`--paste` round trip | PASS | Device A prints a `REIN1-...` code with no keys/passphrase; device B's `--paste` pre-fills the wizard correctly from the decoded endpoint |
| 21 | ASCII glyph fallback on legacy conhost | PASS | With `WT_SESSION`/`TERM_PROGRAM`/`TERM`/locale vars all absent: full ASCII glyph set (`/ > . | ... enter`), matches `internal/ui/theme.go`'s `asciiGlyphs` exactly |
| 22 | Every `--json` document byte-identical to `v0.5.1` | PASS | 21 documents compared; every difference traces to exactly two already-`CHANGELOG.md`-dated classes (Grok tier promotion; new `agent.active` check) — no unmatched difference found |

**Non-blocking findings (5):** F1 (harness — `conptydriver`'s VT model has no
ECH support, matches the pre-tag report's F3), F2 (host — this host's bounded
Git probe reliably times out under same-process concurrent fan-out but not in
isolation; new, deterministic, isolated repro beyond the pre-tag report's F2b),
F3 (dated drift — `B1`/grok no longer demonstrates the no-probe read-only path
on this candidate, because Grok Build's already-shipped tier promotion removed
its read-only status), F4 (harness — `conptydriver` has no `shift+tab` key
name, matches the pre-tag report's F4), F5 (harness, new — `tuisandbox`'s
`sandboxEnv` does not isolate `XDG_DATA_HOME`, OpenCode's declared `RootEnv`;
found and worked around before any row evidence was captured, no real session
content captured or committed). None are release-blocking; full technical
detail is in part C's own text, preserved verbatim in git history.

## 15. Hop parity journeys (16 rows)

Source: part D (`H1`, `H1b`, `H1c`, `H2`, `H3`, `H4`, `H5`, `H9`, `H10`,
`H11`, `H12` — 11 rows) and part E (`H6`, `H6b`, `H7`, `H8`, `H8b` — 5 rows),
against a local, disposable `hopd` + `scripts/testing/fakelocker` lab, never
the hosted control plane (not deployed) and never a developer's real agent
trees except where the ground rules specifically authorize the host's live
Claude Code config (H5, isolated instead by a throwaway project and a
project-mapping filter). **16/16 PASS** — the first time these rows have run
against a tagged artifact rather than a working tree.

| # | Row | Result | Summary |
| - | --- | ------ | ------- |
| H1 | Email sign-in through the approver; token in OS keyring; `whoami` names the device | PASS | Real GET-then-POST via `hoplab approve`; token confirmed present in Windows Credential Manager by a fresh, separate process (`hoplab keyring show`) |
| H1b | Refused sign-in tells the terminal why and stores nothing | PASS | `-refuse` GETs but never POSTs; expired-link refusal surfaced with the documented message and exit; `REINSTATE_HOME` empty afterward |
| H1c | Unreachable control plane: one-line message, documented exit code | PASS | `REINSTATE_HOP_URL` pointed at an unused port; human and `--json` both match `docs/hop.md` verbatim, exit `1`, `details.kind=control_plane_unreachable`; nothing written |
| H2 | `init --hop` provisions the locker exactly once | PASS | `locker_provisioned|1` in `hopd.db`; second `init --hop` refused client-side, exit `7`, no second event |
| H3 | `account init` recovery code shown once; keyring format 5, generation 1, verifies | PASS | Recovery code re-confirmed via a real inheritable-FD pipe; `keyring.v1.json` fetched from the real locker and run through the product's own `keyring.Parse`+`VerifyGenerations` directly: schema 5, generation 1, signature verifies |
| H4 | `push --all` sends one Claude, one Codex, one OpenCode session; `first_push` fires once | PASS (step 4 of automatic post-push verification fails on this lab for a harness reason — F3/fakelocker `AnyBucket`, unchanged from pre-tag) | `first_push|1` exactly once across the session; no-op push reports nothing pushed |
| H5 | Wipe/new-device sign-in/`account recover`/`pull --all`/`resume --dry-run` complete for all three; one real resume through the native agent | **PASS** — clears the pre-tag pass's `PARTIAL` | All three reach `confirmation_required` at exit 0 with `agent.version status=match` at this host's installed ceilings (confirming the range widening this candidate re-tests). Plus: a real Claude Code session created under the host's live config, pushed, deleted locally, pulled back (decrypted), and natively resumed with `claude --resume`, correctly recalling a planted token from restored history |
| H9 | `sync verify` names only observed objects; 404-floor proxy journey reproduces the documented residual | PASS (step 4 fails for the same F3 harness reason as H4) | Steps 1-3 (list/fetch ciphertext/decrypt) PASS; keyring correctly marked unopened; a real `hopd` behind a proxy answering the floor routes 404 reproduces the documented residual exactly (`TestKeyGenerationFloorWithoutTheRouteAgainstRealHopd`) |
| H10 | `sync migrate --to byo`, `--switch`, `--forget-hop` | PASS | Migrated 5 snapshots to a second `fakelocker` bucket; `--switch` flipped local config; `--forget-hop` dropped the token locally with no `device_revoked` event (still enrolled); `sync verify` against the new BYO bucket correctly reports step 4 `NOT APPLICABLE` |
| H11 | Path remap between two homes' project mappings | PASS (macOS leg E-deferred) | Two genuinely separate devices, same account, same project id mapped to two different real local paths; pulled sessions carry the pulling device's own path (confirmed via `cwd` field); `resume --dry-run` then reports `workspace.available=present` |
| H12 | Keyless diagnostics refuse a forged, rolled-back, or re-keyed keyring | PASS | Three real enrolled devices; forged signature, rolled-back generation-1 object, and a different account's genuine re-keyed object each refused by `account status`/`devices` (exit 0, diagnostic) and by `push` (exit 7); the device's own `account.json` anchor unchanged after all three attempts |
| H6 | Device B `rein account join`; device A `rein devices approve`; B pulls | PASS | Live pairing via `hoplab pair init/join` driving the real binary; `pairing_requests` row confirmed `version=2, status=consumed` at the wire; device B pulled all 3 of device A's sessions after adding a project mapping; `sessions --json` on B showed all 6 sessions under correct workspaces, no overlap |
| H6b | An expired pairing request is refused or rolled back; B's wrap is absent from every generation | PASS | Forced the real race with a custom delaying reverse proxy (`HOPD_PAIRING_TTL=20s`, proxy delay `25s`, under the 30s client timeout); the approving device's own `rollBackPairingWrap` success text printed; confirmed via `hopd.db` (`status=expired, payload IS NULL`) and `account status` (`enrolled_devices=1`, the joining device "no root-key wrap yet") |
| H7 | `rein daemon install/status/stop/start/uninstall` round trip through real Task Scheduler; foreground loop | PASS | Elevated via `Start-Process -Verb RunAs`, UAC accepted; full round trip through real `schtasks`, confirmed via `schtasks /Query` showing the task `Running` mid-run and gone after uninstall; 3 real fsnotify changes 1s apart coalesced into exactly 1 debounced push; 4 scheduled pulls fired repeatedly. Non-blocking finding F2 recorded: the Task-Scheduler-launched daemon resolved per-agent home roots from ambient environment rather than isolated overrides (Windows Task Scheduler XML has no per-task environment block); no push occurred while this was true (verified against `hopd.db`'s events table) |
| H8 | A revokes B: generation rolls, B's token refused, B cannot open a later push; lagging-device attack and 404-floor proxy variant | PASS | `internal/cli/keygeneration_crossplane_test.go -tags hopacceptance` against a real `hopd` binary: both `TestKeyGenerationFloorAgainstRealHopd` and `TestKeyGenerationFloorWithoutTheRouteAgainstRealHopd` passed, 404-floor residual reproduced. Live: device A revoked device B, generation rolled 1→2, device B's subsequent push and `whoami` both refused with exit `4` |
| H8b | A Console-initiated revocation stays pending until the recovery-code command writes a strictly newer generation | PASS | A custom program drove the real Console HTTP routes (sign-in, CSRF computed per `internal/server/console.go`) to submit a revocation request. Confirmed pending on one device and confirmed the other device still fully functional (push/whoami succeed) while pending. `rein devices revoke` then printed the Console request confirmed after the keyring reached generation 2; `hopd.db`'s `device_revocation_requests` row shows `status=confirmed, confirmed_generation=2`. The revoked device's next push refused with exit `4` |

**Non-blocking harness/informational findings across parts D+E:**
project-mapping-scoped push discovery is intentional (a first-attempt trap,
not a defect — `TestClaudeDiscoverSkipsUnmappedProjects`); the widened
Claude/OpenCode ranges are confirmed live on the tagged binary; `fakelocker`'s
`AnyBucket:true` does not emulate per-bucket credential scoping (unchanged
lab limitation, contrasted directly by H10's correct `NOT APPLICABLE` on a
genuine BYO destination, and by H8's real-`hopd` crossplane test exercising
the identical product code correctly); a Task-Scheduler-launched daemon
resolves per-agent home roots from ambient environment, not isolated
overrides (inherent to the Windows Task Scheduler API, irrelevant to an
ordinary end-user install). None are release-blocking; full command
transcripts are preserved in git history in the original part files' commits.

## 16. Findings (merged, release-blocking assessment)

| ID | Severity | Row(s) | Description | Release blocking |
| -- | -------- | ------ | ------------ | ----------------- |
| F-CURSOR-ROOTENV | MAJOR | `cursor:C6` | `internal/agents/sources/cursor/source.go`'s `config()` (lines 108-120) never sets `hometree.Config.RootEnv`, unlike every sibling T1 source (`cline`/`copilot`/`pi` all do). `CURSOR_CONFIG_DIR` isolates only the `doctor --agents` probe path, never `sessions`/`search`/`inspect`/`resume`/`fork` for cursor, which share `defaultLocalSources()`/`NewIndexSource(agents.Env{})`. Not caught by the existing conformance suite. No exploit path or data-corruption path found; the risk is to *testers* (the isolation mechanism this whole acceptance program relies on to test Cursor CLI without touching a developer's real data does not work), not end users. | Required-row `FAIL` (`cursor:C6`); reporting executor's own mechanical judgment was NO for release-blocking beyond the row itself, flagged for coordinator review — this assembly treats it as a release blocker because it is a required-row `FAIL` |
| product | MINOR | `cline:C2`, `cursor:C2` | `message_count` unconditionally `0` for both agents, never derived from the vendor's own message-bearing file, confirmed against real content on this host. `pi`/`copilot` correctly compute a non-zero count from equivalent content | Required-row `FAIL` |
| product | MINOR | `cline:C3`, `cursor:C3` | Search for both agents indexes `id, title, project, workspace` only, never message/prompt body — confirmed by source, mirrors the pre-existing `opencode:C3` gap but was undocumented for `cline`/`cursor` specifically | Required-row `FAIL` |
| F-COMPLETION-PUSHPULL | MINOR | `MatrixH:H6` | `push --agent`/`pull --agent` have zero shell-completion candidates wired (no `RegisterFlagCompletionFunc`), while `sessions --agent`/`search --agent`/`handoff --to` all correctly complete | Required-row `FAIL` |
| PD-B1 | product, needs review | `opencode:D5` | `rein handoff <opencode session> --to claude --dry-run --json` produced a different `handoff_id`/destination `session_id` across two runs over an unchanged source; the same command shape against 5 other agent sources was byte-identical across repeats in the same run — specific to an `opencode` source | Required-row `FAIL` |

A tier claimed in the catalog that this device could not verify: none. No
tier reduction is proposed — every agent's declared tier was exercised at
least partially, and `cursor`'s isolation defect is a testing-methodology
defect, not evidence the product misclassifies cursor's own capability tier.

## 17. Tier reductions proposed by this device

| Agent | Claimed tier | Verified tier | Reason |
| ----- | ------------ | -------------- | ------ |
| — | — | — | None proposed. Every declared tier (T0-T5, no T3 this candidate) was exercised as designed; `cursor`'s `F-CURSOR-ROOTENV` defect affects test isolation, not the product's own tier claim (cursor's T1 read-only capability itself was verified live via `C1`/`C4`/`C5`) |

## 18. Pre-tag dispositions carried: cleared or re-recorded

The pre-tag report's own "Dispositions carried" list
(`docs/testing/results/2026-09-06-windows-v060rc1-pretag.md`) recorded 15
rows not `PASS` after its own final pass. Every one is either cleared to
`PASS` on the tagged binary or re-recorded here with the same reason
category, per this run's dispatch instruction.

| Row | Pre-tag disposition | This run | Status |
| --- | -------------------- | -------- | ------ |
| `opencode:C3` | FAIL, definitional | PASS by title only (documented) | **CLEARED** |
| `claude:E2` | PARTIAL, host | PASS | **CLEARED** |
| `claude:E3` | PARTIAL, host | PASS | **CLEARED** |
| `claude:D4` | PARTIAL, host | PARTIAL | **RE-RECORDED**, same reason (fixture workspace does not resolve on Windows; correct refusal) |
| `qwen:E3` | PARTIAL, host (expired credential) | NOT TESTED | **RE-RECORDED**, same reason (credential still expired; no completed turn to verify against) |
| `claude:E5` | NOT TESTED, host (no WMI) | PASS | **CLEARED** (process enumeration now works on this host) |
| `codex:E5` | NOT TESTED, host | PASS | **CLEARED** |
| `grok:E5` | NOT TESTED, host | PASS | **CLEARED** |
| `qwen:E5` | NOT TESTED, host (no WMI) | NOT TESTED | **RE-RECORDED**, reason changed: WMI now works, but qwen's 401 failure returns too fast (~4s) for this run's manual polling method to catch the process — still a host/harness-timing category |
| `opencode:E5` | NOT TESTED, host | PASS | **CLEARED** |
| `opencode:D4` | NOT TESTED, definitional | NOT TESTED | **RE-RECORDED**, same reason (SQLite store has no JSONL boundary) |
| `qwen:D4` | NOT TESTED, host | PASS | **CLEARED** |
| `qwen:E2` | NOT TESTED, host | NOT TESTED | **RE-RECORDED**, same reason (same expired credential) |
| `gemini:D4` | NOT TESTED, time | PASS | **CLEARED** |
| `kimi:D4` | NOT TESTED, time | PASS | **CLEARED** |

**10 cleared, 5 re-recorded with the same reason category, 0 regressed** (no
row that was `PASS` pre-tag is anything but `PASS` here). Two required rows
this run — `MatrixH:H6`, `cline:C2`, `cline:C3`, `cursor:C2`, `cursor:C3`,
`cursor:C6`, and `opencode:D5`'s `PD-B1` — are **new findings this run**, not
part of the pre-tag disposition list: the pre-tag pass's T1-agent (`cline`,
`cursor`) rows used a shallower assertion ("field present") that did not
catch the zero-message-count/search-body gaps this run's deeper source-review
methodology found, its `cursor:C6` used a synthetic fixture directly rather
than exercising `CURSOR_CONFIG_DIR` (so it never hit `F-CURSOR-ROOTENV`), its
`H6` row never exercised `push --agent`/`pull --agent` completion
specifically (only `sessions`/`search`/`handoff`), and `opencode:D5`'s
non-determinism is a fresh discovery from this run's two-runs-byte-identical
methodology. `grok:D4`'s `NOT TESTED` was not in the pre-tag list either (grok
was not yet promoted to a Matrix-D-eligible tier at the relevant pre-tag
pass) and is new this run for the reason stated in §8.

## 19. Harness defects (merged, non-blocking)

- Two untracked, uncommitted Go files (`cmd/h3probe/main.go`,
  `cmd/hopfdrunner/main.go`), self-documented as throwaway acceptance-test
  helpers, were present on the shared worktree during part A and part D's
  runs (left by the concurrent Hop-journey executor's own in-flight work).
  They caused `make lint`/A8 to fail only when run unscoped against literal
  `./...`; both gates pass clean scoped to the actual release/test tree. Both
  files were deleted before any results commit; `git status` at commit time
  shows no diff outside results files.
- Several `python3 -c` invocations against files addressed by an absolute
  Git-Bash `/d/...` path intermittently raised `FileNotFoundError` even
  though the same path read fine with `cat`/`wc`/`head` in the same shell;
  worked around every time by `cd`-ing into the directory and using a
  relative filename (part A).
- `scripts/testing/fakelocker`'s `s3test.Fake{AnyBucket: true}` does not
  emulate per-bucket credential scoping, so `rein sync verify` step 4 and
  `push`'s own post-push verification fail against every Hop-mode locker in
  this lab — contrasted directly by a genuine BYO destination correctly
  reporting `NOT APPLICABLE` (H10), and by H8's real-`hopd` crossplane test
  exercising the identical product code with no such gap (parts A, D, E all
  independently reproduced this; `scripts/testing/fakelocker` is W4-owned).
- `scripts/testing/conptydriver`'s VT model has no ECH (`CSI Ps X`) support,
  so a capture can show stale list-row text bleeding through where the real
  product correctly erased it (rows 9, 12); reproduces the pre-tag report's
  own F3 finding.
- On this host, in this session, the bounded Git probe (`DefaultProbeTimeout`
  2s) reliably times out under same-process concurrent fan-out (9 simultaneous
  `git` subprocess launches from the switcher's initial probe) but completes
  in ~30ms in isolation — a deterministic, isolated repro beyond the pre-tag
  report's own F2b (row 13; part C).
- `scripts/tuisandbox`'s bench's `B1`/grok fixture and the `v0.5.2` contract's
  row 13 text both assume grok is unconditionally read-only; this candidate's
  already-shipped Grok tier promotion removed that status, so row 13's
  no-probe path can no longer be demonstrated against this bench on this or
  any later candidate carrying the same promotion — a bench-fixture
  maintenance item, not a row defect (part C).
- `scripts/testing/conptydriver` has no `shift+tab` key name even though the
  wizard's own key bar advertises it; worked around with the raw sequence
  `\x1b[Z` (row 18; reproduces the pre-tag report's own F4).
- `scripts/tuisandbox`'s `sandboxEnv` does not isolate `XDG_DATA_HOME`
  (OpenCode's declared `RootEnv`); this host's shell carries a live,
  non-empty value pointing at a real unrelated project. Found and worked
  around before any row evidence was captured; no real session content was
  captured or committed (part C).
- A daemon launched by a real Task Scheduler task resolves per-agent home
  roots from the scheduled task's own ambient process environment rather than
  the session's isolated overrides, because Windows Task Scheduler XML
  carries no per-task environment block; `REINSTATE_HOME` itself still
  correctly travels via `--home`. No push of real content occurred while this
  was true, confirmed against `hopd.db`'s events table before the report was
  written (part E, H7).
- Configuring `[[projects]] local_root` to an arbitrary lab directory instead
  of a session's own recorded workspace path makes Claude/Codex push
  `Discover()` silently report zero sessions — intentional, tested behavior
  (`TestClaudeDiscoverSkipsUnmappedProjects`), not a defect, but a first-
  attempt trap worth recording for future Hop-lab runs (part D, F1).

## 20. Deferred: Apple Silicon macOS

Copied verbatim from
[`../v0.6.0-rc.1-agent-verification-prompts.md`](../v0.6.0-rc.1-agent-verification-prompts.md)'s
own "Deferred: Apple Silicon macOS" section, under
[ADR 0005](../../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md) D2.
Nothing in this device's evidence claims macOS. Every row below is run
against this same `v0.6.0-rc.1` tag (or the stable tag it becomes) once the
Apple Silicon macOS host returns from repair
([issue #403](https://github.com/HarjjotSinghh/reinstate/issues/403)). If
that later macOS run fails, the fix ships as `v0.6.1` — this record of this
candidate's Windows acceptance is never edited to hide it.

| Group | Rows | Contract | Result |
| ----- | ---- | -------- | ------ |
| Phase 5 generated matrix, macOS column | all rows section B lists | Phase 5 | `DEFERRED` |
| CLI experience, macOS column | rows 1-20 and 22 | `v0.5.2` | `DEFERRED` |
| Hop cross-device journeys | pairing macOS↔Windows; path remap Windows↔macOS both directions; daemon launchd round trip; first push on macOS | hosted #16 | `DEFERRED` |
| Range widening on macOS | Claude Code through `2.1.263`; OpenCode through `1.18.27` | ADR 0005 D3 | `DEFERRED` |
| Grok GD8 | tool-approval prompt (already collected on macOS; the Windows half is #368 and stays optional) | Grok T4 | `DEFERRED` |

No document, data file, or release note may state that `v0.6.0` was verified
on macOS until that run exists.

## 21. Release-blocking findings

Because 15 of the 216 required rows are not `PASS` (7 `FAIL`, 3 `PARTIAL`,
5 `NOT TESTED`), this report's device verdict is `FAIL` and stable `v0.6.0`
is **not authorized** by this run. The 7 `FAIL` rows are release-blocking by
this run's own gate ("A row is PASS only if its mechanism was exercised;
PARTIAL and NOT TESTED do not pass a required row"), independent of whether
any individual reporting executor judged the underlying product defect itself
severe enough to block mechanically:

1. `MatrixH:H6` — `push --agent`/`pull --agent` shell completion (MINOR)
2. `cline:C2` — message count always zero (MINOR)
3. `cline:C3` — search excludes message body (MINOR)
4. `cursor:C2` — message count always zero (MINOR)
5. `cursor:C3` — search excludes message body (MINOR)
6. `cursor:C6` — `CURSOR_CONFIG_DIR` isolation inert for session discovery (MAJOR, `F-CURSOR-ROOTENV`)
7. `opencode:D5` — non-deterministic handoff dry-run ids (`PD-B1`)

The 8 non-`FAIL` non-passing rows (3 `PARTIAL`, 5 `NOT TESTED`) are all host-
or fixture-limited, not product defects: expired/unavailable qwen credential
(`qwen:E1/E2/E3/E5`), a fixture's non-Windows recorded workspace correctly
refusing the handoff route (`claude:D4`/`codex:D4`), and structural gaps with
no boundary to exercise or no fixture yet committed (`opencode:D4`,
`grok:D4`).

## 22. Terminated device block

> Device testing is terminated for this candidate at the milestone recorded
> above. The results in this report are final for this device and this tag.
> Any further testing requires a new candidate tag and a new report.

- Terminating tester: assembler (tagged-run coordinator role, this session)
- UTC timestamp: 2026-09-06T12:30:00Z
- Device verdict: **FAIL** — 15 of 216 required rows are not `PASS` (7
  `FAIL`, 3 `PARTIAL`, 5 `NOT TESTED`); see the Verdict block and §16/§21
  above for the complete list and severity assessment. This does not
  authorize stable `v0.6.0`; a fix pass and a new tagged candidate are
  required before the next acceptance run.

## 23. Verification-fix pass (2026-09-06, after a `reject` verdict)

This report was submitted for independent verification and **rejected**.
This section is appended, append-only, per the correction instructions: the
21 sections above are unchanged from the assembled report; this section
records what the rejection asked for, what was rechecked against the same
tagged artifact in a fresh lab directory
(`D:\ReinstateAcceptanceProjects\v060-w7b-fix\`, deleted after use, never
reused from any prior executor's or verifier's own lab), and what changed.

### 23.1 What the verifier found

- One **major** finding: `Hop:H4` did not reproduce in the verifier's own
  independent rerun — `push --all` sent only the Claude and OpenCode
  sessions, not Codex; a direct `push --agent codex --session <id>`
  independently returned `no matching local sessions found` even though
  `rein sessions --agent codex --json` correctly listed that session.
- One **minor** finding: §7 (Matrix C)'s own header stated `4 FAIL` where
  its own tables list 5 (`cline:C2`, `cline:C3`, `cursor:C2`, `cursor:C3`,
  `cursor:C6`) — corrected above in §7's heading.
- No counts or privacy blockers (the verifier's `counts_check` and
  `privacy_check` both passed; the one completeness note there, about an
  already-committed evidence file's dev-environment path, is not this
  report and needed no action).

### 23.2 `Hop:H4` recheck — reproduces `PASS`; the verifier's asymmetry not
reproduced, and not explained by the mechanism

**Setup.** A brand-new, disposable `hopd` + `fakelocker` lab was started on
this host (`127.0.0.1:8721`/`127.0.0.1:9721`, never sharing a port or a
`hopd.db` with any prior lab — `hoplab ps` confirmed `no labs recorded`
immediately before `start`). `hoplab.exe` was built fresh from this same
worktree at `63ac5a5b`. `hoplab homes -devices device-a` seeded one Claude,
one Codex, and one OpenCode fixture session, all sharing the fixture's own
recorded project path (`C:\Users\fixture-user\code\device-a` — confirmed
identical `cwd` in both the Claude and Codex raw fixture JSONL, and
identical after `hoplab`'s own `rewriteFixture` renaming). Login/approval
used a fresh `hoplab approve` instance. The extracted, checksum-verified
`v0.6.0-rc.1` release `rein.exe` (§1's own hash, re-verified again into
this fresh lab's install copy) was used as `-rein` for every `hoplab pair`
and manual `rein` invocation, matching the original H4 executor's own
method (part D §1: `hopd built fresh... installed binary` used as the
`-rein` under test).

**First attempt reproduced a known trap, on purpose.** `rein init --hop`
was run with **no `--project` mapping at all** first, deliberately
mirroring what an independent rerun that skips `hoplab pair init` (which
always supplies one — see below) might do. `rein account init` could not
be completed non-interactively this way (no FD wired), so no push was
attempted from that state; the attempt was abandoned and the device's
config/keyring/session-home were reset before the real run below. This is
noted only because it is the same class of unmapped-push setup the
original H4 executor's own §3.6 "method note" (F1) already disclosed and
worked around — not a new finding.

**Corrected run: `hoplab pair init`**, the tool's own built-in device-setup
path (`scripts/testing/hoplab/pair.go`'s `pairInit`), which always runs
`rein init --hop --project hoplab-device-a=<the fixture's own project
path>` before `rein account init` — exactly the corrected mapping the
original H4 executor's method note describes reaching by hand. Then:

```text
$ rein sessions --json
  (claude:session-syn-001-a, codex:rollout-syn-001-a, opencode:ses_fixture001a — all 3 listed)

$ rein push --all --json
{
  "conflicts": null, "dry_run": false, "skipped": 0,
  "snapshots": [
    "07045b07-658a-41af-9699-d4e2f6caaf22",
    "372a7688-c0b8-48d3-a732-3eaebb405b8f",
    "ab90db42-278c-43b7-bc8a-dcbb5c31be80"
  ],
  "verification": { "outcome": "fail", "posted": true }
}
```

The verification report's own step 3 decrypts the pushed index and shows,
in the product's own words: `index revision ..., 3 session(s) (claude 1,
codex 1, opencode 1)` — `codex:rollout-syn-001-a ->
snapshots/372a7688-...-age`, alongside the Claude and OpenCode entries. Step
4 fails for the same disclosed `fakelocker AnyBucket` harness reason as
`H4`/`H9` throughout this report (F3) — unchanged, not a new finding.

Follow-up checks, both matching the original H4 claim exactly:

```text
$ rein push --agent codex --session rollout-syn-001-a --json
{ "conflicts": null, "dry_run": false, "skipped": 1, "snapshots": null }
   # already pushed -- correctly a no-op, NOT "no matching local sessions found"

$ rein push --all --json           # second, no-op push
{ "conflicts": null, "dry_run": false, "skipped": 3, "snapshots": null }

$ sqlite3 hopd.db "select type, count(*) from events group by type"
first_push|1   # exactly once, unchanged by every later command
```

**Conclusion: `Hop:H4`'s claim reproduces.** `push --all`, run the way the
lab tooling and the original executor's own documented method both
intend, genuinely pushes one Claude, one Codex, and one OpenCode session,
and a direct per-session codex push independently confirms it landed. The
row's disposition is unchanged: **PASS** (step 4 fails for the same F3
harness reason, as before).

**On the verifier's specific asymmetry (Codex excluded, Claude not).**
This recheck cannot reproduce that exact asymmetry, and source review does
not support a codex-specific gap in the mapping mechanism itself:
`internal/adapter/claude/claude.go` and `internal/adapter/codex/codex.go`
implement the same `Discover`-time filter — `if len(projectIDsByRoot) > 0
&& !mapped { return nil }` (identical shape, both files) — so a missing or
non-matching project mapping should exclude an unmapped Claude session and
an unmapped Codex session alike, not one and not the other; the original
executor's own F1 method note describes exactly that symmetric failure
(both Claude and Codex silently discovered zero sessions together, only
OpenCode — which is not project-scoped — coming through). The most likely
explanation is that the verifier's own independent rerun did not go
through `hoplab pair init` (which always supplies the matching
`--project`) and instead drove `rein init --hop`/`account init` by hand
without reproducing that exact mapping — the verifier's own report does
not show its `init --hop` command line, so this cannot be confirmed
directly, and is reported as the most likely explanation rather than a
proven one. What is directly re-verified, live, in this round is the row's
own core mechanism: correctly configured, `push --all` sends all three
agents' sessions, including Codex, and the control plane's own decrypted
index confirms it.

### 23.3 What changed in this file

- §7's header: `4 FAIL` corrected to `5 FAIL` (with the caveated-PASS count
  clarified); no row result, no count anywhere else in the document, and no
  section total changes — every other tally in the document (§0, §16, §18,
  §21, the top-of-document Verdict block and failing-rows table) already
  stated 5 `FAIL` rows for Matrix C and was already internally consistent.
- `Hop:H4` is rechecked (§23.2) and its disposition is **unchanged: PASS**.
  No row result in this document changes as a result of this pass.
- Required counts are **unchanged**: `201 PASS / 3 PARTIAL / 7 FAIL / 5 NOT
  TESTED` of `216`. Device verdict is **unchanged: FAIL** — release-blocking
  for reasons unrelated to `H4` (§16/§21's 7 required-row `FAIL`s stand).
- No privacy correction was needed (§23.1); a fresh grep of this file for
  `C:\Users`, `/c/Users`, the author's email domain, and recovery-code-
  shaped strings found nothing beyond the one already-public GitHub issue
  URL naming the repo's own author, unchanged from before this pass.
- Lab cleanup: `hoplab.exe stop -root D:\ReinstateAcceptanceProjects\v060-
  w7b-fix\lab` was run; `hoplab.exe ps` confirmed no lab left running; the
  device-a OS-keyring token was cleared before deleting the isolated home;
  the entire `D:\ReinstateAcceptanceProjects\v060-w7b-fix\` directory was
  deleted after this section was written. Nothing from it was committed.
