# `v0.6.0-rc.4` tagged-artifact native Windows acceptance

`PHASE5-DEVICE-REPORT-V1`

This is the assembled device report for the **published, signed GitHub
prerelease `v0.6.0-rc.4`**, commit `561ec133e7fd040d7937d555a75f0bd7dd7b878e`
(release workflow run `34092326851`). It satisfies
[`../phase-5-universal-agent-coverage-acceptance.md`](../phase-5-universal-agent-coverage-acceptance.md)
as specialized by [`../v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md)
and [`../v0.6.0-rc.4-agent-verification-prompts.md`](../v0.6.0-rc.4-agent-verification-prompts.md),
under [ADR 0005](../../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md)'s
Windows-first waiver. It merges five executors' part files
(`2026-09-07-windows-v060rc4-part-{a,b,c,d,e}.md`, all against this same tag)
into the one cumulative report the dispatch requires, plus an assembler's own
independent install and matrix-generation check (§0). The five part files are
removed from the tree in the same commit as this file; their content is
preserved here.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Matrix generated by:** `rein doctor --agents --acceptance-matrix --json`
  (re-run independently by the assembler against a fresh install; see §0)
- **Row accounting:** `216` rows total (`178` Phase 5 generated matrix +
  `22` CLI experience + `16` Hop parity journeys) — `1` row is
  `N/A (definitional)` (`opencode:D4`) — **`215` required rows**.
- **Host-credential rows:** `0`. `opencode` is a required agent (Claude
  Code, Codex CLI, OpenCode — see "Required installs" in
  [`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md)), so its
  version-drift block (`opencode:E5`/`E6`) cannot qualify for
  `NOT TESTED (host credential)` regardless of cause — that disposition
  applies only to the eight *optional* agents. Zero rows this run qualify
  for that disposition.
- **Required counts:** `208 PASS / 1 PARTIAL / 4 FAIL / 2 NOT TESTED` of
  `215` required rows.
- **Release-blocking findings:** `7` required rows are not `PASS` and are
  not excused by either disposition in
  [`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict):
  `MatrixB:B4`, `MatrixB:B7`, `MatrixG:G4`, CLI row `13`, `opencode:E5`,
  `opencode:E6`, `MatrixH:H7`. See §16 and §20.

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required
result is `FAIL`. Because 7 of the 215 required rows are not `PASS` and are
not covered by an applicable disposition, the device verdict is `FAIL` and
this report does not authorize stable `v0.6.0`. No product code was changed
by this assembly; every result below is transcribed from the five part
files' own evidence.

**This candidate's own two fixes are both confirmed.** The Pi
`message.content[].text` reader defect `v0.6.0-rc.3` found
(`F-PI-CONTENT-EXTRACTION`) is **cleared**: `pi:C3` now `PASS` on a fresh
planted-token session, search finds it, and `prompt_preview` is populated.
The Qwen Code range widening to `0.21.12`–`0.23.0` is **confirmed**: every
`qwen` row `C1`–`E6` now `PASS` against the real installed `0.23.0`. See
§18.

**This run also surfaces four required-row findings `v0.6.0-rc.3` did not
have**, three of them genuinely new product/documentation findings
(`MatrixB:B4`, `MatrixB:B7`, `MatrixG:G4`) and one operational (`MatrixH:H7`
`PARTIAL`, no administrator available this run), plus a new host/harness
event (`F-OPENCODE-VERSION-DRIFT`) that blocks two required `opencode` rows
the same way `F-QWEN-VERSION-DRIFT` blocked `qwen` at `v0.6.0-rc.3`. See §16
and §18.

### Failing rows (7, none pass a required row)

| Row | Result | Category | Reason (full evidence in the matrix sections below) |
| --- | ------ | -------- | ---- |
| `MatrixB:B4` | FAIL | product, new this run | The `gemini` agent's probed `tree` includes two top-level directory segments under `tmp/` that are real, un-normalized project-name strings copied verbatim from this host; every path segment *beneath* each is correctly shape-normalized, but the top segment itself is not. `v0.6.0-rc.3`'s B4 evidence exercised OpenCode's Git object store instead and found no defect there — this is the first run to probe `gemini`'s `tmp/` tree for this assertion. |
| `MatrixB:B7` | FAIL | product, new this run | An overridden agent root (`CLINE_DATA_DIR`) that exists but is empty produces `doctor --agents --json` output byte-identical (apart from the timestamp) to one pointed at a nonexistent path — not distinguishable, as the contract requires. `v0.6.0-rc.3`'s B7 evidence exercised Kimi's marker file instead and found no defect there — this is the first run to probe an empty-vs-absent override on `cline`. |
| `MatrixG:G4` | FAIL | stale contract text, not a new regression | `rein __complete push --agent ""` / `pull --agent ""` return exactly `claude, codex, opencode` — matching OpenCode's already-published T5 promotion and the exact evidence `v0.6.0-rc.3`'s own G4 row recorded as `PASS` ("carry Claude, Codex, and OpenCode"). This run's executor scored the row against the contract's literal, still-unedited text ("Claude and Codex sessions, and no other agent's"), which G3's own carried note already flags as stale for OpenCode. Recorded `FAIL` per the executor's own evidence, not overridden by the assembler; the underlying behavior is unchanged from `v0.6.0-rc.3`. |
| CLI `13` | FAIL | product, new this run | The interactive switcher's default `ScopeAll` ("all projects") scope shows every visible row as `CANNOT RESUME`/`Blocked`, unconditionally, reproduced 9/9 across 7 separate launches — including sessions independently confirmed `ready` by three other channels under the identical harness (`resume --dry-run --json`, the plain picker's `i N` inspect, and the switcher itself scoped to one project). All 22 CLI rows passed 22/22 at `v0.6.0-rc.2` and `v0.6.0-rc.3`; this is not a carried disposition. |
| `opencode:E5` | NOT TESTED | host/harness, new this run, different reason than the row it replaces | The host's real OpenCode self-updated `1.18.27`→`1.18.29` mid-run, above this candidate's verified ceiling (`1.18.21`–`1.18.27`); `resume --dry-run --json` correctly refuses `exit 5` (`agent.version`) before reaching the `agent.active` check this row needs (`F-OPENCODE-VERSION-DRIFT`). The rc.4 dispatch's carried-dispositions table expected this row `PASS` via the ConPTY retry method, or the *same* `F-E5-NO-PTY` reason if not — a version-drift block is a new reason, not the expected disposition. |
| `opencode:E6` | NOT TESTED | same reason | Same version-gate preemption blocks the non-interactive-safety check. `opencode:E6` was `PASS` at `v0.6.0-rc.3` — this is a regression in disposition (PASS → NOT TESTED), caused by host/harness drift, not by this candidate's own change. |
| `MatrixH:H7` | PARTIAL | operator/harness availability, new reason this run | This run's shell held no administrator rights, and the rc.4 dispatch's mandatory lab-isolation method (fresh Windows account, issue #424) itself requires elevation to set up before the row's own UAC prompt can even be reached. The executor polled `D:/ReinstateAcceptanceProjects/h7-go.txt` for a maintainer go-ahead from `07:13:25Z`; it never appeared inside this run's practical window. No unattended-elevation workaround was attempted, per the dispatch's own harness-traps warning. `H7` was `PASS` at `v0.6.0-rc.3` (mechanism); this run could not even start the round trip, so no live-agent-home write of any kind occurred this time either. |

None of the seven is attributable to a change this candidate made. Two are
genuinely new product/documentation findings surfaced by testing a different
agent than prior runs did against the same generic assertions (`B4`, `B7`);
one is a stale-wording mismatch already implicitly acknowledged by the
contract's own G3 note and already recorded `PASS` at `v0.6.0-rc.3` under
identical shipped behavior (`G4`); one is a genuinely new interactive-CLI
defect (row 13); two are a host vendor-CLI auto-update past this candidate's
verified ceiling, the same failure shape `qwen` hit at `v0.6.0-rc.3`
(`opencode:E5`/`E6`); and one is an operator/elevation availability gap this
run's harness could not route around without violating the dispatch's own
safety rule (`H7`).

## 0. Assembler verification

Performed independently of, and in addition to, all five part files, in the
assembler's own fresh directory
(`D:\ReinstateAcceptanceProjects\v060-rc4-z\`), never touching any part
executor's own lab directories or the shared worktree's tracked files before
this commit.

1. **Checksum.** `sha256sum` of the coordinator's staged
   `reinstate_0.6.0-rc.4_windows_amd64.zip` matched `checksums.txt`
   (`f5ae24e2edf0364f0e6f9b0df3c5b2c21f47222ecd394b9c40fc5dbd1ae14d87`)
   independently.
2. **Install.** Unzipped into a fresh directory
   (`D:\ReinstateAcceptanceProjects\v060-rc4-z\install\`), never a shared or
   developer binary. `rein.exe`/`reinstate.exe` `sha256sum`: both
   `fbea94615dabcbc30fc1ebb6e93259ddd330b62573555e551fd3112b31b9d3ef`; `fc.exe
   /b` "no differences encountered" (byte-identical).
3. **Version.** `rein version --json`:
   `{"commit":"561ec133e7fd040d7937d555a75f0bd7dd7b878e","date":"2026-09-07T06:48:08Z","name":"reinstate","version":"0.6.0-rc.4"}`
   — matches every part file's own artifact-identity table exactly.
4. **Required row count.** `rein doctor --agents --acceptance-matrix --json`
   against this fresh install: top-level `"row_count": 178`,
   `"core_row_count": 33` (`A:10, B:9, G:8, H:6`), 18 agents at tiers
   `T0×7, T1×4, T2×2, T4×2, T5×3` — matches the candidate's census in the
   dispatch exactly (no tier promotion this candidate, unchanged from
   `v0.6.0-rc.3`).
5. **Row-set reconciliation.** Programmatically read the full set of 178
   expected row identifiers from that JSON (33 core rows + each agent's own
   row list) and compared it against the union of every row id assigned to
   part A (core Matrix A/B/G/H, Matrix F for all 7 T0 agents, Matrix C for
   the 4 T1 agents — 71 Phase-5 rows) and part B (Matrix C/D/E for the 7 T2+
   agents — 107 Phase-5 rows): **the two sets are set-equal, 178 elements
   each, zero overlap, zero gap.** Every required Phase 5 row appears in
   exactly one part file. Part A's own Section-A automated-gates table (8
   rows) is a separate, non-Phase-5 gate set and is not counted in the 178,
   matching the dispatch's own accounting (`178 + 22 + 16 = 216`).
6. **Part self-report cross-check.** Recounted every part's own row table
   cell-by-cell against its self-reported structured counts:
   - Part A: 79 assigned rows (8 gates + 33 core + 14 T0 + 24 T1); row table
     sums to `76 PASS, 3 FAIL` (`MatrixB:B4`, `MatrixB:B7`, `MatrixG:G4`) —
     matches its own `counts` block exactly.
   - Part B: 108 assigned rows (107 Phase-5 + the T5 push/pull round trip);
     row table sums to `105 PASS, 0 FAIL, 0 PARTIAL, 3 NOT TESTED`
     (`opencode:D4` N/A-definitional, `opencode:E5`/`E6` version-drift) —
     matches its own `counts` block exactly.
   - Part C: 22 rows, `21 PASS, 1 FAIL` (row 13) — matches.
   - Part D: 11 rows, `11 PASS` — matches.
   - Part E: 5 rows, `4 PASS, 1 PARTIAL` (`H7`) — matches.

   **No correction was needed to any part's counts this run.**
7. **Total reconciliation.** `71` (part A Phase 5) + `107` (part B Phase 5,
   including `opencode:D4`) + `22` (part C) + `11` (part D) + `5` (part E) =
   **`216`**, matching the dispatch's total row count exactly; minus the one
   `N/A (definitional)` row = **`215`** required. PASS sum: `68` (part A,
   core33 + T0-14 + T1-24 minus the 3 FAILs) + `104` (part B, 107 minus
   `opencode:D4` N/A minus `opencode:E5`/`E6`) + `21` (part C) + `11` (part
   D) + `4` (part E) = **`208`**. FAIL: `3` (part A) + `1` (part C) =
   **`4`**. NOT TESTED (blocking): `2` (`opencode:E5`/`E6`, part B). PARTIAL:
   `1` (`H7`, part E). `208 + 4 + 2 + 1 = 215`.
8. **Disposition-rule check.** Applied
   [`v0.6.0-windows-acceptance.md`'s "Dispositions that do not block the
   device verdict"](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict)
   rule mechanically to every non-`PASS` required row this run recorded.
   - `opencode:D4` — `N/A (definitional)`, unchanged reason (OpenCode's
     SQLite-only store has no JSONL record boundary). Excluded from both the
     denominator and the numerator, as required. Not a disposition on a
     required row.
   - `opencode:E5`/`E6` — `NOT TESTED (host credential)` requires "the
     host's vendor credential for that agent is expired or absent," **and**
     requires the agent to be *optional* under the "Required environment"
     section. Neither holds here: `opencode` is one of the three required
     agents (Claude Code, Codex CLI, OpenCode), and the block is
     `agent.version` (a compatibility ceiling on a self-updated install),
     not a credential failure. This disposition cannot apply to a required
     agent regardless of cause. **Both rows block.**
   - `MatrixH:H7` is `PARTIAL`, not `NOT TESTED` — `PARTIAL` is not one of
     the two named dispositions at all, and the contract's "Everything
     else" clause applies: `PARTIAL` does not pass a required row.
     **Blocks.**
   - `MatrixB:B4`, `MatrixB:B7`, `MatrixG:G4`, CLI row `13` are all `FAIL` —
     `FAIL` is not a named disposition either. **All four block.**
   - **Zero rows this run qualify for either named disposition.** This
     differs from `v0.6.0-rc.2` (one qualifying row) and matches
     `v0.6.0-rc.3` (zero qualifying rows) — a consequence of this run's
     specific findings' own root causes, not a change in how strictly the
     rule is read.
9. **Cleanup.** The assembler's own install and matrix output live under
   `D:\ReinstateAcceptanceProjects\v060-rc4-z\` and are deleted after this
   report is committed; nothing from that directory is referenced by path
   elsewhere in this report.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-09-07T06:48:00Z`–`2026-09-07T13:42:00Z` (span across all five parts; each part's own timestamp is in its section below); assembler verification `2026-09-07T13:42:00Z`–`2026-09-07T14:05:00Z` |
| Device | `windows-amd64` |
| OS/version/build | Windows 11 Pro, `10.0.26200`, native `windows/amd64`, never WSL |
| CPU architecture/native process | `amd64`, native process (no emulation) |
| Filesystem | NTFS |
| Tested tag | `v0.6.0-rc.4` |
| Tested full commit | `561ec133e7fd040d7937d555a75f0bd7dd7b878e` |
| Installed binary SHA-256 | `fbea94615dabcbc30fc1ebb6e93259ddd330b62573555e551fd3112b31b9d3ef` |
| Installed version JSON | `{"commit":"561ec133e7fd040d7937d555a75f0bd7dd7b878e","date":"2026-09-07T06:48:08Z","name":"reinstate","version":"0.6.0-rc.4"}` |
| Catalog agent count | `18` |
| Catalog tier census | `T0: 7, T1: 4, T2: 2, T3: 0, T4: 2, T5: 3` (unchanged from `v0.6.0-rc.3`) |
| Generated required row count | `178` (`rein doctor --agents --acceptance-matrix --json`, independently reproduced by the assembler in §0 and by part A) |
| Git version | `2.52.0.windows.1` |
| Go version/toolchain | `go1.25.13` (pinned via `GOTOOLCHAIN`; host default `go1.26.1`) |
| Report branch | `v060/rc4-tagged` |
| Device-report commit | filled in by the commit that lands this file |
| Draft report PR | NOT CREATED (worktree report, per this run's dispatch — no PR required) |

### Bootstrap deviation (applies to every part below except executor A)

Per this run's ground rules, only executor A installs from the live
`https://reinstate.dev/install.ps1` bootstrap and records that as the
artifact identity; every other executor (B, C, D, E) and the assembler
installed from the coordinator's pre-verified, checksummed draft directory
(`…/scratchpad/rc4-draft/`) instead — the assigned methodology, not an
unplanned deviation. Executor A's own bootstrap check (Header, part A): the
live bootstrap was saved fresh with `Invoke-WebRequest -OutFile`, its pinned
version line reads `$Version = "v0.6.0-rc.4"`, and installing from it (with
`INSTALL_DIR` pointed at a fresh directory, never the default install
location) produced binaries byte-identical to (and reporting the same
version/commit/date as) the checksummed archive — **no deviation** between
the live bootstrap route and the release-commit candidate. (Note: this host
also carries a pre-existing, unrelated stale dev install at the default
install location, `%LOCALAPPDATA%\Programs\Reinstate\bin`; the bootstrap's
own canonical installer correctly refused to silently replace it, confirming
the "never replace a user binary" rule holds even for the bootstrap path.)

### Artifact identity (common to all parts)

| Field | Value |
| ----- | ----- |
| Tag | `v0.6.0-rc.4`, published GitHub prerelease, signed tag verified against `.github/allowed_signers` (coordinator, pre-run) |
| Full commit | `561ec133e7fd040d7937d555a75f0bd7dd7b878e` |
| Release workflow run | `34092326851` |
| Windows archive | `reinstate_0.6.0-rc.4_windows_amd64.zip` |
| Archive SHA-256 | `f5ae24e2edf0364f0e6f9b0df3c5b2c21f47222ecd394b9c40fc5dbd1ae14d87` (matches `checksums.txt`; re-verified independently by every part and the assembler) |
| Installed binary SHA-256 | `fbea94615dabcbc30fc1ebb6e93259ddd330b62573555e551fd3112b31b9d3ef` — `rein.exe`/`reinstate.exe` byte-identical (`fc.exe /b`/`cmp` exit `0`) in every install |
| `rein version --json` | `{"commit":"561ec133e7fd040d7937d555a75f0bd7dd7b878e","date":"2026-09-07T06:48:08Z","name":"reinstate","version":"0.6.0-rc.4"}` |
| Previous-release comparison binary (rows 2, 22, G7) | `reinstate_0.5.1_windows_amd64.zip`, SHA-256 `b724ca3da4e124004063032d63240c244fb9a59279076f0e87441d327a826e8c` |

## 2. Agent inventory on this device

| Agent key | Installed | Vendor version | Sessions available | Distinct projects | Declared tier | Tier testable here |
| --------- | --------- | -------------- | ------------------- | ----------------- | -------------- | -------------------- |
| `claude` | YES | `2.1.263` | 55 real | 26 | T5 | yes |
| `codex` | YES | `0.149.0` | 100 real (limit) | ≥15 | T5 | yes |
| `opencode` | YES | `1.18.27` at start, self-updated to `1.18.29` mid-run | 2 real + fixtures | 2 | T5 | yes, `E5`/`E6` **NOT TESTED** this run — version drift past the verified ceiling (see §11, §16) |
| `grok` | YES | `1.0.5` | 6 real | 4 | T4 | yes |
| `qwen` | YES | `0.23.0` (within the widened `0.21.12`–`0.23.0` ceiling) | 9 real | 7 | T4 | yes — full range confirmed; every row `PASS` |
| `gemini` | YES (live default home) | `0.53.0` | 25 real | 15 | T2 | yes, `B4` **FAIL** this run — probe redaction gap on `tmp/` project names (see §6, §16) |
| `kimi` | YES | `0.36.1` | 3 real | 3 | T2 | yes |
| `pi` | YES | `0.73.1` | 7 real before this run, 8 after | 6+ | T1 | yes, `C3` **PASS**, confirms `F-PI-CONTENT-EXTRACTION` fixed (see §10, §18) |
| `cursor` | YES (`cursor-agent` itself still broken on this host) | (host) | 2 real | 2 | T1 | yes, `C2`/`C3` remain `PASS` (unchanged since `v0.6.0-rc.3`'s fix) |
| `copilot` | YES | `1.0.80` | 2 real | 2 | T1 | yes |
| `cline` | YES | (host) | 7 real | 5 | T1 | yes, `B7`'s override-distinguishability finding was reproduced on this agent's `CLINE_DATA_DIR` (see §6, §16) |
| `aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode` | ABSENT | — | — | — | T0 | yes (refusal/no-capability rows only) |

## 3. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------- |
| Annotated tag signature verifies against `.github/allowed_signers` | PASS | Verified by the coordinator pre-run (dispatch preamble) |
| Tag peels to the recorded full commit on `origin/main` | PASS | `561ec133e7fd040d7937d555a75f0bd7dd7b878e`, the worktree's own merge base and `git log` ancestor |
| Published release is non-draft, prerelease, and tied to the tag | PASS | Coordinator pre-run verification |
| Exact expected release asset set is present | PASS | `verify-release.ps1` (A6) |
| Checksums, GitHub API digests, and attestations match the tag and commit | PASS | Coordinator pre-verified (`checksums.txt` match, `gh attestation verify` on the Windows archive); independently re-verified by every part and the assembler |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | PASS | `verify-release.ps1`/`test-install.ps1` (A6/A7) |
| Live bootstrap is byte-identical to the tested commit and pins only this candidate | PASS | Executor A: pinned version line `v0.6.0-rc.4`, resulting binary byte-identical to the checksummed archive — **no deviation** this candidate |
| Bootstrap installer digest matches the tagged canonical installer | PASS | Executor A |
| Fresh isolated install did not replace a user binary or persist PATH changes | PASS | Every part and the assembler installed into its own fresh directory; no user binary replaced (the live bootstrap correctly refused to touch this host's pre-existing, unrelated stale dev install without `REINSTATE_CONFIRM_REPLACE`) |
| `rein` and `reinstate` have identical verified bytes and report the candidate | PASS | `fc.exe /b`/`cmp` exit `0` in every install; SHA-256 `fbea94615dabcbc30fc1ebb6e93259ddd330b62573555e551fd3112b31b9d3ef` throughout |
| Installed binary reports the literal full tested commit | PASS | `rein version --json` matches `561ec133e7fd040d7937d555a75f0bd7dd7b878e` in every install |

## 4. Automated gates (Section A of the dispatch; not part of the 178)

Source: part A, run once from the worktree per the ground rules.

| # | Gate | Result | Evidence |
| - | ---- | ------ | -------- |
| A1 | Format, vet, tidy | PASS | `gofmt -l .`, `go vet ./...`, `go mod tidy -diff` all empty, exit 0 |
| A2 | Unit suite | PASS | `CGO_ENABLED=0 go test ./... -count=1 -p 4`: every package `ok`, 0 `FAIL` |
| A3 | Race suite | PASS | `CGO_ENABLED=1 go test -race ./internal/... -count=1 -p 4`: every package `ok`, 0 `FAIL`, 0 `DATA RACE` |
| A4 | Lint and vuln | PASS | `make lint` → golangci-lint v2.11.4, `0 issues`; `make vuln` → govulncheck v1.6.0, `0 vulnerabilities` in code (4 in required-but-uncalled modules, informational) |
| A5 | Doc gate | PASS | `go test ./internal/doctest/... -count=1` → `ok`; `scripts/check-docs.ps1` exit 0 |
| A6 | Snapshot and artifacts | PASS | `scripts/verify-release.ps1 -DistDir rc4-draft` exit 0; `scripts/check-release-binary-identity.ps1` exit 0, `version=0.6.0-rc.4 commit=561ec133e7fd040d7937d555a75f0bd7dd7b878e` |
| A7 | Installers | PASS | `scripts/test-install.ps1 -DistDir rc4-draft` exit 0, `TestInstaller` `ok` |
| A8 | Cross-OS build | PASS | `GOOS=darwin GOARCH=arm64` and `GOOS=linux GOARCH=amd64 go build ./...` both exit 0 |

**8/8 PASS.**

## 5. Matrix A — Catalog integrity (10/10 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| A1 | Descriptors validate at start; no duplicate or empty keys | PASS | Implicit in every successful `rein` invocation, plus `TestShippedAgentsConformance` passing in A2 |
| A2 | `doctor --agents` lists every agent including T0 | PASS | 18 agent keys, all 7 T0 present with reasons |
| A3 | Every T0 agent shows an enumerated reason | PASS | `layout_unverified` (aider, antigravity, minimax-code, roo), `server_backed` (amp, openhands), `desktop_only` (zcode) |
| A4 | Declared tier and capability constructors agree | PASS | `TestShippedAgentsConformance` passed in A2 |
| A5 | Every declared evidence path exists in the tagged tree | PASS | `TestShippedEvidenceIsComplete` passed in A2 |
| A6 | `sessions --agent` accepts exactly T1+ keys, else exit `2` | PASS | Accepted set exactly `claude, cline, codex, copilot, cursor, gemini, grok, kimi, opencode, pi, qwen` (11 keys); `aider` and a bogus key both refused, exit `2` |
| A7 | `resume` accepts exactly T3+ keys, else exit `5` with reason | PASS | `resume pi:x`/`gemini:x`/`aider:x` (T0–T2) all refused `native session action is unsupported: … tier T<N>[, reason]`, exit `5`; `resume claude:x` took the session-lookup path (exit `2`), confirming T5 keys accepted syntactically |
| A8 | Agent ordering is deterministic across runs | PASS | Three consecutive `doctor --agents` runs, byte-identical (same SHA-256) |
| A9 | Broken evidence path fails conformance | PASS | `TestBrokenEvidencePathIsCaught`/`TestBrokenEvidencePathFails` passed in A2 |
| A10 | No scan writes, renames, or locks under any agent root | PASS | SHA-256 digest (relative path + size + mtime) of 8 real agent roots, identical before/after a full `sessions --agent all --json` refresh |

## 6. Matrix B — Probe and redaction (7/9 PASS, 2 FAIL)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| B1 | Output conforms to `AGENT-PROBE-V1` | PASS | `"schema": "AGENT-PROBE-V1"` |
| B2 | No absolute path, home name, or username | PASS | Full-document scan: 0 hits; every path field is `relative_to: home/env` + a relative `suffix` |
| B3 | No JSON values from session files, keys only | PASS | `first_line_keys` entries are field-name arrays only, never values |
| B4 | Names shape-normalized | **FAIL** | The `gemini` agent's probed `tmp/` subtree includes **two top-level directory segments that are real, human-chosen project-name strings copied verbatim from this host**, not shape-normalized — every path segment beneath each is correctly normalized (`*`, `<uuid-v4>`, `<n>`), only the top segment is not. Not quoted here per the no-private-names rule; reproducible via `agents[key=="gemini"].tree` under `tmp/`. **New finding** — `v0.6.0-rc.3`'s B4 evidence exercised a real OpenCode Git object store instead and found no defect there; this is the first run to probe `gemini`'s `tmp/` tree for this assertion |
| B5 | No excluded subtree present | PASS | Full-text scan for credential/token/secret/`.ssh`/password/keyring substrings: only benign usage-metric field names (`cachedTokens`, etc.) |
| B6 | Absent agent reported without error | PASS | 7 T0 agents (none installed): `resolved_root: null`, exit 0, no error |
| B7 | Empty root distinguishable from absent root | **FAIL** | Pointed `CLINE_DATA_DIR` at (a) a real, empty directory and (b) a nonexistent path: both `doctor --agents --json` outputs are **byte-identical apart from `generated_at`** — `resolved_root: null`, empty `tree`, `root=no` in both. The override correctly stops silent fallback to the real home root (a different, non-null `resolved_root` when unset), but "exists and empty" and "does not exist" are not distinguishable in either output. **New finding** — `v0.6.0-rc.3`'s B7 evidence exercised Kimi's marker file instead (`.kimi-code` vs `.kimi`, `marker_present:true/false`) and found no defect there; this is the first run to probe an empty-vs-absent override specifically on `cline` |
| B8 | Wrapper scripts match binary output | PASS | `agent-storage-probe.sh`/`.ps1` diffed against a direct binary invocation (ignoring only `generated_at`): identical |
| B9 | Secret scanner passes over committed probes | PASS | `gitleaks v8.30.1 dir --config .gitleaks.toml docs/testing/results/agent-probes/`: "no leaks found", exit 0 |

**Neither `B4` nor `B7` is a regression from `v0.6.0-rc.3`'s own passing
evidence** — both prior runs exercised the same generic assertion against a
*different* agent's root-override or tree-naming mechanism and found no
defect there. This run's assignment happened to probe `gemini`'s `tmp/` tree
and `cline`'s directory-override behavior specifically, surfacing two
real, previously-unobserved gaps in the underlying mechanism itself. Both
are genuine product findings, neither release-blocking on its own merit but
both blocking this run's required-row count per the contract's own rule.

## 7. Matrix G — No regression (7/8 PASS, 1 FAIL)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| G1 | Claude/Codex native resume and fork unchanged | PASS | `resume claude:x`/`resume codex:x` (bogus ids) both take the session-lookup path (exit `2`), confirming native resume/fork remain wired for both |
| G2 | Both structured handoff directions complete | PASS | Real (non-`--dry-run`) `--no-launch --json` both directions (`claude→codex`, `codex→claude`), each from its own real workspace: exit `0`, capsule stored, `projection.md` written |
| G3 | Gemini/OpenCode/Grok remain sources; Gemini stays read-only | PASS, with a note | `resume gemini:x`: `unsupported…T2`, exit `5`; `handoff --from gemini --last --to claude --dry-run` engaged the real Gemini source machinery, refused only on a workspace-mismatch check (a correct refusal). Note: `grok` is now T4 and no longer reads as read-only-only — matches its already-published tier, not a new regression, but the literal clause is stale, in the same way the contract already flags for OpenCode |
| G4 | Push and pull carry Claude and Codex only | **FAIL (stale contract text, not a new regression)** | `rein __complete push --agent ""`/`pull --agent ""` both return exactly `claude, codex, opencode`, via `internal/cli/commands_impl.go`'s `defaultRegistry()` reading `agents.Capable(agents.CapabilitySync)` — OpenCode's catalog descriptor declares a sync adapter. This is the **identical shipped behavior** `v0.6.0-rc.3`'s own G4 row recorded `PASS` against, with the note "carry Claude, Codex, and OpenCode"; this run's part A scored it against the contract's literal, still-unedited row text instead, which G3's own carried note already flags as stale for OpenCode's T5 promotion. A real `push --agent opencode --dry-run` could not be driven to completion without a Hop login (`config missing`, exit `3`), so this is a completion/registry-level finding only, not a confirmed data-plane regression |
| G5 | Cross-OS path remapping unchanged | PASS | `internal/pathmap` package tests passed in A2; unchanged this candidate |
| G6 | Exit-code semantics unchanged; no new codes | PASS | Every exit code observed this run (`0, 2, 3, 5`) matches `docs/cli-reference.md`'s documented table, asserted against the shipped binary by the A5 doctest gate |
| G7 | An index built by an older release upgrades without data loss | PASS | A real index built with the checksum-verified `v0.5.1` Windows binary (100+ real sessions), reopened by `v0.6.0-rc.4`: at the default `--limit 100` a naive diff showed an apparent "1 lost/1 new," traced to the default-100 listing window shifting by one live session created on the host mid-test; re-run uncapped (`--limit 5000`): **530/530 `v0.5.1` sessions present after the rc.4 open, 0 lost, +1 new** (a genuinely new live session). No data loss from the upgrade itself |
| G8 | Capsules remain excluded from sync | PASS | `TestRefuseHandoffsPush` passed in A2 |

**`G4`'s `FAIL` reflects a mismatch between the contract's still-literal
wording and the shipped registry that has existed, unchanged, since at
least `v0.6.0-rc.3` — not a change this candidate introduced.** The
assembler records the row exactly as the executor scored it against the
literal contract text, per "never invent a result"; the underlying product
behavior is identical to the behavior `v0.6.0-rc.3`'s own G4 row recorded
`PASS`. See §16 for the reconciliation recommendation.

## 8. Matrix H — CLI contract and performance (6/6 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| H1 | CLI reference matches shipped flags | PASS | Covered by the A5 doctest gate |
| H2 | `--json` shapes match documentation | PASS | `sessions`/`search`/`inspect`/`doctor --agents` all well-formed, exit 0 |
| H3 | Full refresh within documented ceiling | PASS | Cold full refresh, 11 agents, 530+ real sessions: `14.2s`, within the documented ~20–30s range |
| H4 | No-change refresh materially faster | PASS | `0.27s` warm vs `14.2s` cold — ~52× faster |
| H5 | One slow or unreachable source does not block others | PASS | `QWEN_HOME` pointed at an unreachable UNC path: exit `0`, `15.6s` (comparable to baseline), 522 sessions across the 10 other agents, `qwen` correctly absent |
| H6 | Completion lists correct agent keys per command | PASS | `push`/`pull --agent ""` → `claude, codex, opencode` (see G4 above); `sessions --agent ""` → the wider correct T1+ list, no T0 key present |

## 9. Matrix F — Per T0 agent (14/14 PASS)

Source: part A. All 7 T0 agents (`aider`, `amp`, `antigravity`,
`minimax-code`, `openhands`, `roo`, `zcode`), not installed on this host —
a scope reduction, not a `FAIL`.

| Agent key | F1 listed with reason | F2 no capability offered or implied |
| --------- | --------------------- | ------------------------------------- |
| `aider` | PASS — `t0_reason=layout_unverified` | PASS |
| `amp` | PASS — `t0_reason=server_backed` | PASS |
| `antigravity` | PASS — `t0_reason=layout_unverified` | PASS |
| `minimax-code` | PASS — `t0_reason=layout_unverified` | PASS |
| `openhands` | PASS — `t0_reason=server_backed` | PASS |
| `roo` | PASS — `t0_reason=layout_unverified` | PASS |
| `zcode` | PASS — `t0_reason=desktop_only` | PASS |

## 10. Matrix C — Per T1+ agent (66 rows: 65 PASS, 1 PASS-confirms-a-prior-fix)

Per the ground rules, T1 discovery rows ran read-only through `rein` alone
against the host's real root; corruption rows used a copy, never live data.

### T1 agents (source: part A)

`C1`/`C2`/`C4`/`C5`/`C6` ran `rein` alone, read-only, against the host's
real vendor roots. `C6` used a **copy** of each real tree, discarded after
use. `cline:C3`/`pi:C3` created fresh throwaway sessions through the
vendor's own live CLI with a planted, non-sensitive search token.
`cursor:C2`/`C3`/`copilot:C3` ran against the host's real store under the
T1-discovery exception (counts, field names, a self-chosen search word
only).

| # | cline | copilot | cursor | pi |
| - | ----- | ------- | ------ | -- |
| C1 (≥2 sessions, ≥2 projects) | PASS — 7 sessions / 5 projects | PASS — 2 sessions / 2 projects | PASS — 2 sessions / 2 projects | PASS — 7 pre-existing sessions / 6 projects |
| C2 (metadata matches vendor) | PASS — `project`/`title`/`updated_at`/`message_count` all populated | PASS — `project`/`branch`/`title`/`updated_at`/`message_count` | PASS — real `message_count` (3, 3), the real `blobs`/`meta` schema | PASS — `project`/`title`/`updated_at`/`message_count` |
| C3 (message-body search) | PASS — live credential answered "PONG"; a fresh planted-token session was created and found | PASS — a word from a real session's own bounded `prompt_preview`, exactly 1 match | PASS — a word from a real session's `prompt_preview`, exactly 1 match | **PASS — confirms the `F-PI-CONTENT-EXTRACTION` fix.** A fresh `pi -p` session with a planted token, confirmed real `version:3` shape (`content:[{type:"text",text:"…"}]`, not a legacy top-level `"text"` key) on disk; `rein search` found it; `rein inspect` shows a **populated** (59-char), token-carrying `prompt_preview` — the opposite of `v0.6.0-rc.3`'s result on the same code path |
| C4 (bounded inspect, no body) | PASS | PASS | PASS | PASS |
| C5 (resume gate correct for tier) | PASS — exit 5 | PASS — exit 5 | PASS — exit 5 | PASS — exit 5 |
| C6 (corrupt/empty/absent degrade cleanly) | PASS — truncated `sessions.db` copy: unaffected sessions still list, exit 0; empty/absent → clean, `root=no` | PASS — truncated `session.db` ×2 copies: no crash, exit 0; empty/absent clean | PASS — `chats/` copy only, `store.db` ×2 truncated, bounded 30s job: both sessions still returned, correct `message_count`; empty/absent clean | PASS — truncated `.jsonl` copy (torn last line): all 8 sessions (incl. the new one) still enumerated, no panic; empty/absent clean |

**24 rows: 24 PASS**, including the dispatch's specific re-test `pi:C3` —
**confirmed fixed**. `cursor:C2`/`C3` remain `PASS`, unchanged since
`v0.6.0-rc.3`'s own fix.

### T2+ agents (source: part B; 7 agents × varying rows, all PASS except opencode's two version-drift rows in §12)

Real sessions were created in throwaway git repositories with each vendor's
own headless flag and a planted per-agent, per-project token
(`RC4B-<AGENT>-<epoch>-<suffix>`) in the **message body**. `claude`/`codex`
used their live/isolated homes; `grok`/`qwen` used an isolated home seeded
only with the needed credential file (`qwen`'s confirmed
`agent.version.status=match` before every row); `gemini`/`kimi` used the
live default home (no portable credential file found for either).

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| C1 (≥2 sessions, ≥2 projects) | PASS — 55/26 | PASS — 100(limit)/≥15 | PASS — real sessions/2 | PASS — 6/4 | PASS — 9/7 | PASS — 25/15 | PASS — 3/3 |
| C2 (metadata matches vendor) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C3 (search finds planted token, message body) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C4 (bounded inspect, no transcript body) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C5 (resume gate correct for tier) | PASS | PASS | PASS | PASS | PASS — `agent.version.status=match` | PASS (T2 refusal) | PASS (T2 refusal) |
| C6 (corrupt/empty/absent degrade cleanly) | PASS — truncated fixture copy, `incomplete_trailing_record` warning; empty/absent clean | PASS — same shape | PASS — imported SQLite copy truncated 50%, exit 0; empty/absent clean | PASS — own single-session copy truncated 70%, no crash; empty/absent clean | PASS — own single-session copy truncated 70%, no crash; empty/absent clean | PASS — own single-session copy truncated 70%, no crash; empty/absent clean | PASS — own single-session copy truncated 70%, `message_count` degraded to 0, no crash; empty/absent clean |

Every `qwen` row above ran only after `rein inspect qwen:<id> --json`
confirmed `agent.version.status=match` against the real installed `0.23.0`
in an isolated `QWEN_HOME` seeded only with `settings.json` — **the widened
`0.21.12`–`0.23.0` ceiling is confirmed correct on native Windows
evidence**, the exact re-test this candidate's dispatch required.

## 11. Matrix D — Per T2+ agent (35 rows: 34 PASS, 1 N/A definitional)

Source: part B. D1/D2/D3/D5 ran through `rein handoff <key>:<id> --to <dest>
--dry-run --json` from the session's own workspace. D4 used `--no-launch`
against a copy truncated to ~70% of its byte length, with the reported
`raw_source.byte_offset`/`artifact_sha256` independently recomputed from the
raw bytes for every agent.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| D1 (capsule + fidelity report) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D2 (no invented content) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D3 (unknowns referenced/omitted with reason) | PASS — 23 unknown | PASS — 10 unknown | PASS — 1 unknown | PASS — none this session | PASS — none this session | PASS — none this session | PASS — none this session |
| D4 (truncation boundary, offset + hash, independently recomputed) | PASS — offset 84904, hash matched | PASS — offset 71470, hash matched | N/A (definitional) | PASS — targets `updates.jsonl` (identified by matching `size_bytes`), offset 1845, hash matched exactly | PASS — offset 4706, hash matched | PASS — offset 2964, hash matched | PASS — offset 78860, hash matched |
| D5 (two runs byte-identical) | PASS | PASS | PASS (the `opencode:D5` fix from `v0.6.0-rc.1` holds) | PASS | PASS | PASS | PASS |

**`opencode:D4` — N/A (definitional), same reason as every prior candidate.**
OpenCode's SQLite-only store has no JSONL record boundary this row's
mechanism applies to. Excluded from the required row count.

**`qwen`'s D-rows are all `PASS` this run**, confirming the widened ceiling:
D4's independently recomputed hash matched exactly, the same fidelity this
run achieved for every other agent — a cleaner, fully-reconciled result than
`v0.6.0-rc.3`'s `grok:D4` multi-file gap (also resolved this run — `grok`'s
`raw_source` now correctly identifies `updates.jsonl` as the actual boundary
file, and the independently recomputed hash matches exactly).

## 12. Matrix E — Per T3 agent (30 rows: 27 PASS, 2 NOT TESTED, 1 PASS on a re-tested harness gap)

Source: part B. Only `claude`, `codex`, `opencode`, `grok`, `qwen` are T3+;
`gemini`/`kimi` (T2) have no Matrix E rows.

| Row | claude | codex | opencode | grok | qwen |
| --- | ------ | ----- | -------- | ---- | ---- |
| E1 (resume launches vendor CLI, session continues) | PASS — token recalled | PASS — file-level continuation proven | PASS — token recalled | PASS — via ConPTY (headless `-p --resume` hangs, see §21) | PASS — token recalled |
| E2 (resumed session is the requested one) | PASS | PASS — file-level + in-agent evidence, disclosed recall inconsistency (see §21) | PASS | PASS — via ConPTY | PASS |
| E3 (fork produces a distinct session) | PASS — distinct session id | PASS — distinct session id | PASS — distinct session id, title suffixed | PASS — distinct session id via ConPTY | PASS — distinct session id |
| E4 (below-min/above-max both exit 5, naming the range) | PASS — `2.1.219–2.1.263` | PASS — `0.133.0–0.149.0` | PASS — `1.18.21–1.18.27` | PASS — `1.0.5–1.0.5` | PASS — `0.21.12–0.23.0` (widened ceiling correctly enforced both directions) |
| E5 (active session detected, resume refused/warned) | PASS — via ConPTY, `agent.active status=present, actual=true` | PASS — via ConPTY (the dispatch's required retry method; a background `exec resume` process misdetected as `unknown` first, corrected by ConPTY) | **NOT TESTED** — `F-OPENCODE-VERSION-DRIFT` (see below) | PASS — genuinely active process, confirmed via `tasklist` | PASS — via ConPTY |
| E6 (non-interactive exits 7) | PASS — exit 7 | PASS — exit 7 | **NOT TESTED** — same reason | PASS — exit 7 | PASS — exit 7 |

**`codex:E5`, `opencode:E3`/`E1`/`E2`, and `grok:E5` are all `PASS` this
run, per the rc.4 dispatch's specific re-test method
(`scripts/testing/conptydriver`) — clearing `v0.6.0-rc.3`'s carried
`F-E5-NO-PTY` harness gap for `codex:E5` and `grok:E5`.** See §18.

**`opencode:E5`/`E6` — `F-OPENCODE-VERSION-DRIFT`, a new host/harness
finding this run, unrelated to the ConPTY retry method or to this
candidate's own change.**

```
$ opencode --version
1.18.29
```

The host's real OpenCode self-updated `1.18.27` → `1.18.29` partway through
this run (discovered while launching the `E5` ConPTY probe), above this
candidate's verified ceiling (`1.18.21`–`1.18.27`). `rein resume
opencode:<id> --dry-run --json` correctly refuses `exit 5` (`agent.version`
block) before reaching the `agent.active` check `E5` needs — the identical
mechanism `qwen`'s pre-widening rows hit at `v0.6.0-rc.3`. Every other
`opencode` row in §10/§11 above ran, and was independently confirmed
matching, while the real install was still `1.18.27`; only `E5`/`E6` were
reached after the drift. `opencode:E6` was `PASS` at `v0.6.0-rc.3` — this is
a disposition regression (PASS → NOT TESTED) caused entirely by host/harness
drift, not by anything this candidate changed. Neither row qualifies for
`NOT TESTED (host credential)`: `opencode` is a required agent under this
contract's "Required environment" section, and that disposition applies
only to the eight optional agents regardless of the underlying cause. **Both
rows block the device verdict.** See §0.8, §16, §20.

E4 evidence (shims below/above the range, all real vendor session ids):

| Agent | Verified range | Below-min shim | Above-max shim |
| ----- | ---------------- | ---------------- | ---------------- |
| `claude` | 2.1.219–2.1.263 | `2.1.218` → exit 5, range named | `2.1.264` → exit 5, range named |
| `codex` | 0.133.0–0.149.0 | `0.132.0` → exit 5, range named | `0.149.1` → exit 5, range named |
| `opencode` | 1.18.21–1.18.27 | `1.18.20` → exit 5, range named | `1.18.28` → exit 5, range named |
| `grok` | 1.0.5–1.0.5 | `1.0.4` → exit 5, range named | `1.0.6` → exit 5, range named |
| `qwen` | 0.21.12–0.23.0 | `0.21.11` → exit 5, range named | `0.23.1` → exit 5, range named |

All ten shims exit `5`, `code:"compatibility"`, naming the exact configured
range from each agent's live catalog constant — independent proof that
`qwen`'s widened ceiling and every other agent's range are enforced by the
same mechanism `opencode`'s live drift is caught by.

### Dispositions that do not block the device verdict

| Row | Disposition | Condition 1 (PASS for every required agent: claude/codex/opencode) | Condition 2 (PASS for another optional agent, same tier) | Blocks the verdict? |
| --- | ----------- | -------------------------------------------------------------------- | ------------------------------------------------------------ | -------------------- |
| `opencode:D4` | N/A (definitional) | n/a — excluded from the required count entirely | n/a | No — excluded from both the denominator and the numerator |
| `opencode:E5`, `opencode:E6` | **Does not match `NOT TESTED (host credential)`** | Fails outright — `opencode` is itself one of the three *required* agents, not optional, so this disposition cannot apply regardless of cause | n/a | **Yes — both block** |
| `MatrixH:H7` | `PARTIAL` — not a named disposition at all | n/a | n/a | **Yes — blocks** |
| `MatrixB:B4`, `MatrixB:B7`, `MatrixG:G4`, CLI row `13` | `FAIL` — not a named disposition at all | n/a | n/a | **Yes — all four block** |

No row this run qualifies for either named disposition.

## 13. CLI experience (22 rows: 21 PASS, 1 FAIL)

Source: part C, executed through the real Windows pseudo console
(`scripts/testing/conptydriver`) driving `scripts/tuisandbox`'s synthetic
bench. Rows 2 and 22 compared byte-for-byte against a `v0.5.1` release
binary on the identical bench. Row 21 captured on legacy conhost (no
`WT_SESSION`, no `TERM_PROGRAM`, no UTF-8 locale). Per the rc.4 dispatch,
none of the 22 rows is a carried disposition — all 22 passed at
`v0.6.0-rc.3`, and neither of this candidate's own changes (the Pi reader
fix, the Qwen range widening) touches the interactive CLI or the
frozen-output guard.

**Harness-hygiene finding, fixed before any row counted.** The ambient
shell carried the host's real `XDG_DATA_HOME`, `TERM_PROGRAM`/
`TERM_PROGRAM_VERSION`, and a live `TERM` value that `scripts/tuisandbox`'s
own env fragment does not isolate; a wrapper additionally cleared
`XDG_DATA_HOME`, `GEMINI_CLI_HOME`, `KIMI_CODE_HOME`, `COPILOT_HOME`,
`CURSOR_CONFIG_DIR`, `CLINE_DATA_DIR`, `PI_CODING_AGENT_DIR`, `QWEN_HOME`,
`OH_PERSISTENCE_DIR`, `GROK_HOME`, `TERM`, `WT_SESSION`, `LANG`, `LC_ALL`,
`LC_CTYPE` before every row. Every row below reports the bench's documented
**9 sessions · 3 agents**, confirming no host session leaked in.

| # | Row | Result | Evidence |
| - | --- | ------ | -------- |
| 1 | Bare `rein` on a capable terminal draws the switcher | PASS | Header `9 sessions · 3 agents · all projects`, filter prompt, time-grouped rows, readiness glyphs, split preview, key bar |
| 2 | Bare `rein` on a non-TTY exits `2`, byte-identical to `v0.5.1` | PASS | Both exit `2`; stderr SHA-256 byte-identical, `0476fb687fa1c1209d243016ab020460b8ad55696e4052f491b1fcb8cdb4795b` (matches rc.2/rc.3's own hash). Stdout (cobra help text) legitimately differs — new subcommands/flags since `v0.5.1` |
| 3 | `--plain` falls back to the numbered switcher | PASS | Frozen numbered-picker text |
| 4 | `REINSTATE_NO_TUI=1` does the same | PASS | Byte-identical to row 3 |
| 5 | `TERM=dumb` does the same | PASS | Byte-identical to row 3 |
| 6 | `TERM` unset draws the switcher on Windows | PASS | Full interactive switcher, identical shape to row 1 |
| 7 | A terminal below 40×10 falls back to plain | PASS | `30×8`: wrapped numbered-picker text |
| 8 | `NO_COLOR` draws the switcher with no color escape sequences | PASS | Zero SGR color params, exactly one bare `ESC[m` reset |
| 9 | Typing filters; header count follows | PASS | `"auth"` → `1 session · 1 agent` |
| 10 | `f` in list mode filters, does not fork | PASS | Filter line reads `f`, header narrows; no action screen opened |
| 11 | `tab` opens the action menu; `esc` reverts without acting | PASS | Key bar changes then reverts exactly (`sha256` match) to the pre-`tab` frame |
| 12 | `ctrl+k` opens the palette; a subsequence query finds its command | PASS | `"hof"` → top match `Hand off to another…` |
| 13 | Readiness glyphs resolve for visible rows and a read-only agent shows blocked without a probe | **FAIL — new finding** | The interactive switcher's default `ScopeAll` ("all projects") scope shows every one of the 9 visible rows as `CANNOT RESUME`/`Blocked`, unconditionally, reproduced 9/9 across 7 separate launches — including sessions independently confirmed `ready` by three other channels under the identical harness (single-agent `resume --dry-run --json`; the plain picker's `i N` inspect; the switcher itself auto-scoped to one project via cwd). See "Row 13 — full evidence" below |
| 14 | The warning checklist acknowledges with the spacebar and shows the equivalent command | PASS | One `space` toggles exactly one warning (`baseline.unavailable`); equivalent command live-updates and reverts on a second `space` |
| 15 | A partial acknowledgement is refused with exit `7` | PASS | 2 of 4 warnings acknowledged: exit `7`, names exactly the remaining two (`git.working_tree`, `runtime.node.declaration`) |
| 16 | The handoff studio measures each policy and the equivalent command follows the selection | PASS | Full measurement shows carried-across/left-behind breakdown; policy cycling `balanced→full→checkpoint` tracks the equivalent command exactly |
| 17 | The studio refuses `enter` on a plan that could not be built | PASS | Foreign `repository_url` session: "this handoff cannot be planned"; `enter` does not send, studio stays open with an inline refusal line |
| 18 | `rein init` opens the wizard, validates per field, and allows going back | PASS | Invalid endpoint refused in place with the correct message; `up` returns one step, preserving the typed value |
| 19 | `rein init` collects no secret material inside the full-screen program | PASS | Walked all 7 steps: no access-key/secret-key/passphrase prompt; `--help` has no credential flag |
| 20 | `rein init --link` prints a code that `--paste` consumes on the other device | PASS | Real two-device BYO round trip against the product's own `s3test` fixture: device B's `--paste` pre-fills provider/endpoint/bucket/region/prefix/profile-ID exactly matching device A, down to the identical profile UUID |
| 21 | Glyphs degrade to ASCII where Unicode is not safe (legacy conhost) | PASS | `WT_SESSION`/`TERM_PROGRAM`/`TERM_PROGRAM_VERSION`/`LANG`/`LC_ALL`/`LC_CTYPE` all unset: every glyph is the ASCII set (`asciiGlyphs`), never Unicode |
| 22 | Every `--json` document is byte-identical to `v0.5.1`, except changelog-explained differences | PASS | 8 documents compared on the identical bench: 2 byte-identical (`handoff --no-launch --json`; `doctor --json` differs only in the version string); 6 differ, every difference traces to exactly two documented classes (the `agent.active` check; Grok's tier-promotion capability cascade) — no third class found or needed |

**21/22 PASS, 1 FAIL (row 13, new finding — not a carried `v0.6.0-rc.3`
disposition).**

### Row 13 — full evidence

Launching bare `rein` from a working directory that is **not** inside any
tracked project puts the switcher in its documented `ScopeAll` mode. In that
mode every one of the 9 visible rows shows the `Blocked` glyph and the
preview pane reads `○ CANNOT RESUME`, unconditionally — including sessions
seeded `READY` with a clean repo and a matching branch — regardless of a
3–8 second settle time, reproduced **9/9** across 7 separately-launched
processes with varied viewport sizes and cold/warm indexes.

**Ruled out a stale/pending state.** `internal/ui/theme.go`'s `Glyph`
function maps every readiness value other than `Ready`/`Warn`/`Blocked` to
the *pending* glyph, never to `Blocked` — the captured glyph is
unambiguously the completed `Blocked` verdict, not an unresolved probe.

**Ruled out the underlying engine**, via an A/B across four channels under
the identical `Start-Process`/ConPTY harness on the same 9-session index:
(1) single-agent `resume --dry-run --json` from a non-repo cwd →
`"decision":"ready"`, correct; (2) `--plain` then `i 1` (multi-agent
refresh, non-repo cwd) → every check `match`/`present`, correct; (3) the
interactive switcher, cwd **inside** a tracked project (auto-narrows to
`ScopeProject`) → `● READY TO RESUME`, correct, 3/3; (4) the interactive
switcher, cwd outside any repo (`ScopeAll`, 9 sessions) → every row `○
CANNOT RESUME`, wrong, 9/9. Channel 2 ran from the identical non-repo
directory that produces the failure in channel 4, ruling out "cwd is not a
repository" as a sufficient explanation on its own, and ruling out harness
overhead (channels 1–3 all use the identical `Start-Process`/ConPTY harness
and are all correct). The one variable separating the wrong outcome from
the three correct ones is the interactive switcher specifically, in
`ScopeAll` (`internal/tui/switcher/model.go`'s `New` sets `scope =
ScopeAll` whenever `opts.Project == ""`).

**Where this points in the source** (read-only observation, no rebuild
attempted against the tagged binary): `internal/tui/readiness/prober.go`'s
`Lookup` returns `ReadinessBlocked` immediately, without consulting the
probe cache, whenever `record.ReadOnlyReason != "" || !record.CanResume` —
the only path to `Blocked` that bypasses `FromReport` entirely. `rein
sessions --json` shows `capabilities:{resume:true,fork:true}` for every one
of the 9 records including `grok` (consistent with its tier promotion), so
`CanResume` is `true` in the index proper; whether the switcher's own
`ScopeAll` record-loading path populates `CanResume`/`ReadOnlyReason`
differently than `ScopeProject`'s does was not established from outside the
binary.

**Disposition.** `FAIL`, not carried from any prior run (all 22 rows passed
22/22 at `v0.6.0-rc.2` and `v0.6.0-rc.3`) and not attributable to the
harness by the A/B above. This is the switcher's **default** launch
condition for any user not already standing inside a tracked project
directory — not a narrow edge case.

**Non-blocking findings (harness, this run):** the `ctrl+k` palette overlay
renders with some background list text visible through/around the panel in
the ConPTY-captured frame (row 12) — the top match and prompt text are both
unambiguous, not scored; `scripts/testing/conptydriver`'s grammar has no
`shift+tab` keystroke, `key up` substituted successfully everywhere tried
(rows 18, 20); a `send "text\n"` step reliably fails to submit to a plain
`bufio.Scanner`-based prompt under this harness, a separate `key enter`
step works every time.

## 14. Hop parity journeys (16 rows)

Source: part D (`H1`, `H1b`, `H1c`, `H2`, `H3`, `H4`, `H5`, `H9`, `H10`,
`H11`, `H12` — 11 rows, all `PASS`) and part E (`H6`, `H6b`, `H8`, `H8b` —
4 rows, all `PASS`; `H7` — `PARTIAL`), against a local, disposable `hopd` +
`scripts/testing/fakelocker` lab, never the hosted control plane and never a
developer's real agent trees except where the ground rules specifically
authorize the host's live Claude Code config (H5, isolated via a throwaway
project and single-session pushes/pulls only). **15/16 PASS, 1 PARTIAL
(`H7`)** — per the rc.4 dispatch, none of these 16 rows is a carried
disposition; every one passed at `v0.6.0-rc.3`'s tagged run and this
candidate's own fixes touch neither Hop, the daemon (beyond the `H7`
lab-isolation harness method), nor the interactive surfaces.

| # | Row | Result | Summary |
| - | --- | ------ | ------- |
| H1 | Email sign-in through the approver; token in OS keyring; `whoami` names the device | PASS | Real GET-then-POST via `hoplab approve`; token confirmed present in Windows Credential Manager by a fresh, separate process |
| H1b | Refused sign-in tells the terminal why and stores nothing | PASS | `hoplab approve -refuse`; link expired on its own (~10min default TTL); documented refusal, exit 1; `REINSTATE_HOME` never created |
| H1c | Unreachable control plane prints the one-line message and documented exit code | PASS | `REINSTATE_HOP_URL` at an unused loopback port; human+json both match documented message, exit 1, `details.kind=control_plane_unreachable`; nothing written |
| H2 | `init --hop` provisions the locker exactly once | PASS | `locker_provisioned\|1` in `hopd.db`; second run refused client-side, exit 7, no second event |
| H3 | `account init` recovery code shown once; keyring format 5, generation 1, verifies | PASS | Recovery code via a live inheritable-FD pipe; `keyring.v1.json` fetched directly and verified: `CurrentGeneration=1`, signature verifies pinned and unpinned |
| H4 | `push --all` sends one Claude, one Codex, one OpenCode session; `first_push` fires once | PASS (step 4 fails for the pre-existing `fakelocker AnyBucket` harness reason, unchanged) | Decrypted manifest: `3 session(s) (claude 1, codex 1, opencode 1)`; `first_push\|1` exactly once; no-op push reports `skipped:3` |
| H5 | Wipe/new-device sign-in/`account recover`/`pull --all`/`resume --dry-run` complete for all three; one real resume | PASS | Codex and OpenCode legs **genuinely deleted from local disk**, then restored byte-for-byte from ciphertext; the Claude leg's file could not be deleted (tool-use policy block on the live config dir), so its pull correctly reported a conflict against still-present identical content; `resume --dry-run` reached `confirmation_required` for all three; a real `claude --resume` against the still-intact live config recalled the exact planted token `RC4H5TOKEN-9d3c72` |
| H9 | `sync verify` names only observed objects | PASS (step 4 fails for the same harness reason as H4) | `checked_objects` names exactly `"the index"` and `"the newest snapshot in the index"`; step 4 status is `"fail"`, never `"could not run"` |
| H10 | `sync migrate --to byo`, `--switch`, `--forget-hop` | PASS | 3 snapshots migrated in one combined call; `switched:true, forgot_hop:true`; local `whoami` afterward correctly refuses; `hopd.db.devices` shows the migrated device **not** revoked — confirms "does not revoke"; new BYO bucket's `sync verify` step 4 reports `not-applicable` |
| H11 | Path remap between two homes' project mappings | PASS (macOS leg deferred) | A third device, mapping the pushing device's project id to its own different real local path, pulled with `cwd` rewritten to its own path; `resume --dry-run` reports `workspace.available: present` |
| H12 | Keyless diagnostics refuse a forged, rolled-back, or re-keyed keyring with nothing written | PASS | Direct S3 `PUT`s of (1) a byte-tampered signature, (2) a genuinely-signed but stale single-device object, and (3) a different, genuinely-enrolled account's own keyring: each refused correctly (signature-verify error / not-enrolled refusal / explicit different-account refusal, exit 7); the device's own `account.json` byte-identical throughout |
| H6 | Device B `account join`; device A `devices approve`; B pulls | PASS | Live pairing via `hoplab pair join`; `pairing_requests` row confirmed `version=2, status=consumed, claims=2`; device B pulled all 3 of device A's sessions; `sessions --json` on B showed 6 sessions, no key overlap |
| H6b | An expired pairing request is refused/rolled back; B's wrap absent from every generation | PASS | Real expiry forced with `HOPD_PAIRING_TTL=8s`; both `account join`/`devices approve` refused client-side; `pairing_requests` row confirmed `status=expired, payload IS NULL`; `account status` showed `device_in_keyring=false` |
| H7 | `daemon install/status/stop/start/uninstall` round trip through Task Scheduler; the foreground loop pushes/pulls correctly | **PARTIAL — operator unavailable, new reason this run** | Not exercised. This run's shell held no administrator rights, a mandatory prerequisite for the rc.4 dispatch's own lab-isolation method (a fresh Windows account) before the row's own UAC prompt can even be reached. Polled `D:/ReinstateAcceptanceProjects/h7-go.txt` for a maintainer go-ahead from `07:13:25Z` through `07:16:09Z` (and, per the coordinator's later confirmation, on to `07:17:50Z`); it never appeared. No unattended-elevation workaround was attempted, per the dispatch's own harness-traps warning. Because the round trip was never started, **no write of any kind occurred this run to `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, or `XDG_DATA_HOME`** — the opposite outcome from `v0.6.0-rc.3`'s part E, which did write to those live homes (that report's §16/§21, the reason this dispatch added the lab-isolation requirement in the first place) |
| H8 | A revokes B: generation rolls, B's token refused; lagging-device attack; 404-floor proxy | PASS | `keygeneration_crossplane_test.go -tags hopacceptance` against a real `hopd`: both crossplane tests passed (`1.46s`, `1.32s`), 404-floor residual reproduced (asked 4 times, answered 404 every time). Live: device A revoked device B, generation rolled 1→2; B's `whoami`/`push --all` both refused, exit 4 |
| H8b | A Console-initiated revocation stays pending until the recovery-code command confirms it | PASS | A local, ephemeral self-signed-TLS reverse proxy drove the real Console HTTP routes end to end (email sign-in, CSRF scrape, `POST /console/devices/{id}/revocation`); `device_revocation_requests` row went `pending` (target device stayed functional) then `confirmed` after `rein devices revoke` printed "confirmed after the keyring reached generation 3"; target's next push refused, exit 4 |

**`H7`'s `PARTIAL` this run has a different root cause than any prior run's
`H7` disposition** (`v0.6.0-rc.2`: UAC declined twice, no acceptor present;
`v0.6.0-rc.3`: fully completed, but incidentally wrote to live agent homes,
motivating this candidate's lab-isolation method): this run's executor
could not even reach the elevation prompt because setting up the mandatory
fresh-lab-account prerequisite itself needs administrator rights this
session never held, and no maintainer go-ahead arrived inside the run's
practical window. See §16, §18, §20.

## 15. T5 encrypted sync round trip (beyond the 178; source: part B)

A disposable `scripts/testing/fakelocker` instance served the S3-compatible
locker; two fully isolated `REINSTATE_HOME`s (including isolated
`CLAUDE_CONFIG_DIR`/`CODEX_HOME`/`XDG_DATA_HOME` for both devices, never the
host's live homes on either side) were paired against it; passphrase
delivery used a purpose-built inherited-handle helper (`passfd.exe`) driving
`REINSTATE_PASSPHRASE_FD`, the same `AdditionalInheritedHandles` pattern
`scripts/testing/hoplab/secretfd_windows.go` already documents for
`REINSTATE_RECOVERY_CODE_FD`.

| Row | Result | Evidence |
| --- | ------ | -------- |
| `push:claude` | PASS | Pushed the isolated device-A `claude` session |
| `push:codex` | PASS | Pushed the isolated device-A `codex` session |
| `push:opencode` | PASS | Pushed a freshly-created isolated device-A `opencode` session |
| `pull:claude` | PASS | Device B's isolated home restored the pulled session; raw file contains the planted token 10 times |
| `pull:codex` | PASS | Device B's isolated home restored the pulled session; raw file contains the planted token 7 times |
| `pull:opencode` | PASS | Initial refusal (`compatibility`, `NOT_INSTALLED` — device B's isolated `XDG_DATA_HOME` had no OpenCode layout yet) matched the row's own documented reason; one harmless `opencode run` created the layout, then the retry pulled successfully; `rein search` on device B's isolated home found the pulled session |

**Full isolation this run — no write touched either device's live agent
homes at any point in the round trip, including the pull half**, unlike
`v0.6.0-rc.3`'s part B, whose pull half was not re-isolated and wrote into
the host's live `CODEX_HOME`/`XDG_DATA_HOME` (that report's §15, §21). This
run's method — setting all four isolation variables in every command on
both devices, including pull — is the fix for that incident and should be
the standard method going forward.

## 16. Findings (merged, release-blocking assessment)

| ID | Severity | Row(s) | Description | Release blocking |
| -- | -------- | ------ | ------------ | ----------------- |
| — | product, MAJOR, new this run | `MatrixB:B4` | The `gemini` agent's probed `tree` includes two real, un-normalized project-name directory segments under `tmp/`, verbatim from this host; every path segment beneath them is correctly shape-normalized, only the top segment is not | Blocks — a confirmed redaction gap, first surfaced by this run's choice to probe `gemini`'s tree for this generic assertion |
| — | product, MINOR, new this run | `MatrixB:B7` | An overridden agent root (`CLINE_DATA_DIR`) that exists but is empty is indistinguishable from one that does not exist at all — `doctor --agents --json` output is byte-identical apart from the timestamp in both cases | Blocks — a confirmed correctness gap, first surfaced by this run's choice to probe `cline`'s override for this generic assertion |
| — | documentation/contract, MINOR, not a new regression | `MatrixG:G4` | `push`/`pull --agent` completion (and `defaultRegistry()`) already include `opencode` alongside `claude`/`codex`, matching OpenCode's published T5 promotion and the exact behavior `v0.6.0-rc.3`'s own G4 row scored `PASS`; the contract's row text ("Claude and Codex sessions, and no other agent's") was never updated to match, and this run's executor scored the row against that literal text | Blocks the required-row count per the literal contract text and this run's own scoring, but reflects no behavior change from `v0.6.0-rc.3`; recommend the maintainer either edit G4's row text to read "Claude, Codex, and OpenCode" (matching G3's own carried note) or explicitly decide OpenCode's presence in sync completion is itself unintended and should be reverted — either resolution clears this finding without further product work |
| — | product, MAJOR, new this run | CLI row `13` | The interactive switcher's default `ScopeAll` scope shows every visible session as unresumable regardless of true state — every user who runs bare `rein` from outside a tracked project directory (the most common real-world launch condition) sees this | Blocks — a confirmed interactive-CLI defect, isolated to `internal/tui/switcher`'s `ScopeAll` record-loading path via black-box A/B testing (full repro in §13); root cause not confirmed against the tagged binary's own source, out of this run's scope |
| F-OPENCODE-VERSION-DRIFT | host/harness, new this run | `opencode:E5`, `opencode:E6` | Host's real OpenCode auto-updated to `1.18.29`, above the verified ceiling `1.18.27`, mid-run; every launch-plan-building row past the point of drift is correctly refused (`exit 5`, `agent.version` block) | Blocks — matches neither disposition (`opencode` is a required agent, not optional); same shape and remedy path as `v0.6.0-rc.3`'s `F-QWEN-VERSION-DRIFT`, which this candidate already resolved for `qwen` — recommend the same treatment: verify `1.18.29` (or later) on native Windows physical-resume evidence and widen the ceiling under ADR 0005 D3, or pin the lab/CI install to prevent auto-update mid-run |
| — | operator/harness availability, new reason this run | `MatrixH:H7` | This run's shell held no administrator rights, and the mandatory lab-isolation prerequisite (a fresh Windows account) itself needs elevation to create — the row's own UAC prompt could not be reached at all this run | Blocks — `PARTIAL` does not pass a required row; no unattended-elevation workaround was attempted, per the dispatch's own explicit prohibition; no live-agent-home write occurred this run (the opposite risk from `v0.6.0-rc.3`'s incident) |
| — | product, disclosed, non-blocking | `codex:E2` | Several recall probes aimed specifically at the earliest turn's planted token were answered incorrectly or refused by the model, while probes about more recent turns were answered correctly, and the raw session file was independently confirmed to contain every turn intact | Not blocking (row scored PASS on file-level and partial in-agent evidence) — flagged as worth a follow-up to determine whether this is a `codex exec resume` context-window characteristic |
| — | harness, disclosed, non-blocking | `grok:E3` | The scripted "fork check ack" completion prompt had not yet been answered when the ConPTY driver's timeout fired; the new, distinct session id (the row's own assertion) is unambiguous regardless | Not blocking (row scored PASS on the distinct-session-id evidence) — a longer wait window would give a fully clean transcript too |

## 17. Tier reductions proposed by this device

| Agent | Claimed tier | Verified tier | Reason |
| ----- | ------------ | -------------- | ------ |
| — | — | — | None proposed. Every declared tier (T0–T5, no T3 this candidate) was exercised as designed. `opencode`'s E5/E6 gap this run is a version-compatibility block against a specific auto-updated install, not evidence the product misclassifies `opencode`'s own capability tier — every other `opencode` row (`C1`–`E4`) passed cleanly against the same install before the drift, and the tier gate itself (`C5`) is independently confirmed correct, separate from the version gate that blocks `E5`/`E6` |

## 18. `v0.6.0-rc.3` dispositions carried: cleared or re-recorded

Per [the rc.4 dispatch's own table](../v0.6.0-rc.4-agent-verification-prompts.md#dispositions-carried-from-the-v060-rc3-tagged-run):

| Row | `v0.6.0-rc.3` tagged result | This candidate expected | This run | Status |
| --- | ---------------------------- | ------------------------- | -------- | ------ |
| `pi:C3` | FAIL, product (confirmed code defect, `F-PI-CONTENT-EXTRACTION`) | Fixed — expect `PASS` on a fresh planted-token session | PASS — fresh planted-token session, `version:3` shape confirmed on disk, search found it, `prompt_preview` populated and contains the token | **CLEARED** |
| `qwen:D1` | NOT TESTED, host/harness (version drift, `F-QWEN-VERSION-DRIFT`) | Fixed — expect `PASS` against the real `0.23.0` install in an isolated `QWEN_HOME` | PASS — `agent.version.status=match` confirmed, full mechanism run | **CLEARED** |
| `qwen:D2` | NOT TESTED, same reason | Same — expect `PASS` | PASS | **CLEARED** |
| `qwen:D3` | NOT TESTED, same reason | Same — expect `PASS` | PASS | **CLEARED** |
| `qwen:D4` | NOT TESTED, same reason | Same — expect `PASS` | PASS — offset/hash independently recomputed, matched | **CLEARED** |
| `qwen:D5` | NOT TESTED, same reason | Same — expect `PASS` | PASS | **CLEARED** |
| `qwen:E1` | NOT TESTED, same reason | Same — expect `PASS` | PASS — planted token recalled via real `-p --resume` | **CLEARED** |
| `qwen:E2` | NOT TESTED, same reason | Same — expect `PASS` | PASS | **CLEARED** |
| `qwen:E3` | NOT TESTED, same reason | Same — expect `PASS` | PASS — distinct forked session id confirmed | **CLEARED** |
| `qwen:E5` | NOT TESTED, same reason (version drift preempted `agent.active` scoring) | Same — expect `PASS` | PASS — via ConPTY, `agent.active status=present,actual=true` | **CLEARED** |
| `qwen:E6` | NOT TESTED, same reason (version drift preempted the non-interactive safety check) | Same — expect `PASS` | PASS — exit 7 | **CLEARED** |
| `codex:E5` | PARTIAL, harness gap (`F-E5-NO-PTY`, no TTY) | Retry under `scripts/testing/conptydriver`; expect `PASS`, else record the same harness-gap reason | PASS — ConPTY per dispatch requirement | **CLEARED** |
| `opencode:E5` | PARTIAL, same reason | Same retry method; expect `PASS`, else record the same reason | **NOT TESTED** — but for a **different** reason (`F-OPENCODE-VERSION-DRIFT`, a live host version-drift block, not the no-PTY harness gap) | **RE-RECORDED**, new reason, still blocks |
| `grok:E5` | PARTIAL, same reason (not re-attempted at rc.3) | Same retry method; expect `PASS`, else record the same reason | PASS — genuinely active process, confirmed via `tasklist` (also usable without ConPTY here) | **CLEARED** |
| `MatrixH:H7` | PASS at rc.3 (mechanism), but its round trip incidentally wrote to this host's live agent homes (report §21, `#424`) | Run only under the lab-isolation method above; expect `PASS` with matching before/after listing digests | **PARTIAL** — the mandatory fresh-lab-account prerequisite itself needs administrator rights this session never held; the round trip could not be started at all, so no digest comparison was possible and no live-home write occurred either | **RE-RECORDED**, new reason (operator/elevation availability, not a completed-but-contaminated run), now blocks in a different way than it did not block at rc.3 |
| `opencode:D4` | N/A (definitional), unchanged since `v0.6.0-rc.1` | Not fixed, not fixable — expect `N/A (definitional)`, same reason, excluded from the required count | N/A (definitional), same reason | **CLEARED** (unchanged) |

**14 cleared** (`pi:C3`, `qwen:D1`–`D5`, `qwen:E1`/`E2`/`E3`/`E5`/`E6`,
`codex:E5`, `grok:E5`, `opencode:D4`) **and 2 re-recorded** (`opencode:E5`,
`MatrixH:H7` — both with a genuinely different reason than the disposition
they carry forward from), **0** regressed in the sense of a worse
disposition on the *same* underlying cause.

Both of this candidate's own targeted fixes are fully confirmed:
`F-PI-CONTENT-EXTRACTION` (11 `pi`/`qwen` rows plus the standalone `pi:C3`
row all clear) and the Qwen Code range widening (every `qwen` row `C1`–`E6`
now `PASS`, the widened ceiling independently confirmed correct both
directions by `E4`'s shim evidence).

**New findings not on the carried table, all blocking:** `MatrixB:B4`,
`MatrixB:B7` (both `PASS` at `v0.6.0-rc.3` against a *different* agent's
mechanism for the same generic assertion — neither is a regression, both
are newly-surfaced real gaps), `MatrixG:G4` (`PASS` at `v0.6.0-rc.3` against
identical shipped behavior, `FAIL` now against the same unchanged behavior
scored literally against stale contract text), CLI row `13` (`PASS` 22/22
at both `v0.6.0-rc.2` and `v0.6.0-rc.3`, a genuinely new interactive-CLI
defect), `opencode:E6` (`PASS` at `v0.6.0-rc.3`, `NOT TESTED` now — the same
`F-OPENCODE-VERSION-DRIFT` root cause as the carried `opencode:E5` row
above, but `E6` itself was not on the carried-dispositions table because it
had not previously failed).

## 19. Deferred: Apple Silicon macOS

Copied verbatim from
[`../v0.6.0-rc.4-agent-verification-prompts.md`](../v0.6.0-rc.4-agent-verification-prompts.md)'s
own "Deferred: Apple Silicon macOS" section, under
[ADR 0005](../../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md) D2.
Nothing in this device's evidence claims macOS. Every row below is run
against this same `v0.6.0-rc.4` tag (or the stable tag it becomes) once the
Apple Silicon macOS host returns from repair
([issue #403](https://github.com/HarjjotSinghh/reinstate/issues/403)). If
that later macOS run fails, the fix ships as `v0.6.1` — this record of this
candidate's Windows acceptance is never edited to hide it.

| Group | Rows | Contract | Result |
| ----- | ---- | -------- | ------ |
| Phase 5 generated matrix, macOS column | all rows section B lists | Phase 5 | `DEFERRED` |
| CLI experience, macOS column | rows 1–20 and 22 | `v0.5.2` | `DEFERRED` |
| Hop cross-device journeys | pairing macOS↔Windows; path remap Windows↔macOS both directions; daemon launchd round trip; first push on macOS | hosted #16 | `DEFERRED` |
| Range widening on macOS | Claude Code through `2.1.263`; OpenCode through `1.18.27`; Qwen Code through `0.23.0` | ADR 0005 D3 | `DEFERRED` |
| Grok GD8 | tool-approval prompt (already collected on macOS; the Windows half is #368 and stays optional) | Grok T4 | `DEFERRED` |

No document, data file, or release note may state that `v0.6.0` was verified
on macOS until that run exists.

## 20. Release-blocking findings

Because 7 of the 215 required rows are not `PASS` and are not excused by
either disposition in the contract, this report's device verdict is `FAIL`
and stable `v0.6.0` is **not authorized** by this run:

1. `MatrixB:B4` — the `gemini` agent's probed `tmp/` tree leaks two real,
   un-normalized project-name directory segments verbatim from this host
   (product, confirmed redaction gap, new finding)
2. `MatrixB:B7` — an empty overridden agent root is indistinguishable from
   an absent one (product, confirmed correctness gap, new finding)
3. `MatrixG:G4` — `push`/`pull` completion already includes `opencode`,
   contradicting the contract's still-literal "Claude and Codex only" row
   text; identical shipped behavior to `v0.6.0-rc.3`'s own `PASS`-scored
   G4 row (documentation/contract mismatch, not a regression)
4. CLI row `13` — the interactive switcher's default `ScopeAll` scope shows
   every visible session as unresumable regardless of true state (product,
   confirmed interactive-CLI defect, new finding)
5. `opencode:E5` — host OpenCode auto-updated past the verified ceiling
   mid-run, correctly refused before reaching the row's own mechanism
   (host/harness, `F-OPENCODE-VERSION-DRIFT`, does not qualify for
   `NOT TESTED (host credential)` because `opencode` is a required agent)
6. `opencode:E6` — same reason
7. `MatrixH:H7` — the mandatory lab-isolation prerequisite itself needed
   administrator rights this run's session never held, and no maintainer
   go-ahead arrived inside the run's practical window (operator/harness
   availability; no live-agent-home write occurred this run, unlike
   `v0.6.0-rc.3`'s incident on the same row)

None of the seven is attributed by its reporting executor to a defect in
this candidate's own two changes (the Pi reader fix, the Qwen Code range
widening) — both are fully confirmed working (§18). Two are genuinely new
product findings surfaced by this run's specific choice of which agent to
probe for a pre-existing generic assertion (`B4`, `B7`); one reflects a
documentation/contract text that has been stale since at least
`v0.6.0-rc.3` describing identical, unchanged shipped behavior (`G4`); one
is a genuinely new interactive-CLI defect (row 13); two are a host
vendor-CLI auto-update past this candidate's verified ceiling, the same
failure shape `qwen` hit at `v0.6.0-rc.3` and this candidate already fixed
for `qwen` (`opencode:E5`/`E6`); and one is an operator/elevation
availability gap this run's harness correctly refused to route around
(`H7`). But per this run's own gate ("A row is PASS only if its mechanism
was exercised; PARTIAL and NOT TESTED do not pass a required row") and the
contract's narrow, mechanically-checked disposition rule (§0.8, §12), none
of the seven qualifies for an exemption, so the device verdict is `FAIL`.

## 21. Harness defects (merged, non-blocking to the mechanism under test)

- **A first `cursor:C6` attempt that copied the entire `~/.cursor` tree
  (including a ~700KB Statsig telemetry cache) via `cp -r` through Git Bash
  appeared to hang past 120s (part A).** A narrower copy (`chats/` subtree
  only) plus a bounded PowerShell job completed the same corruption test in
  well under 30s with a clean, non-hung result — the slow step was the file
  copy on this host, not `rein` itself.
- **A naive `push --agent opencode --dry-run` probe returned `config
  missing` (exit 3, no Hop profile in this shell) before reaching agent
  validation (part A).** This blocked confirming whether a real `opencode`
  push would transfer data, so the `MatrixG:G4` finding above is a
  completion/registry-level finding only, not a confirmed data-plane
  regression.
- **`opencode` self-updated `1.18.27`→`1.18.29` mid-run, above this
  candidate's verified ceiling (part B).** Blocks `opencode:E5`/`E6` as
  `NOT TESTED` — see `F-OPENCODE-VERSION-DRIFT` in §12/§16.
- **`grok -p --resume` hangs headless (part B).** A plain `grok --resume
  <id> -p "<prompt>"` with stdin closed hung indefinitely, reproduced 3×
  with different prompts/timeouts up to 90s; the identical session driven
  through `scripts/testing/conptydriver` completed correctly within
  seconds. Not a `rein` defect — `rein`'s own launch plan is unchanged
  either way; flagged for a follow-up on how future headless E1/E2/E3 rows
  for this agent should be run.
- **`codex exec resume` recall inconsistency (part B, disclosed under
  `codex:E2`, not blocking).** Several recall probes aimed specifically at
  the earliest turn's planted token were answered incorrectly or refused,
  while probes about more recent turns were answered correctly and the raw
  session file was independently confirmed intact; recorded PASS on the
  file-level and partial in-agent evidence.
- **A stray Windows PATH resolution trap (part B).** A bare `rein`/
  `reinstate` lookup in a fresh shell can resolve to a pre-existing
  host-installed copy ahead of a fresh test install's own directory; every
  row in part B used the fully-qualified path to this run's own binary
  after one early exploratory call hit this trap (no test evidence came
  from that call).
- **`REINSTATE_PASSPHRASE_FD` needs a real inherited Windows handle, not a
  bash file descriptor (part B).** A plain `exec 3<file` in Git Bash does
  not cross into a native Windows child process the way it would for a
  POSIX child; a small helper (`passfd.exe`) using the same
  `syscall.SysProcAttr.AdditionalInheritedHandles` pattern
  `scripts/testing/hoplab/secretfd_windows.go` already documents for
  `REINSTATE_RECOVERY_CODE_FD` was required.
- **The interactive switcher's `ctrl+k` palette overlay renders with some
  background list text visible through/around the panel in the
  ConPTY-captured frame (part C).** The top match and prompt text are both
  unambiguous; not scored — worth a maintainer glance at whether a real
  terminal draws the same overlap.
- **`scripts/testing/conptydriver`'s grammar has no `shift+tab` keystroke
  (part C).** `key up` (a documented working alternate) substituted
  successfully everywhere tried.
- **A `send "text\n"` step reliably fails to submit to a plain
  `bufio.Scanner`-based prompt under this ConPTY harness (part C).** A
  separate `key enter` step after `send` (no trailing `\n`) works every
  time.
- **A duplicate second `rein login` for `H1b`'s device-x (part D),**
  self-inflicted by the session chasing backgrounded output; expired on its
  own with no effect on the row's evidence.
- **`hoplab pair init`/`pair recover` always run `rein init --hop` first and
  refuse a device whose `REINSTATE_HOME` is already initialized (parts D,
  E).** Any device reused across two rows needed its `REINSTATE_HOME`
  cleared first — a sequencing note, not a product defect.
- **`hoplab homes`'s fixture `cwd`
  (`C:\Users\fixture-user\code\<device>`) does not exist on this host and
  this account cannot create it (parts D, E, matches `v0.6.0-rc.2`'s and
  `v0.6.0-rc.3`'s prior findings).** For `H11`, worked around cleanly
  through the normal `config.toml` project-mapping mechanism. For `H5`'s
  Codex/OpenCode legs, that mechanism does not apply (their recorded
  workspace is vendor-embedded, not project-id-indirected); worked around
  by directly editing the pulled fixture's own recorded workspace field.
  Also observed: `agent.executable` reported `missing` alongside
  `workspace.available:missing` for both Codex and OpenCode, flipping to
  `present` together once the recorded workspace was fixed — consistent
  with the executable/version probe running inside the session's own
  recorded cwd.
- **This session's own tool-use classifier blocks a broad delete under the
  host's live Claude Code config directory (parts D, E).** Prevented
  literal deletion of the real Claude session file for `H5`'s wipe step;
  worked around per the same evidentiary shape `v0.6.0-rc.2`'s and
  `v0.6.0-rc.3`'s tagged reports used for this row (conflict against
  still-present content + a real resume proving the content is genuinely
  still there).
- **`fakelocker`'s `AnyBucket:true` (carried, unchanged since `v0.6.0-rc.1`,
  parts D, E).** Makes step 4 of `push --all`'s automatic post-push
  verification and `sync verify`'s isolation step fail against every
  Hop-mode locker in this lab (H4, H9) — contrasted directly by H10's real
  BYO `not-applicable` result and H12's real cross-account signature
  refusal, both of which exercise the equivalent real security boundary
  correctly.
- **Ambient host contamination re-observed (part E).** A fresh shell on
  this host shows persistent user-level `REINSTATE_BACKEND=memory` and
  `REINSTATE_MEMORY_BACKEND_DIR=D:\Projects\hop-10-lab\locker`, despite the
  v0.6.0 acceptance contract's run notes recording both removed on
  2026-09-06. Not scored against any row — every `rein` invocation ran
  behind an explicit `unset` or `hoplab env`'s own strip, confirmed
  immediately before each call — but flagged for the maintainer to remove
  again (or find what reintroduces it).
- **Restarting only `hopd` (not the whole lab) with `HOPD_STORAGE=fake`
  silently orphans an account's existing locker bucket in memory (part E,
  carried from `v0.6.0-rc.3`'s part E finding).** Avoided this run by
  re-establishing pairing state freshly after the `HOPD_PAIRING_TTL`
  restart rather than reusing pre-restart state across it.
- **`rein init --hop` on an already-initialized `REINSTATE_HOME` correctly
  refuses without `--force` (carried, part E).** Device B's `H6` project
  mapping was added directly in `config.toml` using a TOML literal
  (single-quoted) string — a double-quoted string with literal Windows
  backslashes fails TOML's own escape parsing, a TOML syntax fact, not a
  product defect.
- **`H7` — not attempted this run; no incident (part E).** Because the
  round trip was never started, this run caused no write of any kind to
  `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, or `XDG_DATA_HOME` — unlike
  `v0.6.0-rc.3`'s part E, which did (that report's §16, §21, the reason the
  rc.4 dispatch added the lab-isolation requirement this disposition is
  honoring).

## 22. Terminated device block

> Device testing is terminated for this candidate at the milestone recorded
> above. The results in this report are final for this device and this tag.
> Any further testing requires a new candidate tag and a new report.

- Terminating tester: assembler (tagged-run coordinator role, this session)
- UTC timestamp: 2026-09-07T14:05:00Z
- Device verdict: **FAIL** — 7 of 215 required rows are not `PASS` and are
  not excused by either disposition in the contract (`MatrixB:B4`,
  `MatrixB:B7`, `MatrixG:G4`, CLI row `13`, `opencode:E5`, `opencode:E6`,
  `MatrixH:H7`); see the Verdict block and §20 above for the complete list
  and reasoning. Both of this candidate's own targeted fixes are confirmed
  working (`F-PI-CONTENT-EXTRACTION`, the Qwen Code range widening — §18),
  and fourteen of the sixteen `v0.6.0-rc.3` carried dispositions clear
  cleanly, including three prior `PARTIAL` harness-gap rows now `PASS` via
  the ConPTY retry method (`codex:E5`, `grok:E5`, and `pi:C3`'s fix
  confirmed) — but two carried rows re-record with a genuinely different
  reason than the one they carry forward from (`opencode:E5`'s harness gap
  became a version-drift block; `H7`'s completed-but-contaminated run
  became an operator-unavailable non-start), and this run's own assigned
  slice of the matrix surfaced three findings no prior tagged run of this
  candidate lineage had recorded (`MatrixB:B4`, `MatrixB:B7`, and CLI row
  `13`). This does not authorize stable `v0.6.0`. No real host data was
  touched by any part of this run requiring maintainer cleanup (unlike
  `v0.6.0-rc.3`'s two URGENT incidents) — `H7`'s non-start and the T5
  round trip's full four-variable isolation on both devices both avoided
  repeating that class of incident. A fix pass for `MatrixB:B4`/`B7`
  (probe redaction), a fix pass for CLI row 13 (`internal/tui/switcher`'s
  `ScopeAll` record-loading path), a reconciliation of `MatrixG:G4`'s
  contract text against the shipped OpenCode-inclusive registry, a decision
  on the OpenCode version ceiling (or a re-pin of the host's OpenCode
  install before the next run), and a maintainer or administrator-capable
  executor available for `MatrixH:H7`'s UAC prompt are required before the
  next tagged acceptance run.
