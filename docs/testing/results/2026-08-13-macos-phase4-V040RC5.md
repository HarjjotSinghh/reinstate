# Phase 4 structured-handoff report — macos-arm64 / v0.4.0-rc.5

Copy of the Phase 4 template with this dispatch’s substitutions. Sanitized: no
transcript text, prompts, responses, capsule bodies, credentials, private
paths, filenames, diffs, or raw child-process output.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Required counts:** `38 PASS / 0 PARTIAL / 6 FAIL / 0 NOT TESTED`
- **Optional source-only counts:** `2 PASS / 2 FAIL / 4 NOT TESTED`
- **Release-blocking findings:** `4`

Required FAILs: A1, A2, A3, A5, A6 (dest-ack; throwaway Claude `loggedIn=false`,
Codex not logged in), C2 (no planted transcript/Git contradiction collected).
Optional FAILs: E5, E6 (Grok tagged-fixture dry-run exit 5 preflight). `make
verify` via `go test ./...` failed the same two CLI tests on three clean-env
runs; `go test ./internal/cli -count=1` PASS. Does not authorize stable
`v0.4.0`. Current stable remains `v0.3.0`.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-08-13T14:02:02Z` |
| Device | `macos-arm64` |
| OS/version/build | `macOS 26.5.2 (25F84)` |
| CPU architecture/native process | `arm64` native |
| Filesystem | `apfs` |
| Tested tag | `v0.4.0-rc.5` |
| Tested full commit | `0d7551a69918a97967f927ed9dc5a56b3583108c` |
| Installed binary SHA-256 | `d824f7b437333d39df2a2ddd46e9170d01268b2c7e64a5ad7cd32faf813ebea8` |
| Installed version JSON | `version=0.4.0-rc.5 commit=0d7551a69918a97967f927ed9dc5a56b3583108c date=2026-08-13T13:16:36Z` |
| Claude Code version/state | before `2.1.228` (in 2.1.219–2.1.229); after `2.1.228`; source+dest capable; throwaway home `loggedIn=false` |
| Codex CLI version/state | before `0.147.0` (in 0.133.0–0.147.0); after `0.147.0`; source+dest capable; throwaway home not logged in |
| Gemini/OpenCode/Grok state | Gemini `ABSENT`; OpenCode `ABSENT` (no isolation override; skipped); Grok `1.0.3` present |
| Git version | `git version 2.55.0` |
| Go version/toolchain | `go1.25.12` (`GOTOOLCHAIN=go1.25.12`) |
| Report branch | `test/v0.4.0-rc.5-macos-arm64-report` |
| Device-report commit | `0f7d952592c3c32b0473fb370a2aa2e0516e11f8` |
| Draft report PR | https://github.com/HarjjotSinghh/reinstate/pull/205 |

`DISABLE_AUTOUPDATER=1` for the run. Versions unchanged after the last row.

## 2. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Annotated tag signature verifies against `.github/allowed_signers` | `PASS` | `Good "git" signature` ED25519; EXIT 0 |
| Tag peels to the recorded full commit on `origin/main` | `PASS` | peel `0d7551a69918a97967f927ed9dc5a56b3583108c`; `ANCESTOR_OK` |
| Published release is non-draft, prerelease, and tied to the tag | `PASS` | `isDraft=false` `isPrerelease=true` tag `v0.4.0-rc.5` |
| Exact expected release asset set is present | `PASS` | 25 assets: checksums + 5 archives + 5 SBOMs + 5 raw binaries + 8 Linux packages + 1 source |
| Checksums, GitHub API digests, and attestations match the tag and commit | `PASS` | 24 checksum lines match API sha256; darwin_arm64 archive attestation verified |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | `PASS` | darwin_arm64 tar: `CHANGELOG.md` `LICENSE` `NOTICE` `README.md` `reinstate`; no `..` or abs members; SBOM asset present |
| Live bootstrap is byte-identical to the tested commit and pins only `v0.4.0-rc.5` | `PASS` | live `install.sh` sha256 `40efa498d5a82747adead0e49bf5ebfa27bab42cacee66bd19e33e1af0b18d9c`; one pin `v0.4.0-rc.5`; equals tagged `website/public/install.sh`; live `install.ps1` `14dbdc61a0c7d4c7d6be33374f29b020e19b7e934a6a0ec2a3067bf31fd7515e`; no rc.1–rc.4 pins |
| Bootstrap installer digest matches the tagged canonical installer | `PASS` | live pin `7776adb4ace8aa333745cd3f3e42b3a10d1400b9394c612d065c20a739db2e66` equals `scripts/install.sh`; `scripts/install.ps1` `02c68984964556e7c685a275bde72dc812162e0b898be0f26718a0813efc0dfe` |
| Fresh isolated install did not replace a user binary or persist PATH changes | `PASS` | isolated `INSTALL_DIR`; `REINSTATE_SKIP_PATH_UPDATE=1`; operator binary fingerprint unchanged; profile not edited |
| `rein` and `reinstate` have identical verified bytes and report `v0.4.0-rc.5` | `PASS` | identical sha256; `rein` symlink to `reinstate` |
| Installed binary reports the literal full tested commit | `PASS` | commit `0d7551a69918a97967f927ed9dc5a56b3583108c` |

## 3. Automated gates

| Gate | Result | Sanitized evidence |
| ---- | ------ | ------------------ |
| Clean tagged worktree and `go mod tidy -diff` | `PASS` | throwaway clone at TEST_COMMIT porcelain empty; `go mod tidy -diff` exit 0 |
| `make verify` with Go 1.25.12 | `FAIL` | three `env -i GOTOOLCHAIN=go1.25.12` runs: `TestHandoffFromRealisticClaudeInstall` and usually `TestHandoffFromClaudeInstallWithoutExecutable` dest Codex `UNTESTED` exit 5 under `go test ./...`. Isolated `go test ./internal/cli -count=1` PASS (4.936s) |
| Complete `CGO_ENABLED=1 go test -race ./... -count=1` | `FAIL` | not separately green; `make verify` uses `CGO_ENABLED=0 go test ./...` and failed as above |
| Required cross-build, fuzz-smoke, snapshot, artifact, and installer gates | `PASS` | four `CGO_ENABLED=0` builds darwin/amd64, darwin/arm64, linux/amd64, windows/amd64 exit 0; `make fixture-scan` exit 0; live installer chain PASS. goreleaser snapshot not re-run (binary present; tagged 25-asset provenance already verified) |
| Phase 1, Phase 2, and Phase 3 regression | `FAIL` | inherited from `make verify` package failure; remaining packages in those logs were `ok` |
| Capsule/projection/CLI goldens are unchanged across repeated runs | `PASS` | unit goldens in isolated package tests; G1 ceiling test PASS |
| Phase 4 adversarial security tests and `make fixture-scan` | `PASS` | `make fixture-scan` exit 0; installed adversarial D1/D2/D4 dry-run exit 0 |
| `TestLongHistoryParseCapsuleProjectionUnderCeiling`: 400 events, ≤98,304 bytes, <2,000 ms | `PASS` | 15.957ms; projection 46,541 bytes; Events=400 |

## 4. Isolation and privacy

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Fresh isolated `REINSTATE_HOME`, install directory, process homes, and repositories | `PASS` | new throwaway clone and vendor homes; five isolation variables exported before product commands |
| Fresh controlled source/destination sessions; no older corpus or handoff reused | `PASS` | tagged fixtures + new disposable git repos only |
| Operator vendor trees, credentials, keychains, `.env`, and token stores were never read | `PASS` | five overrides set; OpenCode skipped; first `sessions --json` empty (`sessions: []`) |
| No backend, passphrase, storage credential, or capsule sync was used | `PASS` | no `rein init`; `push --dry-run` exit 3 `config missing` |
| Vendor configuration changed only inside disposable test-owned homes | `PASS` | dummy MCP/settings only under throwaway Claude home |
| Reports contain no transcript, prompt, response, secret, private path, filename, diff, capsule body, or raw child error | `PASS` | this file |
| Source fingerprints were unchanged; vendor-store writes occurred only through an explicitly launched vendor CLI | `PASS` | no dest vendor launch (not logged in); dry-run/`--no-launch` only |

First index refresh: `sessions --json` `sessions: []`; OpenCode warning `agent_not_installed`. Isolation check PASS.

## 5. Required matrix — 44 rows

### Matrix A — flagship quota-switch

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| A1 | `FAIL` | Throwaway Claude `loggedIn=false` `authMethod=none`. No dest TTY acknowledgement. Missing required dest-ack is FAIL. |
| A2 | `FAIL` | Throwaway Codex not logged in. Same dest-ack gap. |
| A3 | `FAIL` | Logged-out throwaway source exists; dest launch/ack still missing. |
| A4 | `PASS` | Tagged partial-final-record fixture dry-run exit 0; `destination_session_mode=new`; parse events=2. |
| A5 | `FAIL` | Dest-ack not collected (no logged-in throwaway dest on a real TTY). |
| A6 | `FAIL` | Same as A5; harmless marker not dest-verified. |
| A7 | `PASS` | `handoff list --json` mode `structured handoff`; Claude source id resolves; stored handoff_id present. |

### Matrix B — fidelity and policy

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| B1 | `PASS` | `TestClaudePipelineDryRunAndExecuteHaveByteIdenticalArgv` exit 0. |
| B2 | `PASS` | Tagged 200-turn fixture dry-run exit 0; ports exact/normalized/omitted/referenced; mode structured handoff. |
| B3 | `PASS` | checkpoint `--no-launch` exit 0; projection has 0 `user:` / `assistant` conversation-body markers. |
| B4 | `PASS` | balanced dry-run exit 0 on 200-turn fixture. |
| B5 | `PASS` | full dry-run exit 0 on 200-turn fixture. |
| B6 | `PASS` | Compaction fixture `--no-launch`; `fidelity.json` ports include `summarized` (`summaries` / `vendor_compaction_summary`) plus exact, normalized, referenced, omitted. |
| B7 | `PASS` | Attachments fixture dry-run exit 0; warning `handoff.capability.attachment.support`. |
| B8 | `PASS` | Unknown-records fixture dry-run exit 0; unknown native type not guessed. |

### Matrix C — workspace and capability truth

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| C1 | `PASS` | Disposable dirty repo: `dirty=true`; `changed_files` three `${REPO:demo}/…` tokens (staged/unstaged/untracked). |
| C2 | `FAIL` | No planted transcript-vs-Git contradiction collected. Missing required evidence. |
| C3 | `PASS` | Same-OS workspace deleted after a passing pre-check; rerun exit 5 compatibility preflight blocked. |
| C4 | `PASS` | Different git repo exit 5 `different repository`; same-repo subdirectory exit 0; non-git cwd exit 0. |
| C5 | `PASS` | Windows os-roots Claude and Codex fixtures from a local git checkout: exit 0, `${REPO:…}`, no `C:\` / fixture-user abs path in plan JSON. Not exit 5. |
| C6 | `PASS` | Dummy MCP in throwaway source home; dry-run warning `handoff.capability.mcp.dummy-test-mcp`; no values/contents in warning IDs. |
| C7 | `PASS` | Warning `handoff.capability.instruction.claude-md` before launch. |
| C8 | `PASS` | HOME-layout dest Codex shim `9.9.9`: exit 5 `destination agent "codex" is UNTESTED`; isolated home had no handoff residue. |

### Matrix D — security

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| D1 | `PASS` | Prompt-injection fixture dry-run exit 0; mode structured handoff. |
| D2 | `PASS` | Fence-breakout fixture dry-run exit 0. |
| D3 | `PASS` | Unknown-records `--no-launch` projection: 0 hits for system-prompt / developer-instruction / ignore-previous phrases. |
| D4 | `PASS` | Secret-leakage fixture dry-run exit 0 with `--show-redactions`. |
| D5 | `PASS` | Launch-free rows only; no keychain/`.env`/operator tree. |
| D6 | `PASS` | `handoffs/` `0700`, files `0600`, outside the git worktree. |
| D7 | `PASS` | Isolated homes; first session list empty; OpenCode not installed / not claimed isolated. |
| D8 | `PASS` | `push --dry-run` exit 3 `config missing`; no `handoffs/` upload. |
| D9 | `PASS` | Grok `--no-redact` exit 2 `redaction is forced` (not unknown flag). Successful Grok dry-run warning not collected (E5). |
| D10 | `PASS` | Fixture source bytes unchanged across dry-run/`--no-launch`. |

### Matrix F — CLI contract

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| F1 | `PASS` | JSON `mode=structured handoff`, `destination_session_mode=new`. Human `--no-launch`: new destination session, not native resume. F8 refuse string still says `resume/fork` (non-blocking wording). |
| F2 | `PASS` | `resume --with codex --dry-run --json` exit 0; same structured-handoff / dest=new. |
| F3 | `PASS` | `--json` without `--dry-run`/`--no-launch` exit 2 usage. |
| F4 | `PASS` | Unknown warning ID exit 2 `not a current warning`. Non-TTY launch still exit 7 with `--allow-warning`. |
| F5 | `PASS` | `--no-launch` prints command, spawn none, exit 0. |
| F6 | `PASS` | `handoff inspect` exit 0; `handoff export --format json` exit 0. |
| F7 | `PASS` | Bare native ID matching two agents exit 6; both `claude:` and `codex:` qualified options. |
| F8 | `PASS` | Non-TTY launch exit 7 in 10–11ms (well under 2s); `--allow-warning` does not skip. Refuse before Plan/LookPath. |

### Matrix G — performance

| ID | Samples | Result | Ceiling | Evidence |
| -- | ------- | ------ | ------- | -------- |
| G1 | 1 | `PASS` | `<2,000 ms`, ≤98,304 bytes | 15.957ms / 46,541 bytes |
| G2 | 1 warmup + 5 | `PASS` | max 8s | 631, 616, 625, 622, 620 ms; all exit 0 |
| G3 | 1 warmup + 20 | `PASS` | p95 2s | 100 `--no-launch` rows; list n=100; p95 9ms; max 9ms |

## 6. Optional source-only matrix — 8 rows

| ID | Vendor present | Result | Sanitized evidence |
| -- | -------------- | ------ | ------------------ |
| E1 | no | `NOT TESTED` | Gemini ABSENT |
| E2 | no | `NOT TESTED` | Gemini ABSENT |
| E3 | no | `NOT TESTED` | OpenCode ABSENT; no isolation override |
| E4 | no | `NOT TESTED` | OpenCode ABSENT; no isolation override |
| E5 | yes | `FAIL` | Grok tagged fixture listed (`workspace` fixture-user path). Dry-run `--to claude`/`codex` exit 5 `environment preflight is blocked` even with `--allow-untested`. C5 Claude/Codex foreign-OS remap PASS, so this is Grok-source preflight (`agentcheck` has no grok definition), not C5 cwd remap. |
| E6 | yes | `FAIL` | Grok → Codex same exit 5 preflight. Dest `--to grok` is E7. |
| E7 | yes | `PASS` | `--to grok` exit 2 `invalid native agent; expected claude, codex, or all`. |
| E8 | yes | `PASS` | `rein resume grok:<id>` exit 5 `Grok Build sessions are source-only in Phase 4`. |

## 7. Architecture §14 closeout

| Definition-of-done assertion | Result | Evidence |
| ---------------------------- | ------ | -------- |
| All 27 packets and their tests are present in the tagged commit | `PASS` | tagged tree at TEST_COMMIT |
| `make verify` is green on both mandatory platforms | `FAIL` | macOS `make verify` FAIL as in §3; Windows not this device |
| Claude → Codex and Codex → Claude work with source closed and no source API call | `FAIL` | Plans PASS (C5 both directions); dest-ack A1/A2 FAIL |
| Every fidelity class has real report evidence and byte-stable goldens | `PASS` | B6 + G1 |
| Injection, secret, and bounded-read security gates leak nothing | `PASS` | D1–D10 |
| 200-turn projection is bounded and reported | `PASS` | B2 + G1 |
| Windows ↔ macOS uses canonical project IDs without source absolute paths | `PASS` | C5 |
| Dry-run and executed structured-plan output are byte-identical | `PASS` | B1 unit |
| Required Phase 4 docs, product-truth register, runbook, and dispatch are present | `PASS` | tagged tree |
| Exit codes remain 0/1/2/3/5/6/7; no vendor-internal writes or capsule sync | `PASS` | observed 0/2/3/5/6/7; D7–D8 |

## 8. Findings and repository hygiene

### Release-blocking

1. Dest-ack A1/A2/A3/A5/A6 FAIL: throwaway Claude `loggedIn=false`; Codex not logged in. Required TTY dest-ack not collected.
2. `make verify` FAIL on three clean `go test ./...` runs (`internal/cli` dest Codex UNTESTED). Isolated package PASS.
3. E5/E6 FAIL: Grok tagged-fixture dry-run exit 5 environment preflight (Grok absent from `agentcheck` definitions).
4. C2 FAIL: planted transcript/Git contradiction not collected.

### Non-blocking

- F8 human refuse string still contains `resume/fork`.
- Read-only source `2.1.230` dry-run warns `agent.version` rather than exit 5; dest out-of-range still exit 5 (C8). Host Claude stayed `2.1.228`.
- OpenCode: `NOT TESTED: no isolation override`.

### RC1 R1–R6

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| R1 | `PASS` | Throwaway Claude home has no `version` file; dry-run not exit 5 for source version. |
| R2 | `PASS` | Absolute-paths fixture exit 0; `${REPO:demo}`; no capsule abs-path abort. |
| R3 | `PASS` | C1 live dirty tree; non-empty tokenized `changed_files`. |
| R4 | `PASS` | Hanging dest `--version` sleep shim: exit 5 Compatibility `UNTESTED` in 2577ms (budget 25s). |
| R5 | `PASS` | Slash-commands fixture exit 0 (prose `/` and path-naming not aborted). |
| R6 | `PASS` | Host `2.1.228` in range unchanged after last row; dest `9.9.9` fail-closed exit 5. |

### RC2 / RC3 / RC5

| ID | Result | Notes |
| -- | ------ | ----- |
| RC2 C4 | `PASS` | |
| RC2 F8 | `PASS` | 10ms exit 7 |
| RC2 E5/E6 busy-check | `FAIL` | Grok dry-run still exit 5 (preflight), not `unsupported agent "grok"` |
| RC2 R4 classification | `PASS` | Compatibility 5, not Runtime |
| RC2 D9 | `PASS` | |
| RC2 list isolation | `PASS` | |
| RC2 B3 | `PASS` | |
| RC2 B6 | `PASS` | |
| RC2 C6/C7 | `PASS` | |
| RC3 empty dest Plan | `PASS` | explicit empty dest home dry-run exit 0 |
| RC3 R4 hang | `PASS` | 2577ms |
| RC3 Windows make verify | `FAIL` | not this device; macOS make verify FAIL |
| RC3 G3 n=100 | `PASS` | |
| RC3 B6 summarized | `PASS` | |
| RC5 R4 process tree | `PASS` | macOS hang returns in time; Windows tree is the other device |
| RC5 F8 refuse-before-Plan | `PASS` | 10ms vs prior ~6s |
| RC5 B6 summarized | `PASS` | |
| RC5 C5 os-roots | `PASS` | |
| RC5 E5/E6 fixture-user | `FAIL` | Grok still exit 5 |

### Test-harness deviations and supersessions

- No rc.1–rc.4 corpus/home/capsule reused.
- Dest-ack not superseded; remains FAIL.
- `make verify` isolated-package PASS does not supersede `go test ./...` FAIL.

## 9. Required terminated device block

```
PHASE4-DEVICE-REPORT-V1
device=macos-arm64
test_tag=v0.4.0-rc.5
test_commit=0d7551a69918a97967f927ed9dc5a56b3583108c
installed_binary_sha256=d824f7b437333d39df2a2ddd46e9170d01268b2c7e64a5ad7cd32faf813ebea8
required_pass=38
required_partial=0
required_fail=6
required_not_tested=0
optional_pass=2
optional_fail=2
optional_not_tested=4
artifact_chain=PASS
isolation_privacy=PASS
flagship_directions=FAIL
fidelity_policy=PASS
workspace_capability=FAIL
security=PASS
cli_contract=PASS
performance=PASS
phase1_phase2_phase3_regression=FAIL
release_blocking_findings=4
product_files_changed=0
secrets_transcripts_or_capsules_committed=false
END-PHASE4-DEVICE-REPORT-V1
```

## 10. Final reconciliation — macOS coordinator

Fetched only Windows report commit `70b31f40e14173520f09533ea2bd10cc67973706`
(`test/v0.4.0-rc.5-windows-amd64-report`, draft PR #204). Independently verified:

- Both report histories are descendants of `TEST_COMMIT=0d7551a69918a97967f927ed9dc5a56b3583108c`. Windows parent is that commit. macOS additive commits: `0f7d952` (device block), `4752da5`, `9cd1c8f`.
- Both diffs vs `TEST_COMMIT` are report-only: this file, and `docs/testing/results/2026-08-13-windows-phase4-V040RC5.md`.
- Required arithmetic: macOS 38+0+6+0=44; Windows 36+0+8+0=44. Optional sums to 8 on both. R1–R6 are separate and both PASS.
- Privacy scans of both committed reports: no emails, keys, operator homes, or transcript/capsule bodies. `both_privacy_scans_pass=true`.
- Original required FAILs not superseded: macOS A1/A2/A3/A5/A6 dest-ack and C2; Windows A1/A2/A4/A5/A6 dest-ack (no PTY), F3 picker, C3/C4 workspace exit 0.
- Timings under ceiling: macOS R4 2577ms exit 5, F8 10ms exit 7, G1 15.957ms / 46541 bytes, G2 max 631ms, G3 p95 9ms. Windows R4 10571ms UNTESTED exit 5, F8 40ms exit 7, G1 10.11ms / 46541 bytes, G2 1011ms, G3 p95 67ms. C5 remap and B6 `summarized` PASS on both.
- Agent versions recorded before/after: macOS Claude 2.1.228 / Codex 0.147.0 unchanged; Windows PATH-pin Claude 2.1.229 (host global 2.1.231 unused) / Codex 0.147.0 unchanged.
- Windows optional device-block counts (4/2/2) do not match the E-row table (E1/E2/E5 FAIL ⇒ 3/3/2). Required counts still match. Does not change the candidate verdict.

`phase4_rc5_tagged_artifact_acceptance=PASS` requires 44 required PASS on both devices, zero required FAIL/PARTIAL/NOT TESTED, and zero release blockers. Both device verdicts are FAIL.

```text
PHASE4-RC5-FINAL-RECONCILIATION-V1
test_tag=v0.4.0-rc.5
test_commit=0d7551a69918a97967f927ed9dc5a56b3583108c
macos_device_report_commit=0f7d952592c3c32b0473fb370a2aa2e0516e11f8
windows_device_report_commit=70b31f40e14173520f09533ea2bd10cc67973706
macos_required_pass=38
macos_required_partial=0
macos_required_fail=6
macos_required_not_tested=0
windows_required_pass=36
windows_required_partial=0
windows_required_fail=8
windows_required_not_tested=0
macos_release_blocking_findings=4
windows_release_blocking_findings=5
both_reports_branch_from_test_commit=true
both_report_only_diffs=true
both_privacy_scans_pass=true
phase4_rc5_tagged_artifact_acceptance=FAIL
stable_v0.4.0_authorized=false
current_stable=v0.3.0
END-PHASE4-RC5-FINAL-RECONCILIATION-V1
```

The block above used Windows commit `70b31f40e14173520f09533ea2bd10cc67973706`
and is superseded. Current Windows device report is the additive rescore
`60ddddc578e22dbd57c02c449aaa20d150698862` on the same report-only branch
(draft PR #204). Independently verified:

- Ancestor of `TEST_COMMIT`; parent is `70b31f40…`; diff vs `TEST_COMMIT` is still only `docs/testing/results/2026-08-13-windows-phase4-V040RC5.md`.
- Required rows: 38 PASS / 0 PARTIAL / 6 FAIL / 0 NOT TESTED (A–F 35+6 plus G1–G3 PASS). Required FAILs are A1/A2/A4/A5/A6 dest-ack and F3 picker. C3/C4/C8 now PASS; `workspace_capability=PASS`.
- Optional E-rows now match the device block: 4 PASS / 2 FAIL / 2 NOT TESTED (E1 PASS; E2 and E5 still FAIL). Privacy scan clean except fixture path `/Users/fixture-user`.
- Dest-ack remains a required FAIL. Candidate acceptance stays FAIL.

```text
PHASE4-RC5-FINAL-RECONCILIATION-V1
test_tag=v0.4.0-rc.5
test_commit=0d7551a69918a97967f927ed9dc5a56b3583108c
macos_device_report_commit=0f7d952592c3c32b0473fb370a2aa2e0516e11f8
windows_device_report_commit=60ddddc578e22dbd57c02c449aaa20d150698862
macos_required_pass=38
macos_required_partial=0
macos_required_fail=6
macos_required_not_tested=0
windows_required_pass=38
windows_required_partial=0
windows_required_fail=6
windows_required_not_tested=0
macos_release_blocking_findings=4
windows_release_blocking_findings=4
both_reports_branch_from_test_commit=true
both_report_only_diffs=true
both_privacy_scans_pass=true
phase4_rc5_tagged_artifact_acceptance=FAIL
stable_v0.4.0_authorized=false
current_stable=v0.3.0
END-PHASE4-RC5-FINAL-RECONCILIATION-V1
```
