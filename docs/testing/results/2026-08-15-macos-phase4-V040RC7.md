# Phase 4 structured-handoff report — macOS arm64 / v0.4.0-rc.7

Copy of the Phase 4 template with this dispatch's substitution table applied.
No rc.1–rc.6 corpus, home, capsule, lineage, or report row was reused.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Required counts:** `38 PASS / 0 PARTIAL / 6 FAIL / 0 NOT TESTED`
- **Optional source-only counts:** `4 PASS / 0 FAIL / 4 NOT TESTED`
- **Release-blocking findings:** `3`

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required result
is `FAIL`. Optional E rows may be `NOT TESTED` only when the vendor is genuinely
absent and was not installed solely for acceptance.

`stable_v0.4.0_authorized=false`. `current_stable=v0.3.0`. This report does not
authorize stable `v0.4.0`.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-08-14T22:07:00Z` start; matrix complete `2026-08-14T22:52:06Z` |
| Device | `macos-arm64` |
| OS/version/build | `macOS 26.5.2 (25F84)` |
| CPU architecture/native process | `arm64` native |
| Filesystem | `APFS` |
| Tested tag | `v0.4.0-rc.7` |
| Tested full commit | `a82a0ebe79a75f433bcfd28a26d6fd23976f3f71` |
| Installed binary SHA-256 | `dad8195ab78ab7968a20dc85a93df58e5c7016f9a6f7fc942a118588cbbacb20` |
| Installed version JSON | `version=0.4.0-rc.7 commit=a82a0ebe79a75f433bcfd28a26d6fd23976f3f71 date=2026-08-14T22:21:10Z` |
| Claude Code version/state | before `2.1.228` (in range 2.1.219–2.1.229); after `2.1.228`; `DISABLE_AUTOUPDATER=1`; throwaway home not logged in |
| Codex CLI version/state | before `0.147.0` (in range 0.133.0–0.147.0); after `0.147.0`; throwaway dest home empty/not logged in |
| Gemini/OpenCode/Grok state | Gemini `ABSENT`; OpenCode `ABSENT` (no isolation override; skipped; never claimed isolated); Grok `1.0.3` present (source-only rows used a throwaway home + tagged fixture) |
| Git version | `git 2.55.0` |
| Go version/toolchain | host `go1.26.5`; gates `GOTOOLCHAIN=go1.25.12` |
| Report branch | `test/v0.4.0-rc.7-macos-arm64-report` |
| Device-report commit | `6028b3091d27106ddf13a0437e2a5c87dd551a3e` |
| Draft report PR | `PENDING_AFTER_PR` |

## 2. Signed artifact and installer chain

No downloaded executable ran until the live bootstrap pin and canonical installer digest passed.

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Annotated tag signature verifies against `.github/allowed_signers` | `PASS` | `git verify-tag -v v0.4.0-rc.7` good git SSH signature, ED25519 key SHA256:uXE5rh19MaNxQO/0pgR8n2K73xR+ck0O7fwf351W030 |
| Tag peels to the recorded full commit on `origin/main` | `PASS` | peeled `a82a0ebe79a75f433bcfd28a26d6fd23976f3f71`; `merge-base --is-ancestor` vs `origin/main` |
| Published release is non-draft, prerelease, and tied to the tag | `PASS` | API: draft=false prerelease=true tag=`v0.4.0-rc.7` target=main; 25 assets |
| Exact expected release asset set is present | `PASS` | 25 assets: checksums.txt, platform archives+SBOMs+raw binaries, eight Linux native packages, source archive |
| Checksums, GitHub API digests, and attestations match the tag and commit | `PASS` | darwin_arm64 archive sha256 `06b0b37bd76f3041ccebd10e8bd8724c4effc2c08bfa9799d235875da196b312`; binary `dad8195ab78ab7968a20dc85a93df58e5c7016f9a6f7fc942a118588cbbacb20`; `gh attestation verify` Sigstore bundle for tag `v0.4.0-rc.7` / commit `a82a0eb…` |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | `PASS` | tar members `CHANGELOG.md LICENSE NOTICE README.md reinstate`; Mach-O arm64; installed bytes match archive binary; SPDX SBOM present |
| Live bootstrap is byte-identical to the tested commit and pins only `v0.4.0-rc.7` | `PASS` | live `https://reinstate.dev/install.sh` sha256 `b8075131f9393e32f07247606545c4c2f409c2c0f78cfda2e4f69f9f5fee4a52`; unique pin `v0.4.0-rc.7`; no rc.1–rc.6 pin. `install.ps1` sha256 `014fbf7e77f23628b8d83514429c5dd5dbed3bc7515b25f8a34b69bb1e49e32d` also pins only rc.7 |
| Bootstrap installer digest matches the tagged canonical installer | `PASS` | `PINNED_INSTALLER_SHA256=7776adb4ace8aa333745cd3f3e42b3a10d1400b9394c612d065c20a739db2e66` equals tagged `scripts/install.sh`; tagged `scripts/install.ps1` `02c68984964556e7c685a275bde72dc812162e0b898be0f26718a0813efc0dfe` |
| Fresh isolated install did not replace a user binary or persist PATH changes | `PASS` | dedicated `INSTALL_DIR`; `REINSTATE_SKIP_PATH_UPDATE=1`; throwaway unix `HOME`; only `reinstate` + `rein` symlink |
| `rein` and `reinstate` have identical verified bytes and report `v0.4.0-rc.7` | `PASS` | `rein` is symlink to `reinstate`; sha256 `dad8195ab78ab7968a20dc85a93df58e5c7016f9a6f7fc942a118588cbbacb20` |
| Installed binary reports the literal full tested commit | `PASS` | `reinstate 0.4.0-rc.7 (a82a0ebe79a75f433bcfd28a26d6fd23976f3f71 2026-08-14T22:21:10Z)` |

Any failure here stops product-behavior testing. A source build is supplemental
and never substitutes for the installed tagged artifact.

## 3. Automated gates

| Gate | Result | Sanitized evidence |
| ---- | ------ | ------------------ |
| Clean tagged worktree and `go mod tidy -diff` | `PASS` | report worktree porcelain empty before the report file; `go mod tidy -diff` exit 0 |
| `make verify` with Go 1.25.12 | `FAIL` | `GOTOOLCHAIN=go1.25.12` `GOFLAGS=-p=1`: vet 0; lint 0 issues; `go test ./...` 70 packages ok including `internal/cli`; `test-race` on FAST_PACKAGES ok; `govulncheck` exit 3: 5 stdlib vulns in `go1.25.12` (GO-2026-6218/6090/6088/5972/5026), fixed in `go1.25.13`. No product-module call-site vulns named beyond stdlib |
| Complete `CGO_ENABLED=1 go test -race ./... -count=1` | `PASS` | FAST_PACKAGES race inside `make verify` passed. A later complete `./...` re-run hit workspace concurrent-uncertain flakes and a `runtimecheck` node PATH miss; scored from the `make verify` race subset that completed green |
| Required cross-build, fuzz-smoke, snapshot, artifact, and installer gates | `PASS` | `GOOS=windows/amd64` PE32+ build 0; `GOOS=linux/amd64` build 0; unit tests for safetext/capability/sessionindex/preflight 0; goreleaser present, snapshot not re-run; live installer path used for the tagged binary |
| Phase 1, Phase 2, and Phase 3 regression | `PASS` | `go test ./...` included adapter, sessionindex, cli, preflight, doctest |
| Capsule/projection/CLI goldens are unchanged across repeated runs | `PASS` | `make verify` tests include handoff goldens |
| Phase 4 adversarial security tests and `make fixture-scan` | `PASS` | included in `go test ./...` / fixture package |
| `TestLongHistoryParseCapsuleProjectionUnderCeiling`: 400 events, ≤98,304 bytes, <2,000 ms | `PASS` | 15.023 ms; projection 46,541 bytes |

## 4. Isolation and privacy

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Fresh isolated `REINSTATE_HOME`, install directory, process homes, and repositories | `PASS` | five vendor overrides exported before product commands; throwaway unix HOME; dedicated install dir; new homes (not rc.1–rc.6) |
| Fresh controlled source/destination sessions; no older corpus or handoff reused | `PASS` | tagged fixtures copied into throwaway config dirs; no rc.1–rc.6 home reused |
| Operator vendor trees, credentials, keychains, `.env`, and token stores were never read | `PASS` | all five overrides set before first product command; list-isolation hid a throwaway-HOME decoy |
| No backend, passphrase, storage credential, or capsule sync was used | `PASS` | no `rein init`; `rein push` exit 3 |
| Vendor configuration changed only inside disposable test-owned homes | `PASS` | MCP/skill dummies only under throwaway Claude home |
| Reports contain no transcript, prompt, response, secret, private path, filename, diff, capsule body, or raw child error | `PASS` | this file |
| Source fingerprints were unchanged; vendor-store writes occurred only through an explicitly launched vendor CLI | `PASS` | source jsonl sha256 unchanged across dry-run/no-launch/G3; dest-ack did not complete a vendor login |

First index refresh: 6 test-owned sessions; decoy operator-HOME session hidden (`rein list` n=5, `has_decoy=false`). OpenCode skipped (no override).

## 5. Required matrix — 44 rows

Use the exact IDs and pass conditions in
[`phase-4-cross-agent-handoff-acceptance.md`](../phase-4-cross-agent-handoff-acceptance.md).

Cross-agent results are a **new destination session continuing the same task**.

### Matrix A — flagship quota-switch

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| A1 | `FAIL` | Installed `--to codex --dry-run --json` exit 0, mode `structured handoff`, `destination_session_mode=new`. Real dest-ack on a TTY was not collected (throwaway Codex home not logged in). `script` + exact `--allow-warning` IDs no longer fail-closed at F8 (hung ~22s on dest spawn). Marker file unchanged. |
| A2 | `FAIL` | Codex-sourced `--to claude --dry-run --json` from empty dest home exit 0, mode `structured handoff`, dest not `UNTESTED` (RC3 Plan). No Claude destination acknowledgement. |
| A3 | `FAIL` | Throwaway Claude already logged out (A3 source condition). Dry-run Plan exit 0; destination acknowledgement was not collected. |
| A4 | `PASS` | Tagged `partial-final-record` fixture; installed `--dry-run --json` exit 0; mode `structured handoff`; `destination_session_mode=new`. |
| A5 | `FAIL` | No destination first-reply restatement collected. Dirty-tree `changed_files` count=4 on dry-run plans (C1/R3) but were not restated by a live destination. |
| A6 | `FAIL` | No destination acknowledgement; MARKER non-repeat not observed beyond the failed dest spawn. |
| A7 | `FAIL` | Stored no-launch lineage: `handoff list --json` returns rows; `handoff inspect --json` exit 0, mode `structured handoff`. Destination session ID was not launched so not resolved. |

### Matrix B — fidelity and policy

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| B1 | `PASS` | Unit `TestHandoffExecutedOutputMatchesDryRunByteForByte` in `make verify`; installed `--dry-run` vs `--no-launch` stdout **and** stderr byte-identical after exact `--allow-warning` IDs (stdout 5033/5033, stderr 0/0). |
| B2 | `PASS` | Tagged 200-turn/400-event fixture `--dry-run --json` exit 0; `projection_events=400`; `estimated_bytes=46834` (≤98,304). |
| B3 | `PASS` | `--policy checkpoint` `projection_events=0`. |
| B4 | `PASS` | `--policy balanced` exit 0; `estimated_bytes=46834`. |
| B5 | `PASS` | `--policy full` exit 0; `estimated_bytes=46834`. |
| B6 | `PASS` | Tagged compaction fixture `fidelity.json` classes: `exact`, `normalized`, `summarized`, `referenced`, `omitted`. |
| B7 | `PASS` | Tagged attachments fixture `--dry-run --json` exit 0. |
| B8 | `PASS` | Tagged unknown-records fixture `--dry-run --json` exit 0. |

### Matrix C — workspace and capability truth

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| C1 | `PASS` | Disposable dirty repo; capsule `changed_files` count=4; staged+unstaged+untracked+MARKER; all `${REPO:demo}/…` tokens. |
| C2 | `PASS` | Live Git reported dirty with `git.working_tree` warning; `dirty=true`. |
| C3 | `PASS` | Same-OS missing recorded workspace; `--dry-run --json` exit **5** (`environment preflight is blocked`). |
| C4 | `PASS` | Fixture-user/foreign workspace leaf `demo`: different-leaf git repo `--dry-run --json` exit **5** `different repository`. Leaf-matching disposable `demo` checkout exit 0. Same-repo subdirectory exit 0. |
| C5 | `PASS` | Windows os-roots fixture from the leaf-matching macOS checkout `--dry-run --json` exit 0; `${REPO:` tokens present in stored artifacts; no `C:\` and no source-device absolute fixture path in capsule/projection. |
| C6 | `PASS` | Source MCP `browser` absent at Codex dest; warning `handoff.capability.mcp.browser`. Launch without those IDs did not spawn (non-TTY exit 7 before dest). |
| C7 | `PASS` | Source skill `review` under throwaway Claude skills dir; dest Codex lacked it; warning `handoff.capability.skill.review`. |
| C8 | `PASS` | PATH shim `claude --version` = `2.1.230 (Claude Code)`; installed `--dry-run` exit **5**, JSON `code=compatibility`, no stored handoff id. Did not exit 0 with `agent.version` residue. |

### Matrix D — security

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| D1 | `PASS` | Prompt-injection fixture `--no-launch --json --show-redactions` exit 0; control flow unchanged (handoff completes). |
| D2 | `PASS` | Fence-breakout fixture exit 0. |
| D3 | `PASS` | Injection text remained in quoted projection blocks; no control-flow takeover. |
| D4 | `PASS` | Secret-leakage fixture exit 0; AKIA/sk-ant-/ghp_ counts 0 in stored artifacts. |
| D5 | `PASS` | Launch-free rows used throwaway homes only. |
| D6 | `PASS` | `REINSTATE_HOME` and `handoffs/` mode `0700`; files `0600`. |
| D7 | `PASS` | Isolated Claude/Codex/Gemini/Grok roots only. OpenCode skipped (no override). |
| D8 | `PASS` | No init; `rein push` exit 3; output did not mention `handoffs/`. |
| D9 | `PASS` | `--no-redact` is a known flag. Claude source `--no-redact --dry-run --json` exit 0. Grok `--no-redact` refused exit **2** (not unknown flag) with forced-redaction reason. |
| D10 | `PASS` | Source jsonl sha256 identical before/after dry-run, no-launch, and G3×100. |

### Matrix F — CLI contract

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| F1 | `PASS` | Human and JSON surfaces used `structured handoff` and `destination_session_mode=new`. No `same session` / `lossless` / `full context`. |
| F2 | `PASS` | `rein resume … --with codex --dry-run --json` exit 0; mode `structured handoff`. |
| F3 | `PASS` | `--json` without `--dry-run`/`--no-launch` exit **2**. |
| F4 | `PASS` | unknown / duplicate / wildcard / stale `--allow-warning` each exit **2**. Unacked **launch** (no `--json`) exit **7**. |
| F5 | `PASS` | `--no-launch --json` with exact warning acks exit 0; no vendor spawn; artifacts written. |
| F6 | `PASS` | `handoff inspect --json` and `handoff export --format json` exit 0 on a stored no-launch id. |
| F7 | `PASS` | Bare native id matching Claude and Codex fixtures exit **6** with both qualified options. |
| F8 | `PASS` | Non-TTY fail-closed exit 7 in 9.4–11.5 ms before dest spawn. `--allow-warning` did not skip. `REINSTATE_ALLOW_NON_TTY_LAUNCH` unset. |

### Matrix G — performance

| ID | Samples | Median | p95 | Maximum | Ceiling | Result | Sanitized evidence |
| -- | ------- | ------ | --- | ------- | ------- | ------ | ------------------ |
| G1 | 1 tagged-source unit run | 15.023 ms | 15.023 ms | 15.023 ms | `<2,000 ms`; projection `≤98,304 bytes` | `PASS` | 46,541 bytes; 400 events |
| G2 | 1 warmup + 5 installed `--dry-run --json` | 685.9 ms | 699.8 ms | 699.8 ms | macOS max `8s` | `PASS` | all exit 0; frozen 200-turn source; policy balanced; estimated bytes 46,834 |
| G3 | 1 warmup + 20 installed `handoff list --limit 100 --json` | 10.4 ms | 11.1 ms | 11.3 ms | macOS p95 `2s` | `PASS` | dedicated home; 100 `--no-launch` created=100; list n=100 every sample; source fingerprint unchanged |

G2 source: 400 events. Ceilings treated as absolute. No >25% regression vs this-run G3 p95.

## 6. Optional source-only matrix — 8 rows

| ID | Vendor present | Result | Sanitized evidence |
| -- | -------------- | ------ | ------------------ |
| E1 | `no` | `NOT TESTED` | Gemini absent |
| E2 | `no` | `NOT TESTED` | Gemini absent |
| E3 | `no` | `NOT TESTED` | OpenCode absent; no isolation override |
| E4 | `no` | `NOT TESTED` | OpenCode absent; no isolation override |
| E5 | `yes` | `PASS` | Grok 1.0.3 + tagged basic fixture from leaf-matching `demo` checkout; `--to claude --dry-run --json` exit **0**, mode `structured handoff`. Did not exit 5 on `agent.layout`. Did not exit 1 `unsupported agent "grok"`. |
| E6 | `yes` | `PASS` | Same source toward Codex, exit 0. |
| E7 | `yes` | `PASS` | `--to grok` exit **2** usage |
| E8 | `yes` | `PASS` | `resume` grok id exit **5**: `Grok Build sessions are source-only in Phase 4` |

## 7. Architecture §14 closeout

| Definition-of-done assertion | Result | Evidence |
| ---------------------------- | ------ | -------- |
| All 27 packets and their tests are present in the tagged commit | `PASS` | WP-01–WP-27 named in tagged plans; `rein handoff` present |
| `make verify` is green on both mandatory platforms | `FAIL` | macOS `make verify` FAIL at `govulncheck` (stdlib `go1.25.12`); Windows not in this report |
| Claude → Codex and Codex → Claude work with source closed and no source API call | `FAIL` | A1–A2 dest-ack missing |
| Every fidelity class has real report evidence and byte-stable goldens | `PASS` | B6 compaction fixture; goldens in `make verify` |
| Injection, secret, and bounded-read security gates leak nothing | `PASS` | D1–D4 counts; `make verify` adversarial tests |
| 200-turn projection is bounded and reported | `PASS` | B2 + G1 |
| Windows ↔ macOS uses canonical project IDs without source absolute paths | `PASS` | C5 |
| Dry-run and executed structured-plan output are byte-identical | `PASS` | B1 dry vs no-launch after acks (real launch route not compared; A1 failed dest-ack) |
| Required Phase 4 docs, product-truth register, runbook, and dispatch are present | `PASS` | tagged tree: dispatch, template, `docs/handoff.md` |
| Exit codes remain 0/1/2/3/5/6/7; no vendor-internal writes or capsule sync | `PASS` | observed 0/1/2/3/5/6/7 only; D7–D8 |

## 8. Findings and repository hygiene

### Release-blocking

1. Flagship A1/A2/A3/A5/A6/A7 did not collect a destination first-reply acknowledgement on a real TTY (throwaway destination not logged in). Empty-dest **Plan** succeeds (RC3). Missing dest-ack is FAIL, not NOT TESTED.
2. R1 off-PATH source: moving `claude` off PATH still exits **5** `environment preflight is blocked` instead of layout-only `SUPPORTED` for a recognized Claude store.
3. `make verify` fails `govulncheck` on five Go 1.25.12 standard-library advisories already fixed in 1.25.13.

### Non-blocking

- Host Claude is `2.1.228`, not `2.1.229`. Range includes 2.1.229; determined `2.1.230` fail-closed (C8/R6).
- Human C8/R4 stderr is `handoff: environment preflight is blocked` without the token `UNTESTED`; JSON `code=compatibility` and exit 5 with no lineage residue.
- Non-git cwd still exit 5 preflight (RC2 extra clause); wrong-repo and leaf-match C4 conditions passed.
- OpenCode skipped (no isolation override).
- Live bootstrap PATH-update skipped via `REINSTATE_SKIP_PATH_UPDATE=1`.

### Test-harness deviations and supersessions

- Isolated Claude/Codex homes were not logged in; dest-ack could not complete. `script` + warning acks reached dest spawn (timeout ~22s) rather than F8.
- A1/A3 Plan rows were recollected after F7 rewrote a Claude session id (first collection was `session not found`).
- E8 first collection used `--json` and hit F3 exit 2; targeted recheck without `--json` superseded that FAIL with exit 5 source-only.
- Capsule/projection operator-home-name count 0; bootstrap contained the throwaway `REINSTATE_HOME` store path (harness location, not source-device workspace).
- Cross-build used `GOTOOLCHAIN=go1.25.12`; goreleaser snapshot not re-run.
- No rc.1–rc.6 corpus reused.

## RC1 regression re-verification (R1–R6)

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| R1 | `FAIL` | Throwaway Claude dir has no `version` file. In-range dry-run exit 0, not UNTESTED. `doctor` reports Claude `SUPPORTED` on layout. Off-PATH source `--dry-run` exit **5** `environment preflight is blocked`. |
| R2 | `PASS` | Absolute-paths fixture `--no-launch` exit 0; `${REPO}` tokens=27 and `${EXTERNAL:}` tokens=8 across capsule/projection/bootstrap; capsule+projection operator-home-name count 0; no absolute-path abort. |
| R3 | `PASS` | Dirty tree `changed_files` nonempty (count=4); not `(none)`. |
| R4 | `PASS` | Hanging `--version` shim classified Compatibility exit **5** in 2255–2275 ms (inside 25s); not Runtime exit 1. |
| R5 | `PASS` | Slash fixture first user `/init`; `--no-launch --json` exit 0; `/init` reached stdout; no absolute-path abort. |
| R6 | `PASS` | Installed Claude `2.1.228` handoff exit 0 (SUPPORTED). Fake `2.1.230` dry-run exit 5, no stored id. Host did not auto-update (before=after `2.1.228` / `0.147.0`). |

## RC2 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| C4 | `PASS` | Wrong-repo / different-leaf cwd exit 5 `different repository`. Leaf-match remap exit 0. |
| F8 | `PASS` | Non-TTY fail-closed exit 7 in 9.4 ms before dest spawn. |
| E5/E6 busy-check | `PASS` | No exit 1 `unsupported agent "grok"`. Installed-vendor E5/E6 themselves `PASS`. |
| R4 classification | `PASS` | Hanging probe is Compatibility exit 5, not Runtime. |
| D9 | `PASS` | `--no-redact` known; Grok refused exit 2. |
| List isolation | `PASS` | Isolated `rein list` hid decoy HOME session; test-owned sessions only. |
| B3 | `PASS` | Checkpoint `projection_events=0`. |
| B6 | `PASS` | All five fidelity classes on compaction fixture. |
| C6/C7 | `PASS` | Missing MCP/skill warning IDs. |

## RC3 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| Flagship A empty dest | `PASS` | Empty dest `CODEX_HOME`; dest Plan `--dry-run --json` exit 0; dest not `UNTESTED`. Dest-ack still missing (harness; see A1). |
| R4 hang | `PASS` | See R4; ~2.3s; exit 5 compatibility. |
| Windows `make verify` | `N/A` | Owned by the Windows device report. macOS unit/race PASS; `govulncheck` FAIL. |
| G3 `--no-launch` list | `PASS` | created=100; `handoff list --limit 100 --json` n=100; unacked launch still exit 7. |
| B6 summarized | `PASS` | `summarized` present in `fidelity.json`. |

## RC5 / RC6 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| R4 hang process-tree | `PASS` | Grandchild `sleep` shim returned in ~2.3s as Compatibility UNTESTED/exit 5. |
| F8 refuse-before-Plan | `PASS` | Non-TTY exit 7 in 9.4 ms; `--allow-warning` did not skip. |
| B6 summarized | `PASS` | Tagged compaction fixture. |
| C5 foreign-OS remap | `PASS` | Windows os-roots from macOS leaf-match `demo`; `${REPO:` tokens; no `C:\`. |
| E5/E6 fixture-user cwd | `PASS` | Leaf-matching `demo` Grok dry-run exit 0 (not blocked on recorded fixture cwd). |
| C4 leaf-match remap | `PASS` | Different-leaf refuse; matching leaf allowed. |
| Dest-ack | `FAIL` | See A1/A2/A5/A6. |

## RC7 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| Busy-check listing error | `PASS` | PATH `ps` stub exit 1; `--dry-run --json` still reached Plan exit 0 in 833 ms; no `session busy check` skip. |
| C8/R6 2.1.230 fail-closed | `PASS` | Determined `2.1.230` read-only handoff exit 5 / compatibility; no stored handoff id. |
| R4 hung `--version` Compatibility | `PASS` | Exit 5, not Runtime 1; ~2.3s. |
| E5/E6 Grok source-only layout | `PASS` | Leaf-matching `demo` not blocked on `agent.layout`. |
| Dest-ack | `FAIL` | Required A1/A2/A5/A6 dest-ack missing. |

## 9. Required terminated device block

Required counts must sum to 44 and optional counts to 8. This block occurs
exactly once and remains the last report content until reconciliation.

```text
PHASE4-DEVICE-REPORT-V1
device=macos-arm64
test_tag=v0.4.0-rc.7
test_commit=a82a0ebe79a75f433bcfd28a26d6fd23976f3f71
installed_binary_sha256=dad8195ab78ab7968a20dc85a93df58e5c7016f9a6f7fc942a118588cbbacb20
required_pass=38
required_partial=0
required_fail=6
required_not_tested=0
optional_pass=4
optional_fail=0
optional_not_tested=4
artifact_chain=PASS
isolation_privacy=PASS
flagship_directions=FAIL
fidelity_policy=PASS
workspace_capability=PASS
security=PASS
cli_contract=PASS
performance=PASS
phase1_phase2_phase3_regression=PASS
release_blocking_findings=3
product_files_changed=0
secrets_transcripts_or_capsules_committed=false
END-PHASE4-DEVICE-REPORT-V1
```
