# `v0.6.0-rc.2` tagged-artifact native Windows acceptance

`PHASE5-DEVICE-REPORT-V1`

This is the assembled device report for the **published, signed GitHub
prerelease `v0.6.0-rc.2`**, commit `81d74a82ba2a0e27f9f1a68eb0270d224a20da6a`
(release workflow run `34062478935`). It satisfies
[`../phase-5-universal-agent-coverage-acceptance.md`](../phase-5-universal-agent-coverage-acceptance.md)
as specialized by [`../v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md)
and [`../v0.6.0-rc.2-agent-verification-prompts.md`](../v0.6.0-rc.2-agent-verification-prompts.md),
under [ADR 0005](../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md)'s
Windows-first waiver. It merges five executors' part files
(`2026-09-07-windows-v060rc2-part-{a,b,c,d,e}.md`, all against this same tag)
into the one cumulative report the dispatch requires, plus an assembler's own
independent install and matrix-generation check (§0). The five part files are
removed from the tree in the same commit as this file; their content is
preserved here.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Matrix generated by:** `rein doctor --agents --acceptance-matrix --json`
  (re-run independently by the assembler against a fresh install; see §0)
- **Row accounting:** `216` rows total (`178` Phase 5 generated matrix +
  `22` CLI experience + `16` Hop parity journeys) — `1` row is
  `N/A (definitional)` (`opencode:D4`) — **`215` required rows**.
- **Host-credential rows:** `4` (`qwen:E1`, `qwen:E2`, `qwen:E3`, `qwen:E5`),
  recorded `NOT TESTED (host credential)`; only `qwen:E5` satisfies the
  disposition rule's second condition this run (see §12) and is excluded
  from blocking the verdict. `qwen:E1`/`E2`/`E3` do **not** satisfy it this
  run and remain blocking.
- **Required counts:** `204 PASS / 6 PARTIAL / 0 FAIL / 5 NOT TESTED` of
  `215` required rows.
- **Release-blocking findings:** `10` required rows are not `PASS` and are
  not excused by either disposition in
  [`v0.6.0-windows-acceptance.md`](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict):
  `cline:C3`, `cursor:C3`, `pi:C3`, `grok:E1`, `grok:E2`, `grok:E3`,
  `qwen:E1`, `qwen:E2`, `qwen:E3`, `MatrixH:H7`. See §16.

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required
result is `FAIL`. Because 10 of the 215 required rows are not `PASS` and are
not covered by an applicable disposition, the device verdict is `FAIL` and
this report does not authorize stable `v0.6.0`. No product code was changed
by this assembly; every result below is transcribed from the five part
files' own evidence, corrected only where the assembler's own row-by-row
recount found a part's self-reported summary count did not match its own row
table (§0.6), or where a subsequent independent verification pass found a
specific evidentiary detail in this report did not match its own source
part file (§0.9 — the grok `E1`/`E2` attempt count). Neither correction
changes any row's disposition or the required-row counts below.

### Failing rows (10, none pass a required row)

| Row | Result | Category | Reason (full evidence in the matrix sections below) |
| --- | ------ | -------- | ---- |
| `cline:C3` | PARTIAL | host/harness | Could not create a fresh throwaway session via the vendor's own CLI — `cline --data-dir <isolated>` returns `Unauthorized` with no credential, and copying the needed credential file out of the real `~/.cline` store was blocked by this session's own tool-use policy. Mechanism (search-by-message-body) confirmed via a synthetic fixture. |
| `cursor:C3` | PARTIAL | host/harness | `cursor-agent` (2026.08.11) is broken on this host (`Error: Cannot find module 'tree-sitter'`), so no fresh real Cursor CLI session could be created. Mechanism confirmed via an isolated synthetic store. |
| `pi:C3` | PARTIAL | host/harness | The host's 3 real `pi` sessions all carry an empty bounded `prompt_preview`; creating a fresh one needs a configured model/API key not available this run. |
| `grok:E1` | PARTIAL | host/harness (new this run) | Real launch, correct argv, correct session id, confirmed active — but no completed assistant reply across three attempts (corrected post-commit from "4 attempts" — see §0.9), apparently blocked by this host's shared, global MCP-server connect/disconnect context noise repeating on every resumed `--resume` turn. |
| `grok:E2` | PARTIAL | host/harness (new this run) | Same root cause as `grok:E1` — session-identity mechanism proven, no completed reply to verify content against. |
| `grok:E3` | NOT TESTED | host/harness (new this run) | Fork plan argv confirmed correct; no completed live fork turn, same MCP-reconnect stall. |
| `qwen:E1` | NOT TESTED (host credential) | host credential — disposition condition 2 not met this run | Expired `BAILIAN_CODING_PLAN_API_KEY`, same 401 as `v0.6.0-rc.1`. The disposition that would excuse this requires the same row `PASS` for another optional T4 agent (`grok`); `grok:E1` is `PARTIAL` this run, not `PASS`, so the condition fails and this row blocks. |
| `qwen:E2` | NOT TESTED (host credential) | host credential — disposition condition 2 not met | Same expired credential; `grok:E2` is `PARTIAL` this run, so the same condition fails. |
| `qwen:E3` | NOT TESTED (host credential) | host credential — disposition condition 2 not met | Same expired credential; `grok:E3` is `NOT TESTED` this run, so the same condition fails. |
| `MatrixH:H7` | PARTIAL | host/operator availability (new this run) | Elevated `Start-Process -Verb RunAs` was declined twice ("The operation was canceled by the user.") with no interactive operator available to accept the UAC prompt; non-elevated `schtasks /Create` independently confirmed elevation is genuinely required on this host. The identical mechanism passed in full on this same host at `v0.6.0-rc.1`; this is availability of an acceptor, not a regression, but it does not satisfy either disposition and blocks the verdict as recorded. |

`qwen:E5` is also `NOT TESTED (host credential)` but **does** satisfy the
disposition rule (`grok:E5` is `PASS` this run) and does not block the
verdict — see §12.

## 0. Assembler verification

Performed independently of, and in addition to, all five part files, in the
assembler's own fresh directory
(`D:\ReinstateAcceptanceProjects\v060-rc2-assembler\`), never touching any
part executor's own lab directories or the shared worktree's tracked files
before this commit.

1. **Checksum.** `sha256sum` of the coordinator's staged
   `reinstate_0.6.0-rc.2_windows_amd64.zip` matched `checksums.txt`
   (`82ea243cf9aa1b411cc77322abf973afaf8ff8ac13fbbfa05ad32c5d82ecdc84`)
   independently.
2. **Install.** Unzipped into a fresh directory
   (`D:\ReinstateAcceptanceProjects\v060-rc2-assembler\install\`), never a
   shared or developer binary. `rein.exe`/`reinstate.exe` `sha256sum`: both
   `0ae03c4eed8c1af610f04842d5ed51efdd9847724a3b841d249b3841e087fce6`; `cmp`
   exit `0` (byte-identical).
3. **Version.** `rein version --json`:
   `{"commit":"81d74a82ba2a0e27f9f1a68eb0270d224a20da6a","date":"2026-09-06T21:57:00Z","name":"reinstate","version":"0.6.0-rc.2"}`
   — matches every part file's own artifact-identity table exactly.
4. **Required row count.** `rein doctor --agents --acceptance-matrix --json`
   against this fresh install: top-level `"row_count": 178`,
   `"core_row_count": 33` (`A:10, B:9, G:8, H:6`), 18 agents at tiers
   `T0×7, T1×4, T2×2, T4×2, T5×3` — matches the candidate's census in the
   dispatch exactly (grok/qwen at T4, gemini/kimi at T2, not promoted; no T3
   agent this candidate; unchanged from `v0.6.0-rc.1`).
5. **Row-set reconciliation.** Programmatically generated the full set of
   178 expected row identifiers from that JSON (33 core rows + each agent's
   own row list) and compared it against the union of every row id assigned
   to part A (core Matrix A/B/G/H, Matrix F for all 7 T0 agents, Matrix C for
   the 4 T1 agents — 71 Phase-5 rows) and part B (Matrix C/D/E for the 7
   T2+ agents — 107 Phase-5 rows): **the two sets are set-equal, 178
   elements each, zero overlap, zero gap.** Every required Phase 5 row
   appears in exactly one part file. Part A's own Section-A automated-gates
   table (8 rows) is a separate, non-Phase-5 gate set and is not counted in
   the 178, matching the dispatch's own accounting (`178 + 22 + 16 = 216`).
6. **Part-A count correction.** Recounting part A's own 24-row T1 Matrix-C
   table (`cline`/`copilot`/`cursor`/`pi` × C1–C6) cell-by-cell against its
   self-reported summary (`20 PASS, 4 PARTIAL`) found the summary did not
   match its own row table: the row table itself lists exactly **three**
   `PARTIAL` cells (`cline:C3`, `cursor:C3`, `pi:C3` — `copilot:C3` is a
   genuine `PASS` on real data), not four, and therefore **21** `PASS`
   cells in that table, not 20. This report's verdict, counts, and matrix
   sections use the row-by-row recount (`21 PASS, 3 PARTIAL` for the T1
   Matrix-C table; part A's overall assignment is therefore `76 PASS, 3
   PARTIAL` of its 79 rows, not the `75/4` its own footer states), per this
   assembly's "never invent a result" rule — the correction is a re-tally of
   the executor's own recorded rows, not a new judgment. No other part's
   self-reported summary disagreed with its own row table (parts B, C, D, E
   all reconciled exactly against their own row tables).
7. **Disposition-rule check (new this run).** Applied
   [`v0.6.0-windows-acceptance.md`'s "Dispositions that do not block the
   device verdict"](../v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict)
   rule mechanically to every `NOT TESTED (host credential)` row this run
   recorded (`qwen:E1/E2/E3/E5`): the rule requires **both** (1) the same row
   `PASS` for every required agent (`claude`/`codex`/`opencode` — true for
   all four rows) **and** (2) the same row `PASS` for at least one other
   optional agent at the same tier (T4: `grok`). Cross-checking against
   part B's own `grok` E-row results found `grok:E1`/`E2` are `PARTIAL` and
   `grok:E3` is `NOT TESTED` this run (a new, unrelated MCP-reconnect
   finding — see §16), so condition (2) fails for `qwen:E1`/`E2`/`E3`: they
   do **not** qualify for the exemption and remain blocking. `grok:E5` is
   `PASS`, so `qwen:E5` does qualify and does not block. This is a
   mechanical application of the contract's own rule text to this run's
   actual row results, not a new judgment call — see §12 for the full table.
8. **Cleanup.** The assembler's own install and matrix output live under
   `D:\ReinstateAcceptanceProjects\v060-rc2-assembler\` and are deleted after
   this report is committed; nothing from that directory is referenced by
   path elsewhere in this report.
9. **Post-verification correction: grok `E1`/`E2` attempt count (added
   after an independent verifier's review of the commit below).** An
   independent verification pass over the originally committed version of
   this report (`51917a5b`) recovered part B's original text via
   `git show 51917a5b^:docs/testing/results/2026-09-07-windows-v060rc2-part-b.md`
   and found that part B's own grok `E1`/`E2` narrative states the resume
   round trip was tried "three separate attempts" (Matrix E narrative) and
   again "across three attempts" (part B's own Findings section) — the
   figure "4"/"four" does not appear anywhere in part B in connection with
   grok. The originally committed merged report instead read "across 4
   attempts" in four places: the Verdict block's "Failing rows" table
   (`grok:E1`), what is now §12's narrative, the `F-GROK-MCP-RECONNECT` row
   of §16's findings table, and a §21 harness-defects bullet — an
   unexplained, undisclosed alteration of a specific evidentiary detail
   from its source, unlike the disclosed correction in item 6 above. No
   fourth attempt happened and none is documented anywhere; this was a
   transcription error introduced during assembly. This revision corrects
   all four locations back to "three attempts", matching part B's own
   sourced text exactly. **This correction changes no row's disposition
   (`grok:E1`/`E2` remain `PARTIAL`, `grok:E3` remains `NOT TESTED`), no
   count in the Verdict block, and not the device verdict (`FAIL`)** — it
   is a wording-fidelity fix only. This device's own lab
   (`D:\ReinstateAcceptanceProjects\v060-rc2-fix\`) additionally confirmed
   real `grok`/`qwen`/`codex`/`claude`/`rein` binaries are present on this
   host for any future re-attempt of the live round trip, but this pass did
   not re-run the grok `--resume` round trip itself: the verifier did not
   dispute the `PARTIAL`/`NOT TESTED` dispositions or the underlying
   MCP-reconnect mechanism finding (its own hands-on re-testing pass
   independently reproduced host/harness gaps of the same general shape
   elsewhere in this report, e.g. the H4/H9 fakelocker `AnyBucket` step-4
   failure and its own agent.active false negative against a nested
   session), only this one evidentiary number, so re-running the row would
   not change what this correction needed to fix.
10. **Minor findings reviewed, no report change needed.** The same
    verification pass raised two further, minor, non-major points, both of
    which it itself characterized as inconclusive or likely out of scope
    rather than confirmed defects in this report's claims:
    - *CLI row 22 diff classes:* the verifier's own ad hoc comparison used
      real host qwen session data, not this report's actual row-22 method
      (a fixed synthetic `tuisandbox` fixture), and it said the extra
      `title`-derivation diff it saw "very likely" reflects that mismatch in
      method rather than a hole in the report's claim. No report change: the
      claim under row 22 is about the fixed fixture, which the verifier did
      not itself re-run.
    - *`agent.active` false negative on a nested/orchestrated session:* the
      verifier's own controlled test (a plain `claude --bg` session, the
      same process shape §12's E5/E6 evidence uses) correctly reported
      `status=present/actual=true` and a subsequent resume correctly exited
      `7` — confirming, not contradicting, this report's E5/E6 `PASS`
      claims. The false negative it separately saw was against its *own*
      live, currently-executing, subagent-shaped session, a process
      topology outside the native single-process launch scenario this
      contract's E5/E6 rows test, and the verifier itself flagged it as
      "not independently confirmed as a real defect" and "outside the
      native single-process launch scenario". No report change: this
      report's own E5/E6 evidence was gathered against the same
      plain-process shape the verifier's own controlled test used, and both
      agree.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-09-06T22:38:03Z`–`2026-09-07T05:15:00Z` (span across all five parts; each part's own timestamp is in its section below); assembler verification `2026-09-06T23:10:53Z` |
| Device | `windows-amd64` |
| OS/version/build | Windows 11 Pro, `10.0.26200`, native `windows/amd64`, never WSL |
| CPU architecture/native process | `amd64`, native process (no emulation) |
| Filesystem | NTFS |
| Tested tag | `v0.6.0-rc.2` |
| Tested full commit | `81d74a82ba2a0e27f9f1a68eb0270d224a20da6a` |
| Installed binary SHA-256 | `0ae03c4eed8c1af610f04842d5ed51efdd9847724a3b841d249b3841e087fce6` |
| Installed version JSON | `{"commit":"81d74a82ba2a0e27f9f1a68eb0270d224a20da6a","date":"2026-09-06T21:57:00Z","name":"reinstate","version":"0.6.0-rc.2"}` |
| Catalog agent count | `18` |
| Catalog tier census | `T0: 7, T1: 4, T2: 2, T3: 0, T4: 2, T5: 3` (unchanged from `v0.6.0-rc.1`) |
| Generated required row count | `178` (`rein doctor --agents --acceptance-matrix --json`, independently reproduced by the assembler in §0 and by part A) |
| Git version | `2.52.0.windows.1` |
| Go version/toolchain | `go1.25.13` (pinned via `GOTOOLCHAIN`; host default `go1.26.1`) |
| Report branch | `v060/rc2-tagged` |
| Device-report commit | filled in by the commit that lands this file |
| Draft report PR | NOT CREATED (worktree report, per this run's dispatch — no PR required) |

### Bootstrap deviation (applies to every part below except executor A)

Per this run's ground rules, only executor A installs from the live
`https://reinstate.dev/install.ps1` bootstrap and records that as the
artifact identity; every other executor (B, C, D, E) and the assembler
installed from the coordinator's pre-verified, checksummed draft directory
(`…/scratchpad/rc2-draft/`) instead — the assigned methodology, not an
unplanned deviation. Executor A's own bootstrap check (§Header, part A):
the live bootstrap was downloaded fresh with `Invoke-WebRequest -OutFile`,
its pinned version line reads `$Version = "v0.6.0-rc.2"`, and installing
from it produced a binary byte-identical to (and reporting the same
version/commit/date as) the checksummed archive — **no deviation** between
the live bootstrap route and the release-commit candidate.

### Artifact identity (common to all parts)

| Field | Value |
| ----- | ----- |
| Tag | `v0.6.0-rc.2`, published GitHub prerelease, signed tag verified against `.github/allowed_signers` (coordinator, pre-run) |
| Full commit | `81d74a82ba2a0e27f9f1a68eb0270d224a20da6a` |
| Release workflow run | `34062478935` |
| Windows archive | `reinstate_0.6.0-rc.2_windows_amd64.zip` |
| Archive SHA-256 | `82ea243cf9aa1b411cc77322abf973afaf8ff8ac13fbbfa05ad32c5d82ecdc84` (matches `checksums.txt`; re-verified independently by every part and the assembler) |
| Installed binary SHA-256 | `0ae03c4eed8c1af610f04842d5ed51efdd9847724a3b841d249b3841e087fce6` — `rein.exe`/`reinstate.exe` byte-identical (`cmp` exit `0`) in every install |
| `rein version --json` | `{"commit":"81d74a82ba2a0e27f9f1a68eb0270d224a20da6a","date":"2026-09-06T21:57:00Z","name":"reinstate","version":"0.6.0-rc.2"}` |
| Previous-release comparison binary (rows 2, 22, G7) | `reinstate_0.5.1_windows_amd64.zip`, SHA-256 `b724ca3da4e124004063032d63240c244fb9a59279076f0e87441d327a826e8c` |

## 2. Agent inventory on this device

| Agent key | Installed | Vendor version | Sessions available | Distinct projects | Declared tier | Tier testable here |
| --------- | --------- | -------------- | ------------------- | ----------------- | -------------- | -------------------- |
| `claude` | YES | `2.1.263` | ≥2 real + fixtures | ≥2 | T5 | yes |
| `codex` | YES | `0.149.0` | ≥2 real + fixtures | ≥2 | T5 | yes |
| `opencode` | YES | `1.18.27` | ≥2 real + fixtures | ≥2 | T5 | yes |
| `grok` | YES | `1.0.5` | ≥2 real + fixtures | ≥2 | T4 | yes, resume/fork round trip blocked by host MCP noise (new finding, see §16) |
| `qwen` | YES | `0.21.12` | ≥4 real (mixed) | ≥3 | T4 | yes, credential-limited for E1/E2/E3/E5 (host credential, same as `v0.6.0-rc.1`) |
| `gemini` | YES (only `gemini-api-key` account, no key available) | `0.53.0` | fixture (schema-accurate, disclosed) | ≥2 (fixture) | T2 | yes, via committed-shape synthetic fixture |
| `kimi` | YES | `0.36.1` | ≥2 real | ≥2 | T2 | yes |
| `pi` | YES | (host) | 3 real | 2 | T1 | yes, C3 PARTIAL (no searchable content, no API key to create new) |
| `cursor` | YES | (host; `cursor-agent` CLI broken) | 2 real | 2 | T1 | yes, C3 PARTIAL (`cursor-agent` cannot launch on this host) |
| `copilot` | YES | `1.0.80` | 2 real | 2 | T1 | yes |
| `cline` | YES | (host) | 3 real | 2 | T1 | yes, C3 PARTIAL (no isolated credential available) |
| `aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode` | ABSENT | — | — | — | T0 | yes (refusal/no-capability rows only) |

## 3. Signed artifact and installer chain

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------- |
| Annotated tag signature verifies against `.github/allowed_signers` | PASS | Verified by the coordinator pre-run (dispatch preamble) |
| Tag peels to the recorded full commit on `origin/main` | PASS | `81d74a82ba2a0e27f9f1a68eb0270d224a20da6a` |
| Published release is non-draft, prerelease, and tied to the tag | PASS | Coordinator pre-run |
| Exact expected release asset set is present | PASS | `verify-release.ps1` (A6) |
| Checksums, GitHub API digests, and attestations match the tag and commit | PASS | Coordinator pre-verified (`checksums.txt` match, `gh attestation verify`); independently re-verified by every part and the assembler |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | PASS | `verify-release.ps1`/`test-install.ps1` (A6/A7) |
| Live bootstrap is byte-identical to the tested commit and pins only this candidate | PASS | Executor A: pinned version line `v0.6.0-rc.2`, resulting binary byte-identical to the checksummed archive — **no deviation** this candidate (unlike `v0.6.0-rc.1`, which had to fall back to release assets because the live bootstrap still pinned `v0.5.2-rc.1`) |
| Bootstrap installer digest matches the tagged canonical installer | PASS | Executor A |
| Fresh isolated install did not replace a user binary or persist PATH changes | PASS | Every part and the assembler installed into its own fresh directory; no user binary replaced (part A also flags a pre-existing unrelated install on this host's persistent PATH from an earlier session — worked around per-command, not touched) |
| `rein` and `reinstate` have identical verified bytes and report the candidate | PASS | `cmp` exit `0` in every install; SHA-256 `0ae03c4e…7fce6` throughout |
| Installed binary reports the literal full tested commit | PASS | `rein version --json` matches `81d74a82ba2a0e27f9f1a68eb0270d224a20da6a` in every install |

## 4. Automated gates (Section A of the dispatch; not part of the 178)

Source: part A, run once from the worktree per the ground rules.

| # | Gate | Result | Evidence |
| - | ---- | ------ | -------- |
| A1 | Format, vet, tidy | PASS | `gofmt -l .`, `go vet ./...`, `go mod tidy -diff` all empty, exit 0 |
| A2 | Unit suite | PASS | `CGO_ENABLED=0 go test ./... -count=1 -p 4`: every package `ok` |
| A3 | Race suite | PASS | `CGO_ENABLED=1 go test -race ./internal/... -count=1 -p 4`: every package `ok` |
| A4 | Lint and vuln | PASS | `make lint` → golangci-lint v2.11.4, 0 issues; `make vuln` → govulncheck v1.6.0, 0 vulnerabilities in code |
| A5 | Doc gate | PASS | `internal/doctest`: `ok`; `scripts/check-docs.ps1` exit 0 |
| A6 | Snapshot and artifacts | PASS | `scripts/verify-release.ps1 -DistDir rc2-draft` exit 0 |
| A7 | Installers | PASS | `scripts/test-install.ps1 -DistDir rc2-draft` exit 0 |
| A8 | Cross-OS build | PASS | `GOOS=darwin GOARCH=arm64` and `GOOS=linux GOARCH=amd64 go build ./...` both exit 0 |

**8/8 PASS.**

## 5. Matrix A — Catalog integrity (10/10 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| A1 | Descriptors validate at start; no duplicate or empty keys | PASS | 18 agent keys, unique, none empty |
| A2 | `doctor --agents` lists every agent including T0 | PASS | 18 agents, all 7 T0 present |
| A3 | Every T0 agent shows an enumerated reason | PASS | `layout_unverified` (aider, antigravity, minimax-code, roo), `server_backed` (amp, openhands), `desktop_only` (zcode) |
| A4 | Declared tier and capability constructors agree | PASS | Conformance suite passes for all shipped agents |
| A5 | Every declared evidence path exists in the tagged tree | PASS | `checkEvidence` conformance check passes |
| A6 | `sessions --agent` accepts exactly T1+ keys, else exit `2` | PASS | All 7 T0 keys + a bogus key → exit 2; all 11 T1–T5 keys → exit 0 |
| A7 | `resume` accepts exactly T3+ keys, else exit `5` with reason | PASS | T0/T1/T2 keys → exit 5, tier named; T3+ keys → exit 2 (session not found, tier gate passed) |
| A8 | Agent ordering is deterministic across runs | PASS | Two runs, identical 18-key order |
| A9 | Broken evidence path fails conformance | PASS | `TestBrokenEvidencePathFails`/`TestRunFailsBrokenEvidence` pass |
| A10 | No scan writes, renames, or locks under any agent root | PASS | Full recursive mtime+size snapshot of the real `%USERPROFILE%\.pi\agent` tree, identical before/after a full refresh |

## 6. Matrix B — Probe and redaction (9/9 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| B1 | Output conforms to `AGENT-PROBE-V1` | PASS | `"schema": "AGENT-PROBE-V1"` |
| B2 | No absolute path, home name, or username | PASS | Full-document scan: 0 hits |
| B3 | No JSON values from session files, keys only | PASS | `first_line_keys` are field names only |
| B4 | Names shape-normalized | PASS | Only `<uuid-v4>`, `<32-hex>`, `<slug>` etc. appear |
| B5 | No excluded subtree present | PASS | Full-document scan for credential/cache patterns: 0 hits |
| B6 | Absent agent reported without error | PASS | T0 agents: `resolved_root: null`, exit 0 |
| B7 | Empty root distinguishable from absent root | PASS | Kimi's `.kimi-code` (`exists:true, marker_present:true`) vs `.kimi` (`exists:true, marker_present:false`) observed live |
| B8 | Wrapper scripts match binary output | PASS | `.sh`/`.ps1` identical to direct invocation (harness `PATH`-mangling caught and corrected before it produced a false result) |
| B9 | Secret scanner passes over committed probes | PASS | Pattern scan over 26 files under `docs/testing/results/agent-probes/`: 0 hits, plus passing `internal/secretscan` suite |

## 7. Matrix G — No regression (8/8 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| G1 | Claude/Codex native resume and fork unchanged | PASS | Regression-suite evidence (`internal/agentcheck`, catalog descriptors); the fresh live journey itself is Matrix E, owned by part B |
| G2 | Both structured handoff directions complete | PASS | `internal/handoff/...` suite; fresh live journey is Matrix D, owned by part B |
| G3 | Gemini/Kimi remain sources and read-only | PASS | `resume`/`fork` on T2 keys: exit 5, tier-named reason |
| G4 | Push and pull carry Claude, Codex, and OpenCode | PASS | `rein __complete push --agent ""`/`pull --agent ""`: exactly `claude, codex, opencode` — matches the phase-5 doc's own note that OpenCode's T5 promotion widened this set |
| G5 | Cross-OS path remapping unchanged | PASS | `internal/pathmap/...` suite passes; physical Windows↔Windows proof is H11 |
| G6 | Exit-code semantics unchanged; no new codes | PASS | Only documented codes (`0,2,3,5,7`) observed across ~150 live invocations |
| G7 | A `v0.4.0`-lineage index upgrades without data loss | PASS | A real 100-session index built with `v0.5.1` reopened by `v0.6.0-rc.2`: 100/100, no loss, no migration prompt needed |
| G8 | Capsules remain excluded from sync | PASS | `TestRefuseHandoffsPush` passes; a push under `handoffs/.../capsule.json` is refused |

## 8. Matrix H — CLI contract and performance (6/6 PASS)

Source: part A.

| # | Assertion | Result | Evidence |
| - | --------- | ------ | -------- |
| H1 | CLI reference matches shipped flags | PASS | `internal/doctest` passes |
| H2 | `--json` shapes match documentation | PASS | Live `sessions`/`search`/`inspect`/`doctor --agents` conform |
| H3 | Full refresh within documented ceiling | PASS | `13.149s` cold, 11 agents (consistent with `v0.6.0-rc.1`'s `13.978s`) |
| H4 | No-change refresh materially faster | PASS | `3.095s` warm, ~76% faster |
| H5 | One slow or unreachable source does not block others | PASS | `TestSlowAgentDoesNotDiscardTheRun` passes |
| H6 | Completion lists correct agent keys per command (rc.2 retest) | PASS | `push --agent`/`pull --agent` → `claude, codex, opencode` (fixed this candidate; `sessions --agent`, `search --agent`, `handoff --to` already correct and re-confirmed to agree) |

## 9. Matrix F — Per T0 agent (14/14 PASS)

Source: part A. All 7 T0 agents (`aider`, `amp`, `antigravity`,
`minimax-code`, `openhands`, `roo`, `zcode`).

| Agent key | F1 listed with reason | F2 no capability offered or implied |
| --------- | --------------------- | ------------------------------------- |
| `aider` | PASS | PASS |
| `amp` | PASS | PASS |
| `antigravity` | PASS | PASS |
| `minimax-code` | PASS | PASS |
| `openhands` | PASS | PASS |
| `roo` | PASS | PASS |
| `zcode` | PASS | PASS |

## 10. Matrix C — Per T1+ agent (66 rows: 62 PASS, 4 PARTIAL)

Per the ground rules, T1 discovery rows ran read-only through `rein` alone
against the host's real root; corruption rows used a copy or synthetic
fixture, never live data.

### T1 agents (source: part A; `cline`/`cursor`/`pi` C3 corrected per §0.6)

| # | cline | copilot | cursor | pi |
| - | ----- | ------- | ------ | -- |
| C1 | PASS | PASS | PASS | PASS |
| C2 | PASS — real non-zero `message_count` (1,1,1) from `.messages.json` | PASS — real `message_count` (5, 26) | PASS — real host sessions show `0` (see product note below); mechanism confirmed fixed via a synthetic `store.db` (5-row `messages` table → `message_count:5`; `size_bytes` includes `store.db`, not only `meta.json`) | PASS — real non-zero `message_count` (2,2,2) |
| C3 (rc.2 retest: message-body search) | **PARTIAL** — `cline --data-dir <isolated>` needs a credential this run could not supply without reading the real `~/.cline` store, blocked by this session's own tool-use policy | PASS — real host session, searched by a word from its own bounded `prompt_preview`, exactly 1 match | **PARTIAL** — `cursor-agent` (2026.08.11) throws `Error: Cannot find module 'tree-sitter'` on this host, no fresh session could be created; mechanism confirmed via an isolated synthetic store (planted token in a `role='user'` row found; a `role='assistant'`-only token correctly not found) | **PARTIAL** — the 3 real sessions all carry an empty bounded `prompt_preview`; no configured model/API key this run to create a fresh one |
| C4 | PASS | PASS | PASS (via synthetic store) | PASS |
| C5 | PASS | PASS | PASS (live: resume/fork exit 5) | PASS |
| C6 | PASS — truncated copy: unaffected sessions still list, exit 0; empty/absent → `[]` | PASS — same shape | PASS — truncated synthetic `store.db`: degrades to `message_count:0`, no panic; empty/absent → `[]` | PASS — truncated real `.jsonl` mid-record: bounded, non-panicking, reads to last complete line; empty/absent → `[]` |

**24 rows: 21 PASS, 3 PARTIAL** (`cline:C3`, `cursor:C3`, `pi:C3` — could not
create the vendor-CLI-launched throwaway session the rc.2 retest calls for;
recorded `PARTIAL` per the contract's own explicit fallback. Each carries a
passing synthetic/mechanism-level check of the exact code path being
retested, so the underlying fix is not in doubt — only the "created by the
vendor's own CLI in a throwaway project" evidence shape is missing.
`copilot:C3` is a genuine `PASS` on real data. This corrects part A's own
self-reported `20 PASS, 4 PARTIAL` footer — see §0.6.)

**Product note (not a scored defect, flagged for a maintainer):** real
Cursor CLI sessions on this host still report `message_count: 0` via `rein
sessions --agent cursor --json`. The code path itself is confirmed fixed (a
synthetic `store.db` built to the recognized schema correctly reports a
non-zero count); whether the real Cursor CLI 2026.08.11 build's `store.db`
uses one of the three recognized table names could not be determined
without opening the real store, which this run correctly declined to do.

### T2+ agents (source: part B; 7 agents × 6 rows = 42 rows, all PASS)

Real sessions were created in throwaway git repositories with each vendor's
own headless flag and a planted per-agent, per-project token
(`RC2B-<AGENT>-P<N>-<hex>`); `claude` used the host's live, already-signed-in
`CLAUDE_CONFIG_DIR` per the ground rules; `codex`/`opencode`/`grok` used
isolated homes seeded with a copied real credential file; `qwen`'s
credential is expired on this host so its rows use pre-existing real
sessions plus one freshly planted user-turn (the CLI persists the prompt
locally before the 401); `gemini` uses a disclosed, schema-accurate
synthetic fixture (no usable API key on this host for the only
`gemini-api-key`-authenticated account).

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| C1 (≥2 sessions, ≥2 projects) | PASS | PASS | PASS | PASS | PASS | PASS (fixture) | PASS |
| C2 (metadata matches vendor) | PASS | PASS | PASS | PASS | PASS | PASS (fixture) | PASS |
| C3 (search by known string) | PASS | PASS | **PASS, now also by message body (rc.2 fix, closes #405)** | PASS | PASS | PASS | PASS |
| C4 (bounded inspect) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| C5 (resume gate correct for tier) | PASS | PASS | PASS | PASS | PASS | PASS (T2 refusal) | PASS (T2 refusal) |
| C6 (corrupt/empty/absent degrade cleanly) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |

`opencode:C3` evidence: a continuation turn planted a token in the message
body only, with the LLM-generated title left unrelated and unchanged —
`rein search` still matched, proving body-text indexing, not title-only
matching as at `v0.6.0-rc.1`.

## 11. Matrix D — Per T2+ agent (35 rows: 34 PASS, 1 N/A definitional)

Source: part B. D2/D3 (no invented content; unknowns referenced/omitted with
a machine-readable reason) are PASS for all 7 agents.

| Row | claude | codex | opencode | grok | qwen | gemini | kimi |
| --- | ------ | ----- | -------- | ---- | ---- | ------ | ---- |
| D1 (capsule + fidelity report) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D2 (no invented content) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D3 (unknowns referenced/omitted) | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| D4 (truncation boundary, offset+hash; rc.2 fixed rows) | **PASS** (new Windows-shaped fixture) | **PASS** (new Windows-shaped fixture) | N/A (definitional) | **PASS** (countable for the first time) | PASS | PASS | PASS |
| D5 (two runs byte-identical) | PASS | PASS | **PASS** (`PD-B1` fixed) | PASS | PASS | PASS | PASS |

- **`opencode:D4` — N/A (definitional), unchanged:** OpenCode's
  embedded-SQLite store has no JSONL-style truncation boundary to exercise.
  Excluded from the required row count per the dispatch.
- **D4 byte-exact cross-check (`claude`, `codex`, `grok`, `qwen`, `gemini`,
  `kimi`):** each fixture's `capsule.json` `raw_source.byte_offset`/
  `artifact_sha256` was independently recomputed from the fixture's own raw
  bytes and matched exactly, through the real `rein handoff --no-launch
  --json` route (see part B's own table for the six offset/hash pairs).
- **D5 (`opencode`), `PD-B1` disposition cleared:** three consecutive
  `--dry-run --json` runs over the same unchanged OpenCode source produced
  fully byte-identical documents, `handoff_id` and `destination.session_id`
  included.

## 12. Matrix E — Per T3 agent (30 rows: 23 PASS, 2 PARTIAL, 5 NOT TESTED)

Source: part B. Only `claude`, `codex`, `opencode`, `grok`, `qwen` are T3+;
`gemini`/`kimi` (T2) have no Matrix E rows.

| Row | claude | codex | opencode | grok | qwen |
| --- | ------ | ----- | -------- | ---- | ---- |
| E1 (resume launches vendor CLI, session continues) | PASS | PASS | PASS | **PARTIAL** | NOT TESTED (host credential) |
| E2 (resumed session is the requested one) | PASS | PASS | PASS | **PARTIAL** | NOT TESTED (host credential) |
| E3 (fork produces a distinct session) | PASS | PASS | PASS | **NOT TESTED** | NOT TESTED (host credential) |
| E4 (below-min/above-max both exit 5, naming the range) | PASS | PASS | PASS | PASS | PASS |
| E5 (active session detected, resume refused/warned) | PASS | PASS | PASS | PASS | NOT TESTED (host credential) |
| E6 (non-interactive exits 7) | PASS | PASS | PASS | PASS | PASS |

`E1`–`E3`/`E5` for `claude`/`codex`/`opencode` all used real vendor
processes and planted tokens (each recalled or forked correctly, verified
against the actual resumed/forked session's own output).

**`grok` — new finding this run, not carried from `v0.6.0-rc.1` (where
`grok` E1/E2/E3/E5 all `PASS`ed):** real launch, correct argv, correct
session id independently confirmed active (`E5`), but the headless
`--resume -p` path did not complete a full round trip across three
attempts (corrected post-commit from "4 attempts" — see §0.9), apparently blocked by this host's shared, host-global MCP server set
repeating connect/disconnect `<system-reminder>` context turns on every
resumed session before the model produces an assistant reply. `rein`'s own
launch plan, argv, session targeting, and active-session detection were all
independently confirmed correct for the same process — this looks like a
host/vendor-runtime issue, not a `rein` defect, but it does not pass the
row's own required mechanism (a completed answer/fork) and is scored
`PARTIAL`/`NOT TESTED` accordingly, per the gate rule.

**`qwen` — credential-blocked, same reason as `v0.6.0-rc.1`, but this run's
disposition outcome differs (see §0.7):** `qwen -p "reply with the word PONG
only"` returns `[API Error: 401 invalid access token or token expired]`,
identical to the `v0.6.0-rc.1` finding. The dispatch's disposition rule
would excuse `qwen:E1/E2/E3/E5` from blocking the verdict provided `grok`
(the other optional T4 agent) `PASS`es the same row. This run, `grok:E5`
does `PASS` (so `qwen:E5` is excused), but `grok:E1`/`E2`/`E3` do **not**
`PASS` (they are `PARTIAL`/`NOT TESTED` for the unrelated MCP-reconnect
reason above), so `qwen:E1`/`E2`/`E3` do **not** qualify for the exemption
this run and remain blocking required-row failures.

### Dispositions that do not block the device verdict

| Row | Disposition | Condition 1 (PASS for every required agent: claude/codex/opencode) | Condition 2 (PASS for another optional agent, same tier) | Blocks the verdict? |
| --- | ----------- | -------------------------------------------------------------------- | ------------------------------------------------------------ | -------------------- |
| `opencode:D4` | N/A (definitional) | n/a — excluded from the required count entirely, not a disposition on a required row | n/a | No — excluded from both the denominator and the numerator |
| `qwen:E1` | NOT TESTED (host credential) | met (claude/codex/opencode `E1` all `PASS`) | **not met** — `grok:E1` is `PARTIAL`, not `PASS`, this run | **Yes — blocks** |
| `qwen:E2` | NOT TESTED (host credential) | met | **not met** — `grok:E2` is `PARTIAL` this run | **Yes — blocks** |
| `qwen:E3` | NOT TESTED (host credential) | met | **not met** — `grok:E3` is `NOT TESTED` this run | **Yes — blocks** |
| `qwen:E5` | NOT TESTED (host credential) | met (claude/codex/opencode `E5` all `PASS`) | **met** — `grok:E5` is `PASS` this run | No — excused, counted toward the required denominator but not blocking |

This is the one place `v0.6.0-rc.2`'s own new finding (`grok`'s MCP-reconnect
stall on E1/E2/E3) interacts with an existing disposition: because `grok`
itself now fails to reach `PASS` on E1/E2/E3 this run, the qwen host-credential
carve-out that fully excused `qwen:E1/E2/E3/E5` at `v0.6.0-rc.1` (where
`grok` passed all of them) only partially applies here.

## 13. CLI experience (22 rows)

Source: part C, executed through the real Windows pseudo console
(`scripts/testing/conptydriver`) driving `scripts/tuisandbox`'s synthetic
bench. **22/22 PASS.** Nothing in this candidate's changelog touches the
interactive surfaces; this re-tests every row that passed 22/22 at
`v0.6.0-rc.1`.

| # | Row | Result | Evidence |
| - | --- | ------ | -------- |
| 1 | Bare `rein` on a capable terminal draws the switcher | PASS | Header `rein 9 sessions · 3 agents · all projects`, filter prompt, time-grouped rows, readiness glyphs, split preview, key bar |
| 2 | Bare `rein` on a non-TTY exits `2`, byte-identical stderr to `v0.5.1` | PASS | Both exit `2`; raw stderr SHA-256 identical (`0476fb68...4795b`, matches `v0.6.0-rc.1`'s own hash); stdout differs only by legitimately added top-level commands/`--plain` flag |
| 3 | `--plain` falls back to the numbered switcher | PASS | Frozen numbered-picker text |
| 4 | `REINSTATE_NO_TUI=1` does the same | PASS | Byte-identical to row 3 |
| 5 | `TERM=dumb` does the same | PASS | Byte-identical to row 3 |
| 6 | `TERM` unset draws the switcher on Windows | PASS | Full interactive switcher, identical shape to row 1 |
| 7 | A terminal below 40x10 falls back to plain | PASS | `-cols 30 -rows 8`: wrapped frozen numbered-picker text |
| 8 | `NO_COLOR` draws the switcher with no color escape sequences | PASS | Exactly one bare `ESC[m` reset, zero SGR color params, full render |
| 9 | Typing filters; header count follows | PASS | `"auth"` → `1 session · 1 agent`, matched to ground truth; visual list-narrow obscured by a harness VT gap (F1, carried) |
| 10 | `f` in list mode filters, does not fork | PASS | Filter prompt `❯ f`; vendor process count unchanged (5/5) |
| 11 | `tab` opens the action menu; `esc` reverts without acting | PASS | Key bar changes/reverts correctly, no action taken |
| 12 | `ctrl+k` opens the palette; a subsequence query finds its command | PASS | `"hof"` → top match `▸ Hand off to another…` |
| 13 | Readiness glyphs resolve; a blocked-without-probe row | PASS | Ground truth matched via `rein resume --dry-run --json`, needed a reduced viewport (F2, carried); `grok`/`payment-adapter` is WARN not BLOCKED — dated tier-promotion drift (F3, carried), not a regression |
| 14 | Warning checklist acknowledges with spacebar, shows equivalent command | PASS | `key space` toggles `[ ]`→`[x]`, live-updating equivalent command |
| 15 | Partial acknowledgement refused exit `7` | PASS | 2 of 4 warnings: exit `7`, names exactly the remaining two |
| 16 | Handoff studio measures each policy, equivalent command follows | PASS | `balanced`→`full`→`checkpoint`, equivalent command tracked every change |
| 17 | Studio refuses `enter` on an unplannable handoff | PASS | Foreign `repository_url` session settles to "cannot be planned"; `enter` does not send (same F2 timing sensitivity) |
| 18 | Wizard validates per field, allows going back | PASS | Empty/malformed endpoint refused with correct messages; raw shift+tab (`\x1b[Z`, F4 carried) preserves the typed value |
| 19 | Wizard collects no secret material | PASS | Source inspection confirms; `--help` lists no credential flag |
| 20 | `rein init --link`/`--paste` round trip | PASS | Device A prints a `REIN1-...` code with no keys/passphrase; device B's `--paste` pre-fills correctly |
| 21 | ASCII glyph fallback on legacy conhost | PASS | Full ASCII set, matches `internal/ui/theme.go`'s `asciiGlyphs` exactly |
| 22 | Every `--json` document byte-identical to `v0.5.1` | PASS | 10 documents on the identical tuisandbox home; every non-empty diff traces to the two documented accepted classes (Grok tier promotion; new `agent.active` check) |

**Non-blocking findings:** F1/F2/F3/F4 carried unchanged from `v0.6.0-rc.1`
(harness VT/ECH gap; host probe fan-out timing; dated Grok bench-legend
drift; no named shift+tab key in the driver). F5 (new, out of row 22's own
comparison scope): on a completely fresh host with no Claude Code directory
ever created, `v0.5.1`'s `rein list --json`/`diff --json` crash with an
unhandled raw OS error, while `v0.6.0-rc.2` handles it gracefully — no
`CHANGELOG` entry names this specifically; recommend the maintainer confirm
whether it is an intentional unlogged fix. Does not affect row 22's own
pass/fail determination.

## 14. Hop parity journeys (16 rows)

Source: part D (`H1`, `H1b`, `H1c`, `H2`, `H3`, `H4`, `H5`, `H9`, `H10`,
`H11`, `H12` — 11 rows, all `PASS`) and part E (`H6`, `H6b`, `H7`, `H8`,
`H8b` — 5 rows, 4 `PASS` + 1 `PARTIAL`), against a local, disposable `hopd` +
`scripts/testing/fakelocker` lab, never the hosted control plane and never a
developer's real agent trees except where the ground rules specifically
authorize the host's live Claude Code config (H5, isolated via a throwaway
project and a project-mapping filter). **15/16 PASS, 1 PARTIAL** — the
second time these rows run against a tagged artifact.

| # | Row | Result | Summary |
| - | --- | ------ | ------- |
| H1 | Email sign-in through the approver; token in OS keyring; `whoami` names the device | PASS | Real GET-then-POST via `hoplab approve`; token confirmed present in the Windows Credential Manager by a fresh, separate process |
| H1b | Refused sign-in tells the terminal why and stores nothing | PASS | `-refuse` GETs but never POSTs; `HOPD_LOGIN_SESSION_TTL=15s` fast expiry; documented refusal, exit 1; `REINSTATE_HOME` never created |
| H1c | Unreachable control plane prints the one-line message and documented exit code | PASS | `REINSTATE_HOP_URL` at an unused port; human and `--json` both match the documented message, exit 1, `details.kind=control_plane_unreachable`; nothing written |
| H2 | `init --hop` provisions the locker exactly once | PASS | `locker_provisioned|1` in `hopd.db`; second run refused client-side, exit 7, no second event |
| H3 | `account init` recovery code shown once; keyring format 5, generation 1, verifies | PASS | Recovery code via a real inheritable-FD pipe; `keyring.v1.json` fetched directly and run through `keyring.Parse`+`VerifyGenerations`: `SchemaVersion=5, CurrentGeneration=1`, signature verifies pinned and unpinned |
| H4 | `push --all` sends one Claude, one Codex, one OpenCode session; `first_push` fires once | PASS (step 4 fails for the pre-existing `fakelocker AnyBucket` harness reason, unchanged) | Decrypted manifest: `3 session(s) (claude 1, codex 1, opencode 1)`; `first_push|1` exactly once; no-op push reports `skipped:3` |
| H5 | Wipe/new-device sign-in/`account recover`/`pull --all`/`resume --dry-run` complete for all three; one real resume | PASS | All three reach `confirmation_required`; a genuine live-config Claude Code session with a planted token was pushed, pulled into an isolated home, and `claude --resume --print` against the still-intact live config correctly recalled the exact token |
| H9 | `sync verify` names only observed objects | PASS (step 4 fails for the same harness reason as H4) | `checked_objects` names exactly the index and the newest snapshot; step 4 status is `"fail"`, never `"could not run"` |
| H10 | `sync migrate --to byo`, `--switch`, `--forget-hop` | PASS | 4 snapshots migrated; `--switch` flipped local config; `--forget-hop` dropped the token locally with no `device_revoked` event; new-bucket `sync verify` correctly reports step 4 `NOT APPLICABLE` |
| H11 | Path remap between two homes' project mappings | PASS (macOS leg E-deferred) | Device-b pushed under a shared project id mapped to its own fixture cwd; device-c, mapping the same id to a different real local path, pulled with `cwd` rewritten to its own path; `resume --dry-run` then reports `workspace.available=present` |
| H12 | Keyless diagnostics refuse a forged, rolled-back, or re-keyed keyring with nothing written | PASS | A pre-revoke rolled-back object, a byte-tampered forged signature, and a different account's genuine re-keyed object each refused by `account status`/`devices` and by `push` (exit 7); the device's own `account.json` anchor byte-identical after all three attempts |
| H6 | Device B `account join`; device A `devices approve`; B pulls | PASS | Live pairing via `hoplab pair join`; `pairing_requests` row confirmed `version=2, status=consumed`; device B pulled all 3 of device A's sessions; `sessions --json` on B showed 6 sessions under two non-overlapping workspaces |
| H6b | An expired pairing request is refused/rolled back; B's wrap absent from every generation | PASS | Real expiry forced with `HOPD_PAIRING_TTL=8s`; both `account join`/`devices approve` refused client-side; `pairing_requests` row confirmed `status=expired, payload IS NULL`; `account status` on B showed `enrolled_devices=1, device_in_keyring=false` |
| H7 | `daemon install/status/stop/start/uninstall` round trip through Task Scheduler | **PARTIAL** | UAC elevation for `Start-Process -Verb RunAs` was declined twice ("The operation was canceled by the user."); non-elevated `schtasks /Create` independently confirmed elevation is genuinely required on this host (not a harness misconfiguration). No install/status/stop/start/uninstall round trip or debounce/scheduled-pull mechanism exercised this run. This exact mechanism `PASS`ed in full on this host at `v0.6.0-rc.1`; recorded `PARTIAL` per the dispatch's explicit decline/timeout fallback, not a carried disposition (new outcome this run) |
| H8 | A revokes B: generation rolls, B's token refused; lagging-device attack; 404-floor proxy | PASS | `internal/cli/keygeneration_crossplane_test.go -tags hopacceptance` against a real `hopd`: both `TestKeyGenerationFloorAgainstRealHopd` and `TestKeyGenerationFloorWithoutTheRouteAgainstRealHopd` passed, 404-floor residual reproduced. Live: device A revoked device B, generation rolled 1→2; B's `whoami`/`push --all` both refused, exit 4 |
| H8b | A Console-initiated revocation stays pending until the recovery-code command confirms it | PASS | A throwaway `cmd/consoleprobe` drove the real Console HTTP routes over a local TLS-terminating reverse proxy; `device_revocation_requests` row confirmed `status=pending`; device B fully functional while pending; `rein devices revoke` on device A printed "Console request … confirmed after the keyring reached generation 2"; row updated to `status=confirmed, confirmed_generation=2`; device B's next push refused, exit 4 |

## 15. T5 encrypted sync round trip (beyond the 178; source: part B)

A disposable `scripts/testing/fakelocker` instance served as the
S3-compatible locker; two isolated `REINSTATE_HOME`s were paired against it;
passphrase delivery used a real inherited-handle helper driving
`REINSTATE_PASSPHRASE_FD`.

| Row | Result | Evidence |
| --- | ------ | -------- |
| `sync:claude` | PASS | Pushed the real `claude-proj1` session from device A's live config; pulled into fresh device B; content restored, `message_count` matched |
| `sync:codex` | PASS | Pushed all 3 real codex sessions (`--all`); pulled `"pulled": 3` into fresh device B |
| `sync:opencode` | PASS | First pull into a never-run device B correctly refused `NOT_INSTALLED`; after running `opencode` once, retried and pulled `"pulled": 3` |

## 16. Findings (merged, release-blocking assessment)

| ID | Severity | Row(s) | Description | Release blocking |
| -- | -------- | ------ | ------------ | ----------------- |
| F-GROK-MCP-RECONNECT | host/harness, new this run | `grok:E1`, `grok:E2`, `grok:E3` | This host's shared, host-global MCP server set (not isolated by `GROK_HOME`) repeats connect/disconnect `<system-reminder>` context turns on every `--resume` turn, blocking a completed assistant reply/fork across three attempts (corrected post-commit from "4 attempts" — see §0.9). `rein`'s own launch plan, argv, session targeting, and active-session detection were all independently confirmed correct for the same process. Not carried from `v0.6.0-rc.1` (where grok E1/E2/E3/E5 all passed) — a new host-environment finding. | Required-row not `PASS` (`grok:E1`/`E2` `PARTIAL`, `grok:E3` `NOT TESTED`); does not match either disposition in the contract, so it blocks per the gate rule |
| — | host credential, unchanged from `v0.6.0-rc.1` | `qwen:E1`, `qwen:E2`, `qwen:E3` | Expired `BAILIAN_CODING_PLAN_API_KEY`, same 401 as `v0.6.0-rc.1`; the host-credential disposition that would excuse these requires `grok` (the other T4 agent) to `PASS` the same row, which it does not this run (see F-GROK-MCP-RECONNECT) | Blocks — disposition condition 2 not met this run (see §0.7, §12) |
| — | host/operator availability, new this run | `MatrixH:H7` | UAC elevation declined twice with no interactive operator available; non-elevated `schtasks /Create` independently confirms elevation is genuinely required (not a harness misconfiguration). Identical mechanism `PASS`ed in full on this host at `v0.6.0-rc.1`. | Blocks — does not match either disposition |
| — | host/harness, unchanged from prior runs | `cline:C3`, `cursor:C3`, `pi:C3` | Could not create the specific "session created via the vendor's own CLI in a throwaway project" evidence the rc.2 dispatch calls for (credential-policy block, broken `cursor-agent` install, and no configured model/API key respectively); the underlying `search_text` body-match mechanism was confirmed for all three via a substitute method | Blocks — `PARTIAL` does not pass a required row and neither disposition applies |
| — | product, disclosed, needs maintainer confirmation | `cursor:C2` (real data) | Real Cursor CLI sessions on this host still report `message_count: 0`; the code path itself is confirmed fixed against a schema-accurate synthetic fixture. Whether the real Cursor CLI 2026.08.11 build's `store.db` uses a recognized table name could not be determined without opening the real store, which this run correctly declined to do | Not separately release-blocking (the row is scored `PASS` on the dispatch's own prescribed method); flagged for follow-up |

A tier claimed in the catalog that this device could not verify: none. No
tier reduction is proposed — every agent's declared tier was exercised at
least partially.

## 17. Tier reductions proposed by this device

| Agent | Claimed tier | Verified tier | Reason |
| ----- | ------------ | -------------- | ------ |
| — | — | — | None proposed. Every declared tier (T0–T5, no T3 this candidate) was exercised as designed; the `grok`/`qwen` E-row gaps this run are host/harness/credential-scoped, not evidence the product misclassifies either agent's own capability tier (grok's T4 resume/fork capability itself was verified live for `claude`/`codex`/`opencode`-shaped sessions and for grok's own E4/E5/E6; only the completed round-trip reply on E1–E3 was blocked by host MCP noise) |

## 18. `v0.6.0-rc.1` dispositions carried: cleared or re-recorded

Per [the rc.2 dispatch's own table](../v0.6.0-rc.2-agent-verification-prompts.md#dispositions-carried-from-the-v060-rc1-tagged-run):

| Row | `v0.6.0-rc.1` tagged result | This candidate expected | This run | Status |
| --- | ---------------------------- | ------------------------- | -------- | ------ |
| `cursor:C6` | FAIL, `F-CURSOR-ROOTENV` (MAJOR) | Fixed — expect PASS | PASS | **CLEARED** |
| `cline:C2` | FAIL, `message_count` hard-coded 0 | Fixed — expect PASS | PASS | **CLEARED** |
| `cursor:C2` | FAIL, `message_count` hard-coded 0 | Fixed — expect PASS | PASS (via the dispatch's own prescribed synthetic-fixture method; real host data still shows 0 — see §16) | **CLEARED** (on the prescribed method; a product note is carried forward, not a row disposition) |
| `cline:C3` | FAIL, search excludes message body | Fixed — expect PASS (message-body match) | PARTIAL (mechanism confirmed fixed via synthetic fixture; could not create the required real vendor-CLI session — see §16) | **RE-RECORDED**, new reason (host/harness, not the old search-mechanism gap) |
| `cursor:C3` | FAIL, search excludes message body | Fixed — expect PASS (message-body match) | PARTIAL (same shape as `cline:C3`, `cursor-agent` broken on host) | **RE-RECORDED**, new reason |
| `MatrixH:H6` | FAIL, `F-COMPLETION-PUSHPULL` | Fixed — expect PASS | PASS | **CLEARED** |
| `opencode:D5` | FAIL, `PD-B1` non-determinism | Fixed — expect PASS | PASS (3 consecutive byte-identical runs) | **CLEARED** |
| `claude:D4` | PARTIAL, host/fixture (Windows workspace does not resolve; correct refusal) | Fixed — expect PASS via the new Windows-shaped fixture | PASS | **CLEARED** |
| `codex:D4` | PARTIAL, same reason | Fixed — expect PASS | PASS | **CLEARED** |
| `grok:D4` | NOT TESTED, fixture gap | Fixed — expect PASS, countable for the first time | PASS | **CLEARED** |
| `opencode:D4` | NOT TESTED, definitional | Not fixed, not fixable — expect N/A (definitional) | N/A (definitional), same reason | **RE-RECORDED**, unchanged (excluded from the required count) |
| `opencode:C3` | PASS, title match only | Fixed — expect PASS, now also matching message body | PASS, confirmed by message body via a token planted in a continuation turn whose title never changed | **CLEARED** |
| `qwen:E1` | PARTIAL, host (expired credential) | Host credential — expect NOT TESTED (host credential) if unrefreshed | NOT TESTED (host credential); this run's disposition-condition check finds it **blocking** (grok:E1 not PASS this run — see §12) | **RE-RECORDED**, same underlying reason, disposition outcome differs this run |
| `qwen:E2` | NOT TESTED, host | Host credential — expect NOT TESTED (host credential) | NOT TESTED (host credential); blocking (grok:E2 not PASS) | **RE-RECORDED**, same reason, disposition outcome differs |
| `qwen:E3` | NOT TESTED, host | Host credential — expect NOT TESTED (host credential) | NOT TESTED (host credential); blocking (grok:E3 not PASS) | **RE-RECORDED**, same reason, disposition outcome differs |
| `qwen:E5` | NOT TESTED, host (process too fast for polling) | Host credential — expect NOT TESTED (host credential) if unchanged | NOT TESTED (host credential); **not blocking** (grok:E5 PASS this run) | **RE-RECORDED**, same reason, disposition outcome holds |

**11 cleared, 5 re-recorded, 0 regressed to worse than their rc.1 shape**
except in the disposition sense noted above: `cline:C3`/`cursor:C3` moved
from a `FAIL` (search mechanism broken) to a `PARTIAL` (search mechanism
fixed and confirmed, but the specific real-vendor-CLI session evidence
could not be created this run) — an improvement in root cause even though
the row's pass/fail status is unchanged. `qwen:E1/E2/E3` keep the same
underlying host-credential reason as `v0.6.0-rc.1`, but this run's newly
discovered `grok` MCP-reconnect issue (§16) means the disposition that
excused them at rc.1 does not apply this run — this is a consequence of a
**new** finding (`grok`'s own regression-shaped host issue), not a
regression in `qwen` or in the disposition rule itself.

## 19. Deferred: Apple Silicon macOS

Copied verbatim from
[`../v0.6.0-rc.2-agent-verification-prompts.md`](../v0.6.0-rc.2-agent-verification-prompts.md)'s
own "Deferred: Apple Silicon macOS" section, under
[ADR 0005](../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md) D2.
Nothing in this device's evidence claims macOS. Every row below is run
against this same `v0.6.0-rc.2` tag (or the stable tag it becomes) once the
Apple Silicon macOS host returns from repair
([issue #403](https://github.com/HarjjotSinghh/reinstate/issues/403)). If
that later macOS run fails, the fix ships as `v0.6.1` — this record of this
candidate's Windows acceptance is never edited to hide it.

| Group | Rows | Contract | Result |
| ----- | ---- | -------- | ------ |
| Phase 5 generated matrix, macOS column | all rows section B lists | Phase 5 | `DEFERRED` |
| CLI experience, macOS column | rows 1–20 and 22 | `v0.5.2` | `DEFERRED` |
| Hop cross-device journeys | pairing macOS↔Windows; path remap Windows↔macOS both directions; daemon launchd round trip; first push on macOS | hosted #16 | `DEFERRED` |
| Range widening on macOS | Claude Code through `2.1.263`; OpenCode through `1.18.27` | ADR 0005 D3 | `DEFERRED` |
| Grok GD8 | tool-approval prompt (already collected on macOS; the Windows half is #368 and stays optional) | Grok T4 | `DEFERRED` |

No document, data file, or release note may state that `v0.6.0` was verified
on macOS until that run exists.

## 20. Release-blocking findings

Because 10 of the 215 required rows are not `PASS` and are not excused by
either disposition in the contract, this report's device verdict is `FAIL`
and stable `v0.6.0` is **not authorized** by this run:

1. `cline:C3` — could not create the vendor-CLI-launched session the rc.2
   retest calls for (host/harness; mechanism confirmed fixed via a
   substitute method)
2. `cursor:C3` — `cursor-agent` broken on this host (host/harness; mechanism
   confirmed fixed via a substitute method)
3. `pi:C3` — no searchable content on real sessions, no credential to
   create a fresh one (host/harness)
4. `grok:E1` — real launch/session-identity proven, no completed round-trip
   reply (host MCP-reconnect noise, new this run — **F-GROK-MCP-RECONNECT**)
5. `grok:E2` — same reason as `grok:E1`
6. `grok:E3` — fork plan argv proven correct, no completed live fork turn
   (same MCP-reconnect stall)
7. `qwen:E1` — host-credential-expired NOT TESTED; the disposition that
   would excuse it needs `grok:E1` to `PASS`, and it does not this run
8. `qwen:E2` — same reason as `qwen:E1`, keyed to `grok:E2`
9. `qwen:E3` — same reason as `qwen:E1`, keyed to `grok:E3`
10. `MatrixH:H7` — UAC elevation declined twice with no interactive
    operator available this run; the mechanism itself passed in full on
    this host at `v0.6.0-rc.1`

None of the ten is attributed by its reporting executor to a genuine `rein`
product defect in the mechanism under test — nine are host/harness/operator
availability issues (a broken vendor CLI install, an expired credential, a
shared host's MCP reconnect noise, a declined UAC prompt, or a session this
run's own tool-use policy would not create) and one (`cursor:C2` against
real data, §16) is a disclosed, non-blocking product question for a
maintainer. But per this run's own gate ("A row is PASS only if its
mechanism was exercised; PARTIAL and NOT TESTED do not pass a required row")
and the contract's narrow, mechanically-checked disposition rule (§0.7,
§12), none of the ten qualifies for an exemption, so the device verdict is
`FAIL`.

## 21. Harness defects (merged, non-blocking)

- **Host-shared MCP servers block a Grok Build `--resume` round trip.**
  This host's global MCP server configuration (not isolated by `GROK_HOME`)
  produces repeated connect/disconnect context-reminder turns ahead of a
  resumed session's assistant reply, blocking `grok:E1`/`E2`/`E3` from
  completing across three attempts (part B; corrected post-commit from "4
  attempts" — see §0.9). Independently, `rein`'s own launch
  plan and session targeting were confirmed correct for the same process.
- **`taskkill /F /IM go.exe` (self-reported, part B).** A broad image-name
  kill during this executor's own `fakelocker` teardown, rather than a
  specific PID. No evidence of disrupting another executor's concurrent
  `go test`/`make verify` run was found; not independently ruled out.
- **`cursor-agent` broken on this host (part A).** `Error: Cannot find
  module 'tree-sitter'` on invocation, blocking any fresh live Cursor CLI
  session. Host/vendor-install issue, not a `rein` defect.
- **`cline --data-dir <isolated>` requires vendor authentication (part A)**
  this run could not supply without reading a live credential file, which
  this session's own tool-use policy declined mid-task.
- **Pre-existing `rein`/`reinstate` install on this host's persistent PATH
  (part A)**, from an earlier session's default-directory bootstrap; every
  command in this run used an explicit binary path or a prepended PATH
  entry; one `PATH`-mangling mistake in a wrapper-script comparison (B8) was
  caught and corrected before it produced a false result.
- **`hoplab start -background` did not survive its launching shell exiting
  (part D).** Worked around by running the lab in foreground mode as a
  persistent background task instead. Specific to this execution
  environment, not `hoplab` or the product.
- **Shared host's global `opencode-ai` (bun) install broke mid-run twice
  (parts D, E)**, apparently from concurrent reinstall activity by another
  executor; repaired via the package's own `postinstall.mjs`, which briefly
  left the resolved version at `1.18.29`, past this candidate's verified
  ceiling (`1.18.21`–`1.18.27`); re-pinned to `1.18.27` before continuing.
- **`hoplab homes`'s fixture `cwd` (`C:\Users\fixture-user\code\<device>`)
  does not exist on this host (part D, matches `v0.6.0-rc.1`'s §23.2
  finding).** A device's project mapping must match the fixture's exact
  recorded `cwd` for push/pull discovery, unrelated to on-disk existence;
  corrected by matching mappings to the fixture's exact recorded `cwd` on
  the pushing side and to a real local path on the pulling/resuming side —
  exercising the product's own path-remap mechanism, not a defect.
- **UAC elevation declined twice with no interactive operator available
  (part E, `H7`).** The identical mechanism passed in full on this host at
  `v0.6.0-rc.1`; an availability issue for this run, not a regression.
- **`fakelocker`'s `AnyBucket:true` (carried, unchanged, parts D/E).** Does
  not emulate per-bucket credential scoping, so step 4 of `push --all`'s
  automatic post-push verification and `sync verify`'s isolation step fail
  against every Hop-mode locker in this lab — contrasted directly by `H10`'s
  correct `NOT APPLICABLE` on a genuine BYO destination and by `H8`'s
  real-`hopd` crossplane test exercising the identical product code
  correctly.
- **`scripts/testing/conptydriver`'s VT model has no ECH support (part C,
  carried as F1)**, and **has no named shift+tab key (part C, carried as
  F4)**; both worked around, neither a product defect.
- **This host's environment/handoff-plan probes fan out concurrently per
  visible switcher row and can time out under load (part C, carried as
  F2).** A reduced viewport or longer settle resolves it.
- **`tuisandbox`'s bench legend for `grok`/`payment-adapter` is stale (part
  C, carried as F3).** Grok's already-shipped tier promotion means it now
  runs a full preflight (WARN) rather than the bench's documented
  block-without-probe path; dated drift, not a regression.
- **`tuisandbox`'s `sandboxEnv` does not isolate `XDG_DATA_HOME` (part C).**
  Found and worked around (pointed at an isolated empty directory) before
  any row evidence was captured; no real session content was captured or
  committed.

## 22. Terminated device block

> Device testing is terminated for this candidate at the milestone recorded
> above. The results in this report are final for this device and this tag.
> Any further testing requires a new candidate tag and a new report.

- Terminating tester: assembler (tagged-run coordinator role, this session)
- UTC timestamp: 2026-09-06T23:20:00Z
- Device verdict: **FAIL** — 10 of 215 required rows are not `PASS` and are
  not excused by either disposition in the contract (`cline:C3`,
  `cursor:C3`, `pi:C3`, `grok:E1`, `grok:E2`, `grok:E3`, `qwen:E1`,
  `qwen:E2`, `qwen:E3`, `MatrixH:H7`); see the Verdict block and §20 above
  for the complete list and reasoning. This does not authorize stable
  `v0.6.0`; a fix pass (or, where the reporting executors' own findings are
  correct, a fix to this host's shared MCP configuration and the
  availability of an interactive UAC acceptor) and a new tagged candidate
  are required before the next acceptance run.
