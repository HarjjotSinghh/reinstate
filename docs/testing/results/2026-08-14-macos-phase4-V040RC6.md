# Phase 4 structured-handoff report — macOS arm64 / v0.4.0-rc.6

Copy of the Phase 4 template with this dispatch's substitution table applied.
No rc.1–rc.5 corpus, home, capsule, lineage, or report row was reused.

## Verdict

- **Device verdict:** `FAIL`
- **Milestone:** `MATRIX_COMPLETE`
- **Required counts:** `37 PASS / 0 PARTIAL / 7 FAIL / 0 NOT TESTED`
- **Optional source-only counts:** `2 PASS / 2 FAIL / 4 NOT TESTED`
- **Release-blocking findings:** `5`

`PARTIAL` and `NOT TESTED` do not pass a required row. A missing required result
is `FAIL`. Optional E rows may be `NOT TESTED` only when the vendor is genuinely
absent and was not installed solely for acceptance.

`stable_v0.4.0_authorized=false`. `current_stable=v0.3.0`. This report does not
authorize stable `v0.4.0`.

## 1. Immutable test record

| Field | Value |
| ----- | ----- |
| UTC date/time | `2026-08-14T17:20:00Z` start; matrix complete `2026-08-14T17:46:27Z` |
| Device | `macos-arm64` |
| OS/version/build | `macOS 26.5.2 (25F84)` |
| CPU architecture/native process | `arm64` native |
| Filesystem | `APFS` |
| Tested tag | `v0.4.0-rc.6` |
| Tested full commit | `00366cb53f4246bde50c7f6fe96f7c1e1660ec88` |
| Installed binary SHA-256 | `79ce08180e841e73041056be95316599682b67ecd874fbc7f62f982392c02953` |
| Installed version JSON | `version=0.4.0-rc.6 commit=00366cb53f4246bde50c7f6fe96f7c1e1660ec88 date=2026-08-14T17:09:29Z` |
| Claude Code version/state | before `2.1.228` (in range 2.1.219–2.1.229); after `2.1.228`; `DISABLE_AUTOUPDATER=1`; throwaway home not logged in |
| Codex CLI version/state | before `0.147.0` (in range 0.133.0–0.147.0); after `0.147.0` |
| Gemini/OpenCode/Grok state | Gemini `ABSENT`; OpenCode `ABSENT` (no isolation override; skipped; never claimed isolated); Grok `1.0.3` present |
| Git version | `git 2.55.0` |
| Go version/toolchain | host `go1.26.5`; gates `GOTOOLCHAIN=go1.25.12` |
| Report branch | `test/v0.4.0-rc.6-macos-arm64-report` |
| Device-report commit | `689c84cebb44f8035d5818492866720c52c55783` |
| Draft report PR | `PENDING` |

## 2. Signed artifact and installer chain

No downloaded executable ran until the live bootstrap pin and canonical installer digest passed.

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Annotated tag signature verifies against `.github/allowed_signers` | `PASS` | `git verify-tag v0.4.0-rc.6` good git SSH signature, ED25519 key SHA256:uXE5rh19MaNxQO/0pgR8n2K73xR+ck0O7fwf351W030 |
| Tag peels to the recorded full commit on `origin/main` | `PASS` | peeled `00366cb53f4246bde50c7f6fe96f7c1e1660ec88`; `merge-base --is-ancestor` vs `origin/main` |
| Published release is non-draft, prerelease, and tied to the tag | `PASS` | `gh`/API: draft=false prerelease=true tag=`v0.4.0-rc.6` target=main; 25 assets |
| Exact expected release asset set is present | `PASS` | 25 assets: checksums.txt, platform archives+SBOMs+raw binaries, eight Linux native packages, source archive |
| Checksums, GitHub API digests, and attestations match the tag and commit | `PASS` | darwin_arm64 archive sha256 `03df288241af6dd756a6e8390983fdfe713de3dc23a3522e7a41294db1bf93d4`; binary `79ce08180e841e73041056be95316599682b67ecd874fbc7f62f982392c02953`; Sigstore bundle attestation present |
| Archives have safe relative membership, correct platform identity, docs, and SBOMs | `PASS` | tar members `CHANGELOG.md LICENSE NOTICE README.md reinstate`; Mach-O arm64; installed bytes match archive binary |
| Live bootstrap is byte-identical to the tested commit and pins only `v0.4.0-rc.6` | `PASS` | live `https://reinstate.dev/install.sh` sha256 `4ce678e7e655863c0eefccc032878785edfdd517283260f55aa8f6decbf3d275`; unique pin `v0.4.0-rc.6`; no rc.1–rc.5 pin. `install.ps1` sha256 `25bfb4c493c3495b40b121137213df6c3e822d1b74e72264b497da4a83d56aa8` also pins only rc.6 |
| Bootstrap installer digest matches the tagged canonical installer | `PASS` | `PINNED_INSTALLER_SHA256=7776adb4ace8aa333745cd3f3e42b3a10d1400b9394c612d065c20a739db2e66` equals tagged `scripts/install.sh` |
| Fresh isolated install did not replace a user binary or persist PATH changes | `PASS` | dedicated `INSTALL_DIR` then only `reinstate` + `rein` symlink. Live bootstrap appended a PATH line; that line was reverted immediately. No user binary replaced |
| `rein` and `reinstate` have identical verified bytes and report `v0.4.0-rc.6` | `PASS` | `rein` is symlink to `reinstate`; sha256 `79ce08180e841e73041056be95316599682b67ecd874fbc7f62f982392c02953` |
| Installed binary reports the literal full tested commit | `PASS` | `reinstate 0.4.0-rc.6 (00366cb53f4246bde50c7f6fe96f7c1e1660ec88 2026-08-14T17:09:29Z)` |

Any failure here stops product-behavior testing. A source build is supplemental
and never substitutes for the installed tagged artifact.

## 3. Automated gates

| Gate | Result | Sanitized evidence |
| ---- | ------ | ------------------ |
| Clean tagged worktree and `go mod tidy -diff` | `PASS` | report worktree porcelain empty; `go mod tidy -diff` exit 0 |
| `make verify` with Go 1.25.12 | `FAIL` | `GOTOOLCHAIN=go1.25.12` `GOFLAGS=-p=1`: fmt/vet/lint 0 issues; `go test ./...` and race PASS including `internal/cli`. `govulncheck` exit 3: 5 stdlib vulns in `go1.25.12` (GO-2026-6218/6090/6088/5972/5026), fixed in `go1.25.13`. No product-module call-site vulns named beyond stdlib |
| Complete `CGO_ENABLED=1 go test -race ./... -count=1` | `PASS` | race subset inside `make verify` (`internal/cli` race 5.750s ok) |
| Required cross-build, fuzz-smoke, snapshot, artifact, and installer gates | `PASS` | five `CGO_ENABLED=0` builds darwin/amd64+arm64, linux/amd64+arm64, windows/amd64 exit 0; live installer already verified; unit tests include fuzz-smoke packages. goreleaser snapshot not re-run (tagged 25-asset release already proven) |
| Phase 1, Phase 2, and Phase 3 regression | `PASS` | `go test ./...` inside `make verify` exit 0 |
| Capsule/projection/CLI goldens are unchanged across repeated runs | `PASS` | unit goldens in `make verify`; installed `--dry-run` vs `--no-launch` stdout+stderr byte-identical after exact warning acks |
| Phase 4 adversarial security tests and `make fixture-scan` | `PASS` | `internal/fixture` ok in `make verify`; installed adversarial fixtures D1/D2/D4 |
| `TestLongHistoryParseCapsuleProjectionUnderCeiling`: 400 events, ≤98,304 bytes, <2,000 ms | `PASS` | 14.253 ms; projection 46,541 bytes; 200-turn / 400-event fixture |

## 4. Isolation and privacy

| Assertion | Result | Sanitized evidence |
| --------- | ------ | ------------------ |
| Fresh isolated `REINSTATE_HOME`, install directory, process homes, and repositories | `PASS` | five vendor overrides exported before product commands; throwaway unix HOME; dedicated install dir |
| Fresh controlled source/destination sessions; no older corpus or handoff reused | `PASS` | tagged fixtures copied into throwaway config dirs; no rc.1–rc.5 home reused |
| Operator vendor trees, credentials, keychains, `.env`, and token stores were never read | `PASS` | all five overrides set before first product command; list-isolation hid a throwaway-HOME decoy |
| No backend, passphrase, storage credential, or capsule sync was used | `PASS` | no `rein init`; `rein push` exit 3 |
| Vendor configuration changed only inside disposable test-owned homes | `PASS` | MCP/skill dummies only under throwaway Claude/Codex/unix homes |
| Reports contain no transcript, prompt, response, secret, private path, filename, diff, capsule body, or raw child error | `PASS` | this file |
| Source fingerprints were unchanged; vendor-store writes occurred only through an explicitly launched vendor CLI | `PASS` | source jsonl sha256 unchanged across dry-run/no-launch/G3; Claude print-mode probe wrote only under throwaway `CLAUDE_CONFIG_DIR` |

First index refresh: 11 test-owned sessions; decoy operator-HOME session hidden. OpenCode skipped (no override).

## 5. Required matrix — 44 rows

Use the exact IDs and pass conditions in
[`phase-4-cross-agent-handoff-acceptance.md`](../phase-4-cross-agent-handoff-acceptance.md).

### Matrix A — flagship quota-switch

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| A1 | `FAIL` | Tagged Windows os-roots Claude source `--to codex --dry-run --json` exit 0, mode `structured handoff`, `destination_session_mode=new`. Throwaway Claude print-mode probe exit 1. Real dest-ack on a TTY was not collected (throwaway home not logged in). |
| A2 | `FAIL` | No Codex-authored throwaway task session and no Claude destination acknowledgement. |
| A3 | `FAIL` | Throwaway Claude already logged out (A3 source condition). Dry-run from that source is possible; destination acknowledgement was not collected. |
| A4 | `PASS` | Tagged `partial-final-record` fixture; installed `--dry-run --json` exit 0; mode `structured handoff`; `destination_session_mode=new`. |
| A5 | `FAIL` | No destination first-reply restatement collected. Dirty-tree `changed_files` were present on dry-run plans (C1/R3) but were not restated by a live destination. |
| A6 | `FAIL` | No destination acknowledgement; MARKER non-repeat not observed. |
| A7 | `FAIL` | Stored no-launch lineage lists/inspects (mode `structured handoff`, inspect/export exit 0) but Claude destination ID was not launched so not resolved; Codex destination unresolved by absence of launch. |

### Matrix B — fidelity and policy

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| B1 | `PASS` | Unit `TestHandoffExecutedOutputMatchesDryRunByteForByte` in `make verify`; installed `--dry-run` vs `--no-launch` stdout **and** stderr byte-identical after exact `--allow-warning` IDs. |
| B2 | `PASS` | Tagged 200-turn/400-event fixture `--dry-run --json` exit 0; `projection_events=400`; `estimated_bytes=46783` (≤98,304). |
| B3 | `PASS` | `--policy checkpoint` `projection_events=0`. |
| B4 | `PASS` | `--policy balanced` exit 0; `estimated_bytes=46783`. |
| B5 | `PASS` | `--policy full` exit 0; `estimated_bytes=46783`. |
| B6 | `PASS` | Tagged compaction fixture `fidelity.json` classes: `exact`, `normalized`, `summarized`, `referenced`, `omitted`. |
| B7 | `PASS` | Tagged attachments fixture `--dry-run --json` exit 0. |
| B8 | `PASS` | Tagged unknown-records fixture `--dry-run --json` exit 0. |

### Matrix C — workspace and capability truth

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| C1 | `PASS` | Disposable dirty repo; plan `changed_files` count=4; staged+unstaged+untracked+MARKER. |
| C2 | `PASS` | Live Git reported dirty with `git.working_tree` warning; `dirty=true`. |
| C3 | `PASS` | Same-OS missing recorded workspace; `--dry-run --json` exit **5** (`environment preflight is blocked`). |
| C4 | `PASS` | Fixture-user/foreign workspace leaf `demo`: different-leaf git repo `--dry-run --json` exit **5** `different repository`. Leaf-matching disposable `demo` checkout exit 0. Same-repo subdirectory exit 0. |
| C5 | `PASS` | Windows os-roots fixture from the leaf-matching macOS checkout `--dry-run --json` exit 0; `${REPO:` tokens=10; no `C:\` in plan JSON. |
| C6 | `PASS` | Source MCP `browser` absent at Codex dest; warning `handoff.capability.mcp.browser`. |
| C7 | `PASS` | Source skill `review` under throwaway Claude skills dir; dest Codex lacked it; warning `handoff.capability.skill.review`; missing impact `degraded`. First collection missed the Claude skill root; targeted recheck on the same artifact superseded that FAIL. |
| C8 | `FAIL` | PATH shim `claude --version` = `2.1.230 (Claude Code)`; installed `--dry-run --json` exit **0** with warning `agent.version` and a stored handoff id (residue). Did not exit 5 / UNTESTED. |

### Matrix D — security

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| D1 | `PASS` | Prompt-injection fixture `--no-launch --json --show-redactions` exit 0; control flow unchanged (handoff completes). |
| D2 | `PASS` | Fence-breakout fixture exit 0. |
| D3 | `PASS` | No source-instruction control-flow takeover on D1/D2 artifacts. |
| D4 | `PASS` | Secret-leakage fixture exit 0; AKIA/sk-ant-/ghp_ counts 0 in stored artifacts. |
| D5 | `PASS` | Launch-free rows used throwaway homes only. |
| D6 | `PASS` | `REINSTATE_HOME` and `handoffs/` mode `0700`. |
| D7 | `PASS` | Isolated Claude/Codex/Gemini/Grok roots only. OpenCode skipped (no override). |
| D8 | `PASS` | No init; `rein push` exit 3; output did not mention `handoffs/`. |
| D9 | `PASS` | `--no-redact` is a known flag. Claude source `--no-redact --dry-run --json` exit 0. Grok `--no-redact` refused exit **2** (not unknown flag) with forced-redaction reason. |
| D10 | `PASS` | Source jsonl sha256 identical before/after dry-run, no-launch, and G3×100. |

### Matrix F — CLI contract

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| F1 | `PASS` | Human and JSON surfaces used `structured handoff` and `destination_session_mode=new`. No `same session` / `lossless` / `full context`. |
| F2 | `PASS` | `rein resume … --with codex --dry-run --json` exit 0; mode `structured handoff`. |
| F3 | `PASS` | `--json` without `--dry-run`/`--no-launch` exit **2**. |
| F4 | `PASS` | unknown / duplicate / wildcard / stale `--allow-warning` each exit **2**. Unacked **launch** (no `--json`) exit **7**. First launch attempt used `--json` and hit F3; targeted recheck superseded that FAIL. |
| F5 | `PASS` | `--no-launch --json` with exact warning acks exit 0; no vendor spawn; artifacts written. |
| F6 | `PASS` | `handoff inspect --json` and `handoff export --format json` exit 0 on a stored no-launch id. |
| F7 | `PASS` | Bare native id matching Claude and Codex fixtures exit **6** with both qualified options. |
| F8 | `PASS` | Non-TTY launch without `--json` exit **7** `interactive terminal`; wall 10.5–11.5 ms (before Plan/index/LookPath). `--allow-warning` did not skip. `REINSTATE_ALLOW_NON_TTY_LAUNCH` unset. |

### Matrix G — performance

| ID | Samples | Median | p95 | Maximum | Ceiling | Result | Sanitized evidence |
| -- | ------- | ------ | --- | ------- | ------- | ------ | ------------------ |
| G1 | 1 tagged-source unit run | 14.253 ms | 14.253 ms | 14.253 ms | `<2,000 ms`; projection `≤98,304 bytes` | `PASS` | 46,541 bytes; 400 events |
| G2 | 1 warmup + 5 installed `--dry-run --json` | 733.6 ms | 739.4 ms | 739.4 ms | macOS max `8s` | `PASS` | all exit 0; frozen 200-turn source; policy balanced; estimated bytes 46,808 |
| G3 | 1 warmup + 20 installed `handoff list --limit 100 --json` | 12.8 ms | 13.7 ms | 14.1 ms | macOS p95 `2s` | `PASS` | dedicated home; 100 `--no-launch` created=100; list n=100 every sample; source fingerprint unchanged |

G2 source: 400 events. Ceilings treated as absolute. No >25% regression vs this-run G3 p95.

## 6. Optional source-only matrix — 8 rows

| ID | Vendor present | Result | Sanitized evidence |
| -- | -------------- | ------ | ------------------ |
| E1 | `no` | `NOT TESTED` | Gemini absent |
| E2 | `no` | `NOT TESTED` | Gemini absent |
| E3 | `no` | `NOT TESTED` | OpenCode absent; no isolation override |
| E4 | `no` | `NOT TESTED` | OpenCode absent; no isolation override |
| E5 | `yes` | `FAIL` | Grok 1.0.3 + tagged basic fixture from leaf-matching `demo` checkout; `--to claude --dry-run --json` exit **5** `environment preflight is blocked`. Inspected workspace `/Users/fixture-user/code/demo`. Did **not** exit 1 `unsupported agent "grok"` (RC2 busy-check). |
| E6 | `yes` | `FAIL` | Same preflight block toward Codex. |
| E7 | `yes` | `PASS` | `--to grok` exit **2** usage |
| E8 | `yes` | `PASS` | `resume` grok id exit **5**: `Grok Build sessions are source-only in Phase 4` |

## 7. Architecture §14 closeout

| Definition-of-done assertion | Result | Evidence |
| ---------------------------- | ------ | -------- |
| All 27 packets and their tests are present in the tagged commit | `PASS` | WP-01–WP-27 named in tagged plans; `rein handoff` present |
| `make verify` is green on both mandatory platforms | `FAIL` | macOS `make verify` FAIL at `govulncheck` (stdlib `go1.25.12`); Windows not in this report |
| Claude → Codex and Codex → Claude work with source closed and no source API call | `FAIL` | A1–A2 dest-ack missing |
| Every fidelity class has real report evidence and byte-stable goldens | `PASS` | B6 compaction fixture; goldens in `make verify` |
| Injection, secret, and bounded-read security gates leak nothing | `PASS` | D1–D4 counts; `make verify` adversarial tests |
| 200-turn projection is bounded and reported | `PASS` | B2 + G1 |
| Windows ↔ macOS uses canonical project IDs without source absolute paths | `PASS` | C5 |
| Dry-run and executed structured-plan output are byte-identical | `PASS` | B1 dry vs no-launch after acks (real launch route not compared; A1 failed dest-ack) |
| Required Phase 4 docs, product-truth register, runbook, and dispatch are present | `PASS` | tagged tree: dispatch, template, `docs/handoff.md` |
| Exit codes remain 0/1/2/3/5/6/7; no vendor-internal writes or capsule sync | `PASS` | observed 0/1/2/3/5/6/7 only; D7–D8 |

## 8. Findings and repository hygiene

### Release-blocking

1. Flagship A1/A2/A3/A5/A6/A7 did not collect a destination first-reply acknowledgement on a real TTY (throwaway Claude not logged in). Empty-dest **Plan** succeeds (RC3). Missing dest-ack is FAIL, not NOT TESTED.
2. C8/R6: a PATH `claude --version` of `2.1.230` does not fail closed. Installed dry-run exit 0 with warning `agent.version` and lineage residue. `doctor` still reports Claude `SUPPORTED` on layout.
3. Installed Grok-sourced dry-run (E5/E6) exits 5 `environment preflight is blocked` from a leaf-matching git checkout. Busy-check no longer returns exit 1 `unsupported agent "grok"`. Optional installed-vendor failure is a candidate blocker.
4. R4: hanging `claude --version` (grandchild `sleep`) returned exit **1** `runtime` / `environment preflight is blocked` in ~2091 ms, not Compatibility `UNTESTED` exit 5.
5. `make verify` fails `govulncheck` on five Go 1.25.12 standard-library advisories already fixed in 1.25.13.

### Non-blocking

- Host Claude is `2.1.228`, not `2.1.229`. Range includes 2.1.229; 2.1.230 did not fail closed (blocking, above).
- OpenCode warning `agent_not_installed` on `sessions --json` is expected.
- Live bootstrap PATH-update was reverted; subsequent product use used `INSTALL_DIR` only.

### Test-harness deviations and supersessions

- Isolated Claude print-mode session failed closed (not logged in); no operator `.claude` tree was read.
- `make verify` under parallel package load classified dest Codex `UNTESTED`; recorded gate used `GOFLAGS=-p=1` without leaked vendor homes. Isolated `TestHandoffFromRealisticClaudeInstall` PASS (0.27s).
- F4/F8 first collection used `--json` on a launch attempt and hit F3 exit 2 (11–12 ms). Targeted recheck without `--json` on the same artifact superseded those FAILs: F8 exit 7 in 10.5–11.5 ms; F4 unacked launch exit 7.
- C7 first collection planted the skill under unix-HOME `.agents` (Codex dest also saw it). Targeted recheck used throwaway Claude `skills/review` only; warning `handoff.capability.skill.review` present.
- R2 first scan counted operator-home hits inside planned-file store paths; targeted recheck of capsule/projection/bootstrap: `${REPO:`=22, `${EXTERNAL:`=8, operator-home hits=0.
- Cross-build used `GOTOOLCHAIN=go1.25.12`; goreleaser snapshot not re-run.
- No rc.1–rc.5 corpus reused.

Record the report-only diff, privacy scan result, merge-base with `TEST_COMMIT`,
branch tip, and every superseded row. Never amend or force-push evidence already
shared with the coordinator.

## RC1 regression re-verification (R1–R6)

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| R1 | `FAIL` | Throwaway Claude dir has no `version` file. In-range dry-run exit 0, not UNTESTED. Off-PATH source (dest Codex still on PATH) `--dry-run --json` exit **5** `environment preflight is blocked`. |
| R2 | `PASS` | Absolute-paths fixture `--no-launch` exit 0; `${REPO}` tokens=22 and `${EXTERNAL:}` tokens=8 in capsule/projection/bootstrap; operator-home path count 0; no absolute-path abort. |
| R3 | `PASS` | Dirty tree `changed_files` nonempty (count=4); not `(none)`. |
| R4 | `FAIL` | Hanging `--version` shim classified `runtime` exit **1** preflight-blocked, wall 2091 ms; not Compatibility `UNTESTED` exit 5. |
| R5 | `PASS` | Slash fixture first user `/init`; `--no-launch --json` exit 0; `/init` reached stdout; no absolute-path abort. |
| R6 | `FAIL` | Installed Claude `2.1.228` handoff exit 0 (SUPPORTED). Fake `2.1.230` dry-run exit 0 with `agent.version` warning (not fail-closed). Host did not auto-update (before=after `2.1.228` / `0.147.0`). |

## RC2 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| C4 | `PASS` | Wrong-repo / different-leaf cwd exit 5 `different repository`. Leaf-match remap exit 0. |
| F8 | `PASS` | Non-TTY fail-closed exit 7 in 11.5 ms before dest spawn. |
| E5/E6 busy-check | `PASS` | No exit 1 `unsupported agent "grok"`. Rows themselves `FAIL` on preflight block. |
| R4 classification | `FAIL` | Timed-out/hanging probe is Runtime preflight, not Compatibility `UNTESTED`. |
| D9 | `PASS` | `--no-redact` known; Grok refused exit 2. |
| List isolation | `PASS` | Isolated `rein list` hid decoy HOME session; 11 test-owned sessions. |
| B3 | `PASS` | Checkpoint `projection_events=0`. |
| B6 | `PASS` | All five fidelity classes on compaction fixture. |
| C6/C7 | `PASS` | Missing MCP/skill warning IDs. |

## RC3 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| Flagship A empty dest | `PASS` | Empty dest `CODEX_HOME` (no `sessions/`); dest Plan `--dry-run --json` exit 0; dest not `UNTESTED`. Dest-ack still missing (harness; see A1). |
| R4 hang | `FAIL` | See R4; 2091 ms; exit 1 runtime. |
| Windows `make verify` | `N/A` | Owned by the Windows device report. macOS unit/race PASS; `govulncheck` FAIL. |
| G3 `--no-launch` list | `PASS` | created=100; `handoff list --limit 100 --json` n=100; unacked launch still exit 7. |
| B6 summarized | `PASS` | Compaction `fidelity.json` includes `summarized`; not rewritten to `referenced`. |

## RC5 / RC6 regression re-verification

| ID | Result | Sanitized evidence |
| -- | ------ | ------------------ |
| R4 hang process-tree | `FAIL` | Grandchild `/bin/sleep` `--version` did not classify `UNTESTED` exit 5 inside 25s as Compatibility. |
| F8 refuse-before-Plan / before-index | `PASS` | Exit 7 in 10.5–11.5 ms; `--allow-warning` did not skip. |
| B6 summarized class | `PASS` | Compaction fixture. |
| C5 foreign-OS os-roots remap | `PASS` | Windows fixture remapped onto leaf-matching checkout; `${REPO:` present; no `C:\`. |
| E5/E6 fixture-user cwd | `FAIL` | Grok still exit 5 preflight-blocked from a `demo` git checkout. |
| C4 leaf-match remap | `PASS` | Leaf `demo` remaps; different leaf refuses exit 5. |

## 9. Required terminated device block

Required counts must sum to 44 and optional counts to 8. This block occurs
exactly once and remains the last report content until reconciliation.

```text
PHASE4-DEVICE-REPORT-V1
device=macos-arm64
test_tag=v0.4.0-rc.6
test_commit=00366cb53f4246bde50c7f6fe96f7c1e1660ec88
installed_binary_sha256=79ce08180e841e73041056be95316599682b67ecd874fbc7f62f982392c02953
required_pass=37
required_partial=0
required_fail=7
required_not_tested=0
optional_pass=2
optional_fail=2
optional_not_tested=4
artifact_chain=PASS
isolation_privacy=PASS
flagship_directions=FAIL
fidelity_policy=PASS
workspace_capability=FAIL
security=PASS
cli_contract=PASS
performance=PASS
phase1_phase2_phase3_regression=PASS
release_blocking_findings=5
product_files_changed=0
secrets_transcripts_or_capsules_committed=false
stable_v0.4.0_authorized=false
current_stable=v0.3.0
END-PHASE4-DEVICE-REPORT-V1
```
