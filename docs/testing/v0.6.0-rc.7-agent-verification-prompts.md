# v0.6.0-rc.7 tagged-artifact acceptance prompts

Use these prompts only after the signed `v0.6.0-rc.7` GitHub prerelease is
published and both live installer routes pin that exact tag. They specialize
the release-neutral
[`v0.6.0` native Windows acceptance contract](v0.6.0-windows-acceptance.md),
which itself composes the
[Phase 5 acceptance contract](phase-5-universal-agent-coverage-acceptance.md),
the [`v0.5.2` CLI experience contract](v0.5.2-cli-experience-acceptance.md),
and hosted ticket #16 into one Windows column, under the
[Windows-first waiver](../../RELEASING.md#v060-windows-first-waiver) in
[ADR 0005](../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md).

`v0.6.0-rc.7` is a corrective candidate. `v0.6.0-rc.6` was published and its
own tagged-artifact acceptance
([`docs/testing/results/2026-09-08-windows-v060rc6.md`](results/2026-09-08-windows-v060rc6.md))
ended device verdict `FAIL` — **211 PASS / 1 PARTIAL / 0 FAIL / 3 NOT
TESTED** of **215** required rows — **zero product defects.** Both
required-row gaps that candidate's own two targeted fixes addressed
(`grok:E1`–`E3`, `MatrixH:H7`) cleared to `PASS`. Two new, genuinely
blocking gaps surfaced, neither present at `v0.6.0-rc.5`; `v0.6.0-rc.7`
addresses the version half of them.

- **`codex:E1`, `codex:E2`, `codex:E3`** (host/harness — version drift,
  `NOT TESTED (version drift)`). The acceptance host's live, PATH-first
  Codex CLI self-updated from the verified `0.149.0` ceiling to `0.153.4`
  mid-cycle. A real, non-`--dry-run` resume against it correctly refused
  with exit `5` (`agent.version`, outside the then-verified range) — the
  documented "read the refusal correctly" case, not a defect. Following
  Q27's standing policy, the verified range widens to `0.153.4` on
  read-only native Windows evidence plus a real before/after `--dry-run`
  launch-plan build against a committed fixture (Q27, standing self-update
  policy; see
  [`docs/testing/results/2026-09-08-windows-range-widening-codex-v060.md`](results/2026-09-08-windows-range-widening-codex-v060.md)).
  Version-output parsing and every launch-plan-relevant `--help` flag
  (`resume`, `fork`, the positional prompt argument) are unchanged from
  `0.149.0`.
- **`MatrixG:G1`'s Codex half** (host/account — usage-limit exhaustion, not
  fixed by this candidate). The acceptance host's live, authenticated
  Codex account returned a usage-limit refusal (`codex exec` prints
  "You've hit your usage limit ... try again at 10:13 AM"), reproduced on 3
  independent attempts against both the drifted `0.153.4` build and the
  in-range `0.146.0` build. This is an account constraint, not a version or
  harness issue, and the range widening above does not fix it — see the
  "Codex account usage-limit rule" below.

Because the host's Codex account usage limit blocks any completed Codex
conversational turn until `10:13` local on `2026-09-08`, the widening
record's own evidence is read-only; the completed-turn `codex:E1`–`E3`
rows against `0.153.4` are collected for the first time by **this**
candidate's own tagged run, once the limit has reset.

Publication means ready for physical acceptance; it does not mean acceptance
passed. Current stable remains `v0.5.1`. This candidate does not authorize
stable `v0.6.0`.

One mandatory device run starts this candidate's tagged-artifact acceptance:
native Windows x64 (`windows/amd64`, never WSL). Apple Silicon macOS
acceptance is **deferred** under ADR 0005 (see the "Deferred" section below)
— nothing in this candidate's evidence claims macOS.

The device must report all rows: **178** generated by
`rein doctor --agents --acceptance-matrix --json` on the tagged binary,
built from this tree (section B, unchanged from `v0.6.0-rc.6` — widening
`Min`/`Max` on an already-registered agent changes no tier, family, or
agent count), **22** interactive CLI rows (section C), and **16** Hop
parity journey rows (section D) — **216** rows in total, of which
`opencode:D4` is `N/A (definitional)` (see
[`v0.6.0-windows-acceptance.md`](v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict)):
**215** required rows. Record each count in the report header. A report whose
Phase 5 count disagrees with the candidate catalog is invalid. `PARTIAL` and
`NOT TESTED` do not pass a required row, except a `NOT TESTED (host
credential)` row for an optional agent under the same disposition rule.

## Fixed release identity and evidence boundary

- `TEST_TAG` is exactly `v0.6.0-rc.7`; `TEST_COMMIT` is its peeled literal
  40-character commit and must be an ancestor of `origin/main`. **`TEST_COMMIT`
  is filled in once the signed tag exists** — it is not yet known while this
  candidate is only a reviewed release commit on `release/v0.6.0-rc.7`.
- Require an annotated tag with a valid signature against
  `.github/allowed_signers` and a published non-draft prerelease tied to it.
- Install from the live bootstrap into a new directory after proving the
  bootstrap pins exactly `v0.6.0-rc.7`. Never replace a user binary.
- Require `rein` and `reinstate` to have identical bytes and to report that
  tag and full commit (`rein version --json`). Report both the tag and the
  full commit in the device report header.
- Rebuild the local session index. Do not replay a stale index without
  exercising the reader's own upgrade path.
- **Host environment.** Every shell that runs `rein`, the Hop lab, or a Go
  test must `unset REINSTATE_BACKEND REINSTATE_MEMORY_BACKEND_DIR` first;
  `rein` honours `REINSTATE_BACKEND` before it reads the profile, so a
  contaminated shell silently uses a local store and every Hop row reports
  nothing to the control plane. The report must state that every shell did
  this. Do **not** unset the host's `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, or
  `XDG_DATA_HOME`: they are its live agent homes, and a Claude Code run
  spawned without `CLAUDE_CONFIG_DIR` falls back to a stale `~/.claude` that
  cannot refresh its sign-in. Isolate a row by pointing the vendor's home
  variable at your own directory. This rule is unchanged from `v0.6.0-rc.6`.
- **Always invoke the tagged binary by its full path**, never bare `rein`
  or `reinstate` — a PATH-resolved binary can silently answer from an
  older installed release. Put the tagged binary's directory first on
  `PATH` only as a convenience for the maintainer's own console commands
  below, and confirm `rein version --json` reports the exact tag before
  relying on it.
- **ConPTY console rule.** Every row that needs a live, running vendor
  process (`E1`–`E3`-shaped rows, `codex:E1`–`E3` and `grok:E1`–`E3`
  included) launches that process in a real Windows pseudo console via
  [`scripts/testing/conptydriver`](../../scripts/testing/conptydriver) or
  the maintainer's own interactive terminal — never headless, and never a
  bare, unattached process the harness cannot observe completing.
- No real transcripts, prompts, credentials, or private paths in the report.
  A maintainer console transcript (Codex, and Grok if the executor cannot
  drive it) is the one exception this dispatch asks for by design: record it
  in full, verbatim, and redact nothing from it, but do not embellish or
  summarize over it either.

## What this candidate specifically re-tests

**The `codex:E1`–`E3` version-drift gap and `MatrixG:G1`'s Codex half the
`v0.6.0-rc.6` run found.**

Both require a real, completed conversational turn against the live `codex`
binary, and both are blocked by the same host constraint: the acceptance
host's live, authenticated Codex account returns a usage-limit refusal
until `10:13` local on `2026-09-08`. The version-drift half of the gap is
fixed by this candidate's widened range (`0.133.0`–`0.153.4`); the
account-limit half is not fixed by anything in this repository and must be
waited out.

### Codex account usage-limit rule

The host's Codex account usage limit resets at `10:13` local time on
`2026-09-08`. Run every other row in the assigned part **first** — every
row that does not need a live Codex conversational turn, in any order that
finishes them. Attempt `codex:E1`, `codex:E2`, `codex:E3`, and
`MatrixG:G1`'s Codex half only after everything else in the part is done.

- If a Codex turn (`codex exec`, a real `rein resume codex:<id>`, or a real
  `rein fork codex:<id>`) returns the usage-limit error (`codex exec`
  prints "You've hit your usage limit ... try again at 10:13 AM", or the
  equivalent from a resumed/forked session), **wait until `10:15` local**
  (two minutes past the documented reset, to absorb clock skew) and
  **retry**. Repeat the wait-and-retry cycle for **up to 60 minutes** of
  total waiting past the first usage-limit error.
- **Record the exact error line and the exact times** for every attempt:
  the timestamp the command was run, the verbatim error text returned, and
  the timestamp of the next retry. A report that records only "hit the
  usage limit, waited, then passed" without the exact lines and times is
  incomplete evidence.
- If the limit has not cleared after 60 minutes of waiting, record
  `codex:E1`–`E3` and `MatrixG:G1`'s Codex half `NOT TESTED (host
  credential)` with the full wait-and-retry log as evidence, and note in
  the report that the account-limit constraint outlived the wait window.
- Do **not** attempt to route around the limit (a different, unauthenticated
  Codex invocation, a synthetic response, or skipping the real turn) — the
  row's mechanism is the real, completed conversational turn, and routing
  around the account is a false pass.

### Executing `codex:E1`–`E3` and `MatrixG:G1`'s Codex half

Once the account limit has cleared (immediately, or after a wait-and-retry
cycle above), run these rows exactly as the Phase 5 and Matrix G contracts
specify, against the real, installed `0.153.4` binary, through
`conptydriver`:

1. Create a real Codex session and plant a recall token in its own first
   turn (`codex:E1`'s resume half).
2. Confirm Reinstate indexed it (`rein search <token> --json`), then
   preview and run the real resume (`rein resume codex:<id> --dry-run
   --json`, then `rein resume codex:<id>`), and confirm the resumed
   session recalls the token verbatim.
3. Preview and run a real fork (`codex:E2`), confirming the forked session
   also recalls the token at a distinct session id.
4. Confirm the catalog reflects both sessions (`codex:E3`,
   `rein sessions --agent codex --json`).
5. `MatrixG:G1`'s Codex half: the Codex leg of the cross-agent structured
   handoff journey, end to end, against the real, in-range binary.

**`MatrixH:H7` under the refined live-home rule, again.**

Run the fresh-Hop-account round trip exactly as the `v0.6.0-rc.6` dispatch
specified, scored against the same refined pass condition in "Lab isolation
for `MatrixH:H7`" below — a full per-file before/after listing of the
session-bearing subtrees with per-entry attribution, not aggregate digest
equality. This row cleared `PASS` at `v0.6.0-rc.6`; nothing in this
candidate touches the daemon's own mechanism, so a regression is a new
finding, not an expected one. `#424` (the daemon reading the live roots
under the ambient login environment) is recorded, not failed, on its own,
since it remains scheduled for `v0.6.1`.

**`grok:E1`–`E3`, run again.**

These rows cleared `PASS` at `v0.6.0-rc.6` via the maintainer's own console
transcript against `1.0.13`; nothing in this candidate touches Grok. Run
them again against this tag's own tagged binary, either path:

- **Executor-driven, if it can be:** attempt the same three-step
  resume/recall, fork/recall, catalog sequence through `conptydriver`
  first. If the live `grok` backend completes a real conversational turn
  from the harness this time, record that transcript as the evidence and
  note that `F-GROK-BACKEND-CONNECTIVITY` did not reproduce.
- **Maintainer console, if it cannot:** if the harness-driven attempt does
  not complete a turn (the same failure mode `v0.6.0-rc.5` and its
  rechecks found), fall back to the maintainer running the sequence
  themselves at their own console, exactly as in "Maintainer console
  evidence for `grok:E1`–`E3`" below, and record their pasted transcript
  verbatim.

A regression on either path (the token not recalled, a command erroring, or
neither path completing a turn) is a new finding, not an expected one —
`grok:E1`–`E3` are not this candidate's own targeted fix, but they must
still be exercised, not merely assumed carried.

### Maintainer console evidence for `grok:E1`–`E3`

Hand the maintainer this exact sequence, run at their own console with the
tagged binary on `PATH` by full path (confirm `rein version --json` first),
if the executor-driven attempt above does not complete a turn:

1. Start a real Grok Build session and plant a recall token in its own
   first turn, so its own reply is unambiguous:
   ```
   grok -p "Remember this exact token for later: RC7-GROK-<random>. Reply with just OK."
   ```
   Let it answer, then exit that session normally.
2. Confirm Reinstate indexed it:
   ```
   rein search RC7-GROK-<random> --json
   ```
   Confirm the matching `grok` session id appears with `prompt_preview`
   populated.
3. Preview the resume launch plan (no process started):
   ```
   rein resume grok:<id> --dry-run --json
   ```
   Confirm the JSON reports the correct launch command shape and
   `agent.version` inside the `1.0.5`–`1.0.13` range.
4. Run the **real** resume and ask Grok to recall the token, to prove the
   resumed process is genuinely continuing the original session's own
   history, not a fresh one that merely echoes the prompt:
   ```
   rein resume grok:<id>
   ```
   Inside the resumed session: `What was the exact token I asked you to
   remember?` Confirm the reply names the same `RC7-GROK-<random>` token
   verbatim. Exit normally.
5. Preview a fork (no process started):
   ```
   rein fork grok:<id> --dry-run --json
   ```
   Confirm the JSON reports a distinct new session id and the correct
   `--session-id`-shaped launch command.
6. Run the **real** fork and ask the same recall question again inside the
   forked session, confirming it also returns the same token (the fork
   started from the same history, at a new session id):
   ```
   rein fork grok:<id>
   ```
   `What was the exact token I asked you to remember?` — confirm the same
   token comes back. Exit normally.
7. Confirm the catalog reflects both sessions:
   ```
   rein sessions --agent grok --json
   ```
   Confirm both the original and forked session ids are present, with the
   forked one's `forked_from` (or equivalent field) naming the original.

The maintainer pastes the full terminal transcript (every command and its
complete output, from step 1 through step 7) back to the executor. The
executor records that transcript verbatim as the evidence for `grok:E1`
(resume/recall), `grok:E2` (fork/recall), and `grok:E3` (catalog reflects
both) — redacting nothing from it, and never adding to it or paraphrasing
over it.

**Everything else `v0.6.0-rc.6`'s tagged run left blocking is unchanged —
nothing else in this candidate touches it:**

- **`opencode:D4`** — remains `N/A (definitional)`: OpenCode's SQLite-only
  store has no JSONL record boundary the row's mechanism can apply to,
  regardless of any fix. Record the same reason; excluded from the required
  count.

**Every row that cleared at `v0.6.0-rc.6` and is not listed above is
expected `PASS` again, unchanged: `MatrixB:B4`, `MatrixB:B7`, `MatrixG:G4`
(and `MatrixG:G1`'s Claude half), CLI row `13`, `opencode:E5`, `opencode:E6`,
`pi:C3`, every `qwen` row `C1`–`E6`, `codex:E4`, `codex:E5`, `codex:E6`,
`opencode:C1`–`C6`/`D1`–`D3`/`E1`–`E4`.** A `FAIL` on any of those is a new
finding, not a carried disposition.

**Every Hop parity row (section D, other than `H7`, which is re-run above)
and every CLI experience row (section C) passed at `v0.6.0-rc.6` and
nothing in this candidate touches Hop, the daemon's own mechanism, or the
interactive surfaces.** Run the full 22 + 16 rows anyway — this is the
first time they run against a `v0.6.0-rc.7` tagged artifact — but a
regression in any of them is a new finding, not an expected one.

**The rest of the 178-row Phase 5 matrix, in full**, exactly as the rc.6
dispatch required, including the widened Claude Code (`2.1.219`–`2.1.263`),
Grok Build (`1.0.5`–`1.0.13`), OpenCode (`1.18.21`–`1.18.29`), and Qwen
Code (`0.21.12`–`0.23.0`) ranges, none of which moves in this candidate;
re-check the Claude Code ceiling immediately before this run — it
auto-updates.

**The host contamination rule**, above — confirm `REINSTATE_BACKEND` and
`REINSTATE_MEMORY_BACKEND_DIR` are unset in the shell that runs each row,
not only in the report's preamble.

## Lab isolation for `MatrixH:H7`

Unchanged from `v0.6.0-rc.6`. Refined 2026-09-08 after three tagged-artifact
exercises of the corrected fresh-Hop-account rule on `v0.6.0-rc.5` (the run
itself, plus two same-artifact rechecks) all reached, or nearly reached, the
daemon mechanism, and none produced a clean `PASS` under aggregate
byte-for-byte digest equality — see
[`v0.6.0-windows-acceptance.md`](v0.6.0-windows-acceptance.md#run-notes)
and
[ADR 0005 Amendment 2](../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md#amendment-2-2026-09-08)
for the full rationale. `v0.6.0-rc.6`'s own tagged run produced the first
clean `PASS` under this rule.

- **"Fresh Hop lab account"** means a new **Hop** account created through
  the lab's `hopd` (`hoplab pair init`, or the `rein account init` flow
  against the lab control plane) that has had **zero sessions pushed to
  it** — not a new Windows OS user account. Unchanged from `v0.6.0-rc.6`.
- **(a) Full per-file listing, before and after.** Before the round trip,
  and again after it, list every file (relative path, size, mtime) under
  exactly the **session-bearing subtrees**, not the whole agent-home
  roots: `<CLAUDE_CONFIG_DIR>/projects`, `<CODEX_HOME>/sessions`, and
  `<XDG_DATA_HOME>/opencode`, excluding `*-wal`, `*-shm`, and `*.lock`
  files. Read `CODEX_HOME` as the **persistent, user-level** value the
  scheduled task will actually see (a `daemon status` call's reported
  `roots`, or the registry) — never the executing shell's own transient
  `CODEX_HOME`, which the `v0.6.0-rc.5` second recheck showed can silently
  differ from it and invalidate the comparison.
- **(b) `PASS` condition.** The row is `PASS` only if all three hold: the
  daemon's pull restored **zero** snapshots; **no file** under those three
  subtrees was created or modified carrying a lab/synthetic name or a name
  matching the lab account's own session ids; and **every** differing
  entry between the before- and after-listings is attributable, by
  relative-path pattern, to a process other than `rein` (backup rotation,
  another session's own transcript or `file-history` writes, WAL/journal
  churn, and the like). Any entry that cannot be so attributed keeps the
  row at `PARTIAL` or worse — the burden is on the attribution, not on
  digest equality.
- **(c) Reporting.** The report lists every differing entry between the
  before- and after-listings by relative path, with its attribution, even
  when the overall disposition is `PASS` — a clean row still shows its
  work.
- **(d) `#424` itself.** The daemon reading the live roots and pushing
  into the run's disposable locker is **recorded, not failed**, on its
  own: `#424` is a documented design gap, scheduled to be fixed separately
  for `v0.6.1`, not a fresh finding each time `H7` runs until it lands.
- `H7` still needs the maintainer at the keyboard for the one UAC prompt
  the elevation raises — this refinement does not remove that requirement
  and must not attempt to route around it (see "Harness traps" below).
  Start the wait only **after every other row in the executor's part is
  already done**, not in parallel with them: poll
  `D:/ReinstateAcceptanceProjects/h7-go.txt` every **60 seconds**, for **up
  to 150 minutes**, and do not attempt the elevation until that file exists
  and its content confirms the maintainer is present to accept the prompt.
  If the file never appears within that full wait window, record `PARTIAL`
  (operator/harness availability) with that reason — a shorter poll window
  is not a basis for `PARTIAL`.

This lab-isolation method is a harness clarification only; it does not
change `rein daemon install`'s own behavior (that is `#424`'s own fix,
scheduled for `v0.6.1`, not attempted by this candidate) and does not
change what `MatrixH:H7`'s row scores on beyond the refined pass condition
itself.

## Dispositions carried from the `v0.6.0-rc.6` tagged run

The `v0.6.0-rc.6` tagged run
([full report](results/2026-09-08-windows-v060rc6.md), Verdict block, §16,
§18, §20) carried these non-`PASS` required rows. Record each as `PASS`, or
as the same disposition with the same reason; a new reason is a new
finding.

| Row | `v0.6.0-rc.6` tagged result | This candidate expects |
| --- | ---------------------------- | ----------------------- |
| `MatrixG:G1` (Codex half) | PARTIAL, host/account — the acceptance host's live, authenticated Codex account was usage-limit exhausted, reproduced on 3 independent attempts against both the drifted default CLI and a second in-range CLI (Claude half fully `PASS`) | Not fixed by this candidate's own range widening — expect `PASS` once the account's usage limit resets and the "Codex account usage-limit rule" above is followed, else the same `PARTIAL`/`NOT TESTED (host credential)` disposition with the exact wait-and-retry log |
| `codex:E1` | NOT TESTED (version drift) — the live, installed Codex CLI (`0.153.4`) was past the then-verified ceiling (`0.149.0`); `rein` correctly refused to build a live launch plan (exit `5`, range named) | Fixed via the widened `0.133.0`–`0.153.4` range — expect `PASS` on a real, completed conversational turn once the account's usage limit resets |
| `codex:E2` | NOT TESTED (version drift), same reason | Fixed via the widened range — expect `PASS` on a real, completed conversational turn once the account's usage limit resets |
| `codex:E3` | NOT TESTED (version drift), same reason | Fixed via the widened range — expect `PASS` on a real, completed conversational turn once the account's usage limit resets |

**Also carried, cleared at `v0.6.0-rc.6`, expected `PASS` again (not a
disposition, but named here so a report does not mistake a first-time
re-run for a fresh finding):** `grok:E1`, `grok:E2`, `grok:E3` (maintainer
console evidence or executor-driven, above), `MatrixH:H7` (refined
per-file-listing rule, above), `opencode:D4` (`N/A (definitional)`,
unchanged since `v0.6.0-rc.1`, excluded from the required count).

A row not on either list that fails is a new finding, not a carried
disposition.

## Census the candidate must hold

Unchanged from `v0.6.0-rc.6` — this candidate promotes no agent's tier.

| Tier | Keys |
| ---- | ---- |
| T5 | `claude`, `codex`, `opencode` |
| T4 | `grok`, `qwen` |
| T2 | `gemini`, `kimi` |
| T1 | `pi`, `cursor`, `copilot`, `cline` |
| T0 | `aider`, `amp`, `antigravity`, `minimax-code`, `openhands`, `roo`, `zcode` |

Do not promote Gemini to T3 on this candidate. Do not invent a reader for a
T0 agent. If a T1 agent is not installed, that is a scope reduction: drop
that agent to the highest tier this platform verified, in the candidate,
before any later stable. Tiers are never claimed on macOS evidence this
candidate does not have.

## What to run

1. **Section A** automated gates (A1–A8) on the tagged installed binary.
2. **Section B** — the Phase 5 generated matrix, all **178** rows, for every
   installed key per the Phase 5 contract. Run every row that does not need
   a live Codex conversational turn first, per the account usage-limit rule
   above.
3. **Section C** — the 22-row CLI matrix, Windows column, through the
   ConPTY driver (`scripts/testing/conptydriver`) against
   `scripts/tuisandbox`. Rows 2 and 22 compared byte-for-byte against a
   `v0.5.1` release binary. Row 21 (ASCII glyph fallback on legacy conhost)
   is required.
4. **Section D** — all 16 Hop parity rows against the local lab, two
   isolated device homes on this one host, real vendor binaries for every
   session a row requires. `MatrixH:H7` uses the refined per-file listing,
   go-signal method above. `MatrixG:G1`'s Codex half follows the account
   usage-limit rule above.
5. **`codex:E1`–`E3`** — attempt against the real installed `0.153.4`
   binary once the account's usage limit clears, following the
   wait-and-retry rule above.
6. **`grok:E1`–`E3`** — attempt through the harness first; fall back to the
   maintainer's own console sequence above if the harness attempt does not
   complete a turn.

A structured handoff starts a **new** destination session. It is not a
cross-agent resume. Report it that way.

## Reading a refusal correctly

Several refusals in this product are correct behaviour and must not be scored
as defects. A row fails only when the refusal is wrong, not when it fires.

- `working directory is a different repository than the source session` — run
  from the session's own workspace.
- `the recorded Git branch cannot be verified outside a Git repository` — the
  chosen session's workspace is not a repository; choose one that is.
- `source session is active; close it or pass --allow-active` — the operator is
  running that agent. For an embedded store, any write to the database counts.
- `native agent version X is outside the verified range` — the host's vendor CLI
  auto-updated past the ceiling. Pin the verified version or record the drift
  as `NOT TESTED (version drift)` per
  [`v0.6.0-windows-acceptance.md`](v0.6.0-windows-acceptance.md#dispositions-that-do-not-block-the-device-verdict).
- `You've hit your usage limit ... try again at <time>` — a real Codex account
  constraint, not a `rein` defect. Follow the "Codex account usage-limit
  rule" above: wait and retry within the window, and record it as
  `NOT TESTED (host credential)` only if the full window elapses without
  the limit clearing.
- `could not reach the Reinstate Hop control plane at <url>: <cause>` (H1c) —
  correct when the control plane genuinely does not resolve; the row passes on
  the message shape and the `control_plane_unreachable` `--json` kind, not on
  the control plane being reachable.

## Harness traps that produce a wrong verdict

Physical acceptance on this program has repeatedly found more harness defects
than product defects, and some have been **false passes**. Check the harness
before concluding the product is wrong.

- Read each agent's `root_env` from `rein doctor --agents --json` rather than
  assuming a variable name. Redirecting with a variable the product does not
  declare tests the operator's real root, not the copy.
- Put the candidate on `PATH` under **both** `rein` and `reinstate`, or an
  installed release answers instead and the comparison runs between two
  builds. Always invoke by full path where the contract requires it, above.
- A `--dry-run` capsule is a preview and carries no `raw_source` boundary.
  Only a harness that truncates a copied source can measure the fidelity rows
  that need it.
- Assert that a refusal **names a range**, not that it names a particular
  version. Hardcoded bounds stop testing the message the moment a ceiling
  moves.
- A Hop row that passes without exercising its mechanism is `FAIL`. Check the
  harness (the lab's env, the isolated home, `hopd.db`) before trusting a
  green result.
- Attribute a write before blaming it on a refresh. Any agent the operator is
  running writes to its own root continuously, and the audit may itself be
  running inside one.
- `codex:E1`–`E3` and `MatrixG:G1`'s Codex half specifically: do not treat a
  usage-limit error as a version or harness defect — it is an account
  constraint. Do not skip the wait-and-retry cycle and record `NOT TESTED`
  early; the full 60-minute window is the rule, not a suggestion. Do not
  accept a second, differently-authenticated Codex account as a substitute
  without recording that substitution explicitly — the row's evidence must
  say which account and which CLI build produced it.
- `grok:E1`–`E3` specifically: if the executor-driven attempt does not
  complete a turn, do not retry the harness path repeatedly hoping for a
  different result — fall back to the maintainer console path on the first
  clean failure. Do not accept a maintainer transcript that skips a step,
  does not show the exact recall question asked, or does not show the exact
  token returned — a transcript that merely asserts success without showing
  the command output is not evidence.
- `MatrixH:H7` specifically: "fresh Hop lab account" means a new **Hop**
  account with zero pushed sessions, not a new Windows OS user account —
  do not abandon the row over a missing Windows account elevation the rule
  does not actually require. Do not attempt an unattended elevation
  workaround (a scheduled task that self-elevates, a stored credential,
  etc.) to route around the missing operator — the row's mechanism is the
  real UAC prompt, and routing around it is a false pass. Score the row
  against the refined per-file listing with attribution, not aggregate
  digest equality. Start the go-signal poll only after every other row in
  the part is done, not before, and not in parallel.

## Required installs

Claude Code, OpenCode, and Grok Build at the versions this candidate
verifies (re-check immediately before the run; Claude Code auto-updates).
Codex CLI `0.153.4` is required for `codex:E1`–`E3`; do not downgrade it to
force the prior ceiling. A second, in-range Codex CLI install (`0.146.0` or
similar) remains useful for version-gated rows (`E4`, `E6`) that need an
in-range binary independent of the account's own usage limit. Optional,
never installed solely for a row: Gemini CLI, Kimi Code CLI, Copilot CLI,
Cursor CLI, Cline, Pi.

## Reports

- `docs/testing/results/<DATE>-windows-v060rc7-pretag.md` (pre-tag snapshot
  build; evidence that the candidate is ready for tagged-artifact acceptance,
  authorizes nothing past that)
- `docs/testing/results/<DATE>-windows-v060rc7.md` (tagged artifact; this is
  the run that can authorize stable `v0.6.0`)
- `docs/testing/results/<DATE>-windows-hop-parity-v060.md` (section D; may be
  shared by the pre-tag and tagged runs if re-run against the tagged binary
  and both identities are recorded)

Each report is committed on its own branch. `PARTIAL` and `NOT TESTED` do not
pass a required row; a missing required result is `FAIL`. Cumulative and
append-only: a `FAIL` that later passes keeps both entries and says what
changed.

Stable `v0.6.0` is authorized only when the tagged Windows report is `PASS`
with zero release-blocking findings, section E is recorded exactly as
`DEFERRED` (never as passed), and the stable promotion decision in
`RELEASING.md` names the deferred-macOS issue,
[#403](https://github.com/HarjjotSinghh/reinstate/issues/403).

## Deferred: Apple Silicon macOS

Every row below is **deferred**, not claimed, under
[ADR 0005](../adr/0005-v0.6.0-scope-and-windows-first-acceptance.md) D2. They
are run against this same `v0.6.0-rc.7` tag (or the stable tag it becomes)
once the Apple Silicon macOS host returns from repair. If that later macOS
run fails, the fix ships as `v0.6.1` — the record of this candidate's Windows
acceptance is never edited to hide it.

| Group | Rows | Contract |
| ----- | ---- | -------- |
| Phase 5 generated matrix, macOS column | all rows section B lists | Phase 5 |
| CLI experience, macOS column | rows 1–20 and 22 | `v0.5.2` |
| Hop cross-device journeys | pairing macOS↔Windows; path remap Windows↔macOS both directions; daemon launchd round trip; first push on macOS | hosted #16 |
| Range widening on macOS | Claude Code through `2.1.263`; Codex CLI through `0.153.4`; OpenCode through `1.18.29`; Qwen Code through `0.23.0`; Grok Build through `1.0.13` | ADR 0005 D3 |
| Grok GD8 | tool-approval prompt (already collected on macOS; the Windows half is #368 and stays optional) | Grok T4 |

No document, data file, or release note may state that `v0.6.0` was verified
on macOS until that run exists.
