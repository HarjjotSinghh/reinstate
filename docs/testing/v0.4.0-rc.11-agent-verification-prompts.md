# v0.4.0-rc.11 tagged-artifact acceptance prompts

Use these prompts only after the signed `v0.4.0-rc.11` GitHub prerelease is
published and both live installer routes pin that exact tag. They specialize
the release-neutral [Phase 4 acceptance contract](phase-4-cross-agent-handoff-acceptance.md)
and use the [Phase 4 report template](results/phase-4-report-template.md).

`v0.4.0-rc.11` carries the same Phase 4 surface as `v0.4.0-rc.10` — canonical
capsules, five source readers, Claude Code and Codex destinations, the
model-free handoff pipeline, local private lineage, the full CLI surface,
synthetic fixtures, adversarial tests, and documentation — plus dest first-reply
five-bullet argv/bootstrap, lineage-before-launch / list dir recovery, and
dest-home workspace trust (`internal/handoff/projection.go`,
`internal/handoff/pipeline.go`, `internal/handoff/desttrust.go`). Publication
means ready for tagged-artifact acceptance; it does not mean acceptance
passed. Current stable remains `v0.3.0`, and stable `v0.4.0` requires a later
promotion decision and fresh validation.

A structured handoff starts a **new** destination session that continues the
same task. It is not a cross-agent resume, not the same session, not lossless,
and not full context. Report it that way in every row and every summary.

## Why RC11 exists

**`v0.4.0-rc.10` was published and its dual-platform physical acceptance
FAILED** (macOS 41 PASS / 3 FAIL: A1, A3, A7; Windows 40 PASS / 4 FAIL: A1, A2,
A3, A5). Device report PRs #232 and #233 stay draft/unmerged.

What held: Windows dest argv one-line absolute `projection.md` (rc.9 CR/LF
fix); Mac A2/A5 Claude dest restated five bullets after harness workspace-trust
pre-accept; A4/A6 PASS both OS; RC10.argv PASS.

Remaining **PRODUCT** defects in those seven FAILs:

1. Dest first-reply did not restate all five acknowledgement bullets. Windows
   short argv was only "Acknowledge before any mutation." Full bootstrap still
   said "cannot police".
2. Mac A7: `lineage.jsonl` was written after Launch, so killing dest-ack left
   artifact dirs with empty `handoff list` (`handoffs=[]`; score
   `len(handoffs)`, there is no `n` field).
3. Windows A2 Claude dest stuck on "Do you trust this folder?"; Codex dest
   initially blocked on directory trust. Trust must be dest-home config, not
   dest argv `--dangerously-skip-permissions`.

`v0.4.0-rc.11` requires the five-bullet first-reply in the multiline bootstrap
and the Windows one-line argv, records lineage before dest Launch and recovers
artifact dirs in `handoff list`, and Materializes Claude/Codex workspace trust
in an explicit dest home (`CLAUDE_CONFIG_DIR` / `CODEX_HOME`) with Windows
Codex path aliases. Publication means ready for tagged-artifact acceptance; it
does not mean acceptance passed. Current stable remains `v0.3.0`, and this
candidate does not authorize stable `v0.4.0`.

The RC1 defects R1–R6 still must `PASS`. The RC2, RC3, RC5, RC6, RC7, RC8, RC9,
RC10, and RC11 regression sets below must also `PASS` on both devices. Do not
reuse any rc.1 through rc.10 corpus, home, capsule, lineage, or report row.

**Harness (mandatory, both prompts):** Dest-ack requires a real TTY (macOS
Terminal / `script`; Windows Terminal or real `ssh -t` so `IsTerminal` is true)
AND throwaway dest Claude/Codex **logged in**. Before the first A row, prove
dest login in the isolated homes (`loggedIn=true` / a throwaway dest prompt
succeeds). Materialize now records dest-home folder trust, so the harness
should not need a manual trust pre-accept. Missing dest-ack is `FAIL`. Do not
weaken F8. Do not use `05-destack.ps1`. Autonomous SSH without a TTY is not an
excuse to skip A1/A2/A5/A6/A7; collect them or `FAIL`. Host Claude auto-update
past `2.1.229` stops the run: pin PATH and re-collect.

Two mandatory device runs start independently and may run in parallel:

1. Apple Silicon macOS (`darwin/arm64`); and
2. native Windows x64 (`windows/amd64`, never WSL).

Each device must report all 44 required A–D, F, and G rows, plus an explicit
`PASS`/`FAIL` for each of the six RC1 regression items R1–R6, each RC2
regression item, and each RC3 regression item. The eight E rows are optional per installed vendor. Gemini CLI,
OpenCode, and Grok Build must not be installed solely for acceptance; their
absent rows are `NOT TESTED`. A missing required result is `FAIL`.

## Fixed release identity and evidence boundary

- `TEST_TAG` is exactly `v0.4.0-rc.11`; `TEST_COMMIT` is its peeled literal
  40-character commit and must be an ancestor of `origin/main`.
- Require an annotated tag with a valid SSH signature against
  `.github/allowed_signers` and a published non-draft prerelease tied to it.
- Before executing downloads, verify the release workflow's exact 25-asset set:
  `checksums.txt` plus five platform archives, five archive SBOMs, five raw
  binaries, eight Linux native packages, and one source archive. Verify every
  checksum, GitHub API digest, and attestation against the repository, tag,
  workflow, and full commit. Inspect safe archive membership without extraction.
- Prove the platform's live bootstrap is byte-identical to the file at
  `TEST_COMMIT`, contains one `v0.4.0-rc.11` pin, and pins the exact canonical
  installer digest. A surviving `v0.4.0-rc.1` through `v0.4.0-rc.10` pin anywhere on a live installer
  route is a release blocker. Install into a new directory; never replace a
  user binary.
- Require `rein` and `reinstate` to have identical bytes and to report version
  `0.4.0-rc.11` plus the literal full `TEST_COMMIT`. Stop before behavior rows on
  any provenance or identity failure. Source builds are supplemental only.

### Report-template substitutions for this candidate

The committed [Phase 4 report template](results/phase-4-report-template.md) is
still written with `v0.4.0-rc.1` literals. Use the template's structure exactly
and substitute these values; this dispatch governs where the two disagree.

| Template literal | Value for this candidate |
| ---------------- | ------------------------ |
| `v0.4.0-rc.1` (tag, pin, and version assertions) | `v0.4.0-rc.11` |
| `REPORT_DATE-macos-phase4-V040RC1.md` | `REPORT_DATE-macos-phase4-V040RC11.md` |
| `REPORT_DATE-windows-phase4-V040RC1.md` | `REPORT_DATE-windows-phase4-V040RC11.md` |
| `test/v0.4.0-rc.1-<platform>-report` | `test/v0.4.0-rc.11-<platform>-report` |
| `test_tag=v0.4.0-rc.1` | `test_tag=v0.4.0-rc.11` |
| `PHASE4-RC1-FINAL-RECONCILIATION-V1` | `PHASE4-RC11-FINAL-RECONCILIATION-V1` |
| `END-PHASE4-RC1-FINAL-RECONCILIATION-V1` | `END-PHASE4-RC11-FINAL-RECONCILIATION-V1` |
| `phase4_rc1_tagged_artifact_acceptance` | `phase4_rc11_tagged_artifact_acceptance` |

`PHASE4-DEVICE-REPORT-V1` and its terminator are version-neutral and unchanged.
Required counts still sum to 44 and optional counts to 8; the six R items and
the RC2 regression items are reported separately and are not folded into that
arithmetic. `stable_v0.4.0_authorized=false`
and `current_stable=v0.3.0` are fixed values regardless of the candidate verdict.

## Record installed agent versions before starting

Do this **before the first product command**, not at write-up time.

1. Record the exact installed version of Claude Code and Codex CLI, and the
   version or `ABSENT` for Gemini CLI, OpenCode, and Grok Build, into the
   report's section 1. A run that cannot state its source and destination agent
   versions has no interpretable Claude or Codex rows.
2. Require Claude Code within `2.1.219`–`2.1.229` inclusive and Codex CLI within
   `0.133.0`–`0.147.0` inclusive. An out-of-range primary agent stops the run:
   report it and stop, never widen a range inside the report PR.
3. If the vendor supports pinning or deferring auto-update, do it for the
   duration of the run and record that you did.
4. Re-record all agent versions immediately after the last row. **Claude Code
   auto-updated mid-run during rc.1**, on both hosts. Any version change
   invalidates every row collected across the change: record the before and
   after values, mark the affected rows invalid, and re-collect them on a
   single stable version. Silently continuing across a version change is a
   release-blocking process failure.

## Fixed isolation and controlled-session rules

1. Export **all five** vendor overrides before the first command, including for
   agents the run does not expect to touch. Each source falls back to its
   documented default when its variable is unset, so one omission silently
   reads the operator's real tree.

   | Vendor | Variable |
   | ------ | -------- |
   | Reinstate | `REINSTATE_HOME` |
   | Claude Code | `CLAUDE_CONFIG_DIR` |
   | Codex CLI | `CODEX_HOME` |
   | Gemini CLI | `GEMINI_CLI_HOME` |
   | Grok Build | `GROK_HOME` |

2. **OpenCode has no equivalent override.** It is read through
   `opencode session list`, which always targets the real store. Either skip
   OpenCode for the run and record its rows `NOT TESTED: no isolation override`,
   or record explicitly that OpenCode rows were collected **without** isolation.
   Never report an OpenCode row as isolated.
3. After the first index refresh, verify the session list contains **only**
   sessions the run itself created. Any unexpected session is an isolation
   failure: destroy the Reinstate home and restart, do not continue. Record the
   session count and the check result.
4. Use new absolute paths for `REINSTATE_HOME`, the install, private evidence,
   disposable Git repositories, temp files, and every vendor process home.
   Never point a command at the operator's real Claude, Codex, Gemini, OpenCode,
   or Grok tree.
5. Create the required Claude Code and Codex sessions through each vendor's own
   documented CLI in a disposable repository. A human may authenticate the
   throwaway home before the run; credentials and auth contents stay invisible
   to the acceptance agent and report.
6. Fully close the source CLI before every flagship handoff. Handoff execution
   may launch the destination CLI, but Reinstate itself must not create or edit
   a vendor session-store file. Unit tests use fake runners and no network or
   real vendor process.
7. Keep all raw output, timings, hashes, capsule bodies, and terminal recordings
   under the private evidence root. Commit only counts, hashes, timings, exit
   codes, mode labels, and paraphrased destination acknowledgements.
8. Never run `rein init`, configure a backend, sync a capsule, inspect a
   keychain or `.env`, or transfer a real transcript between devices. Use only
   fresh controlled sessions and the tagged synthetic fixtures. Do not reuse any
   rc.1, rc.2, rc.3, rc.4, rc.5, or rc.6 corpus, home, capsule, lineage store, or report row.

Before and after each relevant row, fingerprint the controlled source artifact
and vendor trees. A changed source hash, unexpected vendor write during a
launch-free row, access to an operator tree, or committed sensitive content is
a release blocker.

## RC1 regression re-verification (required, both devices)

Record an explicit `PASS`/`FAIL` and sanitized evidence for each. Any `FAIL`
here is release-blocking even when the corresponding A–G rows pass, because
these are the defects that failed rc.1.

### R1 — Claude source probes without a `version` file

Confirm the throwaway `CLAUDE_CONFIG_DIR` contains no file named `version` and
do not create one; a real installation has none. Then, on a real installed
Claude Code:

- `rein handoff claude:<id> --to codex --dry-run --json` must not exit `5` for
  any version reason, and must not report the source agent `UNTESTED`.
- `rein inspect` on the same session, in the same shell, must report the **same**
  version the handoff gate used. Two different answers for one agent in one
  invocation is a `FAIL` regardless of exit codes.
- Walk the shared contract on both Claude and Codex sources and record one row
  each: an unrecognized layout is `UNSUPPORTED`; a determinable in-range version
  is `SUPPORTED`; a determinable out-of-range version is `UNTESTED` and exits
  `5` with no artifact or lineage residue (this is C8's condition, recorded
  again here); a version that cannot be determined at all is `SUPPORTED` on
  layout alone. Produce the last case honestly — move the source executable off
  the curated `PATH` inside the throwaway environment — because a handoff must
  work when the source agent is gone.
- **R1 must PASS:** an off-PATH source with a recognized Claude layout is
  layout-only `SUPPORTED`. Score `rein inspect --json` `environment.agent.status`
  (or the equivalent `agent.status`): it must be `supported`, never
  `not_installed`, when `layout_recognized=true`. `executable_present` may be
  `false`. Dry-run handoff must exit `0`, not `5`. Inspect and handoff must
  agree. Destination launch with a missing executable must still fail closed.

### R2 — reader-emitted paths are tokenized

Create controlled Claude and Codex sources that actually read a file, so the
transcript carries tool-call inputs, tool results, and an attachment reference
with real absolute paths. Include one path outside every configured root.

- The handoff must complete. A failure with
  `capsule: absolute filesystem path is not allowed` is a `FAIL`.
- Privately scan the capsule, `projection.md`, the bootstrap, and every exported
  artifact for source-device absolute paths and for the operator's home
  directory name. Record zero/nonzero counts only; nonzero is a `FAIL`.
- Require `${REPO:<id>}` and `${HOME}` tokens for in-root paths and
  `${EXTERNAL:<digest>}/<basename>` for the out-of-root path. The external form
  must be a digest and a basename, never a reconstructable location.
- Prose is untouched: a sentence that merely names a path is carried as written.

### R3 — `changed_files` reflects the live working tree

Use a disposable repository with a deliberately dirty tree: at least one staged,
one unstaged, and one untracked file.

- The capsule must contain a non-empty `changed_files` key, `projection.md` must
  list those files, and the destination bootstrap must carry them.
  `Changed files: (none)` over a dirty tree is a release blocker.
- Every entry must be a `${REPO:<id>}/…` token. No absolute path may appear.
- Force both drop points — more than 64 changed paths for the probe cap, and a
  path set large enough to pressure the 8 KiB argv bootstrap budget — and
  require an explicit rendered count of omitted entries in each case. A silently
  short list is a `FAIL`.
- Confirm path lists stay out of the probe report's JSON: `verify`/`doctor`
  output is printed and pasted, and only the capsule holds paths.
- Confirm a transcript file claim is marked `evidence_conflicts_with_workspace`
  only when live Git produced a complete observation. An unavailable, uncertain,
  or capped observation must be reported as missing evidence, not as
  counter-evidence.

### R4 — a timed-out version probe is not an absent agent

- With an out-of-range source or destination agent, run the handoff gate while
  the host is deliberately loaded enough to make a two-second probe miss its
  budget (for example, with the full test suite running in parallel). Require
  exit `5`, not `0`. An accepted out-of-range agent under load is a `FAIL`.
- Confirm the retry policy: a timed-out probe is measured once more with a
  larger budget; a non-zero exit is not retried.
- Confirm an agent that is genuinely absent still resolves `SUPPORTED`, so the
  fix did not trade a rare fail-open for a rare fail-closed.
- Record this as production-reachable behavior, not a harness artifact.

### R5 — a message beginning with a slash is prose

Create a controlled source session whose first user message begins with `/init`,
whose history includes `/compact`, and whose prose names an absolute path.

- The handoff must complete. An abort with
  `capsule: absolute filesystem path is not allowed` is a `FAIL`.
- The message text must reach the projection exactly as written.
- Confirm the invariant still bites: a genuine path-typed violation (an absolute
  path planted in a path-typed field via the tagged fixtures) must still be
  refused, and the refusal must name the offending field.

### R6 — Claude ceiling through `2.1.229`

- Record the installed Claude Code version before the run and confirm `rein`
  calls it `SUPPORTED`. `2.1.228` and `2.1.229` must both be in range.
- Confirm `2.1.230` still fails closed as `UNTESTED`, so the range stayed
  inclusive without becoming open-ended.
- `2.1.229` has no physical evidence before this run; this run is what supplies
  it. If the host auto-updates past `2.1.229` mid-run, stop and report under the
  version-recording rule above.

## RC2 regression re-verification (required, both devices)

Record an explicit `PASS`/`FAIL` and sanitized evidence for each. Any `FAIL`
here is release-blocking even when the corresponding A–G rows pass, because
these are the defects that failed rc.2 physical dual-platform acceptance.
Do not reuse rc.1, rc.2, or rc.3 evidence for these rows.

### C4 — wrong-repo cwd

`rein handoff SOURCE --to DEST --dry-run --json` from a different git
repository than the source session must exit `5` with a different-repository
refusal. A same-repo subdirectory is allowed. A cwd that is not in a git
repository must not block.

### F8 — non-TTY destination launch

Without a real TTY, destination launch must refuse with
`ErrNonInteractiveLaunch` / exit `7` BEFORE LookPath-or-spawn delay (~seconds
of child start is `FAIL`). `--allow-warning` must not skip this.
`REINSTATE_ALLOW_NON_TTY_LAUNCH` is the only opt-out and must not be set for
the row.

### E5/E6 — Grok source busy-check

A Grok-sourced dry-run must not exit `1` with `unsupported agent "grok"`.
Destination `--to grok` remains usage exit `2`.

### R4 classification — timed-out probe is Compatibility, not Runtime

A timed-out version probe must classify `UNTESTED` and exit `5`
(Compatibility), never Runtime/absent. An off-PATH source with a recognized
layout must not block a read-only handoff (Info).

### D9 — `--no-redact` is a known flag

`--no-redact` is a known flag. Codex and Claude sources may use it. Grok
`--no-redact` is refused with exit `2` (not "unknown flag").

### List isolation

With `CLAUDE_CONFIG_DIR` / `CODEX_HOME` / `GEMINI_CLI_HOME` / `GROK_HOME` set,
`rein list` must not show operator-home sessions.

### B3 — checkpoint sidecar has no conversation bodies

`--policy checkpoint` sidecar/events must not contain verbatim conversation
bodies. JSON/human output includes `projection_events`.

### B6 — fidelity.json portability classes

`fidelity.json` includes omitted/normalized/summarized/referenced classes when
those portabilities exist (not only `user_messages` exact).

### C6/C7 — destination MCP/skill gaps

Removing a destination MCP/skill the source still has must produce Missing +
`handoff.capability.*` warning IDs. Execute without `--allow-warning` of those
IDs must not launch.

## RC3 regression re-verification (required, both devices)

Record an explicit `PASS`/`FAIL` and sanitized evidence for each. Any `FAIL`
here is release-blocking even when the corresponding A–G rows pass, because
these are the defects that failed rc.3 physical dual-platform acceptance.
Do not reuse rc.1, rc.2, or rc.3 evidence for these rows.

### Flagship A — empty dest home is plan-supported

The throwaway destination `CLAUDE_CONFIG_DIR` / `CODEX_HOME` may be an empty
explicit layout (no `projects/` or `sessions/` yet). Dest Plan must not exit
`5` with destination `UNTESTED` solely for that empty layout. Login and a real
TTY are still required for dest acknowledgement; missing dest-ack after a
successful Plan is harness, not this product row.

### R4 hang — hanging `--version` returns UNTESTED in time

A PATH shim whose `--version` sleeps without `exec` (grandchild holds pipes)
must classify `UNTESTED` and exit `5` within the 25s harness budget, not hang.
Record wall time.

### Windows `make verify`

On the native Windows host, `make verify` must pass. GnuWin32 `make` cannot
apply Unix `GOTOOLCHAIN=` prefixes: set `$env:GOTOOLCHAIN='go1.25.13'` first,
then run the PowerShell gates. If `make` hangs, run `go test` / `-race`
directly. `govulncheck` must be green on Go 1.25.13. The rc.3 failures were
`TestClaudeDefaultRootSupportedVersionFileDoesNotRequireExecutable`,
`TestCLISyntheticSyncPath`, and `TestDiffMissingManifestUsesAuthStorageExit`.

### G3 — `--no-launch` list is non-empty

After 100 `--no-launch` handoffs in the isolated home, `rein handoff list`
must report `n=100` (or the exact created count). Unacknowledged warnings must
not zero the list. Launch without acks must still refuse.

### B6 — summarized stays in fidelity.json

On a source whose transcript includes a vendor summary, `fidelity.json` must
include a `summarized` portability class. Rewriting that class to `referenced`
because the summary event missed the projection budget is a `FAIL`.

## RC6 regression re-verification (required, both devices)

Record an explicit `PASS`/`FAIL` and sanitized evidence for each. Any `FAIL`
here is release-blocking even when the corresponding A–G rows pass, because
these are the defects that failed rc.5 physical dual-platform acceptance.
Do not reuse rc.1–rc.5 evidence for these rows.

### R4 hang — Windows process tree returns in time

A PATH `.cmd` / shell shim whose `--version` sleeps in a grandchild (`ping`
or `/bin/sleep`, pipes held) must classify `UNTESTED` and exit `5` within the
25s harness budget. Record wall time. Classification without the budget is
`FAIL`.

### F8 — non-TTY refuse before Plan

Without a real TTY, destination launch must refuse exit `7` / interactive
terminal **before** `Plan`, `LookPath`, or version probes. Wall time must not
be spawn-scale. `--allow-warning` must not skip this.

### B6 — summarized class is present

Use a tagged Claude compaction fixture, a Codex `context_compacted` /
`summary` record, or plant a `{"type":"summary",...}` line in the throwaway
source. `fidelity.json` must include a `summarized` portability class. A live
session that never compacted is not this row.

### C5 — foreign-OS os-roots remap

From a local git checkout, `rein handoff` `--dry-run --json` against the
tagged Windows os-roots fixture (on macOS) or macOS os-roots fixture (on
Windows) must emit `${REPO:…}` and must not carry `C:\` or the source-device
absolute path in the capsule. A missing **same-OS** workspace still blocks
(C3). Exit `5` solely because the foreign fixture cwd does not exist locally
is `FAIL`.

### E5/E6 — fixture-user cwd is not a preflight block

A Grok tagged-fixture dry-run from a local git checkout must not exit `5`
solely because the recorded cwd is `/Users/fixture-user/…`. Destination
`--to grok` remains usage exit `2`.

### C4 — foreign remap must not bind a different repository

A `fixture-user` / foreign-OS recorded workspace whose last path component is
`demo` must refuse exit `5` / different-repository when cwd is a different git
repo (leaf not `demo`). Remap onto the local checkout only when the project
leaf matches.

### Dest-ack and previously uncollected rows

A1, A2, A5, A6, A7 dest-ack: collect a destination first-reply restatement on
a real TTY. Windows: `Windows Terminal` or `ssh -t`. Skipping is `FAIL`.
B4, B5, B8, C2, C3, D3, F3, F4, F7: run the row; missing collection is `FAIL`.


## RC7 regression re-verification (required, both devices)

`v0.4.0-rc.6` dual-platform physical acceptance FAILED. Score the product, not
the Windows cascade count. These rows are release-blocking on both devices.

### Busy-check listing error
Windows `Get-CimInstance Win32_Process` must not inherit the Plan deadline.
Bound listing (~5s), hide the console, fall back to `tasklist`. A listing
**error** is not evidence the source is busy: `--dry-run` / `--no-launch` /
`--allow-active` must still reach Plan (C4, C5, B, D, E, G2, G3). Fail if Plan
is skipped with runtime exit 1 `session busy check` after tens or hundreds of
seconds.

### C8/R6 — determined `2.1.230` is fail-closed
PATH `claude --version` = `2.1.230 (Claude Code)` on a read-only handoff must
exit 5 / `UNTESTED` without `--allow-untested`. A warning plus exit 0 with a
stored handoff id is FAIL.

### R4 — hung `--version` is Compatibility `UNTESTED`
A hanging `--version` shim must not classify Runtime exit 1 from leftover
capability cancel. Expect Compatibility exit 5 / `UNTESTED` inside the 25s
budget. Windows `go test` grandchild `--version` pipes must not run ~60s.

### E5/E6 — Grok source-only layout
If Grok is already installed (do not install it solely for acceptance), a
leaf-matching `demo` checkout must not block on missing native verified-resume
layout. Exit 5 `environment preflight is blocked` because `agent.layout` is
unrecognized is FAIL.

### Dest-ack and previously uncollected rows
Dest-ack A1/A2/A5/A6 still require a real console (`Windows Terminal` or
`ssh -t` so `IsTerminal` is true) and a logged-in throwaway destination.
Autonomous SSH without a TTY is not an excuse. Missing required dest-ack is
`FAIL`.


## RC8 regression re-verification (required, both devices)

`v0.4.0-rc.7` dual-platform physical acceptance FAILED (macOS 38/44, Windows
34/44; coordinators macOS `83b81f6f`, Windows `ce7eac00`). RC7 product rows
(busy-check, C8/R6 2.1.230, R4 hang, E5/E6) PASSED. Score remaining PRODUCT vs
HARNESS separately. Dest-ack A1–A7 and Windows C3/F2/F3/F7/E7/E8 are
HARNESS/UNCOLLECTED — not product — but a missing required result is still
`FAIL`.

### R1 — off-PATH Inspect layout scan

Off-PATH source with a recognized Claude layout: `rein inspect` must scan layout
and report layout-only `SUPPORTED`, never `StatusNotInstalled` from a `LookPath`
miss. Dry-run handoff must exit `0`, not `5`.

### Dest-ack A1–A7 (harness, not product)

Requires a real TTY (macOS Terminal / `script`; Windows Terminal or real
`ssh -t` so `IsTerminal` is true) AND throwaway dest Claude/Codex **logged in**.
Missing dest-ack is `FAIL`. Do not weaken F8.

### Windows C3 — isolate from C5

Missing-workspace fixture leaf must NOT be `demo` (that leaf is C4/C5). Score C3
on a same-OS missing workspace whose leaf is not `demo`.

### Windows F2 — no `--last` on `resume`

`resume` has no `--last`. Use `resume claude:<uuid> --with codex --dry-run --json`.

### Windows F3/E7 — score JSON usage

Score JSON usage (exit `2`). Never score `Start-Process` `ExitCode`.

### Windows F7 — overlapping native IDs

Overlapping native IDs across two agents → exit `6`, not `session not found`.

### Windows E8 — Grok resume syntax

Use `resume grok:<id>`, not `--last --from grok`.

### Windows make / Go 1.25.13

GnuWin32 `make` cannot apply Unix `GOTOOLCHAIN=` prefixes. Set
`$env:GOTOOLCHAIN='go1.25.13'` then run PowerShell gates. If `make` hangs, run
`go test` / `-race` directly. `govulncheck` must be green.


## RC9 regression re-verification (required, both devices)

`v0.4.0-rc.8` dual-platform physical acceptance FAILED (macOS 38/44, Windows
38/44; recon `0570c1f2`, coordinators macOS `0a738783`, Windows `f479d1e9`).
Off-PATH dry-run exit `0` and `layout_recognized=true` now PASS. Remaining
**PRODUCT:** Windows inspect JSON `agent.status=not_installed` despite recognized
layout. Dest-ack A1–A7 remain **HARNESS/UNCOLLECTED** (TTY real; dest not
logged in). Score PRODUCT vs HARNESS separately. Missing required dest-ack is
still `FAIL`.

### R1 — inspect JSON layout-only SUPPORTED

Off-PATH source with a recognized Claude layout: `rein inspect --json` must
report `environment.agent.status=supported` (never `not_installed`) with
`layout_recognized=true`. Dry-run must exit `0`. Destination launch without the
executable must still fail closed. Do not score R1 PASS from dry-run alone.

### Dest-ack A1–A7 (harness, not product)

Prove throwaway dest Claude/Codex **logged in** in the isolated homes *before*
the first A row, on a real TTY. rc.8 TTY collection was not sufficient. Missing
dest-ack is `FAIL`. Do not weaken F8.


## RC10 regression re-verification (required, both devices)

`v0.4.0-rc.9` dual-platform physical acceptance FAILED (macOS 38/44, Windows
38/44). Dest-ack A4 PASS; A1/A2/A3/A5/A6/A7 FAIL. Remaining **PRODUCT:** Windows
CreateProcess truncated a multi-line Codex dest argv at CR/LF, so dest only
received line 1 of the bootstrap (handoff `fbcc8249d79f3826c5d6bc69f3d35dfa`;
first reply was not the A5 restatement). Dest-ack remains **HARNESS** (logged-in
throwaway dest on Windows Terminal), not product. Score PRODUCT vs HARNESS
separately. Missing required dest-ack is still `FAIL`. Do not weaken F8. Do not
use `05-destack.ps1`. Use `--allow-warning` as needed.

### Dest argv — one-line projection.md pointer on Windows

On Windows, dest argv must be one line containing the absolute `projection.md`
path whenever the briefing contains CR/LF. Non-Windows may still pass the full
briefing in argv. Verify hashes still apply.

### Dest first-reply — five acknowledgement bullets

Dest first-reply must restate the five acknowledgement bullets from the
briefing, not only "structured handoff, not native resume".

### Dest-ack A1–A7 (harness, not product)

Prove throwaway dest Claude/Codex **logged in** in the isolated homes *before*
the first A row, on a real TTY (macOS Terminal / `script`; Windows Terminal or
real `ssh -t` so `IsTerminal` is true). Missing dest-ack is `FAIL`. Do not
weaken F8. Do not use `05-destack.ps1`.


## RC11 regression re-verification (required, both devices)

`v0.4.0-rc.10` dual-platform physical acceptance FAILED (macOS 41/44, Windows
40/44). Remaining **PRODUCT:** dest first-reply missed the five bullets; Mac A7
`handoff list` was empty after a killed dest-ack because lineage was written
after Launch; Windows A2 hung on Claude folder trust. Dest-ack remains
**HARNESS** (logged-in throwaway dest on a real TTY), not product. Score
PRODUCT vs HARNESS separately. Missing required dest-ack is still `FAIL`. Do
not weaken F8. Do not use `05-destack.ps1`. Use `--allow-warning` as needed.
Never pass dest argv `--dangerously-skip-permissions`.

### Dest first-reply — five bullets in argv and bootstrap

Windows dest argv must be one line: absolute `projection.md` **and** the
five-bullet first-reply one-liner, with no CR/LF. Full bootstrap must require
the destination's first reply to restate those five bullets before any
mutation, without the dest-facing "cannot police" hedge. Dest first-reply must
restate the five bullets, not only "structured handoff, not native resume".

### Lineage before launch / list dir recovery

After dest-ack — even if dest is killed — `handoff list --json` `handoffs`
length must be ≥ 1. Score `len(handoffs)`, not a `n` field. `lineage.jsonl`
must exist before Launch returns. Claude dest pin comes from lineage
`destination.session_id`. Do not dedupe G3 by `HandoffID` (100 lineage lines
with one directory is still 100 rows).

### Dest-home workspace trust

Materialize records Claude `hasTrustDialogAccepted` and Codex
`trust_level = "trusted"` in the isolated dest home when `CLAUDE_CONFIG_DIR` /
`CODEX_HOME` (or the adapter Root) is set. Dest-ack must not hang on the TUI
trust prompt. Never write default `~/.claude` or `~/.codex`. Windows Codex
project keys with backslashes must be literal-quoted, with slash-style and
lowercased aliases.

### Dest-ack A1–A7 (harness, not product)

Prove throwaway dest Claude/Codex **logged in** in the isolated homes *before*
the first A row, on a real TTY. A3 is source logged out, dest stays logged in;
collect live, do not skip. Missing dest-ack is `FAIL`. Do not weaken F8. Do not
use `05-destack.ps1`.

## Execution map for the Phase 4 matrices

Read the acceptance contract's pass condition for every ID; the instructions
below define how to collect it. Do not collapse IDs into one result.

### A — flagship quota-switch

- Create one benign task in each required source with a current goal, latest
  request, changed file, known test result, and next action. Include a harmless
  completed marker write so A6 can prove it is not repeated. Leave the working
  tree dirty so A5 and R3 exercise a real changed-file list.
- For A1 and A2, close the source process, remove its auth variables from the
  test shell, run the installed `rein handoff` into the other agent from a real
  TTY, and confirm no source process or source API dependency appears. For A3,
  repeat Claude → Codex from a logged-out throwaway source environment.
- For A4, use a fresh controlled session whose vendor CLI is terminated during
  its final response so the vendor naturally leaves an incomplete final record;
  do not hand-edit its store. Confirm the last complete user intent survives.
- For A5–A6, allow only the destination's requested acknowledgement. Paraphrase
  its restatement in the report and prove the harmless source marker did not
  change. A5 now includes the changed-file list: a destination that restates the
  repository as clean while the tree is dirty fails A5 and R3. Do not authorize
  additional destination actions.
- For A7, record sanitized `handoff list`/`inspect` evidence: `handoffs` length ≥ 1
  (score `len(handoffs)`, not a `n` field) even if dest was killed; Claude's pinned
  destination ID resolves; Codex is truthfully `resolved`, `unresolved`, or
  `ambiguous`.

### B — fidelity and policy

- Run the tagged `TestHandoffExecutedOutputMatchesDryRunByteForByte` test, then
  compare installed-artifact `--dry-run` and `--no-launch` structured-plan
  stdout and stderr as separate byte sequences for the same frozen source and
  flags. The real-launch route is independently required by A1–A2. Any mismatch
  fails B1; never normalize paths, whitespace, or line endings for comparison.
- Use the tagged 200-turn fixture for B2 and G1. Run checkpoint, balanced, and
  full policies on frozen controlled sources. Record projection size, token
  estimate, truncation count, and sidecar-reference count without recording
  content.
- Inspect sanitized fidelity metadata for all five classes, attachment metadata,
  and unknown opaque records. Each class and every omission/reference needs its
  reason; never paste the capsule or projection into the report.

### C — workspace and capability truth

- Use disposable clean, dirty, moved, missing, and wrong repositories with
  frozen Git identities. Compare reported branch, HEAD, and dirty state to Git;
  create a controlled transcript/Git contradiction and require live Git to win.
- Exercise the tagged macOS and Windows path fixtures plus a real local
  canonical project mapping. Require `${REPO:<id>}` portability and scan private
  artifacts to prove no source-device absolute path survived. C5 and R2 are
  collected from the same artifacts but recorded as separate results.
- In test-owned vendor homes only, create then remove bounded dummy MCP, skill,
  and instruction declarations. Require missing-capability reports before
  launch and verify reports contain no values or file contents.
- Exercise one out-of-range source or destination version in a throwaway home.
  Require exit `5` and no artifact or lineage residue.

### D — security

- Run all tagged `internal/handoff` adversarial tests and `make fixture-scan`.
  Exercise the installed CLI against the tagged injection, delimiter,
  source-instruction, secret, oversized, `absolute-paths`, and `slash-commands`
  fixtures through disposable roots.
- Privately search every produced artifact for planted synthetic secrets,
  source authority text, and unescaped delimiters. Record only zero/nonzero
  counts. `--show-redactions` may expose categories and counts only.
- Prove launch-free rows do not read credentials and do not change any vendor
  tree. For real destination launches, attribute new vendor files to the vendor
  process and require the source tree to remain byte-identical.
- Verify owner-only storage, capsule sync exclusion, source SHA-256 immutability,
  the Grok warning, forced Grok redaction, and `--no-redact` refusal with exit
  `2`. Grok evidence uses a tagged synthetic fixture when Grok is absent.
- D7 covers every vendor tree the five isolation variables point at, plus
  OpenCode storage. If OpenCode was collected un-isolated, say so on D7 rather
  than claiming an isolated result.

### E — optional source-only agents

If a vendor is already installed, create a fresh controlled source in its
throwaway process home and run both allowed destinations. Test rewinds,
OpenCode storage/fallback truth, forced Grok redaction/warning, and destination
refusals. If absent, record the applicable rows `NOT TESTED: vendor absent`.
Grok native continuation is refused for the documented read-only reason. Gemini
CLI, OpenCode, and Grok Build remain **source-only**; Claude Code and Codex are
the only destinations.

### F — CLI contract

- Exercise human and JSON output for `handoff`, `list`, `inspect`, `export`,
  `resume --with`, and picker `h NUMBER`. Require the mode label `structured
  handoff` and new-destination-session truth everywhere. Any surface that says
  "resume", "same session", "lossless", or "full context" for a cross-agent
  operation fails F1.
- Check `--json` gating; exact, missing, duplicate, wildcard, and stale warning
  acknowledgement; launch-free exact-command output; stored artifact byte
  reproduction; ambiguous native references; and non-TTY fail-closed behavior.
- Record only exit codes `0`, `1`, `2`, `3`, `5`, `6`, or `7`. Any other public
  code is a release blocker.

### G — fixed performance plan

Use a monotonic process-start-to-exit clock, bounded capture, a 30-second
per-sample timeout, one untimed warmup where specified, and nearest-rank p95.
Validate every exit and result. Keep raw samples private.

| ID | Exact corpus and command | Samples | macOS ceiling | Windows ceiling |
| -- | ------------------------ | ------- | ------------- | --------------- |
| G1 | `go test ./internal/handoff -run '^TestLongHistoryParseCapsuleProjectionUnderCeiling$' -count=1 -v`; tagged 200-turn/400-event fixture | 1 | `<2,000 ms`, projection `≤98,304 bytes` | same |
| G2 | Installed `rein handoff SOURCE --to DEST --policy balanced --dry-run --json` on the largest frozen controlled local session | 1 warmup + 5 timed | maximum `8s` | maximum `12s` |
| G3 | Installed `rein handoff list --limit 100 --json` with exactly 100 valid stored lineage rows | 1 warmup + 20 timed | p95 `2s` | p95 `4s` |

Prepare G3 in a dedicated fresh home by running the same safe, frozen controlled
source through installed `--no-launch --json` exactly 100 times. This creates
100 valid append-only lineage records without spawning a vendor or hand-editing
the store. Require `list --limit 100 --json` to return exactly 100 rows before
timing. Fingerprint the source before and after setup.

G1's 2-second and 98,304-byte limits are the shipped deterministic unit
contract and are unchanged for this candidate, even though the projection now
carries a changed-file list it previously omitted. G2 uses the established cold
local-command envelope; G3 uses the established warm process-start envelope. The
looser Windows limits preserve the existing physical-host allowance without
permitting a 20–30 second regression. Because the rc.1 device runs produced no
certified timings, there is no prior physical baseline to compare against; treat
the ceilings as absolute and record a same-host comparison only against another
row from this run. A timeout, invalid output, source mutation, unbounded growth,
or more than 25 percent comparable same-host p95 regression is blocking even
inside an absolute ceiling.

## Prompt 1 — Claude Code on Apple Silicon macOS

```text
Certify the exact signed Reinstate v0.4.0-rc.11 tagged artifacts on native Apple
Silicon macOS. You own the macOS evidence and final reconciliation.

Read AGENTS.md, docs/handoff.md,
docs/testing/phase-4-cross-agent-handoff-acceptance.md,
docs/testing/v0.4.0-rc.11-agent-verification-prompts.md (this dispatch), and
docs/testing/results/phase-4-report-template.md completely before acting. The
committed template still carries v0.4.0-rc.1 literals; apply this dispatch's
substitution table.

v0.4.0-rc.10 was published and FAILED this acceptance (macOS 41/44 A1/A3/A7,
Windows 40/44 A1/A2/A3/A5). Remaining PRODUCT: dest first-reply missed the five
bullets; Mac A7 list empty after killed dest-ack (lineage after Launch); Windows
A2 hung on Claude folder trust. Dest-ack remains HARNESS (real TTY + logged-in
throwaway dest), not product. Do not use 05-destack.ps1. Re-verify
R1-R6 PLUS the RC2, RC3, RC5, RC6, RC7, RC8, RC9, RC10, and RC11 regression IDs with explicit
PASS/FAIL: R1-R6 (Claude source probing without a
version file, reader path tokenization, live changed_files, timed-out version
probes classified UNTESTED, slash-prefixed prose accepted, and the Claude
ceiling through 2.1.229), RC2 C4, F8, E5/E6, R4 classification, D9, list
isolation, B3, B6, C6/C7, RC3 flagship A empty dest, R4 hang, Windows make
verify, G3, B6, RC5 R4 process-tree, F8 refuse-before-Plan, B6 summarized, C5
foreign-OS remap, and RC6 C4 leaf-match remap plus F8 refuse-before-index, plus
RC8 R1 layout scan, RC9 inspect JSON status=supported, RC10 dest argv one-line
projection.md, and RC11 five-bullet first-reply / lineage-before-launch /
dest-home trust. Any of those failures is release-blocking. Do not reuse rc.1
through rc.10 artifacts.

Before the first product command, record the installed Claude Code and Codex CLI
versions and the version or ABSENT for Gemini, OpenCode, and Grok. Require
Claude Code in 2.1.219-2.1.229 and Codex CLI in 0.133.0-0.147.0. Pin or defer
vendor auto-update if possible. Re-record every version after the last row:
Claude Code auto-updated mid-run during rc.1, and any change invalidates the
rows collected across it.

Set TEST_TAG exactly to v0.4.0-rc.11. Fetch tags without merging; verify the
annotated SSH-signed tag, peel its literal full TEST_COMMIT, require main
ancestry, verify the published prerelease's complete 25-asset provenance, and
install only through the live install.sh after proving its bytes and pin. A
surviving v0.4.0-rc.1 through v0.4.0-rc.10 pin on a live installer route is a release
blocker. Stop before behavior rows on any chain-of-custody failure.

Export all five vendor isolation variables before the first command:
REINSTATE_HOME, CLAUDE_CONFIG_DIR, CODEX_HOME, GEMINI_CLI_HOME, and GROK_HOME.
OpenCode has no override: skip it and record NOT TESTED, or record explicitly
that its rows were collected without isolation; never report them as isolated.
After the first index refresh, verify the session list contains only sessions
this run created, and destroy the home and restart if it does not. Use fresh
isolated INSTALL_DIR, evidence root, repositories, and process homes. Never read
the operator's real vendor trees or credentials. Create fresh controlled Claude
and Codex sessions through their own CLIs. Fully close the source before every
flagship row. Never write a vendor-internal session file, execute transcript
content, call the source model on the handoff path, sync a capsule, reuse any
rc.1, rc.2, rc.3, rc.4, rc.5, rc.6, rc.7, rc.8, or rc.10 artifact, or commit sensitive evidence.

Run the complete automated gates with Go 1.25.13 (govulncheck must be green) and
all 44 required Phase 4 rows using this dispatch's execution map, plus R1-R6 and
the RC2, RC3, RC5, RC6, RC7, RC8, RC9, RC10, and RC11 regression IDs. Run optional E rows only for
already-installed vendors; Gemini, OpenCode, and Grok are source-only and Claude
Code and Codex are the only destinations. Execute G1-G3 exactly with the macOS
ceilings. Dest-ack requires a real TTY AND throwaway dest Claude/Codex logged in. Prove login before A rows. Missing dest-ack is FAIL. Do not weaken F8. Do not use 05-destack.ps1. Use --allow-warning as needed. Collect
the destination acknowledgement only, and leave further destination actions
unauthorized. R1 inspect JSON agent.status must be supported when layout_recognized=true; dry-run exit 0 not 5. Prove dest logged in before A rows. Missing required evidence is FAIL, never NOT TESTED.
Describe every cross-agent result as a new destination session continuing the
same task: never "resume", "same session", "lossless", or "full context".

Create test/v0.4.0-rc.11-macos-arm64-report from TEST_COMMIT. Commit only
docs/testing/results/REPORT_DATE-macos-phase4-V040RC11.md using the template.
End it with exactly one PHASE4-DEVICE-REPORT-V1 block carrying
test_tag=v0.4.0-rc.11. Never amend or force-push shared evidence; keep the report
PR draft and unmerged. Do not edit product files, do not modify any existing
file under docs/testing/results/, and do not merge, tag, release, deploy, or
claim stable.

Pause after the immutable macOS report commit until the complete Windows report
commit is supplied. Then independently verify its ancestry, report-only diff,
count arithmetic, privacy, original failures, timings, agent-version records,
and supersessions. Add one PHASE4-RC11-FINAL-RECONCILIATION-V1 block in an
additive macOS report commit, with phase4_rc11_tagged_artifact_acceptance set
from the evidence. Set stable_v0.4.0_authorized=false and current_stable=v0.3.0
regardless of the candidate verdict.

Return the device verdict/counts, TEST_COMMIT, installed binary identity,
recorded before/after agent versions, R1-R6 and RC2/RC3/RC6/RC7/RC8/RC9/RC10/RC11 regression results, report
branch/commit/draft PR, sanitized failed commands, G1-G3 aggregates,
optional-vendor disposition, OpenCode isolation disposition, and every
release-blocking finding.
```

## Prompt 2 — Codex on native Windows x64

```text
Certify the exact signed Reinstate v0.4.0-rc.11 tagged artifacts on native
Windows x64 in a native 64-bit PowerShell process, never WSL. You independently
own the Windows evidence.

Read AGENTS.md, docs/handoff.md,
docs/testing/phase-4-cross-agent-handoff-acceptance.md,
docs/testing/v0.4.0-rc.11-agent-verification-prompts.md (this dispatch),
docs/testing/windows-acceptance-host.md, and
docs/testing/results/phase-4-report-template.md completely before acting. The
committed template still carries v0.4.0-rc.1 literals; apply this dispatch's
substitution table.

v0.4.0-rc.10 was published and FAILED this acceptance (macOS 41/44 A1/A3/A7,
Windows 40/44 A1/A2/A3/A5). Remaining PRODUCT: dest first-reply missed the five
bullets; Mac A7 list empty after killed dest-ack (lineage after Launch); Windows
A2 hung on Claude folder trust. Dest-ack remains HARNESS (real TTY + logged-in
throwaway dest), not product. Do not use 05-destack.ps1. Re-verify
R1-R6 PLUS the RC2, RC3, RC5, RC6, RC7, RC8, RC9, RC10, and RC11 regression IDs with explicit
PASS/FAIL: R1-R6 (Claude source probing without a
version file, reader path tokenization, live changed_files, timed-out version
probes classified UNTESTED, slash-prefixed prose accepted, and the Claude
ceiling through 2.1.229), RC2 C4, F8, E5/E6, R4 classification, D9, list
isolation, B3, B6, C6/C7, RC3 flagship A empty dest, R4 hang, Windows make
verify, G3, B6, RC5 R4 process-tree, F8 refuse-before-Plan, B6 summarized, C5
foreign-OS remap, and RC6 C4 leaf-match remap plus F8 refuse-before-index, plus
RC8 R1 layout scan, RC9 inspect JSON status=supported, RC10 dest argv one-line
projection.md, and RC11 five-bullet first-reply / lineage-before-launch /
dest-home trust. Any of those failures is release-blocking. Do not reuse rc.1
through rc.10 artifacts. R6
matters most here: the Windows host reached 2.1.229 during the rc.1 window and
would have refused every Claude-sourced row under the older ceiling.

Before the first product command, record the installed Claude Code and Codex CLI
versions and the version or ABSENT for Gemini, OpenCode, and Grok. Require
Claude Code in 2.1.219-2.1.229 and Codex CLI in 0.133.0-0.147.0. Pin or defer
vendor auto-update if possible. Re-record every version after the last row:
Claude Code auto-updated mid-run during rc.1, and any change invalidates the
rows collected across it.

Provision the host exactly as windows-acceptance-host.md requires before the
product matrix. Set TEST_TAG exactly to v0.4.0-rc.11. Fetch tags without merging;
verify the annotated SSH-signed tag, literal full TEST_COMMIT and main ancestry,
the published prerelease's complete 25-asset provenance, and the live
install.ps1 bytes, digest, and pin. A surviving v0.4.0-rc.1 through v0.4.0-rc.10 pin
on a live installer route is a release blocker. Install into a new directory
with REINSTATE_BOOTSTRAP_PATH_SCOPE=Process. Stop before behavior rows on any
chain-of-custody or native-process failure.

Export all five vendor isolation variables before the first command:
REINSTATE_HOME, CLAUDE_CONFIG_DIR, CODEX_HOME, GEMINI_CLI_HOME, and GROK_HOME.
OpenCode has no override: skip it and record NOT TESTED, or record explicitly
that its rows were collected without isolation; never report them as isolated.
After the first index refresh, verify the session list contains only sessions
this run created, and destroy the home and restart if it does not. Use fresh
isolated INSTALL_DIR, evidence root, native-Windows repositories, and process
homes. Never read the operator's real vendor trees or credentials. Create fresh
controlled Claude and Codex sessions through their own CLIs. Fully close the
source before every flagship row. Never write a vendor-internal session file,
execute transcript content, call the source model on the handoff path, sync a
capsule, reuse any rc.1, rc.2, rc.3, rc.4, rc.5, rc.6, rc.7, rc.8, or rc.10 artifact, or commit sensitive evidence.

Run the complete automated gates with Go 1.25.13, including govulncheck green
and the PowerShell-native snapshot, staging, artifact, and installer scripts.
GnuWin32 make cannot apply Unix GOTOOLCHAIN= prefixes: set
$env:GOTOOLCHAIN='go1.25.13' then run PowerShell gates; if make hangs, run go
test / -race directly. Run all 44 required Phase 4 rows using this dispatch's
execution map, plus R1-R6 and the RC2, RC3, RC5, RC6, RC7, RC8, RC9, RC10, and RC11 regression IDs,
and optional E rows only for already-installed vendors; Gemini, OpenCode, and
Grok are source-only and Claude Code and Codex are the only destinations.
Execute G1-G3 exactly with the Windows ceilings and ordinary antivirus
protections enabled. Dest-ack requires Windows Terminal or real ssh -t so
IsTerminal is true, AND throwaway dest Claude/Codex logged in. Windows dest argv
must be one line containing absolute projection.md; dest first-reply must restate
the five acknowledgement bullets from the briefing. Do not use 05-destack.ps1.
Use --allow-warning as needed. Missing dest-ack
is FAIL. Do not weaken F8. Windows C3 fixture leaf must not be demo; F2 is
resume claude:<uuid> --with codex --dry-run --json (no --last); F3/E7 score JSON
usage exit 2 never Start-Process ExitCode; F7 overlapping native IDs exit 6 not
session not found; E8 is resume grok:<id> not --last --from grok. R1 off-PATH
Inspect must be layout-only SUPPORTED, dry-run exit 0 not 5. Describe every cross-agent
result as a new destination session continuing the same task: never "resume",
"same session", "lossless", or "full context".

Create test/v0.4.0-rc.11-windows-amd64-report from TEST_COMMIT. Commit only
docs/testing/results/REPORT_DATE-windows-phase4-V040RC11.md using the template.
End it with exactly one PHASE4-DEVICE-REPORT-V1 block carrying
test_tag=v0.4.0-rc.11. Never amend or force-push shared evidence; keep the report
PR draft and unmerged. Do not edit product files, do not modify any existing
file under docs/testing/results/, and do not merge, tag, release, deploy,
transfer a real corpus, or claim stable. Return the immutable report commit to
the macOS coordinator.

Return the device verdict/counts, TEST_COMMIT, installed binary identity,
recorded before/after agent versions, R1-R6 and RC2/RC3/RC6/RC7/RC8/RC9/RC10/RC11 regression results, report
branch/commit/draft PR, sanitized failed commands, G1-G3 aggregates,
optional-vendor disposition, OpenCode isolation disposition, and every
release-blocking finding.
```

## Handoff and reconciliation sequence

1. Publish the signed candidate and pin both live installers before either run.
   Confirm no live route still pins `v0.4.0-rc.1` through `v0.4.0-rc.9`.
2. Start both prompts independently at the same `TEST_COMMIT`; each agent owns
   only its report path and private local evidence.
3. Each report branch starts at `TEST_COMMIT` and receives additive,
   report-only commits. No amend, force-push, product edit, corpus transfer, or
   edit to an existing file under `docs/testing/results/`.
4. Give the complete Windows report commit to the existing macOS coordinator.
5. The coordinator fetches only the report ref and independently checks
   ancestry, diff scope, counts, privacy, failures, supersessions, timings, and
   the before/after agent-version records.
6. `phase4_rc11_tagged_artifact_acceptance=PASS` requires both devices to have
   44 required PASS, six R items PASS, RC2/RC3/RC6/RC7/RC8/RC9/RC10/RC11 regression items PASS, zero required PARTIAL/FAIL/NOT TESTED,
   zero release blockers, valid report hygiene, and every §14 closeout assertion
   PASS.
7. Keep both report PRs draft and unmerged pending a maintainer decision. A
   candidate PASS does not authorize stable `v0.4.0`; current stable remains
   `v0.3.0`.
