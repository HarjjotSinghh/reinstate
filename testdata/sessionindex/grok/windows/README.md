# Grok Build synthetic sessions (Windows)

Root: `%USERPROFILE%\.grok` (override with `GROK_HOME`).

Workspace key for `C:\Users\fixture-user\code\demo` is URL-encoded:

`C%3A%5CUsers%5Cfixture-user%5Ccode%5Cdemo`

Same directory schema as macOS under `sessions/<encoded-cwd>/<uuid>/`.
