// Package adapter defines the interface every agent adapter implements.
// Concrete adapters (claude, codex) live in subpackages.
package adapter

// This file intentionally keeps package docs. Types live in types.go.
