{
  "posted": false,
  "report": {
    "checked_objects": [
      "the index",
      "the newest snapshot in the index"
    ],
    "client_version": "reinstate <version>",
    "generated_at": "2026-08-23T12:10:00Z",
    "locker": {
      "bucket": "memory:<home>/cache/memory-backend",
      "prefix": "team/a"
    },
    "outcome": "pass",
    "steps": [
      {
        "detail": [
          "snapshots/<snapshot-id>.age"
        ],
        "did": "Asked the storage endpoint for every object under this account's prefix (following every listing page), signed with the credentials this device uses to push.",
        "id": "list",
        "name": "List the locker with this device's credentials",
        "observed": "2 object(s): manifest.age (the encrypted index); 1 snapshot(s) under snapshots/ named by opaque ids.",
        "status": "pass"
      },
      {
        "detail": [
          "manifest.age first line: age-encryption.org/v1",
          "snapshots/<snapshot-id>.age first line: age-encryption.org/v1"
        ],
        "did": "Downloaded the index object and, when the index names one, the snapshot it records as updated last, and looked at the raw bytes for the age encryption header and for any field name that appears in the plaintext.",
        "id": "ciphertext",
        "name": "Fetch an object and check it is ciphertext",
        "observed": "manifest.age (<n> bytes): begins with the age v1 header (recipient scrypt (passphrase)); no plaintext field name appears anywhere in the body. snapshots/<snapshot-id>.age (<n> bytes): begins with the age v1 header (recipient scrypt (passphrase)); no plaintext field name appears anywhere in the body.",
        "status": "pass"
      },
      {
        "detail": [
          "index revision <snapshot-id>, 1 session(s) (claude 1)",
          "index entry claude:session-locker -> snapshots/<snapshot-id>.age (updated <time>)",
          "snapshots/<snapshot-id>.age: agent claude, session session-locker, project local/locker, created <time> on <platform>, 1 file(s), payload 2048 bytes, file projects/[REDACTED_PATH]/session-locker.jsonl"
        ],
        "did": "Opened the same bytes on this device with the key held here (the root key for Hop, the passphrase for BYO storage) and read what they contain. Nothing was sent anywhere.",
        "id": "decrypt",
        "name": "Decrypt the object locally",
        "observed": "manifest.age decrypted into a schema v1 index. snapshots/<snapshot-id>.age decrypted into a snapshot envelope whose payload sha256 matches the envelope.",
        "status": "pass"
      },
      {
        "did": "Nothing. This profile stores to a bucket you configured yourself, so there is no control plane to name a reference locker and no operator-owned bucket to offer these credentials to. No request was made.",
        "id": "isolation",
        "name": "Prove this account's credentials are refused from another bucket",
        "observed": "Not applicable: BYO storage has no control plane and no reference locker. Whether your credentials reach other buckets is decided by your own bucket policy.",
        "status": "not-applicable"
      }
    ],
    "storage": "byo",
    "summary": "PASS. The objects checked (the index and the newest snapshot in the index) are ciphertext this device can open. No other object is in the locker. Whether the credentials reach other buckets was not checked (step 4 above says why), so nothing is claimed about that.",
    "unopened": "No other object is in the locker.",
    "version": 1
  }
}
